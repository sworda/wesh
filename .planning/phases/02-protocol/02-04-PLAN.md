---
phase: 02-protocol
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - internal/server/server.go
  - internal/server/export_test.go
  - internal/server/keepalive_test.go
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
autonomous: true
requirements:
  - CORE-06

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "握手升档完成后服务端按可配间隔向客户端发 WS ping（控制帧 op=0x9），浏览器自动回 pong——保活流量刷新反代空闲计时（CORE-06；ping 间隔默认 5s，对 nginx 60s / Cloudflare 100s / 30s 型 ingress 均显著小，D-16）"
    - "--ping-interval flag 以秒为单位配置间隔（默认 5；0=禁用，pinger 直接返回不起 ticker，D-16——TestPingDisabled 证明 0 时线上零 ping 帧）"
    - "发出 ping 后 10s 内未收到匹配 pong，连接被关闭（pong 超时经 context.WithTimeout(10s) 传入 c.Ping，超时由库内 AfterFunc 自动 c.close() 收口——沿既有 reader 终结路径零新终结分支；pongTimeout 为包级私有变量恒 10s，经 export_test.go 的 SetPongTimeout 钩子注入，不违反 D-10/D-16）"
    - "pinger goroutine 生命周期随连接：Welcome 升档序列末尾启动（Resize→Welcome→16KiB→停计时器→release→pinger），ctx 随 Attach defer cancel 终结，禁止 r.Context()；c.Ping 与并发 Reader 的库语义前提由本 Attach 常阻塞读循环天然满足（conn.go:216-220 VERIFIED）"
    - "保活不误伤健康连接：正常 RTT 毫秒级，10s pong 超时极宽；读路径不传 deadline ctx（Pitfall 2 纪律沿用）"
    - "plan 02-01~02-03 全部既有测试在 pinger 上线后保持绿（默认 5s 间隔下秒级测试窗内 ping 帧不改变任何断言面）"
  artifacts:
    - path: "internal/server/server.go"
      provides: "pinger goroutine + Options.PingInterval + pongTimeout 私有变量"
      contains: "pinger / pongTimeout / PingInterval"
    - path: "cmd/wesh/main.go"
      provides: "--ping-interval flag（秒，默认 5，0=禁用）"
      contains: "--ping-interval"
    - path: "cmd/wesh/main_test.go"
      provides: "parseArgs 新增 --ping-interval 表驱动用例"
      contains: "ping-interval"
    - path: "internal/server/keepalive_test.go"
      provides: "ping 间隔到达与 pong 超时两 e2e（裸帧）"
      contains: "TestPingKeepalive / TestPongTimeout"
  key_links:
    - from: "internal/server/server.go"
      to: "github.com/coder/websocket（库 Ping 语义）"
      via: "c.Ping(ctx) 发自增计数器载荷 ping 并阻塞等匹配 pong；须与 Reader 并发（conn.go:216-259 VERIFIED）——本 Attach 读循环常阻塞天然满足"
      pattern: "c\\.Ping\\(|pinger\\("
    - from: "cmd/wesh/main.go"
      to: "internal/server/server.go"
      via: "cfg.pingInterval（秒）→ time.Duration → server.Options.PingInterval 注入"
      pattern: "PingInterval|ping-interval"
    - from: "internal/server/keepalive_test.go"
      to: "internal/server/rawws_test.go"
      via: "ping 帧（op=0x9）观测与 pong 帧（op=0xA）回复经裸帧 helper（库客户端自动回 pong 无法构造超时负例）"
      pattern: "readRawFrame\\(|writeRawFrame\\("
---

<objective>
落地 CORE-06 ping/pong 保活：Welcome 升档后启动 pinger goroutine 按 --ping-interval（默认 5s，0=禁用）发 WS 原生 ping，pong 超时 10s 常量；超时经库内路径收口连接，零新终结分支。

Purpose: 反代空闲超时（nginx 60s / Cloudflare 100s / 30s 型 ingress）会切断静默长连接——保活是远程终端生产可用的必要条件；WS 原生 ping/pong 是代理公认的空闲刷新流量。
Output: pinger + --ping-interval flag + pong 超时 + 两 e2e，`go test ./...` 全绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-protocol/02-CONTEXT.md
@.planning/phases/02-protocol/02-RESEARCH.md
@.planning/phases/02-protocol/02-PATTERNS.md
@.planning/phases/02-protocol/02-VALIDATION.md
@.planning/phases/02-protocol/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pinger goroutine + --ping-interval flag + TestPingKeepalive</name>
  <reversibility rating="one-way">D-16 --ping-interval flag 名是公开 CLI 契约——用户已锁定，仅记录不重问（默认值 5s 为 reversible，此处评级针对 flag 名）。</reversibility>
  <files>internal/server/server.go, cmd/wesh/main.go, cmd/wesh/main_test.go, internal/server/keepalive_test.go</files>
  <read_first>
    - internal/server/server.go（被改文件现状——plan 02-03 产物：Options、升档序列、readCounted 数据面、eventf）
    - cmd/wesh/main.go（被改文件现状：config、parseArgs、server.New 调用、http.Server）
    - cmd/wesh/main_test.go（被改文件现状：TestParseArgs 表驱动）
    - .planning/phases/02-protocol/02-RESEARCH.md（Pattern 4 pinger 行 319-349 逐段落定；Pitfall 2 ctx 纪律行 440-445；Open Questions Q2 行 611-614）
    - .planning/phases/02-protocol/02-PATTERNS.md（§internal/server/server.go ping 段行 245-264；§Shared Patterns ctx 纪律段）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-16）
    - internal/server/rawws_test.go（readRawFrame/writeRawFrame 签名——plan 02-02 产物）
    - internal/server/e2e_test.go（startTestServerOpts 签名——plan 02-01 产物）
  </read_first>
  <action>
    1. `internal/server/server.go`：`Options` 结构体加 `PingInterval time.Duration` 字段；包级私有变量 `pongTimeout = 10 * time.Second`（D-16 常量、生产恒 10s）。`internal/server/export_test.go` 增量加钩子（package server 内测文件，plan 02-02 已建）：`SetPongTimeout(d time.Duration) (restore func())`——外部测试包 server_test 经钩子注入短超时免等真 10s（RESEARCH Open Questions Q2 裁决形态，不违反 D-10/D-16"常量不开 CLI flag"：可配性指用户面）。新增 `func (s *Server) pinger(ctx context.Context, c *websocket.Conn, interval time.Duration)`（照 RESEARCH Pattern 4 行 327-346 逐段落定）：`interval <= 0` 直接 return（--ping-interval=0 禁用）；`time.NewTicker(interval)` + defer Stop；select 循环 `case <-ctx.Done(): return` / `case <-t.C:`；每 tick `pctx, cancel := context.WithTimeout(ctx, pongTimeout)` + `c.Ping(pctx)` + cancel；`err != nil` return（连接已死/对端无响应：终结由 reader 路径收口，零新终结分支）。**装配**：升档序列末尾（release 半开名额之后）`go s.pinger(ctx, c, s.opts.PingInterval)`——ctx 为 Attach 的 context.Background() 派生 ctx 并加 `ctx, cancel := context.WithCancel(...)` + `defer cancel()`（Attach 返回即终结 pinger；禁止 r.Context()，hijack 后行为意外）。写并发安全由库保证（Ping→writeControl 与 onChunk 的 c.Write 共用写互斥）。
    2. `cmd/wesh/main.go`：config 加 `pingInterval int`（秒——CLI 面单位秒，内部乘 time.Second 转 Duration）；parseArgs 注册 `fs.IntVar(&cfg.pingInterval, "ping-interval", 5, "WS ping interval in seconds (0 = disable)")`（D-16）；run 内 New 调用改为 `server.Options{Writable: cfg.writable, PingInterval: time.Duration(cfg.pingInterval) * time.Second}`。
    3. `cmd/wesh/main_test.go`：TestParseArgs 表驱动加行——默认断言 cfg.pingInterval==5；`["--ping-interval", "0", "--", "bash"]` → 0；`["--ping-interval", "30", "--", "bash"]` → 30。
    4. 新建 `internal/server/keepalive_test.go`（package server_test）：`TestPingKeepalive`（VALIDATION 行 59）——startTestServerOpts 装配 `Options{PingInterval: 200 * time.Millisecond}`；裸帧 dialRawWS 完成 Hello 握手；随后 1s 窗口内 readRawFrame 循环，计数 op==0x9 的 ping 帧 ≥2 个（间隔到达证明）；对每个 ping 以 writeRawFrame 回 pong（fin=true, op=0xA, 载荷回显 ping 载荷——库按载荷匹配 activePings）；窗口结束断言连接仍存活（再发一帧合法消息或继续读无关闭帧）。ping 帧可能与 OUTPUT 数据帧（op=0x2）交织——按 opcode 过滤计数即可。`TestPingDisabled`（D-16 0=禁用，VALIDATION 行 61）——startTestServerOpts 装配 `Options{PingInterval: 0}`（pinger 早退分支）；裸帧完成 Hello 握手后 1s 窗口内 readRawFrame 循环，断言收到零个 op==0x9 帧（OUTPUT 数据帧不计）且连接存活。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go vet ./... && go test ./internal/server -count=1 -v -run 'TestPingKeepalive|TestPingDisabled' && go test ./cmd/wesh -count=1 && go test ./... -count=1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - TestPingKeepalive 绿：interval=200ms 时 1s 内收 ≥2 个 ping（op=0x9），回 pong 后连接存活（VALIDATION 行 59 落位）
    - TestPingDisabled 绿：PingInterval=0 装配下 1s 窗口零 ping 帧且连接存活（VALIDATION 行 61 落位，D-16 "0=禁用"行为证明）
    - server.go 升档序列末尾含 `go s.pinger(` 启动点；pinger 内 `interval <= 0` 早退分支存在；pong 超时经 context.WithTimeout(ctx, pongTimeout) 传入 c.Ping
    - Attach 的 ctx 为 context.Background() 派生的 WithCancel ctx 且 defer cancel（pinger 随连接生命周期终结）；不存在 r.Context() 使用
    - main.go 注册 --ping-interval（int 秒，默认 5，help "WS ping interval in seconds (0 = disable)"）；Options.PingInterval 注入链路完整（config → New）
    - `go test ./... -count=1` 与 `go test -race -count=1 ./internal/server` 退出 0（pinger goroutine 竞态面与全量回归）
  </acceptance_criteria>
  <done>保活正向路径锁定：ping 按间隔到达、pong 匹配、--ping-interval 可配（0 禁用）、pinger 生命周期随连接，全量测试绿。</done>
</task>

<task type="auto">
  <name>Task 2: TestPongTimeout——pong 缺失时连接在 interval+timeout 内被关闭</name>
  <files>internal/server/keepalive_test.go</files>
  <read_first>
    - internal/server/keepalive_test.go（被改文件现状——本 plan Task 1 产物：TestPingKeepalive 与裸帧用法）
    - internal/server/server.go（pinger/pongTimeout 现状）
    - internal/server/rawws_test.go（dialRawWS/readRawFrame/writeRawFrame 签名）
    - .planning/phases/02-protocol/02-RESEARCH.md（Pattern 4 行 319-349——pong 超时经库内 AfterFunc c.close() 收口的语义链；Open Questions Q2 行 611-614）
    - .planning/phases/02-protocol/02-VALIDATION.md（行 663 TestPongTimeout 验收描述）
  </read_first>
  <action>
    在 `internal/server/keepalive_test.go` 追加 `TestPongTimeout`（CORE-06 负向路径，VALIDATION 行 663）：
    1. 经 `server.SetPongTimeout(500 * time.Millisecond)` 注入短超时（t.Cleanup 调恢复闭包，生产值恒 10s——export_test.go 钩子，plan 02-04 Task 1 已建）。
    2. startTestServerOpts 装配 `Options{PingInterval: 200 * time.Millisecond}`；裸帧 dialRawWS 完成 Hello 握手。
    3. 此后读到 ping 帧（op=0x9）也**一律不回 pong**（库客户端会自动回 pong 无法构造本负例——裸帧 helper 的必要性）。
    4. 断言：自首个 ping 到达起 2s 内连接被关闭（readRawFrame 读到关闭帧或读返回错误/TCP EOF——pong 超时触发库内 writeControl 超时 AfterFunc → c.close()，conn.go:171-182 VERIFIED；关闭码不作具体断言：库内 close 路径可能无合规关闭帧直达，行为锚点是"连接在 interval+timeout 量级内终结"）。
    5. 对照断言（防误杀回归面）：同测试前半段已证 ping 帧确实到达（≥1 个 op=0x9）——若 pinger 未启动，连接永不因本路径关闭，测试超时即暴露装配缺失。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test ./internal/server -count=1 -v -run 'TestPongTimeout' && go test -race -count=1 ./... && go test ./... -count=1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - TestPongTimeout 绿：不回 pong 的裸连接在 2s 内被关闭（interval 200ms + 注入 timeout 500ms + 余量；VALIDATION 行 663 落位）
    - 测试内 pongTimeout 改写经 t.Cleanup 恢复 10s（生产值恒 10s——D-16）
    - `go test -race -count=1 ./...` 全仓退出 0；`go test ./... -count=1` 退出 0（无回归）
  </acceptance_criteria>
  <done>CORE-06 双路径齐：ping 间隔到达且 pong 匹配保活；pong 缺失时连接在 interval+timeout 内被库内路径关闭——反代空闲超时断连风险结构性消除。</done>
</task>

</tasks>

## Flagged Assumptions（spec-less probe fallback §A/§C——显式呈现，绝不静默丢弃）

edge probe 对 CORE-06 返回 `unclassified`，按规则保持 `unresolved` 并在此登记：

| Requirement | probe 结果 | 状态 | 处置 |
|-------------|-----------|------|------|
| CORE-06 | unclassified — review manually | unresolved | 以 RESEARCH §Validation Architecture 行 662-663 两条验收准则为锚点（TestPingKeepalive / TestPongTimeout，本 plan 落位） |

§B prohibition recall：CORE-06 经对抗召回后的候选全部为常规工程项（goroutine 泄漏、误杀健康连接等——已由 ctx 纪律与 pong 超时覆盖），按精确化规则 drop；values/safety/ethics 类零存活——空列表是该纯机制需求的正确产出。prohibition recall surfaced 0 条 = authored 0 条 ✓。

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 服务端 → 客户端（ping 控制帧） | 保活流量经不可信网络段；载荷为库内自增计数器，无会话数据 |
| pinger goroutine ↔ reader 路径 | 并发装配边界——终结路径必须单一收口 |
| npm/go 依赖安装 | 本 plan 零新增依赖 |

## STRIDE Threat Register（ASVS L1，block on high）

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04-01 | DoS | pinger goroutine 泄漏（连接终结后 ticker 空转累积） | medium | mitigate | ctx 随 Attach defer cancel 派生，pinger 首 select 分支即 ctx.Done()；interval<=0 早退；-race 全量门禁 |
| T-02-04-02 | DoS | pong 超时误杀健康空闲连接（ctx 到期 = 整连接关闭的语义陷阱） | high | mitigate | Pitfall 2 纪律：pong 的 10s ctx 独立短命仅裹 c.Ping，读路径不传 deadline ctx；10s 对毫秒级 RTT 极宽；TestPongTimeout 证明负向路径、TestPingKeepalive 证明回 pong 后存活 |
| T-02-04-03 | Tampering | 应用层自造心跳帧（协议面污染） | low | mitigate | 库内建 ping/pong（c.Ping + 库自动 pong 回复对端 ping，read.go:317-323 VERIFIED）；Anti-Pattern 纪律注释沿用 Phase 1 |
| T-02-04-04 | DoS | 反代空闲超时切断静默连接（本需求的威胁本体） | high | mitigate | WS 原生 ping/pong 5s 默认间隔 ≪ nginx 60s / Cloudflare 100s / 30s 型 ingress（D-16）；控制帧同样刷新代理空闲计时 |
| T-02-04-SC | Tampering | 依赖安装 | high | mitigate | 本 plan 零新增第三方依赖 |
</threat_model>

<verification>
- `go vet ./... && go test ./... -count=1` 全绿
- `go test ./internal/server -run 'TestPingKeepalive|TestPongTimeout' -count=1` 绿
- `go test -race -count=1 ./...` 全仓绿（goroutine 竞态面）
</verification>

<success_criteria>
- ROADMAP 准则 3 成立：WS ping/pong 按可配间隔保活（--ping-interval，默认 5s、0 禁用），反代空闲超时下连接不被切断；pong 缺失 10s 连接关闭
- CORE-06 双向证明：正向（间隔到达 + 回 pong 存活）与负向（不回 pong 被关）各有 e2e
- 本 plan 的 must_haves.truths 全部可由 acceptance_criteria 对应断言证明
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `internal/server/server.go`（扩展）：`func (s *Server) pinger(ctx context.Context, c *websocket.Conn, interval time.Duration)`；`Options.PingInterval time.Duration` 字段；包级私有变量 `pongTimeout = 10 * time.Second`；升档序列末尾 pinger 启动点；Attach ctx 改 WithCancel 派生
- `internal/server/export_test.go`（增量）：`SetPongTimeout(d time.Duration) (restore func())`
- `cmd/wesh/main.go`（扩展）：config 字段 `pingInterval int`；CLI flag `--ping-interval`（int 秒，默认 5，0=禁用，help "WS ping interval in seconds (0 = disable)"）
- `cmd/wesh/main_test.go`（扩展）：TestParseArgs 新增 --ping-interval 用例行（默认 5 / 显式 0 / 显式 30）
- `internal/server/keepalive_test.go`（新建）：`TestPingKeepalive`、`TestPingDisabled`、`TestPongTimeout`

<output>
Create `.planning/phases/02-protocol/02-04-SUMMARY.md` when done.
</output>
