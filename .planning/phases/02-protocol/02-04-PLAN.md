---
phase: 02-protocol
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - internal/server/server.go
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
  - internal/server/keepalive_test.go
autonomous: true
requirements:
  - CORE-06

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "连接建立后服务端按 --ping-interval（默认 5s）周期发送 WS ping，浏览器自动回 pong——反代 60s/100s 空闲超时下长空闲连接不被切断（D-16，CORE-06）"
    - "发出 ping 后 10s 内未收到 pong（pong 超时），服务端打 stderr 单行事件并 CloseNow 断开该连接（D-16；Ping 超时不关连接，应用须自行 CloseNow——conn.go:251-258）"
    - "--ping-interval 0 禁用保活：不发任何 ping，长空闲连接保持（用户显式选择，D-16）"
    - "pinger 与单 reader 循环并发装配（库要求 Ping 必须与 Reader 并发，conn.go:218-220）；pinger 终结挂进 Attach 的 ctx cancel，进既有 sync.Once 同一生收口，零新 exitf 分支"
    - "ping 与 onChunk 的 OUTPUT 写并发安全（库 writeFrameMu 串行化所有帧，write.go:288-293），无帧交错"
  prohibitions:
    - "保活实现不得误杀健康的长空闲会话——读路径永不携带 deadline（deadline ctx 到期即整连接关闭且无关闭帧，conn.go:188-199）；只有 pong 超时（10s 无应答）才允许断开连接"
  artifacts:
    - path: "internal/server/server.go"
      provides: "pinger goroutine（ticker + Ping + pong 超时 CloseNow）与 Options.PingInterval/PongTimeout 注入"
      contains: "pinger"
    - path: "cmd/wesh/main.go"
      provides: "--ping-interval flag（默认 5s，0 禁用）"
      contains: "ping-interval"
    - path: "internal/server/keepalive_test.go"
      provides: "保活三测：存活/pong 超时/禁用"
      contains: "TestPingKeepalive / TestPongTimeout / TestPingDisabled"
  key_links:
    - from: "internal/server/server.go"
      to: "github.com/coder/websocket"
      via: "c.Ping(pctx) 每 ticker 触发；pong 由既有单 reader 读循环 handleControl 处理（read.go:317-337）"
      pattern: "\\.Ping\\("
    - from: "cmd/wesh/main.go"
      to: "internal/server/server.go"
      via: "parseArgs --ping-interval → config.pingInterval → server.New Options{PingInterval}"
      pattern: "ping-interval"
---

<objective>
落地 CORE-06 保活：Welcome 升档后启动 pinger goroutine，按 --ping-interval（默认 5s，0 禁用）周期 Ping；pong 超时 10s 未应答则 stderr 事件 + CloseNow。配套 keepalive_test.go 三测（存活/pong 超时/禁用）与 parseArgs flag 测试。

Purpose: 反代空闲超时（nginx 60s / Cloudflare 100s / 30s 型 ingress）看的是应用层流量——TCP keepalive 多数反代不计入，WS ping 才是对症解（RESEARCH State of the Art）。5s 默认值对主流阈值安全两个数量级。
Output: server.go pinger + main.go --ping-interval + keepalive_test.go 三测全绿。

规格探针标记（spec-less probe fallback）：CORE-06 探针报为 unclassified；边界按 D-16 与 RESEARCH Ping 语义（conn.go:216-259 源码核实）落入 must_haves.truths，"不误杀健康空闲会话"落入 prohibitions，无静默丢弃。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-protocol/02-CONTEXT.md
@.planning/phases/02-protocol/02-RESEARCH.md
@.planning/phases/02-protocol/02-PATTERNS.md
@internal/server/server.go
@cmd/wesh/main.go
@cmd/wesh/main_test.go
@internal/server/e2e_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pinger goroutine + --ping-interval flag + Options 扩展</name>
  <reversibility rating="one-way">D-16 --ping-interval flag 名是 CLI 公开契约（同 D-15 理由）；用户已锁定，记录不设 checkpoint。</reversibility>
  <files>internal/server/server.go, cmd/wesh/main.go, cmd/wesh/main_test.go</files>
  <read_first>
    - internal/server/server.go（被改文件现状：02-03 后的 Attach——升档序列尾段是 pinger 启动点；ctx 派生点 91 行附近；logEvent 已就位）
    - cmd/wesh/main.go（被改文件现状：config 结构体、parseArgs flag 注册、run 装配 New 调用）
    - cmd/wesh/main_test.go（被改文件现状：TestParseArgs 表驱动）
    - .planning/phases/02-protocol/02-RESEARCH.md（Pattern 3 ping 保活 goroutine 265-297 行——三条源码核实纪律照抄；Pitfall 3 ping 饿死 345-349 行）
    - .planning/phases/02-protocol/02-PATTERNS.md（§server.go pinger 段 245-264 行、Planner 注意 8 行 516）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-16，行 45）
  </read_first>
  <behavior>
    - PingInterval=100ms 注入时：握手完成后空闲 350ms 连接保持存活（库客户端读循环自动回 pong）
    - PingInterval=100ms + PongTimeout=300ms 注入且客户端停止 Read（不回 pong）→ 约 interval+timeout 内服务端断开连接，exitf(0) 收口
    - PingInterval=0：不启动 ticker，客户端不回 pong 也永不因保活断开
  </behavior>
  <action>
    1. `internal/server/server.go`：
       - Options 追加两字段并注释分组语义：PingInterval time.Duration（生产直传字段——main.go flag 原样透传，0 = 禁用，D-16）；PongTimeout time.Duration（测试可覆写字段，零值取默认常量 defaultPongTimeout = 10s，D-16——正常 RTT 毫秒级，10s 极宽）。New 中补零值默认。
       - 新增 pinger 方法（RESEARCH Pattern 3 照抄语义）：interval <= 0 直接 return（禁用）；ticker 循环 select ctx.Done() / t.C；每触发 pctx, cancel := context.WithTimeout(ctx, s.pongTimeout) 后 c.Ping(pctx)、cancel；err 非 nil → s.logEvent（pong_timeout 事件，三要素）→ c.CloseNow() → return（Ping 超时不关连接——conn.go:251-258，应用自行 CloseNow；对端已不应答，关闭握手无意义，客户端见 1006 属本地合成码不违反 D-05——D-05 约束服务端 wire 发送）。
       - pinger 启动点：Welcome 升档序列尾段（SetReadLimit(proto.ReadLimitPostAuth) 之后）go s.pinger(ctx, c, s.pingInterval)——ctx 为 Attach 内 context.WithCancel(context.Background()) 派生（02-02 的 ctx 改为 WithCancel 并 defer cancel()）；pinger 随 Attach 返回经 cancel 终结，进既有 wsDisconnected/terminate 收口，禁止新增 exitf 分支（CONTEXT 行 92 硬约束）。注释引三条纪律：Ping 必须与 Reader 并发（conn.go:218-220，现有单 reader 天然满足，不得为 ping 再开 reader）；写并发安全由库 writeFrameMu 串行化（write.go:288-293）；pong 由读循环 handleControl 自动处理（read.go:317-337）。
       - Server 结构体加 pingInterval/pongTimeout 字段（plain——New 装配期固化）。
    2. `cmd/wesh/main.go`：config 加 pingInterval time.Duration；parseArgs 加 fs.DurationVar(&cfg.pingInterval, "ping-interval", 5*time.Second, "WS ping interval (0 = disable)")（D-16；DurationVar 原生支持 "5s" 语法，RESEARCH Don't Hand-Roll 表）；run 装配 Options 加 PingInterval: cfg.pingInterval。imports 加 time（若 02-03 已加则复用）。
    3. `cmd/wesh/main_test.go`：TestParseArgs 表结构加 wantPingInterval time.Duration；新组 {"ping interval", []string{"--ping-interval", "30s", "--", "bash"}, ...} 断言 30s；既有组补默认值 5s 断言；加 {"ping disabled", []string{"--ping-interval", "0", "--", "bash"}, ...} 断言 0。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go build ./... && go vet ./... && go test -race -count=1 ./internal/server/ ./cmd/wesh/</automated>
  </verify>
  <acceptance_criteria>
    - server.go 含 pinger 方法且启动点在 Welcome 升档之后；Ping 错误路径含 logEvent + CloseNow
    - Options 含 PingInterval（0=禁用直传）与 PongTimeout（零值默认 10s）
    - main.go 含 `--ping-interval` flag 默认 5s；Options 装配含 PingInterval
    - TestParseArgs 含 30s 解析组与 0 禁用组
    - 既有测试全量保持绿色（pinger 不干扰既有生命周期语义）
  </acceptance_criteria>
  <done>pinger 按默认 5s 工作，--ping-interval 全链路（flag → config → Options → goroutine）就位。</done>
</task>

<task type="auto">
  <name>Task 2: keepalive_test.go——保活三测（存活 / pong 超时 / 禁用）</name>
  <files>internal/server/keepalive_test.go</files>
  <read_first>
    - internal/server/e2e_test.go（同包 helper：startTestServerWith/dialHello/waitExit；CloseError 读取循环 267-278 行）
    - internal/server/server.go（本 plan Task 1 产物：pinger 行为与 Options 注入字段——测试断言目标）
    - .planning/phases/02-protocol/02-RESEARCH.md（§Validation Architecture CORE-06 两行映射 553-554 行、§测试可注射性注意 559 行）
    - .planning/phases/02-protocol/02-PATTERNS.md（§用例矩阵 TestPingKeepalive/TestPongTimeout 行 447-448）
  </read_first>
  <action>
    新建 internal/server/keepalive_test.go（package server_test，复用 helper，超时护栏 10s）：
    1. TestPingKeepalive（CORE-06）：startTestServerWith(t, []string{"/bin/cat"}, server.Options{Writable: true, PingInterval: 100 * time.Millisecond})；dialHello 后空闲 350ms（>3 个 ping 间隔——库客户端读循环自动回 pong，此间连接若无保活兼容即已被误杀）；随后发 INPUT payload 并累积收齐 echo 断言连接功能完好 → c.Close(1000) → waitExit(0)。注释：空闲存活的反面是 Pitfall 2/3（读 deadline 误杀或 ping 饿死），本测试即其回归。
    2. TestPongTimeout（CORE-06/D-16）：startTestServerWith Options{Writable: true, PingInterval: 100 * time.Millisecond, PongTimeout: 300 * time.Millisecond}；websocket.Dial（经 dialHello 完成握手）后客户端停止一切 Read（库只在读路径回 pong——read.go:317-323，不 Read 即不应答）→ 等服务端断开：下一次 Read 在 100ms+300ms+宽限（2s 护栏）内返回错误（CloseNow 无关闭帧，客户端见本地合成 1006/异常——断言 err 非 nil 即可，不断言码）→ waitExit(0)（服务端 reader 随连接终结走 D-11 收口）。
    3. TestPingDisabled（CORE-06/D-16）：startTestServerWith Options{Writable: true, PingInterval: 0, PongTimeout: 100 * time.Millisecond}；dialHello 后客户端停止 Read 500ms——若 0 被误作默认而发出 ping，则 100ms pong 超时必触发断开；随后发 INPUT echo 成功即证明未发 ping、连接未被保活路径触碰 → c.Close(1000) → waitExit(0)。（强断言形态：存活证据反证无 ping。）
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test -race -count=1 -v -run 'TestPingKeepalive|TestPongTimeout|TestPingDisabled' ./internal/server/ && go test -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - 三测全部绿色且 -race 无报告
    - TestPongTimeout 在 interval+timeout+宽限内观察到连接断开，且 exitf(0) 收口（单一终结路径不破坏）
    - TestPingDisabled 以"不回 pong 仍存活"反证 0 禁用语义
    - `go test -race -count=1 ./...` 全仓绿
  </acceptance_criteria>
  <done>CORE-06 保活三态（默认工作/超时断开/0 禁用）自动化锁定；ROADMAP 准则 3 成立。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client（保活控制帧） | ping/pong 是唯一服务端主动发起的周期流量；错误装配会自伤连接可用性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-14 | DoS | pinger 误装配（读循环内顺序执行/ping 无 reader 并发） | high | mitigate | 独立 goroutine + 单 reader 并发（conn.go:218-220 库硬性要求）；TestPingKeepalive 空闲存活回归（Pitfall 3） |
| T-02-15 | DoS | 健康长空闲会话被误杀 | high | mitigate | 读路径恒无 deadline（Pitfall 2）；仅 pong 超时断开；prohibition 显式入 must_haves；TestPingKeepalive 锁定 |
| T-02-16 | DoS | 攻击者占用连接不应答 ping 导致资源常驻 | low | accept | pong 超时 CloseNow 即处置路径；单次语义下 exitf 收口释放全部资源 |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | 零新增依赖（RESEARCH §Package Legitimacy Audit） |
</threat_model>

<verification>
- `go test -race -count=1 ./...` 全绿（三新测 + 既有全部）
- `go build ./... && go vet ./...` 退出 0
- pinger 启动点代码评审：Welcome 升档之后、ctx 经 WithCancel 派生、defer cancel 进同一生收口
</verification>

<success_criteria>
- ROADMAP 准则 3 成立：WS ping/pong 按可配间隔保活，反代空闲超时下连接不被切断（TestPingKeepalive 为证；反代配方文档属 Phase 9）
- pong 无应答连接被服务端主动清理（TestPongTimeout），不泄漏半死连接
- --ping-interval 0 禁用语义自动化锁定（TestPingDisabled）
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `internal/server/server.go`：`pinger` 方法、`Options.PingInterval`/`Options.PongTimeout` 字段、默认常量 `defaultPongTimeout`（10s）、Attach 内 `context.WithCancel` 派生与 defer cancel
- `cmd/wesh/main.go`：`--ping-interval` flag（DurationVar，默认 5s）、`config.pingInterval` 字段
- `cmd/wesh/main_test.go`：TestParseArgs 新增 ping-interval 30s 组与 0 禁用组
- `internal/server/keepalive_test.go`：`TestPingKeepalive`/`TestPongTimeout`/`TestPingDisabled`

<output>
Create `.planning/phases/02-protocol/02-04-SUMMARY.md` when done
</output>
