---
phase: 02-protocol
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - README.md
autonomous: true
requirements:
  - CORE-04
  - CORE-06
  - SEC-08
  - RES-01

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "README 与代码现状一致：flag 表含 --writable 与 --ping-interval（默认值/语义正确），默认只读行为成文（文档不得说谎——01-05 确立的收口原则）"
    - "README 协议节含 wesh.v1 子协议、Hello/Welcome/Error 帧表、关闭码集合 {1000,1002,1008,1009,1011}（1001/1013 占位说明）、三层上限数值（16KiB/32 分片/16KiB 累积、预认证 4KiB）、5s 未认证超时、per-IP 半开帽 8、ping/pong 保活默认 5s"
    - "全量验证链绿：go vet + time go test -race -count=1 ./... + pnpm -C web build + gofmt -l 空输出 + go build 成功"
    - "CLI 冒烟：--help 输出含 --writable 与 --ping-interval；--port 0 -- /bin/true 打印单行 listening 并以退出码 0 退出（D-07/D-10 不回归）"
  artifacts:
    - path: "README.md"
      provides: "Phase 2 用户面契约文档：默认只读、两新 flag、协议基线说明"
      contains: "--writable / --ping-interval / wesh.v1 / 只读"
  key_links:
    - from: "README.md"
      to: "cmd/wesh/main.go"
      via: "flag 表与 parseArgs 注册逐行一致（名称/默认值/help 语义）"
      pattern: "writable|ping-interval"
  prohibitions: []
---

<objective>
Phase 2 收口：README 更新到协议基线现状（默认只读是重大行为变更，用户必须能读到）+ 全量收口验证（-race 全量/前端构建/gofmt/CLI 冒烟）+ 浏览器手动 checklist 交接。

Purpose: 01-05 确立的原则——README 按现状描述、不说谎；默认 ro 是 Phase 2 最显眼的行为变化（老用户升级后键盘"没反应"必须有文档与 [ro] 标题双重提示）。
Output: README.md 更新；全量验证链绿证据；手动验证 checklist 交接。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-protocol/02-CONTEXT.md
@.planning/phases/02-protocol/02-VALIDATION.md
@README.md
@cmd/wesh/main.go
@.planning/phases/02-protocol/02-01-SUMMARY.md
@.planning/phases/02-protocol/02-02-SUMMARY.md
@.planning/phases/02-protocol/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README 更新——默认只读 + 两新 flag + 协议基线节</name>
  <files>README.md</files>
  <read_first>
    - README.md（被改文件现状：警示语行 5-7、单次语义行 9-11、flag 表行 21-28、协议帧形状节行 50-57）
    - cmd/wesh/main.go（flag 注册与默认值现状——文档对齐源）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-04/D-05/D-09/D-13/D-14/D-15/D-16 文档落点）
    - .planning/phases/02-protocol/02-01-SUMMARY.md、02-02-SUMMARY.md、02-03-SUMMARY.md（实际落地的行为与数值——以 SUMMARY 为准纠偏）
  </read_first>
  <action>
    按现状改写（保持现有中文风格、无 banner/emoji 纪律）：
    1. 警示语段（行 5-7）保留"无认证"警示；补一句：Phase 2 起默认**只读**——任何能访问地址的人能看到 shell 输出，加 `--writable` 才允许其输入——警示级别不降。
    2. flag 表（行 21-26）追加两行：`--writable` | `false` | 允许客户端输入（默认只读，键盘输入被服务端丢弃）；`--ping-interval` | `5` | WS ping 保活间隔秒数（`0` = 禁用；防反代空闲超时断连）。
    3. 单次语义节标题泛化为"单次语义（现阶段）"或保留——内容不变（仍成立）；末尾补：第二连接 409 语义不变（多客户端 Phase 5）。
    4. "协议帧形状"节（行 50-57）扩写为"协议基线（wesh.v1）"：
       - 子协议：WS 握手必须带 `Sec-WebSocket-Protocol: wesh.v1`，否则 HTTP 400；握手后首帧必须为 Hello，抢跑/5s 超时分别 1002/1008。
       - 帧表追加：`'H'` Hello（C→S，JSON `{"version","cols","rows"}`，首尺寸同步）、`'W'` Welcome（S→C，JSON `{"version","mode"}`，mode 为 `"ro"`/`"rw"`）、`'E'` Error（S→C，JSON `{"code","message"}`，仅 version_mismatch/server_error 等正常客户端路径）。
       - 关闭码行改写：线上主动关闭码只出现 1000/1002/1008/1009/1011（1001 服务端下线与 1013 背压踢出为后续 phase 占位；1005/1006/1015 永不发送）。
       - 新增三层上限小表（C→S）：单帧 ≤16KiB、每条消息分片数 ≤32、每条消息累积 ≤16KiB；预认证（Hello 完成前）单消息 ≤4KiB；超限一律 1009 关闭。per-IP 半开连接帽 8（超限 HTTP 429）。
       - 保活：服务端每 `--ping-interval` 秒发 WS ping，pong 超时 10s 关连接。
    5. 不改构建/测试节（仍准确）。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && grep -q -- '--writable' README.md && grep -q -- '--ping-interval' README.md && grep -q 'wesh.v1' README.md && go run ./cmd/wesh --help 2>&1 | grep -q 'writable'</automated>
  </verify>
  <acceptance_criteria>
    - README flag 表含 --writable（默认 false）与 --ping-interval（默认 5，0=禁用），与 main.go parseArgs 注册一致
    - README 协议节含 wesh.v1 子协议要求、'H'/'W'/'E' 帧行、关闭码集合 {1000,1002,1008,1009,1011}、三层上限数值（16KiB/32/16KiB/预认证 4KiB）、半开帽 8、ping 5s/pong 10s
    - 警示语仍含无认证警示且新增默认只读说明；全文无与现状矛盾表述（如"线上关闭码只出现 1000/1002/1009"旧句已改写）
  </acceptance_criteria>
  <done>README 每一条用户面契约与代码现状一致，默认只读行为变更对老用户可见。</done>
</task>

<task type="auto">
  <name>Task 2: 全量收口验证 + 浏览器手动 checklist 交接</name>
  <precondition>plan 02-01/02-02/02-03 已全部完成并合入（depends_on 保证，wave 3 在 wave 1/2 之后）；各 plan SUMMARY 已落盘。</precondition>
  <files>README.md</files>
  <read_first>
    - .planning/phases/02-protocol/02-VALIDATION.md（Sampling Rate 与 Manual-Only Verifications 两节）
    - .planning/phases/02-protocol/02-01-SUMMARY.md、02-02-SUMMARY.md、02-03-SUMMARY.md（各 plan 自述验证结论与遗留）
  </read_first>
  <action>
    1. 全量验证链（含构建耗时跟踪）：`go vet ./...` → `time go test -race -count=1 ./...`（全量竞态）→ `pnpm -C web build`（tsc+vite 一体）→ `go build ./...` → `"$(go env GOROOT)/bin/gofmt" -l .` 输出为空（必须用 GOROOT 版本——/usr/bin/gofmt 陈旧不识泛型，Phase 1 教训）→ `git status --short` 确认无意外改动。
    2. CLI 冒烟：`go run ./cmd/wesh --help` 输出含两新 flag；`go run ./cmd/wesh --port 0 -- /bin/true` 打印单行 listening 并以退出码 0 退出；`go run ./cmd/wesh --writable --ping-interval 3 --port 0 -- /bin/true` 同样成立（新 flag 组合不破坏启动路径）。
    3. 产物时间戳核验：`go build -o /tmp/wesh-p2 ./cmd/wesh` 后 `ls -l /tmp/wesh-p2` 时间戳为当前（编译产物新鲜度）。
    4. 汇总本 phase 验证证据（各 SUMMARY 的自检结论 + 本任务全量输出）写入 SUMMARY。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && go vet ./... && time go test -race -count=1 ./... && pnpm -C web build && go build ./... && test -z "$("$(go env GOROOT)"/bin/gofmt -l .)"</automated>
    <human-check>
      浏览器手动 checklist（VALIDATION Manual-Only 两条的交接）：
      1. `go run ./cmd/wesh -- bash`（默认 ro）：浏览器打开后标题带 "[ro] " 前缀；键盘敲击终端无任何回显；拖动窗口 resize 正常跟随（ro 下 RESIZE 放行）。
      2. DevTools → Network → WS 帧面板：可见每 ~5s 一个 ping/pong 控制帧；首帧为 Hello（'H'+JSON）、随后 Welcome（'W'+JSON，mode:"ro"）。
      3. `go run ./cmd/wesh --writable -- bash`：键盘输入正常回显，无 [ro] 前缀。
      4. 预认证内存平坦走查（SEC-08）：确认 Attach 守卫区（400/429/409）全部位于 websocket.Accept 之前（源码走查，非浏览器）。
    </human-check>
  </verify>
  <acceptance_criteria>
    - 全量验证链每条命令退出 0；gofmt -l 输出为空
    - --help 含 --writable 与 --ping-interval；两种 flag 组合下 /bin/true 冒烟均以 0 退出且打印单行 listening
    - /tmp/wesh-p2 时间戳为本次构建时刻
    - human-check 四项 checklist 交接给用户（end-of-phase 人工验证模式）
  </acceptance_criteria>
  <done>Phase 2 全量验证链绿 + CLI 冒烟绿 + 文档现状一致，phase 达到可人工验收状态。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档 → 用户 | README 是用户理解攻击面与行为变更的入口；文档说谎 = 用户错误暴露 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-18 | Information Disclosure | README 与现状漂移（默认 ro 未文档化 → 用户误以为可写或误以为安全） | medium | mitigate | flag 表/协议节与 main.go、proto 常量逐项对齐；wired：Task 1 acceptance grep 断言 + Task 2 --help 对照 |
| T-02-SC | Tampering | npm/pip/cargo 安装（供应链） | high | mitigate | 本 plan 零安装任务（文档与验证），legitimacy checkpoint 不适用 |
</threat_model>

<verification>
全量链：`go vet ./... && time go test -race -count=1 ./... && pnpm -C web build && go build ./...` + gofmt 空 + CLI 双冒烟。
</verification>

<success_criteria>
1. README 与现状零矛盾，默认只读/两新 flag/协议基线成文
2. 全量验证链绿、产物时间戳新鲜
3. 手动 checklist 交接完成（ro 标题/键盘无响应/WS 帧面板 ping/1009 文案）
</success_criteria>

## Artifacts this phase produces（本 plan 份额，符号清单供 plan-review 源接地排除）

本 plan 不产生代码符号——仅 README.md 文档更新（章节：协议基线 wesh.v1、flag 表两行、默认只读警示）。

<output>
Create `.planning/phases/02-protocol/02-04-SUMMARY.md` when done
</output>
