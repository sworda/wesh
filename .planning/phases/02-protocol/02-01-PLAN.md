---
phase: 02-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/proto/proto.go
  - internal/proto/proto_test.go
  - internal/server/server.go
  - internal/server/e2e_test.go
  - internal/server/handshake_test.go
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
  - web/src/main.ts
autonomous: true
requirements:
  - CORE-04

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WS 握手必须携带子协议 wesh.v1：前端 new WebSocket(url, ['wesh.v1'])，服务端 AcceptOptions.Subprotocols=[proto.Subprotocol] 且 Accept 后 c.Subprotocol() assert 兜底（D-03 的双闸之 Accept 侧；HTTP 400 预检在 plan 02-02 落地）"
    - "连接建立后 C→S 首条消息必须是 Hello 帧（'H' + JSON {version, cols, rows}）：version != \"wesh.v1\" 时服务端先写 Error 帧 {code:\"version_mismatch\", message} 再以 1008 关闭且 close reason 为 \"version_mismatch\"（D-02/D-06/D-07，TestVersionMismatch 证明）"
    - "Hello 前收到任何数据帧（抢跑帧）以 1002 关闭、reason \"unexpected_frame\"，且全程线上无 'E' Error 帧（D-04/D-06 攻击面不发 Error，TestPrematureFrame 证明）"
    - "Hello JSON 混入未知字段（如 ticket/future）仍解析成功——stdlib Unmarshal 默认忽略未知字段，禁止开启严格未知字段拒绝（D-02 演化承诺，TestHelloIgnoresUnknown 证明）"
    - "握手成功即按 Hello 携带的 cols/rows 执行 sess.Resize，随后下发 Welcome 帧（'W' + JSON {version:\"wesh.v1\", mode}），消除 Phase 1 的 80x24 首帧窗口（RESEARCH 架构图行 161）"
    - "默认只读：ro 模式下服务端丢弃 INPUT 帧（安全边界在服务端，D-13），RESIZE 放行（ro 下同）；Welcome.mode==\"ro\" 时前端 term.options.disableStdin=true 且 document.title 加 \"[ro] \" 前缀（D-14）；--writable 开启后 INPUT 生效回显（D-15，TestWritableEcho / TestReadOnlyDropsInput / TestReadOnlyAllowsResize 证明）"
    - "关闭码静态合规：server.go 全部 c.Close 调用点的码值 ⊆ {1000,1002,1008,1009,1011}，无 1005/1006/1015 字面量、无 4000 段自定义码（D-05/D-08，TestCloseCodeSet 证明）；1001/1013 进 proto 常量注释表占位不实现（D-08）"
    - "前端 onclose 按码分派人话文案：1009 显示超限文案且不提任何 flag（D-12①），1008/1011 优先展示 Error 帧 message（D-07），1006 不作为分派依据（异常断开走 !ev.wasClean/default 分支）"
    - "Phase 1 全部既有 e2e 经 dialAndHandshake helper 收口改造后保持绿（dial 带 wesh.v1 子协议 → 发 Hello → 断言 Welcome.mode）"
  artifacts:
    - path: "internal/proto/proto.go"
      provides: "协议单一事实源扩展：Subprotocol 常量、'H'/'W'/'E' 类型字节、Error code 表、关闭码纪律注释表、Hello/Welcome/Error 编解码"
      exports: ["Subprotocol", "Hello", "Welcome", "Error", "ErrVersionMismatch", "ErrServerError", "ModeRO", "ModeRW", "DecodeHello", "EncodeWelcome", "EncodeError"]
    - path: "internal/proto/proto_test.go"
      provides: "proto 单测：关闭码静态合规、Hello 未知字段忽略、编解码往返"
      contains: "TestCloseCodeSet / TestHelloIgnoresUnknown"
    - path: "internal/server/server.go"
      provides: "Attach 握手状态机（Hello 校验/Welcome 下发/首尺寸 Resize）+ ro 边界 + Options 注入"
      exports: ["Server", "New", "Handler", "Attach", "Options"]
    - path: "internal/server/e2e_test.go"
      provides: "dialAndHandshake helper + 既有五测改造 + CORE-04 三测"
      contains: "dialAndHandshake / TestWritableEcho / TestReadOnlyDropsInput / TestReadOnlyAllowsResize"
    - path: "internal/server/handshake_test.go"
      provides: "握手负例 e2e：版本不匹配 1008、抢跑帧 1002 无 Error"
      contains: "TestVersionMismatch / TestPrematureFrame"
    - path: "cmd/wesh/main.go"
      provides: "--writable flag 与 Options 装配"
      contains: "--writable"
    - path: "cmd/wesh/main_test.go"
      provides: "parseArgs 新增 --writable 表驱动用例"
      contains: "writable"
    - path: "web/src/main.ts"
      provides: "前端握手（子协议 + Hello 首帧）+ Welcome 驱动 ro/rw + onclose 按码分派"
      contains: "wesh.v1 / disableStdin / [ro] "
  key_links:
    - from: "internal/server/server.go"
      to: "internal/proto/proto.go"
      via: "握手与编解码全部走 proto 常量/函数（Subprotocol/Hello/Welcome/Error/DecodeHello/EncodeWelcome/EncodeError）"
      pattern: "proto\\.(Subprotocol|Hello|Welcome|Error|DecodeHello|EncodeWelcome|EncodeError)"
    - from: "cmd/wesh/main.go"
      to: "internal/server/server.go"
      via: "server.New(sess, os.Exit, server.Options{Writable: cfg.writable}) 注入只读边界"
      pattern: "server\\.New\\("
    - from: "web/src/main.ts"
      to: "internal/proto/proto.go"
      via: "HELLO/WELCOME/ERROR = 0x48/0x57/0x45 与 proto 包手工对齐（D-16 注释模式）；子协议字符串 wesh.v1 同值"
      pattern: "0x48|0x57|0x45|wesh\\.v1"
    - from: "internal/server/e2e_test.go"
      to: "internal/server/server.go"
      via: "dialAndHandshake 统一收口全部 WS 测试的握手路径（Dial Subprotocols → Hello → Welcome.mode 断言）"
      pattern: "dialAndHandshake\\("
  prohibitions:
    - statement: "键盘输入不得在无任何只读可见指示的情况下被静默丢弃——ro 模式必须同时落地 disableStdin（前端层面不产生 onData）与标题 \"[ro] \" 前缀（用户可感知），仅服务端隐式丢帧是禁止形态（D-14 的用户可感知性要求）"
      status: unresolved
---

## 执行纪律（tracer 一次穿透 8 文件的有意取舍，不拆 plan）

本 plan 是 Phase 2 的 tracer：proto 契约 → 服务端握手/ro → CLI flag → 前端 → 测试收口改造，必须一次贯通（再拆则浏览器与服务端协议步调错位、中间态全红），故不拆 plan，以执行纪律替代：

1. **严格按 Task 1 → Task 2 → Task 3 顺序推进**，不并行、不跳跃（Task 2 依赖 Task 1 的常量与编解码签名；Task 3 依赖两者的线格式）。
2. **每个 Task 的 `<verify>` 全绿后立即提交一个原子 commit**，message 形如 `feat(02-01): <task 摘要>`。
3. **中断可恢复**：以 `git log` 确认最后完成的 Task，从下一 Task 的 `<read_first>` 重新进入；每个 Task 的 acceptance_criteria 独立可断言，即为恢复检查点。

<objective>
交付 Phase 2 的 tracer 切片：wesh.v1 握手（Hello/Welcome/Error）与默认只读端到端贯通——浏览器以 ['wesh.v1'] 子协议连接、首帧 Hello（含首尺寸 cols/rows）、服务端校验版本下发 Welcome(mode)，ro 丢弃 INPUT 放行 RESIZE，--writable 开启可写；前端 onclose 按码分派。合规关闭路径（1008+Error / 1002 无 Error）随握手一并落地。

Purpose: 握手与 ro/rw 是后续全部协议工作（守卫链/三层上限/保活）的承载骨架；tracer 先证明 proto→server→main→前端全链路成立，后续 plan 只做守卫、上限、保活与收口。
Output: 扩展的 proto 单一事实源 + 握手/ro 服务端 + --writable flag + 新前端 + 全量测试绿（含 Phase 1 回归改造）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-protocol/02-CONTEXT.md
@.planning/phases/02-protocol/02-RESEARCH.md
@.planning/phases/02-protocol/02-PATTERNS.md
@.planning/phases/02-protocol/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: proto 契约扩展——Subprotocol/'H'/'W'/'E'/Error 码表/关闭码纪律 + 编解码 + 单测</name>
  <reversibility rating="one-way">D-01 帧编码、D-02 未知字段忽略承诺、D-05 关闭码集合均为前后端公开契约——用户在 CONTEXT.md 已锁定，仅记录、不插入 checkpoint 重问（已决策的门不打断）。</reversibility>
  <files>internal/proto/proto.go, internal/proto/proto_test.go</files>
  <read_first>
    - internal/proto/proto.go（被改文件现状：包 doc 三段式行 1-11、帧常量块行 16-20、显式 json tag + Decode 返回 ok 惯例行 22-37、ClampDim 行 39-48）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-01/D-02/D-05/D-06/D-07/D-08 决策原文）
    - .planning/phases/02-protocol/02-RESEARCH.md（proto 骨架行 485-514；Pitfall 4 行 453-457）
    - .planning/phases/02-protocol/02-PATTERNS.md（§internal/proto/proto.go 段；§Shared Patterns 关闭码纪律段）
    - cmd/wesh/main_test.go（表驱动风格 analog，行 15-44）
  </read_first>
  <action>
    1. 改写 `internal/proto/proto.go` 包 doc 为三段式：帧格式（binary frame = 1 字节 ASCII 类型 + 载荷，控制帧载荷 JSON，D-01）/ 前端对齐（web/src/main.ts 帧常量手工对齐，D-16）/ 关闭码纪律（D-05 全集注释表：1000 正常、1001 服务端下线【Phase 7 启用，占位不实现】、1002 协议错误（未知帧/抢跑/畸形）、1008 策略违反（版本/hello_timeout）、1009 超限、1011 内部错误、1013 踢出可重试【Phase 5 启用，占位不实现】；1005/1006/1015 永不发送——库 validWireCloseCode 线上拒发，close.go:279-293；禁止发明新码或自定义 4000 段，D-08）。删除 Phase 1 的"Phase 2 预留"注释段（已被本实现取代）。
    2. 常量与类型（全部照 PATTERNS §proto 段与 RESEARCH 行 489-511 落定）：`const Subprotocol = "wesh.v1"`（D-03：子协议名 = Hello.version 期望值，同一字符串单一常量，RESEARCH A2）；帧类型块新增 `Hello = 'H'`（0x48, C→S, JSON）、`Welcome = 'W'`（0x57, S→C, JSON）、`Error = 'E'`（0x45, S→C, JSON），沿用现状 Input/Resize/Output 常量不动；'X' EXIT/'T' TITLE/'P' PREFS 以注释占位并标注启用 phase（D-08 同纪律）；`ErrVersionMismatch = "version_mismatch"`（+1008，发 Error 帧）、`ErrServerError = "server_error"`（+1011，发 Error 帧）（D-06/D-07）；`ModeRO = "ro"`、`ModeRW = "rw"`（Welcome.mode 取值单一事实源）。
    3. 编解码（沿用 resizePayload 显式 json tag + 包内唯一事实源原则）：`type Hello struct { Version string \`json:"version"\`; Cols int \`json:"cols"\`; Rows int \`json:"rows"\` }`；`func DecodeHello(payload []byte) (Hello, error)`——json.Unmarshal 进 struct，**未知字段忽略是 stdlib 默认行为（D-02 演化承诺），禁止引入 DisallowUnknownFields 或任何严格校验器**；`func EncodeWelcome(mode string) []byte`——组 Welcome{Version: Subprotocol, Mode: mode} 的 JSON（json.Marshal 对该固定 struct 不可失败，直接返回字节）；`type ErrorMsg struct { Code string \`json:"code"\`; Message string \`json:"message"\` }`（命名避开内建 error 与帧常量 Error）；`func EncodeError(code, message string) []byte`。
    4. 新建 `internal/proto/proto_test.go`（package proto 内测，表驱动仿 main_test.go:15-44 风格）：
       - `TestHelloIgnoresUnknown`（D-02，VALIDATION 行 665）：DecodeHello 输入 `{"version":"wesh.v1","cols":80,"rows":24,"ticket":"abc","future":true}`，断言无 error 且 Version/Cols/Rows 正确解析。
       - `TestCloseCodeSet`（D-05/D-08，VALIDATION 行 664）：经 runtime.Caller(0) 定位本文件推导仓库根，读 `internal/server/server.go` 源码，正则提取全部 `.Close(websocket.StatusXxx` 调用点的常量名，断言每个 ∈ {NormalClosure, ProtocolError, PolicyViolation, MessageTooBig, InternalError}（对应 1000/1002/1008/1009/1011）；另断言 Close 调用行内不存在 1005/1006/1015 数字字面量与 4xxx 自定义码。
       - 编解码往返表驱动：合法 Hello 解析成功且字段相等；坏 JSON 返回 error；EncodeWelcome("ro") 产物含 `"wesh.v1"` 与 `"ro"`；EncodeError("version_mismatch", ...) 产物含 code 与 message 原文。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test ./internal/proto -count=1 -v && go vet ./internal/proto && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/proto -count=1` 退出 0：TestCloseCodeSet、TestHelloIgnoresUnknown 与编解码往返全绿（VALIDATION 行 664-665 两条落位）
    - proto.go 含 `Subprotocol = "wesh.v1"`、`Hello = 'H'`、`Welcome = 'W'`、`Error = 'E'`、`ErrVersionMismatch = "version_mismatch"`、`ErrServerError = "server_error"`、`ModeRO = "ro"`、`ModeRW = "rw"` 常量与 DecodeHello/EncodeWelcome/EncodeError 三个函数
    - 关闭码注释表覆盖 D-05 全集 {1000,1001,1002,1008,1009,1011,1013}，1001/1013 明确标注启用 phase 且占位不实现（D-08）
    - proto.go 中不存在任何"严格未知字段拒绝"机制的调用（D-02；行为由 TestHelloIgnoresUnknown 锁定而非源码 grep）
    - 'X'/'T'/'P' 仅以注释占位，无任何编码/解码实现（D-08/deferred：语义实现分属 Phase 6/4）
  </acceptance_criteria>
  <done>proto 单一事实源扩展完成：握手三帧、错误码表、关闭码纪律注释表与编解码落地，两个 VALIDATION 单测绿。</done>
</task>

<task type="tracer">
  <name>Task 2: 服务端握手状态机 + ro 边界 + --writable（tracer 切片：Accept→Hello→Welcome→mode 全链路）</name>
  <reversibility rating="one-way">D-15 --writable flag 名是公开 CLI 契约（发布后改名破坏脚本与文档）——用户已锁定，仅记录不重问。</reversibility>
  <files>internal/server/server.go, cmd/wesh/main.go, internal/server/e2e_test.go, internal/server/handshake_test.go, cmd/wesh/main_test.go</files>
  <read_first>
    - internal/server/server.go（被改文件现状：Attach 行 72-117、Server 结构体原子态行 24-38、onChunk 行 122-131、生命周期收口行 135-173）
    - cmd/wesh/main.go（被改文件现状：config 行 22-26、parseArgs 行 31-51、run 行 53-84、http.Serve 行 79）
    - internal/server/e2e_test.go（被改文件现状：startTestServer 行 106-122、waitExit 行 125-135、CloseError 读取循环行 267-278、五个既有测试）
    - cmd/wesh/main_test.go（被改文件现状：TestParseArgs 表驱动行 15-44）
    - .planning/phases/02-protocol/02-RESEARCH.md（Pattern 2 握手状态机行 226-269；升档序列行 269；Pitfall 5 行 459-462）
    - .planning/phases/02-protocol/02-PATTERNS.md（§internal/server/server.go 段——握手状态机/升档序列/ro 分支/组帧纪律；§Shared Patterns 原子态惯例、exitf 收口、ctx 纪律）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-03/D-04/D-06/D-07/D-13/D-14/D-15）
    - 本 plan Task 1 产物：internal/proto/proto.go（Subprotocol/Hello/Welcome/Error 常量与 DecodeHello/EncodeWelcome/EncodeError 签名）
  </read_first>
  <action>
    1. `internal/server/server.go` 结构体与装配：新增 `type Options struct { Writable bool }`（pingInterval 字段由 plan 02-04 加入，本 plan 不预留）；`New(sess, exitf, opts Options)` 替换现签名（`New(sess *pty.Session, exitf func(int), opts Options) *Server`），opts 存为 Server 字段；New 的两个 goroutine 启动点（ReadLoop drain / lifecycle）与注释纪律不变。
    2. Attach 重构为握手状态机（照 RESEARCH Pattern 2 行 237-266 与 PATTERNS §server 段，本 plan 不含守卫链/两档 ReadLimit/5s 计时器——那是 plan 02-02/02-03 的扩展点）：409 CAS 原子门保持首位（Phase 1 既有）→ `websocket.Accept(w, r, &websocket.AcceptOptions{Subprotocols: []string{proto.Subprotocol}})`（一行开启子协议协商，D-03；不设 InsecureSkipVerify——库默认 Origin 同源校验保留；压缩默认禁用不动，D-17）→ Accept 失败保持现状释放门位 → `if c.Subprotocol() != proto.Subprotocol { c.CloseNow(); s.attached.Store(false); return }`（D-03 assert 兜底，理论不可达）→ defer c.CloseNow()（现状保留）→ ctx 由 context.Background() 派生（禁止 r.Context()，现状注释同源）。
    3. 握手函数（Attach 内联或拆 `func (s *Server) handshake(c *websocket.Conn, ctx context.Context) (*proto.Hello, bool)`，planner 自由度内推荐拆函数）：以现状 `c.Read(ctx)` 读首条消息（readCounted 替换属 plan 02-03，本 plan 保持 c.Read——库默认 32KiB 读限仍为基线）→ 非 binary / 空帧 / data[0] != proto.Hello → `c.Close(websocket.StatusProtocolError, "unexpected_frame")` 返回 false（1002 抢跑帧，无 Error 帧，D-04/D-06）→ `proto.DecodeHello(data[1:])` 出错或 `h.Version != proto.Subprotocol` → 先写 Error 帧（局部组帧 `append([]byte{proto.Error}, proto.EncodeError(proto.ErrVersionMismatch, "Protocol version mismatch. This server speaks wesh.v1.")...)`，c.Write binary）→ 再 `c.Close(websocket.StatusPolicyViolation, proto.ErrVersionMismatch)`（1008，reason 与 code 同名机器串，D-07；先 graceful Close 写关闭帧、CloseNow 仅 defer 兜底——Pitfall 5：CloseNow 会丢未刷出的 Error 帧）→ 返回 false。
    4. 握手成功后的升档序列（顺序敏感，RESEARCH 行 269 的本 plan 子集）：`s.sess.Resize(h.Cols, h.Rows)`（Hello 携首尺寸，消除 80x24 首帧窗口；ClampDim 已在 DecodeHello 之外——注意 DecodeHello 不钳制，这里直接传 h.Cols/h.Rows，sess.Resize 内部无钳制，故此处须 proto.ClampDim 两处各钳一次）→ 写 Welcome 帧（局部组帧 `append([]byte{proto.Welcome}, proto.EncodeWelcome(mode)...)`；mode = proto.ModeRW 若 opts.Writable 否则 proto.ModeRO；**禁止复用 s.frame**——那是 onChunk 在 ReadLoop goroutine 的组帧缓冲，握手跑在 Attach goroutine，共享即数据竞争）→ `s.conn.Store(c)` + `defer s.conn.Store(nil)`（Welcome 发出后再发布写端，OUTPUT 不会抢在 Welcome 前）。
    5. 数据面 switch（现状行 105-115 骨架扩展，c.Read 循环保持）：`case proto.Input:` 加 ro 分支——`if s.opts.Writable { s.sess.Master.Write(data[1:]) }`（ro 丢弃，D-13 安全边界在服务端）；`case proto.Resize:` 不动（ro 放行，D-13）；default 1002 不动。
    6. `cmd/wesh/main.go`：config 加 `writable bool`；parseArgs 注册 `fs.BoolVar(&cfg.writable, "writable", false, "allow client input (default read-only)")`（D-15 逐字 help，全名无短选项）；run 内 `server.New(sess, os.Exit, server.Options{Writable: cfg.writable})`；http.Serve 行本 plan 不动（ReadHeaderTimeout 属 plan 02-02）。
    7. `cmd/wesh/main_test.go`：TestParseArgs 表驱动加一行——`["--writable", "--", "bash"]` → cfg.writable==true；默认行断言 cfg.writable==false。
    8. `internal/server/e2e_test.go` 改造（RESEARCH 行 668 统一收口）：新 helper `dialAndHandshake(t *testing.T, wsURL, wantMode string) *websocket.Conn`——Dial 带 `&websocket.DialOptions{Subprotocols: []string{proto.Subprotocol}}` → 发 Hello 帧（`append([]byte{proto.Hello}, json {version: proto.Subprotocol, cols: 80, rows: 24}...)`）→ 读首帧断言 data[0]==proto.Welcome，json 解析断言 version=="wesh.v1" 且 mode==wantMode → 返回 conn；startTestServer 加 opts 参数变体（`startTestServerOpts(t, argv, opts server.Options)`，原 startTestServer 改为默认 Options{} 的薄封装——ro 为默认）。逐测改造：TestEchoPTY **改名 TestWritableEcho**（VALIDATION 行 661：startTestServerOpts(..., Options{Writable: true}) + dialAndHandshake(..., "rw")，echo 断言与 exitf(0) 断言不变）；TestSecondClient409——第一连接 dialAndHandshake(..., "rw")（Writable: true 配置，echo 断言保留），第二次 Dial 仍断言 HTTP 409（409 CAS 在守卫链之先的现状顺序下，第二连接无论子协议均被 409）；TestExitCodePropagation——Writable: true + handshake("rw")，INPUT 'x\n' 触发 helper exit42，1000→exitf(42) 断言不变；TestUnknownFrame1002——默认 ro 即可（未知帧检查独立于 mode），handshake("ro") 后发 '9' 帧，1002 断言不变；TestClientDisconnectSIGHUP——默认 ro（无 INPUT 需求），handshake("ro") 后等 READY 再 CloseNow，断言不变；TestDrainBeforeAttach 无 WS 不动。
    9. e2e_test.go 新增 CORE-04 两测（VALIDATION 行 659-660）：`TestReadOnlyDropsInput`——默认 ro 装配 /bin/cat，handshake 断言 mode=="ro"，发 INPUT "x"，断言 500ms 窗口内无任何 OUTPUT 帧到达且连接存活（随后正常关闭收口 exitf(0)）；`TestReadOnlyAllowsResize`（D-13）——argv `["/bin/sh", "-c", "sleep 0.5; stty size; sleep 5"]`（测试以 sh 为被测命令合法——wesh 的 argv 透传纪律不限制用户命令本身），默认 ro，handshake 后发 RESIZE {cols:132, rows:43}，读 OUTPUT 直至含 "43 132"（stty 在 RESIZE 应用后打印，0.5s 睡眠给足握手+Resize 时序余量，总超时 5s 护栏）。
    10. 新建 `internal/server/handshake_test.go`（package server_test，复用 e2e_test.go helper）：`TestVersionMismatch`（D-06/D-07）——dial 带子协议，发 Hello{version:"wesh.v2"}：先读到 'E' 帧且 JSON code=="version_mismatch"、message 非空，随后读循环收 CloseError 断言 Code==1008 且 Reason=="version_mismatch"；`TestPrematureFrame`（D-04/D-06，VALIDATION 行 658）——dial 带子协议后直接发 INPUT 帧（无 Hello）：收 CloseError 断言 Code==1002、Reason=="unexpected_frame"，且全程读到的帧无一 data[0]=='E'（攻击面不发 Error 帧）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go vet ./... && go test ./... -count=1 && go test ./internal/server -count=1 -v -run 'TestWritableEcho|TestReadOnlyDropsInput|TestReadOnlyAllowsResize|TestVersionMismatch|TestPrematureFrame|TestSecondClient409|TestExitCodePropagation|TestUnknownFrame1002|TestClientDisconnectSIGHUP' && go test ./cmd/wesh -count=1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -count=1` 与 `go vet ./...` 退出 0（Phase 1 回归全部经 dialAndHandshake 改造后保持绿）
    - 五个新/改名测试全绿：TestWritableEcho（VALIDATION 行 661）、TestReadOnlyDropsInput（行 659）、TestReadOnlyAllowsResize（行 660）、TestVersionMismatch、TestPrematureFrame（行 658）
    - server.go 中 `websocket.AcceptOptions` 仅设置 Subprotocols 字段；`grep -A3 'websocket.AcceptOptions{' internal/server/server.go` 输出含 Subprotocols 且不含其他选项字段
    - server.go 握手成功路径的调用顺序为 sess.Resize → 写 Welcome 帧 → conn.Store（OUTPUT 不抢在 Welcome 前）；Welcome/Error 组帧均为函数局部缓冲，未共享 s.frame
    - `grep -c 'server.New' cmd/wesh/main.go` ≥ 1 且调用带 `server.Options{Writable: cfg.writable}`；main.go 注册 --writable 且 help 文本恰为 "allow client input (default read-only)"（D-15）
    - TestReadOnlyDropsInput 断言 Welcome.mode=="ro"、INPUT 发出后 500ms 内零 OUTPUT 且连接存活（D-13 服务端边界证明）
    - TestPrematureFrame 断言 1002 + reason "unexpected_frame" 且线上无 'E' 帧（D-06）；TestVersionMismatch 断言先收 'E' 帧 code=="version_mismatch" 后收 1008 且 Reason=="version_mismatch"（D-07）
    - e2e_test.go 中不再有名为 TestEchoPTY 的函数（已改名 TestWritableEcho）；全部 WS Dial 路径均经 dialAndHandshake 或显式带 Subprotocols 的负例 Dial
  </acceptance_criteria>
  <done>tracer 全链路绿：浏览器（Task 3 接入前为库客户端）以 wesh.v1 完成 Hello/Welcome 握手，ro 丢弃 INPUT、RESIZE 放行、--writable 回显，1008/1002 合规关闭路径锁定，整仓测试绿。</done>
</task>

<task type="auto">
  <name>Task 3: 前端握手接入——子协议 + Hello 首帧 + Welcome 驱动 ro/rw + onclose 按码分派</name>
  <files>web/src/main.ts</files>
  <read_first>
    - web/src/main.ts（被改文件现状：帧常量行 6-9、concat 行 68-76、onmessage 行 79-84、onData 行 89-93、sendResize 行 98-102、onopen 行 111-116、showStatus 行 120-136、onerror/onclose 行 138-169）
    - .planning/phases/02-protocol/02-RESEARCH.md（Pattern 5 行 351-404——逐段照抄骨架；Pitfall 3 行 447-451）
    - .planning/phases/02-protocol/02-PATTERNS.md（§web/src/main.ts 段——onopen/onmessage/onData/onclose 各改造点；§Shared Patterns 前端文案纪律）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-03/D-13/D-14/D-12①；specifics 行 111 文案全英文）
    - 本 plan Task 1 产物：internal/proto/proto.go（'H'/'W'/'E' 与 wesh.v1 取值）
  </read_first>
  <action>
    照 RESEARCH Pattern 5（行 353-404）扩展 `web/src/main.ts`（全部沿用现状 concat/showStatus/enc 件，文案全英文）：
    1. 帧常量块（现状行 6-9）新增 `HELLO = 0x48`、`WELCOME = 0x57`、`ERROR = 0x45`，对齐注释同步更新（'H' Hello / 'W' Welcome / 'E' Error，与 proto 手工对齐）。
    2. WS 构造（现状行 65）：`new WebSocket('ws://' + location.host + '/ws', ['wesh.v1'])`（D-03）。
    3. 新增模块态：`let mode: 'ro' | 'rw' = 'rw'`（Welcome 到达前的乐观默认，服务端 Welcome 为准）与 `let lastError: { code: string; message: string } | null = null`（Error 帧暂存，onclose 展示，D-07）。
    4. `ws.onopen`：opened=true → fit.fit() → **发 Hello 首帧取代原首帧 sendResize**（`ws.send(concat(new Uint8Array([HELLO]), enc.encode(JSON.stringify({ version: 'wesh.v1', cols: term.cols, rows: term.rows }))))`——Hello 携带首尺寸，服务端握手即 Resize，消除 80x24 首帧窗口；后续 resize 仍走 onResize→sendResize 回路不动）→ term.focus()。
    5. `ws.onmessage` 由单 if 扩展为 switch（buf[0] 分派）：`case OUTPUT:` term.write 不动；`case WELCOME:` JSON.parse（TextDecoder 解 subarray(1)）→ `mode = w.mode === 'ro' ? 'ro' : 'rw'`；mode==='ro' 时 `term.options.disableStdin = true`（D-14，xterm.d.ts:92 已 VERIFIED 存在该选项）且 `document.title = '[ro] ' + document.title`（零新 UI 组件）；`case ERROR:` `lastError = JSON.parse(...)`（暂存不展示，onclose 统一展示）；`default: break`（未知 S→C 类型忽略，前向兼容，D-02 同纪律）。
    6. `term.onData` 发送门叠加第二道闸：`ws.readyState === WebSocket.OPEN && mode === 'rw'` 才发 INPUT（disableStdin 之外的前端冗余闸；真防线在服务端丢 INPUT，D-13）。
    7. `ws.onclose` 由三分法扩展为 per-code switch（showStatus 三态面板与既有文案风格沿用，具体措辞属 discretion 但必须满足下列要点）：`!opened` 分支保持现状 "Unable to connect" 文案（覆盖子协议缺失 400/429/409 场景）；`case 1000:` 保持 "Session ended" 现状文案；`case 1008:` 标题 "Connection refused"，正文优先 `lastError?.message` 否则 "The server refused this connection."；`case 1009:` 标题 "Message too large"，正文 "Input exceeded the server message size limit and the connection was closed."（D-12①：不提任何 flag——本 phase 无可调 flag）；`case 1011:` 标题 "Server error"，正文优先 `lastError?.message` 否则 "The server hit an internal error."；`case 1013:` 标题 "Disconnected"，正文 "The server asked this client to retry later."（Phase 5 占位路径的防御性文案）；`default:` "Connection lost" 现状文案（含 1002 与无码异常断开——1006 线上永不到达，不得以 1006 作分派依据，用 !ev.wasClean/default 覆盖）。
    8. `pnpm -C web build` 重建 dist（真实产物覆盖，embed 链以新 HTML 重建验证 `go build ./...`）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && pnpm -C web build && test -f web/dist/index.html && test -f web/dist/index.html.gz && go build ./... && go vet ./... && grep -q "wesh.v1" web/src/main.ts && grep -q "disableStdin" web/src/main.ts && echo OK</automated>
    <human-check>浏览器手动验证（ro 标题前缀、键盘无响应、1009 文案、ping/pong 帧面板）汇总在 plan 02-05 收口；本任务完成后可先行自测：`go run ./cmd/wesh -- bash`（ro：键盘无响应、标题 [ro] 前缀）与 `go run ./cmd/wesh --writable -- bash`（可输入）。</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm -C web build` 退出 0 且 web/dist/index.html 与 .gz 均存在；`go build ./...` 在真实 dist 下退出 0（embed 链重建，D-18 沿用）
    - main.ts 含 `new WebSocket(..., ['wesh.v1'])`（D-03）与 HELLO/WELCOME/ERROR = 0x48/0x57/0x45 常量及对齐注释
    - onopen 发送的首帧为 HELLO 帧且 JSON 含 version:"wesh.v1"、cols、rows（原首帧 sendResize 调用已移除，resize 回路 onResize→sendResize 保留）
    - WELCOME 分支含 `term.options.disableStdin = true` 与 `'[ro] ' + document.title`（D-14）；onData 发送门含 `mode === 'rw'` 条件
    - onclose 含 1000/1008/1009/1011/1013 五个 case 与 default 分支；1009 文案不含任何 flag 名（D-12①）；1008/1011 文案优先取 lastError?.message（D-07）
    - ERROR 分支将帧体解析存入 lastError；onmessage 的 default 分支为空 break（未知类型忽略）
    - main.ts 全文不含以 1006 为判断条件的分派（1006 永不作为文案分派依据）
  </acceptance_criteria>
  <done>浏览器完成 wesh.v1 握手：Hello 首帧、Welcome 驱动 ro/rw（disableStdin + [ro] 标题）、onclose 按码人话分派；dist 为真实构建产物，embed 链重建绿。</done>
</task>

</tasks>

## Flagged Assumptions（spec-less probe fallback §A/§C——显式呈现，绝不静默丢弃）

本阶段无 SPEC ## Edge Coverage；edge probe（deterministic）对本阶段 4 条需求全部返回 `unclassified`。按规则保持 `unresolved`——绝不 auto-resolve、绝不加 backstop——按需求归属分摊到各 plan 显式登记。本 plan 登记 CORE-04 一行：

| Requirement | probe 结果 | 状态 | 处置 |
|-------------|-----------|------|------|
| CORE-04 | unclassified — review manually | unresolved | 以 RESEARCH §Validation Architecture 行 659-661 三条验收准则为锚点（TestReadOnlyDropsInput / TestReadOnlyAllowsResize / TestWritableEcho，本 plan Task 2 落位） |

§B prohibition recall（两阶段：对抗召回 → 精确化）——CORE-04 的存活 prohibition 一条（transparency：ro 不得无可见指示地静默吞输入），已 authoring 进本 plan `must_haves.prohibitions`（descriptor-less，无 check_* 字段——下游 dispose 为 flagged-unverified，never auto-dismiss）。canon 安全项（拒绝面信息泄露等 OWASP 类）按规则不 mint，留 breadcrumb：由本 plan 与 02-02 threat_model 覆盖。prohibition recall surfaced 1 条 = authored 1 条 ✓。

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 ↔ WS/HTTP | 不可信客户端输入（Hello/INPUT/RESIZE 帧）从此进入；Phase 2 仍无认证（Phase 3 收口） |
| WS 握手层 | 子协议协商与 Hello 版本断言是协议演化与攻击面分治的第一道闸 |
| 服务端 → 子进程 | ro/rw 边界决定客户端输入能否抵达 PTY master |
| npm/go 依赖安装 | 本 plan 零新增依赖（Phase 1 审计沿用） |

## STRIDE Threat Register（ASVS L1，block on high）

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-01 | Tampering | server.go 握手/数据面分派 | high | mitigate | Hello 首帧强制（非 'H' 即 1002）+ version 等值断言（≠wesh.v1 即 1008）+ 未知帧 1002 默认分支（Phase 1 既有）；TestVersionMismatch/TestPrematureFrame/TestUnknownFrame1002 锁定 |
| T-02-01-02 | Elevation of Privilege | 子协议协商（库不匹配仍 101，accept.go:141-144 VERIFIED） | high | mitigate | AcceptOptions.Subprotocols + c.Subprotocol() assert 双保险（本 plan）；HTTP 400 预检由 plan 02-02 补全最早拦截（D-03 双闸）；TestSubprotocolRequired 在 02-02 落位 |
| T-02-01-03 | Information Disclosure | Error 帧反馈面 | medium | mitigate | D-06 受众分治：仅 version_mismatch/server_error 发 Error 帧；抢跑/未知帧只发关闭码（TestPrematureFrame 断言线上无 'E' 帧）——不给攻击者反馈面 |
| T-02-01-04 | DoS | 预认证窗口资源占用 | medium | mitigate | 本 plan 保持库默认 32768 读限基线（read.go:107）；4KiB 预认证档与 400/429/5s 守卫链由 plan 02-02/02-03 到位（phase 末收口，中间态不发布） |
| T-02-01-05 | Tampering | Hello schema 演化纪律被破坏（严格化回归） | low | mitigate | D-02 由 stdlib Unmarshal 默认忽略承载 + TestHelloIgnoresUnknown 行为锁定 |
| T-02-01-06 | Elevation of Privilege | ro 边界旁路（前端不发但服务端照收） | high | mitigate | 服务端 ro 丢弃 INPUT 是真防线（D-13），前端 disableStdin 仅 UX 层；TestReadOnlyDropsInput 断言 ro 下 INPUT 零效果 |
| T-02-01-SC | Tampering | 依赖安装 | high | mitigate | 本 plan 零新增第三方依赖（RESEARCH §Package Legitimacy Audit：无适用对象）；go.mod/go.sum 与 web/package.json 依赖段不变 |
</threat_model>

<verification>
- `go vet ./... && go test ./... -count=1` 全绿（含 Phase 1 回归改造）
- `go test ./internal/proto -count=1` 绿（TestCloseCodeSet / TestHelloIgnoresUnknown / 编解码往返）
- `go test ./internal/server -run 'TestWritableEcho|TestReadOnlyDropsInput|TestReadOnlyAllowsResize|TestVersionMismatch|TestPrematureFrame' -count=1` 绿（tracer 端到端证明）
- `pnpm -C web build` 绿且 `go build ./...` 以真实 dist 重建成功
</verification>

<success_criteria>
- ROADMAP 准则 2 的 ro 部分成立：默认只读下浏览器键盘输入被丢弃，--writable 开启后输入生效（CORE-04）
- 握手契约成立：wesh.v1 子协议 + Hello/Welcome/Error 三帧 + 1008/1002 合规关闭路径（D-01~D-08、D-13~D-15 落地）
- 本 plan 的 must_haves.truths 全部可由 acceptance_criteria 对应断言证明
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

修改/新建文件与新增符号（Go module `github.com/sworda/wesh` 之下）：
- `internal/proto/proto.go`（扩展）：`Subprotocol = "wesh.v1"`；`Hello = 'H'` / `Welcome = 'W'` / `Error = 'E'`（0x48/0x57/0x45）；`ErrVersionMismatch = "version_mismatch"` / `ErrServerError = "server_error"`；`ModeRO = "ro"` / `ModeRW = "rw"`；`type Hello struct{Version,Cols,Rows}`；`func DecodeHello(payload []byte) (Hello, error)`；`func EncodeWelcome(mode string) []byte`；`type ErrorMsg struct{Code,Message}`；`func EncodeError(code, message string) []byte`；关闭码 D-05 全集注释表（1001/1013 占位不实现）
- `internal/proto/proto_test.go`（新建）：`TestCloseCodeSet`、`TestHelloIgnoresUnknown`、编解码往返表驱动测试
- `internal/server/server.go`（重构）：`type Options struct { Writable bool }`；`New(sess *pty.Session, exitf func(int), opts Options) *Server`（签名变更）；`(*Server).Attach` 握手状态机（Subprotocols 协商 + assert + Hello 校验 + Welcome 下发 + 首尺寸 Resize + ro INPUT 丢弃分支）
- `cmd/wesh/main.go`（扩展）：config 字段 `writable bool`；CLI flag `--writable`（bool，默认 false，help "allow client input (default read-only)"）
- `cmd/wesh/main_test.go`（扩展）：TestParseArgs 新增 --writable 用例行
- `internal/server/e2e_test.go`（改造）：`dialAndHandshake(t, wsURL, wantMode string) *websocket.Conn` helper；`startTestServerOpts(t, argv, opts server.Options)`；`TestWritableEcho`（TestEchoPTY 改名改造）/ `TestReadOnlyDropsInput` / `TestReadOnlyAllowsResize`；TestSecondClient409/TestExitCodePropagation/TestUnknownFrame1002/TestClientDisconnectSIGHUP 四测握手改造
- `internal/server/handshake_test.go`（新建）：`TestVersionMismatch`、`TestPrematureFrame`
- `web/src/main.ts`（扩展）：常量 `HELLO/WELCOME/ERROR`（0x48/0x57/0x45）；`mode: 'ro'|'rw'` 与 `lastError` 模块态；WS 构造第二参 `['wesh.v1']`；onopen Hello 首帧；onmessage WELCOME/ERROR 分支；onData `mode === 'rw'` 闸；onclose per-code 分派（1000/1008/1009/1011/1013/default）
- `web/dist/index.html`（构建产物刷新）与 `web/dist/index.html.gz`

<output>
Create `.planning/phases/02-protocol/02-01-SUMMARY.md` when done.
</output>

