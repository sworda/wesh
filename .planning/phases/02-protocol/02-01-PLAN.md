---
phase: 02-protocol
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/proto/proto.go
  - internal/proto/proto_test.go
autonomous: true
requirements:
  - CORE-04
  - SEC-08
  - RES-01

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "proto 包是 wesh.v1 协议唯一事实源：帧类型 'H' Hello / 'W' Welcome / 'E' Error 与既有 '0' INPUT/OUTPUT / '1' RESIZE 同块定义，前端 main.ts 常量手工对齐（D-01/D-16）"
    - "Subprotocol 常量 \"wesh.v1\" 同时是 HTTP 预检 token、AcceptOptions 协商值与 Hello.version 期望值——三处同源（D-03，Open Question 3 裁决）"
    - "Hello 解码对未知字段一律忽略（json.Unmarshal 默认行为，禁止 DisallowUnknownFields）——Phase 3 加 ticket、Phase 5 加 attach/mode 只是加字段（D-02）"
    - "Error 帧 schema 为 {code, message}：code 为 snake_case 机器串，本期仅 version_mismatch(1008) 与 server_error(1011) 两个正常客户端可见码（D-06/D-07）"
    - "关闭码注释表完整记录 D-05 全集 {1000,1001,1002,1008,1009,1011,1013}：1001/1013 占位不实现并标注启用 phase，1005/1006/1015 永不发送"
    - "上限常量 ReadLimitPreAuth=4096 / ReadLimitPostAuth=16384 进 proto 包并注释标定依据；分片数上限仅保留注释位（值 32），标注库不暴露 API、依赖等效防线（D-09 2026-08-15 修订、D-10）"
  prohibitions:
    - "协议层不得发明 D-05 集合之外的关闭码，不得使用自定义 4000 段私码——应用层超限一律复用 1009（D-08 纪律）"
  artifacts:
    - path: "internal/proto/proto.go"
      provides: "wesh.v1 协议契约：帧类型/子协议/Error code/上限常量 + Hello/Welcome/Error 编解码"
      contains: "Subprotocol / Hello / Welcome / Error / ReadLimitPreAuth / DecodeHello"
    - path: "internal/proto/proto_test.go"
      provides: "编解码往返 + 未知字段忽略 + 常量形状测试"
      contains: "TestDecodeHello"
  key_links:
    - from: "internal/proto/proto.go"
      to: "web/src/main.ts"
      via: "帧常量与子协议字符串前后端手工对齐，两侧注释互相指路（D-16 注释模式）"
      pattern: "wesh\\.v1"
    - from: "internal/proto/proto.go"
      to: "internal/server/server.go"
      via: "DecodeHello / WelcomeFrame / ErrorFrame / ReadLimitPreAuth / ReadLimitPostAuth 被 Attach 握手段调用（02-02 落地）"
      pattern: "proto\\.(DecodeHello|WelcomeFrame|ErrorFrame)"
---

<objective>
落地 wesh.v1 协议契约到 `internal/proto` 单一事实源：'H'/'W'/'E' 类型字节、Subprotocol 常量、Error code 表、关闭码注释表（1001/1013 占位）、两档读上限常量（含 D-09 修订的分片层注释位），以及 Hello/Welcome/Error 的 JSON 编解码函数。本 plan 只动 proto 包，不触碰 server/前端——契约先钉死，后续 plan 全部建立在这份接口上。

Purpose: 协议编码、版本常量与关闭码集合是前后端公开契约（D-01/D-02/D-05 均 one-way），必须在任何实现任务前一次定稿；独立成 plan 使契约获得专属测试与评审窗口。
Output: 扩展后的 proto.go + 新建 proto_test.go，`go test -race ./internal/proto/` 全绿。

规格探针标记（spec-less probe fallback）：RES-01 / SEC-08 / CORE-04 的边界在 SPEC 缺席下由探针报为 unclassified；本 plan 的边界定义已按 D-09 修订裁决（两层硬顶 + 等效防线）与 D-02/D-06/D-08 显式落入 must_haves.truths，无静默丢弃。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-protocol/02-CONTEXT.md
@.planning/phases/02-protocol/02-RESEARCH.md
@.planning/phases/02-protocol/02-PATTERNS.md
@internal/proto/proto.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: proto.go 扩展——wesh.v1 全量契约（常量 + payload 类型 + 编解码函数）</name>
  <reversibility rating="one-way">D-01 帧编码 / D-02 未知字段忽略承诺 / D-05 关闭码集合均为前后端公开契约，上线后变更即协议破坏；用户在 CONTEXT.md 已裁决锁定，不再设 checkpoint，仅留记录。</reversibility>
  <files>internal/proto/proto.go</files>
  <read_first>
    - internal/proto/proto.go（被改文件现状：帧常量块 16-20 行、Decode+ok 惯例 28-37 行、ClampDim 39-48 行、包 doc 1-12 行）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-01~D-12 全部契约决策，行 20-39；specifics 行 106-114）
    - .planning/phases/02-protocol/02-RESEARCH.md（§Code Examples 帧类型与关闭码常量 388-402 行、Hello/Welcome/Error JSON 404-421 行）
    - .planning/phases/02-protocol/02-PATTERNS.md（§proto.go 扩展段 57-117 行：包 doc 三段式与常量块组织）
  </read_first>
  <behavior>
    - DecodeHello(`{"version":"wesh.v1","cols":80,"rows":24}`) → ok=true，Version="wesh.v1"，Cols=80，Rows=24
    - DecodeHello 混入未知字段（如 `"ticket":"x"`、`"attach":1`）→ 仍 ok=true（D-02 演化纪律）
    - DecodeHello(畸形 JSON) → ok=false；DecodeHello(cols=0 或 9999) → ClampDim 钳制到 [1,1000]
    - WelcomeFrame("ro") → 首字节 'W'(0x57) + JSON `{"mode":"ro"}`；ErrorFrame("version_mismatch", msg) → 首字节 'E'(0x45) + JSON `{"code":...,"message":...}`
  </behavior>
  <action>
    在 internal/proto/proto.go 现状上扩展（组织方式沿用现状常量块/显式 json tag/Decode 返回 ok 三惯例，PATTERNS 57-117 行）：
    1. 包 doc 改写为三段式：帧格式（binary = 1 字节 ASCII 类型 + 载荷，控制帧载荷 JSON，D-01）；前端 web/src/main.ts 常量与本文件手工对齐互相指路（D-16）；关闭码纪律（D-05 全集 {1000,1001,1002,1008,1009,1011,1013}，发送侧值直接用库常量 websocket.StatusNormalClosure/StatusProtocolError/StatusPolicyViolation/StatusMessageTooBig/StatusInternalError；1001 优雅下线 Phase 7 启用、1013 背压踢出 Phase 5 启用，本期占位不实现（D-08）；1005/1006/1015 永不发送，库层 validWireCloseCode 兜底；禁止自定义 4000 段）。删除现状 6-8 行的 Phase 2 预留注释（已被本扩展实现）。
    2. 帧常量块新增：Hello = 'H'（0x48, C→S, JSON {version, cols, rows}）、Welcome = 'W'（0x57, S→C, JSON {mode}）、Error = 'E'（0x45, S→C, JSON {code, message}）；同块注释占住 'X' EXIT / 'T' TITLE / 'P' PREFS（类型字节本 phase 占住，语义分属 Phase 6/4，D-01）。既有 Input/Resize/Output 常量不动。
    3. 新增 Subprotocol = "wesh.v1" 字符串常量，注释：D-03 子协议 token = Hello.version 期望值 = 前端 new WebSocket 第二参，三处复用同一常量（Open Question 3 裁决，双写漂移面最小）。
    4. 新增 Error code 常量：ErrVersionMismatch = "version_mismatch"（正常客户端可见，发 Error 帧 + 1008）、ErrServerError = "server_error"（发 Error 帧 + 1011）；注释引 D-06/D-07 受众分治纪律（攻击面路径 unknown_frame/抢跑/超限/hello_timeout 只发关闭码不发 Error 帧，不给攻击者反馈面）与 D-07 close reason 同名机器串 ≤123 字节约定；auth_failed/permission_denied 属 Phase 3/5（deferred，不写）。
    5. 新增上限常量：ReadLimitPreAuth = 4 * 1024（D-11：Accept 后 Hello 前的预认证读上限；Hello JSON 实测 ~45-100 字节，余量两个数量级，SEC-08 预认证窗口内存最小化）；ReadLimitPostAuth = 16 * 1024（D-09 修订：Hello 完成后稳态读上限，单帧与累积字节同由 SetReadLimit 库执行，超限库自动 1009）。上限数值注释标定依据：合法 C→S 流量极小（键盘 INPUT 字节级、RESIZE/Hello JSON <200B、粘贴几 KB），浏览器 WS API 不产生分片。紧邻注释放分片数上限注释位（值 32）：标注"coder/websocket 不暴露分片计数 API（read.go:457-479 空帧内部吞掉），本层由等效防线覆盖——两层字节硬顶 + 预认证三道闸 + 409 单客户端门；Bandit CVE-2026-65623 官方修复同为 running byte count 而非分片计数"（D-09 2026-08-15 用户裁决修订）。不创建分片数可执行常量。
    6. 新增 payload 类型（显式 json tag 防字段名漂移）：HelloPayload{Version string `json:"version"`; Cols int `json:"cols"`; Rows int `json:"rows"`}；WelcomePayload{Mode string `json:"mode"`}；ErrorPayload{Code string `json:"code"`; Message string `json:"message"`}。
    7. 新增 ModeRO = "ro" / ModeRW = "rw" 常量（D-14，前后端对齐字符串入 proto）。
    8. 新增 DecodeHello(payload []byte) (HelloPayload, bool)：json.Unmarshal 解码（默认忽略未知字段——D-02 的零成本实现，禁止 DisallowUnknownFields）；失败返回 ok=false；成功时 Cols/Rows 经 ClampDim 钳制后返回。不做 version 校验（校验语义在 server 握手段，02-02）。
    9. 新增 WelcomeFrame(mode string) []byte 与 ErrorFrame(code, message string) []byte：返回 1 字节类型 + json.Marshal(payload) 的完整帧字节，调用方直接 c.Write——S→C 控制帧组帧与 onChunk 的 1+payload 模式同构（CONTEXT code_context 行 90）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go build ./... && go vet ./internal/proto/</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` 与 `go vet ./internal/proto/` 退出 0
    - proto.go 含 Hello/Welcome/Error/Subprotocol/ModeRO/ModeRW/ErrVersionMismatch/ErrServerError/ReadLimitPreAuth/ReadLimitPostAuth 定义
    - DecodeHello 不使用 DisallowUnknownFields（未知字段忽略由 Unmarshal 默认行为保证）
    - 分片数上限仅以注释形式存在（无可执行常量），注释含"等效防线"与 D-09 修订标定
    - 1001/1013 在关闭码注释表中标注启用 phase（Phase 7 / Phase 5），无可执行发送路径
  </acceptance_criteria>
  <done>proto 包成为 wesh.v1 协议完整单一事实源，全部后续 plan 的符号依赖就位。</done>
</task>

<task type="auto">
  <name>Task 2: proto_test.go——编解码往返 + D-02 未知字段忽略 + 常量形状锁定</name>
  <files>internal/proto/proto_test.go</files>
  <read_first>
    - internal/proto/proto.go（Task 1 产物——被测对象）
    - cmd/wesh/main_test.go（表驱动风格 analog：TestParseArgs 15-44 行）
    - .planning/phases/02-protocol/02-PATTERNS.md（§测试文件 proto_test.go 行，406 行断言要点）
  </read_first>
  <action>
    新建 internal/proto/proto_test.go（package proto 内测，表驱动风格仿 main_test.go）：
    1. TestDecodeHello：表驱动四组——(a) 标准载荷 `{"version":"wesh.v1","cols":80,"rows":24}` → ok，三字段精确断言；(b) 混未知字段 `{"version":"wesh.v1","cols":100,"rows":40,"ticket":"secret","attach":2}` → 仍 ok 且三字段正确（D-02 核心回归：若未来有人加 DisallowUnknownFields 本组即红）；(c) 畸形 JSON（如 `{not json`）→ ok=false；(d) 边界钳制 cols=0 → 1、cols=9999 → 1000（ClampDim 复用断言）。
    2. TestWelcomeFrameErrorFrame：WelcomeFrame(ModeRO) 首字节 == Welcome(0x57)，余下 JSON 经 json.Unmarshal 回 WelcomePayload 且 Mode=="ro"；ErrorFrame(ErrVersionMismatch, "protocol version wesh.v1 required") 首字节 == Error(0x45)，JSON 解码回 ErrorPayload 且 Code/Message 精确相等。
    3. TestProtocolConstants：Subprotocol == "wesh.v1" 精确断言；ErrVersionMismatch/ErrServerError 匹配 snake_case 形状（正则 ^[a-z][a-z0-9_]*$）；Hello=='H'、Welcome=='W'、Error=='E'、Input=='0'、Resize=='1'、Output=='0' 逐字断死（防手滑改码——协议破坏面）；ReadLimitPreAuth==4096、ReadLimitPostAuth==16384。
    关闭码集合纪律不做源码 grep（脆弱且属 comment-text 反模式）——由 02-03/02-05 的 e2e 全程 CloseError 码值断言行为层覆盖（准则 2）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test -race -count=1 -v ./internal/proto/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/proto/` 退出 0，含 TestDecodeHello/TestWelcomeFrameErrorFrame/TestProtocolConstants 三函数
    - TestDecodeHello 含未知字段混合组（b）且断言 ok=true——D-02 演化纪律的自动化回归
    - TestProtocolConstants 逐字断言 Subprotocol 与五个帧类型字节值
  </acceptance_criteria>
  <done>proto 契约的编解码正确性与 D-02 忽略纪律获得自动化锁定。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 协议契约 → 前后端全体消费者 | 本 plan 定义的常量/schema 是后续所有实现的信任锚点；错误常量会在每个边界被复制放大 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | proto.go 帧类型/版本常量 | high | mitigate | TestProtocolConstants 逐字锁定帧字节与 Subprotocol 字符串——手滑改码即协议破坏，测试即红 |
| T-02-02 | Information Disclosure | ErrorPayload schema | medium | mitigate | D-06 受众分治在常量注释钉死：仅 version_mismatch/server_error 两个正常客户端可见码；攻击面路径不发 Error 帧的纪律随 ErrXxx 常量注释传递 |
| T-02-03 | DoS | 上限常量取值 | high | mitigate | ReadLimitPreAuth/PostAuth 注释标定合法流量依据与 D-09 修订出处；分片层以注释位显式记录等效防线与残余风险，防后续误读为"已落地" |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | 本 phase 零新增依赖（RESEARCH §Package Legitimacy Audit：Legitimacy Gate 不适用）；coder/websocket v1.8.15 与 @xterm/xterm 6.0.0 均 Phase 1 已钉版 |
</threat_model>

<verification>
- `go build ./... && go vet ./...` 退出 0
- `go test -race -count=1 ./internal/proto/` 全绿
- 既有测试不受影响：`go test -race -count=1 ./...` 全绿（proto 扩展为纯增量）
</verification>

<success_criteria>
- D-01/D-02/D-03/D-05/D-06/D-07/D-08/D-09(修订)/D-10 九项契约决策全部落入 proto 包代码或注释
- 02-02 握手段所需的全部 proto 符号（DecodeHello/WelcomeFrame/ErrorFrame/ReadLimit*/Subprotocol/ModeRO/ModeRW/ErrXxx）就绪
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `internal/proto/proto.go`：常量 `Hello`/`Welcome`/`Error`/`Subprotocol`/`ModeRO`/`ModeRW`/`ErrVersionMismatch`/`ErrServerError`/`ReadLimitPreAuth`/`ReadLimitPostAuth`；类型 `HelloPayload`/`WelcomePayload`/`ErrorPayload`；函数 `DecodeHello`/`WelcomeFrame`/`ErrorFrame`
- `internal/proto/proto_test.go`：`TestDecodeHello`/`TestWelcomeFrameErrorFrame`/`TestProtocolConstants`

<output>
Create `.planning/phases/02-protocol/02-01-SUMMARY.md` when done
</output>
