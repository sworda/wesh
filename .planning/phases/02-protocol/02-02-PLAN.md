---
phase: 02-protocol
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - internal/server/server.go
  - internal/server/export_test.go
  - internal/server/rawws_test.go
  - internal/server/handshake_test.go
  - cmd/wesh/main.go
autonomous: true
requirements:
  - SEC-08

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "不带 wesh.v1 子协议的升级请求在 Accept 之前收到 HTTP 400、连接未升级（零 WS 资源分配，D-03；Sec-WebSocket-Protocol 按逗号分隔 token 拆分精确匹配，wesh.v1.evil 前缀伪造同样 400——TestSubprotocolRequired 证明）"
    - "per-IP 未认证半开连接上限为 8：同 IP 第 9 个半开连接在 Accept 前收到 HTTP 429（D-04；守卫链顺序 400→429→409，被 409 拒绝的连接不消耗半开名额——acquire 成功后 release 恰好一次，TestHalfOpenCap / TestHalfOpenRelease 证明）"
    - "WS 升级完成后 5s 内未完成 Hello 的连接被以 1008 关闭、reason \"hello_timeout\"、无 Error 帧（D-04/D-06，容忍窗 4-8s，TestHelloTimeout 证明）"
    - "Accept 成功后预认证读限收紧为 4KiB（SetReadLimit(4096)，D-11 第一档；Hello JSON ~100B，余量两个数量级）——SEC-08 预认证窗口单连接可占内存最小化；Hello 前 >4KiB 消息收库自动 1009，3KiB 未知字段填充的合法 Hello 不误杀（TestPreAuthReadLimit 证明）"
    - "permessage-deflate 保持默认关：offer 压缩扩展的握手在 101 响应中不协商任何扩展（D-17，库 CompressionDisabled 默认，TestNoPerMessageDeflate 证明）"
    - "HTTP 握手层具 slowloris 防护：http.Server 显式 ReadHeaderTimeout 5s（Pitfall 8——http.Serve 零超时是 /ws 预检代码都到不了的更前置预认证面；不设 ReadTimeout/WriteTimeout 误伤长连接）"
    - "认证完成前零缓冲分配（SEC-08）结构性成立：400/429/409 三道拒绝全部发生在 Accept 之前（零 WS 对象）；预认证期唯一分配 = 库固定缓冲（8B 头 buf + 125B 控制 buf + bufio）+ 4KiB 读限档"
    - "plan 02-01 全部既有测试在守卫链上线后保持绿（正常浏览器秒发 Hello 不受限；Hello 已完成不计入半开计数）"
  artifacts:
    - path: "internal/server/server.go"
      provides: "Attach 前置守卫链（400 子协议预检 → 429 per-IP 半开帽 → 409 CAS → Accept → 4KiB → 5s Hello 计时器）"
      contains: "headerHasToken / clientIP / halfOpen / maxHalfOpenPerIP / readLimitPreAuth / helloTimeout"
    - path: "internal/server/rawws_test.go"
      provides: "裸 TCP 手写帧测试 helper（库客户端发不出畸形/无子协议握手）"
      contains: "dialRawWS / writeRawFrame / readRawFrame"
    - path: "internal/server/handshake_test.go"
      provides: "守卫链 e2e：400/429/5s 超时/预认证 4KiB/压缩拒绝 + 半开计数释放不变量"
      contains: "TestSubprotocolRequired / TestHalfOpenCap / TestHalfOpenRelease / TestHelloTimeout / TestPreAuthReadLimit / TestNoPerMessageDeflate"
    - path: "cmd/wesh/main.go"
      provides: "http.Server{ReadHeaderTimeout: 5s} 替换 http.Serve"
      contains: "ReadHeaderTimeout"
  key_links:
    - from: "internal/server/server.go"
      to: "internal/proto/proto.go"
      via: "子协议预检与 Accept 选项统一引用 proto.Subprotocol 单一常量"
      pattern: "proto\\.Subprotocol"
    - from: "cmd/wesh/main.go"
      to: "internal/server/server.go"
      via: "http.Server 包装 srv.Handler() 并设 ReadHeaderTimeout（5s，与 helloTimeout 语义对齐）"
      pattern: "ReadHeaderTimeout"
    - from: "internal/server/handshake_test.go"
      to: "internal/server/rawws_test.go"
      via: "400/429/5s 用例全部经裸帧 helper 构造（库客户端无法省略子协议头或保持静默半开）"
      pattern: "dialRawWS\\(|writeRawFrame\\(|readRawFrame\\("
---

<objective>
在 plan 02-01 的握手骨架上落地 SEC-08 预认证攻击面消除：Attach 前置守卫链（子协议 400 预检 → per-IP 半开帽 429 → 409 CAS → Accept → SetReadLimit 4KiB → 5s Hello 计时器）+ HTTP 层 ReadHeaderTimeout 5s，全部拒绝路径零 WS 资源分配。

Purpose: 预认证攻击面必须在结构上消除（事后补洞要动协议）；守卫链顺序敏感（400 最廉价无状态排最前，409 被拒者不消耗半开名额），是本 phase 安全目标的主承载 plan。
Output: 守卫链 + 裸帧测试 helper + 400/429/1008 三路径 e2e + ReadHeaderTimeout，`go test ./...` 全绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-protocol/02-CONTEXT.md
@.planning/phases/02-protocol/02-RESEARCH.md
@.planning/phases/02-protocol/02-PATTERNS.md
@.planning/phases/02-protocol/02-VALIDATION.md
@.planning/phases/02-protocol/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 守卫链落地（400→429→409→Accept→4KiB→5s 计时器）+ ReadHeaderTimeout + 裸帧 helper + 400/5s 两测</name>
  <files>internal/server/server.go, internal/server/rawws_test.go, internal/server/handshake_test.go, cmd/wesh/main.go</files>
  <read_first>
    - internal/server/server.go（被改文件现状——plan 02-01 产物：Attach 握手状态机、Options、ro 分支）
    - cmd/wesh/main.go（被改文件现状：run 内 http.Serve 行 79）
    - .planning/phases/02-protocol/02-RESEARCH.md（Pattern 1 守卫链行 176-224 含 headerHasToken 纪律与顺序理由；Pattern 2 行 230-266 两档常量与 5s 计时器形态；Pitfall 6 per-IP 不变量行 465-469；Pitfall 7 反代聚合并文档化行 471-475；Pitfall 8 ReadHeaderTimeout 行 477-481；裸帧 helper 行 516-557）
    - .planning/phases/02-protocol/02-PATTERNS.md（§internal/server/server.go 守卫区段行 155-185；§Shared Patterns per-IP 计数不变量段；§No Analog Found 的 halfOpen/clientIP 行）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-03/D-04/D-06/D-11）
    - internal/server/e2e_test.go（dialAndHandshake 与 startTestServerOpts 签名——plan 02-01 产物）
  </read_first>
  <action>
    1. `internal/server/rawws_test.go` 新建（package server_test，照 RESEARCH 行 519-556 实现，帧线格式经库 frame.go:52-102 核实）：`dialRawWS(t *testing.T, addr string, extraHeaders ...string) net.Conn`——裸 TCP  Dial 后手写 Upgrade 请求（GET /ws HTTP/1.1 + Host + Upgrade: websocket + Connection: Upgrade + 16B base64 Sec-WebSocket-Key + Sec-WebSocket-Version: 13 + extraHeaders 逐行追加），读响应头至 \r\n\r\n 并返回首行供断言（返回 conn + status line——签名可调，满足断言即可）；`writeRawFrame(t, conn, fin bool, opcode byte, payload []byte)`——mask 强制（maskKey [4]byte 逐字节 XOR，服务端对未 mask 帧直接报错）；`readRawFrame(t, conn) (fin bool, opcode byte, payload []byte)`——解析 S→C 帧（无 mask），供关闭帧码值（op=0x8，payload 前 2 字节大端码值 + 其余 reason）与 ping（op=0x9）断言复用。
    2. `internal/server/server.go` 守卫链（照 RESEARCH Pattern 1 行 184-218 与 PATTERNS 守卫区段，插入到 plan 02-01 的 Attach 开头、409 CAS 之前；**顺序敏感：400 → 429 → 409 → Accept → assert → SetReadLimit(4KiB) → 5s 计时器**）：
       - 包级私有件：`func headerHasToken(h http.Header, key, token string) bool`——`Sec-WebSocket-Protocol` 是逗号分隔 token 列表，按 token 拆分去空格后**精确等值比较**，禁止子串包含判断（防 wesh.v1.evil 前缀绕过，RESEARCH 行 224）；`func clientIP(r *http.Request) string`——net.SplitHostPort(r.RemoteAddr) 取 IP，SplitHostPort 出错时退化用整串；`type halfOpen struct { mu sync.Mutex; n map[string]int }` + `acquire(ip string, max int) bool`（≥max 拒）+ `release(ip string)`（减一归零删 key），挂为 Server 字段（New 内初始化 map）；包级私有变量 `maxHalfOpenPerIP = 8`（D-04；私有变量形态供测试改写，不违反 D-10：可配性指用户面 flag，非测试缝）；常量 `readLimitPreAuth = 4 * 1024`（D-11 注释：Hello JSON ~100B 余量两个数量级）与 `helloTimeout = 5 * time.Second`（D-04）。
       - **测试缝（硬约束）**：e2e 测试在 `package server_test` 外部测试包，无法触达包级私有变量——新建 `internal/server/export_test.go`（`package server`，仅测试期编译的内测文件），暴露 set/restore 钩子：`func SetMaxHalfOpenPerIP(n int) (restore func())`（改写私有变量并返回恢复闭包）；后续 plan 在此文件增量加钩子（02-03 的 msgCompleteTimeout/stderrw、02-04 的 pongTimeout），单文件收口全部测试缝。
       - Attach 守卫区：① `if !headerHasToken(r.Header, "Sec-WebSocket-Protocol", proto.Subprotocol) { http.Error(w, "subprotocol wesh.v1 required", http.StatusBadRequest); return }`（D-03，最廉价无状态排最前）；② `ip := clientIP(r)` + `if !s.halfOpen.acquire(ip, maxHalfOpenPerIP) { http.Error(w, "too many half-open connections", http.StatusTooManyRequests); return }`（D-04）；③ 409 CAS（02-01 既有）失败路径先 `release(ip)` 再 http.Error 409。**释放不变量（Pitfall 6）：acquire 成功后 release 恰好一次，发生在 Hello 完成或连接/拒绝终结（先到为准）**——用 Attach 局部 `released := false` + 闭包 `release := func() { if !released { released = true; s.halfOpen.release(ip) } }`（单 goroutine 纪律：timer 回调只调 c.Close 不碰计数，release 全部在 Attach goroutine 内）；Accept 失败/assert 失败路径先 release 再现状释放门位；握手成功后（Welcome 发出后）显式 release()（D-04：Hello 已完成不计入）；defer release() 兜底一切提前 return。
       - Accept 成功后、读 Hello 前：`c.SetReadLimit(readLimitPreAuth)`（D-11 第一档，SEC-08 预认证窗口内存最小化）。
       - 5s Hello 计时器（照 RESEARCH Pattern 2 行 239-247 形态）：handshake 入口处 `helloDone := make(chan struct{})` + `time.AfterFunc(helloTimeout, func() { select { case <-helloDone: default: c.Close(websocket.StatusPolicyViolation, "hello_timeout") } })`（1008，机器串 reason，D-07；无 Error 帧——攻击/异常路径，D-06/A3）；Hello 成功路径 close(helloDone) + timer.Stop()（02-01 握手函数的升档序列插入"停计时器"一步）。
    3. `cmd/wesh/main.go`：`http.Serve(ln, srv.Handler())` 替换为 `hs := &http.Server{Handler: srv.Handler(), ReadHeaderTimeout: 5 * time.Second}; hs.Serve(ln)`（Pitfall 8——http.Serve 零超时是 slowloris 面；**不设 ReadTimeout/WriteTimeout**，hijack 后虽不生效但避免误配）；import 加 "time"。
    4. `internal/server/handshake_test.go` 追加四测（复用 rawws helper）：
       - `TestSubprotocolRequired`（D-03，VALIDATION 行 50）：dialRawWS 不带 Sec-WebSocket-Protocol → 断言响应首行含 "400" 且连接未升级；带 `Sec-WebSocket-Protocol: wesh.v1.evil` → 同样 400（前缀伪造旁路证明）；带 `Sec-WebSocket-Protocol: wesh.v1` → 首行 "101"（随后完成 Hello 握手正常收口，顺带证明 token 精确匹配不误伤合法值）。
       - `TestHelloTimeout`（D-04，VALIDATION 行 52）：dialRawWS 带正确子协议（101）后保持静默；readRawFrame 读至关闭帧，断言码值 1008、reason "hello_timeout"，且从 101 到关闭帧的耗时落在 4-8s 容忍窗内。
       - `TestPreAuthReadLimit`（D-11 预认证档，VALIDATION 行 48）：dialRawWS 完成升级（101）后、发 Hello 前，先发一个 >4KiB 的单条 binary 消息（5KiB 载荷单帧）→ 库在预认证 4KiB 档自动 1009，readRawFrame 收关闭帧断言码值 1009；对照例：另起连接发 ~3KiB 的"合法 Hello"——Hello JSON 以未知字段填充至 3KiB（如 `"padding":"xxx..."`，D-02 未知字段忽略）→ 握手正常完成收 Welcome（预认证档不误杀合法大 Hello）。
       - `TestNoPerMessageDeflate`（D-17，VALIDATION 行 49）：dialRawWS 同时带 `Sec-WebSocket-Protocol: wesh.v1` 与 `Sec-WebSocket-Extensions: permessage-deflate` → 断言 101 响应头中**不含** `Sec-WebSocket-Extensions`（库默认 CompressionDisabled 拒绝协商，accept.go:55-59 VERIFIED——压缩默认关的线上证明）。
    5. per-IP 反代聚合限制按 Pitfall 7 在 server.go 守卫区注释文档化（本 phase 不解析 X-Forwarded-For——不可信头直接信等于伪造 IP 绕过计数器；半开窗口极短：5s 超时 + Hello 完成即释放）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go vet ./... && go test ./... -count=1 && go test ./internal/server -count=1 -v -run 'TestSubprotocolRequired|TestHelloTimeout|TestPreAuthReadLimit|TestNoPerMessageDeflate' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -count=1` 退出 0（02-01 全部测试在守卫链上线后保持绿——正常握手秒发 Hello 不受 400/429/5s 影响）
    - TestSubprotocolRequired 三例全绿：无头 400、wesh.v1.evil 400、wesh.v1 101（VALIDATION 行 50 落位）
    - TestHelloTimeout 绿：静默连接 4-8s 内收 1008 且 reason=="hello_timeout"（VALIDATION 行 52 落位）
    - TestPreAuthReadLimit 绿：Hello 前 5KiB 消息收 1009；3KiB 未知字段填充 Hello 握手成功（VALIDATION 行 48 落位，D-11 预认证档 + D-02 双证明）
    - TestNoPerMessageDeflate 绿：offer permessage-deflate 的 101 响应无 Sec-WebSocket-Extensions 头（VALIDATION 行 49 落位，D-17 线上证明）
    - server.go Attach 守卫区调用顺序为：headerHasToken 400 → halfOpen.acquire 429 → attached CAS 409 → Accept → Subprotocol assert → SetReadLimit(4096) → 5s 计时器（顺序敏感，RESEARCH 行 222）
    - 全部 acquire 成功后的退出路径（409 拒绝/Accept 失败/assert 失败/Hello 完成/连接终结）经 release 闭包恰好释放一次（released 布尔守卫）
    - `grep -q 'ReadHeaderTimeout: 5 \* time.Second' cmd/wesh/main.go` 成立且 main.go 不含 ReadTimeout/WriteTimeout 设置
    - `go test -race -count=1 ./internal/server` 退出 0（计数器竞态面）
  </acceptance_criteria>
  <done>预认证守卫链四环落地：400/429/409 拒绝全部发生在 Accept 前（零 WS 分配），4KiB 预认证读限与 5s Hello 超时武装，HTTP 层 slowloris 防护到位，四 e2e + 全量回归绿。</done>
</task>

<task type="auto">
  <name>Task 2: per-IP 半开帽 e2e——429 触发与 exactly-once 释放不变量</name>
  <files>internal/server/handshake_test.go</files>
  <read_first>
    - internal/server/handshake_test.go（被改文件现状——plan 02-02 Task 1 产物：TestSubprotocolRequired/TestHelloTimeout 与 rawws helper 用法）
    - internal/server/rawws_test.go（dialRawWS/writeRawFrame/readRawFrame 签名）
    - internal/server/server.go（halfOpen acquire/release 与守卫链现状）
    - .planning/phases/02-protocol/02-RESEARCH.md（Pitfall 6 行 465-469；验证用例矩阵行 571）
    - .planning/phases/02-protocol/02-VALIDATION.md（行 655 TestHalfOpenCap 验收描述）
  </read_first>
  <action>
    在 `internal/server/handshake_test.go` 追加两测（确定化构造——单客户端 409 CAS 下"8 连接完成升级"不可能：CAS 持有者之外全被 409，故经 maxHalfOpenPerIP 私有变量注入小帽构造确定性用例，与 pongTimeout 注入先例同源）：
    1. `TestHalfOpenCap`（D-04，VALIDATION 行 51）：经 `server.SetMaxHalfOpenPerIP(1)` 注入小帽（t.Cleanup 调恢复闭包——单客户端 409 CAS 下"8 连接完成升级"不可能：CAS 持有者之外全被 409，注入缝是确定性构造的唯一路径）；conn1 = dialRawWS 带正确子协议（101）保持静默（占住唯一半开名额 + CAS）；conn2 = dialRawWS 带正确子协议 → 断言 HTTP 429（守卫链顺序证明：429 先于 409——conn2 在 CAS 前已被帽拒）；conn1 完成 Hello 握手（名额释放）但 CAS 仍被 conn1 持有 → conn3 dialRawWS → 断言 HTTP 409（非 429——Hello 完成即释放的直接证明）。收口：conn1 关闭。
    2. `TestHalfOpenRelease`（Pitfall 6 不变量：release 恰好一次、拒绝路径零泄漏）：保持 `SetMaxHalfOpenPerIP(1)`；conn1 dialAndHandshake 完成握手（名额已释放）；随后连续 3 次 dialRawWS 带正确子协议 → 全部断言 409（若 409 路径漏 release，第二次起名额被占满将退化为 429——序列即泄漏探针）；再证连接终结释放：connA 半开静默后直接 TCP 关闭，随后 connB dialRawWS 应得 101 或 409（非 429——连接终结路径释放证明；服务端侧 connA 的读循环随 TCP RST 终结触发 defer release）。
    3. 两测全部走 startTestServerOpts 默认装配；每测结束收口所有裸 conn（t.Cleanup 关闭）防 fd 泄漏影响邻测。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test ./internal/server -count=1 -v -run 'TestHalfOpenCap|TestHalfOpenRelease' && go test -race -count=1 ./internal/server && go test ./... -count=1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - TestHalfOpenCap 绿：帽=1 时第二半开连接恰收 429；Hello 完成后新连接收 409 而非 429（VALIDATION 行 655 落位，含守卫链顺序与 D-04"Hello 已完成不计入"双证明）
    - TestHalfOpenRelease 绿：连续 409 序列不退化为 429（拒绝路径零泄漏）；半开连接 TCP 终结后名额释放（Pitfall 6 全部退出路径覆盖）
    - `go test -race -count=1 ./internal/server` 退出 0（map+Mutex 计数器竞态面）
    - `go test ./... -count=1` 退出 0（无回归）
  </acceptance_criteria>
  <done>per-IP 半开帽的触发（429）与释放（Hello 完成/409 拒绝/连接终结三路径恰好一次）全部确定性锁定，SEC-08 守卫链收官。</done>
</task>

</tasks>

## Flagged Assumptions（spec-less probe fallback §A/§C——显式呈现，绝不静默丢弃）

edge probe 对 SEC-08 返回 `unclassified`，按规则保持 `unresolved` 并在此登记：

| Requirement | probe 结果 | 状态 | 处置 |
|-------------|-----------|------|------|
| SEC-08 | unclassified — review manually | unresolved | 以 RESEARCH §Validation Architecture 行 654-658 四条验收准则为锚点（TestSubprotocolRequired / TestHalfOpenCap / TestHelloTimeout 本 plan 落位；TestPrematureFrame 已在 plan 02-01 落位）+ 行 658 预认证内存平坦走查（代码走查 Accept 前零分配，flood 内存采样在 plan 02-03 作参考不门禁） |

§B prohibition recall：SEC-08 召回的候选（"拒绝响应不得泄露服务端栈/版本细节"）属 OWASP 通用 canon——按 canon-referral 规则不 mint，留 breadcrumb：由本 plan threat_model T-02-02-05 与 Phase 3 安全阶段覆盖。prohibition recall surfaced 0 条 bespoke = authored 0 条 ✓（空列表是精确化阶段的正确产出，非遗漏）。

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 任意 TCP 客户端 ↔ HTTP 层 | 未升级请求在此被 400/429/409 拒绝——零 WS 对象分配（SEC-08 结构防线） |
| 扫描器/攻击者 ↔ WS 握手层 | 升级后 5s 窗口与 4KiB 读限约束预认证可占资源 |
| 反代部署 ↔ per-IP 计数 | RemoteAddr 聚合语义边界（已知限制，文档化） |
| npm/go 依赖安装 | 本 plan 零新增依赖 |

## STRIDE Threat Register（ASVS L1，block on high）

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | DoS | 半开连接洪泛（SYN 式占满握手窗口） | high | mitigate | per-IP 帽 8 + Accept 前 HTTP 429（D-04）+ 5s Hello 超时收敛窗口；TestHalfOpenCap/TestHalfOpenRelease 锁定触发与释放不变量 |
| T-02-02-02 | DoS | HTTP 握手层 slowloris（慢滴请求头占连接 goroutine） | high | mitigate | http.Server.ReadHeaderTimeout=5s（Pitfall 8，http.Serve 零超时 GOROOT server.go:2940-2943 VERIFIED）；不设 Read/WriteTimeout 防误伤长连接 |
| T-02-02-03 | DoS | 预认证内存放大（ttyd protocol.c:288-296 模式） | high | mitigate | 守卫链全在 Accept 前（零 WS 对象）+ SetReadLimit(4KiB) 预认证档（D-11）+ 库流式重组 O(1)/帧；代码走查 + plan 02-03 flood 用例内存采样（参考不门禁） |
| T-02-02-04 | DoS | per-IP 计数泄漏/双重释放（帽失效或误伤合法用户） | medium | mitigate | acquire→release 恰好一次闭包不变量（Pitfall 6）；TestHalfOpenRelease 覆盖全部退出路径；-race 门禁 |
| T-02-02-05 | Information Disclosure | 400/429 响应体泄露栈细节 | low | mitigate | http.Error 固定短文案，不含版本/栈信息；canon 项 breadcrumb 至 Phase 3 安全头/TLS 阶段 |
| T-02-02-06 | Elevation of Privilege | 子协议前缀伪造（wesh.v1.evil 绕过预检） | high | mitigate | token 拆分精确等值匹配（禁子串判断）+ Accept 后 assert 兜底；TestSubprotocolRequired 负例锁定 |
| T-02-02-07 | DoS | 反代后按代理 IP 聚合误伤/被绕过 | medium | accept | Pitfall 7：本 phase 不解析 X-Forwarded-For（直接信不可信头更糟——伪造 IP 绕过计数器）；半开窗口极短（5s + Hello 即释放）缓解；守卫区注释文档化，可信头处理属 Phase 3/7 |
</threat_model>

<verification>
- `go vet ./... && go test ./... -count=1` 全绿
- `go test ./internal/server -run 'TestSubprotocolRequired|TestHelloTimeout|TestHalfOpenCap|TestHalfOpenRelease' -count=1` 绿
- `go test -race -count=1 ./internal/server` 绿（计数器竞态面）
</verification>

<success_criteria>
- ROADMAP 准则 1 的预认证部分成立：无子协议 400、半开帽 429、5s 静默 1008，全部零 WS 资源分配（SEC-08）
- SEC-08 结构性成立：Accept 前守卫区零分配 + 4KiB 预认证档 + 库固定缓冲为预认证期唯一分配
- 本 plan 的 must_haves.truths 全部可由 acceptance_criteria 对应断言证明
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `internal/server/server.go`（扩展）：`headerHasToken(h http.Header, key, token string) bool`；`clientIP(r *http.Request) string`；`type halfOpen struct`（map+Mutex）+ `acquire(ip string, max int) bool` + `release(ip string)`；包级私有变量 `maxHalfOpenPerIP = 8`（测试注入缝）；常量 `readLimitPreAuth = 4 * 1024`、`helloTimeout = 5 * time.Second`；Attach 前置守卫链（400→429→409→Accept→assert→4KiB→5s 计时器）；5s Hello 计时器（time.AfterFunc + helloDone channel，Close(1008,"hello_timeout")）
- `cmd/wesh/main.go`（修改）：`http.Server{Handler: srv.Handler(), ReadHeaderTimeout: 5 * time.Second}` 替换 http.Serve
- `internal/server/rawws_test.go`（新建 helper）：`dialRawWS(t, addr, extraHeaders...)`、`writeRawFrame(t, conn, fin, opcode, payload)`、`readRawFrame(t, conn) (fin, opcode, payload)`
- `internal/server/export_test.go`（新建，package server 测试缝）：`SetMaxHalfOpenPerIP(n int) (restore func())`
- `internal/server/handshake_test.go`（扩展）：`TestSubprotocolRequired`、`TestHelloTimeout`、`TestPreAuthReadLimit`、`TestNoPerMessageDeflate`、`TestHalfOpenCap`、`TestHalfOpenRelease`

<output>
Create `.planning/phases/02-protocol/02-02-SUMMARY.md` when done.
</output>
