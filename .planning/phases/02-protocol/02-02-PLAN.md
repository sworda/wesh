---
phase: 02-protocol
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - internal/server/server.go
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
  - internal/server/e2e_test.go
  - web/src/main.ts
autonomous: true
requirements:
  - CORE-04
  - SEC-08
  - RES-01

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "浏览器打开页面后 WS 以 wesh.v1 子协议建连，onopen 首发 Hello{version,cols,rows}——线上首帧必为 'H'（helloSent 门住 sendResize：term.onResize 常驻接线 main.ts:103 在首次 fit 几乎必然触发，不门则 RESIZE 抢跑首帧被 1002 frame_before_hello 直关），服务端校验通过即下发 Welcome{mode} 并按 Hello 尺寸 sess.Resize——80x24 首帧窗口消除（D-01/D-02/D-03，OQ3）"
    - "默认只读：Welcome{mode:\"ro\"} 时前端 disableStdin=true 且标题加 [ro] 前缀；服务端 INPUT 分支按 writable 标志丢弃——裸 WS 客户端（wscat/脚本）发 INPUT 也不进 PTY（D-13/D-14，Pitfall 7 服务端为真边界）"
    - "ro 模式下 RESIZE 照常放行同步 PTY 尺寸（D-13：只丢 INPUT）"
    - "--writable 开启后 Welcome{mode:\"rw\"}，INPUT 写入 PTY 正常回显（D-15）"
    - "Hello 前收到任何数据帧（抢跑）或空消息，服务端以 1002 直关且不发 Error 帧；5s 内未完成 Hello 以 1008 hello_timeout 关闭（D-04/D-06，OQ2 裁决）"
    - "Hello.version != \"wesh.v1\" 时服务端先发 Error{version_mismatch} 帧再以 1008 关闭，close reason 带同名机器串（D-06/D-07 正常客户端路径）"
    - "SetReadLimit 两档切换：Accept 后 4KiB（预认证窗口），Welcome 发出后 16KiB（稳态）；切换经库 atomic store 下一条消息起生效（D-11，read.go:97-105）"
    - "前端 onclose 按 ev.code 分派人话文案（1000/1008/1009/1011/1013/default），分派只认 code 不认 reason（库自动 1009 的 reason 不可控）；1009 文案不提 flag（D-12①）"
    - "全部既有 e2e 六测（TestEchoPTY/TestSecondClient409/TestExitCodePropagation/TestUnknownFrame1002/TestClientDisconnectSIGHUP 经 dialHello 改造 + TestDrainBeforeAttach 适配 New 签名——无 Dial 不需握手）在握手语义下保持绿色"
  prohibitions:
    - "writable 标志未显式开启时，任何路径（非浏览器客户端、畸形序列、抢跑重试）都不得将客户端输入写入 PTY——只读必须 fail-closed，不得静默降级为可写（CORE-04 安全边界）"
  artifacts:
    - path: "internal/server/server.go"
      provides: "Attach 握手段（子协议双闸/4KiB/5s 超时/抢跑 1002/version 校验/Welcome/升档）+ ro INPUT 门 + Options 注入"
      contains: "func (s *Server) Attach"
    - path: "cmd/wesh/main.go"
      provides: "--writable flag 与 server.New 新装配"
      contains: "writable"
    - path: "web/src/main.ts"
      provides: "子协议建连 + Hello 首帧 + Welcome 处理 + onclose 按码分派"
      contains: "wesh.v1"
    - path: "internal/server/e2e_test.go"
      provides: "dialHello 握手 helper + 全量既有测试握手改造 + TestHelloWelcome"
      contains: "dialHello"
  key_links:
    - from: "internal/server/server.go"
      to: "internal/proto/proto.go"
      via: "握手段调用 DecodeHello/WelcomeFrame/ErrorFrame/Subprotocol/ReadLimitPreAuth/ReadLimitPostAuth（02-01 产物）"
      pattern: "proto\\.(DecodeHello|WelcomeFrame|Subprotocol)"
    - from: "web/src/main.ts"
      to: "internal/proto/proto.go"
      via: "HELLO/WELCOME/ERROR 帧常量与 SUBPROTOCOL 字符串手工对齐（D-16 注释互相指路）"
      pattern: "wesh\\.v1"
    - from: "cmd/wesh/main.go"
      to: "internal/server/server.go"
      via: "parseArgs --writable → config.writable → server.New Options{Writable}"
      pattern: "server\\.New\\("
---

<objective>
打通本 phase 的端到端脊梁（tracer）：浏览器以 wesh.v1 子协议建连 → onopen 发 Hello 首帧 → 服务端预认证窗口（子协议双闸 / 4KiB 读上限 / 5s 超时 / 抢跑 1002）校验 → Welcome{mode} 下发 → 升档 16KiB → ro/rw 数据面（ro 丢 INPUT、RESIZE 放行）→ 前端按 mode 禁输入并按关闭码分派文案。本 plan 完成后，默认只读模式的完整用户路径在真实浏览器里可用，且全部既有 e2e 在握手语义下保持绿色。

Purpose: 握手是 phase 全部后续能力（守卫链/保活/上限测试）的挂点——先端到端证明 Hello→Welcome→数据面架构成立，再逐层加防御与测试。
Output: server.go 握手段 + ro 门、--writable flag、前端握手全链路、e2e 全量握手改造 + TestHelloWelcome 冒烟。

规格探针标记（spec-less probe fallback）：CORE-04 与 SEC-08 探针报为 unclassified；其边界已按 D-13（ro 只丢 INPUT）、D-14（Welcome mode + disableStdin）、D-03/D-04/D-11（预认证窗口三闸）显式落入 must_haves.truths，fail-closed 要求落入 prohibitions，无静默丢弃。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-protocol/02-CONTEXT.md
@.planning/phases/02-protocol/02-RESEARCH.md
@.planning/phases/02-protocol/02-PATTERNS.md
@internal/proto/proto.go
@internal/server/server.go
@cmd/wesh/main.go
@web/src/main.ts
@internal/server/e2e_test.go
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 服务端握手段 + ro 门 + --writable + e2e 全量握手改造（端到端 tracer）</name>
  <reversibility rating="one-way">D-15 --writable flag 名是 CLI 公开契约，发布后改名破坏脚本与文档；用户在 CONTEXT.md 已锁定，不再设 checkpoint，仅留记录。</reversibility>
  <files>internal/server/server.go, cmd/wesh/main.go, cmd/wesh/main_test.go, internal/server/e2e_test.go</files>
  <read_first>
    - internal/server/server.go（被改文件现状：Attach 72-117 行、New 44-54 行、原子态惯例 24-38 行、wsDisconnected/terminate 收口 155-173 行）
    - internal/proto/proto.go（02-01 产物：DecodeHello/WelcomeFrame/ErrorFrame/Subprotocol/ModeRO/ModeRW/ErrVersionMismatch/ReadLimitPreAuth/ReadLimitPostAuth 签名）
    - cmd/wesh/main.go（被改文件现状：config 22-26 行、parseArgs 31-51 行、run 装配 53-84 行）
    - cmd/wesh/main_test.go（被改文件现状：TestParseArgs 表驱动 15-44 行）
    - internal/server/e2e_test.go（被改文件现状：startTestServer 106-122 行、waitExit 125-135 行、六个既有测试——含 TestDrainBeforeAttach 84-100 行，无 Dial 直调 server.New）
    - .planning/phases/02-protocol/02-RESEARCH.md（Pattern 1 子协议双闸 186-224 行、Pattern 2 预认证窗口 226-263 行、Anti-Patterns 300-306 行、Pitfall 2 带 deadline ctx 339-343 行）
    - .planning/phases/02-protocol/02-PATTERNS.md（§server.go 重构段 119-259 行、Planner 注意事项 3/4/5/6/8 行 487-492——2026-08-15 已按 D-09 修订与 planner 裁决重写，内容与本文一致）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-03/D-04/D-06/D-11/D-13/D-14/D-15，行 23-44）
  </read_first>
  <behavior>
    - dialHello 成功路径：Dial(Subprotocols:["wesh.v1"]) → 发 Hello → 收 Welcome，mode 与服务端 writable 配置一致（"ro"/"rw"）
    - 默认（writable=false）：握手后发 INPUT 无任何 OUTPUT 回显，连接保持；发 RESIZE 后 PTY 尺寸跟随
    - writable=true：握手后 INPUT 正常 echo（既有 TestEchoPTY 语义）
    - 首帧非 'H'（直接发 INPUT）→ CloseError 1002 且之前不收到任何 'E' 帧
    - 首帧 Hello{version:"wesh.v9"} → 先收 'E' 帧 {code:"version_mismatch"} 再收 CloseError 1008
    - Hello 前发空消息（0 字节 binary）→ CloseError 1002
    - 握手成功后全部既有生命周期语义不变（exitf 经既有 wsDisconnected/terminate 单一路径收口）
  </behavior>
  <action>
    1. `internal/server/server.go` 重构 Attach（全部新终结路径只关 conn，exitf 竞争维持 wsDisconnected→terminate 单一路径，禁止新增 exitf 分支——CONTEXT 行 92 硬约束）：
       - Server 结构体：新增 `writable bool` 字段（注释：New 装配期固化、运行期只读，故 plain 字段无需 atomic）；新增 `helloTimeout time.Duration` 字段（注释同源）。
       - 新增 `Options` struct：{ Writable bool; HelloTimeout time.Duration }——注释分组语义：Writable 为生产直传字段（main.go flag 原样透传）；HelloTimeout 为测试可覆写字段（零值取默认常量 defaultHelloTimeout = 5s，D-04）。02-03/02-04 会向 Options 追加 MaxHalfOpenPerIP/PingInterval/PongTimeout 字段，结构预留。
       - New 签名改为 New(sess *pty.Session, exitf func(int), opts Options)：opts.HelloTimeout 零值时置 defaultHelloTimeout；其余字段原样采用。注释更新装配契约。
       - Attach 守卫区第一步（Accept 前，HTTP 层零 WS 资源）：新增包级函数 headerHasToken(h http.Header, name, token string) bool——按 token 拆分比较（strings.Split "," + TrimSpace + EqualFold 逐 token），禁止 strings.Contains 整头匹配（防 wesh.v1.evil 前缀绕过，PATTERNS 185 行硬纪律；库 accept.go:357-368 headerTokens 同语义）。预检失败：http.Error(w, "subprotocol wesh.v1 required", http.StatusBadRequest) 返回（D-03，扫描器最早被拦）。
       - 保留 409 原子门（CompareAndSwap，Phase 1 现状 74-77 行）。per-IP 429 闸不在本 plan（02-03 插入到子协议预检与 409 之间——planner 裁决：per-IP 必须在 409 之前，否则 D-04 的 429 在单客户端模型下结构性不可达；RESEARCH Pattern 1 的 409→per-IP 顺序与其自身 TestHalfOpenPerIP429 测试映射矛盾，以 locked decision D-04 优先）。
       - AcceptOptions{Subprotocols: []string{proto.Subprotocol}}（一行开启协商回显，D-03）；Accept 后立即 c.Subprotocol() != proto.Subprotocol assert 兜底（不等则 s.logEvent(...) + c.Close(websocket.StatusPolicyViolation, "subprotocol_required") + 释放门位返回——理论不可达，D-03 双闸；logEvent 埋点在此建立，02-05 复核清单以此为准）。
       - Accept 成功后 c.SetReadLimit(proto.ReadLimitPreAuth)（D-11 预认证窗口 4KiB）。
       - 5s 未认证超时：helloDone := make(chan struct{}) + time.AfterFunc(s.helloTimeout, func() { select { case <-helloDone: default: c.Close(websocket.StatusPolicyViolation, "hello_timeout"); s.logEvent(...) } })，defer timer.Stop()。注释引 RESEARCH Pitfall 2：读循环 ctx 恒 context.Background() 永不带 deadline（conn.go:188-199 deadline ctx 到期经 AfterFunc 关整条连接且无关闭帧）。
       - 首帧读取与校验（握手状态机，RESEARCH Pattern 2 照抄语义）：c.Read 返回 err → 走既有 wsDisconnected 收口（含对端断开与 hello_timeout 关闭后的 reader 终结）；len(data)==0 → c.Close(websocket.StatusProtocolError, "empty_frame") + logEvent 后 return（Open Question 2 裁决：Hello 前空消息按畸形处理）；data[0] != proto.Hello → c.Close(websocket.StatusProtocolError, "frame_before_hello") + logEvent 后 return（D-04 抢跑帧；不发 Error 帧，D-06 攻击面零反馈）。
       - h, ok := proto.DecodeHello(data[1:])：!ok → c.Close(websocket.StatusProtocolError, "malformed_hello") + logEvent 后 return（D-05 1002 桶含畸形）；h.Version != proto.Subprotocol → 先 c.Write(context.Background(), websocket.MessageBinary, proto.ErrorFrame(proto.ErrVersionMismatch, "protocol version wesh.v1 required")) 再 c.Close(websocket.StatusPolicyViolation, proto.ErrVersionMismatch)（D-06 正常客户端路径：Error 帧 + 1008；close reason 与 code 同名机器串，D-07）+ logEvent 后 return。
       - 握手成功升档序列（顺序敏感，PATTERNS 注意 5）：close(helloDone)（停 5s 计时器）→ s.sess.Resize(h.Cols, h.Rows)（Hello 携首尺寸，消除 80x24 首帧窗口，PATTERNS 注意 6）→ c.Write(..., proto.WelcomeFrame(mode))（mode := proto.ModeRO；s.writable 时 proto.ModeRW，D-14）→ c.SetReadLimit(proto.ReadLimitPostAuth)（16KiB 稳态，下一条消息起生效）。
       - 数据面 switch 改造：case proto.Input 增加 ro 门——if !s.writable { continue }（D-13 安全边界在服务端；静默丢弃，不打日志防按键洪水）；case proto.Resize 不变（ro 同放行，D-13）；Hello 完成后收到的空消息维持静默跳过（现状 102-104 行 continue，OQ2）；default 未知帧 1002 现状保留。
       - 新增 logEvent 方法（D-12② stderr 单行事件，格式 discretion 但三要素齐全：对端 remote、码值 code、reason 机器串；例：fmt.Fprintf(os.Stderr, "wesh: close remote=%s code=%d reason=%s\n", ...)）。本 plan 覆盖 hello_timeout/empty_frame/frame_before_hello/malformed_hello/version_mismatch/subprotocol_required（assert 兜底分支）六处埋点；1009 的 ErrMessageTooBig 钩子属 02-05。remote 取值用 c 的底层对端地址或保存 r.RemoteAddr——实现时选可获取者，注释注明反代聚合限制（Pitfall 6：直连部署有效，X-Forwarded-For 属 Phase 3 SEC-07，本 phase 不解析）。
    2. `cmd/wesh/main.go`：config 加 `writable bool`；parseArgs 加 fs.BoolVar(&cfg.writable, "writable", false, "allow client input (default read-only)")（D-15 逐字 help 文案，全名无短选项）；run 装配改 server.New(sess, os.Exit, server.Options{Writable: cfg.writable})。
    3. `cmd/wesh/main_test.go`：TestParseArgs 表驱动加一组 {"writable flag", []string{"--writable", "--", "bash"}, ...} 断言 cfg.writable==true（表结构加 wantWritable 字段）；默认组断言 writable==false。
    4. `internal/server/e2e_test.go` 全量握手改造：
       - startTestServer 拆分为 startTestServerWith(t, argv, opts server.Options)（主实现）+ startTestServer(t, argv)（兼容包装，内部调 With 传 server.Options{Writable: true}——保持既有五个测试的 echo 语义）。New 签名变更的全部调用处同步适配。
       - 新增 dialHello(t, ctx, wsURL string, cols, rows int) (*websocket.Conn, string)：websocket.Dial(ctx, wsURL, &websocket.DialOptions{Subprotocols: []string{proto.Subprotocol}}) → 发 Hello 帧（append([]byte{proto.Hello}, JSON{version: proto.Subprotocol, cols: cols, rows: rows}...)）→ 读首帧断言 buf[0]==proto.Welcome 且 JSON 解码取 mode → 返回 (conn, mode)。t.Helper() 标记；注释职责（RESEARCH §Wave 0 dialHello）。**cols/rows 参数化是签名硬要求**——02-03 TestReadOnlyAllowsResize 以 (111, 44) 复用同一签名验证 Hello 携尺寸生效，禁止硬编码 80x24；本 plan 内既有测试调用处统一传 (80, 24)。
       - 六个既有测试适配（New 签名变更的全部调用处同步适配，缺一编译即破）：五个 Dial 测试调用全部换 dialHello（统一传 80, 24）——TestEchoPTY / TestExitCodePropagation / TestUnknownFrame1002 / TestClientDisconnectSIGHUP 直接替换；TestSecondClient409 的第二次 Dial 必须同样带 Subprotocols: []string{proto.Subprotocol}——否则被子协议预检以 400 拦截而非到达 409（负例语义漂移防空断言）；TestDrainBeforeAttach（84-100 行，无 WS 客户端）直调 server.New 处加第三参 server.Options{} 零值——不 attach 客户端，writable/超时语义均不涉及，仅需编译适配。
       - 新增 TestHelloWelcome（tracer 端到端断言）：startTestServerWith(t, []string{"/bin/cat"}, server.Options{Writable: false}) → Dial（带子协议）不发 INPUT，先断言 Welcome mode=="ro"；随后改用 startTestServerWith Writable:true 变体断言 mode=="rw" 且 INPUT echo 正常（一段 payload 累积收齐断言，沿用 TestEchoPTY 的累积模式）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go build ./... && go vet ./... && go test -race -count=1 ./internal/proto/ ./internal/server/ ./cmd/wesh/</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/server/` 退出 0：六个既有测试保持绿色（五个 Dial 测试经 dialHello 过握手；TestDrainBeforeAttach 适配 New 签名），TestHelloWelcome 新绿
    - TestSecondClient409 的第二次握手带 wesh.v1 子协议且仍收到 HTTP 409
    - server.go 握手段含：headerHasToken 预检（400）、AcceptOptions Subprotocols 回显 + c.Subprotocol() assert、SetReadLimit(proto.ReadLimitPreAuth)、AfterFunc hello_timeout(1008)、empty_frame/frame_before_hello/malformed_hello 三处 1002 直关（均无 Error 帧）、version_mismatch 先 Error 帧后 1008、升档序列（Resize → Welcome → SetReadLimit 16KiB）；logEvent 六处埋点含 assert 兜底的 subprotocol_required
    - case proto.Input 含 `if !s.writable { continue }` ro 门；case proto.Resize 无 ro 条件分支（D-13 放行）
    - main.go 含 `--writable` flag 且 help 文案恰为 "allow client input (default read-only)"；server.New 调用带 Options{Writable: cfg.writable}
    - Attach 全部新关闭路径仅关 conn，无新增 exitf 调用点（terminate/wsDisconnected 未新增分支）
    - `go test -race -count=1 ./cmd/wesh/` 退出 0（TestParseArgs 含 writable 组）
  </acceptance_criteria>
  <done>Hello→Welcome→ro/rw 数据面端到端打通并提交；默认只读为真实服务端边界；全部既有生命周期回归在握手语义下保持绿色。</done>
</task>

<task type="auto">
  <name>Task 2: 前端握手全链路——子协议 + Hello 首帧 + Welcome 处理 + onclose 按码分派</name>
  <reversibility rating="costly">D-07：前端直接展示 Error.message 并依赖 code 分派，code 语义变更牵动前后端两侧；用户已锁定，记录权重。</reversibility>
  <files>web/src/main.ts</files>
  <read_first>
    - web/src/main.ts（被改文件现状：帧常量 6-9 行、WS 构造 65 行、onmessage 79-84 行、onData 86-93 行、sendResize 95-102 行、onopen 111-116 行、showStatus 118-136 行、onerror/onclose 138-169 行）
    - internal/proto/proto.go（02-01 产物：对齐目标常量值与 Welcome/Error JSON schema）
    - .planning/phases/02-protocol/02-RESEARCH.md（§Code Examples 前端段 438-475 行）
    - .planning/phases/02-protocol/02-PATTERNS.md（§main.ts 扩展段 296-376 行：onopen helloSent 门骨架 + onclose per-code 文案骨架）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-03/D-12①/D-14，行 23-24、38-39、43-44；specifics 行 112 前端全英文纪律）
  </read_first>
  <action>
    扩展 web/src/main.ts（全英文文案纪律，沿用 showStatus 三态面板风格）：
    1. 帧常量块新增 HELLO = 0x48、WELCOME = 0x57、ERROR = 0x45 与 SUBPROTOCOL = 'wesh.v1'，对齐注释更新为同时引用 internal/proto/proto.go（D-16 互相指路模式沿用现状 6 行注释风格）。
    2. WS 构造改 new WebSocket('ws://' + location.host + '/ws', [SUBPROTOCOL])（D-03）。
    3. onopen 重排（**顺序硬约束：线上首帧必须 Hello**）：opened=true → fit.fit() → ws.send(concat(new Uint8Array([HELLO]), enc.encode(JSON.stringify({ version: SUBPROTOCOL, cols: term.cols, rows: term.rows })))) → helloSent = true → term.focus()（Hello 携首尺寸，D-02）。term.onResize 常驻接线（现状 103 行）在首次 fit.fit() 几乎必然改尺寸触发 sendResize——不门住则 RESIZE 抢跑首帧，服务端握手段以 1002 frame_before_hello 直关（真实浏览器主路径断连，Go e2e 覆盖不到此前端时序）。落地：新增模块级 let helloSent = false（近现状 63 行 opened 声明处）；sendResize 首行防护加 if (!helloSent) return（注释钉理由：Hello 完成前禁发任何数据帧）；onopen 内 Hello send 完成后置 true——fit 先行的尺寸由 Hello cols/rows 承载（PATTERNS 注意 6 的 80x24 首帧窗口消除语义不变），Hello 之后窗口拖动经 onResize → sendResize 正常发送（握手已完成，协议合法）；删除现状 114 行的 sendResize 首发调用（已被 Hello 取代）。
    4. onmessage 由单 if 改 switch (buf[0])：case OUTPUT 不变；case WELCOME——JSON.parse(new TextDecoder().decode(buf.subarray(1)))，w.mode === 'ro' 时 term.options.disableStdin = true 且 document.title = '[ro] ' + document.title（D-14，disableStdin 使键盘不产生 onData——UX 层，真边界在服务端）；case ERROR——解析 {code,message} 存入新模块级变量 lastError（onclose 展示用）；default 忽略（未知 S→C 类型静默跳过，D-02 前向兼容同纪律）。
    5. onData 发送门保持现状（readyState OPEN 判断）——不叠加 mode 门：disableStdin 已使 ro 下不产生 onData，服务端丢 INPUT 是真防线，避免冗余（D-13/D-14 足够）。
    6. onclose 扩展为按 ev.code 分派（分派只认 code 不认 reason——库自动 1009 的 reason 是库内字符串不可控，RESEARCH Anti-Patterns 304 行）：!opened 分支保持现状 Unable to connect 文案；case 1000 保持 Session ended 现状文案；case 1008 → showStatus('Connection refused', lastError?.message ?? 'The server refused this connection.', 现状 hint)；case 1009 → showStatus('Message too large', 'Input exceeded the server message size limit and the connection was closed.', 现状 hint)（D-12① 不提 flag——本 phase 无可调 flag）；case 1011 → showStatus('Server error', lastError?.message ?? 'The server hit an internal error.', 现状 hint)；case 1013 → showStatus('Disconnected', 'The server asked this client to retry later.', 现状 hint)（Phase 5 占位路径，文案先行不占协议）；default → 现状 Connection lost 文案（覆盖 1002 与无码异常断开；1006 永不作为分派依据，RFC6455 §7.4）。
    7. 声明 lastError 变量（类型 { code: string; message: string } | null，初始 null）；onerror 现状不动。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && pnpm -C web build</automated>
    <human-check>浏览器 UAT 在 02-06 收口统一进行（end-of-phase 模式）：本任务前端行为的浏览器确认并入期末清单，此处以 tsc+vite 构建通过为准。</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm -C web build` 退出 0（tsc 类型检查随构建一体通过）
    - main.ts 含 new WebSocket(url, [SUBPROTOCOL]) 且 SUBPROTOCOL === 'wesh.v1'
    - 线上首帧必为 HELLO：sendResize 含 helloSent 门（未置位直接 return），onopen 内 Hello send 完成后才置位 helloSent；Hello JSON 载荷含 version/cols/rows 三键；不再有独立 sendResize 首发调用（term.onResize 常驻接线保留，由门吞掉首次 fit 的抢跑）
    - onmessage 为 switch 分派且含 WELCOME（disableStdin + '[ro] ' 标题前缀）与 ERROR（暂存 lastError）分支；default 静默忽略
    - onclose 含 1000/1008/1009/1011/default 分支（1013 可选占位），1009 文案不含任何 flag 名；不存在按 ev.reason 分派的代码
  </acceptance_criteria>
  <done>前端完成 wesh.v1 握手与 ro/rw 表现层适配，构建绿；真实浏览器行为并入 02-06 UAT 清单确认。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server（未认证 WS 握手） | 本 plan 的核心边界：Accept 前 HTTP 预检、Accept 后 4KiB/5s 窗口内的全部输入均不可信 |
| server → PTY 会话 | INPUT 帧跨越信任边界写入 PTY；ro 门是该边界的唯一写守门 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Elevation of Privilege | server.go case proto.Input（ro 门） | high | mitigate | 服务端按 writable 标志丢弃 INPUT（D-13 安全边界），前端 disableStdin 仅 UX；TestHelloWelcome + 02-03 TestReadOnlyDropsInput 双层锁定，禁止只在前端禁输入（Pitfall 7） |
| T-02-05 | DoS | Attach 预认证窗口 | high | mitigate | SetReadLimit(4KiB) 预认证档 + 5s AfterFunc hello_timeout(1008)（D-04/D-11）；读 ctx 恒无 deadline（Pitfall 2 防误杀） |
| T-02-06 | Tampering | 首帧校验（抢跑/空消息/畸形 Hello） | medium | mitigate | 三类违规全部 1002 直关不发 Error 帧（D-06 零反馈面）+ stderr logEvent 三要素事件（D-12②） |
| T-02-07 | Information Disclosure | version_mismatch 反馈 | low | accept | 正常客户端需要可诊断的版本错误（D-06 已裁决受众分治）；Error 帧只含 code+固定文案，无内部细节 |
| T-02-08 | Tampering | 子协议头解析 | medium | mitigate | headerHasToken 按 token 拆分精确比较（禁整头匹配，防 wesh.v1.evil 前缀绕过）+ Accept 后 c.Subprotocol() assert 双闸（D-03） |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | 零新增依赖（RESEARCH §Package Legitimacy Audit）；全部能力由已钉版 coder/websocket v1.8.15 + @xterm/xterm 6.0.0 + stdlib 覆盖 |
</threat_model>

<verification>
- `go build ./... && go vet ./...` 退出 0
- `go test -race -count=1 ./internal/... ./cmd/...` 全绿（含五个改造后既有测试 + TestHelloWelcome）
- `pnpm -C web build` 退出 0
</verification>

<success_criteria>
- 真实浏览器路径成立：wesh -- bash 后页面建连即发 Hello，收到 Welcome 后 ro 模式键盘不产生输入、标题带 [ro] 前缀
- wesh --writable -- bash 后键盘输入正常回显（rw）
- ROADMAP 准则 2 的"默认只读键盘输入被丢弃、显式开启可写后生效"自动化成立（TestHelloWelcome + TestEchoPTY）
- 预认证窗口三要素（子协议/4KiB/5s）就位，SEC-08 攻击面收敛迈出结构性一步
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `internal/server/server.go`：`Options` struct（`Writable`/`HelloTimeout` 字段）、`New(sess, exitf, opts)` 新签名、`headerHasToken` 函数、`logEvent` 方法、Attach 握手段与 ro 门、默认常量 `defaultHelloTimeout`（5s）
- `cmd/wesh/main.go`：`--writable` flag、`config.writable` 字段
- `cmd/wesh/main_test.go`：TestParseArgs 新增 writable 表驱动组
- `internal/server/e2e_test.go`：`dialHello` helper、`startTestServerWith` 变体、`TestHelloWelcome`
- `web/src/main.ts`：`HELLO`/`WELCOME`/`ERROR`/`SUBPROTOCOL` 常量、`lastError` 变量、onmessage switch 分派、onclose per-code 分派

<output>
Create `.planning/phases/02-protocol/02-02-SUMMARY.md` when done
</output>
