---
phase: 02-protocol
plan: 06
type: execute
wave: 6
depends_on: ["02-02", "02-03", "02-04", "02-05"]
files_modified:
  - README.md
autonomous: true
requirements:
  - CORE-04
  - CORE-06
  - SEC-08
  - RES-01

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "README 协议节同步 wesh.v1 现状：子协议握手、Hello{version,cols,rows}/Welcome{mode}/Error{code,message} 帧、关闭码全集表（含 1001/1013 占位标注）、默认只读语义"
    - "README 用法节含 --writable 与 --ping-interval 两个新 flag（默认值与 0=禁用语义）；无认证警示保持首屏醒目（协议基线不等于认证，Phase 3 才是公网安全）"
    - "README 明示 per-IP 半开上限在直连部署下有效、反代聚合为代理 IP 是已知限制（Pitfall 6 文档化义务）"
    - "全量收口验证六段式全绿：gofmt（GOROOT 版本）/go vet/go test -race 全量/pnpm -C web build/裸 clone 编译测试/启动冒烟（含新 flag 帮助输出）"
  artifacts:
    - path: "README.md"
      provides: "wesh.v1 协议说明、新 flag 用法、ro 默认语义、无认证警示、per-IP 直连限制"
      contains: "wesh.v1"
  key_links:
    - from: "README.md"
      to: ".planning/phases/02-protocol/02-CONTEXT.md"
      via: "协议语义与 flag 文档直译自 D-03/D-05/D-13/D-14/D-15/D-16 决策"
      pattern: "wesh\\.v1|--writable"
---

<objective>
Phase 2 收口：README 同步 wesh.v1 协议语义与两个新 flag（保持无认证警示醒目），执行全量收口验证六段式（gofmt/vet/-race 全量/前端构建/裸 clone/启动冒烟），汇总浏览器人工 UAT 清单供 end-of-phase 验证。

Purpose: 文档与实现同步是"协议即契约"的最后一环——用户按 README 理解的行为必须与 wire 实际一致；全量绿证据作为 /gsd:verify-work 前的最后一道闸。
Output: README.md 更新 + 全量验证记录 + human-check 清单。

规格探针标记（spec-less probe fallback）：本 plan 汇总四个需求（均探针 unclassified）的端到端证据链；各 plan 已分别将边界落入 truths/prohibitions，本 plan 无新增探针项，无静默丢弃。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-protocol/02-CONTEXT.md
@.planning/phases/02-protocol/02-RESEARCH.md
@.planning/phases/02-protocol/02-VALIDATION.md
@README.md
@cmd/wesh/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md——wesh.v1 协议节 + 新 flag + 默认只读语义</name>
  <files>README.md</files>
  <read_first>
    - README.md（被改文件现状：Phase 1 落地的用法/构建顺序/无认证警示/单次语义/env 白名单/协议帧各节）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-03/D-05/D-12/D-13/D-14/D-15/D-16；deferred 行 116-126——1001/1013 占位不实现的表述边界）
    - .planning/phases/02-protocol/02-RESEARCH.md（Pitfall 6 per-IP 直连限制 362-366 行）
    - cmd/wesh/main.go（flag 帮助文案与默认值真源：--writable/--ping-interval）
  </read_first>
  <action>
    更新 README.md（简体中文为主、命令与标识符英文，沿用 Phase 1 文档风格）：
    1. 无认证警示段保持首屏醒目位置不动，措辞微调补充：协议基线（握手/上限/关闭码）已就位不等于可公网暴露——认证与 TLS 在 Phase 3 到位前，仍仅在可信网络使用。
    2. 用法节 flag 表新增两行：--writable（默认 false：只读模式，客户端输入被丢弃；开启后客户端输入写入终端）；--ping-interval（默认 5s：WS ping 保活间隔，防反代空闲超时断连；0 = 禁用）。
    3. 协议节重写为 wesh.v1 现状：连接必须协商子协议 wesh.v1（缺失以 HTTP 400 拒绝）；建连后客户端首帧必须 Hello{version,cols,rows}（5s 超时，抢跑/畸形以 1002 关闭）；服务端回 Welcome{mode}（ro/rw）；数据帧 '0' INPUT/OUTPUT、'1' RESIZE；错误帧 'E' {code,message}（仅 version_mismatch/server_error 两个正常客户端可见码）；关闭码全集表 {1000 正常, 1002 协议错误, 1008 策略违反, 1009 超限, 1011 内部错误} + 注释 1001/1013 已在协议占住、由后续阶段启用；消息上限：预认证 4KiB、稳态 16KiB（单帧与累积字节，超限以 1009 关闭）。
    4. 只读语义小节：默认只读——浏览器键盘不产生输入（标题显示 [ro] 前缀），裸 WS 客户端发 INPUT 同样被服务端丢弃；RESIZE 在只读下照常生效；--writable 开启可写。
    5. 部署注意小节（新增或并入安全说明）：per-IP 半开连接上限（默认 8）在直连部署下有效；置于反向代理后时所有客户端聚合为代理 IP，该限制可能误伤——可信头透传属后续阶段（SEC-07）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && test -f README.md && grep -q 'wesh.v1' README.md && grep -q -- '--writable' README.md && grep -q -- '--ping-interval' README.md && grep -q '1009' README.md && grep -q 'Phase 1 无认证\|无认证' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README 含 wesh.v1 子协议要求、Hello/Welcome/Error 帧说明、关闭码全集表（含 1001/1013 占位标注）
    - 含 --writable 与 --ping-interval 的默认值与语义（0=禁用）
    - 含默认只读语义（服务端丢弃 + [ro] 标题前缀 + RESIZE 放行）
    - 含 per-IP 上限的直连部署限制说明
    - 无认证警示仍在标题之后的首个段落区块
  </acceptance_criteria>
  <done>README 与 wesh.v1 wire 实际行为一致；两项新 flag 与只读默认对用户透明。</done>
</task>

<task type="auto">
  <name>Task 2: 全量收口验证（六段式）+ 浏览器 UAT 清单</name>
  <precondition>plan 02-01 至 02-05 已全部完成并合入（depends_on 保证）；CI 远端运行结果不在本任务断言范围（推送时机由用户决定）。</precondition>
  <files>README.md</files>
  <read_first>
    - .planning/phases/02-protocol/02-VALIDATION.md（Sampling Rate 节：phase gate 命令）
    - .planning/phases/01-pty/01-05-PLAN.md（Task 2 六段式收口模式参照：含 GOROOT gofmt 与裸 clone 归档测试——/usr/bin/gofmt 陈旧教训，Phase 01-03 决策）
    - .planning/ROADMAP.md（Phase 2 成功准则三条，行 64-68）
  </read_first>
  <action>
    按 VALIDATION §Sampling Rate 的 phase gate 执行全量收口（全程在 ~/open_src/wesh）：
    1. "$(go env GOROOT)/bin/gofmt" -l . 输出为空（不用 /usr/bin/gofmt——陈旧版本教训）；如有输出则 -w 修正后重跑。go vet ./... 退出 0。
    2. go test -race -count=1 ./... 退出 0（不设 CGO_ENABLED=0）；确认握手/守卫/保活/上限全部测试组在列。
    3. pnpm -C web install --frozen-lockfile && pnpm -C web build 退出 0；test -f web/dist/index.html.gz。
    4. 裸 clone 模拟：git archive --format=tar HEAD | (mkdir -p /tmp/wesh-clean && tar -x -C /tmp/wesh-clean) 后 cd /tmp/wesh-clean && go build ./... && go test ./... -count=1 退出 0；清理 /tmp/wesh-clean。
    5. 运行冒烟：go build -o /tmp/wesh-bin ./cmd/wesh；/tmp/wesh-bin --help 输出含 --writable 与 --ping-interval 两行；/tmp/wesh-bin --writable --ping-interval 1s -- /bin/cat &（记录 pid）→ 启动输出恰一行且匹配 listening on http:// → curl / 返回 200 → curl 无子协议直取 /ws 返回 400（子协议预检冒烟）→ 结束后 kill 进程组并 pgrep -f wesh-bin 确认无残留。
    6. 若 web 构建产物使 git status 显示 web/dist/index.html 被修改，保留真实产物提交（README 已说明构建顺序）。
    7. 把全量验证结果写入 SUMMARY（逐条命令 + 退出码）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go vet ./... && go test -race -count=1 ./... && pnpm -C web build && test -z "$("$(go env GOROOT)/bin/gofmt" -l .)"</automated>
    <human-check>
      浏览器手动确认清单（end-of-phase 汇总，对应 ROADMAP 准则 2/3 与 D-12①/D-14）：
      1. 只读默认：wesh -- bash 后浏览器打开页面——标题带 [ro] 前缀，键盘敲击终端无反应；DevTools → Network → WS 面板可见首帧 Hello 与 Welcome(mode=ro)，后续每 5s 一个 ping/pong 帧对。
      2. 可写模式：wesh --writable -- bash 刷新页面——键入命令正常回显，Welcome(mode=rw)。
      3. resize：ro 模式下拖动浏览器窗口，web shell 里无法输入但全屏程序（先以 --writable 起 vim 观察）尺寸跟随——或直接经 DevTools 观察 RESIZE 帧照常发出。
      4. 关闭码文案：wesh -- bash 后在 shell 里 Ctrl-C 或让子进程 exit，页面显示 Session ended；用 wscat 或 DevTools 伪造 wesh.v9 版本 Hello（或不发 Hello 等 5s），页面分别显示 Connection refused（含 version_mismatch 文案）/被 1008 关闭——onclose 按码分派生效。
      5. 单客户端：另开第二个标签访问同地址显示 Unable to connect（409 语义不变）。
    </human-check>
  </verify>
  <acceptance_criteria>
    - automated 四命令全部退出 0；裸 clone 目录中 go build 与 go test 退出 0
    - 冒烟：--help 含两个新 flag；启动单行打印；/ 返回 200；无子协议 /ws 返回 400；收尾无 wesh 进程残留
    - SUMMARY 记录全部验证命令与退出码；human-check 五项由用户在 end-of-phase 验证确认
  </acceptance_criteria>
  <done>Phase 2 全量验证绿、文档与 wire 一致、浏览器 UAT 清单就绪待确认。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档 → 部署者 | README 是部署安全认知的唯一来源：无认证警示与 per-IP 直连限制属补偿控制 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-21 | Information Disclosure | README（部署者误部署到不可信网络） | high | mitigate | 无认证警示保持首屏醒目并补充"协议基线 ≠ 公网安全"；per-IP 反代聚合限制显式文档化（Pitfall 6 义务） |
| T-02-22 | Tampering | 文档与 wire 漂移（用户按 README 构造客户端被意外拒绝） | medium | mitigate | 协议节直译 D-03/D-05/D-13~D-16 决策与 main.go flag 真源；grep 断言关键串存在 |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | 零新增依赖（RESEARCH §Package Legitimacy Audit） |
</threat_model>

<verification>
- Task 2 automated 四命令退出 0 + 裸 clone 编译测试绿 + 冒烟四断言
- human-check 五项在 end-of-phase 人工验证中确认
- VALIDATION.md Per-Task Verification Map 全部行为 green 或手动确认
</verification>

<success_criteria>
- ROADMAP Phase 2 三条成功准则全部有证据：准则 1（两层硬顶 + 等效防线，02-05 五测）、准则 2（ro 默认 + 关闭码集合，02-02/02-03 测试 + UAT）、准则 3（ping/pong 保活，02-04 三测 + UAT devtools 观察）
- README 与实现一致，无认证警示与部署限制两项补偿控制就位
- 仓库处于可推送状态（gofmt/vet/-race/前端构建全绿）
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `README.md`（更新；无新代码符号）

<output>
Create `.planning/phases/02-protocol/02-06-SUMMARY.md` when done
</output>
