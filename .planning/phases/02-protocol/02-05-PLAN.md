---
phase: 02-protocol
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - README.md
autonomous: true
requirements:
  - CORE-04
  - CORE-06
  - SEC-08
  - RES-01

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "README 用法段与实际 CLI 一致：--writable（默认只读，开启后接受客户端输入）与 --ping-interval（默认 5s，0=禁用）两个新 flag 在列，flag 总数与 `wesh --help` 输出一致"
    - "README 明示 Phase 2 协议基线语义：wesh.v1 子协议握手、默认只读边界（ro 丢 INPUT 放行 RESIZE）、三层上限行为（超限以 1009 关闭并见人话文案）、ping/pong 保活与反代空闲超时的关系"
    - "README 保留 Phase 1 的无认证醒目警示与单次语义说明（Phase 3 认证落地前不得弱化）；新增反代部署注意：per-IP 半开帽按代理 IP 聚合的已知限制（Pitfall 7 文档化要求）"
    - "全量验证绿：go vet ./... && go test -race -count=1 ./... && pnpm -C web build && go build ./... 退出 0"
  artifacts:
    - path: "README.md"
      provides: "用法（含新 flag）/ 协议基线说明 / 无认证警示 / 反代注意 / 构建顺序"
      contains: "--writable"
  key_links:
    - from: "README.md"
      to: "cmd/wesh/main.go"
      via: "flag 文档与 parseArgs 注册集一致（--port/--bind/--version/--writable/--ping-interval）"
      pattern: "--writable|--ping-interval"
---

<objective>
Phase 2 收口：README 同步协议基线与新 flag（文档一致性——Phase 1 README 只写四个 flag，本 phase 新增两个后文档必须与 --help 输出一致）+ 全量收口验证（-race 全量、前端构建、embed 链）+ 浏览器手动 checklist（ro 表现/1009 文案/ping-pong 帧）。

Purpose: 把协议行为语义与全绿证据一并落地，作为 /gsd:verify-work 前的最后一道闸；VALIDATION.md 的两个 manual-only 项（浏览器端 ro 表现、预认证内存平坦走查）在此汇总。
Output: README.md 更新 + 全量验证记录 + 手动 checklist 结果。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-protocol/02-CONTEXT.md
@.planning/phases/02-protocol/02-RESEARCH.md
@.planning/phases/02-protocol/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README 同步——新 flag + 协议基线语义 + 反代注意</name>
  <files>README.md</files>
  <read_first>
    - README.md（被改文件现状——Phase 1 plan 01-05 产物：无认证警示、单次语义、四 flag 用法、构建顺序）
    - cmd/wesh/main.go（flag 注册集与 help 文案现状——plan 02-04 产物）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-12①不提 flag 的文案纪律、D-13 ro 边界、D-16 保活参数）
    - .planning/phases/02-protocol/02-RESEARCH.md（Pitfall 7 反代聚合行 471-475——文档化是本 phase 的明确处置）
  </read_first>
  <action>
    更新 `README.md`（沿用 Phase 1 简体中文为主、命令与标识符英文的文体；既有段落结构保留，以下为修改点）：
    1. 用法段 flag 列表更新为六个：`--port`（默认 7681，0=随机并打印实际端口）、`--bind`（默认 0.0.0.0）、`--version`、`--help`、`--writable`（allow client input (default read-only)——默认只读：浏览器键盘输入被服务端丢弃，终端窗口 resize 仍同步）、`--ping-interval`（WS ping interval in seconds (0 = disable)，默认 5）。
    2. 新增"协议"小节（简明段落即可）：WS 端点 /ws 强制 wesh.v1 子协议握手（Hello/Welcome/Error 类型化帧）；默认只读边界 = 丢 INPUT 放行 RESIZE（--writable 开启输入）；消息三层上限（单帧/分片数/累积字节）超限连接以 1009 关闭并显示 "Message too large" 面板；ping/pong 保活防反代空闲超时断连（间隔 --ping-interval）。
    3. 无认证警示段保留不弱化（Phase 3 才落地认证/TLS）；单次语义段保留。
    4. 反代注意（一段）：经 nginx/Cloudflare 等反代部署时，未认证半开连接帽按代理 IP 聚合（本阶段不解析 X-Forwarded-For——直接信不可信头等于伪造绕过）；正常浏览器秒过握手窗口不受影响；--ping-interval 须显著小于反代空闲超时（如 nginx proxy_read_timeout 60s，默认 5s 即安全）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && grep -q -- '--writable' README.md && grep -q -- '--ping-interval' README.md && grep -q 'wesh.v1' README.md && grep -q '无认证' README.md && go run ./cmd/wesh --help 2>&1 | grep -c -- '--' | grep -qE '^[0-9]+$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - README 含 --writable 与 --ping-interval 两条 flag 说明且语义与 main.go help 文案一致（默认只读 / 默认 5s、0=禁用）
    - README 协议小节含 wesh.v1 子协议、默认只读边界（INPUT 丢弃 + RESIZE 放行）、1009 超限关闭、ping/pong 保活四要点
    - 无认证醒目警示与单次语义说明原文保留（grep '无认证' 命中）
    - 反代注意段含 per-IP 聚合限制与 ping 间隔 < 反代空闲超时两要点
    - `wesh --help` 实际输出中的 flag 集合与 README 列表一致（六条：--port/--bind/--version/--help/--writable/--ping-interval）
  </acceptance_criteria>
  <done>README 与 Phase 2 协议行为及 CLI 面完全一致，警示不弱化，反代部署注意文档化。</done>
</task>

<task type="auto">
  <name>Task 2: 全量收口验证 + 浏览器手动 checklist 汇总</name>
  <files>（无文件修改——验证任务）</files>
  <read_first>
    - .planning/phases/02-protocol/02-VALIDATION.md（Per-Task Verification Map 全部条目；Manual-Only Verifications 行 73-79 两项；Sampling Rate 行 30-36）
  </read_first>
  <action>
    1. 全量验证序列逐项执行并记录输出：`go vet ./...` → `go test -race -count=1 ./...` → `pnpm -C web build` → `go build ./...`（embed 链以真实 dist 重建）。
    2. VALIDATION.md Per-Task Verification Map 全量点名执行（22 条自动化命令逐条跑一遍 -count=1，确认无遗漏无 skip）：TestCloseCodeSet / TestHelloIgnoresUnknown / TestEchoPTY / TestReadOnlyDropsInput / TestReadOnlyAllowsResize / TestVersionMismatch / TestPrematureFrame / TestTextFrame1002 / TestSubprotocolRequired / TestHelloTimeout / TestHalfOpenCap / TestHalfOpenRelease / TestPreAuthReadLimit / TestNoPerMessageDeflate / TestMessageTooBig / TestFragmentCountLimit / TestEmptyFrame / TestFragmentByteFlood / TestFragmentTrickle / TestPingKeepalive / TestPingDisabled / TestPongTimeout，外加既有回归全量（go test ./internal/server -count=1 隐含覆盖）。
    3. 启动冒烟：`go run ./cmd/wesh -- /bin/cat` 起服后 curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:<port>/ 断言 200；curl 无子协议头直接升级 /ws 断言 400（预检线上行为抽查）。
    4. 预认证内存平坦走查（VALIDATION manual-only 行 77）：人工走查 Attach 源码确认守卫区（400/429/409）在 Accept 前零 WS 对象分配、预认证期仅 4KiB 读限档与库固定缓冲——走查结论写入 SUMMARY。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go vet ./... && go test -race -count=1 ./... && pnpm -C web build && go build ./... && echo FULL-GATE-OK</automated>
    <human-check>浏览器手动 checklist（VALIDATION 行 78 汇总）：① `go run ./cmd/wesh -- bash` 开页面——标题带 "[ro] " 前缀、键盘输入无任何回显（ro 默认）；拖动窗口终端尺寸跟随（RESIZE 放行）；② DevTools → Network → WS 连接 → Frames 面板可见周期性 ping/pong 控制帧；③ 粘贴一段 >16KiB 的长文本（--writable 模式下）触发 1009——面板显示 "Message too large" 人话文案；④ `go run ./cmd/wesh --writable -- bash` 键盘输入正常回显。四项全过即 Phase 2 手动面收口。</human-check>
  </verify>
  <acceptance_criteria>
    - 全量验证序列四步全部退出 0（FULL-GATE-OK）
    - VALIDATION.md 全部 22 条点名测试逐条 -count=1 通过，无 skip
    - 启动冒烟：/ 返回 200；无子协议升级 /ws 返回 400
    - 预认证内存平坦走查结论记录于 SUMMARY（守卫区零分配 + 4KiB 档确认）
    - human-check 四项结果记录于 SUMMARY（通过/问题逐条）
  </acceptance_criteria>
  <done>全量绿证据 + 手动 checklist + 走查结论落地，Phase 2 达到 /gsd:verify-work 入口条件。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档 ↔ 用户 | README 是部署者的安全行为依据（无认证警示的补偿控制） |
| npm/go 依赖安装 | 本 plan 零依赖变更 |

## STRIDE Threat Register（ASVS L1，block on high）

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-01 | Information Disclosure | README 弱化/遗漏无认证警示（用户误以为已可公网暴露） | high | mitigate | acceptance criteria 断言警示原文保留（grep '无认证'）；警示仅在 Phase 3 认证/TLS 落地后才允许改写 |
| T-02-05-02 | Tampering | 文档与实现漂移（flag 列表/默认值不符误导运维） | medium | mitigate | acceptance criteria 断言 README flag 集与 `wesh --help` 输出一致（六条） |
| T-02-05-SC | Tampering | 依赖安装 | high | mitigate | 本 plan 零依赖变更 |
</threat_model>

<verification>
- `go vet ./... && go test -race -count=1 ./... && pnpm -C web build && go build ./...` 全绿
- VALIDATION.md 22 条点名测试逐条通过
- 启动冒烟：/ 200、无子协议 /ws 400
- 浏览器 human-check 四项（[ro] 前缀与键盘无响应 / ping-pong 帧可见 / 1009 文案 / --writable 回显）
</verification>

<success_criteria>
- ROADMAP Phase 2 三条成功准则全部具备自动化或手动证据：三层上限 1009 不崩溃（plan 02-03）✓、默认只读 + 关闭码集合（plan 02-01/02-02）✓、ping/pong 保活（plan 02-04）✓
- README 与实现一致，无认证警示不弱化
- 达到 /gsd:verify-work 入口条件
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `README.md`（修改）：--writable/--ping-interval flag 文档；协议小节（wesh.v1/ro 边界/1009/保活）；反代注意段。无新增代码符号。

<output>
Create `.planning/phases/02-protocol/02-05-SUMMARY.md` when done.
</output>
