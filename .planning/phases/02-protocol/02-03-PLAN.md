---
phase: 02-protocol
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - internal/server/server.go
  - cmd/wesh/main.go
  - internal/server/handshake_test.go
autonomous: true
requirements:
  - SEC-08
  - CORE-04

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "同 IP 第 N+1 条半开（Hello 未完成）连接在 Accept 前收到 HTTP 429，per-IP 上限默认 8（D-04）；Hello 完成即释放名额，正常浏览器秒发 Hello 不受限，NAT 多人场景已完成 Hello 者不计入"
    - "per-IP 计数不变量成立：acquire 成功后 release 恰好一次（Hello 完成 / Accept 失败 / assert 失败 / 409 拒绝 / 连接终结各路径不泄漏不双重释放，Pitfall 4）"
    - "per-IP 429 闸位于子协议 400 之后、409 原子门之前——被 409 拒的连接已释放名额不残留计数（planner 裁决：此顺序是 D-04 在单客户端模型下可触达可测试的唯一形态）"
    - "http.Server ReadHeaderTimeout=5s 落地，HTTP 层慢 loris 被盒住；ReadTimeout/WriteTimeout 不设（误伤 WS 长连接）"
    - "无子协议或错子协议握手收到 HTTP 400 且零 WS 资源分配；多值头含 wesh.v1 正常放行（D-03 + Pitfall 5）"
    - "5s 内未完成 Hello 的连接收到 1008 且 close reason 为 hello_timeout（D-04/D-07）"
    - "ro 模式下裸 WS 客户端发 INPUT 不进 PTY（无回显）而 RESIZE 放行尺寸跟随（D-13 服务端边界，Pitfall 7 回归）"
    - "守卫拒绝响应（400/429/409/1002/1008）不携带任何内部实现细节（对攻击者只给标准状态/码值，D-06）"
    - statement: "预认证路径（Accept 前守卫区 + 4KiB 窗口）无消息级缓冲预分配——拒绝与握手全程单连接内存占用有界（SEC-08 结构目标，内存采样为参考断言）"
      verification: backstop
  prohibitions:
    - "预认证违规路径（无子协议/抢跑/畸形/超时/超限）不得向对端发送 Error 帧或任何解释性载荷——只给关闭码与标准 HTTP 状态，攻击面零反馈（D-06）"
  artifacts:
    - path: "internal/server/server.go"
      provides: "per-IP 半开计数器（acquire/release）+ clientIP + 429 闸插入守卫区"
      contains: "halfOpen"
    - path: "cmd/wesh/main.go"
      provides: "http.Server ReadHeaderTimeout=5s"
      contains: "ReadHeaderTimeout"
    - path: "internal/server/handshake_test.go"
      provides: "SEC-08 守卫链与握手违规全测试组（七测）"
      contains: "TestSubprotocolRequired / TestHalfOpenPerIP429 / TestHelloTimeout / TestPrematureFrame / TestVersionMismatch / TestReadOnlyDropsInput / TestReadOnlyAllowsResize"
  key_links:
    - from: "internal/server/server.go"
      to: "cmd/wesh/main.go"
      via: "main 以 http.Server{Handler: srv.Handler(), ReadHeaderTimeout: 5s} 替换 http.Serve"
      pattern: "ReadHeaderTimeout"
    - from: "internal/server/handshake_test.go"
      to: "internal/server/server.go"
      via: "startTestServerWith Options{MaxHalfOpenPerIP/HelloTimeout} 注入小值驱动守卫行为"
      pattern: "MaxHalfOpenPerIP|HelloTimeout"
---

<objective>
补齐 SEC-08 预认证守卫链最后一道闸与全部握手违规测试：per-IP 半开连接上限（默认 8，超限 HTTP 429）插入守卫区（子协议 400 → per-IP 429 → 409 → Accept → assert），http.Server ReadHeaderTimeout=5s 盒住 HTTP 层慢 loris，新建 handshake_test.go 承载守卫链与 ro 边界的七个攻击面/行为测试。

Purpose: 02-02 已把握手骨架端到端打通；本 plan 把"不发言占坑"（半开慢 loris）与"扫描洪水"两条剩余预认证 DoS 面盒住，并用测试把 D-03/D-04/D-06/D-13 的违规路径全部锁成自动化回归。
Output: server.go per-IP 计数器 + main.go ReadHeaderTimeout + handshake_test.go 七测全绿。

规格探针标记（spec-less probe fallback）：SEC-08 探针报为 unclassified；边界按 D-03/D-04 与研究验证落入 must_haves.truths，"预认证零缓冲"以 backstop 标记（内存采样脆弱、代码走查 + flood 测试为证），攻击面零反馈落入 prohibitions，无静默丢弃。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-protocol/02-CONTEXT.md
@.planning/phases/02-protocol/02-RESEARCH.md
@.planning/phases/02-protocol/02-PATTERNS.md
@internal/server/server.go
@cmd/wesh/main.go
@internal/server/e2e_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: per-IP 半开计数器 + 429 闸 + http.Server ReadHeaderTimeout</name>
  <files>internal/server/server.go, cmd/wesh/main.go</files>
  <read_first>
    - internal/server/server.go（被改文件现状：02-02 后的守卫区与握手段——子协议预检/409 门/Accept/assert/4KiB/5s 计时器各埋点位置）
    - cmd/wesh/main.go（被改文件现状：run 装配 53-84 行，http.Serve 调用点 79 行）
    - .planning/phases/02-protocol/02-RESEARCH.md（Pattern 1 守卫链 186-224 行、Pitfall 4 计数器泄漏 350-355 行、Pitfall 5 子协议多值 356-361 行、Pitfall 6 per-IP 键 362-366 行）
    - .planning/phases/02-protocol/02-PATTERNS.md（§No Analog Found 表 471-481 行：per-IP 计数器与 clientIP 为决策直译，无库内 analog）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-04，行 23-24）
  </read_first>
  <behavior>
    - MaxHalfOpenPerIP=1 注入时：c1 半开（Accept 后不发 Hello）→ c2 收到 HTTP 429；c1 随后正常发 Hello 完成握手（429 不误伤在先连接）
    - 生产默认：同 IP 第 9 条并存半开 → 429；Hello 完成的连接立即释放名额
    - 被 409 拒绝的连接不残留半开计数（acquire→release 恰好一次）
    - 慢发 HTTP 头的连接 5s 内被 http.Server 切断（ReadHeaderTimeout）
  </behavior>
  <action>
    1. `internal/server/server.go`：
       - Options 追加字段 MaxHalfOpenPerIP int（零值取默认常量 defaultMaxHalfOpenPerIP = 8，D-04；注释标定：正常浏览器秒发 Hello 不受限；NAT 多人场景 Hello 已完成不计入）。
       - 新增 halfOpenCounter 类型（struct{ mu sync.Mutex; n map[string]int }）：acquire(ip string, max int) bool（n[ip] >= max 返回 false，否则 +1 返回 true）；release(ip string)（-1，到 0 删除 key 防 map 单调增长——Pitfall 4 泄漏面）。Server 结构体加 halfOpen 字段，New 中初始化。
       - 新增 clientIP(r *http.Request) string：net.SplitHostPort(r.RemoteAddr) 取主机部分；SplitHostPort 失败回退 r.RemoteAddr 整串。注释注明：反代部署下同键聚合为代理 IP 是已知限制（Pitfall 6），X-Forwarded-For 信任属 Phase 3 SEC-07，本 phase 不解析。
       - 守卫区插入 429 闸，最终顺序（planner 裁决，代码注释记录裁决理由）：① 子协议 400（现状）→ ② per-IP acquire 失败 → http.Error(w, "too many pending connections", http.StatusTooManyRequests) 返回 → ③ 409 CAS（失败路径先 release(ip) 再 409）→ Accept（失败 release + 释放门位）→ assert（失败 Close(1008) + release + 释放门位）→ 握手段。不变量：acquire 成功 → release 恰好一次，发生在 Hello 完成或任一拒绝/失败路径（先到为准）；握手段内 Hello 校验通过后、Welcome 发出前调用 release——用局部 sync.Once 或 released 布尔保证恰好一次（实现形态 discretion，注释钉不变量）。注释写明：per-IP 在 409 之前是 D-04 可触达性裁决（409 在前则 429 在单客户端模型下结构性不可达，RESEARCH 自身的 TestHalfOpenPerIP429 映射亦无法构造）；被 409 拒的连接 acquire→release 不残留计数。
    2. `cmd/wesh/main.go`：http.Serve(ln, srv.Handler()) 替换为 hs := &http.Server{Handler: srv.Handler(), ReadHeaderTimeout: 5 * time.Second} 后 hs.Serve(ln)（注释：预认证 HTTP 层慢 loris 防御，与 helloTimeout 同 5s 量级；ReadTimeout/WriteTimeout 不设——会误伤 WS 长连接语义）。imports 加 time。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go build ./... && go vet ./... && go test -race -count=1 ./internal/server/ ./cmd/wesh/</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./... && go vet ./...` 与既有测试全量退出 0（本任务不改行为语义，只加新闸——既有测试保持绿色即证明无回归）
    - server.go 守卫区顺序为：子协议 400 → per-IP 429 → 409 → Accept → assert，注释含 D-04 可触达性裁决理由
    - halfOpenCounter.release 到 0 删除 map key；Options 含 MaxHalfOpenPerIP 且零值默认 8
    - main.go 使用 http.Server{ReadHeaderTimeout: 5 * time.Second}，未设置 ReadTimeout/WriteTimeout
  </acceptance_criteria>
  <done>per-IP 429 与 ReadHeaderTimeout 两道闸落地，守卫区四闸顺序完整，计数不变量注释钉死。</done>
</task>

<task type="auto">
  <name>Task 2: handshake_test.go——守卫链与握手违规七测</name>
  <files>internal/server/handshake_test.go</files>
  <read_first>
    - internal/server/e2e_test.go（同包 helper 直接复用：startTestServerWith/dialHello/waitExit/CloseError 读取循环 267-278 行、负例 HTTP 状态断言 213-223 行）
    - internal/server/server.go（02-02 + 本 plan Task 1 后的守卫区与握手段实现——测试断言目标）
    - .planning/phases/02-protocol/02-RESEARCH.md（§Validation Architecture 测试映射 547-558 行、§测试可注射性注意 559 行）
    - .planning/phases/02-protocol/02-PATTERNS.md（§关键攻击面用例矩阵 408-425 行——D-09 修订后形态，全部库客户端构造，裸帧 helper 已作废）
  </read_first>
  <action>
    新建 internal/server/handshake_test.go（package server_test，复用 e2e_test.go 的 startTestServerWith/dialHello/waitExit，全部测试超时护栏 10s）：
    1. TestSubprotocolRequired（SEC-08/D-03）：三组——(a) Dial 不带 Subprotocols → err 非 nil 且 resp.StatusCode==400；(b) Dial Subprotocols:["wesh.v2"] → 400；(c) Dial Subprotocols:["other","wesh.v1"] → 成功（多值头 token 命中放行，Pitfall 5 回归）；(c) 随后正常 dialHello 发 Hello 流程走完或直接 Close 收口。exitf 收口断言：因 (a)(b) 未建连不触发终结，(c) Close 后 waitExit(0)。
    2. TestHalfOpenPerIP429（SEC-08/D-04）：startTestServerWith(t, []string{"/bin/cat"}, server.Options{Writable: true, MaxHalfOpenPerIP: 1})；c1 Dial（带 wesh.v1）成功但不发 Hello（占住半开名额）→ c2 Dial（带 wesh.v1）→ err 非 nil 且 resp.StatusCode==429 → c1 补发 Hello 收 Welcome（断言 429 未误伤在先连接）→ INPUT echo 一句确认全链路 → c1.Close(1000) → waitExit(0)。
    3. TestHelloTimeout（SEC-08/D-04）：startTestServerWith Options{HelloTimeout: 200 * time.Millisecond}；Dial（带子协议）后静默 → 读至 CloseError，断言 Code==1008 且 Reason=="hello_timeout"（容忍窗 2s 内到达）→ waitExit(0)（reader 终结走 D-11 收口）。
    4. TestPrematureFrame（D-04/D-06）：默认 startTestServerWith Writable:true；Dial（带子协议）后直接发 INPUT 帧（不发 Hello）→ 读至 CloseError Code==1002；断言读取过程中未收到任何 'E' 类型消息帧（攻击面零反馈：第一个读事件即关闭）。
    5. TestVersionMismatch（D-06/D-07）：Dial（带子协议）后发 Hello{version:"wesh.v9",cols:80,rows:24} → 首读收到 'E' 帧，JSON 解码 code=="version_mismatch" 且 message 非空 → 继续读至 CloseError Code==1008 且 Reason=="version_mismatch"（D-07 同名机器串）→ waitExit(0)。
    6. TestReadOnlyDropsInput（CORE-04/D-13，Pitfall 7 服务端边界回归）：startTestServerWith Writable:false；dialHello(t, ctx, wsURL, 80, 24) 断言 mode=="ro"；发 INPUT "x" → 200ms 静默窗口断言服务端丢弃（/bin/cat 无启动 banner，窗口内静默即证明）。**窗口实现硬约束：禁止客户端 Read 携带 deadline ctx**（RESEARCH Pitfall 2 已核实 conn.go:188-199——deadline 到期库内 AfterFunc 直接 c.close() 关整条连接且无关闭帧，随后的 RESIZE 存活断言写已关闭连接必败）；窗口用 goroutine 跑 c.Read(context.Background()) 结果入缓冲 channel，select channel 对 time.After(200ms) 竞速：窗口内 channel 出队任何结果（读到数据=INPUT 未被丢弃；读到错误=连接被误关）均 t.Fatal，超时无消息即静默证明成立 → 连接存活断言：发 RESIZE {cols:90,rows:30} 写路径不报错，随后 c.Close(1000) 正常完成关闭握手（写关闭帧成功即连接存活证据；读 goroutine 以 CloseError 1000 收场，channel 带缓冲无泄漏）→ waitExit(0)。
    7. TestReadOnlyAllowsResize（CORE-04/D-13）：startTestServerWith Writable:false，argv 用 []string{"/bin/sh", "-c", "sleep 0.5; stty size; sleep 1; stty size"}（测试编排夹具，非产品 spawn 路径；**前导 sleep 0.5 是硬要求**——sh 随 spawn 立即执行，attach 前 PTY 输出被 onChunk drain 丢弃（server.go:124 现状），无前导休眠则 stty#1 输出丢失、stty#2 后 sh 退出以 1000 关闭，第二个尺寸断言永不可达；0.5s 窗口保证 dialHello 完成握手后两次 stty 才执行，本地回环 attach 毫秒级、竞态余量充足）；dialHello(t, ctx, wsURL, 111, 44)（02-02 参数化签名复用）→ 收首个 stty 输出断言 "44 111"（Hello 携尺寸生效，80x24 窗口消除）；发 RESIZE {cols:120,rows:50} → 收第二个 stty 输出断言 "50 120"（ro 下 RESIZE 放行）；PTY 输出按 strings.Fields 切分断言免疫 ONLCR（Phase 1 决策沿用）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test -race -count=1 -v -run 'TestSubprotocolRequired|TestHalfOpenPerIP429|TestHelloTimeout|TestPrematureFrame|TestVersionMismatch|TestReadOnlyDropsInput|TestReadOnlyAllowsResize' ./internal/server/ && go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - 七个测试全部绿色且 -race 无报告
    - TestSubprotocolRequired 含 400（无子协议）/400（错子协议）/放行（多值头）三断言
    - TestHalfOpenPerIP429 断言 c2 收到 429 且 c1 随后握手成功（不误伤）——acquire/release 恰好一次的间接证明
    - TestPrematureFrame 断言 1002 之前无任何 'E' 帧（D-06 零反馈面的自动化锁定）
    - TestVersionMismatch 断言先 'E' 帧后 1008 且两处机器串同名
    - TestReadOnlyDropsInput 证明服务端丢弃（非仅前端 disableStdin），静默窗口由 goroutine Read(context.Background()) + channel + select time.After 竞速实现——客户端 Read 不携带 deadline ctx（Pitfall 2 回归锁，防库 AfterFunc 杀整条连接使存活断言失效）；TestReadOnlyAllowsResize 证明 ro 下 RESIZE 放行，夹具含前导 sleep 使两次 stty 均在 attach 后执行（防 server.go:124 drain 吞掉首个输出）
    - `go test -race -count=1 ./internal/server/` 全量退出 0
  </acceptance_criteria>
  <done>SEC-08 三道预认证闸与 D-04/D-06/D-13 全部违规路径自动化锁定；CORE-04 ro 边界双测（丢 INPUT/放行 RESIZE）成立。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 未认证 HTTP/WS 请求 → 守卫区 | 本 plan 的唯一边界：Accept 前的 HTTP 层与 Accept 后 Hello 前的 4KiB 窗口，全部输入不可信 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | DoS | 半开连接占用（慢 loris 变种） | high | mitigate | per-IP 半开上限 8（HTTP 429）+ 5s hello_timeout（1008）+ ReadHeaderTimeout 5s 三层盒住；TestHalfOpenPerIP429/TestHelloTimeout 锁定 |
| T-02-10 | DoS | per-IP 计数器泄漏导致正常用户被误 429 | medium | mitigate | release 恰好一次不变量注释钉死 + 到 0 删 key；TestHalfOpenPerIP429 的"c1 随后握手成功"断言间接证明不泄漏（Pitfall 4） |
| T-02-11 | Information Disclosure | 守卫拒绝响应（400/429/409） | low | accept | 标准 HTTP 状态语义不含内部细节；D-06 已裁决攻击面零反馈，TestPrematureFrame 锁定无 'E' 帧 |
| T-02-12 | Elevation of Privilege | ro 模式 INPUT 绕过 | high | mitigate | TestReadOnlyDropsInput 以裸 WS 客户端视角断言服务端丢弃——前端 disableStdin 不可信（Pitfall 7） |
| T-02-13 | Tampering | 子协议多值头误判 | low | mitigate | TestSubprotocolRequired(c) 多值头放行组锁定 headerHasToken 拆分语义（Pitfall 5） |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | 零新增依赖（RESEARCH §Package Legitimacy Audit） |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/server/` 全绿（七新测 + 既有七测）
- `go build ./... && go vet ./...` 退出 0
- 守卫区顺序代码评审：子协议 400 → per-IP 429 → 409 → Accept → assert → 4KiB → 5s 计时器
</verification>

<success_criteria>
- SEC-08 预认证攻击面四道闸全部落地并有自动化证据：400（子协议）/429（半开帽）/409（单客户端）/4KiB+5s（窗口）
- ROADMAP 准则 2 后半（关闭码集合）在 1002/1008 路径上获得断言覆盖
- CORE-04 的服务端边界性质（裸客户端也无法写入）自动化证明成立
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `internal/server/server.go`：`halfOpenCounter` 类型（`acquire`/`release`）、`clientIP` 函数、`Options.MaxHalfOpenPerIP` 字段、默认常量 `defaultMaxHalfOpenPerIP`（8）
- `cmd/wesh/main.go`：`http.Server{ReadHeaderTimeout: 5s}` 装配
- `internal/server/handshake_test.go`：`TestSubprotocolRequired`/`TestHalfOpenPerIP429`/`TestHelloTimeout`/`TestPrematureFrame`/`TestVersionMismatch`/`TestReadOnlyDropsInput`/`TestReadOnlyAllowsResize`

<output>
Create `.planning/phases/02-protocol/02-03-SUMMARY.md` when done
</output>
