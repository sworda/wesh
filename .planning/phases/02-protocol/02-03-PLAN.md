---
phase: 02-protocol
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - internal/server/server.go
  - internal/server/export_test.go
  - internal/server/limits_test.go
autonomous: true
requirements:
  - RES-01

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "C→S 三层上限全部生效（D-09/D-10 常量不开 flag）：单帧/累积字节 16KiB 由 SetReadLimit 结构覆盖（库 per-message 跨分片累积限，超限自动 1009）；每消息分片数 32 由应用层 readCounted 计数（库公开 API 不暴露分片边界，c.Read=io.ReadAll 吞边界不可用——本 phase 唯一非常规实现点）"
    - "超限消息（17KiB 单帧）→ 连接以 1009 关闭（库自动，read.go:526-530 VERIFIED；TestMessageTooBig 证明）"
    - "百万个 1 字节 continuation 帧 → 累积至 16385 字节时库精确触发 1009，服务端不崩溃、内存平坦（库流式重组 O(1)/帧，Bandit GHSA-vg8x-66vg-5pxh 的 O(n²) 模式结构上不存在；TestFragmentByteFlood 断言 ≤16385 帧内收 1009 且随后新连接握手成功）"
    - "33 个非空分片（33B 总字节 < 16KiB 字节限）→ 应用层 1009 且 reason \"fragment_limit\"——层2 独立于层3 生效的最小反例（D-08 复用 1009 不发明新码；TestFragmentCountLimit 证明）"
    - "空 binary 消息不崩溃不关连接，后续正常通信（TestEmptyFrame 证明）"
    - "空分片慢滴洪水被每消息完成时限兜底：首帧到达后武装 msgCompleteTimeout（10s 常量），空分片在库内部循环被吞应用不可见，时限到点取消 ctx 经库内 AfterFunc 整连接关闭（Open Questions Q1 裁决：实现；Q3 裁决：仅数据面武装，预认证由 5s helloTimeout 单点覆盖；TestFragmentTrickle 证明）"
    - "SetReadLimit 两档切换完整：Accept 后 4KiB（plan 02-02 已装）→ Welcome 发出后升档 16KiB（D-11；升档序列顺序敏感：Resize→Welcome→16KiB→停计时器→release）"
    - "超限不吞错误（D-12 用户硬要求）：服务端 stderr 打单行事件（对端 IP、码值、reason）；库自动 1009 的 reason 固定不可定制（Pitfall 4），message_too_big 机器串落点在 stderr 事件（errors.Is(err, websocket.ErrMessageTooBig) 归一化）；前端按码分派已在 plan 02-01 落地"
    - "plan 02-01/02-02 全部既有测试在 readCounted 替换后保持绿（读原语替换不改变任何对外行为契约）"
  artifacts:
    - path: "internal/server/server.go"
      provides: "readCounted 分片计数读循环 + 16KiB 升档 + 每消息完成时限 + stderr 单行事件缝"
      contains: "readCounted / errFragmentLimit / readLimitPostAuth / maxFragments / msgCompleteTimeout / stderrw / eventf"
    - path: "internal/server/limits_test.go"
      provides: "RES-01 攻击面五测（裸帧洪水/计数/空帧/慢滴 + 库客户端超限）"
      contains: "TestMessageTooBig / TestFragmentByteFlood / TestFragmentCountLimit / TestEmptyFrame / TestFragmentTrickle"
  key_links:
    - from: "internal/server/server.go"
      to: "github.com/coder/websocket（库读路径）"
      via: "c.Reader + ≥limit 缓冲手动循环：每次 n>0 的 Read 恰对应一个非空分片（read.go:457-498 VERIFIED）；buf ≥ 当前 SetReadLimit 否则伪分片误杀（Pitfall 1）"
      pattern: "readCounted\\(|c\\.Reader\\(|SetReadLimit\\("
    - from: "internal/server/limits_test.go"
      to: "internal/server/rawws_test.go"
      via: "洪水/计数/空帧/慢滴用例全部经裸帧 helper 构造（库客户端发不出分片序列与空帧）"
      pattern: "dialRawWS\\(|writeRawFrame\\(|readRawFrame\\("
  prohibitions:
    - statement: "资源超限导致的连接关闭不得静默或伪装成普通网络故障——三腿提示缺一不可：前端按 1009 人话文案（plan 02-01 已落地）、服务端 stderr 单行事件（对端/码值/reason）、close reason 机器串（fragment_limit；库自动 1009 的 message_too_big 归一化落 stderr）（用户原话：超限不得吞错误，D-12）"
      status: unresolved
---

<objective>
落地 RES-01 三层资源上限：以 readCounted（c.Reader 手动循环）整体替换 c.Read 读原语，补齐库不暴露的分片数上限（32）与每消息完成时限（10s），Welcome 后升档 SetReadLimit(16KiB)，超限三腿提示的服务端 stderr 腿到位。

Purpose: ROADMAP 准则 1 的主承载 plan——空帧/百万小帧/超限帧打过来时服务端以 1009 合规关闭、不崩溃、内存平坦；分片数上限是 Bandit CVE 教训的正面对应（只限字节不限帧数无效）。
Output: readCounted + 两层库限 + 一层应用计数 + 完成时限 + stderr 事件 + 攻击面五测，`go test ./...` 全绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-protocol/02-CONTEXT.md
@.planning/phases/02-protocol/02-RESEARCH.md
@.planning/phases/02-protocol/02-PATTERNS.md
@.planning/phases/02-protocol/02-VALIDATION.md
@.planning/phases/02-protocol/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: readCounted 替换读原语 + 16KiB 升档 + 完成时限 + stderr 事件缝 + 三层核心三测</name>
  <files>internal/server/server.go, internal/server/limits_test.go</files>
  <read_first>
    - internal/server/server.go（被改文件现状——plan 02-02 产物：守卫链 + handshake + 数据面 c.Read 循环 + Options）
    - .planning/phases/02-protocol/02-RESEARCH.md（Pattern 3 分片计数读循环行 271-317 逐行照抄基底；三个已核实边界行为行 313-317；Pitfall 1 缓冲纪律行 433-438；Pitfall 2 ctx 纪律行 440-445；Pitfall 4 reason 不可定制行 453-457；Open Questions Q1/Q3 行 606-619）
    - .planning/phases/02-protocol/02-PATTERNS.md（§internal/server/server.go 分片计数段行 202-228；§Shared Patterns ctx 纪律/错误处理段；§No Analog Found stderr 格式行）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-08/D-09/D-10/D-11/D-12）
    - internal/server/rawws_test.go（dialRawWS/writeRawFrame/readRawFrame 签名——plan 02-02 产物）
    - internal/server/e2e_test.go（startTestServerOpts 与 dialAndHandshake 签名——plan 02-01 产物）
  </read_first>
  <action>
    1. `internal/server/server.go` 新增包级件（常量注释标定来源与依据，D-10 不开 CLI flag）：`readLimitPostAuth = 16 * 1024`（D-09/D-11；注释：合法流量键盘 INPUT 字节级、粘贴几 KB，浏览器 WS API 不分片；代理分片余量 16KiB≈11 帧<32，RESEARCH A1）；`maxFragments = 32`（D-09；注释：对空帧攻击的关键防线——0 字节 continuation 不累积字节，Bandit CVE 教训只限字节不限帧数无效）；包级私有变量 `msgCompleteTimeout = 10 * time.Second`（RESEARCH A5/Q1 裁决实现）；`var errFragmentLimit = errors.New("fragment limit exceeded")`。`internal/server/export_test.go` 增量加钩子（package server 内测文件，plan 02-02 已建）：`SetMsgCompleteTimeout(d time.Duration) (restore func())` 与 `SetStderrWriter(w io.Writer) (restore func())`——外部测试包 server_test 经钩子注入（同 02-02 SetMaxHalfOpenPerIP 先例，不违反 D-10）。
    2. `readCounted`（照 RESEARCH Pattern 3 行 277-314 逐段落定——本 phase 唯一非常规实现点）：`func readCounted(ctx context.Context, c *websocket.Conn, buf []byte, maxFragments int) (websocket.MessageType, []byte, error)`——内部 `rctx, rcancel := context.WithCancel(ctx); defer rcancel()`；`typ, r, err := c.Reader(rctx)`；**Reader 返回（首帧头已读）后才武装** `time.AfterFunc(msgCompleteTimeout, rcancel)` 并 defer Stop（空分片慢滴的功能等价防线——空分片在 mr.read 内部循环被吞应用不可见；ctx 取消经库内 AfterFunc 整连接关闭正是预期，Q3：仅数据面与握手读共用此函数，预认证慢滴由 5s helloTimeout 单点覆盖）；循环 `r.Read(buf[len(msg):cap(msg)])` 追加读进同一缓冲零额外分配：`n>0` 时 fragments++ 且 `fragments > maxFragments` 返回 errFragmentLimit；`errors.Is(rerr, io.EOF)` 返回完整消息；其他 rerr 原样返回（含 ErrMessageTooBig——库已自动发 1009）；`len(msg)==cap(msg)` 防御分支返回 websocket.ErrMessageTooBig（理论不可达）。**buf 容量必须 ≥ 当前 SetReadLimit**（否则单帧被缓冲截断成伪分片误杀合法消息，Pitfall 1）。
    3. 读原语整体替换（两处调用点）：握手内读首消息由 `c.Read(ctx)` 改为 `readCounted(ctx, c, helloBuf, maxFragments)`——helloBuf 为握手局部 `make([]byte, readLimitPreAuth)`（4KiB，预认证档语义保持）；数据面循环的 `c.Read(ctx)` 改为 `readCounted(ctx, c, dataBuf, maxFragments)`——dataBuf 在**升档后**分配 `make([]byte, readLimitPostAuth)`（16KiB；预认证期仅 4KiB helloBuf，SEC-08 分配语义保持）。errFragmentLimit 返回路径：握手期与数据面统一 `c.Close(websocket.StatusMessageTooBig, "fragment_limit")`（1009 复用，D-08 纪律：应用层超限检测不发明新码）后按既有读循环终结路径收口（零新终结分支）。
    4. 升档序列插入 16KiB（顺序敏感，RESEARCH 行 269）：现状序列 Resize → 写 Welcome → conn.Store → 停 hello 计时器 → release 半开名额；在 conn.Store 后、停计时器前插入 `c.SetReadLimit(readLimitPostAuth)`（SetReadLimit 内部 atomic.Int64.Store，读循环进行中调档安全，read.go:104 VERIFIED）。
    5. stderr 单行事件缝（D-12②，格式属 discretion 但要素钉死）：包级私有变量 `stderrw io.Writer = os.Stderr`（测试经 export_test.go 的 SetStderrWriter 注入）；helper `eventf(format string, args ...any)`——fmt.Fprintf(stderrw, "wesh: "+format+"\n", ...) 单行。读循环/握手终结分类打事件：主动关闭路径各一行，格式 `wesh: <ip> close <code> <reason>`——1002 unexpected_frame、1008 hello_timeout、1008 version_mismatch、1009 fragment_limit；库自动 1009 路径（读返回错误处）以 `errors.Is(err, websocket.ErrMessageTooBig)` 归一化为 `message_too_big` 机器串打事件（Pitfall 4：库 reason 固定 "read limited at N bytes" 不可定制，机器串落点在 stderr 而非线上 reason——禁止包装库）；对端一般关闭/网络断开不打事件（非攻击面、非超限，保持服务端近零输出纪律）。事件须含对端 ip（clientIP 结果沿 Attach 局部变量传递）。
    6. 新建 `internal/server/limits_test.go`（package server_test，复用 rawws/e2e helper）三测：
       - `TestMessageTooBig`（VALIDATION 行 43）：startTestServerOpts 默认装配；库客户端 dialAndHandshake 后发 17KiB 单帧 INPUT（17*1024 > 16384）；读循环收 CloseError 断言 Code==1009；同时经 `server.SetStderrWriter`（bytes.Buffer 换入，t.Cleanup 调恢复闭包）断言事件行含 "message_too_big" 与对端 IP 串（D-12② 机器串落点）。
       - `TestFragmentCountLimit`（VALIDATION 行 651——层2 独立于层3 的最小反例）：裸帧 dialRawWS 完成握手后，发 binary opener（fin=false, op=0x2, 1B 载荷）+ 32 个 continuation（fin=false, op=0x0, 各 1B）——第 33 个非空分片触发应用层超限；readRawFrame 收关闭帧断言码值 1009 且 reason=="fragment_limit"（33B 总字节远低 16KiB，证明非字节限触发）。
       - `TestEmptyFrame`（VALIDATION 行 652）：裸帧握手后发空 binary 消息（fin=true, op=0x2, 零载荷）；断言连接存活（服务端 continue 跳过）——随后正常发一帧 INPUT（rw 装配 /bin/cat 回显）或再发一帧合法消息读回正常响应，证明连接未被关闭且状态机健康。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go vet ./... && go test ./internal/server -count=1 -v -run 'TestMessageTooBig|TestFragmentCountLimit|TestEmptyFrame' && go test ./... -count=1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - TestMessageTooBig / TestFragmentCountLimit / TestEmptyFrame 三测全绿（VALIDATION 行 649/651/652 落位）；fragment_limit 用例断言 reason 恰为 "fragment_limit"（D-08 复用 1009）
    - `go test ./... -count=1` 退出 0——02-01/02-02 全部测试在 readCounted 替换后保持绿（含握手/守卫链/ro/保活前置用例）
    - server.go 中不再存在对 `c.Read(` 的调用（握手与数据面两处均已替换为 readCounted）；`grep -c 'c\.Read(' internal/server/server.go` 输出为 0
    - readCounted 内完成时限在 `c.Reader` 返回之后才武装（time.AfterFunc 位于 Reader 调用之后）；buf 容量断言：握手用 readLimitPreAuth(4096)、数据面用 readLimitPostAuth(16384) 两块独立缓冲，数据面缓冲在升档后分配
    - 升档序列顺序为 sess.Resize → 写 Welcome → conn.Store → SetReadLimit(16384) → 停 hello 计时器 → release 半开名额
    - stderrw/eventf 缝存在；TestMessageTooBig 断言 stderr 事件含 "message_too_big" 与对端 IP（D-12②）；fragment_limit 路径事件含 "fragment_limit"
    - `go test -race -count=1 ./internal/server` 退出 0（读原语替换 + 事件写缝竞态面）
  </acceptance_criteria>
  <done>三层上限核心落地：库字节限（4KiB→16KiB 两档）+ 应用分片计数 32 + 每消息完成时限 10s，c.Read 原语退役，超限 stderr 事件可见，三测 + 全量回归绿。</done>
</task>

<task type="auto">
  <name>Task 2: 洪水与慢滴压测——百万小帧 1009 存活证明 + 空分片慢滴完成时限</name>
  <files>internal/server/limits_test.go</files>
  <read_first>
    - internal/server/limits_test.go（被改文件现状——本 plan Task 1 产物：三测与 stderrw 注入用法）
    - internal/server/rawws_test.go（dialRawWS/writeRawFrame/readRawFrame 签名）
    - internal/server/server.go（readCounted 与 msgCompleteTimeout 现状）
    - .planning/phases/02-protocol/02-RESEARCH.md（验证用例矩阵行 563-573；Open Questions Q1 行 606-609；Pitfall 2 行 440-445）
    - .planning/phases/02-protocol/02-VALIDATION.md（行 650 TestFragmentByteFlood、行 653 TestFragmentTrickle 验收描述）
  </read_first>
  <action>
    在 `internal/server/limits_test.go` 追加两测：
    1. `TestFragmentByteFlood`（VALIDATION 行 650——ROADMAP 准则 1 核心用例）：裸帧 dialRawWS 完成握手后发 binary opener（fin=false, op=0x2, 1B），随后循环发 continuation（fin=false, op=0x0, 各 1B）直至连接被关；逐帧计数，断言在 ≤16385 个非空分片内收到关闭帧（readRawFrame，码值 1009——库在累积 16385 字节时精确触发）；随后**新连接经 dialRawWS 完成完整 Hello 握手成功**（存活/无崩溃代理断言——服务端在洪水后健康）。内存平坦为结构性结论（库流式重组 O(1)/帧），采样断言脆弱故不门禁（VALIDATION 行 658 纪律）。
    2. `TestFragmentTrickle`（VALIDATION 行 47，Open Questions Q1 裁决实现的证明）：经 `server.SetMsgCompleteTimeout(500 * time.Millisecond)` 注入（t.Cleanup 调恢复闭包，生产值恒 10s）；裸帧完成握手后发 binary opener（fin=false, op=0x2, 1B），随后每 100ms 滴一个空 continuation（fin=false, op=0x0, 零载荷——空分片不推进分片计数也不累积字节，唯一防线是完成时限）；断言自首帧到达起 2s 内连接被关闭（readRawFrame 读到关闭帧或 TCP EOF——ctx 取消经库内 AfterFunc 整连接关闭，关闭码不断言具体值：库路径下可能无合规关闭帧直达，行为锚点是"连接在时限内终结"）。
    3. 两测结束经 t.Cleanup 关闭全部裸 conn；洪水测试注意写端循环须容忍写错误（对端关闭后继续写会 broken pipe——出错即停写转读关闭帧）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test ./internal/server -count=1 -v -run 'TestFragmentByteFlood|TestFragmentTrickle' && go test -race -count=1 ./... && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - TestFragmentByteFlood 绿：≤16385 个 1 字节分片内收 1009，且随后新连接完整握手成功（VALIDATION 行 650 落位——不崩溃、服务存活）
    - TestFragmentTrickle 绿：空分片慢滴在注入的 500ms 完成时限内连接终结（VALIDATION 行 653 落位；msgCompleteTimeout 生产值恒 10s——私有变量注入不违反 D-10）
    - `go test -race -count=1 ./...` 全仓退出 0
    - `go test ./... -count=1` 退出 0（无回归）
  </acceptance_criteria>
  <done>RES-01 五测齐：字节洪水精确 1009 且服务端存活、分片计数独立硬顶、空帧免疫、慢滴时限兜底——三层上限与 ROADMAP 准则 1 全部自动化锁定。</done>
</task>

</tasks>

## Flagged Assumptions（spec-less probe fallback §A/§C——显式呈现，绝不静默丢弃）

edge probe 对 RES-01 返回 `unclassified`，按规则保持 `unresolved` 并在此登记：

| Requirement | probe 结果 | 状态 | 处置 |
|-------------|-----------|------|------|
| RES-01 | unclassified — review manually | unresolved | 以 RESEARCH §Validation Architecture 行 649-653 五条验收准则为锚点（TestMessageTooBig / TestFragmentByteFlood / TestFragmentCountLimit / TestEmptyFrame / TestFragmentTrickle，本 plan 全部落位） |

§B prohibition recall：RES-01 的存活 prohibition 一条（transparency：超限断连不得静默——用户原话"不得吞错误"），已 authoring 进本 plan `must_haves.prohibitions`（descriptor-less，无 check_* 字段——下游 dispose 为 flagged-unverified，never auto-dismiss）。prohibition recall surfaced 1 条 = authored 1 条 ✓。

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 已升级客户端 ↔ WS 读路径 | 分片洪水/空帧/慢滴/超限帧从此进入——三层上限的执行面 |
| 读原语替换缝 | c.Read → readCounted 是本 phase 唯一破坏性改造点 |
| stderr 事件写端 | 运维可见性边界（Phase 8 升级为 slog 结构化日志） |
| npm/go 依赖安装 | 本 plan 零新增依赖 |

## STRIDE Threat Register（ASVS L1，block on high）

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | DoS | 预认证/数据面分片洪水（Bandit O(n²) 模式，GHSA-vg8x-66vg-5pxh） | critical | mitigate | 库流式重组结构上无 O(n²)（read.go:457-498 VERIFIED）+ 层2 分片计数 32 + 层3 累积字节 16KiB + 完成时限兜底空分片；TestFragmentByteFlood/TestFragmentCountLimit/TestFragmentTrickle 三路径锁定 |
| T-02-03-02 | DoS | 预认证内存放大（ttyd protocol.c:288-296 无限累积模式） | high | mitigate | 4KiB 预认证档（plan 02-02）+ 16KiB 数据面档（D-11）+ readCounted 追加读同一缓冲零额外分配；TestMessageTooBig 锁定 |
| T-02-03-03 | DoS | 空分片慢滴（0 字节 continuation 不累积字节、不计数） | medium | mitigate | 每消息完成时限 10s（首帧到达后武装，Q1 裁决实现）；TestFragmentTrickle 注入 500ms 证明 |
| T-02-03-04 | Tampering | 伪分片误杀合法大消息（buf < ReadLimit 时单帧被缓冲截断成多次 Read） | medium | mitigate | Pitfall 1 纪律：握手 buf=4KiB、数据面 buf=16KiB 均 ≥ 各自档 SetReadLimit；acceptance criteria 断言两块缓冲分配点 |
| T-02-03-05 | DoS | 读路径 ctx 误用（deadline ctx = 整连接关闭，误杀空闲合法连接） | high | mitigate | Pitfall 2 纪律：读路径只传无 deadline 可取消 ctx；完成时限用"Reader 返回后 AfterFunc(rcancel)"形态；ctx 纪律注释 |
| T-02-03-06 | Information Disclosure | 超限关闭的用户感知缺失（静默断连） | medium | mitigate | D-12 三腿：前端 1009 文案（02-01）+ stderr 单行事件（message_too_big/fragment_limit 机器串）+ reason 机器串；TestMessageTooBig 断言 stderr 事件 |
| T-02-03-SC | Tampering | 依赖安装 | high | mitigate | 本 plan 零新增第三方依赖；攻击面测试 helper 为 stdlib net 裸写（RESEARCH §Package Legitimacy Audit：无适用对象） |
</threat_model>

<verification>
- `go vet ./... && go test ./... -count=1` 全绿
- `go test ./internal/server -run 'TestMessageTooBig|TestFragmentCountLimit|TestEmptyFrame|TestFragmentByteFlood|TestFragmentTrickle' -count=1` 绿（RES-01 五测）
- `go test -race -count=1 ./...` 全仓绿
</verification>

<success_criteria>
- ROADMAP 准则 1 成立：空帧、百万个 1 字节 continuation 帧、超限帧打过来时，服务端以 1009 合规关闭连接，不崩溃、内存平坦（三层上限：单帧/分片数/累积字节）
- RES-01 三层缺一不可自动化证明：字节限（库）、分片数（应用计数）、完成时限（慢滴兜底）各有独立测试
- 本 plan 的 must_haves.truths 全部可由 acceptance_criteria 对应断言证明
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `internal/server/server.go`（重构读路径）：`readCounted(ctx context.Context, c *websocket.Conn, buf []byte, maxFragments int) (websocket.MessageType, []byte, error)`；`var errFragmentLimit = errors.New("fragment limit exceeded")`；常量 `readLimitPostAuth = 16 * 1024`、`maxFragments = 32`；包级私有变量 `msgCompleteTimeout = 10 * time.Second`；`var stderrw io.Writer = os.Stderr` + `eventf(format string, args ...any)` stderr 单行事件缝；SetReadLimit 两档切换完成（4KiB→16KiB 升档点）；全部 c.Read 调用点替换为 readCounted
- `internal/server/export_test.go`（增量）：`SetMsgCompleteTimeout(d time.Duration) (restore func())`、`SetStderrWriter(w io.Writer) (restore func())`
- `internal/server/limits_test.go`（新建）：`TestMessageTooBig`、`TestFragmentCountLimit`、`TestEmptyFrame`、`TestFragmentByteFlood`、`TestFragmentTrickle`

<output>
Create `.planning/phases/02-protocol/02-03-SUMMARY.md` when done.
</output>
