---
phase: 13-resource-defense
plan: 06
type: execute
wave: 4
depends_on: [13-04, 13-05]
files_modified:
  - internal/pty/spawn.go
  - internal/pty/spawn_test.go
  - internal/server/server.go
  - internal/server/perclient.go
  - cmd/wesh/main.go
  - internal/server/options_test.go
  - internal/server/perclient_test.go
autonomous: true
requirements: [SEC-09]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "per-client 模式且 --auth-header 开启时，反代透传的用户名经 SEC-07 sanitize 后作为 WESH_REMOTE_USER 出现在该客户端子进程环境中（Web shell 内 env 可见——13-07 phase13.mjs S6 进程级回读断言）"
    - "键名白名单固定 = 代码常量 WESH_REMOTE_USER（非配置面）；值 = cl.remoteUser（Attach 入口提取一次的写一次字段，perclient.go:227——提取点 proxy.go:136-141 sanitizeRemoteUser 已清洗：C0/C1/DEL 剥离 + 128 rune 截断；下游零二次清洗，单一写口纪律）"
    - "注入仅 per-client 分支：shared 模式 whitelistEnv 调用点零漂移（RemoteUser 零值空串 → 不出键；D-15 收窄语义不变）；替换式注入纪律保持（spawn.go:111——严禁 os.Environ() 全量追加）"
    - "传递链：server.go:322 SpawnFunc 类型扩展第三参 remoteUser string → perclient.go:174 调用点传 remoteUser（upgradePerClient 既有形参在手，零新管道）→ main.go spawnFunc 闭包签名扩展（闭包内复制 startOpts 后局部赋值 RemoteUser——共享 startOpts 直接改即多客户端串台）→ pty.StartWithSize → StartOptions.RemoteUser → whitelistEnv 形参扩展（空串不出键）"
    - "pty 包单侧定义「键名白名单固定」语义（whitelistEnv 形参扩展 = 倾向选项 1——SEC-06 防线所在函数内聚；StartOptions.Env 通道把键名合法性责任推给调用方的误用面不引入）"
  artifacts:
    - path: "internal/pty/spawn.go"
      provides: "StartOptions.RemoteUser 字段 + whitelistEnv remoteUser 形参扩展（空串不出键）"
      contains: "WESH_REMOTE_USER"
    - path: "internal/server/server.go"
      provides: "SpawnFunc 类型签名第三参 remoteUser string"
      contains: "remoteUser string"
    - path: "cmd/wesh/main.go"
      provides: "spawnFunc 闭包扩展（startOpts 局部复制 + RemoteUser 赋值）"
      contains: "RemoteUser"
    - path: "internal/pty/spawn_test.go"
      provides: "TestEnvWhitelist 扩展（注入可见 / 空串不出键 / 阳性对照保持）"
      contains: "WESH_REMOTE_USER"
  key_links:
    - from: "internal/server/perclient.go"
      to: "internal/pty/spawn.go"
      via: "upgradePerClient remoteUser 形参（Attach 提取 cl.remoteUser 同值）→ spawnFunc 三参调用 → StartWithSize → whitelistEnv 出键"
      pattern: "remoteUser"
    - from: "cmd/wesh/main.go"
      to: "internal/pty/spawn.go"
      via: "spawnFunc 闭包捕获 argv/startOpts + 形参 remoteUser → StartOptions.RemoteUser 局部赋值"
      pattern: "StartWithSize"
  prohibitions:
    - requirement_id: SEC-09
      category: safety
      statement: "WESH_REMOTE_USER 值不得未经 sanitizeRemoteUser 清洗直接进子进程环境——注入值面即子进程 env 污染面（控制字符/超长值经 shell 展开或日志落盘造成二次面）；清洗只在提取点完成，下游零二次清洗也零绕过"
    - requirement_id: SEC-09
      category: values
      statement: "shared 模式不得注入 WESH_REMOTE_USER——D-15 收窄语义是 v1.0 既定承诺（共享进程模型下 env 是一次性快照），模式间语义不得互相渗透"
---

<objective>
SEC-09 落地（防线装配序第四片之 env 面）：per-client 模式下 --auth-header 透传用户名注入子进程环境变量 WESH_REMOTE_USER——D-15 收窄理由结构性消失的兑现（per-client 下 attach 后 spawn，HTTP 上下文在手）。

Purpose: 反代身份注入使 Web shell 内脚本/应用可按 WESH_REMOTE_USER 归因当前用户（herdr 类应用的用户感知面）；shared 保持 D-15 收窄语义（仅审计归因）不变。
Output: whitelistEnv 白名单扩展 + SpawnFunc 签名扩散链 + 双模式测试断言。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-resource-defense/13-CONTEXT.md
@.planning/phases/13-resource-defense/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pty 包——StartOptions.RemoteUser + whitelistEnv 形参扩展（空串不出键）+ TestEnvWhitelist 扩展（SEC-09 防线本体）</name>
  <reversibility rating="one-way">WESH_REMOTE_USER 键名一经发布即被用户脚本/herdr 类应用消费，改键名 = 破坏已发布 env 契约——键名由 REQUIREMENTS.md SEC-09 逐字锁定且 CONTEXT D-05 裁决记录注入语义，门已走过（项目 one-way 确认门惯例针对 CONTEXT 级裁决；需求文本级锁定不再加门），按既定评级记录。</reversibility>
  <files>internal/pty/spawn.go, internal/pty/spawn_test.go</files>
  <read_first>
    - internal/pty/spawn.go 全文 164 行（:76-105 StartWithSize——:81 whitelistEnv 唯一调用点 / :108-164 whitelistEnv 全文——替换式注入纪律 :111 与继承循环内 uid 改写挂点）
    - internal/pty/spawn_test.go:84-124（TestEnvWhitelist 双层形态：(a) 单元层返回值断言（泄露键零出现 + 固定键在场）+ (b) e2e 层 /usr/bin/env 子进程真实输出 + 阳性对照防空串假绿）
    - internal/server/proxy.go:55-67/:136-141（sanitizeRemoteUser 清洗产物形态——值源论证锚点，本任务零改动）
    - .planning/phases/13-resource-defense/13-PATTERNS.md §7（白名单扩展模式逐行锚点）
  </read_first>
  <behavior>
    - StartOptions 加 RemoteUser string 字段（注释载 SEC-09：仅 per-client 分支赋值；值须为提取点已清洗产物，pty 包零二次清洗）
    - whitelistEnv 加 remoteUser string 形参：非空时 append "WESH_REMOTE_USER=" + remoteUser 到白名单产物尾部；空串不出键
    - StartWithSize 内 :81 调用点改为传 opts.RemoteUser——shared 路径 startOpts 零值 "" 自然不出键（零漂移结构性保证，非分支判断）
    - 测试扩展：单元层 remoteUser="alice" → 返回值含 "WESH_REMOTE_USER=alice" 精确行 / remoteUser="" → 键缺席 / e2e 层 /usr/bin/env 输出双形态；泄露键零出现与阳性对照既有断言保持
  </behavior>
  <action>
    1. spawn.go：StartOptions 字段 + whitelistEnv 形参扩展 + :81 调用点传参；注释载键名白名单固定语义由本函数单侧定义（SEC-06 防线内聚论证）与替换式注入纪律逐字保持。
    2. spawn_test.go：TestEnvWhitelist 扩展（t.Run 分支三枚：注入可见/空串不出键/e2e 双形态——既有断言逐字不动）。
    3. go build ./... && go vet ./... && time go test ./internal/pty/ -count=1 -run 'TestEnvWhitelist' -v 全绿 → time go test ./... -count=1 全量（Start/StartWithSize 全调用方编译面——签名扩散在 Task 2 完成前 server 包 spawnFunc 类型与调用点同步在同一 commit 序列内落地，中间态编译红属正常，两任务提交序保证绿）。
  </action>
  <verify>
    <automated>time go test ./internal/pty/ -count=1 -run 'TestEnvWhitelist' -v && time go test ./internal/pty/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - whitelistEnv 含 remoteUser 形参且空串不出键（单元断言双向在场）
    - e2e 层 /usr/bin/env 真实输出含/不含两形态断言（阳性对照保持——防空串假绿）
    - spawn.go:111 替换式注入纪律注释逐字未动；os.Environ() 追加形态零命中（grep 'os.Environ' internal/pty/ 零命中保持）
    - WESH_REMOTE_USER 键名为代码常量字面量（grep 精确命中 spawn.go；无配置面——cmd/wesh 零对应 flag/键）
  </acceptance_criteria>
  <done>pty 包注入面落地：白名单扩展空串不出键，双模式语义由零值结构性保证。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 传递链——SpawnFunc 签名扩散（server/main/测试夹具机械加参）+ remoteUser 到达断言（SEC-09 收口）</name>
  <files>internal/server/server.go, internal/server/perclient.go, cmd/wesh/main.go, internal/server/options_test.go, internal/server/perclient_test.go</files>
  <read_first>
    - internal/server/server.go:314-322（SpawnFunc 字段与类型定义 + T-10-01c 注记保持）与 :362-366（ValidateOptions 互斥校验——SpawnFunc nil 判定不受签名扩展影响）与 :487（New 装配行）
    - internal/server/perclient.go:143（upgradePerClient 签名——remoteUser 既有形参在手）与 :174（spawnFunc 调用点加参）与 :227（cl.remoteUser 写一次字段注释）
    - cmd/wesh/main.go:1326-1333（spawnFunc 闭包现状——startOpts 共享变量声明 :1326 与闭包捕获面）
    - internal/server/options_test.go:18-27（空闭包占位加参点）
    - internal/server/perclient_test.go:40-135 与 :57-70（harness SpawnFunc 包装层——spawnFn 签名扩散与 remoteUser 透传面）与 :106-107（默认 spawnFn 生产镜像注释）与 :367/:588-610（其余 SpawnFunc 注入点枚举）
    - internal/server/proxy_e2e_test.go:116-165（AuthHeader 配置 + X-Remote-User 头注入的 e2e 形态先例）
  </read_first>
  <behavior>
    - SpawnFunc 类型：func(cols, rows int, remoteUser string) (*pty.Session, error)——server.go:322 类型 + :183/:314-320 注释同步（T-10-01c 注记语义保持）
    - perclient.go:174 调用点：s.spawnFunc(h.Cols, h.Rows, remoteUser)——remoteUser 为 upgradePerClient 既有形参（Attach 提取链 :898 remoteUser := s.proxy.remoteUser(r) 产物）
    - main.go 闭包：签名扩展 + 闭包内 startOpts 局部复制后赋 RemoteUser（注释载防串台论证：startOpts 为 run() 共享变量，每客户端 remoteUser 不同——直接改共享字段即多客户端环境串台）
    - 测试夹具机械加参：options_test.go:18 空闭包 / perclient_test.go harness 包装层（spawnFn 签名扩散为 (cols, rows int, remoteUser string)——追踪包装透传第三参，断言面可捕获）
    - 新测 TestPerClientRemoteUserEnv：AuthHeader 配置服务端 + attach 携 X-Remote-User 头 → spawnFn 捕获 remoteUser == 头值 sanitize 产物（含清洗形态：注入控制字符头值 → 捕获值已剥离）；无认证/无头形态 → 捕获 ""（空串不出键链路断言由 Task 1 承载）
  </behavior>
  <action>
    1. server.go：SpawnFunc 类型签名加第三参 + 注释同步。
    2. perclient.go：:174 调用点加参（一行）。
    3. main.go：闭包签名扩展 + startOpts 局部复制赋值（注释防串台论证）。
    4. options_test.go:18 与 perclient_test.go 全部 SpawnFunc/spawnFn 注入点机械加参（harness 包装层透传 remoteUser 供断言捕获；既有断言行零改动）。
    5. perclient_test.go 新测 TestPerClientRemoteUserEnv 落位（AuthHeader 配置 + 头注入 + sanitize 形态断言 + 空值形态）。
    6. TDD 提交收口：Task 1 feat 提交 + 本任务 feat/test 提交（签名扩散与断言同提交——中间态编译红不独立成提交）。
    7. go build ./... && go vet ./... && time go test -race ./... -count=1 全绿（全仓签名扩散编译面 + 既有组零回归）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && time go test -race ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - SpawnFunc 类型/调用点/main.go 闭包/全部测试注入点签名一致（go build ./... 与 go vet ./... 零输出即全链证明）
    - TestPerClientRemoteUserEnv 绿：头值 → sanitize 产物到达 spawnFn 捕获；控制字符注入头值 → 捕获值已剥离；无头 → 空串
    - shared 模式 SpawnFunc 恒 nil 互斥契约保持（options_test 既有断言逐字未动）
    - perclient_test.go/options_test.go 既有断言行零改动（git diff 白名单 = 签名加参机械行 + 新增测试函数）
  </acceptance_criteria>
  <done>SEC-09 全链落地：反代用户名经 sanitize 后注入 per-client 子进程环境，shared 零漂移；进程级 env 回读断言由 13-07 phase13.mjs S6 承接。</done>
</task>

</tasks>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **SEC-09 (unclassified, unresolved)**：探针 unclassified 行永不 auto-resolve（#1110）——SEC-09 注入面的未分类边界（空用户名/缺头/多值头/头值恰好等于 sanitize 截断边界的形态）由 sanitizeRemoteUser 既有清洗语义（C0/C1/DEL 剥离 + 128 rune 截断）与 Task 2 空值/清洗断言覆盖主面；未在探针分类中显式覆盖的边界保持 flagged-unverified，执行中若发现该面之外的未覆盖形态须显式登记而非静默放行，由 13-08 收口闸承载终验。
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 反代→server（auth-header 头值） | 透传用户名是不可信输入——经 SEC-07 sanitize 清洗后才允许跨进子进程 env 边界 |
| server→子进程（env 注入） | env 是子进程的能力面——键名/值的合法性由 pty 包白名单单侧收口 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-19 | Elevation of Privilege | WESH_REMOTE_USER 值注入（控制字符/超长值/伪造头值进子进程 env） | high | mitigate | 键名白名单固定代码常量 + 值 = sanitizeRemoteUser 清洗产物（proxy.go:136-141 既有，下游零二次清洗零绕过）+ Task 2 清洗形态断言；伪造头值面由 --auth-header 信任闸既有语义承接（07-03，部署面文档既定） |
| T-13-20 | Tampering | shared 模式误注入（D-15 收窄破坏） | medium | mitigate | 空串不出键结构性保证（whitelistEnv 形参零值形态，Task 1 双向断言）+ ValidateOptions shared×SpawnFunc≠nil 拒绝既有（SpawnFunc 只在 per-client 装配） |
| T-13-21 | Tampering | 多客户端 env 串台（共享 startOpts 被闭包改写） | medium | mitigate | 闭包内局部复制后赋值（Task 2 步 3 注释论证 + remoteUser 逐端捕获断言） |
| T-13-SC | Tampering | 依赖面 | high | mitigate | 零新依赖红线；本 plan 无装包任务，legitimacy 门不触发 |
</threat_model>

<verification>
- go build ./... && go vet ./... 零输出（签名扩散全链一致）
- time go test -race ./... -count=1 全绿（pty 白名单扩展 + server 传递链断言 + 既有组零回归）
- 13-07 phase13.mjs S6 补进程级断言（Web shell 内 env 可见 + shared 对照无键）
</verification>

<success_criteria>
1. per-client + --auth-header：子进程 env 含 WESH_REMOTE_USER=sanitize 后用户名
2. shared 模式 / 无头场景：键不出（D-15 收窄不变）
3. 全仓签名一致，既有测试零改动
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `internal/pty/spawn.go`：`StartOptions.RemoteUser` 字段；`whitelistEnv` 第三形参 remoteUser（空串不出键，`WESH_REMOTE_USER=` 出键行）
- `internal/server/server.go`：`Options.SpawnFunc` 类型签名第三参 `remoteUser string`（:322）
- `internal/server/perclient.go`：upgradePerClient 内 spawnFunc 调用点加参（:174）
- `cmd/wesh/main.go`：spawnFunc 闭包签名扩展 + startOpts 局部复制 RemoteUser 赋值
- `internal/pty/spawn_test.go`：TestEnvWhitelist 扩展（注入可见/空串不出键/e2e 双形态 t.Run 分支）
- `internal/server/options_test.go` / `internal/server/perclient_test.go`：SpawnFunc 注入点机械加参 + `TestPerClientRemoteUserEnv` 新测

<output>
Create `.planning/phases/13-resource-defense/13-06-SUMMARY.md` when done
</output>
