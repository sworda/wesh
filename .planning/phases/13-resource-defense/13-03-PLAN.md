---
phase: 13-resource-defense
plan: 03
type: execute
wave: 2
depends_on: [13-02]
files_modified:
  - internal/server/server.go
  - internal/server/clients.go
  - internal/server/perclient.go
  - internal/server/export_test.go
  - internal/server/perclient_test.go
  - internal/server/emptyexit_test.go
  - internal/server/events_test.go
autonomous: true
requirements: [PC-09, OPS-12]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "per-client 模式 New 分支钉死唯一全局 goroutine pcSupervisor：hubCond 等待 (pcExitReq || exiting) && len(pcSessions) == 0，收口走 termOnce/terminate 逐字复用（server.go:1548-1552）——「exitf 恰好一次、唯一收口」零漂移（研究 §4.1 蓝本形态）"
    - "maybeExitWhenEmptyLocked 的 per-client 早退守卫（clients.go:957-960）替换为第二终结源触发端：四守卫沿用（!exitWhenEmpty/exiting/非空/门闩），grace==0 立即形态与 grace>0 宽限到期回调（身份比对+复查通过后）均置位 pcExitReq=true + hubCond.Broadcast()；宽限取消点零改动（Pitfall 1 三形态分支表逐行对照）"
    - "退出码 last-reaped-code 规则两时序逐位对齐 shared：子先死 exit 0 → watcher 收割 code=0 → exitf(0)；客户端先断 → detach→teardown HUP → watcher 收割 -1 → exitf(-1) → 进程级 255（accept-255 裁决的 per-client 映射，emptyexit_test.go 头注释 OQ1 门形态）"
    - "session_end 由 sessionWatcher emit：shared schema（server.go:1473-1483 母本 exit_code/duration_seconds/signal 键）+ client_id 关联键（cl.attachSeq 同键先例）；duration 数据源 = pc.startedAt（Phase 11 预留字段本 phase 激活）；KILL 兜底死亡经 signal 字段归因，不另起独立事件（per D-09）"
    - "WR-02 修复落地：teardownPCLocked 快半段对 pc.waitDone 做非阻塞 select——已关闭即视为 reaped 跳过信号（结构性栅栏，零新同步件；STATE.md 登记零成本严格修法）"
    - "sessionWatcher 收割链递减 len(pcSessions) 的 delete 点（teardown 慢半段 perclient.go:498-500）补 hubCond.Broadcast() 唤醒 pcSupervisor 重估"
    - "perclient.go:28-32 头注释「--once/--exit-when-empty 本阶段永不退出」窗口期登记随本 plan 落地移除（State of the Art 表行）"
  artifacts:
    - path: "internal/server/server.go"
      provides: "Server 三新字段（pcExitReq/pcLastExitCode/pcHasExitCode，hubMu 保护）+ pcSupervisor goroutine 钉死"
      contains: "pcSupervisor"
    - path: "internal/server/clients.go"
      provides: "maybeExitWhenEmptyLocked per-client 三形态分支（第二终结源触发端）"
      contains: "pcExitReq"
    - path: "internal/server/perclient.go"
      provides: "sessionWatcher session_end emit + pcLastExitCode 记录 + Broadcast 补行 + teardownPCLocked WR-02 waitDone 栅栏"
      contains: "session_end"
    - path: "internal/server/emptyexit_test.go"
      provides: "per-client 断言分叉（两时序逐位对齐）"
      contains: "PerClient"
  key_links:
    - from: "internal/server/clients.go"
      to: "internal/server/server.go"
      via: "maybeExitWhenEmptyLocked 置位 pcExitReq + hubCond.Broadcast → pcSupervisor 唤醒重估 → terminate(code)"
      pattern: "pcExitReq"
    - from: "internal/server/perclient.go"
      to: "internal/server/server.go"
      via: "sessionWatcher hubMu 内 pcLastExitCode/pcHasExitCode 置位 + 慢半段 delete 后 Broadcast → pcSupervisor 读 last-reaped-code"
      pattern: "pcLastExitCode"
  prohibitions:
    - requirement_id: PC-09
      category: safety
      statement: "注册表空迁移必须存在显式第二终结源——「注册表已空且无子进程可等」形态下服务端永不退出即资源驻留防线破口（PITFALLS P1）；三形态（立即/宽限到期/宽限取消）中取消形态绝不置位、其余两形态必须置位"
    - requirement_id: PC-09
      category: values
      statement: "退出码断言不得放宽成「两模式都接受」——shared 255/退出码规则逐位锁定，per-client 断言走自身分支形态（零回归双证据口径红线）"
---

<objective>
PC-09 终结语义重建（防线装配序第三片）：pcSupervisor/pcExitReq 第二终结源（PITFALLS P1 窗口期闭合）+ last-reaped-code 退出码与 shared 逐位对齐 + session_end 审计 emit（D-09 前半）+ WR-02 reaped 栅栏零成本严格修法。Shutdown N 进程组快照信号属 13-04（server.go Shutdown 填充点分片）。

Purpose: Phase 11 D-04 明示的「per-client 下 --once/--exit-when-empty 永不退出」已知中间态在本 plan 闭合——「注册表已空且无子进程可等」形态仍能退出，且退出码规则与 shared 语义逐位对齐。
Output: pcSupervisor 单例 + 触发端三形态 + watcher 收割链三补行 + WR-02 栅栏 + 双时序/三形态/事件 schema 测试组。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-resource-defense/13-CONTEXT.md
@.planning/phases/13-resource-defense/13-RESEARCH.md
@.planning/phases/13-resource-defense/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 机制——pcSupervisor 钉死 + 触发端三形态 + watcher 收割链（session_end/pcLastExitCode/Broadcast）+ WR-02 栅栏（PC-09 + D-09 前半 + WR-02）</name>
  <reversibility rating="reversible">第二终结源是 Phase 11 D-04 明示窗口期的既定兑现（行为语义从永不退出→可退出，CONTEXT 已锁）；退出码 255 对齐经 06-CONTEXT OQ1 accept-255 裁决既定——机制内部化零公开契约面变更。</reversibility>
  <files>internal/server/server.go, internal/server/clients.go, internal/server/perclient.go</files>
  <read_first>
    - internal/server/server.go:183-192（Server 字段区 pcSessions 注释块——三新字段落点）与 :525（hubCond 既有装配）与 :535-538（New per-client 分支——pcSupervisor 钉死点）与 :1548-1552（terminate/termOnce 逐字复用件）与 :1592（exiting 置位点）
    - internal/server/clients.go:940-1030（maybeExitWhenEmptyLocked 全文——:957-960 早退守卫替换点 / :961 四守卫 / :966-970 立即形态 / :971-997 宽限 AfterFunc 三件套与身份比对复查 / :1022-1029 取消点零改动面）
    - internal/server/perclient.go:28-32（窗口期头注释移除点）与 :425-455（sessionWatcher 全文——:433 Wait 返回 / :439 close(waitDone) / :440-443 hubMu 置位区段）与 :473-504（teardownPCLocked 全文——快半段 :481-491 WR-02 栅栏插点 + 慢半段 :494-503 delete 点 Broadcast 补行）与 :80-84（pc.startedAt/pc.exitCode 预留字段）
    - internal/server/server.go:1473-1483（session_end shared schema 母本——exit_code/duration_seconds/signal 键 + exitSignalNum/signalName 链）
    - .planning/research/ARCHITECTURE.md §4.1（pcSupervisor 参考实现）与 §4.3（三形态分支表 + 两时序逐位对齐证明）
    - .planning/phases/13-resource-defense/13-PATTERNS.md §2/§3/§4（watcher 挂点/新字段落点/触发端逐行对照）
  </read_first>
  <behavior>
    - pcSupervisor：hubMu 内 for 循环等待 (s.pcExitReq || s.exiting) && len(s.pcSessions) == 0（hubCond.Wait）；出循环后读 code（pcHasExitCode → pcLastExitCode，缺省 0），放锁后 s.terminate(code)——termOnce 恰好一次与 shared 同一收口件
    - 触发端：per-client 分支四守卫沿用 → 立即形态：logEvent exit_when_empty 同形态发射 + s.pcExitReq = true + s.hubCond.Broadcast()（不发信号——末端断开的 teardown 已 SIGHUP 其会话）；宽限到期回调内（身份比对 s.exitEmptyTimer != t + 复查 exiting/非空通过后）：同两行动作 + 同事件行；宽限取消（cancelExitEmptyTimerLocked）：零改动
    - watcher 收割链：hubMu 内置位区段（:440-443）追加 s.pcLastExitCode = code + s.pcHasExitCode = true；session_end emit 在 hubMu 解锁后、EXIT 组帧直写前（程序序保证事件先于会话终结帧落流；避开 hubMu 内 I/O——emitEvent 写 stderr 经动态 writer 锁，锁序不混入 hubMu）；慢半段 delete(s.pcSessions, pc) 后同锁内 s.hubCond.Broadcast() 补行
    - WR-02：teardownPCLocked 快半段 `if !pc.reaped` 判定前加 waitDone 非阻塞 select 守卫——waitDone 已关闭即视为 reaped（内核态 reap 完成点 = Wait 返回时，close(pc.waitDone) :439 已在该点无需移动）；守卫通过才进 SignalGroup 分支
    - per-client 分支永不调 stopChildLocked（s.sess 恒 nil——早退守卫注释的 nil-deref 防线语义迁移至新分支注释）
  </behavior>
  <action>
    1. server.go：Server struct pcSessions 注释块（:188-192 区）后追加三字段 pcExitReq bool / pcLastExitCode int / pcHasExitCode bool（注释载 hubMu 保护与同步边论证——goroutine 启动 + hubMu 建立 happens-before，本仓注释即契约风格）；pcSupervisor 方法按 behavior 形态实现（蓝本 §4.1 参考实现逐字段对应）；New per-client 分支（:535-538）在 pcSessions make 与 13-02 桶装配之后加 go s.pcSupervisor()（per-client 唯一全局 goroutine，研究 §5 拓扑表；shared 分支零装配）。
    2. clients.go：:957-960 早退守卫替换为 behavior 三形态分支——立即与宽限到期两迁移点置位+Broadcast；事件行 exit_when_empty 同形态（remote/remoteUser 捕获先例沿用 :974-975）；注释更新（窗口期闭合回指 Phase 13；stopChildLocked nil-deref 防线语义迁移——per-client 分支不发信号不调 stopChildLocked）。
    3. perclient.go：watcher 三补（hubMu 内两字段置位 + 慢半段 delete 后 Broadcast + hubMu 外 session_end emit——schema 复制 server.go:1473-1483 母本：event=session_end/exit_code/duration_seconds=time.Since(pc.startedAt)/signal 键经既有 exitSignalNum/signalName 链 + slog.Int64("client_id", cl.attachSeq)）；teardownPCLocked 快半段加 WR-02 waitDone 非阻塞 select 守卫（注释载 STATE.md 登记修法：Wait-return→hubMu-acquire 微窗口内 reaped 误读的结构性栅栏）；:28-32 窗口期头注释移除并改写为第二终结源指针注释（pcSupervisor/pcExitReq）。
    4. go build ./... && go vet ./... && time go test -race ./internal/server/ -count=1 全绿（既有测试组——含 11-04 竞态注入组与 KILL 兜底时序组——原样绿 = 机制插入零回归；既有 emptyexit/shutdown shared 用例零改动）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && time go test -race ./internal/server/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - server.go 含 pcSupervisor 方法且 New per-client 分支含 go s.pcSupervisor()（grep 两命中）；terminate 调用点为 s.terminate(code)（termOnce 单点——grep 's.exitf(' 于 pcSupervisor 函数体零命中）
    - clients.go maybeExitWhenEmptyLocked 函数体内 per-client 分支含 pcExitReq = true 两置位点（立即 + 宽限到期回调——region grep 限定该函数体）；取消点 cancelExitEmptyTimerLocked 零 diff
    - perclient.go watcher 含 session_end emit（client_id 键）与 pcLastExitCode/pcHasExitCode 置位与慢半段 Broadcast 补行三锚点；teardownPCLocked 快半段 SignalGroup 判定前存在 waitDone select 守卫（region grep）
    - perclient.go 头注释窗口期登记（永不退出表述）已移除
    - 全量 -race 绿且既有测试零改动（git diff 白名单 = 本任务三文件；emptyexit_test.go/shutdown_test.go 既有断言行零改动）
  </acceptance_criteria>
  <done>第二终结源机制落地：pcExitReq/exiting 双触发 + pcSessions 归零唤醒 + last-reaped-code 消费链成立；session_end 审计 emit；WR-02 结构性栅栏落码；Task 2 测试组收口后 PC-09 前半完成。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 测试组——两时序退出码分叉 + exit-when-empty 三形态 + WR-02 栅栏 + session_end schema（PC-09/OPS-12 收口）</name>
  <files>internal/server/emptyexit_test.go, internal/server/perclient_test.go, internal/server/events_test.go, internal/server/export_test.go</files>
  <read_first>
    - internal/server/emptyexit_test.go:1-46（accept-255 门 :9-13 头注释 + exitf 捕获桩收 -1 恰好一次形态 :29-46——per-client 断言分叉走两时序对齐证明，禁止断言放宽）
    - internal/server/perclient_test.go:40-135（harness）与 :1115-1153（TestPerClientStopTimeoutKillFallback 时序双断言形态——静默窗/护栏竞速纪律）
    - internal/server/events_test.go:427-471（TestSessionEnd schema 断言母本——captureStderr → parseEvents → eventsNamed 计数锁 + 键存在/缺席双向断言）与 :420-422（attach/detach client_id 关联先例）
    - internal/server/export_test.go 全文（ForTest 四件套纪律：hubMu 内读写/调用方不得持 hubMu/仅服务测试声明）
    - internal/server/perclient.go（Task 1 落地后的 waitDone 守卫形态——WR-02 测试构造对象）
  </read_first>
  <behavior>
    - emptyexit per-client 分叉：子先死（exit 0）→ exitf 捕获桩收 0 恰好一次；客户端先断 → exitf 捕获 -1 恰好一次（进程级 255 由 13-07 phase13.mjs S3 进程级断言承接——Go 内断言常量 -1，头注释 OQ1 门形态沿用）
    - exit-when-empty 三形态 per-client：grace=0 立即 → exitCh 收到退出码；grace>0 宽限到期 → 退出（覆写短 grace 或真实等待，时序纪律轮询护栏）；宽限内重连 → 不退出（取消形态——新 attach 后 exitCh 静默窗无值）
    - --once per-client：唯一客户端断开 → exitCh 收到 -1（--once 语法糖展开同路径，06-04 先例）
    - WR-02（TestPerClientReapedFence）：export_test.go 加白盒构造出口——waitDone 已 close 且 reaped==false 的 pcSession → 调 teardownPCLocked → 断言恰好一次收口完成（teardownDone 关闭、不阻塞不 panic、ESRCH 静默面）；「未发信号」半边由源码 region 断言承载（快半段 select 守卫形态锁定）
    - session_end schema：per-client 会话 exit 42 → 事件恰一 + exit_code==42 + duration_seconds 键在 + client_id 键在且与 attach 事件同值 + 无 signal 键（非信号死亡）；信号死亡形态 → signal 键在（KILL 兜底归因面——trap '' HUP + stopTimeout 覆写短值，watcher 收割 signal="KILL"）
  </behavior>
  <action>
    1. emptyexit_test.go：per-client 断言分叉新测（头注释载两时序逐位对齐证明引蓝本 §4.3；accept-255 门注释保留——shared 既有断言逐字不动）。
    2. perclient_test.go：exit-when-empty 三形态 + --once 形态 + TestPerClientReapedFence 落位；exitCh 观测经 harness 既有返回通道；时序纪律——轮询替代固定 sleep（Phase 9 flake 教训）、静默窗 select+time.After 竞速、统护只做护栏。
    3. export_test.go：WR-02 白盒构造出口（waitDone 预关闭 + reaped 未置位的 pcSession 构造 + teardownPCLocked 直调——ForTest 四件套逐字形态）。
    4. events_test.go：session_end per-client schema 组（exit 42 与信号死亡两形态；client_id 关联键与 attach 事件同值断言）。
    5. TDD 提交收口：Task 1 feat 提交 + 本任务 test 提交（11-01/11-03/12-03 先例）。
    6. time go test -race -run 'TestPerClientExitWhenEmpty|TestPerClientOnce|TestPerClientReapedFence|TestPerClientSessionEnd|TestEmptyExit' ./internal/server/ -count=1 -v 全绿 → 全量 time go test -race ./internal/server/ -count=1 回归。
  </action>
  <verify>
    <automated>time go test -race -run 'TestPerClientExitWhenEmpty|TestPerClientOnce|TestPerClientReapedFence|TestPerClientSessionEnd|TestEmptyExit' ./internal/server/ -count=1 -v && time go test -race ./internal/server/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 两时序断言分叉锁定（子先死=0 / 客户端先断=-1 恰好一次）；shared 既有 emptyexit 断言逐字未动
    - exit-when-empty 三形态（立即/宽限到期/宽限取消）+ --once 形态全部覆盖且 -race 全绿——Pitfall 1「只有立即形态用例」告警面闭合
    - TestPerClientReapedFence 白盒构造成立且源码 region 断言（waitDone select 守卫）在场
    - session_end 两形态 schema 断言（exit 42 无 signal 键 / 信号死亡 signal="KILL" 归因）+ client_id 同值关联
    - 新测零固定 sleep 时点断言（region grep：新增函数体 sleep 仅作节奏非断言依据）
  </acceptance_criteria>
  <done>PC-09 前半锁定：两时序退出码逐位对齐、三形态第二终结源全绿、WR-02 栅栏双通道（白盒+源码断言）、session_end per-client schema 出数。</done>
</task>

</tasks>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **PC-09 (unclassified, unresolved)**：探针 unclassified 行永不 auto-resolve（#1110）——PC-09 的未分类边界形态（--once 与 --exit-when-empty 叠加、grace 宽限与 Shutdown 竞态交错）未在探针分类中显式覆盖；以需求文本 + 蓝本 §4.3 三形态分支表 + 两时序对齐证明为完整裁决面，执行中若发现该面之外的未覆盖边界须显式登记而非静默放行。保持 flagged-unverified，由 13-08 收口闸承载终验。
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 子进程→server（退出时序） | 「先断后死」「先死后断」两时序与 Wait-return→hubMu-acquire 微窗口是终结语义的不可信时序面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-09 | DoS | 空即不死——注册表空迁移无第二终结源，per-client --once/--exit-when-empty 永不退出（PITFALLS P1） | high | mitigate | pcSupervisor/pcExitReq 第二终结源 + 三形态触发端（Task 1）+ 三形态测试锁定（Task 2） |
| T-13-10 | Tampering | kill-after-reap 误杀复用 pgid（Wait-return→hubMu-acquire 微窗口内 reaped 误读） | medium | mitigate | WR-02 waitDone 非阻塞 select 结构性栅栏（Task 1）+ TestPerClientReapedFence 白盒构造（Task 2）；既有 reaped 栅栏保持 |
| T-13-11 | DoS | exitf 多触发源（第二终结源与 Shutdown 竞态双收口） | medium | mitigate | termOnce/terminate 逐字复用（server.go:1548-1552 既有单点）；pcSupervisor 出循环后唯一调用点 |
| T-13-12 | Information Disclosure | session_end 事件敏感值 | low | accept | schema 复用 shared 母本（exit_code/duration/signal/client_id 四键全是数值/关联键）；零敏感值红线经 emitEvent 既有通道保持 |
| T-13-SC | Tampering | 依赖面 | high | mitigate | 零新依赖红线；本 plan 无装包任务，legitimacy 门不触发 |
</threat_model>

<verification>
- time go test -race ./internal/server/ -count=1 全绿（新五测组 + 既有组零回归）
- exitf 恰好一次：全量 -race 下竞态注入组（11-04 既有）原样绿
- WR-02：快半段 select 守卫源码断言 + 白盒构造测双通道
</verification>

<success_criteria>
1. per-client --once/exit-when-empty 三形态全部可退出，退出码两时序逐位对齐（0 / -1→进程级 255）
2. session_end 事件带 exit_code/duration/client_id，KILL 兜底经 signal 字段归因
3. WR-02 微窗口结构性闭合；shared 全量测试原样绿
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `internal/server/server.go`：`Server.pcExitReq`/`pcLastExitCode`/`pcHasExitCode` 三字段（hubMu 保护）；`pcSupervisor()` 方法（第二终结源单例 goroutine）；New per-client 分支 `go s.pcSupervisor()` 钉死行
- `internal/server/clients.go`：`maybeExitWhenEmptyLocked` per-client 三形态分支（pcExitReq 置位 + hubCond.Broadcast() 两迁移点）
- `internal/server/perclient.go`：sessionWatcher 三补（`pcLastExitCode`/`pcHasExitCode` 置位、session_end emit、`hubCond.Broadcast()` 补行）；teardownPCLocked 快半段 waitDone 非阻塞 select 守卫（WR-02）；头注释窗口期登记移除
- `internal/server/export_test.go`：WR-02 白盒构造出口（waitDone 预关闭/reaped 未置位 pcSession 构造 + teardown 直调）
- 新测试：emptyexit_test.go per-client 两时序分叉组；perclient_test.go `TestPerClientExitWhenEmpty*`（三形态）/`TestPerClientOnce*`/`TestPerClientReapedFence`；events_test.go session_end per-client schema 组

<output>
Create `.planning/phases/13-resource-defense/13-03-SUMMARY.md` when done
</output>
