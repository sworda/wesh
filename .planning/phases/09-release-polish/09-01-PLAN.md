---
phase: 09-release-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .goreleaser.yml
  - .github/workflows/release.yml
autonomous: true
requirements:
  - OPS-10

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "goreleaser 本机 snapshot 预演产出 linux/darwin × amd64/arm64 四个全静态二进制 + 4 个 tar.gz（每包含 wesh + LICENSE + README.md 三件套）+ 裸 checksums.txt——命名族 wesh_v<TAG>_<os>_<arch>.tar.gz（.Tag 保 v 前缀，RESEARCH Pitfall 2）"
    - "linux/amd64 产物：file 报 statically linked ELF、ldd 报 not a dynamic executable（CGO_ENABLED=0，Pitfall 5）；干净容器（debian:stable-slim bind-mount 单文件）运行 --version 退出 0 且输出非 dev（ldflags -X main.version 注入生效，挂 main.go:32 既有注入点）"
    - "checksums.txt 裸名产出（显式 name_template 钉死，v2.18.0 默认是项目前缀形态，Pitfall 1）且 sha256sum -c 对全部产物校验通过"
    - "darwin amd64/arm64 产物分层验证（本机不可运行，Pitfall 12 既定取舍）：file 报 Mach-O 64-bit 对应架构 + 两 tar 内容同为三件套"
    - ".goreleaser.yml 键名全按 v2.18.0 schema 定稿：builds.main ./cmd/wesh（Pitfall 3）、env CGO_ENABLED=0（唯一出现点）、flags -trimpath、ldflags -s -w -X main.version={{.Version}}、mod_timestamp {{ .CommitTimestamp }}、archives formats: [tar.gz]、changelog filters 排除 docs/test/chore/ci/style"
    - ".github/workflows/release.yml（D-01/D-03）：on.push.tags [\"v*\"] 触发、permissions 仅 contents: write、checkout fetch-depth: 0（Pitfall 11）、pnpm/action-setup@v6.0.10（version 11.21.0）→ setup-node@v4（24）→ pnpm -C web install --frozen-lockfile → pnpm -C web build → setup-go@v7.0.0（go-version-file go.mod）→ goreleaser/goreleaser-action@v7.2.3（args release --clean）显式编排——不用 goreleaser before hooks、不用入库 dist"
    - "release.yml 不设 CGO_ENABLED 环境变量（ci.yml:15 注释纪律：该变量只属于 .goreleaser.yml builds.env 单侧持有）"
  artifacts:
    - path: ".goreleaser.yml"
      provides: "四平台交叉编译/打包/校验和配置（version: 2 schema）"
      contains: "name_template"
    - path: ".github/workflows/release.yml"
      provides: "tag push 触发的发布 workflow（D-01/D-03 显式编排）"
      contains: "goreleaser/goreleaser-action@v7.2.3"
  key_links:
    - from: ".github/workflows/release.yml pnpm build 步骤"
      to: ".goreleaser.yml builds（go:embed 读取 web/dist）"
      via: "D-03 显式编排：pnpm install+build（钉 11.21.0/node 24）先于 setup-go 与 goreleaser——dist 真实产物在 goreleaser 编译期经 go:embed 嵌入（P1 D-18 构建顺序的发布链固化）"
      pattern: "pnpm -C web build"
    - from: ".goreleaser.yml ldflags -X main.version={{.Version}}"
      to: "cmd/wesh/main.go:32 var version"
      via: "发布构建注入版本串（--version 输出与 /metrics wesh_build_info 同源）；开发构建保持 dev"
      pattern: "main.version"
    - from: ".goreleaser.yml archives/checksum name_template"
      to: "D-04 命名族与 D-02 裸 checksums.txt 契约"
      via: ".Tag 保 v 前缀（Pitfall 2：.Version 剥离 v）；checksum 显式钉裸名（Pitfall 1：v2 默认项目前缀形态）"
      pattern: "checksums.txt"
  prohibitions:
    - requirement_id: OPS-10
      category: values
      statement: "发布二进制 MUST NOT 动态链接（CGO_ENABLED=0 四平台；ldd 恒报 not a dynamic executable）——scp 到干净机器即可运行是产品核心承诺（PROJECT Constraints），动态链接 glibc 的发布物静默背弃该承诺"
      status: resolved
      verification: test
    - requirement_id: OPS-10
      category: transparency
      statement: "发布构建 MUST NOT 嵌入陈旧前端产物（D-03：release.yml 中 pnpm -C web build 步骤必须先于 goreleaser 步骤——用户收到的 UI 必须与该 tag 的 web/src 同源，不用入库 dist 防滞后）"
      status: resolved
      verification: test
    - requirement_id: OPS-10
      category: values
      statement: "MUST NOT 发布容器镜像到任何 registry（D-16 裁决：Dockerfile 入库不发镜像——scp 单文件哲学一致，镜像用户自建；release.yml 零 docker push/ghcr 步骤）"
      status: resolved
      verification: test
    - requirement_id: OPS-10
      category: values
      statement: "MUST NOT 发布 Windows 平台二进制（PROJECT 平台边界：Windows ConPTY 属既定 Out of Scope——.goreleaser.yml goos 仅 linux/darwin，产物族不出现 windows 字样）"
      status: resolved
      verification: test
---

<objective>
落地 OPS-10 发布链：新建 .goreleaser.yml（D-01..D-04 定稿形态——RESEARCH §Code Examples 全部键名经 v2.18.0 schema 一手核实）与 .github/workflows/release.yml（tag push 触发、pnpm → setup-go → goreleaser 显式编排），本机安装 goreleaser v2.18.0 完成 check + snapshot 预演，对四平台产物做分层断言（命名族/tar 内容/校验和/静态性/干净容器运行），证明「推 v* tag → GitHub Release 四平台全静态二进制」链路端到端成立。

Purpose: v1.0.0 送出门的唯一通道——goreleaser v2 与直觉/旧版有三处实证不符（checksum 默认名/.Version 剥 v/main 默认 "."），本 plan 用 snapshot 实证钉死，避免首发即翻车（D-04 命名族是 costly 级公开契约）。
Output: .goreleaser.yml + release.yml 入库，snapshot 全绿证据（真实 tag 发布随 09-09 发布脚本首证）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-release-polish/09-CONTEXT.md
@.planning/phases/09-release-polish/09-RESEARCH.md
@.planning/phases/09-release-polish/09-PATTERNS.md
</context>

<tasks>

<!-- planner-discipline-allow: CGO_ENABLED=0 -->
<!-- planner-discipline-allow: CGO_ENABLED -->

<task type="tracer">
  <name>Task 1 (tracer): 发布链端到端——.goreleaser.yml 定稿 + 本机 goreleaser 安装 + check + snapshot 预演 + 四平台产物分层断言（命名/内容/校验和/静态/干净容器运行）</name>
  <reversibility rating="costly">D-04：tar.gz 命名族 wesh_v1.0.0_linux_amd64 发布后用户脚本/文档会引用该产物名格式，改形态破坏既有下载锚点——用户已在 09-CONTEXT 锁定，记录评级不重复设卡。</reversibility>
  <files>.goreleaser.yml</files>
  <read_first>
    - .planning/phases/09-release-polish/09-RESEARCH.md（§Code Examples .goreleaser.yml 定稿推荐逐字形态；Pitfall 1 checksum 默认名 / Pitfall 2 .Version 剥 v / Pitfall 3 main 默认 "." / Pitfall 5 CGO 两通道 / Pitfall 12 darwin 分层验证；Package Legitimacy Audit 全 OK 表）
    - .planning/phases/09-release-polish/09-CONTEXT.md（D-01..D-04 锁定决策逐字）
    - cmd/wesh/main.go（:32 var version = "dev" ldflags 注入点——-X main.version 挂点实证）
    - .github/workflows/ci.yml（:15 CGO 注释纪律——CGO_ENABLED=0 只属于发布构建）
    - .gitignore（web/dist/*.gz 不入库既定——release 二进制走 gzip 旁路、本机 snapshot 嵌入现状 dist）
  </read_first>
  <action>
    ① 本机安装 goreleaser v2.18.0（RESEARCH Environment Availability 既定通道二选一：官方 release 二进制 github.com/goreleaser/goreleaser/releases/download/v2.18.0/goreleaser_Linux_x86_64.tar.gz 解至 ~/bin 或 /tmp，优先——快；或 go install github.com/goreleaser/goreleaser/v2@v2.18.0——Go proxy 实证最新稳定）。装后 goreleaser --version 确认 2.18.x。安装产物不进仓。
    ② 新建 .goreleaser.yml（RESEARCH §Code Examples 定稿逐字形态）：version: 2 + project_name: wesh；builds 单条目——main: ./cmd/wesh（Pitfall 3：默认 "." 在仓库根找不到 main 包必败）、binary: wesh、env: [CGO_ENABLED=0]（Pitfall 5：全仓唯一出现点，ci.yml -race 通道不设）、goos: [linux, darwin]、goarch: [amd64, arm64]、flags: [-trimpath]、ldflags: [-s -w -X main.version={{.Version}}]、mod_timestamp: "{{ .CommitTimestamp }}"；archives 单条目——formats: [tar.gz]、name_template: "{{ .ProjectName }}_{{ .Tag }}_{{ .Os }}_{{ .Arch }}"（Pitfall 2：.Tag 保 v 前缀，D-04 命名族）、files: [LICENSE, README.md]；checksum: name_template: checksums.txt（Pitfall 1：v2.18.0 默认 {{ .ProjectName }}_{{ .Version }}_checksums.txt 不符 D-02）；changelog: sort asc + filters.exclude ^docs:/^test:/^chore:/^ci:/^style:。文件头注释登记 D-01..D-04 与三个 Pitfall 钉点。
    ③ goreleaser check 退出 0（schema 合法）。
    ④ goreleaser release --snapshot --clean 预演：dist/ 下产出四构建目录（linux_amd64/linux_arm64/darwin_amd64/darwin_arm64 各含 wesh）+ 4 个 tar.gz + checksums.txt。逐项断言：tar -tzf 每个 tar.gz 恰含 wesh/LICENSE/README.md 三条目；命名族匹配 ^wesh_v.*_(linux|darwin)_(amd64|arm64)\.tar\.gz$（snapshot 伪 tag 同样走 .Tag 模板——形状断言，精确 v1.0.0 名随真实发布首证）；dist/ 内 sha256sum -c checksums.txt 全 OK；file linux_amd64 产物报 ELF 64-bit statically linked；ldd 报 not a dynamic executable；file darwin 两产物报 Mach-O 64-bit（x86_64/arm64 对应）；干净容器运行——docker run --rm -v $PWD/dist/linux_amd64/wesh:/wesh debian:stable-slim /wesh --version 退出 0 输出 wesh 前缀且非 "wesh dev"（ldflags 注入生效；snapshot 伪版本串可接受）。
    ⑤ 预演后清理：rm -rf dist/（goreleaser 输出目录，非 web/dist——路径辨别纪律，web/dist 是 embed 资产绝不可动）；git status 确认仓库零残留（.goreleaser.yml 之外零新文件）。
    提交：feat(09-01): add goreleaser config for four-platform static binary release
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && goreleaser check && goreleaser release --snapshot --clean && test -f dist/checksums.txt && ls dist/*.tar.gz | grep -cE '^dist/wesh_v.*_(linux|darwin)_(amd64|arm64)\.tar\.gz$' | grep -qx 4 && (cd dist && sha256sum -c checksums.txt >/dev/null) && tar -tzf $(ls dist/wesh_v*_linux_amd64.tar.gz | head -1) | sort | tr '\n' ' ' | grep -qx 'LICENSE README.md wesh ' && file dist/linux_amd64/wesh | grep -q 'ELF 64-bit' && file dist/linux_amd64/wesh | grep -qi 'statically linked' && (ldd dist/linux_amd64/wesh 2>&1 | grep -q 'not a dynamic executable') && docker run --rm -v $PWD/dist/linux_amd64/wesh:/wesh debian:stable-slim /wesh --version | grep -qE '^wesh .+' && test "$(docker run --rm -v $PWD/dist/linux_amd64/wesh:/wesh debian:stable-slim /wesh --version)" != "wesh dev" && rm -rf dist && git status --porcelain | grep -cv '^?? dist/' </automated>
  </verify>
  <acceptance_criteria>
    - goreleaser check 退出 0；snapshot 产出 4 tar.gz + checksums.txt，命名族正则全命中
    - linux_amd64 产物：statically linked ELF + ldd 非动态 + 干净容器 --version 非 dev
    - 每 tar 恰含 wesh/LICENSE/README.md 三件套；sha256sum -c 全 OK；darwin 产物 file 报 Mach-O
    - `grep -c 'CGO_ENABLED=0' .goreleaser.yml` == 1（单侧持有——ci.yml:15 既有注释提及不计；release.yml 侧零出现由 Task 2 验收锁定）
    - `grep -c 'main: ./cmd/wesh' .goreleaser.yml` == 1；`grep -c '{{ .Tag }}' .goreleaser.yml` == 1；`grep -c 'name_template: checksums.txt' .goreleaser.yml` == 1
    - 预演后 dist/ 已清理，git status 除 .goreleaser.yml 外零残留
  </acceptance_criteria>
  <done>
    .goreleaser.yml 定稿入库；本机 snapshot 端到端证明四平台全静态二进制 + D-04 命名族 + D-02 裸 checksums.txt 全部成立；干净容器可运行。
  </done>
</task>

<task type="auto">
  <name>Task 2: release.yml 定稿——tag 触发 + pnpm→setup-go→goreleaser 显式编排（D-01/D-03）+ 静态审查断言</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/ci.yml（全文 30 行——web leg 钉版组合 pnpm/action-setup@v6.0.10 version 11.21.0 + setup-node@v4 node 24 逐行复用源；:15 CGO 注释纪律）
    - .planning/phases/09-release-polish/09-RESEARCH.md（§Code Examples release.yml 定稿推荐；Pattern 1 关键实证点：fetch-depth: 0 必配 / permissions: contents: write 最小授权 / tags ["v*"] 触发写法；Pitfall 11）
    - .planning/phases/09-release-polish/09-PATTERNS.md（§1 release.yml pattern assignment——钉版与步骤形态 + 差异点清单）
    - web/package.json（build 脚本 = tsc && vite build && gzip -k9 实证——workflow 注释引用形态）
  </read_first>
  <action>
    ① 新建 .github/workflows/release.yml（RESEARCH 定稿逐字形态）：name: release；on.push.tags: ["v*"]（D-01：版本号起点 v1.0.0，版本史与 git tag 同源）；permissions: contents: write（GITHUB_TOKEN 创建 Release 最小授权）；单 job release（runs-on: ubuntu-latest）步骤序——actions/checkout@v7.0.1（with fetch-depth: 0，Pitfall 11：changelog/tag 推断前提）→ pnpm/action-setup@v6.0.10（with version: 11.21.0，web/package.json 无 packageManager 字段需显式钉版——ci.yml:24 注释同款）→ actions/setup-node@v4（with node-version: 24）→ run: pnpm -C web install --frozen-lockfile → run: pnpm -C web build（D-03 显式编排核心：dist 真实产物先于 goreleaser——不用 goreleaser before hooks 环境隐式、不用入库 dist 防滞后 web/src 未构建提交）→ actions/setup-go@v7.0.0（with go-version-file: go.mod）→ goreleaser/goreleaser-action@v7.2.3（with distribution: goreleaser、version: "~> v2.18.0"、args: release --clean；env GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}）。
    ② 注释纪律：web leg 步骤块上方加一行注释指明「D-03 显式编排：pnpm 先于 goreleaser（与 ci.yml web leg 同钉版）」；文件不设 CGO_ENABLED（ci.yml:15 纪律——该变量只属 .goreleaser.yml builds.env）。
    ③ 静态审查：python3 或 node 解析 YAML 确认语法合法（无 actionlint 环境时以 ruby/python yaml 解析兜底）；逐项 grep 断言钉版与键值（见 acceptance）。
    提交：feat(09-01): add tag-triggered release workflow with explicit pnpm-first orchestration
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release.yml'))" && grep -c 'goreleaser/goreleaser-action@v7.2.3' .github/workflows/release.yml | grep -qx 1 && grep -c 'fetch-depth: 0' .github/workflows/release.yml | grep -qx 1 && grep -c 'contents: write' .github/workflows/release.yml | grep -qx 1 && grep -c 'pnpm -C web build' .github/workflows/release.yml | grep -qx 1 && ! grep -q 'CGO_ENABLED' .github/workflows/release.yml && ! grep -q 'before' .goreleaser.yml</automated>
  </verify>
  <acceptance_criteria>
    - YAML 解析合法；on.push.tags 含 "v*"；permissions 仅 contents: write 一键
    - 步骤顺序断言：checkout(fetch-depth: 0) → pnpm/action-setup@v6.0.10(11.21.0) → setup-node@v4(24) → pnpm install --frozen-lockfile → pnpm build → setup-go@v7.0.0 → goreleaser-action@v7.2.3（pnpm build 行号 < goreleaser 行号，D-03 顺序机械断言）
    - `grep -c 'CGO_ENABLED' .github/workflows/release.yml` == 0（单侧持有纪律）
    - .goreleaser.yml 零 before hooks（D-03 排除形态）
    - 全部 Action 钉版（@v7.0.1/@v6.0.10/@v4/@v7.0.0/@v7.2.3），goreleaser version "~> v2.18.0"
  </acceptance_criteria>
  <done>
    release.yml 入库：tag push 触发、D-03 显式编排形态与钉版全经静态断言；真实全链首证 = v1.0.0 发布（09-09 发布脚本最后一步，本 plan 不实际打 tag）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → GitHub Release | GITHUB_TOKEN 创建发布物——权限最小化是唯一闸门 |
| 第三方 Action/goreleaser 下载 → 构建环境 | 供应链投毒面（发布物直接被用户 scp 运行） |
| git tag → 版本史 | 错 tag/脏树发布会把错误产物钉进版本史（D-01 同源纪律） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01a | Tampering | Action/goreleaser 下载供应链投毒 | high | mitigate | 全部 Action 钉版（@v7.0.1/@v6.0.10/@v4/@v7.0.0/@v7.2.3）；goreleaser-action 内建下载包 checksums 自校验（README Verification 节实证）；RESEARCH Package Legitimacy Audit 全制品 gh api 核验 OK；本机 goreleaser 经官方 release/Go proxy 两通道安装 |
| T-09-01b | Tampering | 脏树/错 tag 发布漂移（产物与仓库不一致） | medium | mitigate | D-14 发布脚本前置 worktree 干净校验 + tag 形态/不存在校验 + 确认闸（09-09 落地）；本 plan 只交付链不实际打 tag |
| T-09-01c | Elevation of Privilege | GITHUB_TOKEN 权限过大被 workflow 内步骤滥用 | medium | mitigate | permissions 仅 contents: write 单键于 release job（官方 README 最小授权形态）；无其他 job 授权 |
| T-09-01d | Information Disclosure | 发布物/CI 日志携带敏感值 | low | mitigate | workflow 零秘密（GITHUB_TOKEN runner 注入不打印）；tar 内容白名单三件套（wesh+LICENSE+README.md）机械断言；D-02 裁决仅 checksums.txt 无签名/SBOM——威胁模型已裁决接受 |
| T-09-SC | Tampering | npm/pip/cargo 包安装供应链 | high | mitigate | 本 plan 零 npm/pip/cargo 新包（前端零新运行时依赖，Go 零新依赖）；goreleaser 为 Go 生态制品经 RESEARCH 审计 OK（gsd seam 不支持 go 生态，以 gh api 直读官方仓库核验替代） |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V10 Configuration/供应链 | ✓ 钉版 + 内建校验 + 最小授权 + RESEARCH 审计表 |
| V5 Input Validation（构建输入面） | ✓ goreleaser check schema 闸 + snapshot 预演产物白名单断言 |
</threat_model>

<verification>
- goreleaser check + snapshot 全绿（Task 1 验收全项）
- release.yml YAML 合法 + 钉版/编排/授权静态断言全过
- `git diff --exit-code go.mod go.sum web/pnpm-lock.yaml` 退出 0（零新依赖证据）
</verification>

<success_criteria>
- 本机一次命令（goreleaser release --snapshot --clean）从源码产出四平台全静态 tar.gz + 裸 checksums.txt，干净容器 scp 单文件即可运行——ROADMAP SC1 的本地等价形态成立
- release.yml 推 v* tag 即发布的编排形态经静态审查锁定，真实首证留给 v1.0.0
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **OPS-10 (unclassified, unresolved)**：probe 无法从需求文本分类边界形态——本 plan 以 09-CONTEXT D-01..D-04、RESEARCH §Code Examples/Pitfalls 与 Validation Architecture 为验收来源（snapshot 产物形状/静态性/干净容器运行全显式验证）。SPEC 从未对该需求分类的事实登记于此，不静默丢弃。人工复核项：① darwin 产物 macOS 运行冒烟（本机不可运行——09-VALIDATION Manual-Only 登记，scp 到 Mac 跑 wesh --version）；② D-04 精确命名 wesh_v1.0.0_linux_amd64.tar.gz 的逐字首证 = 真实 tag 发布（snapshot 伪 tag 只证模板形状——已裁决的验证取舍，RESEARCH Pitfall 12/OQ3 登记）；③ release.yml 全链首证 = v1.0.0 真实发布（09-09 发布脚本最后一步）。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `.goreleaser.yml`（新文件，version: 2 schema——project_name wesh / builds.main ./cmd/wesh / env CGO_ENABLED=0 / flags -trimpath / ldflags -s -w -X main.version={{.Version}} / mod_timestamp / archives formats [tar.gz] + name_template "{{ .ProjectName }}_{{ .Tag }}_{{ .Os }}_{{ .Arch }}" + files [LICENSE, README.md] / checksum.name_template checksums.txt / changelog filters）
- `.github/workflows/release.yml`（新文件——name release / on.push.tags ["v*"] / permissions contents: write / job release 七步骤显式编排）
- 无 Go/TS 符号新增；无 CLI flag/config 键新增（version ldflags 注入点 main.go:32 为既有，零代码改动）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/09-release-polish/09-01-SUMMARY.md` when done
</output>
