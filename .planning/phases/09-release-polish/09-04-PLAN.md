---
phase: 09-release-polish
plan: 04
type: execute
wave: 2
depends_on:
  - "09-02"
files_modified:
  - cmd/wesh/config.go
  - cmd/wesh/main.go
  - cmd/wesh/config_test.go
  - cmd/wesh/main_test.go
  - internal/server/server.go
  - web/embed.go
  - internal/server/customindex_test.go
autonomous: false
requirements:
  - OPS-03

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-05/D-06 整页替换双通道统一：--index 给定时 index.html 路径（含空路径回落）返回启动读入的自定义字节（byte-identity，零注入零模板零校验）；/ 与 /s/{token}/（有效 token 分支）伺服同一字节（wh 单点装饰、sharePage 委托天然统一，sharetoken.go 零改动）；其他一切路径照旧走 FileServerFS——自定义页引用的相对路径资源 → 404"
    - "D-07 读入语义：启动一次读入 []byte 常驻内存，运行期零磁盘依赖，改文件需重启生效；入 TOML 配置同名键 index（P7 D-04 覆盖面先例）"
    - "D-07/D-08 启动校验四拒绝 exit 2 fail-fast：不存在/非常规文件（validateStartup stat 级，--cwd 预检同位）+ 不可读/超上限（启动读入 io.LimitReader(max+1)，max 默认 16MiB）；错误行只含路径与原因类别，绝不含文件内容任何字节（P3/P4 启动面红线延伸，Pitfall 9）"
    - "D-08 index-max-size：TOML 整数字节纯配置键（RESEARCH OQ1 推荐形态——零新解析代码，与 max-clients 整数先例同型），无对应 CLI flag（P7 D-03 纪律的明示例外，README 写明防蔓延）；≤0 经 validateStartup 拒绝"
    - "gzip（09-UI-SPEC §4 定稿采纳预压）：启动读入后 gzip 预压一次缓存；Accept-Encoding 显式含 gzip → Content-Encoding: gzip 发预压体，否则明文；Vary: Accept-Encoding 恒发（embed.go:36 同款纪律）；Accept-Encoding 解析复用 web 包 acceptsGzip（零第二份解析器）"
    - "wesh 不承担面（D-05 直接推论）：不注入/不修改/不校验用户 HTML；安全头现状同源——自定义页响应恒带既有 securityHeaders（CSP script-src 'self' 'unsafe-inline' 内联可执行、connect-src 'self' 允许用户页回连 /ws、frame-ancestors 'none' 同内建页）；/api/attach、/ws、/healthz、/metrics 与 share token 门禁照旧不受 --index 影响"
    - "0 字节文件合法：伺服空白页是用户明示的整页替换语义（D-07 拒绝列表 = 不存在/不可读/非常规/超限，不含空文件——「验证为主」纪律不过度校验）"
    - "UI-SPEC §UI Considerations custom-index-page covered 行：16MiB 硬顶在启动读入期拒绝，运行期伺服定长已读入字节无溢出面；启动一次读入运行期零磁盘 IO 无加载态，读入失败即启动失败（exit 2）无部分可用中间态"
    - "UI-SPEC §UI Considerations cli-startup-output covered 行：--index 校验失败 exit 2 错误行为原子单行（路径+原因类别）；长路径在终端自然折行不截断、路径长度不设契约上限"
    - "零值兜底纪律：Options.CustomIndex nil（未配置 --index）时伺服行为与现状逐字节一致"
    - "go.mod/go.sum 逐字节不动（compress/gzip 为 stdlib，零新依赖）"
  artifacts:
    - path: "cmd/wesh/main.go"
      provides: "--index flag + cfg.index/cfg.indexMaxSize + validateStartup stat 级预检 + loadCustomIndex 启动读入 + Options.CustomIndex 透传"
      contains: "loadCustomIndex"
    - path: "cmd/wesh/config.go"
      provides: "fileConfig Index/IndexMaxSize 两键（27→29 键）"
      contains: "index-max-size"
    - path: "web/embed.go"
      provides: "WithCustomIndex 装饰函数（gzip 预压缓存 + Vary 恒发 + acceptsGzip 复用）"
      contains: "WithCustomIndex"
    - path: "internal/server/server.go"
      provides: "Options.CustomIndex + Handler() :449 区单点装饰"
      contains: "CustomIndex"
    - path: "internal/server/customindex_test.go"
      provides: "TestCustomIndex 行为锁（双通道/404/gzip/Vary/0 字节/base-path/零值兜底/认证面不变）"
      contains: "TestCustomIndex"
  key_links:
    - from: "cmd/wesh/main.go loadCustomIndex"
      to: "internal/server Options.CustomIndex → server.go:449 区 web.WithCustomIndex(wh, ...)"
      via: "启动读入 []byte 生产直传（BasePath/AuthHeader/Version 先例形态）；wh 单点装饰使 / 与 /s/{token}/ 两通道经 sharePage 既有委托自然统一（sharetoken.go:87-96 零改动）"
      pattern: "CustomIndex"
    - from: "cmd/wesh/config.go fileConfig Index/IndexMaxSize"
      to: "cmd/wesh/main.go 默认值铺底段"
      via: "两阶段合并：index 键换算 flag 注册默认值（flag > 配置 > 默认）；index-max-size 纯配置键直接赋值 cfg（无 flag 注册——D-08 例外形态）"
      pattern: "IndexMaxSize"
  prohibitions:
    - requirement_id: OPS-03
      category: values
      statement: "自定义页 MUST NOT 被 wesh 注入/修改/模板化/校验任何内容（D-05 零模板注入面——拒绝片段注入/水印/横幅；伺服字节与启动读入字节 byte-identity，operator 自有内容自治）"
      status: resolved
      verification: test
    - requirement_id: OPS-03
      category: privacy
      statement: "启动校验错误行 MUST NOT 含自定义页文件内容任何字节（D-08 红线——路径+原因类别之外零内容；自定义页可能含 operator 私有信息，超限/不可读场景尤其不得回显文件头字节）"
      status: resolved
      verification: test
    - requirement_id: OPS-03
      category: safety
      statement: "--index MUST NOT 改变认证/授权攻击面（/api/attach 守卫链、/ws Origin 与 ticket 核销、share token 门禁、/healthz 免认证例外不蔓延、/metrics 认证闸跟随——自定义页静默变成认证绕过面是本特性最危险的蜕变）"
      status: resolved
      verification: test
---

<objective>
落地 OPS-03 自定义首页（D-05..D-08，09-UI-SPEC §Custom Index Contract 逐字边界）：--index flag + TOML index/index-max-size 两键 + 启动一次读入与四拒绝校验矩阵 + server.go:449 唯一调用点装饰（WithCustomIndex：gzip 预压 + Vary 恒发）+ Go 行为测试组。one-way 确认门（D-05/D-06/D-07 公开行为契约）先行。

Purpose: ttyd -i 同款整页替换语义——operator 以自包含单 HTML 完全替代内建终端页（WS 端点照旧暴露），wesh 只负责伺服，零模板注入面。
Output: --index 全链（CLI/config/读入/校验/伺服/测试）+ TestCustomIndex 行为锁；协议层全链 UAT 属 09-05。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-release-polish/09-CONTEXT.md
@.planning/phases/09-release-polish/09-RESEARCH.md
@.planning/phases/09-release-polish/09-UI-SPEC.md
@.planning/phases/09-release-polish/09-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 确认门——--index 自定义首页公开行为契约（D-05/D-06/D-07，one-way）</name>
  <read_first>
    - .planning/phases/09-release-polish/09-CONTEXT.md（decisions 节 D-05/D-06/D-07/D-08 逐字 + Reversibility 标注）
    - .planning/phases/09-release-polish/09-UI-SPEC.md（§Custom Index Contract 七条逐字边界）
  </read_first>
  <decision>--index 语义 = 整页替换（ttyd -i 同款：用户 HTML 完全替代首页、终端功能用户页自行实现、wesh 零注入零模板）+ 全通道统一替换（/ 与 /s/{token}/ 同一给页源）+ 启动一次读入内存（改文件需重启生效）——三项均属 one-way 行为契约（README 公开承诺后改语义破坏已部署自定义页用户的预期；CLI flag 公开契约 P2 D-15 纪律）</decision>
  <context>
    D-05/D-06/D-07 在 discuss-phase 已锁定（09-CONTEXT 逐字 + UI-SPEC 契约化：替换点唯一/读入语义/四拒绝校验/gzip 预压/CSP 现状同源/wesh 不承担面）。本门是实现前最后一次确认，不是重新决策。
  </context>
  <options>
    <option id="as-locked">
      <name>按锁定值落地（推荐）</name>
      <pros>整页替换 + 双通道统一 + 启动读入 + 16MiB 硬顶（index-max-size 纯配置键例外）；与 09-CONTEXT D-05..D-08、09-UI-SPEC §Custom Index Contract 逐字一致</pros>
      <cons>自定义页引用的相对路径资源 404（契约语义——README 承诺语「自包含单 HTML」义务随 09-09 落地）</cons>
    </option>
    <option id="revise">
      <name>调整语义/通道/读入形态</name>
      <pros>若有部署侧新信息可低成本修正（如片段注入/模板变量诉求）</pros>
      <cons>推翻 09-CONTEXT 锁定项与 09-UI-SPEC 已批准契约，需重新 discuss 并回填 UI-SPEC 修订</cons>
    </option>
  </options>
  <resume-signal>选择 as-locked（继续实现）或 revise（说明调整点）</resume-signal>
  <acceptance_criteria>
    - 用户明确选择 as-locked 后才进入 Task 2；revise 时停止执行并回填 CONTEXT.md
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI/config 半侧——config.go 两键 + main.go --index flag + loadCustomIndex 启动读入 + validateStartup 预检 + 单测矩阵扩展</name>
  <reversibility rating="one-way">D-07：--index 是公开 CLI flag 契约（P2 D-15 纪律——发布后改名/改语义破坏部署脚本）；确认门 Task 1 已锁定，记录评级。</reversibility>
  <files>cmd/wesh/config.go, cmd/wesh/main.go, cmd/wesh/config_test.go, cmd/wesh/main_test.go</files>
  <read_first>
    - cmd/wesh/main.go（config struct L34-86——新字段落位区；parseArgs 头注释 L136-160 flag 计数与 Phase 行；默认值铺底段 L207-304 与 fc 合并先例；flag 注册区 L305+ StringVar 先例；validateStartup L899-999——--cwd stat 预检 L933-937 同位先例；run() 启动序——validateStartup 调用与 TLS 预检之间零资源占区位；server.New Options 字面量 L1142）
    - cmd/wesh/config.go（fileConfig L42-72——两键追加位；文件头覆盖面注释 L9「27 键」；D-04 排除项注释 L70-71）
    - cmd/wesh/main_test.go（TestStartupMatrix/TestParseArgs 表结构——--cwd 预检行与 wantBasePath 命名字段扩展先例）
    - cmd/wesh/config_test.go（writeToml 夹具 + 合并/优先级/未知键子测形态）
    - .planning/phases/09-release-polish/09-PATTERNS.md（§5 main.go 改造逐点指引 + §6 config.go 两键形态）
    - .planning/phases/09-release-polish/09-RESEARCH.md（OQ1 index-max-size 整数字节推荐；Pitfall 9 红线）
  </read_first>
  <behavior>
    - `--index /nonexistent` → exit 2，错误行含路径 + 不存在类别，无文件内容
    - `--index <目录>` → exit 2，含路径 + 非常规类别
    - `--index <合法文件>` → 校验通过；启动后改文件内容不影响伺服（重启才生效）
    - `--index <不可读文件>`（chmod 000，非 root 环境）→ exit 2，含路径 + 不可读类别
    - `--index <17MiB 文件>`（默认 16MiB 顶）→ exit 2，含路径 + 超限类别与上限数值，无内容字节
    - 配置 `index-max-size = 33554432` → 上限生效（33.5MiB 文件放行）；`index-max-size = 0` 或负值 → exit 2
    - 配置 `index = "/path/x.html"` + CLI `--index /path/y.html` → CLI 覆盖（flag > 配置）
    - TestParseArgs/TestStartupMatrix/TestLoadFileConfig 既有行零改动全绿
  </behavior>
  <action>
    ① cmd/wesh/config.go：fileConfig 追加 Index *string `toml:"index"` 与 IndexMaxSize *int `toml:"index-max-size"`（整数字节——RESEARCH OQ1 推荐，max-clients 整数先例同型零新解析）；文件头覆盖面注释「26 个长期运行 flag + command exec 数组 = 27 键」改写为「27 个长期运行 flag 同名键 + command exec 数组 + index-max-size 纯配置键 = 29 键」（D-04 排除项注释不动；index-max-size 行注释登记 D-08——纯配置键无对应 flag 是 P7 D-03 纪律的明示例外，防例外蔓延 README 写明）。
    ② cmd/wesh/main.go config struct 加 index string 与 indexMaxSize int 字段（Phase 9 注释分档：D-07/D-08 one-way 公开契约 P2 D-15 同纪律）。
    ③ parseArgs：默认值区加 indexDefault := "" 与 indexMaxSizeDefault := 16 * 1024 * 1024；fc 合并段加 fc.Index != nil → indexDefault = *fc.Index、fc.IndexMaxSize != nil → indexMaxSizeDefault = *fc.IndexMaxSize；fs.StringVar(&cfg.index, "index", indexDefault, "custom index HTML file served in place of the built-in page (full-page replacement; share links serve it too)")；cfg.indexMaxSize = indexMaxSizeDefault 直接赋值（无 flag 注册——D-08 纯配置键形态，赋值点注释登记）；parseArgs 头注释 flag 计数 30→31 并补 Phase 9 行（--index（09-04 D-07 自定义首页整页替换，启动读入校验归 validateStartup/loadCustomIndex）——07-02/07-05 注释同步先例）。
    ④ validateStartup（--cwd 预检 L933-937 同位——纯配置有效性与 bind 安全形态无关，loopback 早退之前判定）：cfg.index != "" 时 os.Stat 只读探测——serr != nil → return "", fmt.Errorf("invalid --index %q: file does not exist", cfg.index)；!fi.Mode().IsRegular() → return "", fmt.Errorf("invalid --index %q: not a regular file", cfg.index)（目录/设备/socket 同归此类）；cfg.indexMaxSize <= 0 → return "", errors.New("invalid index-max-size: must be positive")（配置键名入文案合法——键名非值）。错误文案只含路径+类别，绝不含内容（D-08 红线，TestStartupMatrix 全行断言纪律）。
    ⑤ loadCustomIndex(path string, max int) ([]byte, error) 新函数（run() 同文件纯 helper）：os.Open 失败 → fmt.Errorf("invalid --index %q: cannot read", path)；io.ReadAll(io.LimitReader(f, int64(max)+1))；len(data) > max → fmt.Errorf("invalid --index %q: exceeds index-max-size (%d bytes)", path, max)；错误形态只含路径+类别+上限数值（Pitfall 9——LimitReader 防误指巨大文件 OOM）。run() 内调用点：validateStartup warn 打印之后、pty.Start 之前（零资源占用拒绝纪律与 TLS 预检同位）；错误经 fmt.Fprintf(os.Stderr, "wesh: %v\n", err); return 2 落地（exit 2 fail-fast 与校验矩阵同语义）；未配置 --index 时 customIndex 保持 nil 跳过调用。
    ⑥ server.New Options 字面量（L1142）尾部追加 CustomIndex: customIndex（生产直传先例形态）。
    ⑦ 测试扩展——cmd/wesh/main_test.go：TestParseArgs 加 --index 行（命名字段转换先例，wantIndex 新字段，既有行零改动）；TestStartupMatrix 加四行（--index 不存在拒绝 / 目录拒绝 / 合法文件放行 / index-max-size=0 配置源拒绝——错误行断言含路径不含内容探针：预写含唯一内容探针串的 HTML 文件触发超限场景时断言 stderr 零探针）；loadCustomIndex 单测组（不可读 chmod 000 非 root 拒绝 / 超限拒绝 / 恰顶放行 / 0 字节放行 / 错误文本零内容探针）。cmd/wesh/config_test.go：index 键（配置设置生效 + CLI 覆盖）与 index-max-size 键（整数字节生效 + 字符串形态被 go-toml 类型不符拒绝）子测——writeToml 夹具复用，既有行零改动。
    提交：feat(09-04): add --index flag with startup validation and config keys
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && go test -count=1 ./cmd/wesh/ -run 'TestParseArgs|TestStartupMatrix|TestLoadFileConfig|TestConfigMerge|TestLoadCustomIndex' -v && go test -race -count=1 ./cmd/wesh/ && go vet ./cmd/wesh/ && git diff --exit-code go.mod go.sum</automated>
  </verify>
  <acceptance_criteria>
    - 新增/既有测试全 PASS（四拒绝 + 覆盖 + 纯配置键 + 红线探针）
    - `grep -c 'toml:"index"' cmd/wesh/config.go` == 1 且 `grep -c 'toml:"index-max-size"' cmd/wesh/config.go` == 1
    - `grep -c '"index"' cmd/wesh/main.go` == 1（--index flag 注册唯一）
    - `! grep -q 'Var(&cfg.indexMaxSize' cmd/wesh/main.go`（无 flag 注册触碰该字段——D-08 纯配置键无 flag 的机械断言；值经默认值变量直接赋值）
    - `grep -c 'func loadCustomIndex(path string, max int) (\[\]byte, error)' cmd/wesh/main.go` == 1
    - `grep -c 'CustomIndex: customIndex' cmd/wesh/main.go` == 1
    - `grep -c '共 31 个' cmd/wesh/main.go` == 1（parseArgs 头注释计数同步）
    - `git diff --exit-code go.mod go.sum` 退出 0
  </acceptance_criteria>
  <done>
    --index CLI/config 全链落地：两 TOML 键、四拒绝校验矩阵、启动读入、Options 透传；红线探针测试锁定错误行零内容；既有测试零回归。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 伺服半侧——web.WithCustomIndex 装饰（gzip 预压 + Vary 恒发）+ server.go Options/Handler() 接线 + TestCustomIndex 行为锁</name>
  <reversibility rating="one-way">D-05/D-06：整页替换语义与全通道统一是公开行为契约（README 承诺后改语义破坏已部署自定义页用户预期）；确认门 Task 1 已锁定，记录评级。</reversibility>
  <files>web/embed.go, internal/server/server.go, internal/server/customindex_test.go</files>
  <read_first>
    - web/embed.go（全文——Handler() L24-51 空路径回落/Vary/gzip 旁路形态；acceptsGzip L53-72 复用点）
    - internal/server/server.go（Options struct L340-406——CustomIndex 落位与生产直传注释分档先例（BasePath/AuthHeader/Version）；Handler() L447-535——wh 获取 L449-455 装饰插入点（bp/auth 分支之前）；New 字段直传区）
    - internal/server/sharetoken.go（sharePage L87-96 委托结构——装饰层在其上游，本文件零改动的机械依据）
    - internal/server/health_test.go 或 e2e_test.go（黑盒实例装配与 http 断言形态先例——TestCustomIndex 夹具选型）
    - internal/server/headers.go（securityHeaders CSP 现状——自定义页响应头断言依据）
    - .planning/phases/09-release-polish/09-PATTERNS.md（§9 装饰语义逐点指引；§11 测试 analog）
    - .planning/phases/09-release-polish/09-UI-SPEC.md（§Custom Index Contract 1/4/5/6/7 逐字边界）
  </read_first>
  <behavior>
    - 配置 CustomIndex 实例：GET / 与 GET /index.html → 200 byte-identity 自定义字节；GET /s/{有效 token}/ → 同一字节（D-06）；GET /s/{无效 token}/ → 委托 root 链现状（无认证模式给同一自定义页——无效 token 不改写落 FileServerFS 404 行为不复活，现状语义保持）
    - GET /style.css 等相对资源 → 404（契约语义——wesh 不伺服自定义页引用资源）
    - Accept-Encoding 显式含 gzip → Content-Encoding: gzip + 解压后 byte-identity；不含 → 明文；两态 Vary: Accept-Encoding 恒在；Content-Type text/html; charset=utf-8
    - 自定义页响应恒带既有安全头（CSP 等 securityHeaders 全套——与内建页同源）
    - 0 字节 CustomIndex → 200 空 body；base-path /wesh 组合 → GET /wesh/ 给自定义字节
    - /api/attach（无认证 404/凭据 401）与 /ws（Hello→Welcome）照旧；CustomIndex nil → 内建页逐字节现状（零值兜底）
  </behavior>
  <action>
    ① web/embed.go 追加 WithCustomIndex(h http.Handler, page []byte) http.Handler（文件尾，注释登记 D-05/D-06/D-07 与 09-UI-SPEC §4）：装饰期 gzip 预压 page 一次缓存（compress/gzip BestCompression——stdlib 零新依赖）；返回 HandlerFunc——name := strings.TrimPrefix(r.URL.Path, "/")，name == "" 视 "index.html"；name != "index.html" 委托 h.ServeHTTP（其他一切路径照旧）；命中时 Vary: Accept-Encoding 恒发 + Content-Type 按 .html 扩展名推断同款 text/html; charset=utf-8 + acceptsGzip(r.Header.Get("Accept-Encoding")) 为真发预压体（Content-Encoding: gzip）否则明文 page。注释写明「装饰层在 sharePage 委托上游——/ 与 /s/{token}/ 单点统一（D-06）；不加 Cache-Control 新头（sharePage 注释既定纪律延伸）」。
    ② internal/server/server.go：Options struct 加 CustomIndex []byte 字段（生产直传注释分档先例——09-04 D-05..D-08：main --index 启动一次读入原样透传；nil = 未配置，New 不做任何兜底改写，Handler() 按零值装配与现状逐字节一致）；Server struct 加 customIndex []byte 字段，New 直传；Handler() 在 wh 获取与错误兜底块（L449-455）之后、bp 分支之前插入 if s.customIndex != nil { wh = web.WithCustomIndex(wh, s.customIndex) }——单点装饰使凭据/无认证两分支与 registerShareRoutes 的 page 参数同获装饰态（sharetoken.go 零改动机械成立）。
    ③ internal/server/customindex_test.go（新文件，package server_test 外部包——全仓测试统一形态）：TestCustomIndex 黑盒组（夹具按 health_test.go/e2e_test.go 先例选型——Options{CustomIndex: []byte(含唯一探针串的自定义 HTML)} 起实例，http 客户端断言）：双通道给页 byte-identity（/ 与 /s/{token}/——ShareTokenRO 配置起票）；相对资源 404；gzip/Vary/Content-Type 三态；安全头在场（Content-Security-Policy 非空且与内建页响应同值）；0 字节 200 空 body；BasePath /wesh 组合；/api/attach 无认证 404 + WS 握手 Hello→Welcome 照旧；nil 兜底内建页（body 不含探针串且 200）。
    ④ gofmt/vet 清零；全量 go test -race ./... 绿（server 面既有测试零回归——TestBasePathEmptyUnchanged 等零漂移锁是 nil 兜底的旁证）。
    提交：feat(09-04): serve custom index via WithCustomIndex decorator
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && go test ./internal/server/ -race -count=1 -run 'TestCustomIndex' -v && go test -race -count=1 ./... && go vet ./... && git diff --exit-code go.mod go.sum</automated>
  </verify>
  <acceptance_criteria>
    - TestCustomIndex 全 PASS（behavior 全行：双通道/404/gzip/Vary/安全头/0 字节/base-path/认证面/nil 兜底）
    - `grep -c 'func WithCustomIndex(h http.Handler, page \[\]byte) http.Handler' web/embed.go` == 1
    - `grep -c 'WithCustomIndex' internal/server/server.go` == 1（单点装饰唯一调用）
    - `grep -c 'acceptsGzip' web/embed.go` == 2（既有 Handler 与装饰各一——零第二份解析器）
    - `grep -c 'CustomIndex \[\]byte' internal/server/server.go` ≥ 2（Options 字段 + Server 字段）
    - `git diff --exit-code internal/server/sharetoken.go` 退出 0（D-06 统一经委托上游装饰——本文件零改动机械证据）
    - `go test -race -count=1 ./...` 全绿
  </acceptance_criteria>
  <done>
    --index 伺服半侧上线：WithCustomIndex 装饰单点统一双通道；gzip/Vary/安全头与内建页同纪律；TestCustomIndex 行为锁全绿；sharetoken.go 零改动。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator 文件系统 → 启动读入 | --index 路径指向的文件是 operator 信任面（不存在/不可读/非常规/超限四闸） |
| 自定义字节 → 全体 HTTP 客户端 | 给页通道输出面（/ 与 /s/{token}/）——注入/篡改即全客户端投毒 |
| 校验错误行 → stderr/journal | 文件内容泄露面（自定义页可能含 operator 私有信息） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04a | Information Disclosure | 启动错误行回显自定义页内容字节 | high | mitigate | D-08 红线：错误行只含路径+类别+上限数值；loadCustomIndex/validateStartup 文案形态单测 + 内容探针串反断言双锁（config_test 红线先例延伸） |
| T-09-04b | Denial of Service | 误指巨大文件读入 OOM | high | mitigate | io.LimitReader(max+1) 启动读入硬顶（默认 16MiB，index-max-size 可调 ≤0 拒绝）；超限即拒不放行 |
| T-09-04c | Tampering | 自定义页被注入/模板化（wesh 侧拼接内容进用户页） | medium | mitigate | D-05 零注入零模板零校验——伺服字节与读入字节 byte-identity 行为锁（TestCustomIndex 探针串断言）；prohibition 随 must_haves surfaced |
| T-09-04d | Elevation of Privilege | --index 改变认证/门禁面（自定义页通道绕过 Basic/share token 门禁） | high | mitigate | 装饰层在既有 auth/门禁装配之内（wh 单点装饰不改变 basicAuth/sharePage 链）；TestCustomIndex 认证面不变断言（/api/attach 404/401、WS 握手照旧）；sharetoken.go 零改动机械证据 |
| T-09-04e | Information Disclosure | 自定义页引用的相对路径资源被误伺服（文件系统面扩大） | low | mitigate | 契约语义：仅 index.html 路径返回自定义字节，其余路径照旧 FileServerFS（dist 内嵌 FS）——相对资源 404 行为锁；文件系统不经 --index 暴露 |
| T-09-SC | Tampering | npm/pip/cargo 包安装供应链 | high | mitigate | N/A——零新依赖（compress/gzip stdlib），go.mod/go.sum 验收逐字节不动 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V5 Input Validation | ✓ 四拒绝校验矩阵（stat 级 + LimitReader）+ 错误行值剥离红线 + TOML 严格模式 |
| V2 Authentication | ✓ 认证面不变行为锁（/api/attach、/ws、share token、/metrics 照旧） |
| V9 Communications（安全头） | ✓ securityHeaders 最外层装配不区分内建/自定义页——CSP 现状同源断言 |
</threat_model>

<verification>
- cmd/wesh 与 internal/server 两包新增测试全 PASS；go test -race -count=1 ./... 全量绿
- 冒烟：真实二进制 --index <probe.html> --port 0 启动，curl / 返回探针字节、curl /x.css 404、curl -H 'Accept-Encoding: gzip' / 返回 gzip 体
</verification>

<success_criteria>
- operator 写一个自包含 HTML 即可整页替换 wesh 首页与分享页；非法路径/超限/不可读启动即拒且错误行零内容
- 认证面零变化、sharetoken.go 零改动、内建页零配置时逐字节现状
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **OPS-03 (unclassified, unresolved)**：probe 无法从需求文本分类边界形态——本 plan 以 09-CONTEXT D-05..D-08、09-UI-SPEC §Custom Index Contract/§UI Considerations 与 RESEARCH Validation Architecture 为验收来源（双通道 byte-identity/四拒绝/gzip/Vary/认证面不变全显式验证）。SPEC 从未对该需求分类的事实登记于此，不静默丢弃。无人工复核残留项（协议层全链断言随 09-05 phase09.mjs 闭合）。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- CLI flag `--index`（cmd/wesh/main.go——第 31 个 flag，全名无短选项）
- TOML 配置键 `index`（fileConfig.Index *string）与 `index-max-size`（fileConfig.IndexMaxSize *int，整数字节纯配置键无 flag）——fileConfig 覆盖面 27→29 键
- `config.index string` / `config.indexMaxSize int`（cmd/wesh/main.go config struct 字段）
- `func loadCustomIndex(path string, max int) ([]byte, error)`（cmd/wesh/main.go）
- `func WithCustomIndex(h http.Handler, page []byte) http.Handler`（web/embed.go——gzip 预压缓存装饰器）
- `Options.CustomIndex []byte` + `Server.customIndex []byte`（internal/server/server.go）
- `TestLoadCustomIndex`（cmd/wesh/main_test.go）+ config_test.go index/index-max-size 子测
- `TestCustomIndex`（internal/server/customindex_test.go，新文件）
- 错误文案形态 `invalid --index %q: file does not exist` / `not a regular file` / `cannot read` / `exceeds index-max-size (%d bytes)`、`invalid index-max-size: must be positive`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/09-release-polish/09-04-SUMMARY.md` when done
</output>
