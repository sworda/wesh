---
phase: 09-release-polish
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - web/uat/pw/phase09-caddy-ctl.sh
  - web/uat/pw/phase09-caddy-pw.mjs
autonomous: false
requirements:
  - OPS-10

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Caddy v2.11.4 官方静态二进制直装（GitHub release tar.gz——CODEBUDDY 禁 apt 纪律不涉服务端软件；仅实证环境安装不入仓）；最小 Caddyfile = reverse_proxy 127.0.0.1:<wesh 端口> 单指令（WS upgrade 内建自动处理、Host 默认原样透传、X-Forwarded-For 默认添加）"
    - "Linux 协议层实证（D-15 兜底通道）：经 Caddy 反代后 凭据页 GET / → 401→200；WS attach 全链（Hello→Welcome→INPUT echo→OUTPUT）——Host 默认透传使 Origin 校验天然通过（不配 nginx 的 Host 行，Pitfall 6 两平台默认语义相反的实证点）"
    - "空闲存活实证（A1 MEDIUM 假设的既定兜底）：WS 空闲 >60s（仅 wesh ping 5s 应用层流量）后连接仍存活可 echo——Caddy hijack 后无默认 WS idle 超时的行为面结论落 SUMMARY（README 关系表素材）"
    - "双机载具入库：web/uat/pw/phase09-caddy-ctl.sh（Linux 侧 setup/variant/teardown case 分派 + fuser -k 预清理 + nohup 起 wesh/caddy + ss -ltn 探活 + CADDY_UP 就绪串——phase07-a2-ctl.sh 形态先例）与 phase09-caddy-pw.mjs（Windows 侧 Playwright——scp 上传/ssh ctl/浏览器 echo 全链/idle 存活/panel hidden，phase07-a2-pw.mjs 套路复用）"
    - "红线同口径：凭据/token 只作构造材料，永不进 detail/控制台/汇总行（phase07-a2-pw.mjs:8 头注释纪律沿用）"
    - "实证记录：Caddy 版本/日期/行为面结论（Host 透传/WS upgrade 自动/无默认 idle 超时）落 SUMMARY——README Caddy 节「实证日期/版本」标注素材（09-09）"
  artifacts:
    - path: "web/uat/pw/phase09-caddy-ctl.sh"
      provides: "Linux 侧双机控制脚本（Caddy+wesh 生命周期 + CADDY_UP 就绪协议）"
      contains: "CADDY_UP"
    - path: "web/uat/pw/phase09-caddy-pw.mjs"
      provides: "Windows 侧 Playwright 双机全链断言（页面/WS echo/idle/panel）"
      contains: "phase09-caddy"
  key_links:
    - from: "web/uat/pw/phase09-caddy-pw.mjs（Windows 工作站）"
      to: "phase09-caddy-ctl.sh（Linux 开发机 /tmp/wesh-uat/）"
      via: "scp 上传 + ssh BatchMode ctl 分派（G-07-2 双机全链套路复用——nginx 换 Caddy；安全组放通端口实证先例 phase07-a2）"
      pattern: "phase09-caddy-ctl"
    - from: "Caddy reverse_proxy 默认 Host 透传"
      to: "wesh Origin 校验（SEC-04 同源检查）"
      via: "Pitfall 6：nginx 默认转发 $proxy_host 须显式 Host $http_host（G-07-2 实证），Caddy 默认原样透传——配方互抄必错，两配方按各自实证写"
      pattern: "reverse_proxy"
---

<objective>
落地 D-15 Caddy 半侧：Caddy v2.11.4 官方二进制本机部署 + Linux 协议层实证（页面/WS 全链/Host 透传/空闲存活），双机全链载具（ctl.sh + pw.mjs）入库并经确认门由用户在 Windows 工作站执行浏览器半侧。

Purpose: ROADMAP SC3 反代配方第二件——「文档即被测物」纪律下 Caddy 配方必须经实证锚点（nginx G-07-2 先例同款）；Cloudflare 是唯一「未实测」例外，Caddy 不是。
Output: Linux 协议层实证全绿 + 双机载具入库 + Windows 侧全链确认（或降级裁决）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-release-polish/09-CONTEXT.md
@.planning/phases/09-release-polish/09-RESEARCH.md
@.planning/phases/09-release-polish/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Caddy 二进制部署 + Linux 协议层实证（凭据页/WS 全链/Host 透传/空闲 65s 存活）</name>
  <files>web/uat/pw/phase09-caddy-ctl.sh</files>
  <read_first>
    - .planning/phases/09-release-polish/09-RESEARCH.md（§Code Examples Caddy 配方骨架；Pitfall 6 Host/超时两平台语义相反；A1 假设与 D-15 兜底；Environment Availability caddy 直装通道）
    - .planning/phases/09-release-polish/09-PATTERNS.md（§12 双机拓扑与断言形态——phase07-a2 先例差异点：Caddy 不配 Host 行）
    - web/uat/phase07.mjs（dialHello/echo 全链与凭据模式 spawn 先例——Linux 协议层断言复用形态）
    - README.md（:301-329 nginx 配方——Caddy 节同构落点的对照面：nginx 须 Host $http_host，Caddy 默认透传）
  </read_first>
  <action>
    ① Caddy v2.11.4 安装：curl 取 github.com/caddyserver/caddy/releases/download/v2.11.4/caddy_2.11.4_linux_amd64.tar.gz 解至 /tmp/wesh-uat/caddy/（仅实证环境，不入仓不经 apt）；/tmp/wesh-uat/caddy/caddy version 确认 2.11.4。
    ② 最小 Caddyfile（/tmp/wesh-uat/caddy/Caddyfile，不入仓——入仓形态是 README 配方与 ctl.sh 内嵌件）：{ admin off } 全局块 + http://127.0.0.1:18080 { reverse_proxy 127.0.0.1:17682 }——单指令形态（WS upgrade 内建、Host 默认透传、XFF 默认添加三事实的实证对象）。
    ③ 实证实例：go build -o /tmp/wesh-uat/wesh ./cmd/wesh；spawn wesh --bind 127.0.0.1 --port 17682 --credential <一次性测试凭据> --writable -- bash --norc --noprofile（凭据模式贴近生产形态；测试凭据只存脚本变量——红线纪律）；caddy run --config Caddyfile 后台起。
    ④ 断言组（Node 零依赖脚本形态或直接本任务内联验证命令——断言面与 phase07.mjs 同族）：a) GET http://127.0.0.1:18080/ 无凭据 → 401（challenge 穿透）；带凭据 → 200 页面；b) WS dial ws://127.0.0.1:18080/ws（Origin: http://127.0.0.1:18080）→ /api/attach 取 ticket → Hello → Welcome → INPUT 'echo MK:$((6*7))\n' → OUTPUT 含 MK:42（Host 透传 → Origin host == Host → 同源过——不配任何 Host 指令）；c) 空闲存活：WS 建链后静默 65s（无应用数据，仅 wesh ping 5s）→ 再 echo 断言 OUTPUT 可达（连接未被 Caddy 切断——A1 行为面实证：hijack 后无默认 idle 超时）；d) 清理：kill caddy/wesh 进程组，端口归零。
    ⑤ 实证记录：caddy version 行、四断言证据、A1 结论（无默认 WS idle 超时成立/不成立）落 SUMMARY（README Caddy 节「实证 2026-08-XX，Caddy v2.11.4」标注素材）。
    提交：test(09-08): verify Caddy reverse proxy at protocol layer
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && /tmp/wesh-uat/caddy/caddy version && go build -o /tmp/wesh-uat/wesh ./cmd/wesh && echo "实证脚本随任务执行（401/200/echo/idle 四断言）"</automated>
  </verify>
  <acceptance_criteria>
    - caddy version 报 2.11.4；Caddyfile 单 reverse_proxy 指令形态（无 Host 配置行——Pitfall 6 实证点）
    - 四断言全过：401→200 穿透、WS echo 全链（Origin 同源天然过）、空闲 65s 后 echo 可达、清理后端口归零
    - A1 结论与 caddy 版本/日期落 SUMMARY（README 标注素材）
  </acceptance_criteria>
  <done>
    Caddy Linux 协议层实证闭合：D-15 兜底通道兑现，README Caddy 节获得与 nginx 同级的实证锚点。
  </done>
</task>

<task type="auto">
  <name>Task 2: 双机载具入库——phase09-caddy-ctl.sh（Linux 侧生命周期）+ phase09-caddy-pw.mjs（Windows 侧 Playwright 全链断言）</name>
  <files>web/uat/pw/phase09-caddy-ctl.sh, web/uat/pw/phase09-caddy-pw.mjs</files>
  <read_first>
    - web/uat/pw/phase07-a2-ctl.sh（全文——脚本头/用法注释/case 分派/fuser -k 预清理/nohup 起实例/ss -ltn 探活/就绪串回显形态；PATTERNS §3 analog）
    - web/uat/pw/phase07-a2-pw.mjs（全文——SSH/BASE 常量、scp 上传、ctl 分派、request 层状态码断言、浏览器 echo 全链 runCmd/waitTermText、idle 存活窗、panel hidden 断言、红线头注释 L8）
    - web/uat/pw/README.md（双机拓扑与运行方式登记——新载具同步点）
    - .planning/phases/09-release-polish/09-PATTERNS.md（§12 Caddy 断言面差异——Host 默认透传/WS upgrade 内建；CADDY_UP 就绪串形态）
  </read_first>
  <action>
    ① web/uat/pw/phase09-caddy-ctl.sh（新建，phase07-a2-ctl.sh 骨架逐字复用——#!/usr/bin/env bash + set -u + 用法注释 + case 分派 + usage exit 2）：setup = fuser -k 预清理端口 → 部署 caddy 二进制与 Caddyfile（LAN 监听形态：http://0.0.0.0:10014 { reverse_proxy 127.0.0.1:17682 }）→ nohup 起 wesh（--bind 127.0.0.1 --port 17682 --credential 一次性测试凭据 --writable -- bash）与 caddy run → ss -ltn 双端口探活 → 回显 CADDY_UP 就绪串；variant 子命令按需切换 Caddyfile 变体（如有）；teardown = 双进程 kill + 临时件清理。
    ② web/uat/pw/phase09-caddy-pw.mjs（新建，phase07-a2-pw.mjs 套路复用）：SSH/BASE 常量（BASE = http://9.134.229.124:10014——与 a2 端口错开；连通性以安全组放通为前提，setup 前置检查）；scp 上传 ctl.sh → ssh setup 等 CADDY_UP → 断言组：t1 页面经反代凭据 401→200（request 层）；t2 浏览器打开页面 → 终端出现 → runCmd echo 标记 → waitTermText 全链（WS 经 Caddy 升级）；t3 idle 存活窗（>60s 无操作）后 echo 仍可达 + 状态面板 hidden（无断连面板）；t4 teardown 端口归零。红线头注释逐字沿用（凭据只作构造材料）。
    ③ web/uat/pw/README.md 运行清单补 phase09-caddy-pw.mjs 一行（双机载具登记不漂移）。
    ④ 静态自检：bash -n phase09-caddy-ctl.sh；node --check phase09-caddy-pw.mjs。
    提交：test(09-08): add dual-machine Caddy verification rig
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && bash -n web/uat/pw/phase09-caddy-ctl.sh && node --check web/uat/pw/phase09-caddy-pw.mjs && grep -c 'CADDY_UP' web/uat/pw/phase09-caddy-ctl.sh | grep -qx 1 && grep -c 'phase09-caddy-ctl' web/uat/pw/phase09-caddy-pw.mjs | grep -qx 1</automated>
  </verify>
  <acceptance_criteria>
    - bash -n / node --check 双过；CADDY_UP 就绪串唯一；pw 脚本引用 ctl 脚本名一致
    - 两文件头注释含红线声明与双机拓扑说明（phase07-a2 形态）
    - `grep -c 'proxy_set_header Host' web/uat/pw/phase09-caddy-ctl.sh` == 0（Caddy 默认透传——不复制 nginx 的 Host 行，Pitfall 6）
    - web/uat/pw/README.md 含新载具登记行
  </acceptance_criteria>
  <done>
    双机载具入库静态合格——Windows 侧执行前提就绪（Task 3 确认门）。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 确认门——Windows 工作站双机全链执行（Playwright 浏览器半侧）</name>
  <read_first>
    - web/uat/pw/README.md（双机拓扑与运行方式）
    - .planning/phases/09-release-polish/09-RESEARCH.md（Environment Availability——Windows 工作站假设可用；不可达降级路径需用户裁决）
  </read_first>
  <what-built>
    Caddy 反代配方双机全链载具（Task 1/2）：Linux 侧 Caddy+wesh 已由 ctl.sh 管理，Windows 侧 phase09-caddy-pw.mjs 将驱动真实 Chromium 经 LAN 反代断言页面/WS echo 全链/idle 存活/状态面板 hidden；Linux 协议层实证（Task 1）已先行全绿。
  </what-built>
  <how-to-verify>
    在 Windows 工作站（Playwright 宿主）执行：
    1. cd <wesh 仓库>/web/uat/pw && pnpm install（若首次——playwright 依赖按 pw/package.json 既有钉版）
    2. node phase09-caddy-pw.mjs
    3. 预期：t1-t4 全 PASS（页面 401→200 / 浏览器 echo 全链 / >60s idle 存活 /  teardown 归零）；失败项按输出定位（ctl.sh setup 日志 / ss -ltn 端口 / 安全组放通）
    若 Windows 工作站不可达：按 RESEARCH 既定降级路径裁决——Caddy 配方降级为「本机协议层实证（Task 1 已绿）+ README 标注浏览器半侧未测」（风险升级需用户明确接受，不建议）。
  </how-to-verify>
  <resume-signal>Type "approved"（双机全链已 PASS）/ "fallback"（接受降级标注）/ describe issues</resume-signal>
  <acceptance_criteria>
    - 用户明确 approved（双机全链 PASS 证据）或 fallback（降级裁决记录进 SUMMARY 与 09-09 README 标注义务）
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 → Caddy（LAN） | 反代入口面（Host/XFF 注入语义按平台默认——实证锁定） |
| Caddy → wesh（loopback） | 后端直连面（反代与源站同机信任区） |
| ctl/pw 脚本 → 测试输出 | 凭据红线面（一次性测试凭据也只作构造材料） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-08a | Tampering | 配方按 nginx 语义照抄（Host 行/超时行互抄致 403 或断连——Pitfall 6） | medium | mitigate | 两平台默认语义相反的实证点入 SUMMARY/README；Caddyfile 断言零 Host 配置行（验收机械检查）；idle 65s 存活实测锁超时行为面（A1 MEDIUM 兜底闭合） |
| T-09-08b | Information Disclosure | 测试凭据/token 进脚本输出或入库文件 | high | mitigate | 红线三件套同口径（构造材料不进 detail）；一次性测试凭据仅存脚本变量；ctl.sh/pw.mjs 入库前 grep 自证零真实凭据 |
| T-09-08c | Elevation of Privilege | LAN 监听形态反代暴露无凭据实例 | medium | mitigate | 实证实例一律凭据模式（--credential 贴近生产）；teardown 端口归零收口；监听形态仅存在于实证窗口期 |
| T-09-SC | Tampering | npm/pip/cargo 包安装供应链 | high | mitigate | N/A——Caddy 为 GitHub release 制品（RESEARCH Package Legitimacy Audit OK，仅实证环境非交付物）；pw 依赖为 web/uat/pw 既有钉版零新增 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V9 Communications | ✓ WS upgrade/Host/超时三要素经实证锚定——配方行为与文档（09-09）同源 |
</threat_model>

<verification>
- Task 1 Linux 协议层四断言全绿（401→200/echo/idle 65s/清理）
- Task 2 载具静态自检双过
- Task 3 用户 approved（双机全链）或 fallback 裁决记录
</verification>

<success_criteria>
- Caddy 配方获得 nginx G-07-2 同级实证锚点：Host 透传/WS 升级/无默认 idle 超时三行为面实测锁定
- README Caddy 节（09-09）可写「实证 <日期>，Caddy v2.11.4」；或经裁决降级标注浏览器半侧未测
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `web/uat/pw/phase09-caddy-ctl.sh`（新文件——setup/variant/teardown case 分派 + CADDY_UP 就绪串 + LAN 监听 Caddyfile 内嵌件）
- `web/uat/pw/phase09-caddy-pw.mjs`（新文件——t1-t4 断言组：页面 401→200 / 浏览器 echo 全链 / idle 存活 / teardown）
- web/uat/pw/README.md 载具登记行
- Caddy 实证数据（版本/日期/A1 结论——SUMMARY 承载，README 素材）
- 无生产代码符号新增（Caddy 二进制仅实证环境 /tmp，不入仓）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/09-release-polish/09-08-SUMMARY.md` when done
</output>
