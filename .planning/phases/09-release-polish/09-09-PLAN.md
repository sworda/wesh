---
phase: 09-release-polish
plan: 09
type: execute
wave: 4
depends_on:
  - "09-05"
  - "09-06"
  - "09-07"
  - "09-08"
files_modified:
  - scripts/release.sh
  - README.md
autonomous: true
requirements:
  - OPS-03
  - OPS-10

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "scripts/release.sh（D-14 定稿形态，POSIX bash 入库可移植——非 fish）：set -euo pipefail + 用法注释 + 干跑/确认闸；顺序 = 前置校验（git status --porcelain 空 / 参数 v\\$X.Y.Z 形态 / tag 不存在 / 当前分支与远端同步——fetch 失败或无上游时降级为 skip note 不阻塞）→ go vet ./... && go test -race -count=1 ./... → pnpm -C web install --frozen-lockfile && time pnpm -C web build → 长 fuzz 两次独立调用（FuzzDecodeHello/FuzzDecodeFileConfig 各 10min——Pitfall 4 单包单目标；崩溃即中止，语料已落 testdata/fuzz 修复后重跑）→ go test -tags=load -count=1 -timeout=30m ./internal/server/ → 确认闸（回显将创建 tag 与最近提交）→ git tag v\\$V && git push origin v\\$V"
    - "干跑验证：脏树拒绝 / 坏 tag 形态拒绝 / 已存在 tag 拒绝 / 好树干跑打印步骤不执行——四态机械断言；bash -n 语法闸（有 shellcheck 则同跑，缺席不阻塞）"
    - "README 发布节（新增）：四平台产物命名族 wesh_v1.0.0_linux_amd64.tar.gz 族（含 wesh+LICENSE+README.md 三件套）+ checksums.txt 验证指引（sha256sum -c）+ 发布流程 = scripts/release.sh 发布之前跑一次（tag push 触发 release.yml 显式编排 pnpm→goreleaser）；不发布容器镜像（D-16）与仅 checksums.txt（D-02）两裁决写明"
    - "README --index 节（新增）：整页替换语义（ttyd -i 同款）+ README-1 承诺语（自定义页完全替代内建终端页——终端功能须自行实现：POST /api/attach 换 ticket + wesh.v1 WS 协议回连，否则根路径与分享链接失去终端功能）+ README-2 承诺语（须为自包含单 HTML——相对路径资源 404；CSP 允许内联脚本与同源 WS，阻断外部源 CDN/webfont，与内建页同约束）+ 16MiB 默认上限与 index-max-size 纯 TOML 配置键（无对应 CLI flag——P7 D-03 纪律的明示例外写明防蔓延）+ 改文件需重启生效"
    - "README flag 表加 --index 行；配置文件节 27 键表述更正为 29 键（27 flag 同名 + command + index-max-size 纯配置键）"
    - "README 部署节扩充：Caddy 配方（reverse_proxy 单指令 + Host 默认透传与 nginx 相反的对照说明 + 实证日期/版本标注 + idle 关系行）+ Cloudflare 配方（按官方文档写 + 显著「未实测」标注——D-15 诚实分级唯一例外；WS 默认开启/空闲 ~100s 社区共识 × ping 5s 关系表「默认即安全」/TLS Full (strict) 建议//s/ token CF 语境脱敏延伸）+ Docker 节（Dockerfile 引用 + 构建前置 CGO_ENABLED=0 +「本镜像不含任何可执行命令——-- 后命令须来自 bind-mount 或 FROM 派生自建」承诺语 + --socket 容器内需配合 volume + 不发布镜像）+ systemd 节（deploy/wesh.service 引用 + Restart=on-failure 与 255 交互说明 + EnvironmentFile 600 通道——与 README:372 既有 SuccessExitStatus 注记口径一致）"
    - "README 标定表（D-13）：表头去「Phase 9 标定」挂账语；初值列 → 标定值/验证结论；覆盖挂账清单全量（outbox 512KiB/水位 50%/input 32KiB/s÷64KiB/input queue 256KiB/max-clients 32/resize 50ms/attachGrace 500ms/pong 10s/hello 5s/EXIT 2s/stop-timeout 0/exit-when-empty 宽限）——负载敏感项给 09-06 实测结论（数据摘要随表），时序项给「行为测试已锁 + 一阶依据复核」结论；「初值为一阶推算…Phase 9 回填」句改写为已验证表述"
    - "证伪分支（D-12，仅当 09-06 数据证伪才执行）：改 clients.go/server.go 常量默认值 + 注释同步改写为实测依据（数据出处+日期——「注释自带依据」传统）；绝不动可配性/flag 面（P2 D-10 纪律延伸）；验证成立分支零 Go 代码改动"
  artifacts:
    - path: "scripts/release.sh"
      provides: "D-14 发布脚本（前置校验 → 六段式同口径测试 → pnpm build → 长 fuzz ×2 → load 矩阵 → 确认闸 → tag+push）"
      contains: "fuzztime=10m"
    - path: "README.md"
      provides: "发布节 / --index 节 / 部署节 Caddy+CF+Docker+systemd / 标定表回填 / flag 表与配置节更正"
      contains: "--index"
  key_links:
    - from: "scripts/release.sh 尾部 git tag + push"
      to: ".github/workflows/release.yml（09-01）"
      via: "D-01/D-14：tag push 是唯一发布触发——脚本即发布文档的可执行形态，README 发布节描述流程、脚本承载流程，两者同源不漂移"
      pattern: "git push origin"
    - from: "README 标定表验证结论"
      to: "09-06 SUMMARY 负载矩阵实测数据"
      via: "D-13 回填纪律：验证为主（现值成立写结论）/证伪才改（常量默认值+注释实测依据）——数据出处可溯源"
      pattern: "标定"
---

<objective>
落地 D-13/D-14 与 ROADMAP SC3 文档面：scripts/release.sh 发布脚本（发布前跑一次——长 fuzz → 负载矩阵 → 打 tag push）+ README 全量更新（发布节/--index 节/部署节 Caddy+CF+Docker+systemd/标定表回填/flag 表与配置节更正）。

Purpose: 「发布脚本即发布文档的可执行形态」——文档描述流程、脚本承载流程，两者同源不漂移；部署配方经 09-07/09-08 实证后落文（文档即被测物，CF 唯一例外标注未实测）。
Output: release.sh + 干跑四态证据 + README 五节更新。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-release-polish/09-CONTEXT.md
@.planning/phases/09-release-polish/09-RESEARCH.md
@.planning/phases/09-release-polish/09-PATTERNS.md
@.planning/phases/09-release-polish/09-01-SUMMARY.md
@.planning/phases/09-release-polish/09-04-SUMMARY.md
@.planning/phases/09-release-polish/09-05-SUMMARY.md
@.planning/phases/09-release-polish/09-06-SUMMARY.md
@.planning/phases/09-release-polish/09-07-SUMMARY.md
@.planning/phases/09-release-polish/09-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/release.sh——D-14 发布脚本（前置校验/六段式同口径/长 fuzz×2/load 矩阵/确认闸/tag+push）+ 干跑四态验证</name>
  <files>scripts/release.sh</files>
  <read_first>
    - .planning/phases/09-release-polish/09-RESEARCH.md（Pattern 5 发布脚本定稿形态——顺序与确认闸；Pitfall 4 fuzz 两次调用；§Code Examples 骨架）
    - .planning/phases/09-release-polish/09-PATTERNS.md（§3 脚本头与结构纪律——phase07-a2-ctl.sh analog + set -euo pipefail 加固 + POSIX bash 非 fish）
    - web/uat/pw/phase07-a2-ctl.sh（脚本头/用法注释/case 分派/usage exit 2 形态先例）
    - .github/workflows/release.yml（09-01 落地终态——脚本尾部 tag push 的对接面）
  </read_first>
  <action>
    ① scripts/release.sh（新建，#!/usr/bin/env bash + set -euo pipefail——入库可移植 artifact 用 POSIX bash 非 fish，CODEBUDDY 纪律）：顶部用法注释（usage: release.sh [--dry-run] vX.Y.Z——「发布之前跑一次即可」D-14 原话登记）；函数化分段：
    a) 前置校验 preflight()——闸序钉死（干跑四态机械断言按文案分流的前提：形态/已存在两闸先于脏树闸，脚本未入库的验证窗口期各态可独立触发）：① $1 须匹配 ^v[0-9]+\.[0-9]+\.[0-9]+$（不符即拒，文案钉死 `release: invalid tag format (want vX.Y.Z)`）→ ② git rev-parse -q --verify refs/tags/$1 存在即拒（tag 不可重复，文案 `release: tag already exists`）→ ③ git status --porcelain 非空即拒（脏树禁发布，文案 `release: working tree not clean`）→ ④ git fetch --dry-run + git status -sb 比对 @{u}（落后/分叉即拒——发布物须与远端同源；ahead 放行——发布物本就是本地新增提交/tag；**降级钉死：fetch 失败〔无网络/远端不可达〕或无上游〔@{u} 不存在〕时本闸降级为跳过并打印 note——文案 `release: upstream check skipped (no network or upstream)`，同源保证无法判定时不伪造失败、不阻塞干跑与后续闸**）；
    b) 六段式同口径测试——go vet ./... && go test -race -count=1 ./...；
    c) 前端固化——pnpm -C web install --frozen-lockfile && time pnpm -C web build（dist 新鲜，embed 链本地验证）；
    d) 长 fuzz——go test -fuzz=FuzzDecodeHello -fuzztime=10m ./internal/proto/ 与 go test -fuzz=FuzzDecodeFileConfig -fuzztime=10m ./cmd/wesh/ 两次独立调用（Pitfall 4；崩溃即中止——语料已落 testdata/fuzz，修复后重跑本脚本）；
    e) 负载矩阵——go test -tags=load -count=1 -timeout=30m ./internal/server/；
    f) 确认闸 confirm()——回显将创建的 tag、最近 5 条提交、release.yml 触发说明，read -r 应答非 yes 即中止；
    g) git tag "$V" && git push origin "$V"（release.yml 接管）；--dry-run 形态：只跑 a) 并打印 b)-g) 步骤清单不执行。
    ② 干跑四态机械验证（各态输出落 /tmp 捕获文件后断言钉死文案——拒绝文案形态见 ① a)；临时件随各态断言后即清）：a) 坏形态——./scripts/release.sh --dry-run 1.0.0（缺 v 前缀）→ 闸①拒，输出含 invalid tag format（闸序最先，树状态无关）；b) 已存在——临时 git tag v9.9.9（严格形态——锚定正则下 -dryrun 类后缀名会先撞闸①，不可用）→ --dry-run v9.9.9 → 闸②拒，输出含 already exists（验毕 git tag -d 清理）；c) 脏树——仓库内 touch .release-probe（探针必须落仓库内才触发 porcelain 非空——/tmp 探针无法触发脏树拒绝）→ --dry-run v0.0.0 → 闸③拒，输出含 working tree not clean（验毕 rm 探针）；d) 好树——脚本本体未跟踪即脏树的验证窗口期形态：mv scripts/release.sh /tmp/wesh-release-probe.sh 后以 /tmp 副本对仓库跑 --dry-run v0.0.0 → 四闸全过（gate④ 在无网络/无上游环境按降级钉死打印 skip note——不视为失败、不阻塞）+ b)-g) 步骤清单打印（含 git tag 行）+ 零副作用（无 tag 创建、树仍干净），验毕 mv 回（verify 链按此序编排并保证 mv 回）。bash -n 语法闸；command -v shellcheck 存在则 shellcheck -S warning 同跑（缺席不阻塞——登记 SUMMARY）。
    ③ chmod +x scripts/release.sh。
    提交：feat(09-09): add release script integrating pre-release gates
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && bash -n scripts/release.sh && test -x scripts/release.sh && rm -f /tmp/wesh-dr-b.out /tmp/wesh-dr-e.out /tmp/wesh-dr-d.out /tmp/wesh-dr-c.out && ./scripts/release.sh --dry-run 1.0.0 > /tmp/wesh-dr-b.out 2>&1; grep -q 'invalid tag format' /tmp/wesh-dr-b.out && rm -f /tmp/wesh-dr-b.out && git tag v9.9.9 && ./scripts/release.sh --dry-run v9.9.9 > /tmp/wesh-dr-e.out 2>&1; git tag -d v9.9.9 > /dev/null 2>&1; grep -q 'already exists' /tmp/wesh-dr-e.out && rm -f /tmp/wesh-dr-e.out && touch .release-probe && ./scripts/release.sh --dry-run v0.0.0 > /tmp/wesh-dr-d.out 2>&1; rm -f .release-probe; grep -q 'working tree not clean' /tmp/wesh-dr-d.out && rm -f /tmp/wesh-dr-d.out && mv scripts/release.sh /tmp/wesh-release-probe.sh && /tmp/wesh-release-probe.sh --dry-run v0.0.0 > /tmp/wesh-dr-c.out 2>&1; test -f /tmp/wesh-release-probe.sh && mv /tmp/wesh-release-probe.sh scripts/release.sh && grep -q 'git tag' /tmp/wesh-dr-c.out && rm -f /tmp/wesh-dr-c.out && git status --porcelain | grep -v '^?? scripts' | grep -c . | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - bash -n 过、可执行位在；干跑四态按钉死文案分流断言（invalid tag format / already exists / working tree not clean / 好树步骤清单含 git tag 行）；验证后仓库零残留（临时 tag v9.9.9 已删、.release-probe 与 /tmp 捕获文件已清、脚本已 mv 回）
    - `grep -c 'set -euo pipefail' scripts/release.sh` == 1
    - `grep -c 'fuzztime=10m' scripts/release.sh` == 2（两目标两次独立调用——Pitfall 4）
    - `grep -c '\-tags=load' scripts/release.sh` == 1
    - `grep -c 'git push origin' scripts/release.sh` == 1 且 push 仅在确认闸函数之后（行号序断言）
    - 脚本为 POSIX bash（首行 shebang 为 `#!/usr/bin/env bash`；无 fish 特有语法——POSIX 形态以 bash -n 与干跑行为自证）
  </acceptance_criteria>
  <done>
    release.sh 入库：D-14「发布之前跑一次」全部操作单脚本整合；干跑四态机械验证；真实首跑 = v1.0.0 发布（09-10 确认门用户裁决）。
  </done>
</task>

<task type="auto">
  <name>Task 2: README 全量更新——发布节/--index 节/flag 表与配置节更正/部署节 Caddy+CF+Docker+systemd/标定表 D-13 回填（证伪分支按需）</name>
  <files>README.md</files>
  <read_first>
    - README.md（全文现状——flag 表 L43-75 / 配置文件节 L254-277（27 键表述 L258）/ nginx 配方节 L301-329（Caddy/CF 节同构落点）/ systemd 配方 L287-295 与 255 注记 L372 / 标定表 L231-243 / 构建节 L87-95）
    - .planning/phases/09-release-polish/09-CONTEXT.md（D-08 README 写明例外义务；D-13 标定落点；D-15 配方验证深度分级）
    - .planning/phases/09-release-polish/09-UI-SPEC.md（§Copywriting README-1/README-2 承诺语逐字）
    - .planning/phases/09-release-polish/09-01-SUMMARY.md（产物命名族/校验和/编排形态——发布节素材）
    - .planning/phases/09-release-polish/09-04-SUMMARY.md + 09-05-SUMMARY.md（--index 行为与错误形态——--index 节素材）
    - .planning/phases/09-release-polish/09-06-SUMMARY.md（负载矩阵数据表与验证结论——标定表回填数据源；证伪对象/建议值如有）
    - .planning/phases/09-release-polish/09-07-SUMMARY.md（Docker/systemd 实测证据——两节素材）
    - .planning/phases/09-release-polish/09-08-SUMMARY.md（Caddy 实证版本/日期/A1 结论或 fallback 裁决——Caddy 节标注素材）
  </read_first>
  <action>
    ① 发布节（新增，「构建」节后）：产物命名族 wesh_v1.0.0_linux_amd64.tar.gz 族四平台（linux/darwin × amd64/arm64——Windows 终局不做）+ 每包含 wesh/LICENSE/README.md + checksums.txt 验证指引（下载后 sha256sum -c checksums.txt）+ 发布流程段（scripts/release.sh 发布之前跑一次——长 fuzz 2×10min → 负载矩阵 → 确认闸 → tag push 触发 release.yml；release.yml 显式编排 pnpm build 先于 goreleaser，CGO_ENABLED=0 仅发布构建）+ 两裁决写明（仅 checksums.txt 无签名/SBOM——D-02；不发布容器镜像——D-16）。
    ② --index 节（新增，「部署与配置」节内）：整页替换语义 + README-1/README-2 承诺语（09-UI-SPEC §Copywriting 逐字）+ 16MiB 默认上限与 index-max-size 纯 TOML 配置键例外写明（D-08——「如果设上限就必须可配置」的调和形态：配置文件承载低频调参，此例外不蔓延）+ 改文件需重启生效 + 四拒绝行为（不存在/不可读/非常规/超限 exit 2，错误行不含内容）。
    ③ flag 表加 --index 行（默认值 —，说明 = 自定义首页 HTML 整页替换，分享链接同效，重启生效）；配置文件节 L258「26 个长期运行 flag 同名键 + command exec 数组，共 27 键」更正为「27 个长期运行 flag 同名键 + command exec 数组 + index-max-size 纯配置键，共 29 键」并补 index/index-max-size 两键示例与例外说明。
    ④ 部署节扩充：Caddy 配方（代码块 = reverse_proxy 单指令；说明段——Host 默认原样透传与 nginx 相反（不配 Host 行，nginx 须 proxy_set_header Host $http_host 对照）、WS upgrade 内建、XFF 默认添加、实证标注「已全链实证 <日期>，Caddy v2.11.4」（09-08 SUMMARY 数据源；fallback 裁决则标注「本机协议层实证 + 浏览器半侧未测」）；idle 关系行——hijack 后无默认 WS idle 超时（实测），wesh ping 5s ≪ 任何中间盒阈值）；Cloudflare 配方（显著「未实测」标注——D-15 唯一例外；要点：DNS 橙云代理、WebSockets 默认开启、空闲 ~100s 社区共识（官方文档标注未直取）× ping 5s 关系表「默认即安全」、TLS 边缘终止 + 源站 Full (strict)（--tls-cert/--tls-key）或 loopback-only、Host 默认保持、/s/{token}/ 对 CF 明文可见的脱敏语境延伸）；Docker 节（Dockerfile 引用 + 构建前置 CGO_ENABLED=0 go build + docker build + 「本镜像不含任何可执行命令——-- 后命令须来自 bind-mount（-v /bin:/bin:ro -v /lib:/lib:ro -v /lib64:/lib64:ro）或 FROM 派生自建」承诺语 + tini PID 1 收割说明（不加 -g——wesh 自管 stop-signal 进程组序列）+ --socket 容器内需配合 volume + 不发布镜像用户自建）；systemd 节（deploy/wesh.service 引用 + 全配说明（Restart=on-failure/RestartSec=2/TimeoutStopSec=15s/LimitNOFILE=65536/EnvironmentFile 600）+ 255 交互段——on-failure 下自主终结会重启属服务形态期望、systemctl stop 永不触发重启、期望「会话完即停」改 Restart=no——与既有 SuccessExitStatus 注记口径调和）。
    ⑤ 标定表（D-13，L231-243）：表头「默认参数与 Phase 9 标定」→「默认参数与标定」；导引句「下列初值为一阶推算的合理值（非负载实测），Phase 9 负载标定后回填」改写为已验证表述（2026-08/09 负载矩阵实测——go test -tags=load）；表格扩至挂账清单全量（补 input queue 256KiB/attachGrace 500ms/pong 10s/hello 5s/EXIT 2s/stop-timeout 0/exit-when-empty 宽限行）——负载敏感项（outbox/水位/input rate+burst/input queue/max-clients/attachGrace）结论列写实测验证成立（数据摘要随表：格子形状/kicks/Alloc/门频率量级，09-06 SUMMARY 数据源），时序项（防抖/pong/hello/EXIT/stop-timeout/宽限）写「行为测试已锁 + 一阶依据复核成立」；方法论段保留并注明已兑现。
    ⑥ 证伪分支（仅当 09-06 SUMMARY 记录证伪）：按 D-12 改对应常量默认值（clients.go/server.go——只改常量不动 flag 面）+ 注释同步改写为实测依据（数据出处+日期），随本任务同批提交并跑 go test -race -count=1 ./... 全绿；验证成立分支零 Go 改动。
    ⑦ 提交：docs(09-09): release/custom-index/deployment sections and calibration table
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && grep -c 'wesh_v1.0.0_linux_amd64.tar.gz' README.md | grep -qv '^0$' && grep -c 'index-max-size' README.md | grep -qv '^0$' && grep -c '未实测' README.md | grep -qv '^0$' && grep -c '默认参数与标定' README.md | grep -qx 1 && ! grep -q 'Phase 9 负载标定后回填' README.md && grep -c 'deploy/wesh.service' README.md | grep -qv '^0$' && grep -c '\-\-index' README.md | grep -qv '^0$' && go test -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - 发布节/--index 节/Caddy/CF/Docker/systemd/标定表五面全部落文；CF 节含「未实测」显著标注；Caddy 节含实证日期/版本（或 fallback 标注）
    - README-1/README-2 承诺语与 09-UI-SPEC §Copywriting 逐字一致（终端功能自行实现 + 自包含单 HTML 两义务）
    - 配置节 29 键表述更正；index-max-size 例外写明「无对应 CLI flag」
    - 标定表去挂账语、覆盖挂账清单全量、负载敏感项附实测结论（09-06 数据源可溯源）
    - `grep -c 'Phase 9 负载标定后回填\|Phase 9 标定' README.md` == 0（挂账语清零——表头改名后无残留）
    - 证伪分支（如触发）：常量默认值与注释实测依据同批 + go test -race 全绿；验证成立分支 git diff 零 Go 文件
    - 文档即被测物纪律：Caddy/Docker/systemd 三节每个承诺与 09-07/09-08 SUMMARY 实测证据同源（无防御性建议式表述）
  </acceptance_criteria>
  <done>
    README 五节更新落文：发布流程/脚本同源、--index 两承诺语逐字、部署配方实证分级齐全、标定表挂账清零——ROADMAP SC3 与 D-13/D-14 闭合。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release.sh → git tag/GitHub | 发布触发面（脏树/错 tag/未确认即发布即版本史污染） |
| README → 用户部署行为 | 文档承诺面（未实证配方/错误指引即部署事故——文档即被测物纪律） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-09a | Tampering | 脏树/错 tag 发布漂移（产物与仓库不一致） | high | mitigate | preflight 四闸（porcelain 空/tag 形态/tag 不存在/远端同步）+ 确认闸回显 tag 与最近提交——干跑四态机械验证 |
| T-09-09b | Information Disclosure | README/脚本泄露敏感值（凭据示例/内部地址） | medium | mitigate | 示例凭据一律占位符（alice/pw-of-alice 先例形态）；脚本零秘密（tag 操作无凭据内嵌——git 既有认证通道） |
| T-09-09c | Denial of Service | 发布脚本长 fuzz/负载矩阵失控占用 | low | mitigate | fuzztime=10m 与 timeout=30m 硬顶；崩溃即中止修复后重跑（语料入库） |
| T-09-09d | Tampering | 文档承诺与实测漂移（配方写成防御性建议） | medium | mitigate | 文档即被测物：每节标注实证日期/版本或「未实测」（D-15 分级）；验收要求与 09-07/09-08 SUMMARY 同源 |
| T-09-SC | Tampering | npm/pip/cargo 包安装供应链 | high | mitigate | N/A——脚本仅编排既有工具链（go/pnpm/git 全既有），零新包 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V10 Configuration/供应链 | ✓ 发布前四闸 + 确认闸 + 脚本/文档同源（发布物可信度的人因防线） |
</threat_model>

<verification>
- release.sh 干跑四态机械验证 + bash -n（shellcheck 有则同跑）
- README 验收 grep 组全过 + go test -race 全量绿（证伪分支时）
</verification>

<success_criteria>
- 发布前操作单脚本整合（D-14 原话「发布之前跑一次即可」）；README 覆盖 nginx/CF/Caddy 三反代 + Docker + systemd 全配方（ROADMAP SC3）
- 标定表挂账清零：每个默认参数都有验证结论与数据出处
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `scripts/release.sh`（新文件，可执行——preflight/六段式测试/pnpm build/长 fuzz×2/load 矩阵/confirm gate/git tag+push 函数段；--dry-run 形态）
- README.md 新增节：发布节、--index 节（「部署与配置」内）、Caddy 配方、Cloudflare 配方、Docker 节、systemd deploy/wesh.service 引用段
- README.md 更正：flag 表 --index 行、配置文件节 29 键表述、标定表（表头改名 + 全量挂账清单 + 验证结论）
- 证伪分支（如触发）：clients.go/server.go 常量默认值修订 + 注释实测依据（仅 09-06 数据证伪时）
- 无 Go/TS 符号新增（验证成立分支）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/09-release-polish/09-09-SUMMARY.md` when done
</output>
