---
phase: 09-release-polish
plan: 10
type: execute
wave: 5
depends_on:
  - "09-09"
files_modified: []
autonomous: false
requirements:
  - OPS-03
  - OPS-10

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "全量六段式绿：GOROOT gofmt -l 零输出（/usr/bin/gofmt 陈旧不用——01-03 登记）/ go vet ./... / go test -race -count=1 ./... / pnpm -C web install --frozen-lockfile && time pnpm -C web build / 裸 clone embed 链（git clone 至临时目录后 go build + go test 可编译——dist 真实产物入库承诺，P1 D-18）/ 启动冒烟（--port 0 启动行解析 + attach echo）"
    - "全量 UAT 回归绿：web/uat/phase02..phase09 全部 .mjs（协议族 phase02/03/04/05/06/07/08/09 + dom 族 phase04-dom/04-t1-width/05-dom/05-dims/06-dom + 08-journal + b 族 .sh 先例形态）——含 09-03 扩展的 phase06-dom D1-D13 与 09-05 新增 phase09"
    - "质量门复演绿：fuzz 短跑两调用各 60s（FuzzDecodeHello/FuzzDecodeFileConfig）+ load 矩阵全量（-tags=load -timeout=30m）+ goreleaser check + snapshot 复演（09-01 断言组重跑）"
    - "ROADMAP SC1 终态：goreleaser 产出 linux/darwin × amd64/arm64 四个全静态二进制（CGO_ENABLED=0），前端单 HTML 经 embed 内嵌，scp 到干净机器即可运行——snapshot + 干净容器双层证据"
    - "ROADMAP SC2 终态：--index 自定义首页可配置生效（09-04/09-05 双层证据）；负载/模糊测试通过（高吞吐 fan-out、慢客户端矩阵、百万小帧/空帧既有 limits 面、高频建销无 defunct），标定数据已回填 README（09-09）"
    - "ROADMAP SC3 终态：部署文档覆盖 nginx（既有实证）/Cloudflare（未实测标注）/Caddy（实证标注）反代配方（含空闲超时与 ping 间隔关系）、Docker（tini/PID 1 收割实测）、systemd unit 模板（Restart/LimitNOFILE/EnvironmentFile 600 实机实测）"
    - "07 三项 UI WARNING 清零（D-18）：1001 hint 条件化 + #status role=\"alert\" + pre-onopen 1001 分派——v1.0.0 发布物无已知 WARNING 挂账"
  artifacts: []
  key_links:
    - from: "本 plan 全量验证"
      to: "09-01..09-09 全部产出"
      via: "收口纪律：发布闸前最后一道全绿证据链——六段式/UAT/fuzz/load/snapshot 五面同源复核"
      pattern: "go test"
---

<objective>
Phase 9 收口：全量六段式 + 全量 UAT 回归 + fuzz/load/snapshot 质量门复演，随后发布闸（checkpoint:decision）由用户裁决立即跑 scripts/release.sh 发布 v1.0.0 或稍后自行发布。

Purpose: 发布质量门语义——v1.0.0 送出门前最后一次全绿证据链复核；发布是 one-way 公开动作（tag push 触发公开 GitHub Release），必须用户明确裁决。
Output: 全绿证据链 + 发布裁决记录（+ 可选 v1.0.0 实发与产物核验）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-release-polish/09-CONTEXT.md
@.planning/phases/09-release-polish/09-VALIDATION.md
@.planning/phases/09-release-polish/09-01-SUMMARY.md
@.planning/phases/09-release-polish/09-05-SUMMARY.md
@.planning/phases/09-release-polish/09-06-SUMMARY.md
@.planning/phases/09-release-polish/09-09-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 全量收口验证——六段式 + 全量 UAT 回归 + fuzz 短跑 + load 矩阵 + goreleaser snapshot 复演</name>
  <files>无（纯验证——只读与临时目录操作；临时件全部清理）</files>
  <read_first>
    - .planning/phases/09-release-polish/09-VALIDATION.md（Sampling Rate 与 Phase gate 口径）
    - .planning/STATE.md（六段式段 1 GOROOT gofmt 先例登记——/usr/bin/gofmt 陈旧须用 GOROOT 版本）
    - web/uat/（phase*.mjs 全清单——回归枚举依据）
    - .planning/phases/09-release-polish/09-01-SUMMARY.md（snapshot 复演命令与断言组）
    - .planning/phases/09-release-polish/09-06-SUMMARY.md（load 矩阵运行命令）
  </read_first>
  <action>
    ① 六段式：a) "$(go env GOROOT)/bin/gofmt" -l . 零输出（既有漂移若有按先例独立 style 提交清零——02-06/03-06/05-09/08-05 先例；b) go vet ./...；c) go test -race -count=1 ./...；d) pnpm -C web install --frozen-lockfile && time pnpm -C web build；e) 裸 clone embed 链——git clone 本仓库至 /tmp/wesh-fresh-clone，其内 go build ./... && go test ./... 编译通过（dist 真实产物入库承诺实证；验毕 rm -rf）；f) 启动冒烟——go build -o /tmp/wesh-uat/wesh ./cmd/wesh 后 --bind 127.0.0.1 --port 0 启动行解析 + WS attach echo 断言。
    ② 全量 UAT 回归：按 web/uat/ 现状枚举全跑（phase02/03/04/04-dom/04-t1-width/05/05-dom/05-dims/06/06-dom/07/08/08-journal/09 各 .mjs + b 族 .sh 先例形态——二进制路径参数统一 /tmp/wesh-uat/wesh）；任一 FAIL 即停修；各脚本 assertOutputClean 自净全过。
    ③ 质量门复演：go test -fuzz=FuzzDecodeHello -fuzztime=60s ./internal/proto/ + go test -fuzz=FuzzDecodeFileConfig -fuzztime=60s ./cmd/wesh/；go test -tags=load -count=1 -timeout=30m ./internal/server/；goreleaser check && goreleaser release --snapshot --clean + 09-01 断言组重跑（四 tar/命名族/checksums/静态/干净容器）+ rm -rf dist 清理。
    ④ 证据汇总：五面结果（六段式/UAT 计数/fuzz/load/snapshot）记入 SUMMARY——发布闸呈堂证据。
    提交：无（纯验证；若有 gofmt 清零按先例独立 style 提交）
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && test -z "$("$(go env GOROOT)/bin/gofmt" -l .)" && go vet ./... && go test -race -count=1 ./... && pnpm -C web install --frozen-lockfile && time pnpm -C web build && go test -fuzz=FuzzDecodeHello -fuzztime=60s ./internal/proto/ && go test -fuzz=FuzzDecodeFileConfig -fuzztime=60s ./cmd/wesh/ && goreleaser check</automated>
  </verify>
  <acceptance_criteria>
    - 六段式六面全绿（gofmt 零输出/vet/-race 全量/pnpm build/裸 clone 编译/冒烟 echo）
    - 全量 UAT 脚本零 FAIL（跳过行均有 reason 登记）
    - fuzz 2×60s PASS、load 矩阵全量 PASS、snapshot 复演断言全过且 dist 已清理
    - SUMMARY 五面证据齐备（发布闸呈堂材料）
  </acceptance_criteria>
  <done>
    发布前全绿证据链闭合——ROADMAP SC1/SC2/SC3 三准则全部有实证背书。
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: 发布闸——v1.0.0 立即发布 vs 稍后自行发布（one-way 公开动作）</name>
  <read_first>
    - .planning/phases/09-release-polish/09-CONTEXT.md（D-01/D-14——tag 触发与发布脚本裁决原话「发布之前跑一次即可」）
    - scripts/release.sh（发布流程全步——确认闸内建于脚本 f) 段）
  </read_first>
  <decision>是否立即以 scripts/release.sh v1.0.0 执行真实发布（长 fuzz 2×10min → 负载矩阵 → 确认闸 → git tag v1.0.0 && git push——触发 release.yml 构建并公开 GitHub Release）。tag push 是 one-way 公开动作：产物名/校验和/版本史自此刻公开可查，撤销只能删 release 与 tag（历史留痕）。</decision>
  <context>
    Task 1 全绿证据链已闭合；D-01/D-14 既定流程下发布本身的全部技术闸门（测试/fuzz/负载/确认闸）已内建于脚本。本门裁决的是「时机」，不是流程——流程已在 09-01/09-09 锁定。
  </context>
  <options>
    <option id="publish-now">
      <name>立即发布 v1.0.0（推荐——全绿证据新鲜）</name>
      <pros>发布链真实首证在本 phase 内闭合（release.yml 全链 + D-04 精确命名 + GitHub Release 产物核验）；milestone v1.0 以实发收尾</pros>
      <cons>公开动作——产物即刻可查（D-02 仅 checksums.txt 威胁模型已裁决接受）</cons>
    </option>
    <option id="publish-later">
      <name>稍后自行发布</name>
      <pros>用户择机跑 scripts/release.sh v1.0.0（流程不变）；phase 以能力交付收尾</pros>
      <cons>release.yml 真实全链首证留待发布时（snapshot 已证形状——RESEARCH Pitfall 12 既定取舍）</cons>
    </option>
  </options>
  <resume-signal>选择 publish-now（执行 Task 3 实发）或 publish-later（记录 deferred 收尾）</resume-signal>
  <acceptance_criteria>
    - 用户明确二选一后才进入 Task 3；选择记录进 SUMMARY
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 发布执行/收尾——publish-now 跑 release.sh v1.0.0 全流并核验 GitHub Release；publish-later 记录 deferred</name>
  <files>无（发布动作为 git/远端操作；仓库内零文件改动）</files>
  <read_first>
    - scripts/release.sh（执行参数与确认闸形态）
    - .github/workflows/release.yml（触发后的 CI 行为——产物核验点）
  </read_first>
  <action>
    publish-now 分支：① ./scripts/release.sh v1.0.0 全流执行（前置校验 → go vet+test -race → pnpm build → 长 fuzz 2×10min → load 矩阵 → 确认闸应答 yes → tag+push）；② 盯 release.yml CI（gh run watch 或 Actions 页）至绿；③ GitHub Release 产物核验：gh release view v1.0.0——产物清单 = 4× wesh_v1.0.0_<os>_<arch>.tar.gz + checksums.txt（D-04 精确命名逐字首证）；下载 checksums.txt 与 linux_amd64 包 sha256sum -c 验证 + tar 内容三件套断言 + （本机可跑）linux_amd64 产物 --version 输出 wesh 1.0.0；④ 证据（run URL/release URL/产物清单/校验和验证）落 SUMMARY。
    publish-later 分支：① SUMMARY 记录 deferred 裁决与发布操作指引（./scripts/release.sh v1.0.0 单命令——前置条件：工作树干净、与远端同步）；② 确认仓库状态干净（无临时件残留）。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && git status --porcelain | grep -c . | grep -qx 0 && (git tag -l v1.0.0 | grep -q v1.0.0 && gh release view v1.0.0 --json assets --jq '.assets[].name' | grep -c '^wesh_v1.0.0_\(linux\|darwin\)_\(amd64\|arm64\)\.tar\.gz$' | grep -qx 4 || echo 'publish-later: no tag, deferred recorded')</automated>
  </verify>
  <acceptance_criteria>
    - publish-now：release.yml CI 绿；Release 产物 = 4 tar.gz（D-04 精确命名逐字）+ checksums.txt；sha256sum -c 过；linux_amd64 --version 输出 wesh 1.0.0；证据落 SUMMARY
    - publish-later：deferred 裁决与发布指引落 SUMMARY；仓库零残留
    - 两分支公共：git status 干净；SUMMARY 含 Task 1 五面证据 + 发布裁决记录
  </acceptance_criteria>
  <done>
    Phase 9 以发布裁决闭合：v1.0.0 实发并核验（publish-now）或能力交付 + 发布指引（publish-later）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 发布闸 → 公开 GitHub Release | one-way 公开动作面（产物名/版本史公开留痕） |
| 全量验证 → 发布裁决 | 证据新鲜度面（陈旧证据发布 = 漂移风险） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-10a | Tampering | 未验证/陈旧状态被发布（验证与发布间漂移） | high | mitigate | 收口验证与发布闸同 plan 相邻执行；release.sh preflight 四闸（脏树/tag 形态/已存在/远端同步）+ 脚本内六段式同口径测试与长 fuzz/load 重跑——发布前状态自证 |
| T-09-10b | Elevation of Privilege | 发布动作越权（tag/Release 创建权） | medium | mitigate | git 既有认证通道（用户本机凭证）；release.yml permissions 仅 contents: write（09-01）；发布闸 blocking 必须用户明示裁决 |
| T-09-SC | Tampering | npm/pip/cargo 包安装供应链 | high | mitigate | N/A——纯验证与发布编排，零新包 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V10 Configuration/供应链 | ✓ 发布闸 blocking + preflight 四闸 + 产物核验（命名/校验和/版本串）三层 |
</threat_model>

<verification>
- Task 1 五面证据全绿
- Task 3 分支验收（publish-now 产物核验 / publish-later 干净收尾）
</verification>

<success_criteria>
- Phase 9 三准则（ROADMAP SC1/SC2/SC3）全部实证闭合；发布以用户裁决形态落地或就绪
- milestone v1.0 全部 44 条需求收尾（OPS-03/OPS-10 最后两条清零）
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- 无仓库内新符号（纯验证 + 发布编排）
- publish-now 分支远端产物：git tag v1.0.0 + GitHub Release v1.0.0（4× wesh_v1.0.0_<os>_<arch>.tar.gz + checksums.txt——远端制品非仓库文件，登记于此防 drift 误报）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/09-release-polish/09-10-SUMMARY.md` when done
</output>
