---
phase: 09-release-polish
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - deploy/wesh.service
autonomous: true
requirements:
  - OPS-10

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Dockerfile（D-16 定稿形态）：# syntax=docker/dockerfile:1 + FROM scratch + ARG TARGETARCH=amd64 + ADD --checksum=sha256 钉死 tini-static-\${TARGETARCH} v0.19.0（amd64 c5b0666b4cb676901f90dfcb37106783c5fe2077b04590973b885950611b30ee / arm64 eae1d3aa50c48fb23b8cbdf4e369d0910dfc538566bfd09df89a774aa84a48b9，gh api 资产核验）+ COPY wesh /wesh + EXPOSE 7681 + ENTRYPOINT [\"/tini\", \"--\", \"/wesh\"]——静态变体（Pitfall 8：动态链接版进 scratch 必败）；scratch 里零 RUN（无 shell——构建期校验全在 ADD --checksum）"
    - "本机 docker 24.0.6 构建实测（D-16 既定验证项）：docker build 成功；PID 1=tini（docker top 取证）；bind-mount 夹具（-v /bin:/bin:ro -v /lib:/lib:ro -v /lib64:/lib64:ro——Pitfall 7 形态）下孤儿孙进程被 PID 1=tini 收割无 Z 态残留，负对照（--entrypoint /wesh 直跑无 init）同夹具 Z 态在场证夹具有牙"
    - "scratch 语义契约（README 承诺语素材）：纯镜像内 wesh -- bash spawn 失败（无可执行命令）属契约行为非 bug；bind-mount 宿主二进制三件套后正常启动打印 listening 行"
    - "deploy/wesh.service（D-17 全配）：[Unit] After=/Wants=network-online.target；[Service] Type=simple + EnvironmentFile=-/etc/wesh/credentials（- 前缀缺席不拒，chmod 600 通道）+ ExecStart=/usr/local/bin/wesh --config /etc/wesh/wesh.toml + Restart=on-failure + RestartSec=2 + TimeoutStopSec=15s（1001 广播 + stall Close 5s+5s 上界余量）+ LimitNOFILE=65536 + KillSignal 默认 SIGTERM（07-05 Shutdown 挂钩）；255 交互与 always/on-failure 选型理由注释（Pitfall 10）；不做 ProtectHome/ProtectSystem 默认加固的说明注释（wesh 本职 spawn 用户 shell）"
    - "实机 systemctl 通道最小实测（D-17，08-05 同通道）：systemd-analyze verify 过；install（drop-in override 指向测试二进制 loopback 固定端口）→ start → /healthz 200 → 直接 SIGTERM 杀进程（exit 255）→ on-failure 经 RestartSec 复活 200 → systemctl stop → 停后不复活（systemd 发起的停止永不触发 Restart=）；stop 窗口 /healthz 503 draining（08-05 观测形态）"
    - "镜像不发布（D-16）：仓库与 CI 零 docker push/ghcr 步骤——scp 哲学一致，镜像用户自建"
  artifacts:
    - path: "Dockerfile"
      provides: "scratch + tini PID 1 收割的参考镜像（用户自建形态）"
      contains: "tini-static"
    - path: ".dockerignore"
      provides: "构建上下文排除（node_modules/.git/.planning/dist 等——上下文体积与产物纯净）"
      contains: "node_modules"
    - path: "deploy/wesh.service"
      provides: "systemd unit 模板（Restart/LimitNOFILE/EnvironmentFile 600 全配 + 255 交互注释）"
      contains: "Restart=on-failure"
  key_links:
    - from: "Dockerfile ENTRYPOINT tini → /wesh"
      to: "wesh 自管 stop-signal 进程组序列（07-04 D-22）"
      via: "tini 默认只向直接子进程转发信号——正确形态：不加 -g（进程组信号由 wesh stop-signal 序列承担，-g 会双重信号）；孤儿孙进程由 PID 1=tini 收割（PITFALLS C8）"
      pattern: "ENTRYPOINT"
    - from: "deploy/wesh.service Restart=on-failure"
      to: "wesh 退出码 255 语义（06-02 OQ1 + 07-05 优雅下线）"
      via: "Pitfall 10：优雅关停/会话终结恒 255——on-failure 分类非零为 failure 故自主终结会重启（服务形态期望）；systemctl stop 永不触发重启（unit 注释写明选型理由）"
      pattern: "Restart=on-failure"
---

<objective>
落地 D-16/D-17：Dockerfile（scratch + tini sha256 钉死 + ADD --checksum）与 deploy/wesh.service 入库，本机 docker 构建实测（PID 1 收割/僵尸残留行为验证）与实机 systemctl 最小实测（Restart 语义/healthz/draining 观测）。

Purpose: ROADMAP SC3 部署文档两件的「文档即被测物」实证半侧——Docker（tini/PID 1 收割）与 systemd（Restart/LimitNOFILE/EnvironmentFile 600）承诺先经本机实测再进 README（09-09）。
Output: Dockerfile + .dockerignore + deploy/wesh.service 入库 + 两通道实测证据（SUMMARY）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-release-polish/09-CONTEXT.md
@.planning/phases/09-release-polish/09-RESEARCH.md
@.planning/phases/09-release-polish/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile + .dockerignore 入库 + 本机 docker 构建实测（PID 1=tini / 僵尸收割 + 负对照 / scratch 语义契约 / bind-mount 夹具）</name>
  <files>Dockerfile, .dockerignore</files>
  <read_first>
    - .planning/phases/09-release-polish/09-RESEARCH.md（§Code Examples Dockerfile 定稿逐字形态——tini sha256 双钉值/ENTRYPOINT 注释；Pitfall 7 scratch 无 shell 与 bind-mount 夹具形态；Pitfall 8 tini-static 变体；A3/A6 假设登记）
    - .planning/phases/09-release-polish/09-CONTEXT.md（D-16 逐字——不发布镜像；构建前置 CGO_ENABLED=0 形态）
    - .gitignore（既有忽略面——.dockerignore 编排参照）
  </read_first>
  <action>
    ① Dockerfile（新建，RESEARCH §Code Examples 定稿逐字形态）：首行 `# syntax=docker/dockerfile:1`（ADD --checksum 需 1.6+ frontend——A6 本机 buildx 0.11.2 实证兜底，不可用则退路 = alpine builder stage 下载+sha256sum -c，两分钟可切换）；FROM scratch；ARG TARGETARCH=amd64；ARG TINI_SHA256=<amd64 钉值>（注释同列 arm64 钉值与升级同步义务——升级 tini 即同步改）；ADD --checksum=sha256:${TINI_SHA256} https://github.com/krallin/tini/releases/download/v0.19.0/tini-static-${TARGETARCH} /tini；COPY wesh /wesh；EXPOSE 7681；ENTRYPOINT ["/tini", "--", "/wesh"]；注释登记：构建前置（CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o wesh ./cmd/wesh）、tini 默认只向直接子进程转发信号（不加 -g——wesh 自管 stop-signal 进程组序列 D-22，-g 双重信号）、镜像不含任何可执行命令（-- 后命令须来自 bind-mount 或 FROM 派生自建——README 承诺语素材）、--socket 容器内需配合 volume、arm64 构建命令（--build-arg TARGETARCH=arm64 --build-arg TINI_SHA256=<arm64 钉值>）。
    ② .dockerignore（新建）：.git / .planning / node_modules（含 web/node_modules 与 web/uat/node_modules——本机已装依赖数百 MB 不进上下文）/ web/dist / dist / *.tar.gz。**红线：绝不可忽略仓库根的 wesh 二进制**——COPY wesh /wesh 依赖构建前置产物落在上下文根（忽略即构建失败）。
    ③ 构建前置与构建：CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o wesh ./cmd/wesh（仓库根——构建上下文根）；time docker build -t wesh-test .；断言 docker image inspect wesh-test 存在；构建后 rm -f wesh 清理（git status 零残留）。
    ④ 实测四面：a) PID 1=tini——docker run -d --name wesh-t1 wesh-test -- sleep 300，docker top wesh-t1 取证（首进程 /tini、子进程 /wesh 与 sleep 在场），docker rm -f wesh-t1；b) 僵尸收割+负对照——正例：docker run -d --name wesh-t2 -v /bin:/bin:ro -v /lib:/lib:ro -v /lib64:/lib64:ro wesh-test -- bash -c 'for i in 1 2 3 4 5; do (setsid bash -c "sleep 0.3" &); done; sleep 3'（孤儿被 PID 1 收养），sleep 4 后 docker inspect -f '{{.State.Pid}}' 取宿主 PID，ps --ppid 递归后代扫描断言零 Z 态，docker rm -f；负对照：docker run --entrypoint /wesh -v 同三件套 wesh-test -- bash -c 同脚本——PID 1=wesh 无收割语义，同法扫描 Z 态在场（fail-first 纪律：夹具有牙才信正例）；c) scratch 语义契约——docker run --rm wesh-test -- bash 报 spawn 失败（契约行为，README 承诺语素材）；d) bind-mount 工作形态——docker run --rm -v 同三件套 wesh-test -- bash -c 'echo ok' 之前先验启动：docker run --rm -v 三件套 wesh-test --bind 127.0.0.1 --port 0 -- true 打印 listening 行。
    ⑤ 实测数据与截图证据记入 SUMMARY（镜像 SIZE、docker top 行、Z 态扫描结果、负对照证据）。
    提交：feat(09-07): add scratch+tini Dockerfile with local build verification
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o wesh ./cmd/wesh && time docker build -t wesh-test . && docker run --rm wesh-test --version && docker run --rm -v /bin:/bin:ro -v /lib:/lib:ro -v /lib64:/lib64:ro wesh-test --bind 127.0.0.1 --port 0 -- true 2>&1 | grep -q 'listening on' && rm -f wesh && git status --porcelain | grep -v '^?? Dockerfile$' | grep -v '^?? \.dockerignore$' | grep -c . | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - docker build 成功（ADD --checksum 校验通过——sha256 钉值不符即构建失败的本证）
    - docker top 取证 PID 1=/tini（正例）；负对照（--entrypoint /wesh）Z 态在场（夹具有牙）
    - 孤儿收割正例零 Z 态残留；scratch 纯镜像 spawn 失败（契约）；bind-mount 形态 listening 行打印
    - `grep -c 'FROM scratch' Dockerfile` == 1 且 `grep -c 'ADD --checksum=sha256' Dockerfile` == 1 且 `grep -c 'ENTRYPOINT \["/tini", "--", "/wesh"\]' Dockerfile` == 1
    - `grep -c 'tini-static' Dockerfile` == 1（静态变体——Pitfall 8）
    - 构建后仓库零残留（wesh 二进制已 rm；git status 干净）
  </acceptance_criteria>
  <done>
    Dockerfile/.dockerignore 入库；本机实测四面全取证（PID 1/收割+负对照/scratch 契约/bind-mount 工作形态）——D-16 验证项闭合。
  </done>
</task>

<task type="auto">
  <name>Task 2: deploy/wesh.service 入库 + systemd-analyze verify + 实机 systemctl 最小实测（start/healthz/255 复活/stop 不复活/draining）</name>
  <files>deploy/wesh.service</files>
  <read_first>
    - .planning/phases/09-release-polish/09-RESEARCH.md（§Code Examples wesh.service 定稿逐字形态；Pitfall 10——255 交互与 Restart 选型；A5 假设登记）
    - .planning/phases/09-release-polish/09-PATTERNS.md（§4 unit 模板在 README:287-295 先例上的扩展点）
    - README.md（:287-295 既有 systemd 配方先例 + :364-372 优雅下线 255 注记——unit 注释与文档口径一致性依据）
    - .planning/phases/08-observability/08-05-SUMMARY.md 若存在（实机 systemctl 通道先例——draining 观测手法；不存在则以 08-CONTEXT D-17 与 RESEARCH 为准）
  </read_first>
  <action>
    ① deploy/wesh.service（新建，RESEARCH 定稿逐字形态）：[Unit] Description=wesh — share terminal over web + After=network-online.target + Wants=network-online.target；[Service] Type=simple + EnvironmentFile=-/etc/wesh/credentials（注释：chmod 600，内容 WESH_CREDENTIAL=user:pass；- 前缀缺席不拒）+ ExecStart=/usr/local/bin/wesh --config /etc/wesh/wesh.toml + Restart=on-failure + RestartSec=2 + TimeoutStopSec=15s + LimitNOFILE=65536；注释三块——255 交互（优雅关停/会话终结恒退出 255：on-failure 下自主终结会重启属服务形态期望；systemctl stop 永不触发重启；期望「会话完即停」改 Restart=no）、KillSignal 默认 SIGTERM 即正确（07-05 Shutdown 1001 广播挂钩）、加固张力说明（ProtectHome/ProtectSystem=strict 会破坏 shell 工作流不做默认加固，NoNewPrivileges= 可自叠加）；[Install] WantedBy=multi-user.target。
    ② systemd-analyze verify deploy/wesh.service（语法/键名闸——警告即修）。
    ③ 实机最小实测（root/sudo 通道，08-05 同通道；无权限则 skip+reason 登记）：cp deploy/wesh.service /etc/systemd/system/wesh.service + mkdir /etc/systemd/system/wesh.service.d 落 override.conf（[Service] 段 ExecStart= 置空后改指 /tmp/wesh-uat/wesh --bind 127.0.0.1 --port 17681 -- bash，EnvironmentFile= 置空——override 语义防路径依赖）→ systemctl daemon-reload → systemctl start wesh → curl /healthz 断言 200 status ok → kill -TERM $(systemctl show -p MainPID --value wesh)（直接杀进程 exit 255）→ sleep 4（RestartSec=2 余量）断言复活且 /healthz 200（on-failure 把 255 分类为 failure——Pitfall 10 实证）→ systemctl stop wesh（停窗口轮询 /healthz 见 503 draining——08-05 观测形态）→ sleep 4 断言未复活（systemd 发起的停止永不触发 Restart=）→ 清理：systemctl disable --now、rm unit 与 drop-in 目录、daemon-reload、git status 零残留。
    ④ 实测证据（systemctl status 行、healthz 200/503 时序、复活/不复活判定）记入 SUMMARY——README systemd 节引用素材（09-09）。
    提交：feat(09-07): add systemd unit template with live systemctl verification
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && systemd-analyze verify deploy/wesh.service && grep -c 'Restart=on-failure' deploy/wesh.service | grep -qx 1 && grep -c 'LimitNOFILE=65536' deploy/wesh.service | grep -qx 1 && grep -c 'EnvironmentFile=-/etc/wesh/credentials' deploy/wesh.service | grep -qx 1</automated>
  </verify>
  <acceptance_criteria>
    - systemd-analyze verify 无错误（警告即修）
    - unit 含 D-17 全配五键（Restart=on-failure/RestartSec=2/TimeoutStopSec=15s/LimitNOFILE=65536/EnvironmentFile=- 前缀）+ 255 交互注释块
    - 实机实测五证据：start 后 /healthz 200；SIGTERM 杀进程后经 RestartSec 复活 200；systemctl stop 窗口见 503 draining；stop 后不复活；清理零残留（无权限环境则 skip+reason 登记并降级为 systemd-analyze verify 单通道——SUMMARY 明示）
    - `grep -c 'ProtectHome\|ProtectSystem' deploy/wesh.service` == 0（不做默认加固——注释说明义务不含配置行）
  </acceptance_criteria>
  <done>
    deploy/wesh.service 入库；systemd-analyze 与实机 systemctl 通道双验证（Restart 语义/healthz/draining）——D-17 验证项闭合，README 引用素材就绪。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tini 下载 → 镜像 | 供应链面（PID 1 组件投毒即容器全控） |
| unit 文件 → systemd（root） | world-readable unit 携秘密即泄漏；Restart 语义误判即服务行为意外 |
| 容器 → 宿主 | bind-mount 只读三件套是唯一宿主面（镜像本体 scratch 零内容） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07a | Tampering | tini 下载投毒/替换（供应链进 PID 1） | high | mitigate | ADD --checksum=sha256 钉死（RESEARCH gh api 资产核验钉值）；tini-static 变体（Pitfall 8）；注释登记升级同步义务 |
| T-09-07b | Information Disclosure | unit 文件携凭据（world-readable 即泄漏） | high | mitigate | EnvironmentFile=-/etc/wesh/credentials 600 通道（unit 本体零秘密——README 既定先例）；注释明示 chmod 600 |
| T-09-07c | Denial of Service | Restart 语义误判（always 复活会话终结服务 / stop 卡 90s） | medium | mitigate | Restart=on-failure + Pitfall 10 注释（255 交互选型理由）；TimeoutStopSec=15s 覆盖 1001 广播 + stall Close 5s+5s 上界；实机 systemctl 实测五证据兜底（A5 MEDIUM 假设的实证闭合） |
| T-09-07d | Elevation of Privilege | 容器内进程逃逸面（镜像内容面） | low | mitigate | scratch 零内容（无 shell/无工具——攻击者落地无落脚点）；wesh 无 TLS 出站故零 CA 证书死重（RESEARCH Alternatives 表裁决） |
| T-09-SC | Tampering | npm/pip/cargo 包安装供应链 | high | mitigate | N/A——零包安装（tini 为 GitHub release 制品经 RESEARCH Package Legitimacy Audit OK + sha256 钉死双重闸；docker/系统组件为宿主机既有） |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V10 Configuration/供应链 | ✓ tini sha256 钉死 + ADD --checksum 构建期强制 + 镜像零秘密 + unit 600 通道 |
| V5 Input Validation（部署输入面） | ✓ systemd-analyze verify + 实机五证据（语义误判在文档化前被实证拦截） |
</threat_model>

<verification>
- docker build + 四面实测（PID 1/收割+负对照/scratch 契约/bind-mount）全取证
- systemd-analyze verify + 实机 systemctl 五证据（或 skip+reason 降级登记）
- 仓库零残留（测试二进制/unit/drop-in 全清理）
</verification>

<success_criteria>
- Dockerfile 本机构建即用：PID 1=tini 收割孤儿、纯 scratch 语义契约明确、bind-mount 形态可工作
- wesh.service 模板可直接安装：Restart 语义与 255 交互经实机实证，文档（09-09）引用即实测同源
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `Dockerfile`（新文件——FROM scratch / ARG TARGETARCH / ARG TINI_SHA256 / ADD --checksum tini-static / COPY wesh / EXPOSE 7681 / ENTRYPOINT ["/tini", "--", "/wesh"]）
- `.dockerignore`（新文件——构建上下文排除集）
- `deploy/wesh.service`（新文件——[Unit]/[Service]/[Install] 全配 + 255 交互与加固张力注释块）
- tini sha256 钉值两枚（Dockerfile ARG 默认值与注释：amd64 c5b0666b…30ee / arm64 eae1d3aa…48b9）
- 无 Go/TS 符号新增
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/09-release-polish/09-07-SUMMARY.md` when done
</output>
