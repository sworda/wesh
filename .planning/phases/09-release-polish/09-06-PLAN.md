---
phase: 09-release-polish
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/server/load_test.go
autonomous: true
requirements:
  - OPS-10

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "load_test.go 首行硬纪律：`//go:build load` 是文件首行（否则常规 CI 捡起 30min 测试——RESEARCH Anti-Pattern）；package server_test 外部包形态（e2e_test.go/limits_test.go/metrics_test.go 全仓统一）；常规 go test ./... 与 -race 全量不含本文件（编译缺席证据）"
    - "矩阵形状（README §默认参数 231-243 既定方法论实例化，D-11）：客户端数 {1,4,16,32} × 输出速率 {seq 洪水/匀速滴漏/突发} × 慢链路 {stall 端/限速合法读者}——格子数与每格时长从首跑实测标定并记入 SUMMARY（Claude's Discretion 既定）"
    - "断言一（合法慢端零误踢，D-12 验收首要）：限速读者（匀速 drain ≈ 产出速率、突发抖动 < outbox 容量）全程 wesh_clients_kicked_total 不增——精确计数断言（非 ≥ 弱化）"
    - "断言二（内存上界成立）：32 端 seq 洪水下 runtime.ReadMemStats Alloc 峰值在账面量级内（32×512KiB outbox ≈ 16MiB + 共享帧；防失控上界取 4× 账面）且矩阵结束 GC 后回基线±容差；wesh_mem_alloc_bytes/wesh_outbox_depth_bytes_max 同步观测记录"
    - "断言三（信用门开闭频率可接受）：wesh_credit_gate_transitions_total 速率不震颤（afterDrain 50% 半水位迟滞——门开闭计数/格时上报 SUMMARY，连续震荡即证伪信号）"
    - "defunct 三面（Linux-only，darwin t.Skip）：高频建销循环（spawn Server + 立即退出子进程 × N 轮）后 runtime.NumGoroutine 回基线 + /proc/self/fd 计数回基线 + /proc/<child>/stat 无 Z 态"
    - "数据源单侧：/metrics 黑盒 scrape（metrics_test.go getMetrics 先例）——wesh_clients_kicked_total / wesh_outbox_depth_bytes_max|sum / wesh_input_rate_dropped_total / wesh_input_queue_dropped_total / wesh_credit_gate_transitions_total / wesh_goroutines / wesh_mem_alloc_bytes 八条已就位（Phase 8 预埋挂点，零生产代码改动）"
    - "夹具纪律：startTrackedServerWith（e2e_test.go:171）复用 + t.Cleanup(killServer) 每格独立收口；stall 端 dialHello 后绝不 Read（slowclient_test.go 头注释戒律）；客户端 Read 永不带 deadline ctx（goroutine + 缓冲 channel + select time.After 竞速形态）；loopback 吸收量 ≈ wmem 4MiB + rmem 6MiB——洪水量级 ≫10MiB"
    - "标定数据产出：每格 {客户端数, 速率形态, 慢链路, kicks, gateTransitions, outboxMax, Alloc 峰值/基线} 记录进 SUMMARY——D-13 README 标定表回填（09-09）的数据源；验证为主、证伪才改（D-12：证伪只改常量默认值，不动 flag 面）"
  artifacts:
    - path: "internal/server/load_test.go"
      provides: "//go:build load 黑盒负载矩阵（TestLoadFanoutMatrix / TestLoadLegitSlowReaderZeroKick / TestLoadMemoryBound / TestLoadDefunct 族）"
      contains: "//go:build load"
  key_links:
    - from: "internal/server/load_test.go 矩阵断言"
      to: "/metrics 八条 series + runtime 进程内观测"
      via: "服务器 in-process 装配（startTrackedServerWith——测试进程即服务端进程）使 runtime.NumGoroutine/ReadMemStats///proc/self/fd 与 /proc/<child>/stat 全部可达（D-11 黑盒选型依据）"
      pattern: "wesh_credit_gate_transitions_total"
    - from: "load 矩阵实测数据"
      to: "README 标定表（09-09 D-13 回填）"
      via: "SUMMARY 落每格数据与验证结论——验证为主（现值成立）/证伪才改（常量默认值 + 注释改写实测依据，09-09 执行）"
      pattern: "标定"
---

<objective>
落地 D-11/D-12 负载测试：新建 internal/server/load_test.go（//go:build load 手动 -tags=load 跑，不进常规 CI），按 README 既定负载矩阵方法论实例化，兑现三断言（合法慢端零误踢/内存上界/信用门频率）+ defunct 三面，产出标定数据源。

Purpose: P2/P5/P6 挂账默认参数（outbox 512KiB/水位 50%/input 32KiB/s÷64KiB/max-clients 32/attachGrace 500ms 等）的验证通道——「验证为主、证伪才改」，防止「为了标定而标定」搅乱已验证行为面。
Output: load_test.go + 全矩阵实测数据（SUMMARY 落表，09-09 回填 README）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-release-polish/09-CONTEXT.md
@.planning/phases/09-release-polish/09-RESEARCH.md
@.planning/phases/09-release-polish/09-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 负载矩阵骨架与三断言——load_test.go（build tag/夹具/矩阵格子/限速读者与 stall 注入/metrics 采集）</name>
  <files>internal/server/load_test.go</files>
  <read_first>
    - internal/server/e2e_test.go（startTrackedServerWith L171-194 装配夹具与 killServer 收口纪律 L120-134；dialHello 形态）
    - internal/server/slowclient_test.go（头注释 stall 夹具纪律 L7-13 + readUntilError L58-75 竞速形态 + seqFlood L44-49 平台分支——load 为 Linux 手动跑，darwin 分支 skip）
    - internal/server/metrics_test.go（getMetrics L62-80 黑盒 scrape 形态 + 八条负载 series 契约清单 L40-58）
    - internal/server/clients.go（L32-67 六常量标定验证对象——outbox 512KiB/input 32KiB/s÷64KiB/inputQueue 256KiB/maxClients 32/resizeDebounce 50ms/attachGrace 500ms 注释一阶依据；afterDrain cap/2 半水位）
    - README.md（:231-243 标定方法论——矩阵形状/验收标准/数据源逐字依据）
    - .planning/phases/09-release-polish/09-RESEARCH.md（Pattern 4 负载矩阵定稿 + §Code Examples 骨架；Anti-Pattern build tag 首行）
    - .planning/phases/09-release-polish/09-PATTERNS.md（§10 load_test.go 三 analog 合成指引）
  </read_first>
  <behavior>
    - TestLoadFanoutMatrix：{1,4,16,32} 端 × seq 洪水（每端活跃读）→ 全端收流一致（采样校验）、kicks==0、wesh_ws_sent_bytes_total ≥ 客户端数 × wesh_pty_output_bytes_total 放大比成立
    - TestLoadLegitSlowReaderZeroKick：滴漏产出 + 限速读者（drain ≈ 产出）+ 突发抖动（< outbox 容量）→ 全程 kicks 不增（精确 ==0）
    - TestLoadMemoryBound：32 端 seq 洪水 → Alloc 峰值 ≤ 4× 账面最坏（64MiB）且结束 GC 后回基线±50%；wesh_mem_alloc_bytes/outbox_depth_max 记录
    - TestLoadGateTransitions：突发 × 限速读者格子 → gateTransitions 计数上报（速率不震颤判定与数据记录）
    - 常规 go test ./...（无 -tags=load）编译清单不含 load_test.go（go list 证据）
  </behavior>
  <action>
    ① internal/server/load_test.go（新文件）：首行 `//go:build load`（空行后 package server_test——首行硬纪律，文件头注释登记 D-11 手动 -tags=load、D-12 验证为主、运行命令 go test -tags=load -count=1 -timeout=30m ./internal/server/）。
    ② 夹具层：startTrackedServerWith 复用（t.Cleanup(killServer) 每格独立收口）；dialHello 客户端工厂（SetReadLimit 4MiB——合并段超 Go 库默认 32KiB 触发 1009 的既有先例 05-02）；限速读者 drainLoop(rate bytes/s)（goroutine Read(context.Background()) + 令牌桶节流——Read 永不带 deadline ctx 竞速形态）；stall 注入（dialHello 后零 Read）；metrics 采集 helper（getMetrics 黑盒 + 行解析取 series 值——kicks/gateTransitions/outboxMax/memAlloc 四键）。
    ③ 矩阵四测试族按 behavior 落地：TestLoadFanoutMatrix（{1,4,16,32} × seq 洪水格——每格：N 端 dialHello 全活跃 drain → seq 洪水 ≫10MiB（loopback 吸收量纪律）→ 采样校验各端收流一致 + kicks==0 + 放大比断言 + 数据行记录）；TestLoadLegitSlowReaderZeroKick（滴漏+限速+突发—— kicks 精确 ==0）；TestLoadMemoryBound（32 端洪水——基线 Alloc → 峰值监控（100ms 采样）→ 结束 runtime.GC() 后回基线断言 + 数据行记录）；TestLoadGateTransitions（突发 × 限速——gateTransitions 前后差/格时上报）。
    ④ 每格时长与洪水量级首跑标定（原则：踢出点 ~10MiB 管道吸收 + 512KiB outbox 下洪水 ≫10MiB——05-07/05-09 实测量级参照；格子跑出过久则收格时——总时长 ≤ 30m timeout 内），终值记入 SUMMARY。
    ⑤ go test -tags=load -count=1 -run 'TestLoadFanoutMatrix|TestLoadLegitSlowReaderZeroKick|TestLoadMemoryBound|TestLoadGateTransitions' ./internal/server/ -v 实跑全绿（本任务先跑通小格——Task 2 跑全量矩阵与 defunct）。
    提交：test(09-06): add load matrix harness with three calibration assertions
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && head -1 internal/server/load_test.go | grep -qx '//go:build load' && ! go list -f '{{.TestGoFiles}}' ./internal/server/ | grep -q load_test && go test -tags=load -count=1 -timeout=20m -run 'TestLoadFanoutMatrix|TestLoadLegitSlowReaderZeroKick|TestLoadMemoryBound|TestLoadGateTransitions' ./internal/server/ -v && go test -race -count=1 ./internal/server/ && go vet -tags=load ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - 首行 `//go:build load`；go list 常规测试文件清单不含 load_test.go（CI 零捡起机械证据）
    - 四测试族全 PASS：kicks 精确 ==0（FanoutMatrix/LegitSlowReader 两族）、Alloc 峰值/基线断言、放大比断言
    - `grep -c 'wesh_clients_kicked_total\|wesh_credit_gate_transitions_total\|wesh_mem_alloc_bytes\|wesh_outbox_depth_bytes_max' internal/server/load_test.go` ≥ 4（四 series 采集点在场）
    - `git diff --exit-code internal/server/clients.go internal/server/server.go go.mod` 退出 0（零生产代码改动——数据源 Phase 8 已预埋）
    - `go test -race -count=1 ./internal/server/` 全绿（既有面零回归）
  </acceptance_criteria>
  <done>
    负载矩阵骨架与三断言落地：build tag 隔离机械锁定、四测试族小格实跑全绿、数据行记录通道就绪。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: defunct 三面 + 全矩阵实跑 + 标定数据落表（D-13 数据源）</name>
  <files>internal/server/load_test.go</files>
  <read_first>
    - internal/server/load_test.go（Task 1 落地终态——defunct 测试追加位与数据行格式）
    - internal/server/e2e_test.go（pty.Start 立即退出子进程夹具先例——true/false 类 argv）
    - .planning/phases/09-release-polish/09-RESEARCH.md（Pattern 4 defunct 三面定口径——goroutine/fd/Z 态；Linux-only darwin skip）
    - internal/server/metrics_test.go（wesh_goroutines series 形态——NumGoroutine 进程内直读同值对照）
  </read_first>
  <behavior>
    - TestLoadDefunct：基线 NumGoroutine/fd 计数 → N 轮（spawn Server + argv=["true"] 立即退出子进程 + 等待会话终结收口）→ 终态 NumGoroutine ≤ 基线+容差、/proc/self/fd 计数 ≤ 基线+容差、全部曾存子进程 /proc/<pid>/stat 无 Z 态；darwin t.Skip（load 测试本机手动跑 CI 不进）
    - 全矩阵实跑（go test -tags=load -count=1 -timeout=30m ./internal/server/ -v）全绿；每格数据行 {客户端数, 速率形态, 慢链路, kicks, gateTransitions, outboxMax, Alloc 峰值/基线} 完整
    - 数据判读：现值成立（零误踢/内存上界/门频率三断言全过）→ 验证结论记录；任一证伪 → 证伪对象常量名 + 实测证据 + 建议值记录（D-12 只改常量默认值不动 flag 面——改动执行在 09-09 README 回填同批）
  </behavior>
  <action>
    ① load_test.go 追加 TestLoadDefunct（Linux-only——runtime.GOOS != "linux" 即 t.Skip）：基线采集（runtime.NumGoroutine + /proc/self/fd 读目录计数）→ N 轮（N 首跑标定如 200——spawn Server(argv=["true"]) → 等待 exitf 收口 + 会话终结 → killServer 清理）→ 终态断言三面（NumGoroutine/fd 回基线+容差；记录的子进程 pid 列表逐一读 /proc/<pid>/stat state != 'Z'）。
    ② 全矩阵实跑：go test -tags=load -count=1 -timeout=30m ./internal/server/ -v——全族全格 PASS；失败格按 D-12 判读（证伪对象/证据/建议值）而非直接放行。
    ③ 数据落表：SUMMARY 承载每格数据行 + 三断言验证结论 + defunct 三面数值（09-09 D-13 README 标定表回填的直接数据源）；证伪时同步记录常量名/现值/建议值/实测依据（clients.go 注释改写素材）。
    提交：test(09-06): defunct three-face checks and full matrix calibration run
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && go test -tags=load -count=1 -timeout=30m ./internal/server/ -v && go test -race -count=1 ./... && go vet -tags=load ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - TestLoadDefunct PASS（goroutine/fd 基线回归 + 无 Z 态；darwin skip 形态正确）
    - 全矩阵 timeout=30m 内全 PASS；数据行每格齐备
    - `grep -c 'func TestLoad' internal/server/load_test.go` ≥ 5（四族 + defunct）
    - SUMMARY 数据表含三断言验证结论与（如有）证伪对象/建议值
    - `go test -race -count=1 ./...` 全绿
  </acceptance_criteria>
  <done>
    负载矩阵全量实测完成：三断言验证结论与 defunct 三面数据落表——D-13 README 标定表回填（09-09）数据源就绪；高频建销无 defunct 证据锁定。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| load 测试 → 常规 CI | build tag 隔离面（隔离失效 = 30min 测试进常规管线拖垮 CI） |
| 洪水/建销负载 → 开发机资源 | 手动跑的重型测试（失控即本机资源耗尽） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-06a | Denial of Service | build tag 缺失/位置错误致 load 测试进常规 CI | high | mitigate | 首行硬纪律 + go list 常规清单不含 load_test.go 机械断言（验收硬条件）；D-11 裁决手动 -tags=load |
| T-09-06b | Denial of Service | 洪水/建销负载拖垮开发机（内存/fd 耗尽） | medium | mitigate | timeout=30m 硬顶；格子独立 t.Cleanup(killServer) 收口（e2e 实测教训先例）；Alloc 峰值断言本身是失控闸 |
| T-09-06c | Tampering | 标定数据失真（时序敏感断言 flaky 致误判证伪/误信成立） | medium | mitigate | stall 夹具戒律（踢出触发前绝不 Read）；客户端 Read 永不带 deadline ctx；kicks 精确计数非 ≥ 弱化；证伪须复跑确认（-count=3 形态）才改常量 |
| T-09-SC | Tampering | npm/pip/cargo 包安装供应链 | high | mitigate | N/A——零新依赖（coder/websocket 为 go.mod 既有，D-11）；go.mod/go.sum 不动 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V5 Input Validation（资源面） | ✓ RES-01..RES-04 既有上限在负载下成立（零误踢 + 内存上界 + 门频率的实证层） |
</threat_model>

<verification>
- go test -tags=load -count=1 -timeout=30m ./internal/server/ -v 全绿（四族 + defunct）
- go test -race -count=1 ./... 常规全量绿（不含 load 文件）
</verification>

<success_criteria>
- 挂账默认参数获负载实证：合法慢端零误踢 + 内存上界成立 + 信用门频率可接受 + 高频建销无 defunct——README 标定方法论（231-243）兑现
- 标定数据落表供 09-09 D-13 回填；证伪路径（如有）带实测证据与建议值
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `internal/server/load_test.go`（新文件，`//go:build load`）——测试族 `TestLoadFanoutMatrix` / `TestLoadLegitSlowReaderZeroKick` / `TestLoadMemoryBound` / `TestLoadGateTransitions` / `TestLoadDefunct`
- 夹具函数（drainLoop 限速读者 / stall 注入 / metrics 采集 helper / 基线采集）——load_test.go 内未导出
- 无生产代码符号新增（数据源 = Phase 8 预埋 /metrics 八 series + runtime 进程内观测）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/09-release-polish/09-06-SUMMARY.md` when done
</output>
