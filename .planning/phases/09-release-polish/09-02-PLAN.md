---
phase: 09-release-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/wesh/config.go
  - cmd/wesh/fuzz_test.go
  - internal/proto/fuzz_test.go
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - OPS-10

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "FuzzDecodeHello / FuzzDecodeResize（internal/proto/fuzz_test.go，package proto_test）直挂既有导出函数零改造：任意输入不 panic；DecodeHello 成功 ⇒ Cols/Rows 恒在 [1,1000]（ClampDim 契约 proto.go:212）；DecodeResize 同不变量"
    - "FuzzDecodeFileConfig（cmd/wesh/fuzz_test.go，package main）经 decodeFileConfig reader 委托：任意输入不 panic；err 非 nil ⇒ 错误文本绝不含 FUZZ_PROBE_SECRET 探针值（值剥离红线的 fuzz 断言形态；键名回显合法不在断言面）"
    - "decodeFileConfig(path string, r io.Reader) (*fileConfig, error) 接缝行为保持：解码 + 错误分类三分支（StrictMissingError/DecodeError/兜底）+ configErr 包装逐字迁入，单写口不复制第二份；loadFileConfig 缩为 open-file + 委托 + D-07 权限警告；config_test.go 既有全部测试逐字节绿"
    - "ci.yml 新增独立 fuzz job 与 go/web 并列：两次独立调用 go test -fuzz=FuzzDecodeHello -fuzztime=60s ./internal/proto/ 与 go test -fuzz=FuzzDecodeFileConfig -fuzztime=60s ./cmd/wesh/（-fuzz 单包单目标结构性约束，RESEARCH Pitfall 4）；不加 -race；种子/崩溃语料随常规 go test ./... 零时长回归（go leg 既有行覆盖）"
    - "崩溃语料 testdata/fuzz/ 入库永久回归（D-10）——首跑发现即入库；本 plan 以 60s 短跑 + 种子语料单测化为验收，长跑 2×10min 属 09-09 发布脚本（D-14）"
    - "go.mod/go.sum 逐字节不动（Go native fuzz 零新依赖，D-09）"
  artifacts:
    - path: "internal/proto/fuzz_test.go"
      provides: "FuzzDecodeHello / FuzzDecodeResize 两 fuzz 目标 + 五类种子语料"
      contains: "FuzzDecodeHello"
    - path: "cmd/wesh/fuzz_test.go"
      provides: "FuzzDecodeFileConfig + 探针种子 + 两不变量"
      contains: "FuzzDecodeFileConfig"
    - path: "cmd/wesh/config.go"
      provides: "decodeFileConfig reader 委托接缝（loadFileConfig 缩为 open-file + 委托 + D-07 警告）"
      contains: "decodeFileConfig"
    - path: ".github/workflows/ci.yml"
      provides: "fuzz job（两调用 ×60s，与 go/web 并列）"
      contains: "fuzztime=60s"
  key_links:
    - from: "cmd/wesh/fuzz_test.go FuzzDecodeFileConfig"
      to: "cmd/wesh/config.go decodeFileConfig"
      via: "bytes-in 接缝：fuzz 需要 io.Reader 输入面——loadFileConfig(path) 是 path-in，提取委托是 D-09 落地的必要前置（错误分类/configErr 单写口不动）"
      pattern: "decodeFileConfig"
    - from: ".github/workflows/ci.yml fuzz job"
      to: "internal/proto/fuzz_test.go + cmd/wesh/fuzz_test.go"
      via: "两次独立 go test -fuzz 调用（工具链单包单目标约束——一把梭会被拒绝）；CI 短跑 1-2min 回归门（D-10）"
      pattern: "-fuzz=Fuzz"
---

<objective>
落地 D-09/D-10 fuzz 验证面：proto 帧解码（DecodeHello/DecodeResize——WS 远程输入面）与 TOML 配置解析（本地配置文件面）两族 fuzz 目标，TOML 侧先完成 decodeFileConfig reader 委托接缝重构（行为保持），ci.yml 加 60s×2 短跑回归门；种子语料自动单测化形成零时长回归。

Purpose: 覆盖率引导变异 + 自动最小化是手写变异脚本的数千行等价物（RESEARCH Don't Hand-Roll）；Go native fuzz 零新依赖，发布前长跑纳入发布脚本（D-14），本 plan 先把目标面与 CI 短跑门立起来。
Output: 两 fuzz 文件 + config.go 接缝 + ci.yml fuzz job；60s 短跑全绿证据。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-release-polish/09-CONTEXT.md
@.planning/phases/09-release-polish/09-RESEARCH.md
@.planning/phases/09-release-polish/09-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TOML fuzz——config.go decodeFileConfig reader 委托接缝（行为保持重构）+ cmd/wesh/fuzz_test.go FuzzDecodeFileConfig + 60s 冒烟</name>
  <files>cmd/wesh/config.go, cmd/wesh/fuzz_test.go</files>
  <read_first>
    - cmd/wesh/config.go（全文——loadFileConfig L97-137：解码 + 错误分类三分支 L105-126 + D-07 权限警告 L127-135；configErr L76-78 单写口；文件头值剥离红线 L23-27）
    - cmd/wesh/config_test.go（writeToml 夹具与探针断言形态——「unknown key rejected, value stripped」子测是 fuzz 不变量的直接祖先；loadFileConfig 调用面全清单）
    - .planning/phases/09-release-polish/09-RESEARCH.md（Pattern 3 fuzz 两目标 + 单接缝重构定稿；§Code Examples FuzzDecodeFileConfig 形态）
    - .planning/phases/09-release-polish/09-PATTERNS.md（§6 config.go 接缝逐字指引 + §7 fuzz_test.go analog）
  </read_first>
  <behavior>
    - 接缝后 loadFileConfig 行为逐字节保持：config_test.go 既有全部测试零改动全绿（含 unknown keys/invalid toml/cannot read/D-07 权限警告/值剥离探针子测）
    - FuzzDecodeFileConfig 种子五类（合法键 / credential 探针键 / 未知键 / 类型不符 / 非 UTF-8 二进制）随常规 go test 作为普通单测运行全 PASS
    - 60s 实跑 go test -fuzz=FuzzDecodeFileConfig -fuzztime=60s ./cmd/wesh/：PASS 且无崩溃语料产出（若产出则崩溃即修——语料落 testdata/fuzz/ 入库并作为单测回归）
    - 不变量实证：err 非 nil 时 err.Error() 不含 FUZZ_PROBE_SECRET；任意输入不 panic
  </behavior>
  <action>
    ① cmd/wesh/config.go 接缝重构（RESEARCH Pattern 3 定稿）：提取 decodeFileConfig(path string, r io.Reader) (*fileConfig, error)——承载 toml.NewDecoder(r).DisallowUnknownFields().Decode(&decoded) + 错误分类三分支（StrictMissingError 提取键名清单 / DecodeError 提取 Key()+Position() 组 detail / 兜底 cannot parse）+ configErr 包装，逐字迁自 loadFileConfig L103-126（单写口不复制第二份；path 参数仅供 configErr 错误包装）；loadFileConfig 缩为 os.Open（cannot read 分支原位保持）+ defer Close + 委托 decodeFileConfig(path, f) + D-07 权限警告块原位（decoded.Credential != nil 且 perm 非 600/400 → warn——Stat 仍按 path，不迁移进委托）。签名注释登记「fuzz 接缝（D-09）：reader 委托使 bytes-in 可测；错误分类与红线逻辑唯一副本」。
    ② cmd/wesh/fuzz_test.go（新文件，package main——调未导出 decodeFileConfig 必须同包）：FuzzDecodeFileConfig 种子五类——"port = 7681\nbind = \"127.0.0.1\"\n"（合法）、"credential = [\"FUZZ_PROBE_SECRET:x\"]\n"（值剥离红线探针）、"unknown-key = 1\n"（严格模式拒绝面）、"port = \"not-a-number\"\n"（类型不符面）、[]byte{0xff, 0xfe, 0x00}（非 UTF-8/二进制）；f.Fuzz 体：_, err := decodeFileConfig("fuzz.toml", bytes.NewReader(data))；err == nil 即 return；strings.Contains(err.Error(), "FUZZ_PROBE_SECRET") 即 t.Fatalf（值剥离红线 fuzz 断言形态——键名回显合法不在断言面，只断言值探针）。文件头注释登记 D-09/D-10 与两不变量。
    ③ 冒烟：go test -run FuzzDecodeFileConfig ./cmd/wesh/（种子单测化全 PASS）→ go test -fuzz=FuzzDecodeFileConfig -fuzztime=60s ./cmd/wesh/（PASS 无崩溃；产出语料则修复后语料落 cmd/wesh/testdata/fuzz/ 入库）。60s 冒烟产物 $GOCACHE/fuzz 不进仓。
    提交：feat(09-02): add TOML config fuzz target via decodeFileConfig reader seam
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && go test -count=1 ./cmd/wesh/ && go test -fuzz=FuzzDecodeFileConfig -fuzztime=60s ./cmd/wesh/ && go vet ./cmd/wesh/ && git diff --exit-code go.mod go.sum</automated>
  </verify>
  <acceptance_criteria>
    - config_test.go 既有全部测试零改动全绿（接缝行为保持证据）
    - FuzzDecodeFileConfig 种子五类单测化 PASS + 60s 实跑 PASS
    - `grep -c 'func decodeFileConfig(path string, r io.Reader) (\*fileConfig, error)' cmd/wesh/config.go` == 1
    - `grep -c 'DisallowUnknownFields' cmd/wesh/config.go` == 1（严格模式调用单写口——委托内唯一副本）
    - `grep -c 'configErr(' cmd/wesh/config.go` ≥ 4（cannot read + unknown keys + invalid toml + cannot parse 四分支不少）
    - `git diff --exit-code go.mod go.sum` 退出 0（零新依赖）
  </acceptance_criteria>
  <done>
    decodeFileConfig 接缝落地且行为保持；FuzzDecodeFileConfig 60s 全绿；值剥离红线获 fuzz 断言形态第二锁（config_test 探针子测之外）。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: proto fuzz——internal/proto/fuzz_test.go 两目标（DecodeHello/DecodeResize + ClampDim 不变量）+ ci.yml fuzz job + 双 60s 冒烟</name>
  <files>internal/proto/fuzz_test.go, .github/workflows/ci.yml</files>
  <read_first>
    - internal/proto/proto.go（DecodeHello L136-144 签名与 ClampDim 钳制语义；DecodeResize L203-209；ClampDim L212-220 [1,1000] 契约；ReadLimitPostAuth L74-76 注释面）
    - internal/proto/proto_test.go（package proto_test 外部包形态与表驱动先例）
    - .github/workflows/ci.yml（全文——go/web 两 job 并列结构；新增 fuzz job 插入点与钉版形态）
    - .planning/phases/09-release-polish/09-RESEARCH.md（Pattern 3 proto 侧定稿 + §Code Examples FuzzDecodeHello 形态；Pattern 1 ci.yml 钉版；Pitfall 4 单包单目标）
    - .planning/phases/09-release-polish/09-PATTERNS.md（§2 ci.yml fuzz job 形态 + §8 proto fuzz analog）
  </read_first>
  <behavior>
    - 种子五类（合法 / 负值超大 / 截断 / 空载荷 / 类型混乱）随常规 go test 单测化全 PASS
    - 60s 实跑两目标各自 PASS 无崩溃（崩溃即修，语料落 internal/proto/testdata/fuzz/ 入库）
    - 不变量实证：DecodeHello 成功 ⇒ Cols/Rows ∈ [1,1000]；DecodeResize 同；任意输入不 panic
    - ci.yml 以 go test ./... 常规路径同步运行种子语料（零时长回归门，go leg 既有行覆盖）
  </behavior>
  <action>
    ① internal/proto/fuzz_test.go（新文件，package proto_test——与 proto_test.go 同外部包形态）：FuzzDecodeHello 种子——[]byte(`{"version":"wesh.v1","cols":80,"rows":24}`)（合法）、[]byte(`{"version":"wesh.v1","cols":-1,"rows":999999,"ticket":"x"}`)（负值超大+可选键）、[]byte(`{"version":`)（截断）、[]byte{}（空载荷——server 侧空帧另有 len 守卫，本层只证不 panic）、[]byte(`{"version":"wesh.v1","cols":1e999,"rows":true}`)（类型混乱）；f.Fuzz 体：hp, ok := proto.DecodeHello(data)；!ok 即 return；hp.Cols/hp.Rows 任一越出 [1,1000] 即 t.Fatalf（ClampDim 契约 fuzz 断言形态）。FuzzDecodeResize 同法：cols, rows, ok := proto.DecodeResize(data)；ok 时 cols/rows ∈ [1,1000] 断言。文件头注释登记 D-09/D-10、bytes-in 纯函数零改造、「帧拆分型字节面在 server.go 有 len(data)==0 前置守卫（:838/:1006）无独立目标必要」。
    ② 冒烟：go test -run 'FuzzDecode' ./internal/proto/（种子单测化 PASS）→ go test -fuzz=FuzzDecodeHello -fuzztime=60s ./internal/proto/ → go test -fuzz=FuzzDecodeResize -fuzztime=60s ./internal/proto/（Pitfall 4：两次独立调用）。
    ③ .github/workflows/ci.yml 新增 fuzz job（与 go/web 并列顶层，PATTERNS §2 定稿形态）：runs-on ubuntu-latest；steps = checkout@v7.0.1 → setup-go@v7.0.0（go-version-file: go.mod）→ run: go test -fuzz=FuzzDecodeHello -fuzztime=60s ./internal/proto/ → run: go test -fuzz=FuzzDecodeFileConfig -fuzztime=60s ./cmd/wesh/（不加 -race——与 go leg 并行墙钟 +60-70s；注释登记 D-10 CI 短跑门与「种子/崩溃语料随 go leg 常规测试零时长回归」）。go/web 两 job 逐字节不动。
    提交：feat(09-02): add proto frame fuzz targets and CI fuzz leg
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && go test -count=1 ./internal/proto/ && go test -fuzz=FuzzDecodeHello -fuzztime=60s ./internal/proto/ && go test -fuzz=FuzzDecodeResize -fuzztime=60s ./internal/proto/ && go vet ./... && go test -race -count=1 ./... && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && git diff --exit-code go.mod go.sum</automated>
  </verify>
  <acceptance_criteria>
    - 两 fuzz 目标种子单测化 PASS + 各 60s 实跑 PASS；go test -race 全量绿（既有面零回归）
    - `grep -c 'func FuzzDecodeHello\|func FuzzDecodeResize' internal/proto/fuzz_test.go` == 2
    - `grep -c 'fuzztime=60s' .github/workflows/ci.yml` == 2（两调用各一）
    - `grep -c '^  fuzz:' .github/workflows/ci.yml` == 1（顶层第三 job）
    - `grep -c '\-race' .github/workflows/ci.yml` == 1（仍只在 go leg——fuzz job 不加）
    - `git diff --exit-code go.mod go.sum` 退出 0
  </acceptance_criteria>
  <done>
    proto/TOML 两面 fuzz 目标全绿入库；CI 短跑门（2×60s）落地；崩溃语料入库通道就绪（D-10），长跑由 09-09 发布脚本承载。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WS 远程输入 → proto 解码 | 未认证窗口的畸形帧面（Hello 是握手首帧——fuzzer 的远程攻击面替身） |
| 本地配置文件 → TOML 解析 | operator 配置面（挂载盘/容器 secret 来源不可控） |
| fuzz 错误文本 → stderr/日志 | 值剥离红线面（credential 值经错误文本泄露即 SEC-01 破口） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02a | Denial of Service | 畸形帧/畸形 TOML 致解码 panic（远程崩溃/启动崩溃） | high | mitigate | D-09 两面 fuzz：不 panic 为首要不变量；CI 短跑门 + 发布长跑双层；崩溃语料入库永久回归 |
| T-09-02b | Information Disclosure | TOML 错误文本回显 credential 值 | high | mitigate | FUZZ_PROBE_SECRET 探针 fuzz 断言（值剥离红线的第二锁）；decodeFileConfig 单写口防红线逻辑分叉 |
| T-09-02c | Tampering | 接缝重构漂移（错误分类行为改变） | medium | mitigate | 行为保持纪律：config_test.go 既有全量零改动为验收硬条件；逐字迁移不复制第二份 |
| T-09-02d | Denial of Service | fuzz CI 时间预算失控拖慢全管线 | low | mitigate | 60s 硬顶两次调用；不加 -race；独立 job 并行不阻塞 go/web leg |
| T-09-SC | Tampering | npm/pip/cargo 包安装供应链 | high | mitigate | N/A——Go stdlib testing.F 零新依赖（D-09 裁决），go.mod/go.sum 验收逐字节不动 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V5 Input Validation | ✓ 远程帧面 + 本地配置面 fuzz 双锁；错误文本值剥离探针断言 |
</threat_model>

<verification>
- 两包种子单测化 + 三目标各 60s 实跑全 PASS
- go test -race -count=1 ./... 全量绿（接缝重构 + 新文件对既有面零回归）
- ci.yml YAML 合法、fuzz job 形态静态断言全过
</verification>

<success_criteria>
- 任意畸形输入下 proto 解码与 TOML 解析不 panic、尺寸恒钳制、错误文本零值泄露——两面鲁棒性获得持续回归门
- 发布脚本（09-09）的长 fuzz 步骤有真实可跑的 2×10min 目标
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `func decodeFileConfig(path string, r io.Reader) (*fileConfig, error)`（cmd/wesh/config.go——loadFileConfig 提取的 reader 委托接缝；loadFileConfig 签名不变）
- `func FuzzDecodeHello(f *testing.F)` / `func FuzzDecodeResize(f *testing.F)`（internal/proto/fuzz_test.go，新文件，package proto_test）
- `func FuzzDecodeFileConfig(f *testing.F)`（cmd/wesh/fuzz_test.go，新文件，package main）
- ci.yml `fuzz:` job（两 go test -fuzz 调用 ×60s）
- `internal/proto/testdata/fuzz/`、`cmd/wesh/testdata/fuzz/`（崩溃语料入库点——仅实跑发现时创建入库）
- 探针种子字面量 `FUZZ_PROBE_SECRET`（cmd/wesh/fuzz_test.go 内，值剥离红线断言材料）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/09-release-polish/09-02-SUMMARY.md` when done
</output>
