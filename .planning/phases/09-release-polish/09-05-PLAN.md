---
phase: 09-release-polish
plan: 05
type: execute
wave: 3
depends_on:
  - "09-03"
  - "09-04"
files_modified:
  - web/uat/phase09.mjs
autonomous: true
requirements:
  - OPS-03

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "phase09.mjs（零依赖 Node 原生 WebSocket/fetch，spawn 真实二进制）全场景 PASS：--index 四拒绝 exit 2（不存在/非常规/不可读/超限）+ 错误行含路径与类别、零文件内容探针（唯一内容探针串反断言）"
    - "给页断言：GET / 与 GET /s/{ro-token}/ 返回自定义字节 byte-identity（启动行解析取分享链接）；GET /index.html 同字节；GET 相对资源（如 /x.css）→ 404；0 字节文件 → 200 空 body"
    - "gzip/Vary：Accept-Encoding 显式含 gzip → Content-Encoding: gzip 且 gunzip 后 byte-identity；两态 Vary: Accept-Encoding 恒在；Content-Type text/html; charset=utf-8"
    - "安全头同源：自定义页响应带 Content-Security-Policy（connect-src 'self' 等现状全集）——与内建页响应同值"
    - "认证面照旧：无认证模式 POST /api/attach → 404；WS 握手 Hello→Welcome 全链（自定义页部署下 WS 端点不受影响——D-05「WS 端点照旧暴露」）；base-path 组合（--index + --base-path /wesh）GET /wesh/ 给自定义字节"
    - "配置通道：TOML index 键生效（无 CLI 时）；index-max-size 调小触发超限拒绝场景（错误行零内容探针断言的触发形态）"
    - "红线三件套（phase06/07 纪律沿用）：share token/探针值只作断言材料，永不进 check detail/控制台/汇总行；sensitiveTokens 闭包数组 + 尾部 assertOutputClean 运行时自净"
    - "既有 UAT 零影响：phase03/phase05/phase06-dom/phase07 脚本全绿（--index 未配置时逐字节现状 + D-18 文案变更后 dom 面守恒）"
  artifacts:
    - path: "web/uat/phase09.mjs"
      provides: "OPS-03 协议层全链 UAT（启动校验矩阵 + 双通道给页 + gzip/Vary + 认证面 + base-path 组合）"
      contains: "phase09"
  key_links:
    - from: "web/uat/phase09.mjs 场景"
      to: "真实 wesh 二进制（go build 自当前源码树）"
      via: "spawn --bind 127.0.0.1 --port 0 前置（loopback 随机端口零干扰）+ 启动行解析取端口与分享链接（phase07.mjs 夹具先例）；期望 exit 2 场景 opts.defaultListen:false argv 原样"
      pattern: "startWesh"
---

<objective>
落地 OPS-03 协议层全链 UAT：新建 web/uat/phase09.mjs（phase07.mjs 骨架件逐字复用——check/skip/assertOutputClean 三件套 + startWesh spawn 夹具），对真实二进制断言 09-04 实现的 --index 全行为面；随后跑既有 UAT 子集确认零影响。

Purpose: Go 单测（09-04 TestCustomIndex）覆盖伺服逻辑，本脚本覆盖「真实二进制启动 → 配置解析 → 校验矩阵 → 双通道给页」全链——文档即被测物纪律下 --index 节的每一项承诺都有可执行断言。
Output: phase09.mjs 全绿 + 既有脚本回归绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-release-polish/09-CONTEXT.md
@.planning/phases/09-release-polish/09-UI-SPEC.md
@.planning/phases/09-release-polish/09-PATTERNS.md
@.planning/phases/09-release-polish/09-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: web/uat/phase09.mjs——OPS-03 协议层全链断言（启动校验矩阵/双通道给页/gzip/Vary/安全头/认证面/base-path/配置通道）</name>
  <files>web/uat/phase09.mjs</files>
  <read_first>
    - web/uat/phase07.mjs（全文——脚本头覆盖清单与红线声明 L1-25；check/skip/assertOutputClean 三件套 L62-106；startWesh spawn 夹具 L108+（--bind 127.0.0.1 --port 0 前置、opts.defaultListen:false argv 原样、redactArgs 脱敏）；S1 配置场景 = --index 校验矩阵的直接先例）
    - web/uat/phase03.mjs（分享链接启动行解析与 /s/{token}/ 全链先例）
    - .planning/phases/09-release-polish/09-PATTERNS.md（§11 phase09.mjs 断言面清单）
    - .planning/phases/09-release-polish/09-04-SUMMARY.md（--index 实现终态——错误文案精确形态/配置键/伺服行为）
    - .planning/phases/09-release-polish/09-UI-SPEC.md（§Custom Index Contract 1-7 逐字边界——场景设计依据）
  </read_first>
  <behavior>
    - S1 启动校验矩阵（独立 spawn 期望 exit 2 不启动，opts.defaultListen:false）：S1a --index 不存在文件 → exit 2 + stderr 含路径与不存在类别；S1b --index 目录 → exit 2 + 非常规类别；S1c TOML index-max-size 调小（如 64）+ --index 100 字节探针文件 → exit 2 + 超限类别；S1a-c 三行 stderr 均不含探针内容串（内容红线反断言——探针串只存 sensitiveTokens 闭包）
    - S2 给页（spawn --index probe.html 常态实例）：S2a GET / → 200 byte-identity 探针字节；S2b GET /index.html → 同字节；S2c GET /s/{ro-token}/ → 同字节（启动行解析取 ro 链接）；S2d GET /x.css → 404；S2e GET / 安全头断言（Content-Security-Policy 含 connect-src 'self'）
    - S3 gzip/Vary：S3a Accept-Encoding: gzip → Content-Encoding gzip + 解压 byte-identity + Vary 在；S3b 无 Accept-Encoding → 明文 byte-identity + Vary 在
    - S4 认证面照旧：S4a 无认证模式 POST /api/attach → 404；S4b WS attach（Hello→Welcome）全链；S4c 凭据模式（--credential）GET / 无凭据 → 401、带凭据 → 200 自定义字节（认证闸在装饰链外层成立）
    - S5 0 字节与 base-path：S5a --index 空文件 → GET / 200 空 body；S5b --index + --base-path /wesh → GET /wesh/ 200 探针字节、GET / 404（根无挂载）
    - S6 配置通道：S6a TOML index 键（无 CLI）→ GET / 给自定义字节；S6b 配置 index + CLI --index 另文件 → CLI 覆盖生效
  </behavior>
  <action>
    ① web/uat/phase09.mjs（新文件）：脚本头注释登记覆盖清单（S1-S6 对应 09-04 行为面 + D-05..D-08）与红线声明（phase06.mjs:11-13 纪律逐字沿用——token/探针值永不进 detail/汇总行）；三件套与 startWesh 夹具逐字复用 phase07.mjs 形态（check/skip/assertOutputClean/sensitiveTokens/startWesh/启动行解析 waitListening）；二进制构建前置：go build -o /tmp/wesh-uat/wesh ./cmd/wesh（脚本参数可覆写路径，phase07.mjs 先例）。
    ② 探针文件夹具：os.mkdtemp 下落 probe.html（内容含唯一探针串如 CIDX-PROBE-<随机>——sensitiveTokens 登记）、empty.html（0 字节）、probe2.html（CLI 覆盖对照）；TOML 夹具写 index/index-max-size 键。
    ③ S1-S6 场景按 behavior 全行实现：S1 期望 exit 2 场景以 spawn 直收 exit code + stderr 全文（不外打印——只断言含路径/类别、不含探针串）；S2-S6 以 fetch/WS 客户端断言（WS 握手复用 phase07.mjs dialHello 形态）；gzip 断言以 node:zlib gunzipSync 校验 byte-identity。
    ④ 尾部汇总 + assertOutputClean() 运行时自净（遍历 emittedDetails 断言零 sensitiveTokens 出现）；进程组收口（每场景实例必 kill——phase07.mjs 收口纪律）。
    提交：test(09-05): add phase09 protocol UAT for custom index
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && go build -o /tmp/wesh-uat/wesh ./cmd/wesh && node web/uat/phase09.mjs /tmp/wesh-uat/wesh</automated>
  </verify>
  <acceptance_criteria>
    - phase09.mjs 全场景 PASS（S1-S6，零 FAIL；无 skip 行——本 phase 无平台豁免面）
    - assertOutputClean 通过（token/探针值零泄漏进输出）
    - `grep -c 'assertOutputClean' web/uat/phase09.mjs` ≥ 1 且 `grep -c 'sensitiveTokens' web/uat/phase09.mjs` ≥ 2（声明 + 登记点）
    - `grep -cF 'CIDX-PROBE' web/uat/phase09.mjs` ≥ 1（内容探针夹具在场——detail 输出零出现的断言材料）
    - 脚本退出码 0（全 PASS 形态）；stderr 断言路径含「含路径+类别、不含探针串」双向
  </acceptance_criteria>
  <done>
    OPS-03 协议层全链 UAT 上线全绿：启动校验矩阵/双通道/gzip/Vary/安全头/认证面/0 字节/base-path/配置通道八类场景对真实二进制锁定。
  </done>
</task>

<task type="auto">
  <name>Task 2: 既有 UAT 回归——phase03/phase05/phase06-dom/phase07 四脚本全绿（--index 零影响 + D-18 后 dom 面守恒）</name>
  <files>web/uat/phase09.mjs</files>
  <read_first>
    - web/uat/phase03.mjs / phase05.mjs / phase07.mjs（运行方式与二进制参数形态——回归执行依据）
    - web/uat/phase06-dom.mjs（Task 列表 D1-D13——09-03 扩展后的全场景面）
    - .planning/phases/09-release-polish/09-CONTEXT.md（「已锁定不重复决策」节——六段式回归纪律）
  </read_first>
  <action>
    ① 以当前源码树重建二进制与 dist（go build ./cmd/wesh + pnpm -C web build 已在 Task 1/09-03 落地——本任务确认产物新鲜后直接跑）。
    ② 顺序跑四脚本：node web/uat/phase03.mjs、node web/uat/phase05.mjs、node web/uat/phase06-dom.mjs、node web/uat/phase07.mjs（各带二进制路径参数，先例形态）；任一 FAIL 即停修——--index 未配置时伺服/认证/协议面须逐字节现状（零值兜底纪律的回归证据），D-18 后 phase06-dom D1-D13 守恒（09-03 已验，本任务二次确认跨 plan 无漂移）。
    ③ 结果记入 SUMMARY（四脚本 PASS 计数与零 FAIL 证据）。
    提交：test(09-05): regression pass over existing UAT suites
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && node web/uat/phase03.mjs /tmp/wesh-uat/wesh && node web/uat/phase05.mjs /tmp/wesh-uat/wesh && node web/uat/phase06-dom.mjs /tmp/wesh-uat/wesh && node web/uat/phase07.mjs /tmp/wesh-uat/wesh</automated>
  </verify>
  <acceptance_criteria>
    - 四脚本全部退出码 0（全 PASS；phase06-dom 的 D12b skip 行属豁免不计失败）
    - 无脚本输出 FAIL 行；各脚本尾部 assertOutputClean 自净通过
  </acceptance_criteria>
  <done>
    既有 UAT 子集回归全绿——--index 零影响与 D-18 后 dom 面守恒获跨 plan 证据。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT 脚本 → 测试输出 | token/探针值泄露面（share token 即访问凭证——进日志/CI 输出即泄漏） |
| spawn 实例 → loopback 端口 | 测试实例生命周期面（泄漏实例占用端口/拖垮后续测试） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-05a | Information Disclosure | share token/凭据/探针值进 check detail 或 CI 日志 | high | mitigate | 红线三件套：detail 只打状态码/布尔/形状/文案常量；sensitiveTokens 闭包登记 + assertOutputClean 运行时自净（phase06/07 纪律逐字沿用） |
| T-09-05b | Denial of Service | 测试实例泄漏（端口占用/僵尸进程拖垮回归） | medium | mitigate | 每场景实例必 kill 的收口纪律（phase07.mjs 先例）；独立 spawn 期望 exit 2 场景不监听零占用 |
| T-09-SC | Tampering | npm/pip/cargo 包安装供应链 | high | mitigate | N/A——零依赖 Node 原生脚本（web/uat/ 既有 node_modules 无新增），pnpm-lock.yaml 不动 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V7 Logging（测试面镜像） | ✓ assertOutputClean 运行时自净——红线在 UAT 通道的机械锁 |
</threat_model>

<verification>
- phase09.mjs 全场景 PASS + assertOutputClean 自净
- phase03/05/06-dom/07 四脚本回归全绿
</verification>

<success_criteria>
- --index 的每一项文档承诺（09-09 README --index 节素材）都有对真实二进制的可执行断言
- 既有 UAT 面零漂移——OPS-03 落地对 v1 既有行为零回归
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `web/uat/phase09.mjs`（新文件——场景 S1a-c/S2a-e/S3a-b/S4a-c/S5a-b/S6a-b；check id 族 S1a..S6b）
- 探针夹具字面量 `CIDX-PROBE-`（phase09.mjs 内，内容红线反断言材料）
- 无生产代码符号新增
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/09-release-polish/09-05-SUMMARY.md` when done
</output>
