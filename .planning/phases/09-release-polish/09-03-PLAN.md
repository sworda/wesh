---
phase: 09-release-polish
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - web/src/main.ts
  - web/index.html
  - web/uat/phase06-dom.mjs
  - web/dist/index.html
autonomous: true
requirements:
  - OPS-10

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "R1（D-18①，UI-SPEC C-10 逐字）：case 1001 的 hintPrefix 改为新常量 HINT_SHUTDOWN = 'If wesh is not restarted for you, start it again from your shell, then'——条件句式通吃 systemd 自重启/非自重启两部署形态；title 'Server shutting down' 与 body 'The wesh server is shutting down. The session has ended.' 逐字不变；case 1000（Session ended）的既有 hintPrefix 逐字不动（语义精确为真，不在本修订面）"
    - "R2（D-18②）：web/index.html #status 容器加 role=\"alert\" 单属性——面板族（C-1..C-10/Reconnecting）整体获得 assertive 播报；零视觉影响、零 CSS 改动、零新顶层元素"
    - "R3（D-18③）：onclose 分派顺序——if (!opened) 分支之前先分派 ev.code === 1001 → 落与稳态 case 1001 完全同一调用（单写口 helper，两处不得各自硬编码文案）；其余 !opened 路径逐字不变；pre-onopen 1001 与稳态 1001 同样不在 CORE-05 触发集（仅 1006——07-05 回归锁 D11b 不受影响）"
    - "UI-SPEC §UI Considerations status-panel covered 行：pre-onopen 1001 不再误述为「refusing new connections」（R3 落 C-10 专版）；C-10 新 hint 渲染行 ~88 字符（12px/400，480px max-width 内容宽 ≈60 字符/行）自然折 2 行无截断、textContent 渲染无 HTML 注入面；面板文案全为契约定稿短文本（title/body/hint 钉死），480px max-width + 自然折行无滚动/裁切面"
    - statement: "#status role=\"alert\" 的真实屏幕阅读器播报与节流行为（含 Reconnecting 面板 1Hz 倒计时更新在 assertive 语义下的重读边界——项目分层测试策略属平台原生豁免面，UAT 以 skipped+reason 记录）"
      verification: backstop
    - "dist 真实产物重建（P1 D-18：pnpm -C web build 先于一切 go 侧消费）：产物含 C-10 新文案串与 role=\"alert\"，源码/产物断言面守恒（esbuild 重命名模块级标识符——断言以字符串字面量与 HTML 属性为指纹，不 grep 标识符）"
    - "前端零新运行时依赖（09-UI-SPEC §Registry Safety）：web/pnpm-lock.yaml 逐字节不动"
  artifacts:
    - path: "web/src/main.ts"
      provides: "HINT_SHUTDOWN 常量 + showShutdown 单写口 helper + onclose 1001 优先分派"
      contains: "HINT_SHUTDOWN"
    - path: "web/index.html"
      provides: "#status role=\"alert\""
      contains: "role=\"alert\""
    - path: "web/uat/phase06-dom.mjs"
      provides: "D11a C-10 断言更新 + D12（R2 role 断言）+ D13（R3 pre-onopen 1001 场景）"
      contains: "If wesh is not restarted for you"
  key_links:
    - from: "web/src/main.ts onclose 分派"
      to: "showShutdown() → showStatus(C-10 三件套)"
      via: "R3 单写口纪律：pre-onopen 1001 与稳态 case 1001 调用同一 helper——文案零复制第二份（05-08 常量化纪律第三次沿用）"
      pattern: "showShutdown"
    - from: "web/index.html #status role=\"alert\""
      to: "全面板族 AT 播报"
      via: "单容器属性隐含 aria-live=\"assertive\" + aria-atomic=\"true\"——hidden 移除时整个面板内容播报（非逐面板属性）"
      pattern: "role=\"alert\""
---

<objective>
落地 D-18 ship 清零（07 deferred-items.md 三项 UI WARNING 全结，09-UI-SPEC §D-18 Contract 逐字定稿）：① 1001 关停面板 hint 条件化（HINT_SHUTDOWN 条件句式）；② #status 加 role="alert"；③ pre-onopen 到达的 1001 先按码分派落 1001 专版。随 dist 真实产物重建与 jsdom 断言扩展，使 v1.0.0 发布物无已知 WARNING 挂账。

Purpose: 「发布质量门」语义——1001 hint 误导、ARIA 缺失、pre-onopen 误述都是用户可见的发布面瑕疵；07-UI-REVIEW 三项 WARNING 的既定清零路由终点。
Output: main.ts/index.html 修订 + phase06-dom.mjs D11a 更新与 D12/D13 新场景 + dist 重建，全部 jsdom 绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-release-polish/09-CONTEXT.md
@.planning/phases/09-release-polish/09-UI-SPEC.md
@.planning/phases/09-release-polish/09-PATTERNS.md
@.planning/phases/07-deployment/deferred-items.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 源码修订——main.ts HINT_SHUTDOWN + showShutdown 单写口 + onclose 1001 优先分派（R1/R3）+ index.html role="alert"（R2）</name>
  <files>web/src/main.ts, web/index.html</files>
  <read_first>
    - web/src/main.ts（:426-447 文案常量族——HINT_RESTART 单写口注释形态先例；:449-475 showStatus 参数化形态；:870-953 onclose 分派全段——reconnecting 两块、!opened 分支、switch 全 case；:885-905 case 1000/1001 现状）
    - web/index.html（:60-68 #status 面板 DOM 结构——顶层三元素注释位）
    - .planning/phases/09-release-polish/09-UI-SPEC.md（§D-18 Ship-Clearance Contract ①②③逐字定稿 + §Revisions R1/R2/R3 行 + C-10 文案行）
    - .planning/phases/07-deployment/deferred-items.md（三项 WARNING 原文与修复指引——main.ts:903/index.html:63/main.ts:881-884 精确落点）
  </read_first>
  <behavior>
    - 稳态 1001（已建立会话收 1001）：面板 title 'Server shutting down'、body 'The wesh server is shutting down. The session has ended.'、hint 渲染 "If wesh is not restarted for you, start it again from your shell, then [Reload this page]."（C-10 逐字）
    - pre-onopen 1001（握手未完成收 1001）：落同一 1001 专版面板（三件套逐字同源），不再落 'Unable to connect'（C-4）；不触发重连
    - 重连上下文中收 1001：循环终止 + 同一专版面板（既有 D11c 行为零漂移）
    - case 1000（Session ended）：面板 hint 保持既有字面逐字（进程退出语义，非本修订面）
    - 1002/1008/1009/1011/1013/1006 各 case 与 default 桶逐字不变
  </behavior>
  <action>
    ① web/src/main.ts 常量族区（:433 HINT_RESTART 旁）追加：const HINT_SHUTDOWN = 'If wesh is not restarted for you, start it again from your shell, then';（注释登记 D-18①/C-10——条件句式通吃 systemd Restart= 自重启与非自重启两形态：条件为假时不构成错误指引、为真时指引精确；09-UI-SPEC ① 定稿依据）。
    ② showShutdown 单写口 helper（showStatus 定义附近）：封装 showStatus('Server shutting down', 'The wesh server is shutting down. The session has ended.', HINT_SHUTDOWN)——title/body 逐字不变（07-UI-REVIEW 判定 house-legal）；注释登记「R1/R3 单写口：pre-onopen 分派与稳态 case 1001 唯一调用形态，文案不得复制第二份（05-08 纪律）」。
    ③ onclose 分派顺序修订（:881-884 区）：if (!opened) 分支之前插入 if (ev.code === 1001) { showShutdown(); return; }——位置在 reconnecting 两块（:874-880）之后、!opened 之前（重连上下文 1001 仍先经 stopReconnect 清循环再落面板，D11c 行为零漂移）；case 1001 函数体改为 showShutdown(); break;（删除旧 hintPrefix 字面量——该字面此后全文件仅存 case 1000 一处，Session ended 语义不动）；其余分支逐字不动。
    ④ web/index.html:63：<div id="status" hidden> → <div id="status" role="alert" hidden>——单属性零 CSS 改动（注释位补 R2 登记：role="alert" 隐含 aria-live="assertive"+aria-atomic="true"，面板族整体播报；Reconnecting 1Hz 重读边界见 09-UI-SPEC ② 已知边界登记）。
    ⑤ tsc 类型检查：pnpm -C web exec tsc --noEmit 通过（dist 重建在 Task 2——本任务不动 dist）。
    提交：feat(09-03): conditional 1001 hint, role="alert" on #status, pre-onopen 1001 dispatch
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && pnpm -C web exec tsc --noEmit && grep -c 'HINT_SHUTDOWN' web/src/main.ts | grep -qx 3 && grep -c 'role="alert"' web/index.html | grep -qx 1 && git diff --exit-code web/pnpm-lock.yaml</automated>
  </verify>
  <acceptance_criteria>
    - pnpm -C web exec tsc --noEmit 退出 0
    - `grep -cF "If wesh is not restarted for you, start it again from your shell, then" web/src/main.ts` == 1（C-10 字面全文件唯一——常量单写口）
    - `grep -c 'showShutdown' web/src/main.ts` ≥ 3（helper 定义 + pre-onopen 分派 + case 1001 三处）
    - `grep -cF 'Start wesh again from your shell' web/src/main.ts` == 1（旧 hint 字面仅存 case 1000 一处——Session ended 语义不动）
    - `grep -c 'role="alert"' web/index.html` == 1 且 #status 行同含 hidden
    - `git diff --exit-code web/pnpm-lock.yaml` 退出 0（零新依赖）
  </acceptance_criteria>
  <done>
    R1/R2/R3 源码修订完成：C-10 条件句式落地单写口、#status 获 role="alert"、pre-onopen 1001 落专版；tsc 绿、零新依赖。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: jsdom 断言扩展 + dist 重建——phase06-dom.mjs D11a 更新 + D12（role 断言）/D13（pre-onopen 1001）新场景 + 前端 UAT 回归 + 产物指纹断言</name>
  <files>web/uat/phase06-dom.mjs, web/dist/index.html</files>
  <read_first>
    - web/uat/phase06-dom.mjs（头注释夹具说明 L1-40；check/skip/assertOutputClean 三件套 L54-106 区；D11 场景 L591-655——D11a :610 旧 hint 断言行是本次更新点；synthClose 形态与面板查询 helper）
    - web/src/main.ts（Task 1 落地后的 onclose 分派终态——D13 场景驱动的行为依据）
    - .planning/phases/09-release-polish/09-UI-SPEC.md（§D-18 ③ 定稿——pre-onopen 1001 落 C-10 专版；② role="alert" jsdom 断言面 + 真实 AT 豁免说明）
    - .planning/phases/09-release-polish/09-PATTERNS.md（§15 phase06-dom.mjs 扩展形态——R1/R2/R3 三断言面）
  </read_first>
  <behavior>
    - D11a 更新后：稳态 1001 面板 hint 断言命中 C-10 新前缀逐字（不再断言旧 1001 提示语）；D11b/c/d 逐字不动全绿
    - D12（新场景）：jsdom 读取 #status 元素 getAttribute('role') === 'alert'；同场景附真实 AT 播报 skip 行（skipped+reason：平台原生豁免面，指向 09-UI-SPEC ② 已知边界）
    - D13（新场景）：loadTerminal 后握手未完成（未收 Welcome、opened=false）即 synthClose(1001) → 面板 title === 'Server shutting down' 且 hint 含 C-10 前缀、title !== 'Unable to connect'；守候窗零新连接构造（不触发重连）
    - dist 重建后 phase06-dom 全场景（D1-D13）+ phase04-dom + phase05-dom 回归全绿；assertOutputClean 运行时自净通过
  </behavior>
  <action>
    ① web/uat/phase06-dom.mjs 头注释覆盖清单补 D12/D13 两行（D-18②/③ 兑现面登记）；D11a（:610 区）hint 断言由旧 1001 提示语改为 C-10 新前缀逐字 'If wesh is not restarted for you, start it again from your shell, then'（p.hint.includes 同形态）。
    ② 新增 D12 场景（独立 spawn + 独立 jsdom 隔离纪律）：loadTerminal 后断言 document.getElementById('status').getAttribute('role') === 'alert'（R2 jsdom 断言面）；随后 skip('D12b', '真实屏幕阅读器播报/节流行为', '平台原生豁免面——真实 AT 栈按分层策略豁免，见 09-UI-SPEC §D-18 ② 已知边界')。
    ③ 新增 D13 场景（独立 spawn + 独立 jsdom）：loadTerminal 建立连接后在 Welcome 到达前（不完成握手——hold 或不发 Hello 使 opened 保持 false）调 synthClose(1001) 驱动 onclose → waitPanel 断言 title 'Server shutting down' + hint 含 C-10 前缀 + title 非 'Unable to connect'；2.5s 守候窗零新连接构造（1001 不在 CORE-05 触发集，R3 不触发重连不变）。夹具细节以 D11 既有形态为模板（SpyWebSocket.synthClose + 面板查询 helper 复用）。
    ④ dist 重建：time pnpm -C web build（tsc && vite build && gzip -k9——P1 D-18 构建链；产物 web/dist/index.html + .gz 真实重建）。
    ⑤ 回归：node web/uat/phase06-dom.mjs 全场景（D1-D13）+ node web/uat/phase04-dom.mjs + node web/uat/phase05-dom.mjs 全绿；产物指纹断言——`grep -cF "If wesh is not restarted for you" web/dist/index.html` ≥ 1（C-10 入产物）、`grep -c 'role="alert"' web/dist/index.html` == 1、旧 hint 字面在产物中恰 1 处（case 1000 保留——esbuild 不改写字符串字面量）。
    提交：test(09-03): extend jsdom coverage for D-18 clearance and rebuild dist
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && time pnpm -C web build && node web/uat/phase06-dom.mjs && node web/uat/phase04-dom.mjs && node web/uat/phase05-dom.mjs && grep -cF 'If wesh is not restarted for you' web/dist/index.html && grep -c 'role="alert"' web/dist/index.html | grep -qx 1</automated>
  </verify>
  <acceptance_criteria>
    - phase06-dom.mjs 全场景 PASS（D1-D13，含 D12b skip 行不计失败）；phase04-dom/phase05-dom 全绿
    - `grep -cF 'If wesh is not restarted for you' web/uat/phase06-dom.mjs` ≥ 2（D11a + D13 两处断言）
    - `grep -cF 'Start wesh again from your shell' web/uat/phase06-dom.mjs` == 0（旧 1001 提示语断言已清除——源码侧该字面仅存 main.ts case 1000 一处）
    - `grep -cF 'If wesh is not restarted for you' web/dist/index.html` ≥ 1（产物指纹）
    - `grep -c 'role="alert"' web/dist/index.html` == 1
    - assertOutputClean 运行时自净通过（token/凭据值零泄漏进输出）
  </acceptance_criteria>
  <done>
    D-18 三项修复获 jsdom 行为锁（D11a 更新 + D12/D13 新场景）；dist 真实产物重建含 C-10 与 role="alert"；前端三 dom 脚本回归全绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 服务端关闭码/文案 → 面板渲染 | 远端语义驱动本地面板——文案常量化防远端内容注入 |
| 面板 DOM → 辅助技术 | role="alert" 播报面（用户可控输入不上面板——title 同步另有剥离截断防线） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03a | Tampering | 面板文案分叉（pre-onopen 与稳态各自硬编码致漂移） | medium | mitigate | R3 单写口 helper（showShutdown）——验收 grep 机械断言 C-10 字面全文件唯一；05-08 常量化纪律第三次沿用 |
| T-09-03b | Information Disclosure | 面板渲染远端内容（伪造关停提示钓鱼） | low | mitigate | 现状防线保持：showStatus 全 textContent 渲染（无 innerHTML 面）；1001 只认 ev.code 不渲染 close reason 内容（1013 注释同款纪律）；本 plan 零新渲染面 |
| T-09-03c | Denial of Service | role="alert" assertive 播报被 Reconnecting 1Hz 更新刷屏 | low | accept | 09-UI-SPEC ② 已知边界登记：D-18 裁决单属性最小修复；重连进展本身是用户需知状态；真实 AT 噪音反馈再评估 v2 拆分（backstop 行随 must_haves  surfaced） |
| T-09-SC | Tampering | npm/pip/cargo 包安装供应链 | high | mitigate | N/A——前端零新运行时依赖（09-UI-SPEC §Registry Safety），pnpm-lock.yaml 验收逐字节不动 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V5 Input Validation（远端事件分派面） | ✓ 1001 按码分派先于 !opened 截流——远端语义精确落版不误述 |
</threat_model>

<verification>
- pnpm -C web build 成功（tsc 类型检查 + vite 构建一体）
- phase06-dom/phase04-dom/phase05-dom 三 jsdom 脚本全绿
- 产物指纹三断言（C-10 字面 / role="alert" / 旧 hint 唯一处）
</verification>

<success_criteria>
- 07-UI-REVIEW 三项 WARNING 全结：自重启形态下 1001 hint 不再误导、面板族向辅助技术播报、pre-onopen 1001 不再误述关停为拒绝服务
- v1.0.0 发布物无已知 UI WARNING 挂账（07 deferred-items.md 三行清零路由终点）
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `const HINT_SHUTDOWN = 'If wesh is not restarted for you, start it again from your shell, then'`（web/src/main.ts 常量族区，HINT_RESTART 旁）
- `function showShutdown(): void`（web/src/main.ts——1001 面板单写口 helper：showStatus('Server shutting down', 'The wesh server is shutting down. The session has ended.', HINT_SHUTDOWN)）
- onclose 分派新增 `if (ev.code === 1001) { showShutdown(); return; }`（!opened 分支之前）；case 1001 函数体改 showShutdown()
- web/index.html #status `role="alert"` 属性
- phase06-dom.mjs D12/D13 场景 + D11a 断言更新（C-10 前缀）
- web/dist/index.html（重建产物——含 C-10 字面与 role="alert"）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/09-release-polish/09-03-SUMMARY.md` when done
</output>
