---
phase: 11-per-client
plan: 04
type: execute
wave: 3
depends_on: ["11-03"]
files_modified:
  - internal/server/perclient_test.go
autonomous: true
requirements:
  - PC-03
  - PC-04

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "子进程退出私有化（PC-04 行为锁）：两客户端 attach 同一 per-client 实例，A 端子进程 exit 42 → 仅 A 收 EXIT 帧（exit_code==42）+ close 1000；B 端在 1.5s 静默窗内零帧扰动且随后 echo 照常（他端逐字节无扰动 + 服务端续跑）；A 端子进程信号死亡（kill -TERM 自身）→ EXIT exit_code==-1 + 1000（信号死亡 -1 语义）"
    - "断开 SIGHUP（PC-03 行为锁）：客户端正常关闭（Close(1000)）后其子进程进程组在 2s 护栏内 kill(-pgid, 0) 得 ESRCH（setsid pgid==pid 既定不变量；ESRCH = 进程组消失含收割完成——无僵尸）且 PCSessionsLenForTest() 收敛到 0；无宽限（ttyd parity）"
    - "断线重连 = 全新进程：重连后回读 pid 与首次不等（PC-03 语义面；前端 terminal.reset() 归 Phase 12 不在此断言）"
    - "D-01 KILL 兜底（costly 机制的 Go 侧证据）：argv 为 trap '' HUP 死循环 + Options.StopTimeout=1s → 断开后 ~300ms 时点进程组仍存活（HUP 免疫实证）→ 1s 到期后 5s 护栏内 ESRCH（SIGKILL 补发收割）；stopseq_test.go 时序双断言形态的 per-client 同构"
    - "teardown 恰好一次（Pitfall 3）：断开↔子死竞态注入（同端并发发 exit 0 + 立即断连，10 轮迭代）下——每轮终态 quiescent（pcSessions 空 / 注册表空 / pgid ESRCH）、exitf 桩全程零调用（per-client 会话终结绝不触达 exitf——D-10/D-13 硬约束）、waitDone/teardownDone 双 close 零 panic（双 close 即 panic 的结构性锁）、-race 干净"
    - "零回归收口闸：shared 全量 Go 测试原样绿 + 期望值逐字未动；既有测试文件一字节不动（全部新测在 perclient_test.go 独立文件内，D-05）"
  artifacts:
    - path: "internal/server/perclient_test.go"
      provides: "TestPerClientExitPrivate42/TestPerClientExitSignalMinus1/TestPerClientDisconnectSIGHUP/TestPerClientReconnectNewPid/TestPerClientStopTimeoutKillFallback/TestPerClientTeardownRaceOnce 六测"
      contains: "TestPerClientTeardownRaceOnce"
  key_links:
    - from: "perclient_test.go EXIT 断言"
      to: "internal/server/exit_test.go readExitClose/decodeExitFrame"
      via: "同包复用既有三件套（读至 CloseError 收集帧序 + 类型字节/JSON 解码）——EXIT 私有化断言 = 本端末帧 EXIT + close 1000，他端零感知 = 静默窗零帧 + echo 存活"
      pattern: "readExitClose"
    - from: "perclient_test.go pgid ESRCH 断言"
      to: "syscall.Kill(-pid, 0) 轮询形态"
      via: "setsid pgid==pid 不变量（pty spawn 既有）——kill 0 探测进程组存在性，ESRCH = 无僵尸强证据"
      pattern: "syscall.Kill(-pid, 0)"
    - from: "perclient_test.go KILL 兜底时序双断言"
      to: "internal/server/stopseq_test.go :97-134"
      via: "「stop-timeout 前静默窗存活 / 到期后护栏内死亡」双断言形态直接镜像——断言对象从 exitf(-1) 换为 pgid ESRCH + pcSessions 收敛"
      pattern: "trap"
  prohibitions:
    - statement: "MUST NOT 让 EXIT 帧广播或串台（Security Mistakes 表——客户端 B 看到客户端 A 进程的退出码/信号 = 会话状态越权可见；他端断言连 detach 事件之外的任何帧扰动都不得出现）"
      status: unresolved
      verification: null
    - statement: "MUST NOT 为断开路径引入 linger 宽限（反特性 A2——宽限窗内进程占资源且收割竞态面增大；ttyd/wetty 均立即杀；断线不死需求由子进程侧 herdr/tmux 承接）"
      status: unresolved
      verification: null
    - statement: "MUST NOT 在断开路径自行调用 cmd.Wait（唯一收割者纪律——Wait 仅 sessionWatcher；断开路径只触发 teardown 并等待 waitDone 同步边）"
      status: unresolved
      verification: null
    - statement: "MUST NOT 把竞态注入测试写成「两模式都接受」或「任意结果都 PASS」的和稀泥形态——锁定的是不变量（quiescent 终态 / exitf 零调用 / 恰好一次），非时序胜者身份（注释明示区别）"
      status: unresolved
      verification: null
---

<objective>
per-client 生命周期的行为锁定层（PC-03/PC-04 的 Go 侧证据）：EXIT 私有化（exit 42 / 信号死亡 -1 / 他端逐字节无扰动）、断开 SIGHUP 无宽限无僵尸（pgid ESRCH）、重连新 pid、D-01 KILL 兜底时序双断言、断开↔子死双路收口的恰好一次竞态注入。

Purpose: 11-01 把机制一次定型（D-01 理由：teardown 序列二期再开口比重装更危险），本 plan 用测试把机制的全部可观测面锁死——Pitfall 2（kill-after-reap）/Pitfall 3（双路竞态）/Pitfall 8（HUP 免疫泄漏）三条 Critical 陷阱的 Phase 11 侧防线在此收口（Pitfall 2/3 的 verification 列明示于 PITFALLS 阶段映射表）。
Output: 六个行为测试全绿 + 全量 -race 双模式证据（shared 原样绿 + per-client 新测绿）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-per-client/11-CONTEXT.md
@.planning/phases/11-per-client/11-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/11-per-client/11-01-PLAN.md
@.planning/phases/11-per-client/11-03-PLAN.md
@internal/server/perclient_test.go
@internal/server/perclient.go
@internal/server/exit_test.go
@internal/server/stopseq_test.go
@internal/server/e2e_test.go
@internal/server/emptyexit_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EXIT 私有化两形态 + 断开 SIGHUP + 重连新 pid 四测（PC-03/PC-04）</name>
  <files>internal/server/perclient_test.go</files>
  <read_first>
    - internal/server/perclient_test.go（11-01/11-03 落地后的 harness、readOutputUntil、spawnedSessions 访问器与既有八测——追加位与复用面）
    - internal/server/exit_test.go（readExitClose :25-58 读至 CloseError 收集帧序 + decodeExitFrame 类型字节+JSON 解码——EXIT 断言三件套母本；10s 统护 ctx 纪律注释）
    - internal/server/stopseq_test.go（:30-31 夹具纪律红线「客户端 Read 永不带 deadline ctx——静默窗口一律 select + time.After 竞速形态」逐字）
    - internal/server/e2e_test.go（dialHello 形态；断开形态 = conn Close(1000) 调用先例）
    - internal/server/emptyexit_test.go（:45/:124 注释——SIGHUP 致死 ExitCode()=-1 的既有断言语义参照）
    - internal/server/perclient.go（sessionWatcher 与 teardownPCLocked 落地形态——断言目标）
  </read_first>
  <behavior>
    - TestPerClientExitPrivate42：A/B dialHello 同一实例（argv=[]string{"sh"}，Writable:true）→ A 发 `exit 42\r` → A readExitClose 收至 CloseError：末帧 decodeExitFrame 得 exit_code==42 且 CloseError.Code==1000；B 在 A 终结后 1.5s 静默窗（select+time.After 竞速）内零帧到达；随后 B 发 echo 标记正常回读（服务端续跑 + 他端无扰动双重实证）
    - TestPerClientExitSignalMinus1：A 发 `kill -TERM $$\r`（sh 自杀，信号死亡）→ A 末帧 EXIT exit_code==-1 且 close 1000（信号死亡 -1 语义，exitSignalNum/exitMessage 复用面）
    - TestPerClientDisconnectSIGHUP：A dialHello → 发 `echo PCPID=$$\r` 回读 pid → conn Close(1000) → 2s 护栏轮询 syscall.Kill(-pid, 0) 至 ESRCH（pgid==pid，setsid 不变量）；PCSessionsLenForTest() 收敛 ==0；/healthz clients==0
    - TestPerClientReconnectNewPid：A attach 回读 pid1 → Close → 轮询 pid1 ESRCH → A2 attach 回读 pid2 → pid2 != pid1（重连 = 全新进程，ttyd parity；前端 reset 归 Phase 12 注释锚定）
  </behavior>
  <action>
    ① perclient_test.go 追加 TestPerClientExitPrivate42（behavior 块形态）：A/B 两端 dialHello；A INPUT `exit 42\r`（append([]byte{proto.Input}, ...）形态）；A 侧 readExitClose 收集至 CloseError——断言末帧 decodeExitFrame exit_code==42、CloseError.Code==websocket.StatusNormalClosure；B 侧静默窗负例（1.5s 内任何 Read 到达即 FAIL——含 OUTPUT/EXIT/任何类型字节；「他端连 A 的终结余波都不可见」强形态）；窗后 B echo 唯一标记回读（服务端与 B 会话双双存活）。注释登记 Security Mistakes 表「EXIT 广播习惯禁带过来」行与 PC-04。
    ② 追加 TestPerClientExitSignalMinus1：A INPUT `kill -TERM $$\r`；断言末帧 EXIT exit_code==-1 + close 1000。注释锚定：信号死亡 -1 语义经 watcher 内联退出码提取（exec.ExitError.ExitCode() 对信号死亡返回 -1）与 shared lifecycle :1361-1365 同形。
    ③ 追加 TestPerClientDisconnectSIGHUP：echo 回读 pid（readOutputUntil 正则 `PCPID=(\d+)`——回显含命令原文但无数字自命中，phase06.mjs readPid 纪律同构）→ conn Close(1000) → poll 循环（每 25ms 探测，2s 总护栏）syscall.Kill(-pid, 0) 返回 error 且 errors.Is(err, syscall.ESRCH)（系统调用级 ESRCH——Go 1.26 kill(2) 包装对不存在进程组返回此值；**严禁**把「kill 0 无错」当死亡证据——那是存活）；断言 PCSessionsLenForTest() 最终 ==0 与 /healthz clients==0。注释锚定 PC-03「无宽限」与 setsid pgid==pid 不变量。
    ④ 追加 TestPerClientReconnectNewPid：pid1 → Close → ESRCH 轮询复用③形态 → 同 wsURL 再 dialHello → pid2 != pid1 断言。注释锚定 D-06 S7 语义（服务端语义本阶段成立；前端 terminal.reset() 归 Phase 12）。
    ⑤ 自检：`go test -race -count=1 ./internal/server/ -run 'TestPerClientExit|TestPerClientDisconnect|TestPerClientReconnect' -v -count=3` 三连跑无 flake（时序敏感形态的铁律——02  digest「-count=3 连跑验证无 flake」先例）；全量 -race 绿。
    提交：`test(11-04): private EXIT two forms + disconnect SIGHUP ESRCH + reconnect new pid — PC-03/PC-04 behaviorally locked (D-01)`
  </action>
  <verify>
    <automated>go test -race -count=3 ./internal/server/ -run 'TestPerClientExitPrivate42|TestPerClientExitSignalMinus1|TestPerClientDisconnectSIGHUP|TestPerClientReconnectNewPid' -v && go test -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - 四新测 -count=3 连跑全 PASS 无 flake；全量 `go test -race -count=1 ./...` 全绿
    - `grep -c 'TestPerClientExitPrivate42\|TestPerClientExitSignalMinus1\|TestPerClientDisconnectSIGHUP\|TestPerClientReconnectNewPid' internal/server/perclient_test.go` == 4
    - `grep -c 'exit_code' internal/server/perclient_test.go` ≥ 2（42 与 -1 两断言位；decodeExitFrame 解码字段名逐字）
    - `grep -c 'syscall.ESRCH' internal/server/perclient_test.go` ≥ 1 且 `grep -c 'syscall.Kill(-pid, 0)\|syscall.Kill(-pgid, 0)' internal/server/perclient_test.go` ≥ 1
    - 行为断言复核（SUMMARY 逐条记录实测输出）：B 端静默窗零帧、close 码 1000 两形态、pid 不等、ESRCH 到达
    - 既有 *_test.go 零改动（git diff 仅 perclient_test.go）
  </acceptance_criteria>
  <done>EXIT 私有化（42 / -1 / 他端零扰动）、断开 SIGHUP 无僵尸（ESRCH + pcSessions 收敛）、重连新 pid 四行为锁定；shared 零回归。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-01 KILL 兜底时序双断言 + teardown 恰好一次竞态注入（Pitfall 3/8，PC-03）</name>
  <files>internal/server/perclient_test.go</files>
  <read_first>
    - internal/server/perclient_test.go（Task 1 落地后现状——追加位）
    - internal/server/stopseq_test.go（:97-134 全文——trap "" TERM 恒活夹具 + waitMarker 同步 +「stop-timeout 前静默 / 到期后 5s 内收码」时序双断言形态，per-client 版的直接镜像母本；:30-31 夹具纪律红线）
    - internal/server/perclient.go（teardownPCLocked 快半段 AfterFunc 补 KILL 的 reaped 闸形态——断言目标）
    - internal/server/emptyexit_test.go（竞态/恰好一次断言的注释先例 :259——termOnce 双保险语义参照）
    - .planning/research/PITFALLS.md（Pitfall 3 双路收口竞态 + Pitfall 8 HUP 免疫泄漏 +「Looks Done But Isn't」检查单断开 SIGHUP 行）
  </read_first>
  <behavior>
    - TestPerClientStopTimeoutKillFallback：mutate 覆写 Options.StopTimeout=1s；argv=[]string{"sh","-c","trap '' HUP; echo PCKPID=$$; while true; do sleep 1; done"}（启动即印 pid，非交互死循环）→ A dialHello 回读 pid → conn Close → 断开后 ~300ms 时点 kill(-pid,0) 仍无错（HUP 免疫存活实证——stop-timeout 前静默窗）→ 到期后 5s 护栏内 ESRCH（SIGKILL 补发 + Wait 收割 + pcSessions 移除全链）→ PCSessionsLenForTest()==0
    - TestPerClientTeardownRaceOnce：10 轮迭代——每轮新客户端 dialHello（argv=[]string{"sh"}）后立即接连发 `exit 0\r` 与 conn Close（两触发源并发：子死 watcher 路径 vs 断开 detach 路径）；读连接至 CloseError（EXIT 帧可能到达或 Close 先落地——两形态均合法，注释明示这是竞态固有二形态而非断言放宽）；每轮终态断言：PCSessionsLenForTest()==0、/healthz clients 回归 0、回读 pid 的 pgid ESRCH；全程 exitf 桩零调用（len(exitCh)==0——per-client 会话终结绝不触达 exitf）；零 panic（waitDone/teardownDone 双 close 的结构性恰好一次锁——重复 close 即 panic 使 Once 失守必现形）
  </behavior>
  <action>
    ① 追加 TestPerClientStopTimeoutKillFallback（behavior 块形态；stopseq_test.go:97-134 镜像——时序双断言：到期前 300ms 静默窗内存活 + 到期后护栏内 ESRCH；静默窗用 time.After 竞速形态，Read/探测永不带 deadline 纪律对 syscall.Kill 探测同为直接调用无 ctx 面）。KILL 兜底经 D-01 固定序列（SIGHUP trap 免疫 → 1s AfterFunc 补 SIGKILL 经 reaped 闸 → Drain → Close → watcher Wait 返回 → pcSessions 移除）逐段注释锚定。测试尾部显式 syscall.Kill(-pid, syscall.SIGKILL) 兜底防御（护栏外仍存活即 FAIL 前的清场——CI 级联减速夹具纪律）；t.Cleanup 的 spawned Kill+Close 对已收割会话幂等无害。
    ② 追加 TestPerClientTeardownRaceOnce（behavior 块形态）：harness 单实例复用 10 轮（每轮新 dialHello）；轮内断言终态四件套（pcSessions 空 / healthz clients==0 / pgid ESRCH / exitf 零调用）；轮间隔 100ms 收敛等待（真实等待 + 护栏上限纪律，phase06.mjs :354-356 时序容差论证先例的 Go 同构）。注释登记 Pitfall 3「两路径都只触发、执行序列只有一个」与「竞态固有二形态 ≠ 断言放宽」的区别论证（零回归红线保护对象是 shared 期望值，本测锁定的是 per-client 竞态不变量）。
    ③ 收口自检：`go test -race -count=1 ./...` 全绿 + 两新测 -count=3 无 flake；`$(go env GOROOT)/bin/gofmt -l .` 零输出；git diff 确认仅 perclient_test.go 改动。
    提交：`test(11-04): KILL fallback timing dual-assertion + teardown exactly-once race injection — Pitfall 3/8 Phase-11 closure (PC-03, D-01)`
  </action>
  <verify>
    <automated>go test -race -count=3 ./internal/server/ -run 'TestPerClientStopTimeoutKillFallback|TestPerClientTeardownRaceOnce' -v && go test -race -count=1 ./... && $(go env GOROOT)/bin/gofmt -l .</automated>
  </verify>
  <acceptance_criteria>
    - 两新测 -count=3 连跑全 PASS 无 flake；全量 -race 全仓绿
    - `grep -c 'TestPerClientStopTimeoutKillFallback\|TestPerClientTeardownRaceOnce' internal/server/perclient_test.go` == 2
    - `grep -c 'StopTimeout' internal/server/perclient_test.go` ≥ 1（mutate 覆写位）
    - `grep -c 'exitCh' internal/server/perclient_test.go` ≥ 2（exitf 零调用断言位）
    - 时序双断言复核（SUMMARY 记录实测时点）：300ms 时点存活 + 到期后 ESRCH 到达时刻 < 5s 护栏
    - 竞态测 10 轮迭代断言注释含「竞态固有二形态非断言放宽」论证（防 Pitfall 11 误读）
    - 既有 *_test.go 与生产文件零改动（git diff 仅 perclient_test.go）
  </acceptance_criteria>
  <done>D-01 KILL 兜底机制经 trap 免疫 + 时序双断言锁定（HUP 免疫进程有界死亡）；断开↔子死竞态下恰好一次与 quiescent 终态不变量锁定；per-client 会话终结零 exitf 触达实测。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 子进程退出/断开事件 → 客户端 wire 面 | 退出码与信号信息只许到达属主客户端（越权可见 = 信息泄露） |
| 断开/子死双路并发 → teardown 序列 | 竞态下双重信号/双重收割/记账漂移 = 完整性事故面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04a | Information Disclosure | EXIT 帧（含 exit_code/信号语义）串台到非属主客户端 | high | mitigate | TestPerClientExitPrivate42 他端静默窗零帧断言（连 OUTPUT 余波都不得出现）+ 11-05 phase11.mjs S5 协议层对照 |
| T-11-04b | Tampering | teardown 双路并发 → 双重 SIGHUP（第二发打中 reap 后复用 pgid）/ 双 close panic / 记账漂移 | high | mitigate | teardownOnce + reaped 栅栏 + 慢半段单 goroutine（11-01 机制）+ TestPerClientTeardownRaceOnce 10 轮竞态注入（quiescent 四件套 + exitf 零调用 + 零 panic）+ 全量 -race |
| T-11-04c | Denial of Service | HUP 免疫子进程断开后永生（nohup/trap 泄漏 ×N——Pitfall 8） | high | mitigate | D-01 KILL 兜底分支（stopTimeout>0 时 AfterFunc 补 SIGKILL 经 reaped 闸）+ TestPerClientStopTimeoutKillFallback 时序双断言；stop-timeout=0 默认值语义不变，per-client 默认值重议归 Phase 13 裁决项① |
| T-11-SC | Tampering | 依赖供应链 | high | accept | 零新依赖；本 plan 仅测试文件改动 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V7 Error Handling（终结语义按属主精确送达） | ✓ EXIT 私有化两形态测试（Task 1） |
| V8 Audit（会话终结不污染审计面——exitf 零新触发源） | ✓ 竞态注入测 exitf 零调用断言（Task 2） |
</threat_model>

<verification>
- `go test -race -count=1 ./...` 全仓全绿（shared 期望值逐字未动）
- 六新测（本 plan 两 + Task 1 四）-count=3 无 flake
- `git diff --stat` 仅含 perclient_test.go
</verification>

<success_criteria>
- ROADMAP SC3 成立：断开（正常/异常形态经竞态注入覆盖）后子进程进程组立即 SIGHUP、无宽限无僵尸、信号与收割锁内序列化不误杀
- ROADMAP SC4 成立：子进程退出仅该客户端收私有 EXIT（exit_code / -1）+ 1000；服务端与其他客户端续跑
- Pitfall 2/3/8 的 Phase 11 侧 verification 全部落地（PITFALLS 阶段映射表）
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **PC-03/PC-04 (unclassified, unresolved)**：探针未分类——本 plan 为两需求的行为锁主体；1006 真实异常断开形态（OS 网卡栈时序）按 CODEBUDDY.md 分层测试策略 §5 平台豁免，协议层覆盖形态 = 正常关闭 + reader 错误路径（Go 侧 conn Close 即触发 reader 错误收口，与 1006 同走 detach 单点——豁免语义已在 11-01 挂点覆盖论证中锚定）。保持 flagged-unverified，由 11-06 收口闸承载显式证据。
- **竞态测试的「两形态合法」注释论证**：EXIT 到达 vs Close 先落地均为正确终结形态——锁定对象为 quiescent 不变量与恰好一次语义，非时序胜者；此非 Pitfall 11 禁止的断言放宽（该禁令保护 shared 零回归期望值）。
- **--once/--exit-when-empty 与 SIGTERM 自退缺口**：延续 11-01 已知中间态①②（Phase 13 pcSupervisor）；本 plan 竞态测的 exitf 零调用断言同时是该中间态的反向锁（Phase 13 落地时需按裁决翻转——注释锚定）。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- 测试 `TestPerClientExitPrivate42` / `TestPerClientExitSignalMinus1` / `TestPerClientDisconnectSIGHUP` / `TestPerClientReconnectNewPid` / `TestPerClientStopTimeoutKillFallback` / `TestPerClientTeardownRaceOnce`（internal/server/perclient_test.go——append-only；至此 D-05 独立文件共 14 测：11-01 五测 + 11-03 三测 + 本 plan 六测）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/11-per-client/11-04-SUMMARY.md` when done
</output>
