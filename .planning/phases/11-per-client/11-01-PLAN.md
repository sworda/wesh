---
phase: 11-per-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/wesh/main.go
  - internal/server/server.go
  - internal/server/clients.go
  - internal/server/perclient.go
  - internal/server/perclient_test.go
autonomous: true
requirements:
  - PC-02
  - PC-03
  - PC-04

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "per-client 模式下启动期零子进程：main.go run() 对 per-client 不调 pty.Start，以 sess=nil 调 server.New；New 尾部模式分岔——shared 分支（initArbiter/startedAt/session_start emit/sessionAlive 置位/ReadLoop+inputWriter+lifecycle 三件套）逐字不动，per-client 分支仅初始化 pcSessions 注册表、零全局 goroutine（pcSupervisor 归 Phase 13）"
    - "sess×mode 装配契约在 New 入口以程序错误 panic 承载（planner 裁定，取代 PATTERNS §8E 的 options_test.go 加行建议——保住 D-05『既有测试一字节不动』与 10-review WR-02 前移纪律）：per-client × sess≠nil panic `server: session-mode per-client requires nil sess`；shared × sess==nil panic `server: session-mode shared requires non-nil sess`（NormalizeOptions 零值归一之后、Server 字面量装配之前）"
    - "客户端 attach（ticket 核销通过）即经 spawnFunc(h.Cols, h.Rows) 获得独立 PTY 子进程——Hello cols/rows 已经 DecodeHello ClampDim 钳制，StartWithSize 直通出生即正确尺寸（无 80x24 中间态闪烁，SC1 后半）；spawn 点 = ticket 核销之后 / Welcome 组帧之前 / hubMu 之外（SEC-08 + Anti-Pattern 1）"
    - "Welcome 恒为 S→C 首帧且 cols/rows 回显本端 Hello 钳制尺寸（不经 sessionDimsLocked——per-client 无仲裁，G-05-1 契约退化为恒等式）；Welcome 入队先于 registerLocked + pcSessions 登记且全程持 hubMu，五 goroutine（writer/pinger/ReadLoop 闭包/inputWriter/sessionWatcher）注册后启动（S5 时序纪律，64KiB 内核缓冲承接 spawn→ReadLoop 启动间输出）"
    - "两个客户端 attach 后各自获得不同 pid、各端输出即自身进程输出互不串台（PC-02；perclient_test.go TestPerClientTwoClientsTwoPids 锁定）"
    - "spawn 失败时该客户端收 Error{server_error, \"failed to start process\"} 直写帧 + Close(1011, \"server_error\") + logEvent spawn_failed 单行审计（D-04，四段 schema 零敏感值）；失败点在注册之前——零注册零登记零残留（Pitfall 5 清理清单）；服务端与其他在线客户端不受影响"
    - "客户端断开（正常关闭/reader 错误/pong 超时/Shutdown 1001 广播引发的 detach 与 1013 kick 全形态）经注册表移除点触发该会话 teardown（Anti-Pattern 7 挂点位：detach :813 与 kickSlowConsumerLocked :602 的 maybeExitWhenEmptyLocked 同位）；D-01 固定序列恰好一次（sync.Once）：SIGHUP（经 reaped 栅栏）→ stopTimeout>0 则 AfterFunc 补 SIGKILL（栅栏同构覆盖补 KILL——Pitfall 2 语义延伸）→ Drain(200ms) → Close(master) → 等待收割返回（waitDone）→ pcSessions 单点移除"
    - "cmd.Wait 仅 sessionWatcher 调用（唯一收割者纪律每会话粒度保持，断开路径绝不自己 Wait）；信号与收割经 hubMu 内 pc.reaped 标志序列化，kill-after-reap 误杀复用 pgid 结构性不可能（Pitfall 2）"
    - "子进程退出（exit N / 信号死亡 exit_code=-1）后仅该客户端收私有 EXIT 帧并以 1000 关闭（PC-04）：sessionWatcher = Wait → close(waitDone) → hubMu 内置 exitCode+reaped → 触发 teardown（Once 去重）→ 等 teardownDone → close(inputDone) → proto.ExitFrame 组帧一次 → 同步 Write(2s ctx) → Close(1000) 直写序（EXIT 直写纪律，禁 outbox 异步——Anti-Pattern 6）；session_end 事件不 emit（D-04 窗口期空白，Phase 13 补齐）"
    - "INPUT case 读循环零分支（Pattern 2）：server.go:1120 入队目标由服务端全局队列改为 cl.inQ——shared 升档赋 s.inputQ、per-client 升档赋 pc.inQ；mode 门与限速门逐字不动；CR-01 纪律保持（读循环永不直写 master——inputWriter 改包级参数化函数 inputWriter(sess *pty.Session, q *inputQ, done chan struct{})，1 份代码 N 实例，Pattern 3）"
    - "maybeExitWhenEmptyLocked 与 Shutdown 的 stop-signal 段对 per-client 显式早退/守卫（s.sess 恒 nil 的 nil-deref 防线——PATTERNS 已标记陷阱），注释锚定「per-client 终结语义归 Phase 13」（D-01 注记窗口期）；已知中间态：per-client 下 --once/--exit-when-empty 本阶段永不退出、SIGTERM 后进程经 1001 广播+teardown 收敛会话但不自退（第二信号经 stopSignals 恢复默认处置逃生，main.go:1455 既有形态）"
    - "main.go run()：StartOptions 字面量收编为单一声明 startOpts（10-01 注释预言兑现——spawnFunc 闭包与 shared 分支 pty.Start 共用）；listen/serve 失败两回滚块的 sess.Close 加 nil 守护；shared 路径行为逐字节零回归"
    - "零回归收口闸（每 plan 必带）：shared 全量 Go 测试原样绿 + phase02-09.mjs 默认模式零修改重跑 + 期望值逐字未动 diff 审查；禁止断言放宽成「两模式都接受」（Pitfall 11）；options_test.go 一字节不动（D-05）"
  artifacts:
    - path: "internal/server/perclient.go"
      provides: "pcSession 结构（sess/inQ/inputDone/waitDone/teardownDone/startedAt/exitCode/reaped/teardownOnce）+ upgradePerClient 升档分支 + startSessionGoroutines 五件装配 + sessionWatcher + teardownPCLocked"
      contains: "type pcSession struct"
    - path: "internal/server/server.go"
      provides: "New 入口 sess×mode 契约 panic + New 尾部模式分岔 + Attach 升档 per-client 分岔（upgradePerClient 调用点）+ INPUT case cl.inQ 一行切换 + Shutdown stop-signal 段 per-client 守卫"
      contains: "upgradePerClient"
    - path: "internal/server/clients.go"
      provides: "client 结构 +inQ/pc 两字段（写一次于升档，remoteUser 先例注释形态）+ inputWriter 包级参数化 + detach/kick 两处 teardown 挂点 + maybeExitWhenEmptyLocked per-client 早退守卫"
      contains: "teardownPCLocked"
    - path: "cmd/wesh/main.go"
      provides: "run() per-client sess=nil 切换 + startOpts 收编 + 回滚 nil 守护"
      contains: "startOpts"
    - path: "internal/server/perclient_test.go"
      provides: "per-client 测试 harness（startPerClientServer + spawned 追踪收口夹具）+ TestPerClientTwoClientsTwoPids/TestPerClientWelcomeDims/TestPerClientInputEcho/TestNewModeSessContract/TestPerClientNoSessionStartEvent 五测（D-05 独立文件）"
      contains: "startPerClientServer"
  key_links:
    - from: "internal/server/server.go Attach 升档 per-client 分支"
      to: "internal/server/perclient.go upgradePerClient → Options.SpawnFunc 闭包（cmd/wesh/main.go run() 装配）"
      via: "spawn 点 = ticket 核销后 / Welcome 组帧前 / hubMu 外；h.Cols/h.Rows 已 ClampDim 钳制直通 StartWithSize"
      pattern: "upgradePerClient"
    - from: "internal/server/clients.go detach / kickSlowConsumerLocked"
      to: "internal/server/perclient.go teardownPCLocked"
      via: "removeLocked 返回 true 之后、maybeExitWhenEmptyLocked 同位——挂注册表移除点覆盖一切断开形态（Anti-Pattern 7）"
      pattern: "teardownPCLocked"
    - from: "internal/server/perclient.go sessionWatcher"
      to: "internal/proto/proto.go ExitFrame + internal/server/server.go exitMessage"
      via: "EXIT 私有化直写：组帧一次共享只读 → 同步 Write 2s ctx → Close(1000)——禁 outbox 异步入队（关闭帧超车防线）"
      pattern: "ExitFrame"
    - from: "internal/server/server.go INPUT case（:1094-1122）"
      to: "internal/server/clients.go client.inQ 字段"
      via: "Pattern 2 间接字段——升档时解析一次，读循环逐行不分支"
      pattern: "cl\\.inQ\\.tryEnqueue"
    - from: "internal/server/perclient.go teardownPCLocked 慢半段"
      to: "pcSessions 注册表移除"
      via: "Drain(200ms) → Close(master) → <-waitDone（watcher Wait 返回信号）→ hubMu 内 delete——慢半段绝不占 hubMu（Pitfall 3 行头阻塞红线）"
      pattern: "waitDone"
  prohibitions:
    - statement: "MUST NOT 在 ticket 核销通过前创建任何 per-client 会话资源（SEC-08 预认证零资源；Anti-Pattern 3——checkTicket 成功是 spawn 唯一前置）"
      status: unresolved
      verification: null
    - statement: "MUST NOT 在 hubMu 持锁态执行 spawn/Drain/Close/Wait 任一阻塞调用（Anti-Pattern 1 + Pitfall 3——fork/exec 或 200ms 级阻塞冻结全控制面）"
      status: unresolved
      verification: null
    - statement: "MUST NOT 让读循环同步直写 PTY master（CR-01——慢子进程 stdin 反堵读循环、pong 停摆误杀；输入一律经 cl.inQ + 每会话 inputWriter 专属 goroutine）"
      status: unresolved
      verification: null
    - statement: "MUST NOT 经 outbox 异步入队 EXIT 帧（Anti-Pattern 6——writer drain 与 Close 关闭帧竞态超车，客户端收 1000 却无退出码）"
      status: unresolved
      verification: null
    - statement: "MUST NOT 在 spawn 失败/容量拒绝文案中携带底层错误细节（路径/errno/系统调用名——服务器文件系统布局泄露面，Pitfall 5 + Security Mistakes 表）；生产代码与注释均以 Pitfall 编号引用该红线，不引字面量"
      status: unresolved
      verification: null
    - statement: "MUST NOT 新增 exitf 触发源（D-10/D-13 零新 exitf 分支——per-client 会话终结只关本端 WS，绝不触达 terminate；pcSupervisor 归 Phase 13）"
      status: unresolved
      verification: null
    - statement: "MUST NOT 放宽任何既有 shared 测试断言为「两模式都接受」形态（Pitfall 11——shared 零回归证据即毁）"
      status: unresolved
      verification: null
---

<objective>
per-client 生命周期主干 tracer：一条完整可用通路端到端打通——「浏览器 attach（ticket 核销）→ 独立 PTY 子进程 spawn（Hello 钳制尺寸直通）→ Welcome 回显 → 输入/输出双向管道 → 客户端断开即 SIGHUP 进程组（D-01 teardown 恰好一次）→ 子进程退出仅本端收私有 EXIT + 1000」。同时完成装配面切换：main.go run() per-client  sess=nil、New 尾部模式分岔、INPUT case 间接字段一行切换、inputWriter 参数化、detach/kick SIGHUP 挂点、两处 nil-deref 守卫。

Purpose: 这是 PC-02/PC-03/PC-04 三条需求的最长链——先以 production-quality tracer 证明「装配期一次分岔、运行期零分岔」架构在真实代码里成立（三大不变量：Welcome 恒首帧 / 唯一收割者 / exitf 恰好一次——后者本阶段 = per-client 永不触达 exitf），后续 plan 在此骨架上扩展容量闸与测试矩阵。D-01 要求 teardown 序列一次定型（含 KILL 兜底分支），故 teardown 不做二次切片。
Output: per-client 模式真实可用（双端双 pid 互不串台）；shared 模式逐字节零回归（全量 -race 绿 + phase02-09 原样重跑）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-per-client/11-CONTEXT.md
@.planning/phases/11-per-client/11-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@internal/server/server.go
@internal/server/clients.go
@internal/server/resize.go
@internal/pty/spawn.go
@internal/pty/io.go
@internal/proto/proto.go
@cmd/wesh/main.go
@internal/server/e2e_test.go
@internal/server/exit_test.go
@internal/server/events_test.go
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: per-client 主干端到端装配——pcSession/升档分支/五 goroutine/sessionWatcher/teardown Once/New 分岔/main.go sess=nil（D-01/D-04，PC-02/PC-03/PC-04）</name>
  <reversibility rating="costly">D-01 teardown 固定序列（含 KILL 兜底分支）一次定型——本阶段被 Go 测试（11-04 竞态注入）与 phase11.mjs S8 双重锁定后，拆除 KILL 分支需同步改写两 face 断言；用户已在 11-CONTEXT 锁定机制先行，记录评级不重复设卡。</reversibility>
  <files>internal/server/perclient.go, internal/server/server.go, internal/server/clients.go, cmd/wesh/main.go, internal/server/perclient_test.go</files>
  <read_first>
    - internal/server/server.go（Server 结构 sessionMode/spawnFunc 字段 :173-180；Options :290-310；ValidateOptions :324-348——:327-328 注释预言 sess 维度归 Phase 11；New :362-497——零值归一 :392-393、字段直传 :445-446、hubCond :481、initArbiter :484、session_start emit :485-489、sessionAlive :490-492、三件套 :493-496；Attach 升档序列 :951-1071——守卫区与 ticket 核销 :938-950、close(helloDone)+release() :979-980、shared 升档 :981-1041、attach 事件 :1047-1056、SetReadLimit :1057、writer/pinger 启动 :1064-1065、SignalForegroundGroup :1066-1070；INPUT case :1094-1122——:1120 入队现状行；lifecycle :1359-1441——退出码提取内联 :1361-1365、Drain :1386、close(inputDone) :1390、EXIT 组帧 :1393、直写纪律 :1424-1431；terminate/termOnce :1443-1450；Shutdown :1479-1513——stop-signal 段 :1508-1512）
    - internal/server/clients.go（defaultInputQueueBytes :49-53；client 结构 :95-160——remoteUser 注释形态母本 :98-105、pongTimedOut :144-148；newInputQ :236-240；onChunk :399-417——P5-1 组帧母本；kickSlowConsumerLocked :578-608——挂点 :599-602；writer :715-724 done 门形态；inputWriter 现状方法 :751-764；detach :775-815——挂点 :810-813；maybeExitWhenEmptyLocked :869-908；stopChildLocked :919-924——D-01 每会话化母本）
    - internal/server/resize.go（零值 arbiter 安全性论证：reportResize 成员守卫 :88-94、recalcNow 零值哨兵提前返回 :129-141——per-client 跳过 initArbiter 后全部天然 no-op 的实证）
    - internal/pty/spawn.go（StartWithSize :76 签名与「调用方已钳制」契约注释；Start 委托 :67-69；SpawnCols/SpawnRows 单一事实源 :34-41）
    - internal/pty/io.go（ReadLoop 缓冲复用红线 :11-15；Drain 带时限形态；SignalGroup 不取 fdMu；Close 幂等 fdMu+closed 形态 :65-76）
    - internal/proto/proto.go（ErrServerError :61；HelloPayload :93-97；WelcomeFrame :154-157；ErrorFrame :181；ExitFrame :189-192；ReadLimitPostAuth :76）
    - cmd/wesh/main.go（run() 分岔区 :1317-1345——spawnFunc 闭包 :1328-1334、ValidateOptions 调用 :1342、pty.Start 现状行 :1348-1352、listen 回滚块 :1353-1370、opts 字面量 :1384、New 调用 :1385、serve 失败回滚 :1483-1490；信号处理 :1451-1457）
    - .planning/phases/11-per-client/11-CONTEXT.md（D-01..D-06 全文 +「已锁定不重复决策」节 + specifics 窗口期登记）
    - .planning/phases/11-per-client/11-PATTERNS.md（§1 升档四母本 / §2 INPUT 切换 / §3 New 分岔 / §4 client 字段 / §5 detach·kick 挂点 / §6 perclient.go 全部件 / S1-S7 Shared Patterns + 行号勘误表）
    - internal/server/e2e_test.go（startTestServerWith harness :140-156 与 killServer 收口注释 :123-134——新夹具母本）
    - internal/server/exit_test.go（readExitClose/decodeExitFrame 三件套 :25-58）
    - internal/server/events_test.go（captureStderr + eventsNamed 形态 :427-460——session_start 缺席断言母本）
  </read_first>
  <behavior>
    - 测试先行（红）：先建 perclient_test.go harness + TestPerClientTwoClientsTwoPids/TestPerClientWelcomeDims 两冒烟测——此时编译失败（无 startPerClientServer 依赖的生产符号），随后实现转绿
    - per-client 实例（argv=[]string{"sh"}，Writable:true）启动后：两客户端 dialHello（cols/rows 各异）→ 各自 INPUT `echo WESHPID=$$\r` → 各自回读 WESHPID=(\d+)，两 pid 不等；A 端发唯一标记串，B 端 2s 静默窗（select+time.After 竞速形态，Read 永不带 per-read deadline）内零该标记
    - dialHello(cols=111, rows=44) → 首帧类型字节 proto.Welcome 且 JSON 载荷 cols==111/rows==44/mode=="rw"（Hello 钳制尺寸回显，无 80x24 中间态）
    - 共享模式既有全量测试（含 exit/emptyexit/shutdown/stopseq/resize/slowclient/multi 全部）原样全绿——零回归证据不动一行
  </behavior>
  <action>
    ① 新建 internal/server/perclient.go（文件头注释照 log.go:1-23 形态：包级职责 + D-01..D-06/PC-02..04 + 研究 §2.1/§3.1/§4.1/§5/Pitfall 2/3/5 锚点登记）。定义 pcSession 结构：sess *pty.Session；inQ *inputQ；inputDone chan struct{}（watcher 收口信号，shared s.inputDone 同构）；waitDone chan struct{}（watcher 的 Wait 返回即 close——teardown 慢半段与唯一收割者的同步边）；teardownDone chan struct{}（teardown 慢半段落定信号）；startedAt time.Time；exitCode int 与 reaped bool（均 hubMu 保护——pinger pongTimedOut 置位先例同形态，PATTERNS No Analog ③ 建议形态：与 hubMu 同锁免新锁类型）；teardownOnce sync.Once。字段注释对齐 client 结构纪律（写一次于升档 = sess/inQ/两 chan/startedAt，此后读者为该会话 goroutine 群与 hubMu 内读者，happens-before 由 goroutine 启动 + hubMu 建立）。
    ② perclient.go 实现 upgradePerClient：签名 `func (s *Server) upgradePerClient(ctx context.Context, c *websocket.Conn, remote, remoteUser string, h proto.HelloPayload, ticketMode string, cancel context.CancelFunc) *client`（拒绝/失败路径已自行写 Error+Close 并返回 nil，调用方据此直接 return）。序列严格按研究 §3.1 时序与 PATTERNS §1/§6b：单行门算 effMode（s.writable && ticketMode==proto.ModeRW → ModeRW 否则 ModeRO——decideModeLocked 不调用，write-policy 仲裁矩阵在 per-client 不装配）→ spawnFunc 调用（hubMu 之外——Anti-Pattern 1；h.Cols/h.Rows 已 ClampDim 钳制满足 StartWithSize 契约）→ 失败路径：Error 帧直写（proto.ErrorFrame(proto.ErrServerError, "failed to start process")——message 为定值常量，绝不携带底层错误细节，Pitfall 5 红线：注释以编号引用，不引字面量）→ logEvent(remote, websocket.StatusInternalError, "spawn_failed", remoteUser)（D-04 单行审计，四段 schema 零敏感值，PATTERNS S3 通道）→ Close(websocket.StatusInternalError, proto.ErrServerError) → return nil（失败点在注册之前：零 client 构造/零注册/零登记/无残余资源——spawnFunc 失败由 pty 包保证 fd 完好，Pitfall 5 清理清单；release() 已在分岔前执行，半开名额不泄漏）→ 成功路径：构造 pcSession（inQ = newInputQ(defaultInputQueueBytes)，两 chan 各自 make，startedAt = time.Now()）→ hubMu.Lock 内：构造 client（conn/remote/remoteUser/dims{h.Cols,h.Rows}/outbox/done/cancel/limiter 字段与 shared 升档 :983-997 同形同值，rwEligible 零值 false 并注释「per-client 无 owner 递补语义——promoteNextLocked 永不可达」；inQ 赋 pc.inQ、pc 赋 pc）→ cl.mode.Store(effMode) → prefs 双档选档（effMode==ModeRW 取 clientPrefsRW 否则 clientPrefsRO，ro 档永不含 osc52 纪律不动）→ Welcome 入队（proto.WelcomeFrame(effMode, prefs, h.Cols, h.Rows)——本端钳制尺寸回显，不经 sessionDimsLocked）→ s.registry.registerLocked(cl) → s.pcSessions[pc] 登记 → hubMu.Unlock → attach 事件 emitEvent（:1047-1056 同形态同字段集：event=attach + remote + client_id=cl.attachSeq + mode + remote_user 非空出键）→ c.SetReadLimit(proto.ReadLimitPostAuth) → 调 startSessionGoroutines → return cl。**本阶段不在此函数内装容量再闸与注册点复检**——归 11-03（D-02/D-03），函数内以注释锚定预留位序（闸在 spawn 前、复检在 registerLocked 同一 hubMu 持有内）。
    ③ perclient.go 实现 startSessionGoroutines(ctx, cl, pc)：按序启动五件——go s.writer(cl)；go s.pinger(ctx, cl, s.pingInterval)；go pc.sess.ReadLoop(闭包)（闭包体逐字按研究 §3.5/PATTERNS §6c：detach 门 select case <-cl.done return default 提前返回——SIGHUP→死亡窗口期不做无谓组帧；s.mc.ptyOutputBytes.Add(int64(len(chunk)))——同一计数器聚合口径不变；每帧 make([]byte, 1+len(chunk)) + frame[0]=proto.Output + copy（P5-1 别名红线，ReadLoop 缓冲复用 io.go:14）；cl.outbox.trySend(frame) 失败 → 取 hubMu 调 kickSlowConsumerLocked(cl) 再放锁——1013 既有机械零改动复用，arbiter 零值下 removeMember/recalcNow 天然 no-op）；go inputWriter(pc.sess, pc.inQ, pc.inputDone)；go s.sessionWatcher(cl, pc)。注释登记 goroutine 拓扑（每客户端 6 = reader+writer+pinger+ReadLoop 闭包+inputWriter+sessionWatcher，研究 §5 表）与 Welcome 恒首帧论证（入队先于注册、goroutine 群注册后启动、spawn→ReadLoop 间输出由 64KiB 内核缓冲承接）。
    ④ perclient.go 实现 sessionWatcher：pc.sess.Wait()（唯一收割者纪律每会话粒度——断开路径绝不自己 Wait）→ 退出码提取内联三行（code 初值 0 + errors.As 取 *exec.ExitError → ee.ExitCode()；信号死亡 ExitCode()=-1 语义免费——lifecycle :1361-1365 同形，不抽 helper 保持 lifecycle 逐字不动）→ close(pc.waitDone) → hubMu.Lock 内置 pc.exitCode=code、pc.reaped=true 并调 s.teardownPCLocked(pc)（同锁持有内触发——若断开路径已先触发则 Once 去重无操作；若本路径先触发，reaped 已在同临界区置位，快半段栅栏成立）→ hubMu.Unlock → 等 <-pc.teardownDone（Drain/Close/注册表移除落定后再发 EXIT——终结输出先经 outbox 送达的时序与 lifecycle :1386-1393 同序）→ close(pc.inputDone)（inputWriter 收口，shared close(s.inputDone) 同构）→ EXIT 私有化直写（proto.ExitFrame(code, exitMessage(err, code)) 组帧一次 → context.WithTimeout 2s 的 wctx 同步 cl.conn.Write → cancel() → cl.conn.Close(websocket.StatusNormalClosure, "")——lifecycle :1424-1431 直写纪律逐字形态；Write 失败不补救直接 Close，server.go:1399-1400 授权先例；与 Shutdown 1001 竞态由库 Close 幂等承接）。**watcher 不 emit session_end**（D-04 窗口期空白，注释锚定 Phase 13 补齐 + client_id 关联键）。
    ⑤ perclient.go 实现 teardownPCLocked（调用方必须已持 hubMu——Locked 后缀纪律；三调用点：detach 挂点、kick 挂点、sessionWatcher 同锁持有内）：teardownOnce.Do 内分两半——快半段（hubMu 持有内同步执行）：若 !pc.reaped 则 pc.sess.SignalGroup(s.stopSignal)（默认 HUP=milestone 字面语义，OPS-04 --stop-signal 可配通道自然继承；SignalGroup 不取 fdMu，hubMu 内发送安全，锁序不受影响）且 s.stopTimeout>0 时 time.AfterFunc(s.stopTimeout, 闭包)——闭包内先取 hubMu 再复检 !pc.reaped 才补 SignalGroup(syscall.SIGKILL)（**planner 裁定：reaped 栅栏覆盖补 KILL 发信号点**——Pitfall 2「信号与 reap 锁内序列化」语义对一切 kill(-pgid) 同构适用，不只 SIGHUP；pinger pongTimedOut 置位取 hubMu 先例同形态；timer 随会话消亡）；慢半段（go func 异步——Drain/Close/Wait 等待绝不在 hubMu 内，Pitfall 3 行头阻塞红线）：pc.sess.Drain(200 * time.Millisecond) → pc.sess.Close() → <-pc.waitDone → hubMu.Lock 内 delete(s.pcSessions, pc)（注册表/容量记账单点移除——map delete 幂等，双触发路径天然恰好一次）→ hubMu.Unlock → close(pc.teardownDone)。
    ⑥ clients.go：client 结构尾部（pongTimedOut :144-148 之后）追加 inQ *inputQ 与 pc *pcSession 两字段，注释照 remoteUser :98-105「写一次+happens-before 论证」形态（写一次于升档 registerLocked 之前，读者为该会话 goroutine 群与 hubMu 内读者；inQ：shared=s.inputQ / per-client=pc.inQ——读循环零分支的关键 Pattern 2；pc：shared 恒 nil——detach/kick 的 teardown 触发以 pc!=nil 为门）。inputWriter 由 (s *Server) 方法改为包级函数 inputWriter(sess *pty.Session, q *inputQ, done chan struct{})：函数体 :751-764 逐字保留仅替换三处引用（s.inputDone→done、s.inputQ→q 两处、s.sess.Master.Write→sess.Master.Write），CR-01 注释整块随迁。detach（:810-813 区）在 promoteNextLocked 块之后、maybeExitWhenEmptyLocked 调用之前插入 per-client 挂点：if c.pc != nil { s.teardownPCLocked(c.pc) }（注释：Anti-Pattern 7 挂注册表移除点覆盖一切断开形态，与 maybeExitWhenEmptyLocked 同位同款纪律 :810-812）。kickSlowConsumerLocked（:599-602 区）同位同款插入同一挂点。maybeExitWhenEmptyLocked 函数首行插入 per-client 早退守卫：if s.sessionMode == SessionModePerClient { return }（注释锚定：per-client --once/exit-when-empty 语义归 Phase 13 第二终结源——Pitfall 1 窗口期；本守卫同时是 s.sess 恒 nil 下 stopChildLocked 的 nil-deref 防线）。
    ⑦ server.go：New 入口在 SessionMode 零值归一（:392-393）之后、Server 字面量装配之前插入装配契约 panic 两行——per-client × sess!=nil → panic("server: session-mode per-client requires nil sess")；shared × sess==nil → panic("server: session-mode shared requires non-nil sess")（**planner 裁定**：sess 维度契约不经 ValidateOptions 签名扩展——该签名变更会迫使 options_test.go 既有五行改动输入面，破坏 D-05「既有测试一字节不动」与 10-review WR-02 前移纪律；sess 与 mode 在 New 边界才同时具备，程序错误 panic 与 New 现状 :489 裸 nil-deref 同档但文案可诊断；研究 §2.1 原始建议即此形态；PATTERNS §8E 的 options_test 加行建议由本裁定取代；main 两分支结构性满足契约，生产不可达，防御面=测试与未来调用方）。ValidateOptions 头注释 :327-328 的「sess 维度规则归 Phase 11」预言句更新为指向 New 入口契约（注释更新非行为变更）。New 尾部（:481-496）模式分岔：hubCond 构造（:481）两模式共用逐字不动；之后 if s.sessionMode == SessionModePerClient 分支——仅 s.pcSessions = make(map[*pcSession]struct{}) + return s，注释登记：零全局 goroutine（pcSupervisor/pcExitReq 归 Phase 13）；不装 initArbiter（arbiter 零值下 reportResize 成员守卫/recalcNow 零值哨兵/removeMember nil-map delete 全部天然 no-op，resize.go:88-141 实证——RESIZE 在 per-client 本阶段静默无效为已知中间态，直通归 Phase 12）；不 emit session_start、不置 sessionAlive（D-04 窗口期——/healthz session_active 恒 false 与 wesh_session_active 恒 0 为 11→13 已知中间态，语义裁决归 Phase 13 OQ①②）。else 分支 = :482-495 现状逐字（initArbiter/startedAt/session_start emit/sessionAlive.Store(true)/go sess.ReadLoop(s.onChunk)/go inputWriter(s.sess, s.inputQ, s.inputDone)——仅 inputWriter 调用点随参数化改传参/go s.lifecycle()）。Server 结构追加 pcSessions map[*pcSession]struct{} 字段（:173-180 区注释同款——hubMu 保护，per-client 会话活性登记 ≠ registry：客户端断开后会话仍可存活至收割）。
    ⑧ server.go Attach 升档分岔：在 close(helloDone)+release()（:979-980，两模式共用逐字不动）之后插入模式分岔——if s.sessionMode == SessionModePerClient { cl = s.upgradePerClient(ctx, c, remote, remoteUser, h, mode, cancel); if cl == nil { return } } else { :981-1070 shared 升档序列逐字不动 }（分岔整块注释：装配期一次分岔、运行期零分岔——分支点 ②；per-client 分支不调用 decideModeLocked/participates/addMember/recalcNow/sessionDimsLocked/SignalForegroundGroup——:1008-1012 与 :1070 整段为 shared-only，子进程以正确尺寸 spawn 无重绘需求）。读循环（:1073 起）两模式共用。INPUT case 唯一改动行 :1120：入队目标由 s.inputQ 改为 cl.inQ（PATTERNS §2 逐字母本——mode 门 :1101 与限速门 :1111 逐字不动；cl==nil 守卫对两模式同形保持；CR-01 注释块 :1115-1119 更新引用为 cl.inQ 间接字段论证，「读循环零同步写」结论不变）。Shutdown（:1508-1512）stop-signal 段加模式守卫：per-client 分支空操作 + 注释锚定「1001 广播引发的 detach 经挂点各自 SIGHUP 其会话；残留待收割会话的 N 组快照信号归 Phase 13（D-01 注记窗口期）」；else 分支现状三行逐字不动。
    ⑨ cmd/wesh/main.go run() 收编（:1317-1352 区）：10-01 注释预言兑现——StartOptions 字面量收编为分支前单一声明 startOpts := pty.StartOptions{Dir: cfg.cwd, Term: cfg.term, Uid: cfg.uid, Gid: cfg.gid}；spawnFunc 闭包体改为 pty.StartWithSize(argv, startOpts, cols, rows)（行为不变，注释更新：去除「零调用方 inert」改为「attach 升档期消费，11-01 起生效」）；pty.Start 现状行（:1348）移入 else（shared）分支——per-client 分支 sess 保持 nil 并注释「启动期零子进程：spawn 移至 attach 升档（ticket 核销后，SEC-08；启动期命令缺失暴露已由 validateStartup LookPath 预检承担，10-02 SC4）」；声明形态改为 var sess *pty.Session 前置 + 分支内 sess, err = pty.Start(argv, startOpts)（err 复用外层声明，值与现状逐字节相同）；listen 失败回滚块（:1364-1369）与 serve 失败回滚块（:1488）的 sess.Close 调用各加 nil 守护（per-client 无 sess 可回滚）；ValidateOptions 调用点（:1342）与 opts 字面量（:1384）、New 调用（:1385）逐字不动。10-01 planner 裁定注释块（:1323-1327）更新为「Phase 11 已落地 sess=nil + attach 期 spawn」事实陈述。
    ⑩ 新建 internal/server/perclient_test.go（package server，D-05 独立文件——本阶段不碰任何既有测试文件装配点；文件头注释登记 D-05 与 Pitfall 11 纪律）。harness startPerClientServer(t *testing.T, argv []string, mutate func(*Options)) (exitCh chan int, wsURL string)：SpawnFunc 闭包捕获 argv + pty.StartOptions{Uid: -1, Gid: -1} 直通 pty.StartWithSize，成功即追加进 mu 保护的 spawned 追踪切片；New(nil, exitf 捕获桩, Options{SessionMode: SessionModePerClient, SpawnFunc: 闭包, Writable: true} + mutate 覆写)；127.0.0.1:0 listen + go http.Serve + t.Cleanup（ln.Close + 逐 spawned sess Kill+Close）——killServer :123-127 的「泄漏子进程在 CPU 受限 CI 上级联减速」实证注释逐字重现于新夹具。dialHello 复用 e2e_test.go 同包既有形态（cols/rows 参数化签名，按其实际签名调用）。新文件内 helper readOutputUntil(t, c, pattern)（OUTPUT 帧剥类型字节累积，10s 统护 ctx + select/time.After 竞速形态——stopseq_test.go:30-31 夹具纪律红线逐字：Read 永不带 deadline ctx）。两冒烟测：TestPerClientTwoClientsTwoPids（behavior 块形态：两端 WESHPID 回读不等 + A 唯一标记 B 端 2s 静默窗零命中）与 TestPerClientWelcomeDims（111x44 首帧 Welcome 三键断言）。
    ⑪ 自检零回归：shared 分支全部既有行逐字未动（例外仅 INPUT case :1120 入队目标、inputWriter 调用点传参、Shutdown/ maybeExitWhenEmptyLocked 两处守卫插入、New 尾部 else 归位、server.go:327-328 注释更新——全部行为等价）；`time go build ./...` 通过；gofmt（GOROOT 版本，10-05 收口闸工具纪律）无 diff。
    提交：`feat(11-01): per-client lifecycle backbone — attach spawn/Welcome echo/5-goroutine assembly/sessionWatcher private EXIT/teardown-once SIGHUP+KILL/New mode fork/main sess=nil (PC-02/PC-03/PC-04, D-01/D-04)`
  </action>
  <verify>
    <automated>time go build ./... && go vet ./... && go test -race -count=1 ./internal/server/ -run 'TestPerClient' -v && go test -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` 成功；`go vet ./...` 无输出；`go test -race -count=1 ./...` 全仓全绿（含两新冒烟测；既有测试文件零改动——本任务 git diff 中 *_test.go 仅 perclient_test.go 新建）
    - `grep -c 'type pcSession struct' internal/server/perclient.go` == 1 且 `grep -c 'func (s \*Server) sessionWatcher' internal/server/perclient.go` == 1 且 `grep -c 'func (s \*Server) teardownPCLocked' internal/server/perclient.go` == 1 且 `grep -c 'func (s \*Server) upgradePerClient' internal/server/perclient.go` == 1
    - `grep -c 'cl\.inQ\.tryEnqueue' internal/server/server.go` == 1 且 `grep -vE '^[[:space:]]*//' internal/server/server.go | grep -c 's\.inputQ\.tryEnqueue'` == 0
    - `grep -c 'func inputWriter(' internal/server/clients.go` == 1 且 `grep -c 'go inputWriter(s\.sess, s\.inputQ, s\.inputDone)' internal/server/server.go` == 1
    - `grep -c 's\.teardownPCLocked(c\.pc)' internal/server/clients.go` == 2（detach 与 kick 两挂点）
    - `grep -c 'session-mode per-client requires nil sess' internal/server/server.go` ≥ 1 且 `grep -c 'session-mode shared requires non-nil sess' internal/server/server.go` ≥ 1
    - `grep -c 's\.pcSessions = make(map\[\*pcSession\]struct{})' internal/server/server.go` == 1
    - `grep -vE '^[[:space:]]*//' internal/server/perclient.go | grep -c 'err\.Error'` == 0（失败文案零底层错误细节——Pitfall 5；注释以编号引用红线）
    - `grep -c 'cl\.conn\.Write' internal/server/perclient.go` ≥ 1 且 `grep -c 'StatusNormalClosure' internal/server/perclient.go` ≥ 1（EXIT 直写序）
    - `grep -c '"spawn_failed"' internal/server/perclient.go` == 1 且 `grep -c 'ErrServerError, "failed to start process"' internal/server/perclient.go` == 1
    - `grep -c 'pty\.StartWithSize(argv, startOpts, cols, rows)' cmd/wesh/main.go` == 1 且 `grep -c 'pty\.Start(argv, startOpts)' cmd/wesh/main.go` == 1 且 `grep -c 'if sess != nil' cmd/wesh/main.go` ≥ 2
    - 行为断言：TestPerClientTwoClientsTwoPids 两端回读 pid 不等且 A 端唯一标记 B 端静默窗零命中；TestPerClientWelcomeDims 首帧 Welcome cols==111/rows==44
  </acceptance_criteria>
  <done>per-client 模式端到端可用：attach 即独立 spawn（Hello 尺寸直通）、断开即 SIGHUP（teardown Once 全序列含 KILL 兜底分支）、子死仅本端 EXIT+1000；shared 逐字节零回归（全量 -race 绿、既有测试零改动）；main per-client 启动期零子进程；New sess×mode 契约 panic 落地。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 装配契约与输入链测试——TestPerClientInputEcho/TestNewModeSessContract/TestPerClientNoSessionStartEvent + 零回归收口闸首跑</name>
  <files>internal/server/perclient_test.go</files>
  <read_first>
    - internal/server/perclient_test.go（Task 1 落地后的 harness 与两冒烟测现状——追加位）
    - internal/server/events_test.go（captureStderr 置换/恢复形态 + eventsNamed 过滤 + session_start 键断言 :427-460——缺席断言的阴性形态母本）
    - internal/server/server.go（Task 1 落地后的 New 入口契约两行与 New 尾部分岔——断言目标形态）
    - internal/server/e2e_test.go（dialHello INPUT 发送与 OUTPUT 回读形态——输入回显测试母本）
    - internal/server/options_test.go（ValidateOptions 表 :17-46——**本文件一字节不动**，仅阅读以确认 Task 1 裁定未触及）
    - internal/server/health.go（session_active 数据源 :44——中间态断言依据）
  </read_first>
  <behavior>
    - TestPerClientInputEcho：rw 客户端 INPUT `echo ECHOMARK_9z2\r` → 输出流含 ECHOMARK_9z2 结果行（cl.inQ→每会话 inputWriter→master 全链实证）
    - TestNewModeSessContract：New(nil, exitf 桩, Options{})（零值归一 shared）→ recover 到含 "requires non-nil sess" 的 panic；New(&pty.Session{}, exitf 桩, Options{SessionMode: SessionModePerClient, SpawnFunc: 非 nil 占位闭包}) → recover 到含 "requires nil sess" 的 panic（两 panic 均在 goroutine 启动前触发，零残留——零值 &pty.Session{} 仅作非 nil 哨兵，永不 Start）
    - TestPerClientNoSessionStartEvent：captureStderr 窗口内构造 per-client server → 事件流零 session_start 行（D-04 窗口期空白锁）；GET /healthz → 200 且 session_active==false（11→13 已知中间态显式锁——Phase 13 语义裁决落地时本断言随之翻转）
    - 收口闸：全量 -race 绿；phase02-09.mjs 八脚本默认模式零修改重跑 exit 全 0
  </behavior>
  <action>
    ① perclient_test.go 追加 TestPerClientInputEcho（harness 起 per-client server，dialHello 后发送 INPUT 帧——append([]byte{proto.Input}, []byte("echo ECHOMARK_9z2\r")...) 形态，readOutputUntil 断言标记回读；标记串含唯一片段防命令原文自命中——参照 phase06.mjs readPid「正则只命中结果行」纪律的 Go 同构）。
    ② 追加 TestNewModeSessContract（表驱动两形态：每子测 defer recover 捕获，断言 panic 值含锁定文案子串；两形态均断言 exitf 桩未被调用——装配失败零终结事件）。注释登记 planner 裁定：sess 维度契约承载于 New 入口 panic 而非 ValidateOptions 签名扩展（D-05/WR-02 纪律调和，PATTERNS §8E 建议被取代）。
    ③ 追加 TestPerClientNoSessionStartEvent（captureStderr 置换 → startPerClientServer 构造 → 恢复 → eventsNamed(evs, "session_start") 长度 == 0；随后 HTTP GET /healthz 断言 200 + body JSON session_active==false——注释锚定 D-04 与 Phase 13 OQ①② 窗口期，**Phase 13 落地时本测试断言按裁决翻转**）。
    ④ 收口闸首跑：`$(go env GOROOT)/bin/gofmt -l .` 零输出（10-05 纪律：GOROOT gofmt 为收口闸工具）；`go test -race -count=1 ./...` 全绿；构建二进制（time go build -o /tmp/wesh-p11/wesh ./cmd/wesh）后按序跑既有协议 UAT 八脚本——node web/uat/phase02.mjs /tmp/wesh-p11/wesh 起至 phase09.mjs（phase04/05/06 的 -dom/-dims/-flood-driver/-t1-width 辅助脚本与 phase07-b*.sh 非主脚本不在此列，10-04 收口口径为八主脚本）——默认 shared 模式零修改重跑，exit 全 0 且无 FAIL 断言行；git diff 审查：既有 *_test.go 与 web/uat/*.mjs 零改动（本 plan 允许的新文件 = perclient_test.go，其余一律不出现）。
    提交：`test(11-01): per-client assembly contract + input chain + zero-session-event locks, first regression gate run (PC-02/PC-03, D-05)`
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/server/ -run 'TestPerClientInputEcho|TestNewModeSessContract|TestPerClientNoSessionStartEvent' -v && go test -race -count=1 ./... && time go build -o /tmp/wesh-p11/wesh ./cmd/wesh && for f in web/uat/phase02.mjs web/uat/phase03.mjs web/uat/phase04.mjs web/uat/phase05.mjs web/uat/phase06.mjs web/uat/phase07.mjs web/uat/phase08.mjs web/uat/phase09.mjs; do node $f /tmp/wesh-p11/wesh || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - 三新测全 PASS（含 panic 文案两子串锁定与 exitf 零调用断言）
    - `grep -c 'TestPerClientInputEcho\|TestNewModeSessContract\|TestPerClientNoSessionStartEvent' internal/server/perclient_test.go` == 3
    - `grep -c 'requires nil sess\|requires non-nil sess' internal/server/perclient_test.go` ≥ 2
    - `go test -race -count=1 ./...` 全仓全绿；`$(go env GOROOT)/bin/gofmt -l .` 零输出
    - 既有 UAT 八脚本零修改重跑 exit 全 0（默认模式 = shared 零回归脚本级证据）
    - `git diff --stat` 中不存在任何既有 *_test.go / web/uat/*.mjs 的改动（D-05 零回归证据不动；options_test.go 一字节不动）
  </acceptance_criteria>
  <done>装配契约（sess×mode panic / 零 session_start / session_active 中间态）与输入链（INPUT→inQ→inputWriter→master→回显）测试锁定；零回归双证据（Go 全量 + 八脚本）首跑全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 已认证 WS 客户端 → attach 升档 spawn 路径 | ticket 核销是唯一资源创建前置；升档后每连接 = 一个 PTY 子进程 + 一套 goroutine（昂贵资源） |
| 服务端 → 宿主机进程组（kill(-pgid) 信号面） | 断开/超时路径向子进程组发信号；pgid 复用窗口内误发 = 杀宿主机无关进程组 |
| 子进程 → 客户端（OUTPUT/EXIT 数据面） | per-client 输出与退出码只许到达属主客户端（会话状态越权可见 = 信息泄露） |
| goroutine 拓扑（teardown × watcher × detach 竞态面） | 双路终结触发并发到达；锁序与恰好一次语义是结构性防线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01a | Elevation of Privilege | 预认证 spawn（绕过 ticket 闸的匿名 fork 面） | critical | mitigate | spawn 点锁定 checkTicket 成功之后（Anti-Pattern 3；SEC-08）；Task 1 acceptance 源码断言 spawnFunc 调用点位于升档分岔内；11-03 失败清理清单测试复核零注册零残留 |
| T-11-01b | Information Disclosure | spawn 失败文案携带路径/errno/系统调用细节 | high | mitigate | message 定值常量 "failed to start process"；负向 grep 闸（perclient.go 过滤注释后零底层错误串拼接）；logEvent spawn_failed 四段 schema 零敏感值（D-04） |
| T-11-01c | Tampering | kill-after-reap：pgid 复用窗口内 SIGHUP/SIGKILL 打中无关进程组 | high | mitigate | reaped 栅栏：hubMu 内标志位，信号与 Wait 收割锁内序列化（Pitfall 2），覆盖 SIGHUP 与 AfterFunc 补 KILL 两发信号点；唯一收割者纪律保持（cmd.Wait 仅 sessionWatcher）；11-04 竞态注入测试 + -race 全量 |
| T-11-01d | Denial of Service | hubMu 横跨 spawn/Drain/Wait 阻塞冻结全控制面 | high | mitigate | Anti-Pattern 1 锁序：闸内读计数 → 放锁 spawn → 再取锁注册；teardown 慢半段独立 goroutine（Drain/Close/waitDone 等待均不占 hubMu）；Task 1 acceptance 挂点形态断言 + 11-06 全量 -race |
| T-11-01e | Repudiation | per-client 会话生命周期审计空白（无 session_start/session_end） | low | accept | D-04 明示接受的 11→13 窗口期：Phase 13 以 per-client 粒度一次补齐（client_id 关联键）；本阶段 spawn_failed/max_clients 两事件先行；TestPerClientNoSessionStartEvent 显式锁定空白语义防漂移 |
| T-11-SC | Tampering | 依赖供应链（go get/包安装） | high | accept | 零新依赖（研究 STACK.md 锁定——全部机制既有六依赖覆盖）；本 plan 无 go get/包安装任务，go.mod/go.sum 零漂移 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V5 Input Validation（Hello cols/rows 钳制 / ticket 核销前置） | ✓ DecodeHello ClampDim 既有 + spawn 点后置（Task 1） |
| V7 Error Handling（错误文案不含敏感值/内部细节） | ✓ 定值常量文案 + logEvent 零敏感值 + 负向 grep 闸（Task 1） |
| V8 Audit（审计事件零敏感值、可检索归因） | ✓ spawn_failed 四段 schema 沿用 logEvent 既有通道（Task 1） |
</threat_model>

<verification>
- `go test -race -count=1 ./...` 全仓全绿（shared 期望值逐字未动——零回归收口闸；禁止断言放宽成「两模式都接受」）
- 既有协议 UAT 八脚本（phase02-09.mjs 主脚本）默认模式零修改重跑 exit 全 0
- `git diff --stat` 对既有 *_test.go / web/uat/*.mjs / internal/proto/ 零改动；main.go 改动仅限 run() 分岔区
- per-client 冒烟：双端双 pid 不等、Welcome 回显 Hello 钳制尺寸、输入回显全链通
</verification>

<success_criteria>
- ROADMAP SC1：两客户端 attach 各得独立 shell（协议层双 pid 可观测、互不串台）且首帧 winsize = Hello 钳制尺寸——perclient_test.go 两冒烟测锁定
- ROADMAP SC2 前半：spawn 失败 wire 面（Error{server_error} + 1011 + spawn_failed 事件）代码路径就位（行为全断言归 11-03 失败桩测试）
- ROADMAP SC3/SC4 的机制本体：teardown Once 全序列（SIGHUP 经栅栏 → KILL 兜底 → Drain → Close → 收割等待 → 注册表单点移除）与 EXIT 私有化直写全部就位（行为断言归 11-04/11-05）
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **PC-02/PC-03/PC-04 (unclassified, unresolved)**：自动边缘探针未能对三条中文需求分类——人工裁定：全部可断言边缘已由 11-CONTEXT.md D-01..D-06 与 specifics 实质覆盖（运行期删命令注入、trap '' HUP KILL 兜底、pgid ESRCH 断言通道、容量再闸 wire 形态、复检回收），本 plan 将 CONTEXT 已锁内容直接转为 must_haves.truths；probe 未覆盖缝隙即下方窗口期项。三行保持 flagged-unverified 不自动闭合，由 11-04/11-05/11-06 的行为断言与收口闸承载显式证据。
- **已知中间态 ①（明示接受，非缺口）**：per-client 下 --once/--exit-when-empty 本阶段永不退出——maybeExitWhenEmptyLocked 早退守卫 + 注释锚定；第二终结源（pcSupervisor/pcExitReq）归 Phase 13（Pitfall 1）。
- **已知中间态 ②（明示接受）**：per-client 下 SIGTERM 后进程不自退——Shutdown 1001 广播 → detach → teardown 覆盖在线会话（残留待收割会话的 N 组快照归 Phase 13）；进程退出依赖 operator 第二信号（stopSignals 已恢复默认处置，main.go:1455 既有逃生形态）或 systemd SIGKILL 兜底。
- **已知中间态 ③（明示接受）**：/healthz session_active 恒 false、wesh_session_active 恒 0（sessionAlive 不置位、metrics.go 零改动——D-04）；语义裁决归 Phase 13 OQ①②，TestPerClientNoSessionStartEvent 的 /healthz 断言届时按裁决翻转。
- **已知中间态 ④（明示接受）**：RESIZE 在 per-client 下经零值 arbiter 天然 no-op（直通归 Phase 12）；ro 门控语义经单行门免费成立但断言归 Phase 12；write-policy×per-client 的 validateStartup warn 行已在 10-02 落地。
- **planner 裁定记录（executor 不得回改）**：① sess×mode 契约以 New 入口程序错误 panic 承载（不改 ValidateOptions 签名——保 D-05 与 WR-02；PATTERNS §8E options_test 加行建议被取代，server.go:327-328 注释同步更新指向）；② reaped 栅栏覆盖 AfterFunc 补 KILL 发信号点（Pitfall 2 语义对一切 kill(-pgid) 同构，不只 SIGHUP——D-01 序列内延伸）。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- 类型 `pcSession`（internal/server/perclient.go——sess/inQ/inputDone/waitDone/teardownDone/startedAt/exitCode/reaped/teardownOnce）
- 方法 `(s *Server) upgradePerClient(...)` / `startSessionGoroutines(ctx, cl, pc)` / `sessionWatcher(cl, pc)` / `teardownPCLocked(pc)`（internal/server/perclient.go）
- 字段 `Server.pcSessions map[*pcSession]struct{}`（internal/server/server.go）
- 字段 `client.inQ *inputQ` / `client.pc *pcSession`（internal/server/clients.go :95-160 尾部）
- 函数 `inputWriter(sess *pty.Session, q *inputQ, done chan struct{})` 包级参数化形态（internal/server/clients.go——原方法形态移除；server.go:494 调用点改传参）
- New 入口装配契约 panic 两行 + New 尾部模式分岔（internal/server/server.go）
- Attach 升档 per-client 分岔 + INPUT case :1120 一行切换 + Shutdown stop-signal 段守卫 + maybeExitWhenEmptyLocked 早退守卫（internal/server/server.go / clients.go）
- run() per-client sess=nil 切换 + startOpts 单一声明收编 + 两处回滚 nil 守护（cmd/wesh/main.go）
- 测试 harness `startPerClientServer` + helper `readOutputUntil`（internal/server/perclient_test.go 新文件）
- 测试 `TestPerClientTwoClientsTwoPids` / `TestPerClientWelcomeDims` / `TestPerClientInputEcho` / `TestNewModeSessContract` / `TestPerClientNoSessionStartEvent`（internal/server/perclient_test.go）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/11-per-client/11-01-SUMMARY.md` when done
</output>
