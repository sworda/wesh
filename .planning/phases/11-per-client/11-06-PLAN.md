---
phase: 11-per-client
plan: 06
type: execute
wave: 4
depends_on: ["11-02", "11-04", "11-05"]
files_modified: []
autonomous: true
requirements:
  - PC-02
  - PC-03
  - PC-04

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "全量静态面干净：GOROOT gofmt（go1.26.3 现代 doc-comment 规则——10-05 收口闸工具纪律）`gofmt -l .` 零输出；`go vet ./...` 无输出；`time go build ./...` 成功"
    - "全量 Go 测试双证据：`go test -race -count=1 ./...` 全仓全绿——shared 既有全部测试原样绿（零回归第一证据）+ per-client 十四测（11-01 五测 + 11-03 三测 + 11-04 六测）绿"
    - "darwin 编译闸：GOOS=darwin go build ./... 与 GOOS=darwin go vet ./... 成功（11-02 dup-watch 防御的编译面实证；运行态由 CI macOS leg 承担）"
    - "既有协议 UAT 零回归脚本级证据：phase02-09.mjs 八主脚本默认 shared 模式零修改重跑 exit 全 0，PASS 计数与 v1.0 基线一致（10-04 收口口径同形态）"
    - "phase11.mjs 八场景全绿（D-06 兑现）：S1-S8 PASS + S4b skipped 带 reason"
    - "期望值逐字未动 diff 审查（Pitfall 11「看似完成实则不然」行的正面证据）：phase 基点（git merge-base main）以来——既有 *_test.go 零改动；export_test.go 仅 append（无删除行）；web/uat/ 仅新增 phase11.mjs；internal/proto/、web/src/、internal/server/metrics.go、health.go、resize.go 全部零改动（协议零改动 / 前端零改动 / D-04 metrics 零改动三红线）"
    - "每阶段收口闸纪律复核：本 phase 任何 plan 未引入「两模式都接受」断言放宽形态——逐 plan SUMMARY 与 git diff 双重复核"
  artifacts: []
  key_links:
    - from: "本 plan 收口闸"
      to: "11-01..11-05 全部交付物"
      via: "目标态总账——六段式验证（gofmt/vet/build/-race 全量/双平台编译闸/UAT 双证据/diff 审查）一次性收口"
      pattern: "go test -race"
  prohibitions:
    - statement: "MUST NOT 为让收口闸转绿而改动任何既有测试期望值或既有 UAT 脚本（零回归证据的定义即『原样绿』——改动即证伪，宁可回退实现）"
      status: unresolved
      verification: null
    - statement: "MUST NOT 在收口阶段补做任何 Phase 12/13 范围的行为（RESIZE 直通/ro 断言/pcSupervisor/metrics 扩展——范围蔓延即漂移源）"
      status: unresolved
      verification: null
---

<objective>
Phase 11 收口闸：六段式验证一次跑齐——静态面（gofmt/vet/build）、全量 -race（shared 原样绿 + per-client 十四测）、GOOS=darwin 编译闸、既有协议 UAT 八脚本零修改重跑、phase11.mjs 八场景全绿、期望值逐字未动 diff 审查。

Purpose: 「最大风险 = 破坏既有不变量而不自知」（SUMMARY.md 方法论警告）——收口闸是零回归承诺的最终证据面；每一段的口径都在 Phase 10（10-04）与本 phase 各 plan 内预演过，本 plan 只做总账复核与证据落档，不做任何行为改动。
Output: 11-06-SUMMARY.md 承载全量证据（命令 + 退出码 + PASS 计数 + diff 清单）；Phase 11 三条需求（PC-02/PC-03/PC-04）的显式证据链闭合。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-per-client/11-CONTEXT.md
@.planning/phases/11-per-client/11-01-PLAN.md
@.planning/phases/11-per-client/11-02-PLAN.md
@.planning/phases/11-per-client/11-03-PLAN.md
@.planning/phases/11-per-client/11-04-PLAN.md
@.planning/phases/11-per-client/11-05-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 静态面 + 全量 -race + GOOS=darwin 编译闸</name>
  <precondition>11-01..11-05 的全部 SUMMARY 已存在且各自收口为绿（.planning/phases/11-per-client/ 目录下 11-01-SUMMARY.md 至 11-05-SUMMARY.md 可读）——本 plan 只复核总账，不修补</precondition>
  <files></files>
  <read_first>
    - .planning/phases/11-per-client/11-01-SUMMARY.md 至 11-05-SUMMARY.md（各 plan 自报证据——总账复核的输入）
    - .planning/phases/10-mode-assembly/10-04-SUMMARY.md（收口六段式口径与记录形态先例 :98-100）
  </read_first>
  <action>
    ① 静态面三段：`$(go env GOROOT)/bin/gofmt -l .`（零输出——GOROOT gofmt 为唯一收口闸工具，10-05 纪律：新旧 gofmt 双 clean 判定以 GOROOT 版为准）；`go vet ./...` 无输出；`time go build ./...` 成功（time 前缀项目纪律）。
    ② 全量 -race：`go test -race -count=1 ./...` 全仓全绿并记录耗时与包清单；确认 per-client 十四测在 internal/server 包输出中逐名出现（-run 复核一次：`go test -race -count=1 ./internal/server/ -run 'TestPerClient|TestNewModeSessContract' -v` 名单对账——五 + 三 + 六 = 十四）。
    ③ darwin 编译闸：`GOOS=darwin go build ./...` 与 `GOOS=darwin go vet ./...` 成功（reap_darwin.go 防御面 + 全仓 darwin 编译完整性）。
    ④ 任一段失败 = 回本 plan 之外修复（先定位归属 plan 回补，重跑该 plan 收口后再进本 plan ②）——本任务不携带任何修复动作。
    提交：本任务纯验证零代码改动，无 commit；证据落 11-06-SUMMARY.md。
  </action>
  <verify>
    <automated>$(go env GOROOT)/bin/gofmt -l . && go vet ./... && time go build ./... && go test -race -count=1 ./... && GOOS=darwin go build ./... && GOOS=darwin go vet ./... && go test -race -count=1 ./internal/server/ -run 'TestPerClient|TestNewModeSessContract' -v</automated>
  </verify>
  <acceptance_criteria>
    - gofmt 输出为空（命令无输出即过——注意该命令成功时 stdout 为空，gofmt -l 的零输出 = 零未格式化文件）
    - `go vet ./...` 无输出；`time go build ./...` exit 0
    - `go test -race -count=1 ./...` 全部包 ok 且零 FAIL
    - `GOOS=darwin go build ./...` 与 `GOOS=darwin go vet ./...` exit 0
    - per-client 十四测逐名出现在 -v 输出（名单对账：11-01 五测 TwoClientsTwoPids/WelcomeDims/InputEcho/NewModeSessContract/NoSessionStartEvent + 11-03 三测 SpawnFailure/CapacityGate/CapacityRecheckRace + 11-04 六测 ExitPrivate42/ExitSignalMinus1/DisconnectSIGHUP/ReconnectNewPid/StopTimeoutKillFallback/TeardownRaceOnce）
  </acceptance_criteria>
  <done>静态面 + 全量 -race + darwin 编译闸三段全绿，per-client 十四测名单对账一致；证据记录入 SUMMARY。</done>
</task>

<task type="auto">
  <name>Task 2: UAT 双证据（八既有脚本零修改重跑 + phase11.mjs 全绿）+ 期望值逐字未动 diff 审查</name>
  <precondition>Task 1 已绿（/tmp/wesh-p11/wesh 为最新全绿构建产物——本任务重跑 UAT 前以 time go build 重建确保二进制即当前源码）</precondition>
  <files></files>
  <read_first>
    - .planning/phases/10-mode-assembly/10-04-SUMMARY.md（UAT 八脚本重跑口径 :55/:100——PASS 计数与 v1.0 基线一致的记录形态）
    - web/uat/phase11.mjs（11-05 落地的八场景——本任务只跑不改）
  </read_first>
  <action>
    ① 重建最新二进制：`time go build -o /tmp/wesh-p11/wesh ./cmd/wesh`。
    ② 既有 UAT 零回归证据：按序跑八主脚本——node web/uat/phase02.mjs /tmp/wesh-p11/wesh 起至 phase09.mjs（04/05/06 辅助脚本与 07-b*.sh 非主脚本不在此列，10-04 口径）；exit 全 0 且 PASS 计数与 v1.0 基线一致（10-04-SUMMARY 记录形态：phase03 18/18、phase08 21/21 等逐脚本对账）；grep -i 'fail' 命中行逐条人工复核为 "fail-fast" 文案词非断言失败（10-04 同款复核）。
    ③ phase11.mjs 全绿：`node web/uat/phase11.mjs /tmp/wesh-p11/wesh` exit 0——S1-S8 PASS + S4b skipped 带 reason。
    ④ 期望值逐字未动 diff 审查（Pitfall 11 正面证据）：`set BASE (git merge-base HEAD main)`（fish 形态——fish 无 `VAR=$(...)` 赋值语法）——`git diff $BASE --stat -- internal/ web/uat/ cmd/` 全清单逐行对账：既有 *_test.go 零出现（perclient_test.go / reap_darwin_test.go 为新文件——git 状态为新增而非修改；reap_darwin_test.go 为 append 需 `git diff $BASE -- internal/pty/reap_darwin_test.go | grep -c '^-[^-]'` == 0 确认零删除行；export_test.go 同款 == 0）；web/uat/ 仅 phase11.mjs 新增；internal/proto/、web/src/、internal/server/metrics.go、health.go、resize.go 在 diff 中零出现（协议零改动 / 前端零改动 / D-04 三红线）。
    ⑤ 逐 plan SUMMARY 复核：五份 SUMMARY 的自报命令与退出码齐全、无「两模式都接受」放宽语句、无遗留 FAIL；本 phase 全部 prohibitions（11-01..11-05 must_haves.prohibitions）逐条对照 diff 人工确认零违反（descriptor-less flagged-unverified 项的人工复核出口——spec-less fallback 的 fail-closed 语义在此由人工确认闭合）。
    提交：本任务纯验证零代码改动，无 commit；证据落 11-06-SUMMARY.md（含逐命令输出摘要与 diff 对账表）。
  </action>
  <verify>
    <automated>time go build -o /tmp/wesh-p11/wesh ./cmd/wesh && for s in phase02 phase03 phase04 phase05 phase06 phase07 phase08 phase09; node web/uat/$s.mjs /tmp/wesh-p11/wesh; or exit 1; end; and node web/uat/phase11.mjs /tmp/wesh-p11/wesh</automated>
  </verify>
  <acceptance_criteria>
    - 既有八主脚本零修改重跑 exit 全 0，PASS 计数与 v1.0 基线逐脚本一致（10-04-SUMMARY 对账）
    - `node web/uat/phase11.mjs /tmp/wesh-p11/wesh` exit 0（八场景全绿 + S4b skipped 带 reason）
    - diff 审查：既有 *_test.go 零修改；reap_darwin_test.go 与 export_test.go 的 '^-[^-]' 删除行计数 == 0；web/uat/ 仅新增 phase11.mjs；proto/web/src/metrics.go/health.go/resize.go 零出现
    - 五份前序 SUMMARY 复核通过（命令齐全、零放宽语句、零遗留 FAIL）；prohibitions 人工确认零违反逐条记录
  </acceptance_criteria>
  <done>零回归双证据（Go 全量 + 八脚本原样重跑）与 per-client 协议证据（phase11.mjs 八场景）同绿；期望值逐字未动经 diff 正面证明；Phase 11 收口。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 收口证据面 → 零回归承诺 | 收口闸是「shared 逐字节不变」的最终证明载体——闸被削弱 = 承诺失效 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-06a | Tampering | 收口阶段为转绿而改动既有期望值/既有 UAT 脚本（零回归证据证伪——Pitfall 11「看似完成」行的最终形态） | high | mitigate | 本 plan 零代码改动（files_modified 空集）；diff 审查段以 '^-[^-]' 删除行计数 == 0 与文件白名单双重机器闸 + 逐条人工复核记录 |
| T-11-06b | Denial of Service | UAT 连跑资源滞留（各脚本子进程未收口叠加） | low | mitigate | 各脚本自带追踪收口 + 11-05 prohibitions 清场纪律；收口顺序串行执行避免并发干扰 PASS 计数 |
| T-11-SC | Tampering | 依赖供应链 | high | accept | 零新依赖；go.mod/go.sum 与 package.json/pnpm-lock.yaml 全程零漂移（diff 白名单复核覆盖） |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V14 Configuration（配置与构建面完整性） | ✓ 依赖清单零漂移 + diff 白名单（Task 2） |
</threat_model>

<verification>
- Task 1：静态三段 + 全量 -race + darwin 编译闸 + 十四测名单对账
- Task 2：八既有脚本 exit 全 0 且基线一致 + phase11.mjs 八场景全绿 + diff 审查四件套（既有测试零改动 / append-only 零删除行 / 新增白名单 / 红线文件零出现）
</verification>

<success_criteria>
- Phase 11 零回归收口闸最终形态成立：shared 全量原样绿 + phase02-09 默认模式零修改重跑 + 期望值逐字未动
- PC-02/PC-03/PC-04 三需求的 Go 测试（十四测）与协议 UAT（八场景）双证据链闭合
- 11-06-SUMMARY.md 落档全部证据（命令、退出码、PASS 计数、diff 对账、prohibitions 人工确认记录）
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **PC-02/PC-03/PC-04 (unclassified, unresolved)**：probe 三行在本 plan 获得显式证据承载（十四测 + 八场景 + diff 审查），由 VERIFICATION 阶段对证收口。
- **prohibitions 的人工确认**：spec-less fallback 的 descriptor-less 项 dispose 为 flagged-unverified——本 plan Task 2 ⑤为唯一人工确认出口，逐条对照 diff 记录；确认结果写入 SUMMARY。
- **已知中间态清单复核**（11-01 登记①②③④ + 11-03 linger 形态）：本 plan 不裁决、仅确认注释锚定与测试锁存在——翻转归 Phase 12/13 对应裁决落地。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- 11-06-SUMMARY.md（纯验证证据落档——六段式全量结果、diff 对账表、prohibitions 人工确认记录、十四测名单对账）
- 本 plan 零代码产物（files_modified 空集）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/11-per-client/11-06-SUMMARY.md` when done
</output>
