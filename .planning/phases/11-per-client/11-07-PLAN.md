---
phase: 11-per-client
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/server/perclient_test.go   # G-11-2 唯一 artifact；生产代码零改动
autonomous: false
gap_closure: true
gap_ids: [G-11-2]
requirements: [PC-03, PC-04]
tags: [gap-closure, esrch, eperm, macos-ci, test-helper, probe-injection]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "waitPgroupESRCH 对 EPERM 探针结果不再立即 Fatal：EPERM 与「无错」同归「进程组仍存在」形态落入护栏轮询——护栏内 ESRCH 收敛即 PASS（CI run 33832096581 的 FAIL 形态结构性移除；G-11-2 missing item 1 前半）"
    - "EPERM 持续到护栏到期仍 Fatal——僵尸残留/卡死进程组检测能力保留（missing item 1 后半：到期未 ESRCH 仍 Fatal）"
    - "其余非 ESRCH 非 EPERM 错误维持立即 Fatal（环境异常检测面零弱化）"
    - "TestWaitPgroupESRCHProbeSemantics 四案例（瞬态容忍收敛/持续到期翻车/他错立即失败/无错存活）确定性全绿——EPERM 分支行为在 Linux 即可锁定，不依赖 macOS 瞬态命中"
    - "函数头注释与实测事实一致：macOS XNU 退出过渡态 EPERM 可达性（引 CI run 33832096581 实证）与 macOS 组信号语义差异注在案，被证伪旧论断整句移除（missing item 2）"
    - "Linux 零回归三证据：go vet + 全量 -race 五包 ok + GOOS=darwin build/vet 双闸 + 代码面 diff 恰 internal/server/perclient_test.go 一文件"
    - "CI macOS leg TestPerClientTeardownRaceOnce PASS 且 Phase 11 套件 darwin 运行面全绿（Task 3 人工确认门承载 UAT 测试 2 复验——G-11-2 truth 原文）"
  artifacts:
    - "internal/server/perclient_test.go — waitPgroupESRCH 段重构（薄包装 + waitPgroupESRCHWithProbe 探针参数化核心 + EPERM 容忍语义 + 重写头注释）+ TestWaitPgroupESRCHProbeSemantics 四子测（紧随 helper 段新增）"
    - ".planning/phases/11-per-client/11-07-SUMMARY.md — 闭合证据（四子测结果 + 收口闸数字 + CI macOS leg 观察结果 + G-11-2 missing 两条对账表）"
  key_links:
    - "waitPgroupESRCH（薄包装，t.Fatal 单点）→ waitPgroupESRCHWithProbe（探针参数化核心，error 返回形态）——四调用点 :946/:977/:1034/:1164 签名与 Fatal 行为零变化；核心内探针调用恒 probe(-pid, 0)（负 pgid 进程组语义 + 信号 0 探测语义锚定）"
    - "TestWaitPgroupESRCHProbeSemantics 四子测 → waitPgroupESRCHWithProbe（脚本化探针注入）——EPERM 分支在 Linux 同 uid 下结构性不可达、在 macOS 为非确定瞬态，该单测是四语义案例的唯一确定性锁定通道"
  prohibitions:
    - "生产代码零改动——internal/server/perclient_test.go 之外禁止任何代码文件改动（go.mod/go.sum 一行不动，零新增依赖锁定）"
    - "既有四调用点断言语义零弱化——guard 时长（2s/2s/5s/2s）与 Fatal 终态不变；禁止以 t.Skip、删调用点、放大护栏等任何形态「修复」（11-CONTEXT 方法论红线：禁止断言放宽；UAT 测试 1 的 IN-01 教训——CI 绿不证明裁决成立，Skip 是伪绿）"
    - "僵尸残留检测能力不得降级——护栏到期未 ESRCH 必须仍 Fatal。EPERM 容忍是「存在形态」归类修正而非检测弱化：POSIX 语义 EPERM = 目标存在但权限拒绝，与探针无错同为存活证据——原立即 Fatal 才是错误分类"
    - "其余非 ESRCH 非 EPERM 错误保持立即 Fatal——环境异常检测面零弱化（意外错误仍即刻翻车暴露）"
    - "禁止改动 phase11.mjs 及任何既有测试期望值（本闭合编辑面限定 waitPgroupESRCH 段 + 紧随新增单测；文件头与 11-01/11-03/11-05 段逐字不动）"
    - "禁止改写 11-UAT.md gap 状态或 VERIFICATION 记录（gap 收口由 /gsd-verify-work 对账，plan 不自闭合账本）"
---

<objective>
闭合 11-UAT.md G-11-2（major）：CI run 33832096581 macOS leg TestPerClientTeardownRaceOnce
FAIL (0.01s)——waitPgroupESRCH（internal/server/perclient_test.go:811-827）把 kill(-pgid, 0)
的非 ESRCH 错误一律立即 Fatal，而该分支在 macOS XNU 退出过渡态（P_LIST_EXITED 标记/
proc_exit 进行中）真实可达且非确定命中（TeardownRaceOnce 竞态注入设计使首探针 µs 级
紧贴 closeCh 确认，唯一命中者；同断言的 DisconnectSIGHUP/StopTimeoutKillFallback 在
macOS PASS——排除环境级拦截）。

Purpose: EPERM 的 POSIX 语义是「目标存在但权限拒绝」而非「消失」——立即 Fatal 属测试
断言错误分类（平台兼容缺陷），非生产收割语义破坏。CI 红阻塞 Phase 11 收口（G-11-2
truth：Phase 11 测试套件在 CI macOS leg 全绿）。生产代码零改动：修复面全部位于测试
helper 的错误归类逻辑与其注释。

修复契约 = UAT gap 条目 missing 两条原文：
1. waitPgroupESRCH 将 EPERM 视为存活探针的一种形态——不 return 不 Fatal，fall through
   到护栏到期检查（护栏内 ESRCH 仍收敛 PASS；到期未 ESRCH 仍 Fatal，僵尸残留检测能力
   保留）；其余非 ESRCH 错误维持立即 Fatal。
2. 函数头注释修正——EPERM 在 macOS 退出过渡态可达（引 CI run 33832096581 实证），
   「ESRCH ⊇ 死亡且收割完成」论证补充 macOS 组信号语义差异。

验证策略（EPERM 分支的可测性困局与解法）：EPERM 在 Linux 同 uid 测试进程下结构性
不可达；真实 EPERM 夹具（异 uid 子进程）会破坏服务端 SIGHUP teardown 语义（非 root
无法向异 uid 进程组发信号）故不可用；macOS 瞬态本身非确定（唯一命中者正是竞态注入
测试）。唯一确定性锁定通道 = 探针参数化（waitPgroupESRCHWithProbe 注入脚本化探针）
+ 四案例单测——这也是护栏保留/他错立即 Fatal 两半边不被顺手弱化的反向回归锁。

Output: internal/server/perclient_test.go 修复（语义 + 注释 + 单测）、零回归收口闸数字、
CI macOS leg 复跑绿确认（人工确认门）、11-07-SUMMARY.md。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-per-client/11-UAT.md
@.planning/phases/11-per-client/11-VERIFICATION.md
@.planning/phases/11-per-client/11-04-SUMMARY.md
@internal/server/perclient_test.go
</context>

<flagged_assumptions>
- **护栏收敛余量假设**：macOS 退出过渡态为 µs-ms 级瞬态，2s/5s 护栏余量足以在 EPERM
  瞬态后收敛至 ESRCH。若 CI 复跑 FAIL 且形态为护栏翻车（kill(-pgid,0) 持续非 ESRCH
  到期），则 EPERM 容忍修法不充分——Task 3 人工确认门显式捕获该形态，进入再诊断而非
  重试或放宽断言。
- **CI 复跑 PASS 的证据边界**：EPERM 瞬态命中本身低概率（µs 窗），复跑 PASS 不单独
  证明修复生效（本轮可能未命中瞬态、直接 ESRCH 收敛）——修复行为的证据主载体是
  Task 1 确定性单测；CI 复跑承载「FAIL 形态不再可达」的运行面确认（G-11-2 truth 的
  darwin 运行面）。SUMMARY 按此口径记录，防证据错位。
- **基点假设**：plan 执行起始基点 = f55c1ea（Phase 11 UAT 提交，2026-09-04 分支
  HEAD）。Task 2 单一文件门以此为 diff 基点；若执行时起始 HEAD 已前移，以实际起始
  提交号替换该门参数并记录于 SUMMARY。
</flagged_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: G-11-2 语义修复——waitPgroupESRCH EPERM 容忍（探针参数化核心）+ 四案例单测 + 头注释实证修正（TDD）</name>
  <files>internal/server/perclient_test.go</files>
  <read_first>
    - internal/server/perclient_test.go — waitPgroupESRCH 段（头注释 :805-810、函数体 :811-827）+ 四调用点（TestPerClientDisconnectSIGHUP :946 / TestPerClientReconnectNewPid :977 / TestPerClientStopTimeoutKillFallback :1034 / TestPerClientTeardownRaceOnce :1164）+ TeardownRaceOnce 竞态注入形态（:1079-1180——首探针 µs 级紧贴 closeCh 确认的设计，理解为何仅其命中瞬态）+ imports 段（:16-38——errors/syscall 均已在案，预期零新 import）
    - .planning/phases/11-per-client/11-UAT.md — G-11-2 条目（root_cause 诊断链 + missing 两条——本任务契约原文）
    - .planning/phases/11-per-client/11-04-SUMMARY.md — waitPgroupESRCH 创建上下文（patterns 记录「kill 0 无错 = 存活探针」红线 + TDD 单 test 提交先例 + append-only 纪律边界）
  </read_first>
  <behavior>
    - Test eperm-transient-converges：探针脚本依次返回 EPERM、EPERM、ESRCH（guard 2s）→ waitPgroupESRCHWithProbe 返回 nil——EPERM 瞬态被容忍且护栏内收敛（G-11-2 FAIL 形态的消除证明）
    - Test eperm-persistent-guard-expiry：探针恒返回 EPERM（guard 150ms）→ 返回非 nil error 且 err.Error() 含「护栏」——僵尸残留检测保留（EPERM 持续存在照样翻车，容忍非无限化）
    - Test other-error-immediate-fatal：探针首调返回 EINVAL（guard 2s）→ 返回非 nil error 且 err.Error() 含 EINVAL——其余错误立即失败不弱化
    - Test alive-then-esrch：探针脚本依次返回 nil、nil、ESRCH（guard 2s）→ 返回 nil——无错存活语义保留
    - 四子测的脚本探针闭包内断言入参形态：首参 = 负 pid（-pid 进程组语义）、次参 = 信号 0（探测语义）——锚定核心内探针调用恒 probe(-pid, 0)
  </behavior>
  <action>
    TDD 双提交（延续 11-01/11-03/11-04 单 test 提交先例）：先写测试（对尚不存在的
    waitPgroupESRCHWithProbe——RED = 编译失败），单 test 提交；再实现核心与注释修正，
    单 fix 提交转 GREEN。

    ① 重构为探针可注入双层形态（EPERM 可测性的唯一出路——见 objective 验证策略）：
    新增非导出核心函数 waitPgroupESRCHWithProbe(pid int, guard time.Duration,
    probe func(int, syscall.Signal) error) error——不接 testing.T、以 error 返回结果
    （确定性单测的断言面，error 形态可在无 Goexit 技巧下断言 Fatal 路径）；既有
    waitPgroupESRCH(t, pid, guard) 改为薄包装：t.Helper() + 调核心（probe 实参直传
    syscall.Kill）+ err 非 nil 即 t.Fatal(err)。四个既有调用点零改动（签名与 Fatal
    行为不变）。核心轮询结构保留既有形态：probe(-pid, 0) → 分类 → deadline 检查 →
    25ms 步进循环；探针调用点恒传负 pid 与信号 0（与现行 syscall.Kill(-pid, 0)
    逐语义等价）。probe 形参类型与 syscall.Kill 签名一致（linux/darwin 双平台同型，
    Task 2 darwin vet 闸验证）。

    ② EPERM 容忍语义（G-11-2 missing item 1，核心分类逻辑）：
    - 探针返回 ESRCH → return nil（护栏内收敛即 PASS——既有语义）；
    - 探针返回 EPERM → 不 return 不报错：与「无错」同归「进程组仍存在」形态，落入
      deadline 检查——到期仍未 ESRCH → return 护栏错误（僵尸残留检测保留）；未到期
      sleep 25ms 续轮询；
    - 其余错误 → 立即 return 错误，文案保持现行 kill(-%d, 0) = %v, want ESRCH
      （进程组消失含收割完成）形态逐字不变；
    - 护栏到期错误文案更新为涵盖 EPERM 形态：含「护栏」与「EPERM」字样，且明确无错
      与 EPERM 同为「进程组仍存在」的存活探针两形态（现行「kill 0 无错 = 存活探针」
      表述的语义扩展，红线不删——**严禁**把探针无错当死亡证据的表述保留）。

    ③ 新增 TestWaitPgroupESRCHProbeSemantics（紧随 waitPgroupESRCH 段新增，四个
    t.Run 子测名与 behavior 一一对应）：脚本化探针 = 闭包消费预置 error 返回值序列
    （耗尽后重复末值——支撑恒 EPERM 案例），直调 waitPgroupESRCHWithProbe 断言返回
    值（不经 t.Fatal 包装层）；guard 取值见 behavior（150ms 案例测试时长 ~150ms，
    2s 案例实际收敛于 2-3 次轮询 ~50ms）。

    ④ 头注释重写（G-11-2 missing item 2——主注释随 waitPgroupESRCH 包装函数，探针
    注入设计理由随核心函数）：删除 :809-810 括号内旧论断整句；新注释载明五要点——
    (a) macOS XNU 上进程组成员处于退出过渡态（P_LIST_EXITED 标记/proc_exit 进行中）
    时 kill(-pgid, 0) 可返回 EPERM 而非 0，实证 = CI run 33832096581
    （TestPerClientTeardownRaceOnce 首探针 µs 级紧贴 closeCh 确认命中该瞬态；同断言
    的 DisconnectSIGHUP/StopTimeoutKillFallback 无竞态注入设计故未命中——排除环境级
    拦截的对照证据）；(b) POSIX 语义 EPERM = 目标存在但权限拒绝 ≠ 消失，与探针无错
    同属「进程组仍存在」形态；(c)「ESRCH ⊇ 死亡且收割完成」论证的 macOS 补注：
    Linux 僵尸成员对信号 0 视为存在的论断在 macOS 退出过渡态被 EPERM 瞬态遮蔽
    （形态不同、结论同向——ESRCH 仍为进程组消失含收割完成的强证据）；(d) EPERM 落入
    护栏轮询：护栏内 ESRCH 收敛即 PASS，到期未 ESRCH 仍 Fatal（僵尸残留检测保留）；
    (e) 其余非 ESRCH 非 EPERM 错误维持立即 Fatal。注释行遵守 CJK 标点接续行空格纪律
    （GOROOT gofmt go1.26.3 现代 doc-comment 规则——11-04 Deviation #2 同款，Task 2
    收口闸兜底）。

    禁止：不碰 waitPgroupESRCH 段与新增单测之外的任何内容（既有测试、文件头注释、
    imports 预期零变化）；不碰任何生产代码文件。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && go test -race -count=1 ./internal/server/ -run 'TestWaitPgroupESRCHProbeSemantics|TestPerClient' -v > /tmp/wesh-11-07-t1.log 2>&1 && grep -q -- '--- PASS: TestWaitPgroupESRCHProbeSemantics' /tmp/wesh-11-07-t1.log && test "$(grep -c -- '--- PASS: TestWaitPgroupESRCHProbeSemantics/' /tmp/wesh-11-07-t1.log)" = "4" && test "$(grep -c 'EPERM 不可达' internal/server/perclient_test.go)" = "0" && test "$(grep -c '33832096581' internal/server/perclient_test.go)" -ge "1" && test "$(grep -c 'func waitPgroupESRCHWithProbe' internal/server/perclient_test.go)" = "1" && echo T1_SEMANTICS_PASS</automated>
  </verify>
  <done>TestWaitPgroupESRCHProbeSemantics 四子测全绿（瞬态容忍收敛 / 持续到期翻车 / 他错立即失败 / 无错存活）；既有 TestPerClient 全部测试同跑零回归；旧论断字面在 perclient_test.go 计数归零（:809-810 整句移除）；CI run 33832096581 引证在案；waitPgroupESRCHWithProbe 恰一定义、waitPgroupESRCH 四调用点签名未变。</done>
  <acceptance_criteria>
    - go test -race -count=1 ./internal/server/ -run 'TestWaitPgroupESRCHProbeSemantics|TestPerClient' 退出码 0（既有 per-client 测试 + 新单测同跑零回归）
    - -v 日志中 '--- PASS: TestWaitPgroupESRCHProbeSemantics/' 子测行计数 == 4（四子测缺一不可）
    - grep 旧论断字面（automated 链内字面量）对 internal/server/perclient_test.go 计数 == 0（负计数门，目标文件限定）
    - grep -c '33832096581' internal/server/perclient_test.go ≥ 1（正计数门：CI 实证引证）
    - grep -c 'func waitPgroupESRCHWithProbe' internal/server/perclient_test.go == 1（核心函数单点定义）
    - 四调用点（:946/:977/:1034/:1164）waitPgroupESRCH 调用形态逐字未动（git diff 复核）
  </acceptance_criteria>
  <reversibility rating="reversible">测试 helper 语义修正 + 新增单测，git 可回退；生产代码零改动，无一-way 面。</reversibility>
</task>

<task type="auto">
  <name>Task 2: 零回归收口闸——全量 -race + darwin 双闸 + gofmt + 单一文件 diff 门（fix 提交后的闭合证据）</name>
  <files>internal/server/perclient_test.go（只读收口闸对象，零修改）</files>
  <read_first>
    - .planning/phases/11-per-client/11-VERIFICATION.md — Behavioral Spot-Checks 表（复跑口径：全量 -race 5 包 1m5s、darwin 双闸双 exit 0 基线）
    - .planning/phases/11-per-client/11-06-SUMMARY.md — Phase 11 收口闸数字基线（对照口径）
  </read_first>
  <action>
    在 Task 1 修复提交之上重跑 Phase 11 既定收口闸面并归集闭合证据：
    1. 静态面与全量测试：go vet ./... 零输出；go test -race -count=1 ./... 五包
       全 ok（11-VERIFICATION 同口径基线）。
    2. darwin 编译面：GOOS=darwin go build ./... 与 GOOS=darwin go vet ./... 双
       exit 0——vet 含测试文件类型检查，覆盖探针 seam 的 syscall.Signal 形参与
       EPERM/ESRCH 常量在 darwin 的编译面（CI macOS leg 将实际运行该测试文件）。
    3. gofmt 闸：GOROOT gofmt -l internal/server/perclient_test.go 零输出
       （go1.26.3 现代 doc-comment 规则——Task 1 新注释 CJK 接续行的兜底闸）。
    4. 单一文件门：git diff --name-only f55c1ea..HEAD -- internal/ cmd/ web/
       输出恰为 internal/server/perclient_test.go 一行——生产代码零改动实证
       （f55c1ea = plan 执行起始基点，Phase 11 UAT 提交；若执行起始 HEAD 已前移，
       以实际起始提交号替换并记录于 SUMMARY）。
    5. 证据归集：本任务 1-4 数字 + Task 1 四子测结果整理为 11-07-SUMMARY.md 素材
       （G-11-2 missing 两条 → 证据映射对账表——语义修复/注释修正各指向 Task 1
       ①②/④，darwin 运行面确认指向 Task 3）。
    不跑 phase11.mjs 等协议 UAT：零生产改动、二进制行为面不变，UAT 对象（产品行为）
    不触及——该省略理由记录于 SUMMARY 免复核疑虑。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && go vet ./... && go test -race -count=1 ./... && GOOS=darwin go build ./... && GOOS=darwin go vet ./... && test -z "$("$(go env GOROOT)"/bin/gofmt -l internal/server/perclient_test.go)" && test "$(git diff --name-only f55c1ea..HEAD -- internal/ cmd/ web/)" = "internal/server/perclient_test.go" && echo T2_REGRESSION_PASS</automated>
  </verify>
  <done>vet 零输出；-race 五包全 ok；darwin build/vet 双 exit 0；gofmt 零输出；代码面 diff 恰 internal/server/perclient_test.go 一文件；SUMMARY 素材含收口闸全数字与 G-11-2 对账表。</done>
  <acceptance_criteria>
    - go vet ./... 退出码 0；go test -race -count=1 ./... 输出五行 ok（cmd/wesh、internal/proto、internal/pty、internal/server、web）
    - GOOS=darwin go build ./... 与 GOOS=darwin go vet ./... 均退出码 0（darwin 编译闸——探针 seam 双平台编译面）
    - GOROOT gofmt -l internal/server/perclient_test.go 零输出
    - git diff --name-only f55c1ea..HEAD -- internal/ cmd/ web/ 输出恰一行 internal/server/perclient_test.go（生产代码零改动实证）
  </acceptance_criteria>
  <reversibility rating="reversible">全任务只读断言 + 证据归集；零代码改动面。</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CI macOS leg 复跑确认——G-11-2 运行面闭合（UAT 测试 2 复验载体）</name>
  <what-built>
    waitPgroupESRCH EPERM 容忍修复已落地并经 Linux 侧全部门实证（Task 1 四案例确定性
    单测 + Task 2 全量 -race/darwin 双闸/单一文件 diff）。唯一剩余未闭合面 = 失败现场
    本身在 CI macOS runner（darwin 运行态在本机 Linux headless 结构性不可观测——
    11-VERIFICATION human 项同款约束）。
  </what-built>
  <how-to-verify>
    1. 推送修复提交：git push origin gsd/phase-11-per-client（用户执行，或授权
       executor 执行同一命令）；确认远端分支含 11-07 的 test 与 fix 两提交。
    2. 等待 CI 新 run 完成（GitHub Actions go 矩阵含 macos-latest leg），打开
       macOS leg 的 go test -race 日志核对：
       a. TestPerClientTeardownRaceOnce PASS（CI run 33832096581 的 FAIL 现场测试）；
       b. TestPerClientDisconnectSIGHUP / TestPerClientReconnectNewPid /
          TestPerClientStopTimeoutKillFallback 保持 PASS（同 helper 其余三调用点
          零回归）；
       c. internal/server 包整体 ok——Phase 11 套件 darwin 运行面全绿（G-11-2
          truth 原文）。
    3. 结果解读纪律：修复后 EPERM 瞬态被容忍，本轮未命中瞬态（直接 ESRCH 收敛）也
       PASS——这是合法收敛形态；唯 FAIL 再现（任意测试任意形态）才构成未闭合。
  </how-to-verify>
  <resume-signal>
    输入 approved 并附 CI run 链接（macOS leg 全绿）；若仍有 FAIL：贴测试名 + 失败行
    原文（护栏翻车形态 = kill(-pgid,0) 持续非 ESRCH 到期——意味着 EPERM 容忍修法
    不充分，需再诊断，勿盲目重试或放宽断言）
  </resume-signal>
</task>

</tasks>

## Artifacts this phase produces

**生产代码符号：零。** 本 plan 零生产代码改动。

**测试符号（internal/server/perclient_test.go）：**
- `waitPgroupESRCHWithProbe`（非导出核心：探针参数化、error 返回形态——新增）
- `waitPgroupESRCH`（薄包装化重构：签名不变，t.Fatal 单点）
- `TestWaitPgroupESRCHProbeSemantics`（四子测：eperm-transient-converges /
  eperm-persistent-guard-expiry / other-error-immediate-fatal / alive-then-esrch）

**规划/证据文件：** `.planning/phases/11-per-client/11-07-SUMMARY.md`（执行输出）

**临时产物（不入库）：** /tmp/wesh-11-07-t1.log（Task 1 测试日志）。

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| （无新增信任边界） | 本 plan 零生产代码改动、零新依赖、零网络/外部输入面——改动全部位于 internal/server 测试进程内既有 helper 的错误归类逻辑 |
| CI runner 运行态 → 人工确认门 | darwin 运行态为外部服务状态（本机结构性不可观测），经 Task 3 人工确认门承载——非代码攻击面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07-01 | Tampering | waitPgroupESRCH 断言强度（EPERM 容忍被顺手扩大为检测弱化） | high | mitigate | 容忍严格限定「存在形态」归类：护栏到期未 ESRCH 仍 Fatal + 其余错误立即 Fatal，两半边由四案例单测反向锁定（持续 EPERM 翻车 / 他错立即翻车两负例）；prohibitions 明令禁止 t.Skip、删调用点、放大护栏 |
| T-11-07-02 | Denial of Service | （理论）持续 EPERM 误报致护栏翻车误报 | low | accept | 持续 EPERM 意味着进程组真实存在（权限拒绝 ≠ 消失）——护栏翻车是该语义下的正确行为（僵尸/卡死检测）；macOS 瞬态 µs-ms 级，2s/5s 护栏余量充足（Task 3 确认门兜底异常形态） |
| T-11-07-SC | Tampering | npm/pip/cargo 安装 | — | not applicable | 本 plan 零包安装（go.mod/go.sum 一行不动）——Package Legitimacy Gate 不触发 |
</threat_model>

<verification>
- Task 1：T1_SEMANTICS_PASS——TestWaitPgroupESRCHProbeSemantics 四子测全绿 + 既有 per-client 测试同跑零回归 + 旧论断计数归零 + CI run 33832096581 引证在案 + 核心函数单点定义
- Task 2：T2_REGRESSION_PASS——vet + 全量 -race 五包 ok + darwin build/vet 双闸 + gofmt 零输出 + 代码面 diff 恰一文件
- Task 3：人工确认 approved + CI run 链接——TestPerClientTeardownRaceOnce PASS + 同 helper 三调用点保持 PASS + internal/server 包 darwin 运行面 ok
- G-11-2 missing 两条 → Task 1（语义 ①② / 注释 ④）对账；运行面确认 → Task 3；SUMMARY 落全部证据与对账表
</verification>

<success_criteria>
G-11-2 闭合：missing 两条全落地（EPERM 存活形态归类 + 注释实证修正）且经四案例确定性
单测锁定——护栏保留与他错立即 Fatal 两半边零弱化；Linux 侧零回归三证据（全量 -race /
darwin 双闸 / 单一文件 diff）；CI macOS leg TestPerClientTeardownRaceOnce PASS 且
Phase 11 套件 darwin 运行面全绿（人工确认门）。Phase 11 可进入复验，G-11-2 可由
验证器对账关闭。
</success_criteria>

<output>
Create `.planning/phases/11-per-client/11-07-SUMMARY.md` when done
</output>
