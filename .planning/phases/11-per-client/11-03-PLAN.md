---
phase: 11-per-client
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - internal/server/perclient.go
  - internal/server/perclient_test.go
  - internal/server/export_test.go
autonomous: true
requirements:
  - PC-02

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "容量再闸（D-02，wire 形态 one-way 已锁）：升档序列在 spawn 之前取 hubMu 读 len(pcSessions) >= maxClients，满员即拒——proto.ErrorFrame(proto.ErrServerError, \"server is at capacity\") 直写 + logEvent(remote, websocket.StatusInternalError, \"max_clients\", remoteUser) + Close(websocket.StatusInternalError, proto.ErrServerError)；与 spawn 失败同码 1011 同机器串 server_error（协议零改动红线），分辨率由 logEvent 事件名承担（max_clients vs spawn_failed——wire 聚合、日志细分，SEC-07 先例同构）"
    - "D-03 注册点复检回收：spawn 成功后在注册段的同一 hubMu 持有内复检 len(pcSessions) >= maxClients——超编者（竞态窗口：两并发升档同时过 pre-spawn 闸）经 reapOrphanSession 回收（SignalGroup(HUP——D-03 字面，容量回收路径非断开语义不走 --stop-signal 通道）→ stopTimeout>0 则 AfterFunc 补 SIGKILL（局部 reaped 原子闸，Pitfall 2 同构）→ Drain(200ms) → Close(master) → Wait 收割，全程异步 goroutine 绝不占 hubMu），该客户端收容量文案 Error + 1011（与 pre-spawn 闸同 wire 形态）——「并发子进程数 ≤ maxClients」硬不变量 Phase 11 即成立"
    - "③位 503 闸（server.go:851-856，registry.n 计数源）与 D-02 WS 面再闸的分工不变：满员且注册表满 → 既有 HTTP 503（零改动）；注册表空出但 pcSessions 满（断开待收割 linger 形态）或并发竞态窗口 → WS 面 1011 容量文案（本 plan 新增面）"
    - "spawn 失败路径 Pitfall 5 清理清单逐条测试锁定：半开名额 release 恰好一次（:979-980 共用行结构性保证）→ 不进注册表（/healthz clients 不变）→ 不产生 attach/session_start 审计事件（仅 spawn_failed 单事件）→ 审计行零敏感值（事件不含底层错误文本与 argv 路径）→ 他端零感知（已 attach 客户端 echo 照常）"
    - "零回归收口闸：shared 全量 Go 测试原样绿 + 期望值逐字未动；既有测试文件一字节不动（perclient_test.go 为 D-05 独立新文件内演进，export_test.go 仅 append-only 出口）"
  artifacts:
    - path: "internal/server/perclient.go"
      provides: "upgradePerClient 内 pre-spawn 容量再闸 + 注册点复检回收分支 + reapOrphanSession 异步回收 goroutine 形态"
      contains: "reapOrphanSession"
    - path: "internal/server/perclient_test.go"
      provides: "harness 扩展（spawnedSessions 访问器 + SpawnFunc 可注入形态 startPerClientServerWithSpawn）+ TestPerClientSpawnFailure/TestPerClientCapacityGate/TestPerClientCapacityRecheckRace 三测"
      contains: "TestPerClientCapacityGate"
    - path: "internal/server/export_test.go"
      provides: "PCSessionsLenForTest 出口（hubMu 内 len 读；调用方不得持 hubMu——ShrinkOutboxForTest 注释形态）"
      contains: "PCSessionsLenForTest"
  key_links:
    - from: "internal/server/perclient.go upgradePerClient pre-spawn 闸"
      to: "internal/server/perclient.go pcSessions 注册表"
      via: "hubMu 短临界区读 len 计数——闸内读 → 放锁 spawn → 再取锁注册（Anti-Pattern 1 锁序）"
      pattern: "len(s\\.pcSessions) >= s\\.maxClients"
    - from: "internal/server/perclient.go upgradePerClient 注册段复检"
      to: "internal/server/perclient.go reapOrphanSession"
      via: "同一 hubMu 持有内复检超编 → 放锁后异步回收（SignalGroup+Drain+Close+Wait）→ 客户端收容量 Error+1011"
      pattern: "reapOrphanSession"
    - from: "internal/server/perclient.go 两拒绝路径"
      to: "internal/server/log.go logEvent 通道"
      via: "D-04 四段 schema（remote/code/reason/remoteUser）——容量=max_clients、失败=spawn_failed，零敏感值"
      pattern: "logEvent\\(remote, websocket\\.StatusInternalError"
  prohibitions:
    - statement: "MUST NOT 把容量拒绝写成 1013 或 1008（D-02 裁决：1013 前端分派只认 code 固定显示慢消费者文案——per-client 满员语义双重错位；1008 既有语义为认证/版本策略违反）——1011 + Error message 通道是唯一合法形态"
      status: unresolved
      verification: null
    - statement: "MUST NOT 在容量/失败文案或事件中携带 maxClients 数值以外的内部状态（路径/errno/argv/计数器快照）；spawn_failed 与 max_clients 事件零敏感值（D-04）"
      status: unresolved
      verification: null
    - statement: "MUST NOT 让超编回收路径跳过 Wait 收割（只 SignalGroup 不 Wait = 僵尸泄漏；回收 goroutine 必须 Drain→Close→Wait 完整序列）"
      status: unresolved
      verification: null
---

<objective>
per-client 容量防线与 spawn 失败面闭合（PC-02 的拒绝面）：pre-spawn 容量再闸（D-02 锁定的 1011+容量文案 wire 形态）+ D-03 注册点复检回收（并发竞态窗口兜底，「并发子进程数 ≤ maxClients」硬不变量）+ spawn 失败 Pitfall 5 清理清单的逐条测试锁定。

Purpose: 11-01 tracer 打通了 happy path 与 spawn 失败帧路径；本 plan 补上昂贵资源（每连接一进程）的硬帽机制——per-client 下超编不再是廉价连接而是完整子进程（Pitfall 4 的容量维），D-03 使 Phase 13 裁决项④（spawn-intent 口径）提前消解。D-02 的 wire 形态是 one-way（1011+容量文案将被 phase11.mjs S6 与 Go 测试双锁），用户已在 11-CONTEXT 锁定，按 planner-reversibility「不重复设卡已定决策」先例只记录评级。
Output: 容量闸三形态（HTTP 503 既有 / WS 1011 pre-spawn 闸 / 1011 复检回收）全锁；spawn 失败清理清单测试锁定；硬不变量成立。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-per-client/11-CONTEXT.md
@.planning/phases/11-per-client/11-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/11-per-client/11-01-PLAN.md
@internal/server/perclient.go
@internal/server/perclient_test.go
@internal/server/server.go
@internal/server/log.go
@internal/server/health.go
@internal/server/export_test.go
@internal/server/events_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 容量再闸 + D-03 注册点复检回收 + reapOrphanSession（D-02/D-03，PC-02）</name>
  <reversibility rating="one-way">D-02 容量再闸 wire 形态（Error{server_error,"server is at capacity"}+Close(1011)）将被本 plan Go 测试与 11-05 phase11.mjs S6 双重锁定，per-client 用户将对其形成依赖，改码即行为破坏——用户已在 11-CONTEXT 锁定该选项，按 planner-reversibility「gating a decision already made」反模式与 10-01 先例：记录评级，不重复设卡。</reversibility>
  <files>internal/server/perclient.go</files>
  <read_first>
    - internal/server/perclient.go（11-01 落地后的 upgradePerClient 全文——容量闸与复检的预留位序注释锚点；teardownPCLocked 的快半段信号形态——reapOrphanSession 的信号纪律参照）
    - internal/server/server.go（③位容量闸 :851-856——HTTP 面 max_clients 先例；version_mismatch 直写形态 :935-937——握手期 Error+Close 先例；Attach 升档分岔调用点）
    - .planning/phases/11-per-client/11-CONTEXT.md（D-02/D-03 决策全文——1013/1008 否决理由与「wire 聚合、日志细分」分工）
    - .planning/phases/11-per-client/11-PATTERNS.md（§1C ③位形态 / §1D version_mismatch 逐字母本 / §6b 复检回收 ≤5 行建议形态 / S3 logEvent schema）
    - .planning/research/ARCHITECTURE.md（§3.1 时序图容量再闸落点 + §5 规则 1 竞态窗口论证）
    - .planning/research/PITFALLS.md（Pitfall 4 超编窗口分析——「8 个并发握手各自通过容量检查后同时 spawn」形态）
  </read_first>
  <action>
    ① upgradePerClient 内在单行门算 effMode 之后、spawnFunc 调用之前插入 pre-spawn 容量再闸（D-02）：hubMu.Lock → full := len(s.pcSessions) >= s.maxClients → hubMu.Unlock；full 时执行拒绝序列（直写 proto.ErrorFrame(proto.ErrServerError, "server is at capacity") → logEvent(remote, websocket.StatusInternalError, "max_clients", remoteUser) → c.Close(websocket.StatusInternalError, proto.ErrServerError) → return nil）——注释登记 D-02 裁决要点：1013 否决理由（main.ts:946 前端分派只认 code 固定慢消费者文案，per-client 满员语义双重错位）与 1008 否决理由（认证/版本策略语义面污染）；与 spawn 失败同码同机器串，分辨率由事件名承担（D-04）；半开名额已在分岔前 release()（:979-980 共用行），拒绝路径零注册零登记零残留。hubMu 短临界区只读计数——绝不持锁 spawn（Anti-Pattern 1）。
    ② 注册段（spawnFunc 成功后的 hubMu.Lock 持有内、client 构造之前）插入 D-03 复检：若 len(s.pcSessions) >= s.maxClients 则 hubMu.Unlock → 调 s.reapOrphanSession(pcSess)（异步回收，下方③）→ 执行与①完全相同的容量拒绝序列（同文案同事件名同关闭码——wire 面两种容量拒绝不可区分是有意为之，D-02「wire 聚合」）→ return nil。注释登记：竞态窗口 = 两并发升档同时过①闸（Pitfall 4 超编形态）；复检使「并发子进程数 ≤ maxClients」硬不变量成立；Phase 13 裁决项④（spawn-intent 口径）由此提前消解——注释锚定 STATE.md Blockers 移除指引。
    ③ 实现 reapOrphanSession(pcSess *pty.Session)（perclient.go，teardownPCLocked 之后）：单 goroutine 异步序列——pcSess.SignalGroup(syscall.SIGHUP)（D-03 字面 HUP：容量回收路径非断开语义，不走 --stop-signal 通道，注释锚定）→ s.stopTimeout>0 时 time.AfterFunc(s.stopTimeout, ...) 补 SIGKILL——回调先查局部 reaped 原子位（atomic.Bool；该会话从未注册 pcSessions、无 watcher——reaped 闸用局部原子量即可，不触 hubMu，Pitfall 2 同构语义：Wait 返回后补 KILL 可能打中复用 pgid）→ pcSess.Drain(200 * time.Millisecond) → pcSess.Close() → pcSess.Wait() → reaped.Store(true)（与 AfterFunc 回调的读配对——窄窗口注释论证：Wait 返回与 Store 相邻两指令，pgid 复用需内核整轮分配，实际不可达；与主 teardown 路径 hubMu 置位同档形态）。注释登记：该路径是孤儿会话的唯一收割者（唯一收割者纪律——每会话恰好一个 Wait 调用方：正常路径 = sessionWatcher，回收路径 = 本 goroutine，两路径互斥不可同时装配）。
    ④ 自检：upgradePerClient 内 11-01 预留位序注释更新为实际代码锚定；`time go build ./...` + `go vet ./...` 通过；`go test -race -count=1 ./internal/server/ -run 'TestPerClient' -v` 全绿（11-01 五测不回归——闸与复检对既有测试路径透明：maxClients 默认 32 不触闸）。
    提交：`feat(11-03): capacity re-gate + registration recheck-recycle — D-02 1011 capacity wire form, D-03 hard process cap invariant (PC-02)`
  </action>
  <verify>
    <automated>time go build ./... && go vet ./... && go test -race -count=1 ./internal/server/ -run 'TestPerClient' -v && go test -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'len(s\.pcSessions) >= s\.maxClients' internal/server/perclient.go` == 2（pre-spawn 闸 + 注册点复检各一）
    - `grep -c '"server is at capacity"' internal/server/perclient.go` == 1（容量文案单点定义——两处调用点共用同一字面量或常量；若实现选常量形态则 == 2 且常量定义 == 1，验收时以「文案字面量仅一处定义」人工核对为准）
    - `grep -c '"max_clients"' internal/server/perclient.go` ≥ 1（logEvent 事件名）
    - `grep -c 'func (s \*Server) reapOrphanSession' internal/server/perclient.go` == 1 且 `grep -c 'pcSess\.Wait()' internal/server/perclient.go` == 1（回收路径含收割——不跳过 Wait）
    - `grep -c 'StatusInternalError, proto\.ErrServerError' internal/server/perclient.go` ≥ 2（失败与容量两路径同码同串）
    - `go build ./...` 成功；`go vet ./...` 无输出；`go test -race -count=1 ./...` 全仓全绿（11-01 五测原样绿，零断言改动）
    - `git diff --stat -- internal/server/server.go internal/server/clients.go cmd/wesh/main.go` 为空（本任务零改动既有文件）
  </acceptance_criteria>
  <done>pre-spawn 容量再闸与注册点复检回收落地：满员/超编两形态均收 Error{server_error,"server is at capacity"}+1011+max_clients 事件；回收 goroutine 完整 SignalGroup→Drain→Close→Wait 序列；硬不变量「并发子进程数 ≤ maxClients」机制就位；全量 -race 绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: spawn 失败清理清单 + 容量闸三形态测试——TestPerClientSpawnFailure/TestPerClientCapacityGate/TestPerClientCapacityRecheckRace</name>
  <files>internal/server/perclient_test.go, internal/server/export_test.go</files>
  <read_first>
    - internal/server/perclient_test.go（11-01 落地的 harness 与五测——扩展位；readOutputUntil 复用）
    - internal/server/export_test.go（ShrinkOutboxForTest :26-39「调用方不得持 hubMu」注释形态——新出口母本）
    - internal/server/events_test.go（captureStderr/LockStderr 纪律 + eventsNamed 过滤 + 键断言形态）
    - internal/server/health.go（/healthz clients 字段 = registry.n 数据源 :42——注册表计数的外部可观测面）
    - internal/server/e2e_test.go（dialHello 形态与原始 websocket.Dial + 手写 Hello 帧形态——并发竞态测试需要裸握手控制）
    - internal/server/handshake_test.go（握手违规直写面的读取断言先例——Error 帧 + CloseError 收集形态）
    - internal/server/stopseq_test.go（:97-134 trap 免疫 + 时序双断言形态；:30-31 夹具纪律红线）
    - internal/proto/proto.go（ErrorPayload/Error 帧 JSON 结构——测试侧解码目标）
  </read_first>
  <behavior>
    - TestPerClientSpawnFailure：harness 的 SpawnFunc 包一层 atomic 失败开关——A attach（开关关）echo 正常 → 开关闭合 → B attach 收 Error 帧（code=="server_error" 且 message=="failed to start process" 逐字）+ close 1011；事件流含 spawn_failed 行（code=1011、无 remote 以外敏感值、不含注入错误文本/argv 路径）；无 B 的 attach 事件；/healthz clients==1；A echo 照常（他端零感知）
    - TestPerClientCapacityGate（D-02 pre-spawn 闸，确定性注入经 linger 形态）：maxClients=1 + argv=trap "" HUP 死循环命令——A attach 得 Welcome → A 关闭（断开 SIGHUP 被 trap 免疫，会话待收割滞留 pcSessions，registry 清空）→ B attach 经 ③位 503 闸放行（registry.n==0）→ Hello 后命中 D-02 闸：Error 帧 message=="server is at capacity" 逐字 + close 1011 + max_clients 事件；PCSessionsLenForTest()==1 不变
    - TestPerClientCapacityRecheckRace（D-03 复检，barrier 注入）：maxClients=1 + SpawnFunc 进入即阻塞于共享 barrier（计数器==2 后测试放行）——两客户端并发裸握手（websocket.Dial + 手写 Hello 帧）；放行后两端分别 spawn 成功，先注册者得 Welcome，后注册者复检超编被回收：收 Error 容量文案 + 1011；最终 PCSessionsLenForTest()==1；harness spawnedSessions() 追踪两会话，败者 pgid（setsid 不变量 pgid==pid）经 syscall.Kill(-pid, 0) 轮询得 ESRCH（回收含 Wait 收割、无僵尸）
    - 全量 -race 绿；既有测试零改动
  </behavior>
  <action>
    ① export_test.go 追加 PCSessionsLenForTest（hubMu.Lock/defer Unlock/return len(s.pcSessions)；注释照 ShrinkOutboxForTest 形态：测试期观测口、调用方不得持 hubMu、per-client 注册表活性 = 未收割会话数）。append-only。
    ② perclient_test.go harness 演进（新文件内演进自由，D-05 不约束新文件）：抽出 startPerClientServerWithSpawn(t, spawnFn, mutate) 为通用形态（spawnFn 可注入——生产闭包的测试镜像），startPerClientServer 缩为薄包装（默认 spawnFn = pty.StartWithSize 直通闭包）；两形态均把每次成功 spawn 的 *pty.Session 追加进 mu 保护追踪切片，并返回新增访问器 spawnedSessions func() []*pty.Session（mu 保护拷贝返回；t.Cleanup 收口逐切片 Kill+Close——泄漏实证注释保持）。11-01 五测适配新签名（调用点机械更新，断言零改动——五测行为逐字保持）。
    ③ TestPerClientSpawnFailure：spawnFn 注入形态（atomic.Bool 失败开关闭合即 return nil, errors.New("injected spawn failure for test")——注入错误文本只进 stub 返回值，wire 与事件面断言其零出现）；A 先 attach（dialHello + echo 标记回读）→ 开关闭合 → B 用裸握手（websocket.Dial + proto 格式 Hello 帧）收集帧至 CloseError：断言恰一帧 Error（type 字节 proto.Error + JSON code=="server_error" + message=="failed to start process" 逐字）且 CloseError.Code==1011；captureStderr 窗口（LockStderr 纪律）断言 spawn_failed 事件恰一条且不含 "injected" 与 argv 路径子串、无 B 的 attach 事件；HTTP GET /healthz 断言 clients==1；A 再 echo 标记回读（他端零感知）。
    ④ TestPerClientCapacityGate：mutate 覆写 Options.MaxClients=1；argv = []string{"sh", "-c", "trap '' HUP; echo PCAPID=$$; while true; do sleep 1; done"}（启动即印 pid——非交互死循环不读 stdin，pid 经初始 OUTPUT 回读，readPid 正则形态复用）；A dialHello → 回读 PCAPID → A 的 conn Close(1000)（detach→teardown→SIGHUP 被 trap 免疫）→ 轮询 PCSessionsLenForTest()==1 且 registry 清空（/healthz clients==0）后 B 裸握手：断言 Error 容量文案逐字 + CloseError.Code==1011 + max_clients 事件恰一条；断言 A 的 pgid 仍存活（trap 免疫实证——kill(-pid,0) 无错）；t.Cleanup 前测试尾部显式 syscall.Kill(-pid, SIGKILL) 收口滞留进程组（防 CI 级联减速的夹具纪律）。
    ⑤ TestPerClientCapacityRecheckRace：mutate 覆写 MaxClients=1；spawnFn 注入形态（原子计数器 + barrier chan——闭包进入即 Add(1) 并 <-barrier，测试等计数器==2 后 close(barrier) 双放行；放行后直通 pty.StartWithSize(argv=[]string{"sh"} 形态)）；A/B 两 goroutine 并发裸握手（各自 websocket.Dial + 发 Hello）；收集两端首帧与关闭：断言恰一端收 Welcome、另一端收 Error 容量文案 + 1011（谁胜谁负不确定——断言锁定的是「恰一胜一负 + 注册表终态 == 1 + 败者被收割」不变量，非时序胜者身份；注释明示此非断言放宽——两结果同为正确形态的竞态固有性）；轮询 PCSessionsLenForTest()==1；经 spawnedSessions() 取两 pid，对非胜者 pgid 轮询 kill(-pid,0) 至 ESRCH（2s 护栏）。
    ⑥ 收口自检：`go test -race -count=1 ./...` 全绿；`$(go env GOROOT)/bin/gofmt -l .` 零输出；git diff 确认既有测试文件与生产文件零改动（本任务仅 perclient_test.go 演进 + export_test.go append）。
    提交：`test(11-03): spawn failure checklist + capacity gate trio — Pitfall 5 cleanup locked, D-02/D-03 wire forms behaviorally closed (PC-02)`
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/server/ -run 'TestPerClientSpawnFailure|TestPerClientCapacityGate|TestPerClientCapacityRecheckRace' -v && go test -race -count=1 ./... && $(go env GOROOT)/bin/gofmt -l .</automated>
  </verify>
  <acceptance_criteria>
    - 三新测全 PASS（-race 下含竞态注入测 10 轮内稳定——必要时 -count=3 复跑确认无 flake）
    - `grep -c 'TestPerClientSpawnFailure\|TestPerClientCapacityGate\|TestPerClientCapacityRecheckRace' internal/server/perclient_test.go` == 3
    - `grep -c 'failed to start process' internal/server/perclient_test.go` ≥ 1 且 `grep -c 'server is at capacity' internal/server/perclient_test.go` ≥ 2（两 wire 文案逐字断言位）
    - `grep -c 'PCSessionsLenForTest' internal/server/export_test.go` == 1 且 `grep -c 'PCSessionsLenForTest()' internal/server/perclient_test.go` ≥ 3
    - `grep -c 'spawn_failed' internal/server/perclient_test.go` ≥ 1 且 `grep -c 'max_clients' internal/server/perclient_test.go` ≥ 1（两事件断言位）
    - `go test -race -count=1 ./...` 全仓全绿；既有 *_test.go 零改动（git diff 中仅 perclient_test.go 与 export_test.go 两文件，且 export_test.go 无删除行）
  </acceptance_criteria>
  <done>Pitfall 5 清理清单（release 恰好一次/不进注册表/仅 spawn_failed 单事件/零敏感值/他端零感知）与容量闸三形态（③位 503 既有面 + D-02 1011 闸 + D-03 复检回收含收割）全部行为锁定；硬不变量经竞态注入实测成立。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 已认证客户端 → spawn 路径容量面 | per-client 下每连接 = 一个子进程；容量闸是昂贵资源的唯一硬帽 |
| 并发升档竞态窗口（两 attach 同过 pre-spawn 闸） | 注册点复检是唯一兜底；回收路径的信号/收割序列化直面 pgid 复用风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03a | Denial of Service | 并发 attach 竞态超编（两升档同过闸 → 瞬时进程数 > maxClients） | high | mitigate | D-03 注册点复检回收：同一 hubMu 持有内复检，超编者 SignalGroup(HUP)+Drain+Close+Wait 完整回收；TestPerClientCapacityRecheckRace barrier 注入实测「恰一胜一负 + 终态 ==1 + 败者 ESRCH」 |
| T-11-03b | Information Disclosure | 容量/失败拒绝文案或事件携带内部状态（路径/errno/argv） | high | mitigate | 文案定值常量（"server is at capacity" / "failed to start process"）；事件四段 schema 零敏感值；TestPerClientSpawnFailure 断言事件不含注入错误文本与 argv 路径 |
| T-11-03c | Tampering | 回收路径 kill-after-reap（AfterFunc 补 KILL 打中复用 pgid） | high | mitigate | reapOrphanSession 局部 reaped 原子闸（Wait 返回置位后才放行 KILL 豁免）——Pitfall 2 同构；唯一收割者纪律（回收 goroutine 是该孤儿会话唯一 Wait 调用方） |
| T-11-SC | Tampering | 依赖供应链 | high | accept | 零新依赖；go.mod/go.sum 零漂移 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V5 Input Validation（容量策略在 spawn 前强制） | ✓ pre-spawn 闸 + 注册点复检双位（Task 1） |
| V7 Error Handling（拒绝文案不含内部状态） | ✓ 定值常量 + 事件零敏感值断言（Task 2） |
| V8 Audit（容量拒绝可检索归因） | ✓ max_clients 事件四段 schema（Task 1/2） |
</threat_model>

<verification>
- `go test -race -count=1 ./...` 全仓全绿（shared 期望值逐字未动）
- 三新测锁定：spawn 失败 Pitfall 5 清单 / D-02 linger 形态 1011 容量文案 / D-03 barrier 竞态「恰一胜一负 + 败者收割」
- `git diff --stat` 仅含 perclient.go / perclient_test.go / export_test.go（后者 append-only 无删除行）
</verification>

<success_criteria>
- ROADMAP SC2 完整成立：spawn 失败该客户端收类型化 Error 帧 + 1011，服务端与其他客户端不受影响（测试实证）
- 「并发子进程数 ≤ maxClients」硬不变量在竞态注入下成立（D-03——Phase 13 裁决项④提前消解）
- D-02 wire 形态（1011 + 容量文案）经 Go 测试锁定，为 11-05 phase11.mjs S6 提供协议层对照
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **PC-02 (unclassified, unresolved)**：探针未分类——本 plan 承担 PC-02 的拒绝面（spawn 失败清单 + 容量闸）；attach spawn 主链已由 11-01 tracer 覆盖。保持 flagged-unverified，由 11-06 收口闸承载显式证据。
- **D-02 闸的确定性注入形态（planner 细化）**：顺序 attach 下第二客户端会在 ③位 HTTP 闸收 503（registry.n 计数既有行为，零改动）——D-02 的 WS 面 1011 闸确定性触发需「注册表空出但 pcSessions 满」窗口，测试以 trap-免疫 linger 形态注入（A 断开待收割滞留 → B attach 过 ③位后命中）；此形态同时实证「断开 SIGHUP 已发但 HUP 免疫」中间态。phase11.mjs S6 将用同形态（11-05 已锚定）。
- **回收路径 reaped 闸用局部原子量而非 hubMu**（planner 细化）：孤儿会话从未注册 pcSessions、无 watcher——局部 atomic.Bool 与主 teardown 路径的 hubMu 内标志位同档语义（Pitfall 2：信号与收割序列化），且避免回收 goroutine 与 hubMu 的无谓耦合。
- **pty 包「N 次连续 spawn 失败后进程 fd 计数不变」测试归属登记（planner 裁定：归 Phase 12，本 phase 不承载）**：PITFALLS.md:124 该测试项为两句——server 包半句（spawn 失败注入桩清理清单逐条 + 他端零感知）已由本 plan Task 2 TestPerClientSpawnFailure 完整承载；pty 包半句（fd 计数不变断言）本 phase 无承载，显式登记归 **Phase 12 EMFILE/限额标定**。理由：①P5「Phase to address」行（PITFALLS.md:131）划分为 P11=失败 UX 与清理 / P12=EMFILE 与限额标定，:370 映射表「P11/P12」共享标注按此切分落实——fd 计数不变量是泄漏限额语义（EMFILE 侧）而非失败 UX 侧；②本 phase 对 internal/pty/ 的唯一改动面是 11-02 darwin dup-watch 防御，spawn 失败路径零改动——fd 断言与 Phase 12 限额/令牌桶测试同批落地可共用注入夹具，本 phase 另起 pty 包测试文件属 scope 蔓延；③EMFILE 级联的可见性消费面（wesh_pty_spawn_failures_total）经 D-04 锁定归 Phase 13 metrics，fd 断言在本 phase 先行无独立消费面。Phase 12 规划时须自本登记认领该项（避免 P5 测试面两半套漂移）。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- upgradePerClient 内 pre-spawn 容量再闸分支（internal/server/perclient.go——D-02 wire 形态）
- upgradePerClient 注册段 D-03 复检回收分支（internal/server/perclient.go）
- 方法 `(s *Server) reapOrphanSession(pcSess *pty.Session)`（internal/server/perclient.go——孤儿会话异步回收 + 局部 reaped 原子闸）
- 出口 `(s *Server) PCSessionsLenForTest() int`（internal/server/export_test.go——append-only）
- harness 演进 `startPerClientServerWithSpawn` + `spawnedSessions` 访问器（internal/server/perclient_test.go）
- 测试 `TestPerClientSpawnFailure` / `TestPerClientCapacityGate` / `TestPerClientCapacityRecheckRace`（internal/server/perclient_test.go）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/11-per-client/11-03-SUMMARY.md` when done
</output>
