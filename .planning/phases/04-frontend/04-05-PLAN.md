---
phase: 04-frontend
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-03"]
files_modified:
  - web/src/main.ts
  - web/src/lib/prefs.ts
  - web/src/lib/prefs.test.ts
  - web/dist/index.html
autonomous: true
requirements:
  - FE-06
  - FE-07

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "query 构造前解析：location.search 经 parseQueryPrefs——白名单 10 键（8 xterm + resizeOverlay/confirmBeforeUnload）逐键 JSON.parse，成功项记入 queryKeys 集合并作为 Terminal 构造选项初值；非法 JSON/白名单外键静默忽略 + console.warn（D-16：用户侧输入不该让终端不可用）；osc52 不在 query 白名单（D-12 安全不对称——node --test 专项锁定）；theme 键构造特判——经 mergeTheme 与 defaultTheme 合并后作构造 theme（部分 theme 未指定键保留 tango 调色板，与 WELCOME prefs 通道同源——RESEARCH §Pitfall 3 修正覆盖两通道；非对象 theme 值中和为 defaultTheme + console.warn）"
    - "theme 常量化：构造选项中的 tango 调色板对象提为模块级 defaultTheme 常量——构造初值、query 特判合并与 WELCOME 运行期合并三处同源（RESEARCH §Pitfall 3 修正前提，调色板单一事实源）"
    - "WELCOME prefs 应用装配顺序（UI-SPEC §Prefs Contract 逐字）：逐项遍历 prefs——键在 queryKeys 中跳过（query 优先，D-16）；否则 xterm 键经 term.options.{key} = value 运行时应用；theme 键为 term.options.theme = { ...defaultTheme, ...prefs.theme } 合并后整体赋值（D-19 整体替换语义保留 + 未指定键保留 wesh 调色板——RESEARCH §Open Questions 1 定稿采纳合并；非对象 theme 值忽略 + console.warn）"
    - "全部应用完 → fit.fit() 重算（fontSize/lineHeight/letterSpacing 改变单元格尺寸——D-13；不 fit 则 cols/rows 与视口不符远端 TUI 画错，RESEARCH §Pitfall 7）→ 既有 onResize→RESIZE 帧链路自动同步服务端"
    - "behavior 两键（resizeOverlay/confirmBeforeUnload）布尔校验后写 04-03 埋好的开关量（typeof v === 'boolean' 才应用，非布尔忽略 + console.warn）；跳过 queryKeys；应用位置在 WELCOME 分支 welcomeDone 置位与 beforeunload 条件注册之前（开关值在注册点已是最终态，04-03 的条件注册代码零改动）"
    - "osc52 === true 且 clipboardOK 时 loadAddon(new ClipboardAddon(undefined, writeOnly))——provider readText 恒 Promise.resolve('')（RESEARCH §Pitfall 4 定稿：reject 在核心异步 OSC 链 rethrow 成 unhandled rejection，resolve 空串协议完整且零泄露同等安全）、writeText 委托 navigator.clipboard.writeText（D-12 只写不读）；clipboardOK 为 false 不加载（OSC52 惰性）"
    - "优先级落地：URL query > --client-option > 内置默认（D-16——queryKeys 跳过机制即优先级实现）"
    - "node --test 锁定 parseQueryPrefs（合法键解析/非法 JSON 忽略/未知键忽略/osc52 排除/theme 对象解析）、splitPrefs（xterm 键与 behavior 键分派）与 mergeTheme（部分 theme 合并保留 base 未指定键、非对象入参返回 null）"
    - "web/dist/index.html 重建入库（含 prefs 接线检索串；.gz 按 .gitignore 既定策略不入库）"
  prohibitions:
    - "query/prefs/client-option 三通道禁止应用白名单外任何 xterm option（D-14：allowProposedApi 等危险面由同一白名单结构性排除——前端 parseQueryPrefs 与 Go 侧 ValidClientOptionKey 语义同源）"
    - "OSC52 provider 禁止提供真实 readText（只写不读——Warp CVE-2025-48725 教训；readText 恒 resolve 空串，任何返回真实剪贴板内容的形态都是安全姿态破坏）"
    - "禁止把 behavior 键（resizeOverlay/confirmBeforeUnload）写入 term.options（非 xterm 选项——只写前端开关量，UI-SPEC §Prefs Contract 步骤 4）"
  artifacts:
    - path: "web/src/lib/prefs.ts"
      provides: "白名单键常量 + parseQueryPrefs + splitPrefs + mergeTheme 纯函数"
      contains: "parseQueryPrefs"
    - path: "web/src/lib/prefs.test.ts"
      provides: "node --test query 解析回归锁（含 osc52 排除与 mergeTheme 合并专项）"
      contains: "node:test"
    - path: "web/src/main.ts"
      provides: "query 解析（构造前）+ query theme 特判合并 + WELCOME prefs 应用 + defaultTheme 常量 + OSC52 write-only 加载"
      contains: "queryKeys"
    - path: "web/dist/index.html"
      provides: "重建后的单文件产物"
      contains: ""
  key_links:
    - from: "web/src/main.ts"
      to: "web/src/lib/prefs.ts"
      via: "构造前 parseQueryPrefs(location.search) 与 WELCOME 分支 splitPrefs 应用（纯函数 node --test 可测）"
      pattern: "parseQueryPrefs"
    - from: "web/src/main.ts"
      to: "internal/server/server.go"
      via: "WELCOME 分支消费 04-01 通道下发的 w.prefs（Welcome JSON 可选键，缺省则零处理）"
      pattern: "w.prefs"
    - from: "web/src/main.ts"
      to: "web/src/main.ts (构造选项段)"
      via: "defaultTheme 常量被构造初值、query theme 特判（mergeTheme）与 WELCOME prefs theme 合并三处同源使用"
      pattern: "defaultTheme"
---

<objective>
FE-07 前端半侧：URL query 覆盖（构造前解析 + queryKeys + theme 特判合并）→ WELCOME prefs 应用（跳过 queryKeys + term.options 运行时 + theme 合并 + fit 重算）→ FE-06 两开关接线 → OSC52 write-only 条件加载。

Purpose: D-13/D-16 的装配顺序与优先级、D-19 的 theme 表达、D-12 的 OSC52 服务端意图落地在此闭合；RESEARCH 两个实现级修正（theme 合并保留调色板——query 与 WELCOME prefs 两通道同源、readText resolve 空串）在本 plan 定稿。
Output: lib/prefs.ts 纯函数 + node --test + main.ts 接线 + dist 重建 + 构建绿。

规格探针标记（spec-less probe fallback）：FE-07 探针 unclassified 的渲染半侧（query 解析容错/优先级/应用后 fit）经 D-13..D-16 与 UI-SPEC §Prefs Contract 防御性核销为显式 truths；FE-06 开关接线半侧同。三条 must-NOT（白名单外 option/OSC52 真读/behavior 键写 term.options）经 §B 回忆落入 prohibitions（descriptor-less）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend/04-CONTEXT.md
@.planning/phases/04-frontend/04-RESEARCH.md
@.planning/phases/04-frontend/04-UI-SPEC.md
@web/src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/prefs.ts 纯函数 + node --test + query 构造前解析与 theme 常量化</name>
  <files>web/src/lib/prefs.ts, web/src/lib/prefs.test.ts, web/src/main.ts</files>
  <read_first>
    - web/src/main.ts（Terminal 构造选项 21-56 行现状——theme 对象常量化提取点；import 段；04-02/04-03 合入后的模块级状态区）
    - web/tsconfig.json（04-02 已加 exclude——test 文件不经 tsc 纪律）
    - .planning/phases/04-frontend/04-RESEARCH.md（§Pattern 6 装配顺序与核实注、§Pitfall 3 theme 逐键回退机制、§Pitfall 8 query 容错）
    - .planning/phases/04-frontend/04-UI-SPEC.md（§Prefs Contract 白名单/优先级/装配顺序/query 值形态示例逐字契约）
  </read_first>
  <action>
    1. 新建 `web/src/lib/prefs.ts`：
       - 导出 `XTERM_PREF_KEYS: readonly string[]` = ['fontSize','fontFamily','cursorBlink','cursorStyle','scrollback','lineHeight','letterSpacing','theme']（8 个 xterm 视觉键，D-14；注释：与 Go 侧 proto.ValidClientOptionKey 语义同源——两侧同白名单）。
       - 导出 `BEHAVIOR_PREF_KEYS: readonly string[]` = ['resizeOverlay','confirmBeforeUnload']（FE-06 两开关，非 xterm 选项）。
       - 导出 `parseQueryPrefs(search: string): { prefs: Record<string, unknown>; keys: Set<string>; invalid: string[] }`——new URLSearchParams(search) 逐 [k, v]：k 不在 XTERM∪BEHAVIOR（osc52 结构性不在内，D-12）→ 跳过；JSON.parse(v) 失败 → invalid.push(k) 跳过；成功 → prefs[k] = 解析值、keys.add(k)。注释引 D-16（非法静默忽略 + warn 由调用方打——纯函数不打日志）。
       - 导出 `splitPrefs(prefs: Record<string, unknown>): { xterm: Record<string, unknown>; behavior: Record<string, unknown> }`——按键名分派两组（osc52 键不属于任一组——由调用方单独处理，注释引 D-12）。
       - 导出 `mergeTheme(base: Record<string, string>, incoming: unknown): Record<string, string> | null`——incoming 为普通对象（typeof === 'object' && 非 null && 非数组）时返回 `{ ...base, ...incoming }`（未指定键保留 base 调色板）；否则返回 null。注释引 RESEARCH §Pitfall 3：构造段（query 通道）与 WELCOME 分支（prefs 通道）theme 合并同源经此函数，两通道行为一致。
    2. 新建 `web/src/lib/prefs.test.ts`（node:test + node:assert/strict + 相对导入带 .ts 扩展名，同 title.test.ts 形态）：用例——'?fontSize=16&cursorBlink=false' 两键解析且 keys 含两键；'?fontFamily=Menlo'（裸词非法 JSON）→ 跳过且 invalid 含 fontFamily（UI-SPEC 示例行）；未知键 '?foo=1' → 跳过；'osc52=true' → 跳过（D-12 专项——query 永不可开启 OSC52）；theme URL 编码 JSON 对象 → 解析为对象；splitPrefs 把 fontSize 分派 xterm、resizeOverlay 分派 behavior、osc52 不进任一组；mergeTheme 专项——以 tango 子集 `{ foreground: '#ffffff', background: '#000000', red: '#cc0000' }` 为 base，合并 `{ background: '#101020' }` 后 background 被覆盖且 foreground/red 等未指定键保留（部分 theme 不丢调色板）；mergeTheme(base, 非对象/数组/null) → null。
    3. `web/src/main.ts`：
       - import 段加 `import { parseQueryPrefs, splitPrefs, mergeTheme } from './lib/prefs';`。
       - theme 常量化：构造选项中的 tango 对象原值提为模块级 `const defaultTheme = { ... } as const;`（值逐字不变——前景/背景/cursor/selectionBackground/16 色 ANSI 全表），构造处改为 `theme: defaultTheme`（注释：RESEARCH §Pitfall 3 修正前提——运行期与构造期 {...defaultTheme, ...incoming} 合并需要构造时对象可引用；调色板单一事实源）。
       - Terminal 构造前插入 query 解析：`const query = parseQueryPrefs(location.search);` `const queryKeys = query.keys;`（模块级常量供 WELCOME 分支引用）；`for (const k of query.invalid) { console.warn('ignoring invalid query pref:', k); }`（D-16 静默忽略 + warn）；`const queryParts = splitPrefs(query.prefs);`，构造选项字面量尾部展开 `...queryParts.xterm`（query 的 xterm 键作构造初值——内置默认 ← query 覆盖先行，注释引 UI-SPEC 装配顺序步骤 1）。
       - query theme 构造特判（RESEARCH §Pitfall 3 修正覆盖 query 通道——部分 theme 丢调色板问题不止 WELCOME 路径，两通道行为须一致）：展开前若 `'theme' in queryParts.xterm`，`const merged = mergeTheme(defaultTheme, queryParts.xterm.theme);`——merged 非 null → `queryParts.xterm.theme = merged`（部分 theme 与 defaultTheme 合并，未指定键保留 tango 调色板，与 WELCOME prefs 通道 Task 2 同源同语义）；merged 为 null（非对象 theme 值）→ console.warn 后 `queryParts.xterm.theme = defaultTheme`（中和非法值为默认调色板，D-16 容错终端不挂）。
       - query 的 behavior 键即startup应用：遍历 `queryParts.behavior`，对 resizeOverlay/confirmBeforeUnload 两键 typeof 值 === 'boolean' 时写 04-03 的对应开关量（非布尔忽略 + console.warn——注释：boolean 校验因服务端对值只验 JSON 不验类型，前端防御性应用）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && node --test web/src/lib/prefs.test.ts && pnpm -C web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - node --test 退出 0（含 osc52 排除、splitPrefs 分派与 mergeTheme 合并专项）
    - tsc --noEmit 退出 0
    - main.ts 构造前完成 query 解析（queryKeys 模块级可见）；构造选项含 query xterm 键展开；defaultTheme 常量被构造引用
    - 构造段 theme 特判存在：`grep -q 'mergeTheme(defaultTheme' web/src/main.ts` 成立——query 通道部分 theme 经 mergeTheme 与 defaultTheme 合并后作构造 theme（未指定键保留 tango 调色板），非对象 theme 值中和为 defaultTheme + console.warn
    - mergeTheme 单测锁定：部分 theme 合并保留 base 未指定键、非对象入参返回 null
    - query behavior 键在 startup 写入开关量（boolean 校验）
  </acceptance_criteria>
  <done>query 通道打通：白名单键构造前生效、非法输入静默降级、theme 调色板常量化且 query 部分 theme 经合并保留调色板（与 WELCOME prefs 通道同源）。</done>
</task>

<task type="auto">
  <name>Task 2: WELCOME prefs 应用 + theme 合并 + behavior 开关 + OSC52 write-only 加载</name>
  <files>web/src/main.ts</files>
  <read_first>
    - web/src/main.ts（WELCOME 分支现状——04-02 的 isRO/setTitle、04-03 的 welcomeDone/beforeunload 条件注册挂点；fit 实例 58 行；ClipboardAddon import 待加位）
    - .planning/phases/04-frontend/04-RESEARCH.md（§Pattern 6 装配顺序逐字、§Pitfall 3 合并建议、§Pitfall 4 readText resolve '' 机制、§Pattern 4③ provider 构造第二参核实注）
    - .planning/phases/04-frontend/04-UI-SPEC.md（§Prefs Contract 装配顺序步骤 2-4 逐字契约、§Clipboard Contract OSC52 段）
  </read_first>
  <action>
    1. import 段补 `import { ClipboardAddon } from '@xterm/addon-clipboard';`（04-02 已装包，本 task 随用随加——noUnusedLocals 纪律）。
    2. WELCOME 分支在 ro 处理之后、welcomeDone 置位之前插入 prefs 应用段（UI-SPEC §Prefs Contract 步骤 2-4 逐字顺序）：
       - 取 prefs：`const prefs = (w.prefs && typeof w.prefs === 'object') ? w.prefs as Record<string, unknown> : null;`（w 为既有 JSON.parse 结果；prefs 缺省则整段跳过——nil 兼容）。
       - xterm 键应用：`const parts = splitPrefs(prefs);` 遍历 parts.xterm 的 [k, v]——`if (queryKeys.has(k) { continue; }`（query 优先，D-16）；k === 'theme' 时 `if (typeof v === 'object' && v !== null && !Array.isArray(v)) { term.options.theme = { ...defaultTheme, ...(v as Record<string, string>) }; } else { console.warn('ignoring non-object theme pref'); }`（注释引 D-19 与 RESEARCH §Pitfall 3：合并保留 wesh 调色板未指定键——部分 theme 不再把 tango 冲成 xterm 内建默认）；其余键 `(term.options as unknown as Record<string, unknown>)[k] = v;`（动态键赋值经一次收窄 cast——注释：白名单已保证键合法性，ITerminalOptions 运行时逐键赋值 xterm 原生支持，RESEARCH §Pattern 6 核实注）。
       - `fit.fit();`（全部应用完后重算——注释引 D-13 与 RESEARCH §Pitfall 7：不 fit 则单元格尺寸变化不同步，远端 TUI 画错；既有 onResize→RESIZE 帧链路自动同步服务端）。
       - behavior 键应用：遍历 parts.behavior 的 [k, v]——queryKeys 跳过；`typeof v === 'boolean'` 时按 k 写 resizeOverlayOn/confirmBeforeUnloadOn（非布尔 console.warn 忽略）；注释：位置必须在下方 welcomeDone/beforeunload 注册点之前——开关值在注册点已最终态。
       - osc52：`if (prefs.osc52 === true && clipboardOK) { const writeOnly = { readText: (): Promise<string> => Promise.resolve(''), writeText: (_sel: unknown, text: string): Promise<void> => navigator.clipboard.writeText(text) }; term.loadAddon(new ClipboardAddon(undefined, writeOnly)); }`——注释逐条引：D-12（仅 --osc52 服务端可开启，只写不读）、RESEARCH §Pitfall 4（readText resolve '' 而非 reject——核心异步 OSC 链对 rejected promise rethrow 成 unhandled rejection，resolve 空串协议完整且零泄露）、§Pattern 4③（provider 是构造第二参；核心无内建 OSC52 handler——不加载则惰性无害）、clipboardOK 门（非安全上下文不加载，OSC52 惰性）。provider 对象形状以 addon-clipboard d.ts 的 IClipboardProvider 为准对齐签名（_sel 参数类型按 d.ts 收窄，不用 unknown 硬扛时以 d.ts 为准）。
    3. 顺带同步 main.ts:6-11 帧常量注释块：在 WELCOME 帧说明处补 prefs——"Welcome 载荷 {mode, prefs?}——prefs 为可选偏好下发字段（D-13 omitempty，proto.go WelcomePayload.Prefs 同形）"（PATTERNS §Shared Patterns 两侧注释同源要求，对齐 04-01 已更新的 proto.go 头注释口径；注释只加 prefs 句，既有帧常量行不动）。
    4. 纪律自查：behavior 键未写 term.options；queryKeys 跳过逻辑同时覆盖 xterm/behavior 两组；osc52 不经 queryKeys（它不可能出现在 query——lib 层已结构性排除）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && pnpm -C web exec tsc --noEmit && grep -q 'defaultTheme' web/src/main.ts && grep -q 'ClipboardAddon' web/src/main.ts && grep -q "Promise.resolve('')" web/src/main.ts && head -15 web/src/main.ts | grep -q 'prefs'</automated>
    <human-check>prefs 下发视觉效果/query 覆盖/theme 合并/OSC52 写入的浏览器确认并入 04-06 的 04-UAT.md 清单</human-check>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit 退出 0
    - WELCOME 分支 prefs 应用顺序为：xterm 键（queryKeys 跳过 + theme 合并）→ fit.fit() → behavior 键 → osc52 条件加载 → 既有 welcomeDone/beforeunload 注册点
    - theme 应用为 {...defaultTheme, ...} 合并形态且非对象 theme 有 warn 分支
    - ClipboardAddon 仅在 prefs.osc52 === true && clipboardOK 时加载；provider readText 恒 resolve 空串
    - behavior 键不出现 term.options 写入路径
    - main.ts 顶部帧常量注释块（6-11 行区）含 prefs 说明（Welcome 载荷 {mode, prefs?}——与 04-01 proto.go 头注释口径一致）
  </acceptance_criteria>
  <done>FE-07 前端闭环：服务端偏好经 Welcome 原子到达并按优先级应用，theme 合并不丢调色板，OSC52 write-only 按服务端意图启用。</done>
</task>

<task type="auto">
  <name>Task 3: dist 重建与产物提交 + 端到端冒烟</name>
  <precondition>Task 1/2 已合入（dist 必须由含 prefs 接线的源码构建）。</precondition>
  <files>web/dist/index.html</files>
  <read_first>
    - README.md（构建节纪律与产物提交约定）
    - 仓库根 .gitignore（dist/*.gz 不入库既定策略）
  </read_first>
  <action>
    1. `time pnpm -C web build` 全量重建；校验产物时间戳新于 main.ts 改动；grep 产物含 osc52、resizeOverlay、confirmBeforeUnload 检索串（prefs 接线入包证据）。
    2. `go build -o /tmp/wesh-uat/wesh ./cmd/wesh` 后冒烟：`/tmp/wesh-uat/wesh --bind 127.0.0.1 --port 0 --client-option fontSize=18 -- true` 启动行解析证明 CLI→serve 链活（node 一行脚本或 phase04.mjs 的 startWesh 形态复用——不新造文件；也可用 `node web/uat/phase04.mjs /tmp/wesh-uat/wesh` 重跑十场景作为通道回归）。
    3. git add web/dist/index.html 随本 plan 提交（.gz 不入库既定策略）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && time pnpm -C web build && grep -q 'osc52' web/dist/index.html && grep -q 'resizeOverlay' web/dist/index.html && go build -o /tmp/wesh-uat/wesh ./cmd/wesh && node web/uat/phase04.mjs /tmp/wesh-uat/wesh</automated>
  </verify>
  <acceptance_criteria>
    - 构建退出 0；dist 含 osc52/resizeOverlay 检索串
    - phase04.mjs 十场景重跑全绿（prefs 通道端到端回归）
    - web/dist/index.html 更新入库
  </acceptance_criteria>
  <done>dist 与 prefs 前端同步入库；CLI→Welcome→前端应用的 FE-07 全链经 UAT 十场景证明。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query → 终端配置 | query 是用户侧输入——非法值不得让终端不可用，白名单外键不得生效 |
| 服务端 prefs → term.options 运行时 | Welcome prefs 驱动 xterm 运行时行为面；OSC52 开启把远程序列接到系统剪贴板写 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13 | Elevation of Privilege | query/prefs 注入白名单外 option（allowProposedApi 等危险面） | high | mitigate | 前端白名单常量与 Go 侧 ValidClientOptionKey 语义同源；osc52 结构性排除出 query（D-12/D-14/D-16）+ node --test 专项锁 |
| T-04-14 | Information Disclosure | OSC52 远程读剪贴板（Warp CVE-2025-48725 形态） | high | mitigate | write-only provider——readText 恒 resolve ''（RESEARCH §Pitfall 4）；默认关 + 仅 --osc52 服务端开启 + clipboardOK 门 |
| T-04-15 | Tampering | OSC52 读查询触发 unhandled rejection 噪音（reject 形态） | low | mitigate | readText resolve '' 替代 reject（机制核实：核心异步 OSC 链 rethrow rejected promise） |
| T-04-16 | DoS | 非法 query 值使终端启动失败 | medium | mitigate | 逐键 JSON.parse try/catch 静默忽略 + console.warn（D-16）+ node --test 容错行 |
| T-04-SC | Tampering | npm installs（addon-clipboard 运行期加载） | high | accept | 钉版与 js-base64 override 已于 04-02 完成（RESEARCH 审计全 OK + postinstall 空）；本 plan 零新安装 |
</threat_model>

<verification>
- `node --test web/src/lib/prefs.test.ts` 退出 0
- `pnpm -C web exec tsc --noEmit && time pnpm -C web build` 退出 0，dist 含 prefs 接线检索串
- `node web/uat/phase04.mjs /tmp/wesh-uat/wesh` 十场景重跑全绿
- 浏览器人工确认（prefs 视觉效果/query 覆盖/theme 合并/OSC52）在 04-06 的 04-UAT.md 清单汇总执行
</verification>

<success_criteria>
- FE-07 闭环：query > --client-option > 内置默认 优先级落地，prefs 应用后 fit 重算
- FE-06 开关接线：resizeOverlay/confirmBeforeUnload 经 prefs/query 可关
- D-12 闭合：OSC52 服务端专有开启 + write-only provider
- RESEARCH 两修正定稿落地：theme 合并保留调色板（query 构造段 + WELCOME prefs 两通道同源，README "未指定的色键保留内置调色板" 承诺对两通道成立）、readText resolve ''
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `web/src/lib/prefs.ts`：`XTERM_PREF_KEYS` / `BEHAVIOR_PREF_KEYS` 常量、`parseQueryPrefs()` / `splitPrefs()` / `mergeTheme()`（新导出符号）
- `web/src/lib/prefs.test.ts`：node --test 用例集
- `web/src/main.ts`：`defaultTheme` 常量、`queryKeys` 集合、query theme 构造特判（mergeTheme 合并）、WELCOME prefs 应用段（xterm 键/theme 合并/fit/behavior 键）、OSC52 write-only provider 与 ClipboardAddon 条件加载、帧常量注释块 prefs 说明同步
- `web/dist/index.html`（重建产物）

<output>
Create `.planning/phases/04-frontend/04-05-SUMMARY.md` when done
</output>
