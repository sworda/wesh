---
phase: 04-frontend
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - web/src/main.ts
  - web/index.html
  - web/dist/index.html
autonomous: true
requirements:
  - FE-05
  - FE-06

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "启动时检测 navigator.clipboard 存在性并记忆为 clipboardOK（[SecureContext] 接口——明文 HTTP 非 localhost 下属性本身 undefined，不检测即调用抛 TypeError，RESEARCH §Pitfall 5）"
    - "选中即复制：term.onSelectionChange → 150ms trailing debounce（拖动选择期间合并只写最终选区）→ term.getSelection() 非空且与上次写入不同 → navigator.clipboard.writeText；写失败 .catch → console.warn 静默（D-09/D-11，UI-SPEC §Clipboard Contract）"
    - "粘贴：window keydown Ctrl+Shift+V → preventDefault → navigator.clipboard.readText → term.paste（走既有 onData→INPUT 帧链路，bracketed paste 语义保留）；clipboardOK 与 isRO 双门——ro 模式永不触发读剪贴板（D-10：ro 下 INPUT 本就被服务端丢弃，读只会换来无谓权限弹窗）；读拒绝 .catch → console.warn 静默"
    - "非安全上下文降级：clipboardOK 为 false 时选中复制与 Ctrl+Shift+V 整体静默不生效（不弹错、不落 execCommand 旧 API——D-11；README 明示义务在 04-06）"
    - "resize 浮层：#resize-overlay 为 body 直属第三个顶层元素（默认 hidden），样式 position fixed top/right 8px、padding 4px 8px、background rgba(0,0,0,0.6)、color #ffffff、border-radius 4px、12px/400/1.5 终端等宽栈、pointer-events none、user-select none、z-index 900、transition opacity 200ms ease；内容 {cols}x{rows}（ASCII x，服务端钳制 1000×1000 → 最长 9 字符无溢出）；term.onResize 驱动——更新文本 + opacity 1 显示 + 重置 600ms 静止后 opacity 0 淡出（D-17 默认开，UI-SPEC §Resize Overlay Spec）"
    - "浮层双门：仅 welcomeDone 置位后响应（浮层是会话辅助不是启动尺寸指示器——onopen 初次 fit 不触发）且 resizeOverlayOn 为 true（04-05 经 prefs/query 可关）；ro 模式同样显示（ro 下 RESIZE 帧本就放行，P2 协议基线）"
    - "beforeunload：handler 仅调 preventDefault() 触发浏览器标准确认框（无自定义文案——现代浏览器一律忽略，D-18 注明）；WELCOME 处理完成时 confirmBeforeUnloadOn 为 true 才注册（默认开，ro 同启；开关接线在 04-05）；WS onclose 任意路径（含状态面板展示后、auth_failed 重试前）移除 listener——Session ended 后关页不再被拦截"
    - statement: "真实浏览器权限模型下：选中即复制生效（Chromium transient activation 窗口远大于 150ms 防抖）、Ctrl+Shift+V 粘贴生效、权限拒绝静默、浮层时序与淡出、beforeunload 拦截与会话终结后放行（sticky activation——零交互直接关页不弹框为浏览器预期语义，RESEARCH §Open Questions 3 已裁决接受）"
      verification: backstop
  prohibitions:
    - "剪贴板读取禁止脱离显式用户手势（D-10：readText 仅由 Ctrl+Shift+V 触发；ro 模式永不读；任何后台/定时/事件驱动的读剪贴板都是隐私越界）"
    - "禁止任何形式回落 document.execCommand（MDN 明示 deprecated 的 ttyd 技术债——clipboard 不可用时整体静默，不落旧 API，D-11）"
    - "beforeunload 禁止写自定义文案、禁止会话终结后保留 listener（MDN：文案被浏览器忽略；Session ended 后关页不得再被拦截——重试路径同样先移除再由新 WELCOME 重注册）"
  artifacts:
    - path: "web/src/main.ts"
      provides: "clipboardOK 门控 + 选中复制防抖 + Ctrl+Shift+V 粘贴 + 浮层驱动 + beforeunload 注册/移除"
      contains: "onSelectionChange"
    - path: "web/index.html"
      provides: "#resize-overlay 元素与样式"
      contains: "resize-overlay"
    - path: "web/dist/index.html"
      provides: "重建后的单文件产物"
      contains: ""
  key_links:
    - from: "web/src/main.ts"
      to: "web/index.html"
      via: "#resize-overlay 元素（HTML 静态声明 hidden + CSS 样式）由 main.ts onResize handler 驱动显隐"
      pattern: "resize-overlay"
    - from: "web/src/main.ts"
      to: "web/src/main.ts (WELCOME 分支)"
      via: "welcomeDone 置位与 beforeunload 条件注册挂 WELCOME 处理完成点（04-05 在同一分支接入 prefs 开关，先于注册点翻转开关值）"
      pattern: "welcomeDone"
---

<objective>
FE-05（选中即复制 navigator.clipboard 现代 API + Ctrl+Shift+V 粘贴）与 FE-06（resize COLSxROWS 浮层 + 离开页面前确认）落地，全部走 D-09..D-11/D-17/D-18 锁定的安全门控形态。

Purpose: 替代 ttyd 已废弃 execCommand 技术债；浮层与离开确认是 ttyd 先例的辅助交互对等项。开关（prefs/query 可关）的接线在 04-05 完成，本 plan 埋好 resizeOverlayOn/confirmBeforeUnloadOn/welcomeDone 三个模块级开关量。
Output: main.ts 事件接线 + index.html 浮层元素 + dist 重建 + 构建绿。

规格探针标记（spec-less probe fallback）：FE-05 concurrency 探针（并发/中断保证）核销为「150ms trailing debounce 合并并发选区变化只写最终值；写失败静默不影响终端主流程」显式 truth；FE-06 探针报 unclassified——UI-SPEC §Resize Overlay Spec/§Auxiliary Interactions 已防御性钉死时序/样式/开关语义，按 §C 核销为显式 truths；真实浏览器权限/手势模型不可自动化落 backstop truth（verify 时无显式证据则 human_needed）。三条 must-NOT（手势外读剪贴板/execCommand 回落/unload 文案与残留 listener）经 §B 回忆落入 prohibitions（descriptor-less）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend/04-CONTEXT.md
@.planning/phases/04-frontend/04-RESEARCH.md
@.planning/phases/04-frontend/04-UI-SPEC.md
@web/src/main.ts
@web/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: 剪贴板——clipboardOK 检测 + 选中即复制防抖写 + Ctrl+Shift+V 粘贴</name>
  <files>web/src/main.ts</files>
  <read_first>
    - web/src/main.ts（被改文件现状全文——含 04-02 合入后的 isRO 模块级状态、onData 常驻接线 92-97 行形态、concat/INPUT 常量）
    - .planning/phases/04-frontend/04-RESEARCH.md（§Pattern 4 三形态逐段代码契约 + §Pitfall 5 安全上下文三态与各浏览器权限模型 + §Anti-Patterns 存在性门控）
    - .planning/phases/04-frontend/04-UI-SPEC.md（§Clipboard Contract 防抖/粘贴/降级逐字契约）
  </read_first>
  <action>
    1. 模块级加 `const clipboardOK = typeof navigator.clipboard !== 'undefined';`（注释引 D-11 与 RESEARCH §Pitfall 5：[SecureContext] 接口在明文 HTTP 非 localhost 下属性本身 undefined，存在性门控防 TypeError；缺失时选中复制与粘贴整体静默）。
    2. 选中即复制（常驻接线段，注释引 D-09）：模块级 `let selTimer: number | undefined; let lastCopied = '';`；`term.onSelectionChange(() => { ... })`——clipboardOK 为 false 直接 return；否则 clearTimeout 后 `selTimer = window.setTimeout(...)` 150ms（trailing debounce：拖动选择期间合并只写最终选区，注释引 UI-SPEC 防抖定稿）；回调内 `const text = term.getSelection();` 为空串或与 lastCopied 相同则 return；否则记录 lastCopied 并 `navigator.clipboard.writeText(text).catch((e) => console.warn('clipboard write failed', e));`（写失败静默不弹错，D-11；console.warn 照 main.ts:69 既有先例形态）。
    3. Ctrl+Shift+V 粘贴（注释引 D-10）：`window.addEventListener('keydown', (e) => { if (!clipboardOK || isRO) return; if (e.ctrlKey && e.shiftKey && e.key.toLowerCase() === 'v') { e.preventDefault(); navigator.clipboard.readText().then((t) => term.paste(t)).catch((err) => console.warn('clipboard read failed', err)); } });`——isRO 门复用 04-02 的模块级布尔（ro 永不读剪贴板：无谓权限弹窗）；preventDefault 阻断浏览器原生粘贴路径防双重粘贴；term.paste 保留 bracketed paste 语义走既有 onData→INPUT 链路（注释引 RESEARCH §Pattern 4 核实注：产物 paste 路径检查 bracketedPasteMode 后 triggerDataEvent）。
    4. 纪律自查：无 execCommand 任何形式回落；读剪贴板仅此一处手势触发；写/读失败均 .catch 静默。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && pnpm -C web exec tsc --noEmit && grep -q 'onSelectionChange' web/src/main.ts && grep -q 'clipboard.writeText' web/src/main.ts && grep -q 'clipboard.readText' web/src/main.ts && ! grep -q 'execCommand' web/src/main.ts</automated>
    <human-check>选中复制/粘贴/权限拒绝/ro 不弹窗的浏览器确认并入 04-06 的 04-UAT.md 清单统一执行</human-check>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit 退出 0
    - main.ts 含 clipboardOK 存在性检测、150ms 防抖选中写入、Ctrl+Shift+V 粘贴 handler（clipboardOK 与 isRO 双门 + preventDefault）
    - 源文件无 execCommand 任何形式出现（负向 grep 绿）
    - 写/读失败路径均为 .catch → console.warn（无 throw、无 alert、无 UI 反馈面）
  </acceptance_criteria>
  <done>FE-05 落地：选中即复制走 navigator.clipboard 现代 API，粘贴经 Ctrl+Shift+V + term.paste，ro 不读、非安全上下文整体静默。</done>
</task>

<task type="auto">
  <name>Task 2: resize 浮层 + beforeunload——三开关量埋点与 WELCOME/onclose 挂接</name>
  <files>web/src/main.ts, web/index.html</files>
  <read_first>
    - web/src/main.ts（onResize 接线 110 行现状、WELCOME 分支 197-210 行、onclose 分派 256-318 行——listener 移除挂点）
    - web/index.html（顶层元素与 style 段现状——#status[hidden] display:none 先例 19 行）
    - .planning/phases/04-frontend/04-RESEARCH.md（§Pattern 5 两开关逐段代码契约 + MDN 核实注 preventDefault 形态/自定义文案忽略/sticky activation）
    - .planning/phases/04-frontend/04-UI-SPEC.md（§Resize Overlay Spec 逐字样式契约 + §Auxiliary Interactions beforeunload 注册/移除时机）
  </read_first>
  <action>
    1. `web/index.html`：body 加第三个顶层元素 `<div id="resize-overlay" hidden></div>`（#terminal/#status 之后，注释引 UI-SPEC §Page Shell 增量）；style 段加 `#resize-overlay` 规则（UI-SPEC §Resize Overlay Spec 逐字：position: fixed; top: 8px; right: 8px; padding: 4px 8px; background: rgba(0,0,0,0.6); color: #ffffff; border-radius: 4px; 12px/400/1.5 终端等宽栈同 fontFamily; pointer-events: none; user-select: none; z-index: 900; transition: opacity 200ms ease）与 `#resize-overlay[hidden] { display: none; }`（照 #status[hidden] 先例）；z-index 序注释更新：浮层 900 < tooltip 901 < 状态面板 1000。
    2. `web/src/main.ts` 模块级加三开关量（注释：FE-06 两开关 + 浮层会话门；04-05 经 query/prefs 翻转，本 plan 先取默认值）：`let welcomeDone = false;`（WELCOME 处理完成置位——浮层与离开确认的会话建立门）、`let resizeOverlayOn = true;`（D-17 默认开）、`let confirmBeforeUnloadOn = true;`（D-18 默认开）。
    3. 浮层驱动：既有 `term.onResize(({ cols, rows }) => sendResize(cols, rows));` 扩展为先 sendResize 后浮层逻辑——`if (!welcomeDone || !resizeOverlayOn) return;` 后取 overlay 元素（`document.getElementById('resize-overlay')!`）、textContent 置 `${cols}x${rows}`（ASCII x 无标签文字，ttyd 先例；服务端钳制 1000×1000 → 最长 9 字符注释）、hidden 置 false、style.opacity 置 '1'、clearTimeout 后 600ms 定时器置 opacity '0'（transition 200ms 淡出；模块级 `let overlayTimer: number | undefined;`）。注释：onopen 初次 fit 不触发（welcomeDone 门——浮层是会话辅助不是启动尺寸指示器）。
    4. beforeunload：模块级 `const onBeforeUnload = (e: BeforeUnloadEvent): void => { e.preventDefault(); };`（注释引 D-18 与 MDN：preventDefault 触发标准确认框；自定义文案被现代浏览器忽略故不写）。
    5. WELCOME 分支尾部（ro 处理与 04-02 setTitle 之后、catch 之前）加：`welcomeDone = true;` 随后 `if (confirmBeforeUnloadOn) { window.addEventListener('beforeunload', onBeforeUnload); }`（注释：默认开 ro 同启，D-18；04-05 在此注册点之前接入 query/prefs 开关翻转——条件注册形态从本 plan 起即为开关驱动）。
    6. onclose 处理器首行（auth_failed 守卫之前）加 `window.removeEventListener('beforeunload', onBeforeUnload);`（注释：WS close 任意路径移除——含状态面板展示后与 auth_failed 重试前；重试成功的新 WELCOME 会按开关重注册，无残留无双重）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && pnpm -C web exec tsc --noEmit && grep -q 'resize-overlay' web/index.html && grep -q 'welcomeDone' web/src/main.ts && grep -q "addEventListener('beforeunload'" web/src/main.ts && grep -q "removeEventListener('beforeunload'" web/src/main.ts</automated>
    <human-check>浮层显示/淡出、beforeunload 拦截与会话终结后放行的浏览器确认并入 04-06 的 04-UAT.md 清单</human-check>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit 退出 0
    - index.html 含 #resize-overlay 元素（hidden）与逐字样式规则（z-index 900、transition opacity 200ms）+ [hidden] display:none 规则
    - main.ts：onResize handler 内含 welcomeDone && resizeOverlayOn 双门浮层逻辑（600ms 静止淡出）；WELCOME 尾部 welcomeDone 置位 + 条件注册 beforeunload；onclose 首行移除 listener
    - beforeunload handler 仅 preventDefault()——无自定义文案字面量
  </acceptance_criteria>
  <done>FE-06 默认形态落地：resize 期间右上角 COLSxROWS 静止 600ms 淡出；离开页面前标准确认框默认拦截、会话终结即放行；三开关量埋点待 04-05 接线。</done>
</task>

<task type="auto">
  <name>Task 3: dist 重建与产物提交</name>
  <precondition>Task 1/2 已合入（dist 必须由含剪贴板与浮层接线的源码构建）。</precondition>
  <files>web/dist/index.html</files>
  <read_first>
    - README.md（构建节纪律与产物提交约定）
    - 仓库根 .gitignore（dist/*.gz 不入库既定策略）
  </read_first>
  <action>
    1. `time pnpm -C web build` 全量重建（time 前缀跟踪耗时，CODEBUDDY）。
    2. 校验产物时间戳新于 main.ts 改动；grep 产物含 resize-overlay 与 beforeunload 检索串（新接线入包证据）。
    3. `go build ./...` 确认 embed 链编译通过；git add web/dist/index.html（.gz 按 .gitignore 既定策略不入库）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && time pnpm -C web build && grep -q 'resize-overlay' web/dist/index.html && grep -q 'beforeunload' web/dist/index.html && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - 构建退出 0；dist/index.html 含 resize-overlay 与 beforeunload 检索串
    - web/dist/index.html 更新入库；go build ./... 退出 0
  </acceptance_criteria>
  <done>dist 产物与剪贴板/浮层/离开确认新前端同步入库。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器剪贴板 ↔ 终端会话 | 系统剪贴板是跨应用共享面——读取必须绑定显式用户手势，写入失败不得干扰终端主流程 |
| 页面生命周期 ↔ 会话生命周期 | 单次语义下关页 = 会话终结；beforeunload 是唯一的误关防线，但不得成为会话结束后的拦路石 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Information Disclosure | 无手势/后台读剪贴板（隐私越界） | medium | mitigate | readText 仅 Ctrl+Shift+V 触发 + ro 永不读（D-10）+ clipboardOK 存在性门控；prohibition 锁定手势外读禁绝 |
| T-04-09 | DoS | 剪贴板权限拒绝/不可用打断终端主流程（弹错/异常） | medium | mitigate | 写读失败一律 .catch → console.warn 静默（D-11）；非安全上下文整体降级不抛 TypeError |
| T-04-10 | Tampering | 会话终结后 beforeunload 残留拦截关页（UI 劫持感） | low | mitigate | onclose 任意路径首行移除 listener；重试路径先移除再由新 WELCOME 按开关重注册 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | accept | 本 plan 零新依赖（Clipboard API 浏览器内建；addon-clipboard 的加载接线在 04-05，钉版已于 04-02 完成） |
</threat_model>

<verification>
- `pnpm -C web exec tsc --noEmit && time pnpm -C web build` 退出 0，dist 含新接线检索串
- `go build ./...` 退出 0
- 浏览器人工确认（选中复制/粘贴/ro 不读/浮层时序/beforeunload 拦截与放行/非安全上下文降级）在 04-06 的 04-UAT.md 清单汇总执行
</verification>

<success_criteria>
- FE-05：选中即复制（150ms 防抖）+ Ctrl+Shift+V 粘贴，execCommand 零残留
- FE-06：浮层默认开（600ms 静止淡出）、beforeunload 默认开（标准框、会话终结放行）
- 三开关量（welcomeDone/resizeOverlayOn/confirmBeforeUnloadOn）就位，04-05 可纯翻转接线
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `web/src/main.ts`：`clipboardOK` 常量、`selTimer`/`lastCopied` 选中复制防抖接线、Ctrl+Shift+V 粘贴 keydown handler、`welcomeDone`/`resizeOverlayOn`/`confirmBeforeUnloadOn` 模块级开关量、`overlayTimer`、浮层驱动逻辑（onResize 扩展）、`onBeforeUnload` handler 与注册/移除接线
- `web/index.html`：`#resize-overlay` 元素与样式规则（含 `[hidden]` 规则）
- `web/dist/index.html`（重建产物）

<output>
Create `.planning/phases/04-frontend/04-03-SUMMARY.md` when done
</output>
