---
phase: 04-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/proto/proto.go
  - internal/proto/proto_test.go
  - internal/server/server.go
  - internal/server/e2e_test.go
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
autonomous: true
requirements:
  - FE-07

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WelcomePayload 加 Prefs json.RawMessage（json tag prefs,omitempty）：prefs 非空时 Welcome JSON 原样携带 prefs 键，nil 时 JSON 无 prefs 键（旧前端零漂移，P2 D-02 加字段纪律，P4 D-13）"
    - "WelcomeFrame 签名演进为 WelcomeFrame(mode string, prefs json.RawMessage)，proto.go 与 server.go:433 两处调用点同步更新，无遗留旧签名调用"
    - "proto.ValidClientOptionKey 白名单恰 10 键：fontSize/fontFamily/cursorBlink/cursorStyle/scrollback/lineHeight/letterSpacing/theme（8 个 xterm 视觉键）+ resizeOverlay/confirmBeforeUnload（2 个前端行为键）；osc52、allowProposedApi 与任意未知键一律拒绝（D-14 白名单制，D-12 安全不对称）"
    - "--client-option key=value 可重复 flag 在 fs.Func 回调内 parse 期校验：缺 '='、key 不在白名单、值非法 JSON 均即时报错（exit 2，D-15）；--osc52 布尔 flag 默认 false（D-12）"
    - "聚合函数把 clientOptions + osc52 合成单个 json.RawMessage：osc52 为 true 时含 \"osc52\":true；同一 key 重复给出时 last-wins；零配置（无 client-option 且 osc52=false）时 ClientPrefs 为 nil（Welcome 不出 prefs 键）"
    - "服务端对 prefs 不透明透传：白名单/JSON 校验只在 --client-option parse 期完成，server/proto 层不二次解析（D-13，RESEARCH §Anti-Patterns 双写漂移面）"
    - "e2e 断言：Options.ClientPrefs 注入时握手收到的 Welcome JSON 含 prefs 键且逐键值相等；未注入时无 prefs 键"
  prohibitions:
    - "禁止把 osc52 放进 --client-option 白名单或任何用户侧可设通道（D-12 安全不对称——安全敏感项只能经服务端 --osc52 开启，URL query 与 client-option 均不得触碰）"
    - "禁止在 server/proto 层解析或二次校验 prefs 内容（D-13：prefs 对服务端是不透明 blob，二次解析引入双写漂移面）"
    - "禁止在服务端做 OSC 序列剥离/过滤（D-08：数据泵零拷贝理念——OSC 8 由前端 hover 兜底、OSC52 由 addon 默认关闭兜底；过滤开关已记 CONTEXT Deferred Ideas，不在本 phase 范围）"
    - "启动报错/日志文案禁止含 prefs 值内容（SEC-01 启动面红线延伸：只含 key 名与错误类别）"
  artifacts:
    - path: "internal/proto/proto.go"
      provides: "WelcomePayload.Prefs 字段 + WelcomeFrame(mode, prefs) + ValidClientOptionKey 白名单"
      contains: "ValidClientOptionKey"
    - path: "internal/server/server.go"
      provides: "Options.ClientPrefs 注入与 Welcome 组帧挂点"
      contains: "ClientPrefs"
    - path: "internal/server/e2e_test.go"
      provides: "Welcome prefs 端到端握手断言"
      contains: "TestWelcomePrefs"
    - path: "cmd/wesh/main.go"
      provides: "--client-option/--osc52 两 flag + 聚合函数 + Options 传递"
      contains: "client-option"
    - path: "cmd/wesh/main_test.go"
      provides: "TestParseArgs 新行 + client-option 错误表 + 聚合单测"
      contains: "invalid --client-option key"
    - path: "internal/proto/proto_test.go"
      provides: "prefs 往返/omitempty 缺席/白名单表"
      contains: "TestValidClientOptionKey"
  key_links:
    - from: "cmd/wesh/main.go"
      to: "internal/server/server.go"
      via: "server.Options.ClientPrefs 装配传递（聚合结果 → Welcome 组帧）"
      pattern: "ClientPrefs"
    - from: "internal/server/server.go"
      to: "internal/proto/proto.go"
      via: "Attach 升档序列尾段 proto.WelcomeFrame(mode, s.clientPrefs)"
      pattern: "WelcomeFrame\\(mode"
    - from: "cmd/wesh/main.go"
      to: "internal/proto/proto.go"
      via: "fs.Func 回调内 proto.ValidClientOptionKey 白名单校验"
      pattern: "ValidClientOptionKey"
---

<objective>
打通 FE-07 偏好下发的 Go 侧全链：proto 契约（WelcomePayload.Prefs + 白名单）→ server 注入 → CLI 两 flag（--client-option/--osc52）与聚合，端到端握手断言 Welcome 携 prefs。

Purpose: D-13（Welcome 帧内嵌 prefs 一次性下发）、D-14（白名单制防任意 option 注入）、D-15（--client-option parse 期 fail-fast）、D-12（--osc52 服务端专有开关）的服务端半侧在此落地；前端 04-05 经此通道接收 prefs。
Output: Go 侧 prefs 通道全链 + 表驱动/端到端测试锁定 + `go test ./...` 绿。

规格探针标记（spec-less probe fallback）：FE-07 探针报为 unclassified——CONTEXT D-13..D-16 与 RESEARCH §Pattern 6 Go 契约已防御性钉死全部语义（通道形状/白名单/校验时点/聚合形态），按 §C 核销为显式 truths，无静默丢弃；三条 must-NOT（osc52 不入用户侧通道/服务端不二次解析/文案不含值）经 §B 回忆落入 prohibitions（descriptor-less，下游 dispose flagged-unverified）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend/04-CONTEXT.md
@.planning/phases/04-frontend/04-RESEARCH.md
@.planning/phases/04-frontend/04-PATTERNS.md
@internal/proto/proto.go
@cmd/wesh/main.go
</context>

<tasks>

<task type="tracer">
  <name>Task 1: FE-07 Go 通道端到端——proto 契约 + server 注入 + CLI flags/聚合 + 握手 e2e</name>
  <reversibility rating="one-way">D-12/D-15：--osc52 与 --client-option 是公开 CLI 契约（P2 D-15 纪律），发布后撤除或改名即破坏既有用法——用户已在 CONTEXT 锁定， checkpoint 不再重问。</reversibility>
  <files>internal/proto/proto.go, internal/server/server.go, cmd/wesh/main.go, internal/server/e2e_test.go</files>
  <read_first>
    - internal/proto/proto.go（被改文件现状全文：WelcomePayload 84-87 行、WelcomeFrame 109-114 行、HelloPayload.Ticket omitempty 先例 72-82 行、常量块注释纪律 33-50 行）
    - internal/server/server.go（Options 结构体 84-96 行、New() 装配拷贝 117-139 行、升档序列 Welcome 挂点 420-443 行——顺序敏感注释保留）
    - cmd/wesh/main.go（config 结构体 25-38 行、--credential fs.Func 先例 54-63 行、--no-auth BoolVar 66 行、Options 传递 201 行、parseArgs 头注释 flag 清单 40-46 行）
    - internal/server/e2e_test.go（startTestServerWith/dialHello 现有签名与 Welcome 断言形态）
    - .planning/phases/04-frontend/04-PATTERNS.md（§proto.go/server.go/main.go 三节逐段改造点）
    - .planning/phases/04-frontend/04-RESEARCH.md（§Pattern 6 Go 侧通道契约形状 + §Code Examples 两段 Go 示例 + §Anti-Patterns 服务端不解析 prefs）
  </read_first>
  <action>
    1. `internal/proto/proto.go`：
       - WelcomePayload 照 HelloPayload.Ticket 同形态加字段：`Prefs json.RawMessage`，json tag `prefs,omitempty`——注释引 P4 D-13（Welcome 内嵌一次性下发）与 P2 D-02（加字段不动协议）。
       - WelcomeFrame 签名演进为 `WelcomeFrame(mode string, prefs json.RawMessage)`：组帧时 WelcomePayload{Mode: mode, Prefs: prefs}——prefs 为 nil/空时 omitempty 使 JSON 不出 prefs 键（旧前端零漂移）；头部包注释的 Welcome 帧说明与 'P' 占位注释（25 行）同步提一句「P4 起 Welcome 可携可选 prefs（D-13），'P' 帧运行期推送仍 v2 再议」。
       - 加 `ValidClientOptionKey(key string) bool`：switch 逐一列出 10 个白名单键（fontSize/fontFamily/cursorBlink/cursorStyle/scrollback/lineHeight/letterSpacing/theme/resizeOverlay/confirmBeforeUnload），case 体加大括号（CODEBUDDY 纪律）；注释引 D-14（白名单制防任意 option 注入）与 D-12（osc52 刻意不在内——安全敏感项只能经 --osc52）。简洁 switch 不引入新类型/注册表抽象（CODEBUDDY 反过度设计）。
    2. `internal/server/server.go`：
       - Options 结构体加 `ClientPrefs json.RawMessage`（注释：--client-option 聚合 + osc52 并入，空 = 不下发，P4 D-13；服务端不透明透传不解析）。
       - Server 结构体加 `clientPrefs json.RawMessage` 字段 + New() 装配期逐字段拷贝（生产直传字段组注释区，与 Writable 同组）。
       - server.go:433 Welcome 挂点改为 `proto.WelcomeFrame(mode, s.clientPrefs)`；升档序列顺序敏感注释（420-428 行）保留并补一句「Welcome 携 prefs（P4 D-13，空则不出键）」。
    3. `cmd/wesh/main.go`：
       - config 加 `clientOptions []clientOption`（尾注释引 P4 D-15）与 `osc52 bool`（尾注释引 P4 D-12）；新增包级 `type clientOption struct { key string; value json.RawMessage }`。
       - 照 --credential 先例加 `fs.Func("client-option", "client preference key=value (repeatable; whitelisted keys, value is JSON)", ...)`：回调内 `strings.Cut(s, "=")`——found=false 返回 `fmt.Errorf("invalid --client-option %q: must be key=value", s)`；`!proto.ValidClientOptionKey(key)` 返回 `fmt.Errorf("invalid --client-option key %q", key)`；`json.Unmarshal([]byte(value), &v)` 失败返回 `fmt.Errorf("invalid --client-option value for %q: not valid JSON", key)`；通过则 append cfg.clientOptions。错误文案只含 key 名不含值（启动面红线）。
       - 照 --no-auth 先例加 `fs.BoolVar(&cfg.osc52, "osc52", false, "enable OSC52 clipboard write (write-only; default off)")`。
       - 加聚合函数 `aggregateClientPrefs(opts []clientOption, osc52 bool) json.RawMessage`：零配置返回 nil；否则 map[string]json.RawMessage 收集（同 key 后者覆盖前者——last-wins 注释钉死），osc52 为 true 时置 `m["osc52"] = json.RawMessage("true")`，json.Marshal 返回。
       - run() 内 server.New 的 Options 字面量加 `ClientPrefs: aggregateClientPrefs(cfg.clientOptions, cfg.osc52)`。
       - parseArgs 头注释 flag 清单「共 11 个」改「共 13 个」并登记两新 flag 及决策号（PATTERNS 纪律）。
    4. `internal/server/e2e_test.go` 加握手端到端断言（dialHello 只返回 mode 不够——新增 helper 不得改既有 dialHello 签名，避免涟漪全部调用方）：
       - 新 helper（如 `dialHelloPayload`）返回完整 Welcome 载荷 map（照 dialHello 形态：Dial → Hello → 读首帧断言类型字节 W → json.Unmarshal 为 map[string]any）。
       - `TestWelcomePrefs` 两半侧：startTestServerWith 注入 `Options{Writable: true, ClientPrefs: json.RawMessage(`{"fontSize":18,"osc52":true}`)}` → Welcome map 的 prefs 键逐键值相等；不注入 ClientPrefs 的既有默认装配 → Welcome map 无 "prefs" 键（omitempty 缺席回归）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go build ./... && go test ./internal/proto ./internal/server ./cmd/wesh -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... 与三包测试均退出 0（含新 TestWelcomePrefs 两半侧）
    - proto.go 含 `Prefs json.RawMessage`（tag prefs,omitempty）与 ValidClientOptionKey；WelcomeFrame 为两参签名，全仓库无旧单参调用残留
    - server.go Options/Server 均含 ClientPrefs 字段且 Welcome 挂点传 s.clientPrefs
    - main.go 含 client-option/osc52 两 flag、clientOption 类型、aggregateClientPrefs；parseArgs 头注释为「共 13 个」
    - Welcome JSON 在 nil prefs 时无 prefs 键（e2e 缺席断言绿）
  </acceptance_criteria>
  <done>--client-option/--osc52 → 聚合 → Welcome 帧携 prefs 的 Go 全链打通，握手 e2e 证明端到端；nil prefs 时 wire 零漂移。</done>
</task>

<task type="auto">
  <name>Task 2: 表驱动测试扩展——proto prefs 往返/omitempty/白名单 + main parseArgs/错误表/聚合</name>
  <files>internal/proto/proto_test.go, cmd/wesh/main_test.go</files>
  <read_first>
    - internal/proto/proto_test.go（TestWelcomeFrameErrorFrame 62-90 行组帧往返形态、TestProtocolConstants 94-150 行逐字钉死形态——新用例照此风格）
    - cmd/wesh/main_test.go（TestParseArgs 表结构 27-98 行含命名字段、TestTLSKeyPairError 错误子串表 139-162 行、TestStartupMatrix 文案红线断言 261-264 行）
    - .planning/phases/04-frontend/04-PATTERNS.md（§proto_test.go/main_test.go 两节扩展方向与 omitempty 反向断言写法）
  </read_first>
  <action>
    1. `internal/proto/proto_test.go`：
       - TestWelcomeFrameErrorFrame 的 WelcomeFrame 调用更新为两参形态（既有 ro 断言行零改动传 nil）。
       - 加 prefs 往返行：WelcomeFrame(ModeRW, json.RawMessage(`{"fontSize":16,"resizeOverlay":false}`)) → unmarshal WelcomePayload 后 Prefs 逐字节相等。
       - 加 omitempty 缺席断言：WelcomeFrame(ModeRO, nil) 的帧体 `bytes.Contains(wf, []byte("prefs"))` 为 false（旧前端零漂移回归锁）。
       - 新 `TestValidClientOptionKey` 表驱动：10 个白名单键逐一为 true；osc52/allowProposedApi/空串/fontsize（大小写敏感）/任意未知键为 false（D-12/D-14 回归锁）。
    2. `cmd/wesh/main_test.go`：
       - TestParseArgs 表加 `wantClientOptions int`（计数断言，照 wantCredentials 先例——value 是 json.RawMessage 计数即契约）与 `wantOSC52 bool` 两命名字段，断言段照既有字段形态补两个 if；新增行：{"client options", --client-option fontSize=16 --client-option cursorBlink=false, wantClientOptions: 2}、{"osc52 flag", --osc52, wantOSC52: true}；defaults 行零值自动覆盖（零配置 nil）。
       - 新 `TestClientOptionError`（照 TestTLSKeyPairError 错误子串表形态）：{"bad key", --client-option allowProposedApi=true, "invalid --client-option key"}、{"bad JSON", --client-option fontSize=abc, "not valid JSON"}、{"osc52 key rejected (D-12)", --client-option osc52=true, "invalid --client-option key"}、{"missing equals", --client-option fontSize, "must be key=value"}；全部行 args 尾部带 `-- bash`。
       - 新 `TestAggregateClientPrefs`：零配置 → nil；keys + osc52=true → unmarshal 后含 fontSize 值与 osc52:true；同 key 重复（fontSize=14 后 fontSize=22）→ 22（last-wins 锁）；断言经 json.Unmarshal 为 map 进行（不逐字节——map marshal 键序确定性不依赖断言顺序）。
       - 文案红线：TestClientOptionError 每行断言 err.Error() 不含测试给出的值内容（如 "abc"、"true"）——只含 key 名与错误类别（SEC-01 启动面红线延伸，照 TestStartupMatrix 261-264 行形态）。注意："not valid JSON" 子串本身含在文案里是合法的，值内容才禁入。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test ./internal/proto ./cmd/wesh -count=1 -v 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - 两包测试全绿退出 0；TestValidClientOptionKey/TestClientOptionError/TestAggregateClientPrefs 均出现在输出且 PASS
    - omitempty 缺席断言与 last-wins 行各自独立成 subtest（回归可定位）
    - 错误文案红线断言存在且全行绿（值内容不入错误串）
  </acceptance_criteria>
  <done>FE-07 Go 侧契约全部表驱动锁定：白名单边界、parse 期报错、聚合语义、wire 兼容性各有回归锁。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flag 输入 → prefs blob | 运维侧 --client-option 值是半可信输入（systemd/脚本拼装），未经白名单+JSON 校验可注入任意 xterm option |
| 服务端 → 浏览器客户端（Welcome prefs） | prefs blob 经 Welcome 帧驱动客户端 term.options 运行时行为；危险键（allowProposedApi 类）可扩大攻击面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | --client-option → prefs 通道注入白名单外 option（allowProposedApi 等） | high | mitigate | proto.ValidClientOptionKey 恰 10 键白名单 parse 期 fail-fast（D-14/D-15）；TestValidClientOptionKey/TestClientOptionError 回归锁 |
| T-04-02 | Tampering | osc52 经 client-option 等用户侧通道绕过服务端意图开启剪贴板写 | high | mitigate | osc52 结构性排除出白名单（D-12 安全不对称），parse 期报错 + 专项测试行锁定 |
| T-04-03 | Information Disclosure | 启动报错文案回显 prefs 值内容（systemd 日志面） | low | mitigate | 错误串只含 key 名与错误类别；main_test 红线断言全行绿 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | accept | 本 plan 纯 Go 零新依赖（encoding/json 标准库）；前端三包审计与钉版在 04-02 |
</threat_model>

<verification>
- `go build ./... && go test ./internal/proto ./internal/server ./cmd/wesh -count=1` 退出 0
- TestWelcomePrefs 端到端两半侧绿（注入携 prefs / 未注入无键）
- 全量 `go test -race ./...` 在 04-06 收口统一执行
</verification>

<success_criteria>
- D-13/D-14/D-15/D-12 服务端半侧落地：Welcome 内嵌 prefs + 白名单 fail-fast + osc52 服务端专有
- wire 兼容：nil prefs 时 Welcome JSON 与 Phase 3 逐字节同形（omitempty 缺席锁）
- 04-04 的 UAT 协议断言与 04-05 的前端消费获得稳定通道
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `internal/proto/proto.go`：`WelcomePayload.Prefs` 字段（json.RawMessage, omitempty）、`WelcomeFrame(mode, prefs)` 两参签名、`ValidClientOptionKey(key string) bool`
- `internal/server/server.go`：`Options.ClientPrefs` 字段、`Server.clientPrefs` 字段
- `internal/server/e2e_test.go`：`dialHelloPayload` helper、`TestWelcomePrefs`
- `cmd/wesh/main.go`：`--client-option`/`--osc52` flags、`clientOption` 类型、`config.clientOptions`/`config.osc52` 字段、`aggregateClientPrefs` 函数
- `internal/proto/proto_test.go`：`TestValidClientOptionKey`（+ TestWelcomeFrameErrorFrame 扩展行）
- `cmd/wesh/main_test.go`：`TestClientOptionError`、`TestAggregateClientPrefs`（+ TestParseArgs 表 wantClientOptions/wantOSC52 字段与新行）

<output>
Create `.planning/phases/04-frontend/04-01-SUMMARY.md` when done
</output>
