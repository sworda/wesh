---
phase: 04-frontend
plan: 06
type: execute
wave: 4
depends_on: ["04-04", "04-05"]
files_modified:
  - README.md
  - .planning/phases/04-frontend/04-UAT.md
autonomous: true
requirements:
  - CORE-03
  - FE-02
  - FE-04
  - FE-05
  - FE-06
  - FE-07

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "README 用法节 flag 表新增 --client-option（可重复 key=value、白名单 10 键、值 JSON、非法启动报错）与 --osc52（默认关、只写不读）两行（D-12/D-15 文档化）"
    - "README 新增「前端体验（Phase 4）」节：标题同步（OSC 0/2 → 标签页，ro 恒 [ro] 前缀）、超链接（URL 自动识别、hover 显示真实地址、单击新标签页）、剪贴板（选中即复制 + Ctrl+Shift+V 粘贴 + **需 HTTPS 或 localhost**——D-11 文档义务明示非安全上下文静默降级 + OSC52 默认关只写不读）、resize 浮层与离开确认（默认开，可经 --client-option/query 关闭）、偏好下发（白名单键表、优先级 query > --client-option > 内置默认、theme 完整 JSON 对象且未指定键保留内置调色板、osc52 仅 --osc52 可开启）"
    - "README 协议节 Welcome payload 行更新为含可选 prefs 键（D-13——{\"mode\":\"ro\"|\"rw\",\"prefs\":{...}?}）"
    - "04-UAT.md 人工清单覆盖全部渲染面：CJK/emoji 宽度与 IME 组合输入（拼音/日文）、URL hover 真实地址与单击新标签页、OSC8 无 confirm 框、选中即复制、Ctrl+Shift+V 粘贴、ro 不弹剪贴板权限、明文 HTTP 非 localhost 剪贴板静默、浮层显示/淡出/开关关闭、beforeunload 拦截与会话终结后放行（含 sticky activation 零交互不弹为浏览器预期——RESEARCH §Open Questions 3 裁决记录）、[ro] 前缀随标题变化保持、theme 部分下发保留调色板、query 覆盖生效、非法 query 静默终端可用"
    - "全量验证六段式绿：GOROOT gofmt 清零 / go vet / go test -race -count=1 ./... / time pnpm -C web build / 裸 clone（git archive）归档编译测试 / 启动冒烟（--port 0 启动行解析）"
    - "三套 UAT 回归绿：node web/uat/phase02.mjs、phase03.mjs、phase04.mjs 对同一新构建二进制全 PASS"
  prohibitions:
    - "文档禁止暗示 osc52 可经 URL query 或 --client-option 开启（D-12 安全不对称——文档面不得给出用户侧绕过路径）"
    - "README 禁止弱化或省略剪贴板 HTTPS/localhost 明示（D-11 文档义务——静默降级设计下文档是唯一出口）"
  artifacts:
    - path: "README.md"
      provides: "--client-option/--osc52 flag 文档 + 前端体验节 + Welcome prefs 协议更新"
      contains: "--client-option"
    - path: ".planning/phases/04-frontend/04-UAT.md"
      provides: "Phase 4 渲染面人工确认清单"
      contains: "UAT"
  key_links:
    - from: "README.md"
      to: "cmd/wesh/main.go"
      via: "flag 表与实际 CLI 契约一致（--client-option/--osc52 help 文案同源）"
      pattern: "client-option"
    - from: ".planning/phases/04-frontend/04-UAT.md"
      to: "web/src/main.ts"
      via: "人工清单逐项对应 main.ts 新接线的浏览器可观察行为"
      pattern: ".*"
---

<objective>
Phase 4 收口：README 用户文档（两新 flag + 前端体验节 + 协议更新）、04-UAT.md 渲染面人工清单、全量六段式验证 + 三套 UAT 回归。

Purpose: D-11 的文档明示义务与 ROADMAP 准则的最终证据面；六段式沿用 P1-P3 既定收口形态。
Output: README 更新 + 04-UAT.md + 全量验证绿证据。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend/04-CONTEXT.md
@.planning/phases/04-frontend/04-RESEARCH.md
@.planning/phases/04-frontend/04-UI-SPEC.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README——flag 表两行 + 前端体验节 + Welcome 协议更新</name>
  <files>README.md</files>
  <read_first>
    - README.md（被改文件现状全文：用法节 flag 表 25-38 行、安全说明节 52-117 行、协议节 Welcome 行 89 行、构建节 42-50 行）
    - cmd/wesh/main.go（两新 flag 的 help 文案——文档与实际 CLI 同源核对）
    - .planning/phases/04-frontend/04-CONTEXT.md（D-11 文档义务、D-12/D-15/D-16/D-19 决策原文）
    - .planning/phases/04-frontend/04-UI-SPEC.md（§Prefs Contract 白名单键表与优先级）
  </read_first>
  <action>
    1. 用法节 flag 表（--origin 行之后、--version 行之前）插入两行，与 main.go help 文案同源：
       - `--client-option` 行：默认值 —；说明「客户端偏好 `key=value`，可重复；白名单键（fontSize/fontFamily/cursorBlink/cursorStyle/scrollback/lineHeight/letterSpacing/theme/resizeOverlay/confirmBeforeUnload），值为 JSON（如 `fontSize=16`、`cursorBlink=false`、`theme={"background":"#000"}`）；key 不在白名单或值非法 JSON 启动报错」。
       - `--osc52` 行：默认值 `false`；说明「开启 OSC52 剪贴板写入（只写不读，默认关）；只能经本 flag 开启——URL query 与 --client-option 均不可设置」。
    2. 安全说明节内（TLS 验证与证书段之后、协议段之前）新增「前端体验（Phase 4）」小节：
       - 标题同步：终端程序经 OSC 0/2 设置的标题同步到浏览器标签页（ro 模式恒 `[ro] ` 前缀最前）；标题经控制字符剥离与 128 字符截断防注入。
       - 超链接：终端输出中的 http(s) URL 自动识别为可点击链接，hover 显示完整真实地址（辨别显示文本与目标不一致的链接），单击新标签页打开（noopener 形态）。
       - 剪贴板：选中即复制、Ctrl+Shift+V 粘贴（现代 navigator.clipboard API）；**需 HTTPS 或 localhost 访问**——明文 HTTP 非 localhost 下浏览器不暴露剪贴板 API，选中复制与粘贴静默不生效（D-11 明示义务逐字落实）；ro 模式不读取剪贴板。OSC52 远程写剪贴板默认关闭，`--osc52` 开启后只写不读。
       - 辅助交互：resize 期间右上角显示 COLSxROWS 浮层、离开页面前浏览器确认（均默认开；经 `--client-option resizeOverlay=false` / `confirmBeforeUnload=false` 或同名 URL query 关闭）。
       - 偏好下发与覆盖：上表白名单键可经 `--client-option` 下发；URL query 同键覆盖（如 `?fontSize=16&cursorBlink=false`，字符串值需 JSON 引号并 URL 编码）；优先级 URL query > --client-option > 内置默认；theme 为完整 JSON 对象，未指定的色键保留内置调色板；非法 query 静默忽略（终端不受影响）。
    3. 协议节 Welcome 行（89 行）更新：`JSON {"mode":"ro"|"rw","prefs":{...}?}`（prefs 可选——--client-option/--osc52 下发时携带，无配置时该键缺席）。
    4. 纪律自查：文档不出现 osc52 经 query/client-option 开启的任何暗示；剪贴板 HTTPS/localhost 明示逐字在文；中文行文与全文风格一致。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && grep -q '\-\-client-option' README.md && grep -q '\-\-osc52' README.md && grep -q 'HTTPS' README.md && grep -q 'prefs' README.md && ! grep -q '?osc52' README.md</automated>
  </verify>
  <acceptance_criteria>
    - flag 表含两行新 flag 且与 main.go help 文案语义一致
    - 前端体验节含剪贴板 HTTPS/localhost 明示、OSC52 只写不读、优先级行、theme 保留调色板说明
    - 协议节 Welcome 行含可选 prefs 键；全文无 ?osc52 之类 query 开启暗示（负向 grep 绿）
  </acceptance_criteria>
  <done>用户文档与 Phase 4 全部用户面能力同步，D-11/D-12 文档义务闭合。</done>
</task>

<task type="auto">
  <name>Task 2: 04-UAT.md 人工清单 + 全量六段式验证 + 三套 UAT 回归</name>
  <precondition>04-02/04-03/04-05 已合入（人工清单逐项对应的前端接线与 dist 产物均已入库）。</precondition>
  <files>.planning/phases/04-frontend/04-UAT.md</files>
  <read_first>
    - .planning/phases/04-frontend/04-UI-SPEC.md（§Title Sync/§Link/§Clipboard/§Prefs/§Auxiliary Interactions 五契约的浏览器可观察行为 + §UI Considerations 🧪 backstop 行）
    - .planning/phases/04-frontend/04-RESEARCH.md（§Open Questions 3 sticky activation 裁决、§Validation Architecture 人工项分工）
    - README.md（构建节——验证用二进制构建路径）
  </read_first>
  <action>
    1. 新建 `.planning/phases/04-frontend/04-UAT.md`（人工确认清单，沿用 03-UAT.md 分工先例：协议层已自动化于 web/uat/phase04.mjs，本清单只列渲染/浏览器平台行为面）。前置条件：按 README 构建新二进制；建议两组启动形态——A: `wesh --bind 127.0.0.1 --port 0 --writable -- bash`（rw 全功能）、B: 默认不带 --writable（ro 形态）、C: `wesh --bind <局域网 IP> --port 0 --no-auth -- bash`（明文 HTTP 非 localhost 降级形态，另一台设备或同机非 localhost 地址访问）。清单项（每项给操作步骤与预期）：
       - T1 CJK/emoji 宽度：终端内 `echo '中文测试🙂🎉'` 与 `printf '中文字符对齐测试\n'` + 打开 vim/htop 中文内容——CJK 占两格不错位、emoji 后光标位置正确（FE-02）。
       - T2 IME 组合输入：中文拼音输入法在终端输入句子、日文 IME（如有）——组合过程与上屏不丢字不乱码（FE-02，UI-SPEC 🧪 backstop 行的人工出口）。
       - T3 标题同步：执行 `printf '\e]2;custom-title\a'` 与 vim/tmux 标题变化——浏览器标签页同步；ro 形态（B）下为 `[ro] custom-title` 且标题多次变化前缀不丢（CORE-03）。
       - T4 超链接：`echo 'see https://example.com and https://github.com/sworda/wesh'`——hover 显示完整真实地址 tooltip、单击新标签页打开；`printf '\e]8;;https://example.com\a显示文本\e]8;;\a'`——hover 显示 https://example.com（显示文本与目标不同可辨别）且无 confirm 原生框（FE-04）。
       - T5 选中即复制：鼠标拖动选中终端文本——系统剪贴板即为所选内容（粘贴到他处验证）；拖动过程不频繁写（最终选区一次写入）（FE-05）。
       - T6 粘贴：rw 形态（A）Ctrl+Shift+V 粘贴进终端——内容到达 shell（bracketed paste 保留）；ro 形态（B）Ctrl+Shift+V 无权限弹窗无效果（FE-05/D-10）。
       - T7 明文降级：形态 C 访问——选中复制与粘贴静默不生效、终端其余功能正常（D-11）。
       - T8 浮层：拖动窗口——右上角显示 COLSxROWS，停止约 600ms 后淡出；`--client-option resizeOverlay=false` 或 `?resizeOverlay=false` 重启后不显示（FE-06/D-17）。
       - T9 beforeunload：会话中关闭标签页——浏览器标准确认框拦截；会话终结（exit 后 Session ended 面板）再关页不再拦截；`?confirmBeforeUnload=false` 后不拦截；零交互直接关页不弹框为浏览器预期（sticky activation，RESEARCH §OQ3 裁决——记录非缺陷）（FE-06/D-18）。
       - T10 偏好下发：A 形态加 `--client-option fontSize=18 --client-option 'theme={"background":"#101020"}'`——字号变大且背景变 #101020 而 ANSI 彩色保持内置调色板（theme 合并）；同页加 `?fontSize=11`——query 覆盖为 11（优先级）；`?fontFamily=Menlo`（裸词非法 JSON）——终端正常且 console 有 warn（FE-07/D-16/D-19）。
       - T11 OSC52（可选，A 加 --osc52）：`printf '\e]52;c;aGVsbG8=\a'`——系统剪贴板写入 hello；`printf '\e]52;c;?\a'`——无 unhandled rejection（console 干净）（D-12）。
       每项记录 PASS/FAIL 与备注；全部 PASS 后 phase 渲染面收口。
    2. 全量验证六段式（逐条执行并记录退出码，P1-P3 既定收口形态）：
       - `$(go env GOROOT)/bin/gofmt -l .` 无输出（GOROOT 版本——/usr/bin/gofmt 陈旧教训，P1 决策）；
       - `go vet ./...` 退出 0；
       - `go test -race -count=1 ./...` 全绿（-race 需 CGO，不设 CGO_ENABLED=0）；
       - `time pnpm -C web build` 退出 0 + 产物时间戳新于源码（time 前缀与产物校验，CODEBUDDY）；
       - 裸 clone 归档验证：`git archive HEAD | (cd /tmp && mkdir -p wesh-bare && tar -x -C wesh-bare)` 后在归档目录 `pnpm -C web install && pnpm -C web build && go build ./... && go test ./...` 全绿（embed 链证据——归档不含未提交改动）；
       - 启动冒烟：`go build -o /tmp/wesh-uat/wesh ./cmd/wesh && /tmp/wesh-uat/wesh --bind 127.0.0.1 --port 0 -- true` 启动行含实际端口后 SIGKILL（不重启任何用户服务，CODEBUDDY）。
    3. 三套 UAT 回归：`node web/uat/phase02.mjs /tmp/wesh-uat/wesh && node web/uat/phase03.mjs /tmp/wesh-uat/wesh && node web/uat/phase04.mjs /tmp/wesh-uat/wesh` 全 PASS。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && test -f .planning/phases/04-frontend/04-UAT.md && $(go env GOROOT)/bin/gofmt -l . | wc -l | grep -q '^0$' && go vet ./... && go test -race -count=1 ./... && node web/uat/phase02.mjs /tmp/wesh-uat/wesh && node web/uat/phase03.mjs /tmp/wesh-uat/wesh && node web/uat/phase04.mjs /tmp/wesh-uat/wesh</automated>
    <human-check>04-UAT.md 清单 T1-T11 由用户在真实浏览器逐项确认（渲染/平台行为面，自动化结构性不可达）</human-check>
  </verify>
  <acceptance_criteria>
    - 04-UAT.md 存在且覆盖 T1-T11（FE-02/FE-04/FE-05/FE-06/FE-07/CORE-03 渲染面全映射，含 sticky activation 预期记录）
    - 六段式各段退出 0（gofmt 无输出/vet/-race 全绿/web 构建/裸 clone 归档/冒烟）
    - 三套 UAT 脚本对同一二进制全 PASS
  </acceptance_criteria>
  <done>Phase 4 收口证据齐：文档同步、人工清单就绪、自动化全量绿 + 零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档 → 用户部署行为 | README 是用户安全配置的唯一指引面——弱化剪贴板/OSC52 表述会直接把用户导向风险部署 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-17 | Information Disclosure | 文档省略剪贴板 HTTPS/localhost 明示，用户在明文 HTTP 下误判功能失效或盲目排障 | medium | mitigate | D-11 明示逐字入文（prohibition 锁定）；负向 grep 验收 |
| T-04-18 | Tampering | 文档暗示 osc52 用户侧开启路径，绕过 D-12 服务端意图 | medium | mitigate | prohibition 锁定 + 负向 grep（无 ?osc52 形态）；文档只述 --osc52 单一路径 |
| T-04-SC | Tampering | npm/pip/cargo installs | high | accept | 本 plan 零新依赖（裸 clone 验证的 pnpm install 走 lockfile 既定链） |
</threat_model>

<verification>
- 六段式全绿（gofmt/vet/-race/web 构建/裸 clone/冒烟）+ 三套 UAT 全 PASS
- README 与 04-UAT.md 入提交
- 人工 T1-T11 由用户确认后 phase 方可宣告渲染面收口
</verification>

<success_criteria>
- ROADMAP 三条成功准则全部具备证据面：自动化（Go 测试 + 三套 UAT）+ 人工清单（T1-T11）
- 全部 phase 需求（CORE-03/FE-02/FE-04/FE-05/FE-06/FE-07）文档化且可验证
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `README.md`：--client-option/--osc52 flag 行、「前端体验（Phase 4）」节、Welcome prefs 协议行（文档增量，无代码符号）
- `.planning/phases/04-frontend/04-UAT.md`：T1-T11 人工确认清单（新文件）

<output>
Create `.planning/phases/04-frontend/04-06-SUMMARY.md` when done
</output>
