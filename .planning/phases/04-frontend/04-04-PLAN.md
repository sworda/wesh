---
phase: 04-frontend
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - web/uat/phase04.mjs
autonomous: true
requirements:
  - FE-07

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "web/uat/phase04.mjs 零依赖（Node >= 22 原生 WebSocket/fetch，phase03.mjs 同形态）：spawn wesh 实例（--bind 127.0.0.1 --port 0 + 启动行端口解析）→ dialHello 握手 → Welcome JSON 断言；单次语义纪律——每个需 WS 的场景独立 spawn 实例（同进程第二次建连不可行）"
    - "默认形态断言：无任何新 flag 时 Welcome JSON 无 prefs 键（omitempty 缺席回归——旧前端零漂移，D-13）"
    - "--client-option 注入断言：fontSize=18 与 cursorBlink=false 两 flag 时 prefs.fontSize === 18 且 prefs.cursorBlink === false；theme={\"background\":\"#000000\"} 时 prefs.theme 为对象且 background 键值相等（D-15/D-19 值 JSON 原样透传）"
    - "--osc52 注入断言：prefs.osc52 === true；与 --client-option 组合时两键俱在（D-12 服务端专有开关经 prefs 下发）"
    - "重复 key last-wins 断言：--client-option fontSize=14 后 --client-option fontSize=22 → prefs.fontSize === 22"
    - "启动拒绝断言（spawnExpectExit 形态，3s 内自行退出）：--client-option allowProposedApi=true → exit 2 + stderr 含 invalid --client-option key；fontSize=abc → exit 2 + 含 not valid JSON；osc52=true → exit 2（D-12 安全不对称）；fontSize（缺 '='）→ exit 2 + 含 must be key=value"
    - "输出红线：check detail 只打印状态码/布尔/键形状（phase03.mjs 红线延伸——不打值内容；theme 等用户配置虽非凭据，detail 仍只打形状保持纪律一致）"
  prohibitions:
    - "UAT 禁止打 prefs/theme 的值内容到任何输出（红线延伸：detail 只打布尔/形状/状态码——测试输出同样可能进 CI 日志）"
    - "禁止同实例复用 WS 建连（单次语义：每场景独立 spawn——phase03.mjs S1f 注释既定纪律）"
  artifacts:
    - path: "web/uat/phase04.mjs"
      provides: "Phase 4 协议层自动化 UAT（Welcome prefs 形状/osc52 注入/启动拒绝）"
      contains: "phase04"
  key_links:
    - from: "web/uat/phase04.mjs"
      to: "cmd/wesh/main.go"
      via: "spawn --client-option/--osc52 实例驱动断言（CLI 契约的协议面投影）"
      pattern: "client-option"
    - from: "web/uat/phase04.mjs"
      to: "internal/proto/proto.go"
      via: "Welcome JSON prefs 键断言（omitempty 形状与透传语义）"
      pattern: "prefs"
---

<objective>
新建 Phase 4 协议层自动化 UAT：Welcome prefs 形状六正场景 + 启动校验拒绝四负场景，覆盖 FE-07/D-12 的全部可自动化协议面。

Purpose: 沿用 phase02/phase03 的「协议层 Node 自动化 + 渲染层人工确认」分工先例（P2 UAT 决策）——Welcome prefs 是 wire 可断言面，全部锁进脚本；IME/剪贴板/浮层等渲染面留 04-06 人工清单。
Output: web/uat/phase04.mjs 全绿 + 二进制构建接入。

规格探针标记（spec-less probe fallback）：FE-07 探针 unclassified 的可自动化半侧（wire 形状/校验拒绝）在本 plan 核销为显式 truths；渲染半侧（query 覆盖/prefs 应用后视觉效果）属 04-05/04-06。两条 must-NOT 经 §B 回忆落入 prohibitions（descriptor-less）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend/04-CONTEXT.md
@.planning/phases/04-frontend/04-PATTERNS.md
@web/uat/phase03.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: web/uat/phase04.mjs——六正四负场景脚本</name>
  <files>web/uat/phase04.mjs</files>
  <read_first>
    - web/uat/phase03.mjs（逐字复用骨架：文件头红线注释 1-9 行、帧常量 24-26 行、check/results 收集 48-52 行、startWesh 57-73 行、spawnExpectExit 77-89 行、dialHello 94-108 行、尾部出口码形态——PATTERNS §phase04.mjs 五段对照）
    - .planning/phases/04-frontend/04-PATTERNS.md（§phase04.mjs 节——单次语义适配纪律与各断言挂点）
    - cmd/wesh/main.go（--client-option/--osc52 flag 形态与报错文案——04-01 合入后现状）
  </read_first>
  <action>
    新建 `web/uat/phase04.mjs`（零依赖，Node >= 22；文件头注释：Phase 4 协议层自动化 UAT——Welcome prefs 形状/osc52 注入/client-option 启动拒绝；渲染层由 04-UAT.md 人工确认；红线：detail 只打状态码/布尔/键形状永不打值内容；运行方式 node web/uat/phase04.mjs [wesh 二进制路径]，默认 /tmp/wesh-uat/wesh）。逐字复用 phase03.mjs 骨架件：帧常量（OUTPUT/INPUT/RESIZE/HELLO/WELCOME/ERROR + SUBPROTOCOL）、concat/helloFrame（无 ticket 形态——loopback 无认证直连）、results/check、startWesh（--bind 127.0.0.1 --port 0 + scheme 感知启动行解析 + 8s 超时）、spawnExpectExit（3s 自行退出形态）、dialHello（Welcome 到达即握手完成）、waitClose、尾部 passed/failed 汇总与出口码。场景：
    - S1 默认无 prefs：startWesh([]) → dialHello → welcome 对象无 'prefs' 键（omitempty 缺席回归）。
    - S2 client-option 两键注入：startWesh(['--client-option','fontSize=18','--client-option','cursorBlink=false']) → welcome.prefs.fontSize === 18 且 welcome.prefs.cursorBlink === false。
    - S3 theme JSON 对象：startWesh(['--client-option','theme={"background":"#000000"}']) → welcome.prefs.theme 为对象且其 background === '#000000'（detail 只打形状不打色值）。
    - S4 osc52 注入：startWesh(['--osc52']) → welcome.prefs.osc52 === true。
    - S5 组合：startWesh(['--client-option','fontSize=20','--osc52']) → fontSize 与 osc52 两键俱在且值正确。
    - S6 重复 key last-wins：startWesh(['--client-option','fontSize=14','--client-option','fontSize=22']) → welcome.prefs.fontSize === 22。
    - E1 非法 key：spawnExpectExit(['--client-option','allowProposedApi=true','--','bash']) → code === 2 且 stderr 含 'invalid --client-option key'。
    - E2 非法 JSON：spawnExpectExit(['--client-option','fontSize=abc','--','bash']) → code === 2 且 stderr 含 'not valid JSON'。
    - E3 osc52 key 拒绝（D-12）：spawnExpectExit(['--client-option','osc52=true','--','bash']) → code === 2 且 stderr 含 'invalid --client-option key'。
    - E4 缺 '='：spawnExpectExit(['--client-option','fontSize','--','bash']) → code === 2 且 stderr 含 'must be key=value'。
    每个 S 场景结束 kill 实例；dialHello 断言 welcome 取自 frames 中首帧类型字节 WELCOME 的 JSON.parse。check 的 detail 形态：mode/布尔/键存在性（如 `prefs keys=fontSize,cursorBlink` 键名可打、值不打）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && node --check web/uat/phase04.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node --check 语法校验退出 0
    - 脚本含 S1-S6 六正场景与 E1-E4 四负场景；S 场景各自独立 spawn 实例（单次语义纪律）
    - detail 断言位只引布尔/键名/状态码（无值内容字面量进 check 调用）
    - 零 import 第三方包（仅 node: 前缀内建模块）
  </acceptance_criteria>
  <done>phase04.mjs 十场景脚本就绪，覆盖 Welcome prefs 全部 wire 可断言面与 client-option 启动拒绝面。</done>
</task>

<task type="auto">
  <name>Task 2: 二进制构建接入与十场景全绿</name>
  <precondition>04-01 已合入（Go 侧 --client-option/--osc52 与 Welcome prefs 通道是全部断言的前提）。</precondition>
  <files>web/uat/phase04.mjs</files>
  <read_first>
    - README.md（构建节：前端构建先于 go build 的硬依赖——二进制内嵌 dist）
    - web/uat/phase03.mjs（尾部出口码与运行方式注释形态）
  </read_first>
  <action>
    1. `mkdir -p /tmp/wesh-uat && time pnpm -C web build && go build -o /tmp/wesh-uat/wesh ./cmd/wesh`（UAT 用临时路径测试实例——CODEBUDDY 未经确认禁止操作用户服务；构建 time 前缀跟踪耗时）。
    2. `node web/uat/phase04.mjs /tmp/wesh-uat/wesh` 运行；十场景全 PASS 退出 0。
    3. 失败时按 detail 定位：wire 形状问题回查 04-01（WelcomeFrame/聚合），启动拒绝问题回查 main.go fs.Func 校验——修复后重跑至全绿（不降级断言凑绿）。
    4. 回归确认：`node web/uat/phase02.mjs /tmp/wesh-uat/wesh` 与 `node web/uat/phase03.mjs /tmp/wesh-uat/wesh` 仍全绿（Welcome 加可选键的 wire 兼容证据——两脚本断言 mode 不受 nil prefs 缺席影响）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && time pnpm -C web build && go build -o /tmp/wesh-uat/wesh ./cmd/wesh && node web/uat/phase04.mjs /tmp/wesh-uat/wesh && node web/uat/phase02.mjs /tmp/wesh-uat/wesh && node web/uat/phase03.mjs /tmp/wesh-uat/wesh</automated>
  </verify>
  <acceptance_criteria>
    - phase04.mjs 十场景全 PASS 退出 0
    - phase02.mjs 与 phase03.mjs 回归全绿退出 0（wire 兼容）
  </acceptance_criteria>
  <done>FE-07 协议面自动化回归网就位：十场景绿 + 前两 phase UAT 零回归。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试脚本 → CI/控制台输出 | UAT 输出进 CI 日志面——值内容不得经 detail 泄漏（红线延伸到测试面） |
| 测试实例 ↔ 用户环境 | spawn 的 wesh 实例必须隔离于用户服务（临时端口 + loopback + 临时路径二进制） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Information Disclosure | UAT detail 打印 prefs/theme 值内容进 CI 日志 | low | mitigate | check detail 只打状态码/布尔/键名（prohibition 锁定；照 phase03.mjs 红线注释形态） |
| T-04-12 | Tampering | 测试实例占用固定端口/干扰用户服务 | low | mitigate | --port 0 随机端口 + --bind 127.0.0.1 + /tmp/wesh-uat 临时路径 + SIGKILL 收口（phase03.mjs 既定形态） |
| T-04-SC | Tampering | npm/pip/cargo installs | high | accept | 脚本零依赖（Node 原生 WebSocket/fetch）——无安装面 |
</threat_model>

<verification>
- `node web/uat/phase04.mjs /tmp/wesh-uat/wesh` 十场景全 PASS 退出 0
- phase02/phase03 UAT 回归全绿
- `go test ./...` 不受影响（本 plan 不动 Go 源码）
</verification>

<success_criteria>
- FE-07 可自动化协议面全部上锁：omitempty 缺席、逐键注入、theme 对象、osc52 下发、last-wins、四类启动拒绝
- wire 兼容性第三方证据：旧 UAT 脚本零修改全绿
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `web/uat/phase04.mjs`：S1-S6/E1-E4 十场景协议断言脚本（新文件；复用 phase03.mjs 的 startWesh/spawnExpectExit/dialHello/check 形态，无新导出符号——零依赖脚本）

<output>
Create `.planning/phases/04-frontend/04-04-SUMMARY.md` when done
</output>
