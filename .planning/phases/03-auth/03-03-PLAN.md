---
phase: 03-auth
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - internal/server/server.go
  - internal/server/auth.go
  - internal/server/e2e_test.go
  - internal/server/auth_e2e_test.go
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "已认证 POST /api/attach 换取一次性 ticket（单次使用、60s TTL、绑定权限级别），WS Hello 首帧核销，核销成功走既有升档序列（Welcome 的 mode 取自 ticket 绑定值，D-11）；重放/过期/非法 ticket 与节流中同口径 Error{auth_failed} + 1008（close reason 同名机器串，D-10 无 oracle）"
    - "/api/attach 守卫链完整：ServeMux 方法模式非 POST 405（+Allow 头）→ Origin 检查 403 → 节流闸 429 + Retry-After → Basic 常数时间比较 401 → 签发 200 {\"ticket\":...} + Cache-Control: no-store；body 经 MaxBytesReader 1KiB 上限，超限 413"
    - "整站 Basic（D-02）：/ 与 /api/attach 挂 basicAuth 中间件；无凭据/错用户/错密码的 401 响应完全同文（WWW-Authenticate: Basic realm=\"wesh\", charset=\"UTF-8\" + 通用 body，无枚举 oracle）；/ws 不挂 Basic（ticket 即其认证）"
    - "D-08 统一计数器落地：/api/attach 凭据失败与 Hello ticket 核销失败计入同一 per-IP throttleStore；认证成功清零；节流命中时 ticket 不核销不 recordFail（不延长窗口）"
    - "Origin 双端点执行（SEC-04/D-13）：/ws 守卫区 ⓪ originAllowed 403（Accept 前拒绝，HTTP 层可测）+ AcceptOptions.OriginPatterns 库内二次校验（纵深防御）；/api/attach 中间件同款检查；静态页 GET 不查"
    - "无认证模式（len(Credentials)==0）：/api/attach 返回 404（前端探测跳过 fetch 直连）；Hello 无 ticket 字段直接走既有升档（核销分支整体跳过）；既有全部无凭据测试零改动保持绿色"
    - "爆破 100 次错误凭据触发可观测退避（429 + Retry-After；ROADMAP 准则 2 锚点经 HTTP 层集成测试证明）"
    - "日志红线（SEC-01）：跑完整失败轮（401 + auth_failed + 429）后 stderr 输出不含 base64(凭据)、明文密码、已签发 ticket 值、authorization 字样（大小写不敏感）——运行时捕获断言（Pattern 8）"
    - "server.go 行 166-167、224-225 与 440（logEvent 注释块内）三处注释「Phase 3 SEC-07」校正为「Phase 7 SEC-07」（CONTEXT deferred 节明示的顺手项）"
    - "凭据比较与 ticket 核销全部在预认证 4KiB 读上限档内完成（Hello JSON +ticket ~120B，P2 D-11 两档纪律不变）"
  prohibitions:
    - "凭据、ticket、Authorization 头任何形态（含 base64）禁止作为任何 log/logEvent/Fprintf 参数（ttyd server.c:142 反例，SEC-01 红线）；新埋点只走 logEvent 三要素（remote/code/reason）"
    - "禁止手拆 Authorization: Basic base64（用 r.BasicAuth() stdlib 解析器，RESEARCH Don't Hand-Roll 表）"
    - "核销失败各形态（过期/非法/重放/节流）禁止发出可区分的响应（不同 code/reason/时延都是 oracle，D-10）；Hello 侧节流命中不得再 recordFail 延长窗口"
    - "basicAuth 401/403/429 响应 body 禁止回显用户名、Origin 值或任何请求细节（通用文案，无枚举/反射面）"
  artifacts:
    - path: "internal/server/server.go"
      provides: "Options 新字段（Credentials/Origins/TLS/TicketTTL/ThrottleBase/ThrottleCap）+ Handler 安全头与 Basic 装配 + /api/attach 端点 + 守卫区 ⓪ Origin + 握手段 auth_failed 分支"
      contains: "attachHandler"
    - path: "internal/server/auth.go"
      provides: "basicAuth 中间件（429 闸 + 401 同文 challenge + 统一计数器钩子）"
      contains: "basicAuth"
    - path: "internal/server/auth_e2e_test.go"
      provides: "认证集成测试组（tracer 全链路/端点守卫/非法与过期 ticket/无认证模式/日志脱敏）——新建黑盒 package server_test 文件（集成测依赖 server_test 包 helper，白盒 auth_test.go 跨包不可见；白盒文件仅留 03-01/03-02 单元测）"
      contains: "TestLogRedaction"
    - path: "internal/server/auth_e2e_test.go"
      provides: "HTTP 层节流集成（429+Retry-After/成功清零/Hello 共享计数器）"
      contains: "TestThrottleHTTP"
    - path: "internal/server/auth_e2e_test.go"
      provides: "双端点 Origin 集成断言"
      contains: "TestOriginEndpoints"
  key_links:
    - from: "internal/server/server.go"
      to: "internal/server/tickets.go"
      via: "attachHandler 调 tickets.issue(mode=writable 派生, now)；握手段 else-if 分支调 redeem + throttle.allow/recordFail"
      pattern: "redeem"
    - from: "internal/server/server.go"
      to: "internal/server/headers.go"
      via: "Handler() 最外层 securityHeaders(mux, s.tlsOn) 包裹全部路由（含 /ws）"
      pattern: "securityHeaders"
    - from: "internal/server/auth_e2e_test.go"
      to: "internal/server/e2e_test.go"
      via: "同包（server_test）复用 startTestServerWith/dialHello/waitExit + 新增 dialHelloTicket/attachURL/postAttach helper"
      pattern: "dialHelloTicket"
---

<objective>
Phase 3 集成 tracer：把 Wave 1 的全部组件接进 server.go——Options 扩展、Handler() 重装配（securityHeaders 最外层、整站 Basic、/api/attach 端点）、Attach 守卫区 ⓪ Origin 检查、握手段 ticket 核销与 auth_failed 统一口径分支，并以集成测试组锁定 HTTP 守卫链、WS 核销、节流共享计数器、Origin 双端点与日志红线。

Purpose: 本 plan 是 ROADMAP 准则 1（ticket 流程）与准则 2（节流 + subtle + 日志红线）的行为落地点；tracer 任务先证明「Basic → ticket → Hello → Welcome → 非法拒绝」单一主链路端到端可达，随后两个任务把攻击面与边界全部自动化。
Output: server.go 集成完成 + auth_e2e_test.go 集成测试组全绿（package server_test 黑盒新建文件；白盒测试文件保持 03-01/03-02 单元测零改动）。

规格探针标记（spec-less probe fallback）：SEC-01..SEC-04 探针报为 unclassified——按 §C 保持 unresolved，不自动核销；边界按 D-02/D-08/D-10/D-13 与 RESEARCH Pattern 1/2/3/8 落入 truths，日志红线与无 oracle 等 must-NOT 经 §B 回忆落入 prohibitions（descriptor-less，下游 dispose flagged-unverified），无静默丢弃。

**单会话模型测试约束（planner 裁决，记录理由）**：Phase 1 单客户端 409 门 + 握手失败经 wsDisconnected→terminate 收口（每服务器实例仅一次握手机会），「同一 ticket 第二次 Hello」的 wire 级重放测试结构上不可构造。ROADMAP 准则 1「重放被拒绝」由三层组合证明：ticketStore 单元测重放 false（03-01）+ wire 级非法 ticket auth_failed（本 plan，重放即已删除键的同一代码路径）+ UAT 脚本端到端（03-06）。executor 不得为迁就重放测试改动 409 门或终结收口（CONTEXT L92 硬约束）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth/03-CONTEXT.md
@.planning/phases/03-auth/03-RESEARCH.md
@.planning/phases/03-auth/03-PATTERNS.md
@internal/server/server.go
@internal/server/e2e_test.go
@internal/server/handshake_test.go
@cmd/wesh/main_test.go
</context>

<tasks>

<task type="tracer">
  <name>Task 1: server.go 集成 tracer——/api/attach 端点 + Hello 核销 + auth_failed 统一口径</name>
  <reversibility rating="costly">D-10：auth_failed wire 行为（Error 帧 + 1008 + 同名 reason）上线即前后端公开契约；用户已在 CONTEXT 锁定，仅记录不插 gate。</reversibility>
  <files>internal/server/server.go, internal/server/auth.go, internal/server/e2e_test.go, internal/server/auth_e2e_test.go</files>
  <read_first>
    - internal/server/server.go（被改文件现状：Options 63-69 行、Handler 117-130 行、守卫区与顺序敏感注释块 190-223 行、AcceptOptions 227-231 行、握手状态机 274-330 行、logEvent 433-443 行、待校正注释 166-167、224-225 与 440 行共三处）
    - internal/server/auth.go（03-02 产物：Credential/matchCredential——basicAuth 中间件追加进本文件）
    - internal/server/tickets.go + internal/server/throttle.go + internal/server/origin.go + internal/server/headers.go（03-01/03-02 产物的消费面：issue/redeem/allow/recordFail/recordSuccess/originAllowed/securityHeaders 签名）
    - internal/server/e2e_test.go（startTestServerWith 105-121 行/dialHello 129-158 行——新 helper 的模板）
    - .planning/phases/03-auth/03-RESEARCH.md（§System Architecture Diagram 行 153-190 装配图；Pattern 2 行 300-318 basicAuth 定稿；Pattern 3 行 377-387 Hello 侧接入点；Open Question 2 行 823-826 version 先查裁决）
    - .planning/phases/03-auth/03-PATTERNS.md（§server.go 改 175-209 行——三个插入点逐字形态；§Shared Patterns 日志出口 367-378 行红线）
    - .planning/phases/03-auth/03-CONTEXT.md（D-02/D-08/D-10/D-11/D-13）
  </read_first>
  <behavior>
    - tracer 主链路（TestAttachFlow）：凭据装配（Writable:true）→ POST /api/attach 无凭据 401 → 错凭据 401（与无凭据同文）→ 正确凭据 200 且 JSON 含 ticket、Cache-Control: no-store → dialHelloTicket(ticket) 收 Welcome 且 mode=="rw" → 客户端正常关闭 → waitExit(0)
    - 非法 ticket：Hello 携从未签发的 ticket → 首帧收 Error{code:"auth_failed", message 非空} → 随后 1008 且 close reason == "auth_failed" → waitExit(0)
    - 无认证模式：无凭据装配 → Hello 不带 ticket 直接收 Welcome（既有行为零漂移）
    - 既有全部测试（无凭据装配）零改动保持绿色——auth 分支仅在 len(Credentials)>0 时激活
  </behavior>
  <action>
    1. `internal/server/server.go` Options 扩展（注释延续分组纪律——生产直传 vs 测试覆写）：`Credentials []Credential`（生产直传，main 经 ParseCredential 构造；空 = 无认证模式）；`Origins []string`（生产直传，main 经 NormalizeOrigin 规范化后的串）；`TLS bool`（生产直传，仅驱动 HSTS 分支，D-06）；`TicketTTL time.Duration` / `ThrottleBase time.Duration` / `ThrottleCap time.Duration`（测试可覆写，零值各取 defaultTicketTTL/defaultThrottleBase/defaultThrottleCap）。
    2. Server 结构体追加字段：credentials []Credential、origins map[string]struct{}（规范化集合）、originList []string（规范化串切片，喂 OriginPatterns）、tickets *ticketStore、throttle *throttleStore、tlsOn bool。New() 装配：len(opts.Credentials)>0 时才构造 tickets/throttle（无认证模式两 store 为 nil，核销分支整体跳过）；origins 集合/list 在 opts.Origins 非空时构建（与凭据正交——--origin 无凭据也生效，D-12）；tlsOn = opts.TLS。
    3. `internal/server/auth.go` 追加 `basicAuth(next http.Handler, creds []Credential, th *throttleStore) http.Handler`：进 handler 先 clientIP 取键；th.allow(ip, time.Now()) false → 429 + Retry-After（ceil(notBefore-now) 秒；需 throttleStore 加一个返回剩余等待的查询方法或在 allow 签名上返回——形态 discretion，注释钉「窗口内不延长」不变量）+ logEvent(remote, 429, "throttled") + 通用文案 return；r.BasicAuth() 解析失败或 matchCredential false → th.recordFail + logEvent(remote, 401, proto.ErrAuthFailed) + WWW-Authenticate: Basic realm="wesh", charset="UTF-8"（RFC 7617）+ http.Error 通用文案 401——无/错凭据完全同文（OWASP 无枚举 oracle）；成功 → th.recordSuccess + next。**红线：凭据/Authorization 任何形态永不入 logEvent 参数**（三要素只有 remote/code/reason）。注释注明：logEvent code 参数复用 HTTP 状态码值（websocket.StatusCode 底层 int），HTTP 层事件三要素结构不变（PATTERNS Shared Patterns 裁决）。
    4. Handler() 重装配：mux 保持 "/"（wh）与 "/ws"（s.Attach）；新增 `mux.HandleFunc("POST /api/attach", ...)`——**无论认证模式都注册**：无认证模式 handler 直写 http.NotFound（404 是前端探测信号，RESEARCH Pattern 1 决策；显式注册避免依赖静态 handler 对 POST 的偶发行为）；认证模式链 = originMiddleware(basicAuth(attachHandler))。/ 与 /api/attach 在认证模式下包 basicAuth（D-02 整站 Basic）；/ws 不挂 Basic。最外层 `return securityHeaders(mux, s.tlsOn)` 包裹全部路由。守卫顺序注释更新：⓪ Origin 403（D-13，新增）→ ① 子协议 400 → ② per-IP 429 → ③ 409（原注释块同步改写，裁决理由：Origin 与库 Accept 内检查同语义前置，拒绝形态与 /api/attach 一致且 HTTP 层可测）。
    5. `attachHandler`（server.go 内方法）：MaxBytesReader(w, r.Body, 1024) + io.Copy(io.Discard, r.Body) 排空，出错 → 413 通用文案（D-11 请求体为空，上限纯防御）；mode = proto.ModeRW（s.writable）否则 proto.ModeRO；ticket := s.tickets.issue(mode, time.Now())；响应 200 + Content-Type: application/json + Cache-Control: no-store（ticket 不可落缓存，RESEARCH Pattern 6 表）+ 体 {"ticket":"..."}（json.Marshal 固定 schema）。**禁止把 ticket 值打进任何日志**。
    6. Attach 守卫区 ⓪：`if !originAllowed(r, s.origins) { http.Error(w, "origin not allowed", http.StatusForbidden); return }` 插在 ① 之前（通用文案不回显 Origin 值）。AcceptOptions 追加 OriginPatterns: s.originList（仅非空时设置——零值 nil 保持库默认同源校验，D-12 零配置零漂移）。
    7. 握手段新 else-if 分支（version 检查之后、升档之前——Open Question 2 裁决：version 为公开协议信息先查，核销紧随其后）：条件为 s.tickets != nil 且核销闸失败——闸语义（可实现为 s.checkTicket(ip, ticket) (mode string, ok bool) 方法收口）：throttle.allow(ip, now) false → 返回失败（**不 recordFail**——节流命中不延长窗口，ticket 不核销）；redeem 返回 ok=false → throttle.recordFail(ip, now)（D-08 统一计数）→ 失败；成功返回 ticket 的 mode。失败分支照抄 version_mismatch 桶形态：c.Write(ErrorFrame(proto.ErrAuthFailed, "authentication failed")) → s.logEvent(remote, websocket.StatusPolicyViolation, proto.ErrAuthFailed) → c.Close(1008, proto.ErrAuthFailed)（P2 D-07 机器串同名纪律）。升档序列内 mode 取值：认证模式用核销返回的 mode（D-11 绑定生效），无认证模式维持 s.writable 派生现状。
    8. 注释校正：行 166-167、224-225 与 440（logEvent 注释块内）共三处「Phase 3 SEC-07」→「Phase 7 SEC-07」（顺手项，CONTEXT deferred 节明示；RESEARCH 行 847 只记了前两处——以 grep 实查为准，440 行同文误写一并校正）。
    9. `internal/server/e2e_test.go` 追加 helper `dialHelloTicket(t, ctx, wsURL string, ticket string, cols, rows int) (*websocket.Conn, string)`：逐字复制 dialHello 形态，HelloPayload 加 Ticket: ticket；dialHello 本体不动（既有调用零漂移）。同文件加 `attachURL(wsURL string) string`（ws://→http://、/ws→/api/attach 串替换）与 `postAttach(t, url, user, pass string, headers map[string]string) *http.Response`（http.Client Do，SetBasicAuth 或显式跳过的开关参数）两个小 helper——Phase 3 集成组（auth_e2e_test.go，同包 server_test）复用。
    10. 新建 `internal/server/auth_e2e_test.go`（**package server_test 黑盒**——集成测依赖 e2e_test.go 的 startTestServerWith/dialHello/waitExit 与 step 9 新增 helper，白盒 package server 文件对其跨包不可见，放白盒必编译失败；03-02 的 auth_test.go 白盒文件保持纯单元测，本 plan 不追加）tracer 端到端 **TestAttachFlow**：startTestServerWith(t, []string{"/bin/cat"}, server.Options{Writable: true, Credentials: 经 ParseCredential 构造的一组 "uat-alice:correct-horse"}) → POST 无凭据断言 401 + WWW-Authenticate 在 → POST 错凭据断言 401 且两响应 body 逐字节相等（同文无 oracle）→ POST 正确凭据断言 200 + Cache-Control 含 no-store + 解码 ticket 非空 → dialHelloTicket 收 Welcome mode=="rw"（D-11：ticket 绑定 = 全局 writable）→ 正常关闭 → waitExit(0)。**TestTicketInvalid**：同装配 → Hello 携 "AAAAAAAAAAAAAAAAAAAAAA"（22 字符合法形态但未签发）→ 首帧 Error 帧 code==proto.ErrAuthFailed 且 message 非空 → 续读 CloseError Code==1008 且 Reason==proto.ErrAuthFailed → waitExit(0)。**TestNoAuthMode**：零值 Options 装配 → POST /api/attach 断言 404 → dialHello（无 ticket 既有 helper）收 Welcome → 关闭 → waitExit(0)。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test -race -count=1 -v -run 'TestAttachFlow|TestTicketInvalid|TestNoAuthMode' ./internal/server/ && go test -race -count=1 ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - TestAttachFlow 主链路全绿：401 同文/200+no-store/Welcome rw/退出码 0——单一 happy path 端到端证明（tracer 验收）
    - TestTicketInvalid 断言 Error 帧 + 1008 + reason 同名机器串（D-10/D-07）；TestNoAuthMode 断言 404 探测与无 ticket 直连
    - `go test -race -count=1 ./...` 全量退出 0（既有 14+ 测试零改动全绿——无认证模式零行为漂移的反证）
    - server.go 守卫区注释更新为 ⓪ Origin → ① 400 → ② 429 → ③ 409；三处（166-167/224-225/440 行）「Phase 7 SEC-07」校正完成
    - basicAuth 与 attachHandler 中无凭据/ticket/Authorization 值作为日志参数（走查 + 03-03 Task 2 的 TestLogRedaction 行为锁）
  </acceptance_criteria>
  <done>认证主链路端到端打通并提交：Basic → ticket → Hello 核销 → Welcome；非法 ticket 统一 auth_failed+1008；无认证模式零漂移（全部既有测试绿）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: auth_e2e_test.go 集成组——端点守卫链 / ticket 过期 / 日志红线</name>
  <files>internal/server/auth_e2e_test.go</files>
  <read_first>
    - internal/server/auth_e2e_test.go（被改文件现状：03-03 Task 1 新建并落 TestAttachFlow/TestTicketInvalid/TestNoAuthMode——本任务在其上追加；auth_test.go 白盒文件不碰）
    - internal/server/server.go（本 plan Task 1 后的守卫链实现——断言目标）
    - cmd/wesh/main_test.go（captureFd 行 62-79——os.Pipe 置换 *os.File 形态，本任务复制到 server 包测试；进程全局不并行纪律）
    - .planning/phases/03-auth/03-RESEARCH.md（Pattern 8 行 559-563 脱敏测试定稿；Pitfall 6 行 621-625 fetch 401 不弹窗）
    - .planning/phases/03-auth/03-PATTERNS.md（§测试文件组 313-336 行——dialWantStatus 负例三段式与关闭码+reason 双断言模式）
  </read_first>
  <behavior>
    - TestAttachEndpoint：GET /api/attach → 405 且 Allow: POST；POST 超 1KiB body → 413；POST 邪恶 Origin → 403（正文不回显 Origin 值）；POST 正确凭据 → 200 + Content-Type: application/json + Cache-Control: no-store + ticket 22 字符
    - TestTicketExpiry：Options.TicketTTL=100ms 注入 → 取 ticket 后 sleep 200ms → Hello 携该 ticket → Error{auth_failed} + 1008（过期与非法同口径）
    - TestLogRedaction：os.Pipe 捕获 stderr，跑完整失败轮（401 错凭据 + WS 非法 ticket auth_failed + 节流触发 429），断言输出不含 base64("user:pass")、明文 pass、已签发 ticket 值、"authorization"（大小写不敏感）；正向对照：输出确实含 auth_failed/throttled 事件行（证明捕获有效）
  </behavior>
  <action>
    均在 `internal/server/auth_e2e_test.go` 追加（同包复用 e2e_test.go 的 postAttach/attachURL/dialHelloTicket helper；每场景独立 startTestServerWith 实例——单会话模型约束见 objective）：
    1. **TestAttachEndpoint**（SEC-02 端点守卫链）：凭据装配。a) http.Get → 405 且 Allow 头含 POST（ServeMux 方法模式白拿）；b) POST 2KiB body（正确凭据）→ 413；c) POST 正确凭据 + Origin: https://evil.example → 403 且 body 不含 "evil.example"（不回显）；d) POST 正确凭据（无 Origin——D-13 非浏览器放行）→ 200 + Content-Type application/json + Cache-Control no-store + JSON 解出 22 字符 ticket；e) 200 响应同时断言 X-Content-Type-Options: nosniff 在（securityHeaders 最外层装配的横向证据）。收口：d) 的 ticket 不必使用（该服务器实例不再接 WS 亦可——测试末不 dial 即无终结路径触发，直接 return；exitf 不断言，注释说明本测试只验 HTTP 层）。
    2. **TestTicketExpiry**（SEC-02 TTL）：Options{TicketTTL: 100 * time.Millisecond, Credentials: 一组} → postAttach 取 ticket → time.Sleep(200ms) → dialHelloTicket → 首帧 Error{auth_failed} → 1008 + reason 同名 → waitExit(0)。（真实 sleep 仅此一处——TTL 相对语义由 now 注入无法跨进程模拟；200ms 对 10s 护栏余量充足。）
    3. **TestLogRedaction**（SEC-01 红线，Pattern 8 定稿）：os.Pipe 置换 os.Stderr（helper 复制 main_test.go captureFd 形态，注释「进程全局不并行」纪律）；编排：凭据 "redact-user:redact-pass-9f2c" 装配 → (0) 先以正确凭据 POST 取一张 ticket 作对照样本（200，recordSuccess 清零计数器，不影响后续编排）→ (a) POST 错凭据（"redact-user:redact-WRONG-9f2c"）→ 401（fails=1，notBefore=+1s）→ (b) 立即再 POST 错凭据一次 → 429（base 生产 1s：(a) 的 401 后窗口内第 2 次请求即命中节流——429 起于 fail 后第 1 次窗口内请求，无需 sleep 确定性成立；429 短路不 recordFail 不延长窗口）→ (c) dialHelloTicket 携非法 ticket → auth_failed（窗口内节流命中与非法 ticket 同口径 auth_failed，D-10——事件行照出）；恢复 stderr 后断言捕获串：不含 base64 编码的 "redact-user:redact-WRONG-9f2c"、不含明文 "redact-WRONG-9f2c"、不含 (0) 取出的 ticket 值、不含 "authorization" 小写形态（strings.ToLower 后检查）；正向对照断言含 "auth_failed" 与 "throttled" 事件行（捕获有效性证明，防空捕获假绿）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test -race -count=1 -v -run 'TestAttachEndpoint|TestTicketExpiry|TestLogRedaction' ./internal/server/ && go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - TestAttachEndpoint 五组断言全过（405+Allow/413/403 不回显/200 三头 + ticket 形态/nosniff 横向证据）
    - TestTicketExpiry 证明过期 ticket 同口径 auth_failed + 1008
    - TestLogRedaction 四类禁出串断言行齐全且含正向对照行（auth_failed/throttled 在捕获中）
    - `go test -race -count=1 ./internal/server/` 全量退出 0
  </acceptance_criteria>
  <done>ROADMAP 准则 2 的「日志脱敏测试」与准则 1 的端点守卫链/TTL 语义全部自动化锁定（SEC-01 红线获运行时行为证据）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: auth_e2e_test.go 集成组（续）——429 闸 / 共享计数器 / 双端点 Origin</name>
  <files>internal/server/auth_e2e_test.go</files>
  <read_first>
    - internal/server/auth_e2e_test.go（被改文件现状：Task 1/2 已落六个集成测——本任务追加节流与 Origin 集成段；throttle_test.go/origin_test.go 白盒文件保持 03-01/03-02 单元测不追加——集成测所需 helper 在 server_test 包，白盒跨包不可见）
    - internal/server/server.go（本 plan Task 1 后的装配——节流闸位置与 ⓪ Origin 位置）
    - internal/server/handshake_test.go（dialWantStatus 三段式负例断言 104-116 行——/ws 侧 403 断言模板）
    - .planning/phases/03-auth/03-RESEARCH.md（Pattern 3 行 377-387 Hello 侧接入点语义；D-08 统一计数器论证 行 72 段）
    - .planning/phases/03-auth/03-CONTEXT.md（D-08/D-09 验收锚点、D-13 双端点）
  </read_first>
  <behavior>
    - TestThrottleHTTP：Options.ThrottleBase=200ms 注入 → #1 错凭据 POST → 401（fails=1，notBefore=+200ms）→ sleep 250ms → #2 错凭据 POST → 401（fails=2，notBefore=+400ms）→ #3 立即 POST → 429 且 Retry-After ≥1（fail 后窗口内第 1 次请求即 429——429 短路不 recordFail 不延长窗口）→ sleep 450ms → 正确凭据 POST → 200（窗口过 + 成功清零）→ 紧接错凭据 POST → 401（清零后从 base 重启级数，第 1 次失败仍可请求）→ sleep 250ms → 末次正确凭据 POST → 200 收口（避开 +200ms 窗口）
    - TestThrottleHelloSharedCounter（D-08 共享证明）：先取合法 ticket → #1 错凭据 POST → 401（fails=1，notBefore=+200ms）→ #2 立即错凭据 POST → 429（窗口内命中，不 recordFail）→ 立即 dialHelloTicket（合法 ticket！）→ auth_failed + 1008——合法 ticket 被拒证明 HTTP 侧失败把 WS 侧闸住了（无节流时该 ticket 必然核销成功）
    - TestOriginEndpoints：--origin 等效装配（Origins: ["https://portal.example"]）→ /ws：无 Origin 放行（握手正常进 400/101 路径而非 403）、同源放行、白名单放行（Origin: https://portal.example 过 ⓪ 到达后续闸）、邪恶源（https://evil.example）HTTP 403、null 403；/api/attach：邪恶源 403、无 Origin 达 401（过 Origin 闸后凭据失败）、白名单源达 401/200
  </behavior>
  <action>
    1. `internal/server/auth_e2e_test.go` 追加（黑盒 server_test 延续——直接复用 postAttach/dialHelloTicket/startTestServerWith；throttle_test.go 白盒文件保持 03-01 单元测不追加）：
       - **TestThrottleHTTP**（SEC-03）：Options{Credentials: 一组, ThrottleBase: 200 * time.Millisecond}。节流语义锚点（03-01：fail#1 后 notBefore=+200ms——窗口内第 2 次请求即 429，「×2→401 后 #3→429」编排必红，严禁按旧序列写）：#1 错凭据 POST 断言 401 → sleep 250ms（等窗口过）→ #2 错凭据 POST 断言 401（fails=2，notBefore=+400ms）→ #3 立即 POST 断言 429 + Retry-After 解析为 ≥1 整数（429 短路不 recordFail、notBefore 不延长——注释钉死）→ sleep 450ms（过 400ms 窗口）→ 正确凭据 POST 断言 200（recordSuccess 清零生效）→ 紧接错凭据 POST 断言 401（级数从 base 重启，fail#1 仍可请求）→ sleep 250ms → 末次正确凭据 POST 断言 200 收口（避开 +200ms 窗口防 429；清零防状态污染后续测试——每测试独立实例，注释说明）。
       - **TestThrottleHelloSharedCounter**（D-08）：Options{Credentials: 一组, ThrottleBase: 200 * time.Millisecond}；正确凭据 postAttach 取合法 ticket → #1 错凭据 POST 断言 401（fails=1，notBefore=+200ms）→ #2 立即错凭据 POST 断言 429（窗口内命中，不 recordFail 不延长窗口——429 起于 fail 后的第 1 次窗口内请求，注释钉死）→ 立即 dialHelloTicket（合法 ticket）→ 首帧 Error{auth_failed} + 1008 → waitExit(0)。注释写明反证逻辑：无节流闸时合法 ticket 必核销成功收 Welcome——被拒即 HTTP 失败共享计数器把 WS 侧闸住的直接证据；且节流命中未核销 ticket（短路与 recordFail 纪律——走查点，wire 上按 D-10 不可区分）。
    2. `internal/server/auth_e2e_test.go` 追加 **TestOriginEndpoints**（SEC-04/D-13；origin_test.go 白盒文件保持 03-02 单元测不追加）：装配 helper 需支持 Origins 注入（Options{Origins: []string{"https://portal.example"} }——注意 Options 收规范化串，测试直接用已规范化形态）。/ws 侧经 websocket.DialOptions{HTTPHeader: ...} 设置 Origin 头：a) 无 Origin → Dial 失败形态为 400（子协议预检——证明过 ⓪ 到达 ①；带 wesh.v1 子协议时则可达 101/409 路径，取便于断言者）b) Origin: https://evil.example → resp.StatusCode==403；c) Origin: null → 403；d) Origin 置为 r.Host 同源值 → 过 ⓪（到达 400/101 任一后续形态即可证明非 403）；e) Origin: https://portal.example → 非 403（白名单放行，D-12）。/api/attach 侧（同实例或新实例）：f) 邪恶源 POST → 403；g) 无 Origin POST 错凭据 → 401（过 Origin 闸达 Basic）；h) 白名单源 POST 错凭据 → 401（同上）。每 WS 场景独立服务器实例收口 waitExit(0)（单会话约束）；纯 HTTP 场景可共用一个实例（注释说明）。
    3. auth_e2e_test.go 文件头注释更新：列出本 plan 追加的九个集成测试名与对应需求映射（延续 handshake_test.go 行 18-21 的文件头纪律）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test -race -count=1 -v -run 'TestThrottleHTTP|TestThrottleHelloSharedCounter|TestOriginEndpoints' ./internal/server/ && go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - TestThrottleHTTP 断言 429 + Retry-After、窗口后成功清零、级数重启、末次 200 收口避开窗口（sleep 编排与 base=200ms 语义一致）
    - TestThrottleHelloSharedCounter 以合法 ticket 被拒反证 D-08 共享计数器
    - TestOriginEndpoints 双端点合计 ≥7 子断言（含 null 403、白名单放行、无 Origin 放行）
    - `go test -race -count=1 ./internal/server/` 全量退出 0
  </acceptance_criteria>
  <done>SEC-03 的 HTTP 层退避（ROADMAP 准则 2 锚点）与 SEC-04 的双端点执行全部自动化锁定；D-08 统一计数器获行为级反证。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 未认证 HTTP → /api/attach 守卫链 | 凭据、Origin、body 全部不可信；405/403/429/401 各闸是本 plan 的核心防线 |
| 未认证 WS → 握手段核销 | Hello 载荷（含 ticket）不可信；核销与节流闸在升档前执行 |
| 凭据/ticket → stderr 日志 | 日志是泄密面；任何新埋点必须过红线审查 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Elevation of Privilege | /api/attach 凭据爆破 | high | mitigate | basicAuth 内 429 闸 + 常数时间比较 + 401 同文（无枚举 oracle）；TestThrottleHTTP/TestAttachFlow 同文断言 |
| T-03-13 | Spoofing | 非法/过期 ticket 建连 | high | mitigate | 握手段核销闸（version 后升档前）统一 auth_failed+1008；TestTicketInvalid/TestTicketExpiry 锁定 |
| T-03-14 | Information Disclosure | 凭据/ticket/Authorization 经日志外泄（ttyd server.c:142 反例） | high | mitigate | logEvent 三要素唯一出口 + TestLogRedaction 运行时捕获断言四类禁出串 + 正向对照防假绿 |
| T-03-15 | Information Disclosure | 核销/认证结果 oracle（区分过期/非法/重放/节流/错用户/错密码） | medium | mitigate | D-10 统一口径 + 401 同文 + 节流命中不核销不 recordFail；测试断言 body 逐字节相等 |
| T-03-16 | Elevation of Privilege | CSWSH 经 /ws 或 /api/attach 跨源调用 | high | mitigate | ⓪ 守卫 + 库 OriginPatterns 双执行 + /api/attach 中间件同款；TestOriginEndpoints ≥7 断言 |
| T-03-17 | DoS | /api/attach 大 body 资源占用 | medium | mitigate | MaxBytesReader 1KiB + 413（D-11 空 body 语义）；ReadHeaderTimeout 5s 既有（main.go:92） |
| T-03-18 | DoS | 节流计数器被用作锁定合法用户（shared-IP 误伤） | low | accept | per-IP 语义下 NAT 共担是已知限制（clientIP 注释钉死）；封顶 30s 无锁定状态机（D-09 裁决）；X-Forwarded-For 属 Phase 7 |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | 零新增依赖（RESEARCH §Package Legitimacy Audit） |
</threat_model>

<verification>
- `go test -race -count=1 ./...` 全绿（新增 9 个集成测试 + 既有全部零改动）
- `go build ./... && go vet ./...` 退出 0
- 代码评审：守卫链顺序（⓪ Origin → 405 → 429 → 401 → 签发）与握手段分支顺序（malformed → version → 核销 → 升档）；红线走查（无凭据/ticket 入日志参数）
</verification>

<success_criteria>
- ROADMAP 准则 1（ticket 签发/核销/重放拒绝）与准则 2 前半（节流 + 日志红线）行为级落地
- SEC-04 双端点执行完成；D-08 统一计数器获共享反证
- 无认证模式零漂移（既有测试全绿）；03-04（main 装配）与 03-05（前端消费）的全部服务端接口就绪
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `internal/server/server.go`：`Options.Credentials`/`Options.Origins`/`Options.TLS`/`Options.TicketTTL`/`Options.ThrottleBase`/`Options.ThrottleCap` 字段、`attachHandler` 方法、Attach 守卫区 ⓪ Origin 检查、握手段核销分支（`checkTicket` 收口方法，形态 discretion）
- `internal/server/auth.go`：`basicAuth` 中间件（追加）
- `internal/server/e2e_test.go`：`dialHelloTicket`/`attachURL`/`postAttach` helper（追加）
- `internal/server/auth_e2e_test.go`（新建，package server_test 黑盒）：`TestAttachFlow`/`TestTicketInvalid`/`TestNoAuthMode`/`TestAttachEndpoint`/`TestTicketExpiry`/`TestLogRedaction`/`TestThrottleHTTP`/`TestThrottleHelloSharedCounter`/`TestOriginEndpoints`；白盒 auth_test.go/throttle_test.go/origin_test.go 保持 03-01/03-02 单元测零改动

<output>
Create `.planning/phases/03-auth/03-03-SUMMARY.md` when done
</output>
