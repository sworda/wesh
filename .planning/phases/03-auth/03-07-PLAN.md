---
phase: 03-auth
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
  - .planning/phases/03-auth/03-VERIFICATION.md
  - .planning/phases/03-auth/03-UAT.md
autonomous: true
gap_closure: true
gap_ids: [G-03-5]
requirements: [SEC-05, SEC-01]
tags: [tls, startup, resource-leak, docs]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "坏 --tls-cert/--tls-key 路径时 wesh 在零资源占用阶段（无 spawn/无 listen/无 listening 打印）以 exit 1 报错退出"
    - "net.Listen 与 Serve/ServeTLS 任一启动失败路径均回滚 sess.Close()，不留 pty 子进程孤儿"
    - "03-VERIFICATION.md 与 03-UAT.md 的交互式复现命令全部带 --writable（ro 默认姿态下可真实操作终端）"
  artifacts:
    - "cmd/wesh/main.go — 证书预检 + serve 失败回滚"
    - "cmd/wesh/main_test.go — TestBadCertPreflight 两场景回归锁"
    - ".planning/phases/03-auth/03-VERIFICATION.md — 2 处复现命令修正"
    - ".planning/phases/03-auth/03-UAT.md — Test 5 复现命令修正"
  key_links:
    - "tls.LoadX509KeyPair 预检位于 run() 内 pty.Start / net.Listen / listening 打印三者之前"
    - "hs.TLSConfig.Certificates 复用预加载证书对 → ServeTLS(ln, \"\", \"\")（证书加载单一事实源）"
    - "Serve/ServeTLS 共享错误路径 _ = sess.Close() 与 main.go:178-181 listen 失败回滚逐字对称"
---

<objective>
闭合 UAT 诊断缺口 G-03-5（severity: minor）——TLS 启动路径的 print-then-die 时序缺陷与
serve 失败孤儿泄漏，外加文档 --writable 清扫。

Purpose: 坏证书路径必须在零资源占用阶段报错（与 validateStartup「拒绝路径零资源占用」
纪律一致），且任何启动失败都不得残留 pty 子进程孤儿；复现文档命令必须带 --writable
（ro 是默认安全姿态，交互式复测需要显式可写）。
Output: main.go 预检+回滚修复、TestBadCertPreflight 回归锁、两份 .planning 文档命令修正。

Gap 根因（03-UAT.md ## Gaps，已诊断实证）:
1. main.go:192 先打印 "listening on https://..."，:202 ServeTLS 才加载证书文件——坏证书时
   print-then-die，启动输出误导。
2. main.go:206-209 ServeTLS 失败路径直接 return 1，未 sess.Close() 回滚已 spawn 的 pty
   子进程（对照 :178-181 net.Listen 失败路径有回滚）——孤儿 bash 残留。
3. 03-VERIFICATION.md 复现命令漏 --writable（03-UAT.md Test 1 已在 UAT 期间修正，Test 5
   命令片段仍漏）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@cmd/wesh/main.go
@cmd/wesh/main_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TLS 证书启动预检 + serve 失败 sess.Close() 回滚（G-03-5 根因①②）</name>
  <files>cmd/wesh/main.go, cmd/wesh/main_test.go</files>
  <behavior>
    TestBadCertPreflight（新增，captureFd 纪律，t.Setenv 清空 WESH_CREDENTIAL，import 增
    net/strconv；两子场景均 --bind 127.0.0.1 loopback 免 validateStartup 拦截、证书路径
    指向不存在文件如 /nonexistent/cert.pem 与 /nonexistent/key.pem、命令 `-- true`）：
    - 场景 A（free port，--port 0）：run 返回 1；captureFd(&os.Stdout) 断言 stdout 不含
      "listening on"（print-then-die 回归锁）；captureFd(&os.Stderr) 断言 stderr 含所给
      cert 路径。当前代码会先 spawn+listen+打印 listening 再死于证书加载——RED 可见。
    - 场景 B（occupied port）：测试内 net.Listen 占住 127.0.0.1:0 取实际端口、defer
      Close，--port 传该端口；run 返回 1 且 stderr 含 cert 路径、不含 "already in use"
      ——锁预检先于 net.Listen 的顺序。当前代码先撞 listen 冲突报地址错——RED 可见。
    - 两场景均即时返回即证明无 spawn/无 listen 挂起（TestStartupRefusalNoResource 同构
      纪律，在测试注释中写明该推理）。
    - serve 失败回滚路径无故障注入手段（Serve 阻塞语义 + lifecycle os.Exit 不可在单测
      驱动），以与 listen 路径逐字对称 + 代码评审锁定——在测试或实现注释中说明这一点。
  </behavior>
  <action>
    先写 TestBadCertPreflight 并跑 RED，再实现。cmd/wesh/main.go 三处改动（顺序敏感）：
    1. 证书预检（根因①）：run() 中 validateStartup 警告处理之后、pty.Start 之前插入——
       当 cfg.tlsCert != "" 时调用 tls.LoadX509KeyPair(cfg.tlsCert, cfg.tlsKey)（import
       增 crypto/tls）；失败则 fmt.Fprintf(os.Stderr, "wesh: %v\n", err) 并 return 1
       （exit 1 与 pty.Start/net.Listen 失败路径同档——运行时 I/O 错误，非配置矩阵错误；
       此时无 spawn/无 listen/无 listening 打印，零资源占用）。成功则保留返回的
       tls.Certificate 局部变量供下方复用。
    2. ServeTLS 复用预加载证书：TLS 分支改为 hs.TLSConfig = server.TLSConfig() 后将其
       Certificates 字段赋值为预加载对的单元素切片，然后 hs.ServeTLS(ln, "", "")——
       stdlib 约定 TLSConfig.Certificates 非空时 certFile/keyFile 可为空串；证书加载
       因此只有预检一个事实源，不存在第二次磁盘读取。
    3. serve 失败回滚（根因②）：Serve/ServeTLS 共享的 `if err != nil` 错误路径在打印
       前补 `_ = sess.Close()`——与 net.Listen 失败路径（main.go:178-181）逐字对称
       （Close master 后 setsid 组长子进程收 SIGHUP 退出，不留孤儿）。
    注释纪律：新增注释用中文并引用 G-03-5，风格对齐文件既有注释（沿用「拒绝路径零资源
    占用」表述）。红线：错误文案只含证书路径与 OS/tls 错误，绝不含凭据值（SEC-01 日志
    红线延伸到启动面，与 TestStartupMatrix 同纪律）。D-07 单行启动打印不动——listening
    行仍只在 listener 就绪后打印一次，文案逐字不变。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && go test ./cmd/wesh/ -run TestBadCertPreflight -v -count=1 && go vet ./cmd/wesh && test -z "$("$(go env GOROOT)"/bin/gofmt -l cmd/wesh)" && go test ./... -race -count=1</automated>
  </verify>
  <done>TestBadCertPreflight 两场景绿（当前代码下均 RED：场景 A stdout 有 listening，场景 B 报地址冲突）；gofmt 经 GOROOT 版本零输出（01-03 决策：/usr/bin/gofmt 陈旧不可用）；vet 零告警；-race 全量套件零回归；坏证书路径 exit 1 且无 spawn/listen/打印副作用；serve 失败路径与 listen 失败路径回滚逐字对称。</done>
</task>

<task type="auto">
  <name>Task 2: 文档清扫——复现命令补 --writable（G-03-5 missing 第 3 条）</name>
  <files>.planning/phases/03-auth/03-VERIFICATION.md, .planning/phases/03-auth/03-UAT.md</files>
  <action>
    交互式复现命令补 --writable（ro 是默认安全姿态，不带则浏览器端禁输入、标题 [ro]，交互复测必卡）：
    1. 03-VERIFICATION.md 两处：frontmatter human_verification 第 1 条（:10）与「Human
       Verification Required」第 1 节（:154）的 `wesh --bind 127.0.0.1 --credential user:pass -- bash`
       → 在 `--credential user:pass` 后插入 `--writable`，与 03-UAT.md Test 1 已修形态逐字一致。
    2. 03-UAT.md Test 5（:92）TLS 实例命令片段 `--credential user:pass --tls-cert cert.pem --tls-key key.pem`
       → 尾部补 ` --writable`（用户 2026-08-18 复测实际即带 --writable，仅补记指令）。
    3. README.md：仅核查不改动——`wesh -- bash` 出现于 :11（Phase 1/2 历史行为变更说明，
       非复现命令）与 :79（systemd 部署单元示例，ro/rw 属部署形态选择，改它会变更安全示例
       语义，超出本 gap 范围）。若判断 systemd 示例会引发误解，登记一行到
       .planning/phases/03-auth/deferred-items.md，不直接改 README。
    禁碰：03-UAT.md 的 result/manual_evidence/auto_evidence 等判定字段一个字不动——
    已 pass 的测试记录是历史证据；03-VERIFICATION.md 其余 verified 结论不动。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && test "$(grep -c -- '--credential user:pass --writable -- bash' .planning/phases/03-auth/03-VERIFICATION.md)" = "2" && test "$(grep -c -- 'key.pem --writable' .planning/phases/03-auth/03-UAT.md)" = "1" && git diff --quiet -- README.md</automated>
  </verify>
  <done>03-VERIFICATION.md 两处命令带 --writable（计数=2）；03-UAT.md Test 5 命令带 --writable（计数=1，Test 1 既有修正不计入该模式）；README.md 零改动；03-UAT.md 判定字段零改动（git diff 仅 test 指令行）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本地证书/私钥文件 → 进程 | 启动面读取 operator 提供的密钥材料（本地信任域） |
| 进程 stderr → 操作员/日志收集 | 启动错误文案跨出进程边界，适用 SEC-01 红线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07-01 | Information Disclosure | 证书预检错误文案（run() stderr） | medium | mitigate | 错误仅经 fmt %v 透传 OS/tls 包错误（路径+系统错误），不拼接任何凭据材料；新测试不引入凭据 flag，既有 TestStartupMatrix 红线断言纪律覆盖启动面 |
| T-03-07-02 | Denial of Service | pty 子进程生命周期（serve 失败孤儿泄漏） | low | mitigate | Serve/ServeTLS 共享错误路径 `_ = sess.Close()` 与 listen 失败路径对称回滚（setsid 组长收 SIGHUP 退出） |

（本计划无 npm/pip/cargo 包安装，不适用 Package Legitimacy Gate。）
</threat_model>

<verification>
- `go test ./cmd/wesh/ -run TestBadCertPreflight -v -count=1` 两场景通过
- `go test ./... -race -count=1` 全量零回归（README 既定项目测试命令）
- GOROOT gofmt / go vet 零输出
- 文档 grep 计数门全过；README.md 与 UAT 判定字段零改动
- G-03-5 三条 missing 全部落地（预检 / 回滚 / 文档清扫）
</verification>

<success_criteria>
坏 --tls-cert/--tls-key 路径下 wesh 先于 listening 打印报错退出（exit 1、零资源占用）；
listen/serve 任一启动失败均无 pty 孤儿；两份 .planning 文档的交互复现命令可直接复制
执行并真实操作终端。G-03-5 可由 /gsd-verify-work 对账关闭。
</success_criteria>

<output>
Create `.planning/phases/03-auth/03-07-SUMMARY.md` when done
</output>
