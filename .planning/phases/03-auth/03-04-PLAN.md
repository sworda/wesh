---
phase: 03-auth
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
  - internal/pty/spawn_test.go
autonomous: true
requirements:
  - SEC-01
  - SEC-04
  - SEC-05

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "--credential user:pass 可重复 flag（fs.Func 收集），回调内经 server.ParseCredential parse 期校验（畸形值即时报错，systemd 配置错误零窗口暴露）；WESH_CREDENTIAL env 兜底单组凭据，flag 非空时 env 整体忽略（flag 优先，D-01）；help 文案注明 flag 值对同机用户可见（ps）、生产建议 env（Pitfall 8）"
    - "--tls-cert/--tls-key 成对校验：只给其一 parse 期报错（D-04）；--no-auth 与 --insecure-http 为显式逃生门（D-03/D-05 同构）；--origin 可重复，回调内经 server.NormalizeOrigin parse 期规范化（D-12）"
    - "启动校验矩阵（RESEARCH Pattern 7 八行）经纯函数 validateStartup 落地，先于 pty.Start/net.Listen 执行：非 loopback + 无凭据 + 无 --no-auth → 拒绝启动（文案精确含 \"refusing to listen on non-loopback address without credentials; pass --no-auth to disable authentication\"）；非 loopback + 明文 + 有凭据 + 无 --insecure-http → 拒绝启动（文案精确含 \"refusing to serve credentials over plaintext HTTP on non-loopback address; pass --insecure-http or provide --tls-cert/--tls-key\"）"
    - "loopback 判定规则：bind 空串视为非 loopback；net.ParseIP(bind).IsLoopback()；localhost 特判 loopback；0.0.0.0/:: 非 loopback；其他主机名保守按非 loopback（Pattern 7 裁决，保守方向出错 = 多要一个显式逃生门，不削弱安全）"
    - "明文 HTTP 且非 loopback（含逃生门放行路径）stderr 醒目警告；loopback + 明文（有无凭据皆然）放行免警告（D-03/D-05：流量不出机）；TLS + 凭据为最强形态免警告"
    - "TLS 分岔：cfg.tlsCert 非空 → hs.TLSConfig = server.TLSConfig() 后 hs.ServeTLS(ln, cert, key)；否则 hs.Serve(ln)（D-04）；启动行 scheme 感知（http:// vs https://）"
    - "行为变更落地（D-03 收口 Phase 1 D-05 临时取舍）：默认 bind 0.0.0.0 下 `wesh -- bash` 现拒绝启动，需 --no-auth 或凭据；Phase 1/2 旧用法迁移路径在 README 明示（03-06 落地文档）"
    - "WESH_CREDENTIAL 不出现在子进程环境（SEC-06 白名单回归：spawn 路径替换式注入天然剥离——本 phase 新增针对该变量名的断言，防未来有人改累加式注入）"
    - "server.New 装配透传：Options{Writable, PingInterval, Credentials, Origins, TLS: cfg.tlsCert != \"\"}（TicketTTL/ThrottleBase/ThrottleCap 零值走默认，生产无 flag——D-09 标定值即契约）"
  prohibitions:
    - "启动路径任何 stderr/stdout 输出（错误/警告/启动行/usage）禁止包含凭据值、ticket、证书私钥内容（日志红线延伸到启动面）"
    - "validateStartup 必须是纯函数——禁止在其中 listen/spawn/写文件（副作用会使拒绝路径先占资源、测试挂死；main_test.go:81-83 纪律）"
    - "新 flag 禁止短选项与别名（P2 D-15 全名契约）；禁止为兼容 Phase 1/2 裸跑而设默认跳过的隐式逃生门"
  artifacts:
    - path: "cmd/wesh/main.go"
      provides: "6 个新 flag 注册 + WESH_CREDENTIAL 兜底 + validateStartup 纯函数 + ServeTLS 分岔 + 警告打印"
      contains: "validateStartup"
    - path: "cmd/wesh/main_test.go"
      provides: "TestParseArgs 新 flag 表行 + TestStartupMatrix 八行矩阵 + env 兜底断言"
      contains: "TestStartupMatrix"
  key_links:
    - from: "cmd/wesh/main.go"
      to: "internal/server/auth.go"
      via: "--credential/WESH_CREDENTIAL 经 server.ParseCredential 构造 []server.Credential 入 Options"
      pattern: "ParseCredential"
    - from: "cmd/wesh/main.go"
      to: "internal/server/tls.go"
      via: "run() 分岔 hs.TLSConfig = server.TLSConfig() + ServeTLS"
      pattern: "TLSConfig"
    - from: "cmd/wesh/main.go"
      to: "internal/server/origin.go"
      via: "--origin 回调经 server.NormalizeOrigin 规范化入 Options.Origins"
      pattern: "NormalizeOrigin"
---

<objective>
main.go 落地全部 CLI 面：6 个新 flag（--credential/--tls-cert/--tls-key/--no-auth/--insecure-http/--origin）+ WESH_CREDENTIAL env 兜底、启动校验矩阵（D-03/D-05 八行）、ServeTLS 分岔与警示打印，并以 main_test.go 表驱动与 captureFd 断言锁定契约。

Purpose: flag 名与启动行为是公开发布契约（D-01/D-03/D-04/D-05/D-12 均 one-way 已锁定）；本 plan 把 03-02/03-03 的组件接上用户可触面，SEC-05 的 TLS 从此可真实伺服。
Output: main.go 全部新装配 + TestStartupMatrix 八行 + parse 表扩展全绿。

规格探针标记（spec-less probe fallback）：SEC-01/SEC-04/SEC-05 探针报为 unclassified——按 §C 保持 unresolved，不自动核销；边界按 D-01/D-03/D-04/D-05/D-12 决策与 RESEARCH Pattern 7 落入 truths，启动面红线等 must-NOT 经 §B 回忆落入 prohibitions（descriptor-less，下游 dispose flagged-unverified），无静默丢弃。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth/03-CONTEXT.md
@.planning/phases/03-auth/03-RESEARCH.md
@.planning/phases/03-auth/03-PATTERNS.md
@cmd/wesh/main.go
@cmd/wesh/main_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseArgs——6 个新 flag + WESH_CREDENTIAL env 兜底</name>
  <reversibility rating="one-way">D-01/D-12：CLI flag 与 env 名是公开契约（P2 D-15 同纪律），发布后改名破坏脚本与 systemd unit；用户已在 CONTEXT 锁定，仅记录不插 gate。</reversibility>
  <files>cmd/wesh/main.go, cmd/wesh/main_test.go</files>
  <read_first>
    - cmd/wesh/main.go（被改文件现状：config struct 23-29 行、parseArgs 36-58 行 flag 注册与 Usage 覆写）
    - cmd/wesh/main_test.go（被改文件现状：TestParseArgs 表驱动 18-58 行、captureFd 62-79 行）
    - internal/server/auth.go（ParseCredential 签名与错误形态）、internal/server/origin.go（NormalizeOrigin 签名）
    - .planning/phases/03-auth/03-RESEARCH.md（Pattern 7 行 556-557 新 flag 一览与 6 名计数口径；Pitfall 8 行 631-635 help 文案义务）
    - .planning/phases/03-auth/03-PATTERNS.md（§main.go 改 244-271 行——fs.Func 收集与 parse 期校验挂点）
    - .planning/phases/03-auth/03-CONTEXT.md（D-01/D-03/D-04/D-05/D-12 flag 契约原文）
  </read_first>
  <behavior>
    - 默认零值：无凭据、无 TLS、无 origin、两逃生门 false
    - --credential 两次给出 → cfg.credentials 长度 2；畸形值（无冒号/空 user）→ parseArgs 返回错误
    - WESH_CREDENTIAL 设置且无 --credential → cfg.credentials 长度 1；同时给 flag → env 忽略（flag 优先）；env 畸形值 → 错误
    - --tls-cert 单给（无 --tls-key）→ 错误；成对给出 → cfg 两字段就位；--origin 合法值经规范化收集（https://EXAMPLE.com:443 → https://example.com），非法值 parse 期报错
  </behavior>
  <action>
    1. `cmd/wesh/main.go` config struct 追加：`credentials []server.Credential`、`tlsCert, tlsKey string`、`noAuth, insecureHTTP bool`、`origins []string`（注释逐字段标 D-xx 锚点）。
    2. parseArgs 注册 6 个新 flag（全名无短选项，P2 D-15；help 文案小写开头短语式延续现状）：
       - fs.Func("credential", "...", ...)——回调内 server.ParseCredential(s)，错误直返（parse 期暴露配置错误）；成功 append cfg.credentials。help 文案必须含「value visible to local users via ps; prefer WESH_CREDENTIAL env in production」语义（Pitfall 8）。
       - fs.StringVar(&cfg.tlsCert, "tls-cert", "", ...)/fs.StringVar(&cfg.tlsKey, "tls-key", "", ...)——help 注明成对给出才启用 TLS（D-04）。
       - fs.BoolVar(&cfg.noAuth, "no-auth", false, ...)——help 注明允许无凭据监听非 loopback（D-03 逃生门，醒目语义）。
       - fs.BoolVar(&cfg.insecureHTTP, "insecure-http", false, ...)——help 注明允许明文 HTTP 携带凭据于非 loopback（D-05 逃生门；反代终止 TLS 的常态场景）。
       - fs.Func("origin", "...", ...)——回调内 server.NormalizeOrigin(s) 后 append cfg.origins（D-12；规范化错误 parse 期暴露）。
    3. fs.Parse 成功后、argv 校验前的位置插入两段 parse 期校验：(a) tlsCert/tlsKey 恰好一个非空 → 返回错误（文案含 "must give both --tls-cert and --tls-key"，D-04）；(b) len(cfg.credentials)==0 且 os.Getenv("WESH_CREDENTIAL") 非空 → server.ParseCredential 解析 env 值，成功 append，失败返回错误（文案注明来源 env 名）。注释写明 flag 优先语义：flag 非空时 env 整体忽略（D-01）。
    4. parseArgs 注释块更新：列全 11 个 flag 与 env 兜底（延续行 31-35 的 D-xx 引用风格）。
    5. `cmd/wesh/main_test.go` TestParseArgs 表结构扩展（wantCredentials int 计数——不断言哈希值；wantOrigins []string 等）并加行：两 --credential 计数 2、--tls-cert/--tls-key 成对、--origin 大写+默认端口规范化结果断言、--no-auth/--insecure-http 布尔。新增独立测试：TestCredentialFlagEnv——t.Setenv("WESH_CREDENTIAL", "env-user:env-pass") 三组（仅 env → 1 组；flag+env → flag 优先 env 忽略仍 1 组且与 env 无关——以 flag 值计数断言；env 畸形 → 报错）；TestTLSKeyPairError——单给 --tls-cert 报错且文案含 both；--credential 畸形与 --origin 含 glob 字符各报错。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test -count=1 -v -run 'TestParseArgs|TestCredentialFlagEnv|TestTLSKeyPairError' ./cmd/wesh/ && go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - main.go 恰新增 6 个 flag 注册名（credential/tls-cert/tls-key/no-auth/insecure-http/origin），无短选项
    - --credential 与 --origin 的校验在 fs.Func 回调内（parse 期报错）；WESH_CREDENTIAL 兜底且 flag 优先
    - --tls-cert 单给报错；help 文案含 ps 可见性提示
    - `go test -count=1 ./cmd/wesh/` 退出 0（既有表行零改动保持绿）
  </acceptance_criteria>
  <done>CLI 解析面契约锁定：6 flag + env 兜底 + parse 期校验全部表驱动断言（D-01/D-04/D-12 落地）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run()——启动校验矩阵 + ServeTLS 分岔 + 警示打印</name>
  <reversibility rating="one-way">D-03/D-04/D-05：裸跑收口与 TLS 分岔改变 Phase 1/2 既有启动行为，发布后回退破坏已文档化的部署形态；用户已在 CONTEXT 锁定，仅记录不插 gate。</reversibility>
  <files>cmd/wesh/main.go, cmd/wesh/main_test.go, internal/pty/spawn_test.go</files>
  <read_first>
    - cmd/wesh/main.go（被改文件现状：run 60-98 行——错误出口模式 60-68 行、http.Server 分岔挂点 86-96 行、启动行打印 88 行）
    - cmd/wesh/main_test.go（被改文件现状：TestNoCommandError 的 run 级 captureFd 断言模式 81-94 行）
    - internal/pty/spawn_test.go（被改文件现状：env 白名单测试——WESH_CREDENTIAL 断言追加点的现状形态）
    - .planning/phases/03-auth/03-RESEARCH.md（Pattern 7 行 539-554——八行矩阵与拒绝文案逐字值、loopback 判定规则；Pattern 5 行 488-495 run() 分岔定稿）
    - .planning/phases/03-auth/03-PATTERNS.md（§main.go 改 271-279 行——分岔挂点与启动行分支感知）
    - .planning/phases/03-auth/03-CONTEXT.md（D-03/D-04/D-05）
  </read_first>
  <behavior>
    - validateStartup 八行矩阵（RESEARCH Pattern 7 逐字）：loopback+无凭据+无 TLS → nil 无警告；loopback+有凭据+明文 → nil 无警告；loopback+TLS → nil；非 loopback+无凭据+无 --no-auth → error（D-03 文案）；非 loopback+无凭据+--no-auth → nil + 裸奔警告；非 loopback+有凭据+明文+无 --insecure-http → error（D-05 文案）；非 loopback+有凭据+明文+--insecure-http → nil + 明文警告；非 loopback+有凭据+TLS → nil
    - run() 拒绝路径：stderr 输出拒绝文案 + 返回非零（经既有 "wesh: %v" 出口），且在 pty.Start/net.Listen 之前返回（无资源占用）
    - run() 放行路径：Options 透传 Credentials/Origins/TLS；tlsCert 非空 → TLSConfig + ServeTLS；启动行 scheme 正确（http/https）；明文非 loopback 时 stderr 有醒目警告
    - WESH_CREDENTIAL 在父进程环境设置时，spawn 的子进程环境不含该变量（SEC-06 回归）
  </behavior>
  <action>
    1. `cmd/wesh/main.go` 新增两个纯函数（均无副作用——main_test.go:81-83 纪律：校验必须先于 pty.Start/net.Listen）：
       - `isLoopbackBind(bind string) bool`：空串 false（视为全网卡）；net.ParseIP(bind) 成功取 IsLoopback()；bind == "localhost" 特判 true；其余主机名保守 false（注释写明保守方向裁决，Pattern 7）。
       - `validateStartup(cfg config) (warn string, err error)`：按 RESEARCH Pattern 7 八行矩阵实现——非 loopback && len(credentials)==0 && !noAuth → err 精确文案 "refusing to listen on non-loopback address without credentials; pass --no-auth to disable authentication"；非 loopback && len(credentials)>0 && tlsCert=="" && !insecureHTTP → err 精确文案 "refusing to serve credentials over plaintext HTTP on non-loopback address; pass --insecure-http or provide --tls-cert/--tls-key"；逃生门放行与非 loopback 明文场景给中文前缀 `wesh: warning:` 的醒目 warn 串（裸奔/明文语义分别写明，含对应逃生门 flag 名）；其余组合空 warn。cert/key 成对校验在 Task 1 已落 parse 期，此处不重复（注释注明分层）。**红线：warn/err 文案不得含凭据值**。
    2. run() 装配顺序改造：parseArgs → showVersion → **validateStartup（err 则 Fprintf stderr + return 2，warn 非空则 Fprintf stderr 警告——均在 pty.Start 前）** → pty.Start → net.Listen → server.New(sess, os.Exit, server.Options{Writable: cfg.writable, PingInterval: cfg.pingInterval, Credentials: cfg.credentials, Origins: cfg.origins, TLS: cfg.tlsCert != ""}) → 启动行 scheme 感知（tlsCert 非空打印 https:// 否则 http://，单行无 banner 纪律不变）→ 分岔：cfg.tlsCert != "" 时 hs.TLSConfig = server.TLSConfig() 且 hs.ServeTLS(ln, cfg.tlsCert, cfg.tlsKey)，否则 hs.Serve(ln)（注释引用 D-04/D-06；ReadHeaderTimeout 5s 既有保留）。
    3. `cmd/wesh/main_test.go` 新增 **TestStartupMatrix** 表驱动（直调 validateStartup 纯函数，八行全覆盖）：wantErr 行断言精确文案子串（"refusing to listen on non-loopback" / "refusing to serve credentials over plaintext"）；wantWarn 行断言警告非空且不含凭据值（凭据列用 "matrix-user:matrix-secret-7d1f"，断言 warn 串不含 "matrix-secret-7d1f"——启动面红线断言）。新增 run 级测试 **TestStartupRefusalNoResource**（captureFd 模式参照 TestNoCommandError）：run([]string{"--", "true"})（默认 bind 0.0.0.0 无凭据）→ 返回非零 + stderr 含 D-03 文案——正常快速返回即证明未 listen（否则挂死变红，既有纪律同构）。
    4. `internal/pty/spawn_test.go`：env 白名单既有测试追加断言——父环境设 WESH_CREDENTIAL=t.Setenv 后 spawn 的子进程环境不含该键（SEC-06 替换式注入的针对回归；读现状测试形态后选最小改法：若现状为表驱动名单断言则加一行样例，若逐键断言则加单条）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test -count=1 -v -run 'TestStartupMatrix|TestStartupRefusalNoResource' ./cmd/wesh/ && go test -race -count=1 ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - validateStartup 八行矩阵全覆盖表驱动断言，两拒绝文案与 RESEARCH Pattern 7 逐字一致
    - TestStartupRefusalNoResource 证明拒绝路径零资源占用（快速返回非零 + 文案）
    - run() 分岔：tlsCert 非空走 TLSConfig+ServeTLS，启动行 scheme 分支感知；警告路径 stderr 有醒目行
    - spawn_test.go 含 WESH_CREDENTIAL 剥离断言
    - `go test -race -count=1 ./...` 全量退出 0
  </acceptance_criteria>
  <done>D-03/D-04/D-05 启动安全矩阵落地：裸跑收口、成对证书、明文耦合全部可测；SEC-05 的 TLS 伺服路径与 SEC-01 的凭据入口（env 优先推荐）接线上线。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 部署者输入 → parseArgs/validateStartup | flag/env/配置文件值是半可信输入（部署者可能误配）；parse 期校验与启动矩阵是把误配变为显式错误的防线 |
| 同机其他用户 → 凭据面 | ps 输出与环境文件属本机威胁面（Pitfall 8） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-19 | Elevation of Privilege | 无认证裸奔监听公网（Phase 1/2 默认行为残留） | high | mitigate | D-03 非 loopback 无凭据拒绝启动 + --no-auth 显式逃生门；TestStartupMatrix/TestStartupRefusalNoResource 锁定 |
| T-03-20 | Information Disclosure | 凭据经明文 HTTP 跨网传输 | high | mitigate | D-05 非 loopback 明文+凭据拒绝启动 + --insecure-http 逃生门；矩阵测试锁定 |
| T-03-21 | Information Disclosure | 凭据经 ps 输出对同机用户泄露 | medium | mitigate | WESH_CREDENTIAL env 兜底（flag 优先）+ help 文案显式提示 + 03-06 README 推 EnvironmentFile 600 形态（Pitfall 8） |
| T-03-22 | Information Disclosure | WESH_CREDENTIAL 透传进 Web shell 子进程环境 | high | mitigate | SEC-06 替换式白名单天然剥离 + spawn_test.go 新增针对断言（防未来改累加式注入） |
| T-03-23 | Tampering | TLS 误配（单给证书/弱配置）导致启动失败或降级 | medium | mitigate | 成对校验 parse 期报错（D-04）；TLSConfig 声明式下限（03-02 组件复用，无二手配置路径） |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | 零新增依赖（RESEARCH §Package Legitimacy Audit） |
</threat_model>

<verification>
- `go test -race -count=1 ./...` 全绿（cmd/wesh 新增三测 + pty 断言 + 既有全部）
- `go build ./... && go vet ./...` 退出 0
- `go run ./cmd/wesh -- --help` 输出含 6 个新 flag 行（人工抽查，收进 03-06 冒烟）
</verification>

<success_criteria>
- 六个新 flag 与 env 兜底契约锁定，启动矩阵把两类危险误配变为显式拒绝
- TLS 伺服路径真实可用（03-06 UAT 的 wss 场景前提）
- Phase 1/2 裸跑行为变更完成代码侧收口，文档侧待 03-06
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `cmd/wesh/main.go`：`config` 新字段（`credentials`/`tlsCert`/`tlsKey`/`noAuth`/`insecureHTTP`/`origins`）、6 个新 CLI flag（`--credential`/`--tls-cert`/`--tls-key`/`--no-auth`/`--insecure-http`/`--origin`）、`isLoopbackBind` 函数、`validateStartup` 函数、`WESH_CREDENTIAL` env 读取
- `cmd/wesh/main_test.go`：`TestStartupMatrix`/`TestStartupRefusalNoResource`/`TestCredentialFlagEnv`/`TestTLSKeyPairError`（新增），`TestParseArgs` 表行扩展
- `internal/pty/spawn_test.go`：WESH_CREDENTIAL 剥离断言（追加）

<output>
Create `.planning/phases/03-auth/03-04-SUMMARY.md` when done
</output>
