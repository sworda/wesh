---
phase: 03-auth
plan: 06
type: execute
wave: 4
depends_on: ["03-03", "03-04", "03-05"]
files_modified:
  - web/uat/phase03.mjs
  - web/uat/phase02.mjs
  - README.md
  - .planning/phases/03-auth/03-UAT.md
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
  - SEC-04
  - SEC-05

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "web/uat/phase03.mjs 协议层自动化 UAT 全绿（Node 原生 WebSocket/fetch 零依赖，phase02.mjs 模式沿用）：完整链路（401 challenge → Basic → ticket → Hello → Welcome）、非法 ticket auth_failed+1008、爆破触发 429+Retry-After、邪恶 Origin 403（/ws 与 /api/attach）、无认证模式 404 探测与直连、TLS wss 全链路与 HSTS/nosniff 头断言"
    - "web/uat/phase02.mjs 在 D-03 裸跑收口后仍可运行（startWesh spawn 参数加 --bind 127.0.0.1——loopback 免凭据路径，一行适配）"
    - "README 认证与安全节落地：--credential/WESH_CREDENTIAL（systemd EnvironmentFile 600 推荐形态，Pitfall 8）/--no-auth/--insecure-http/--tls-cert/--tls-key/--origin 六 flag 语义与逃生门矩阵；D-03 行为变更在首屏警示区与用法节双重明示（默认 0.0.0.0 下 wesh -- bash 现需 --no-auth 或凭据）"
    - "README 协议节同步：Hello{version,ticket?,cols,rows}、Error code 表加 auth_failed（重试语义）、POST /api/attach 端点契约（POST-only/空 body/200 {ticket}/no-store/401/403/405/413/429）"
    - ".planning/phases/03-auth/03-UAT.md 人工清单文档化：浏览器 Basic 弹窗一次后同源 fetch 自动带凭据（A2 验证点）、页面放置 >60s 后重连静默重取 ticket（D-10 前端半侧）、testssl.sh docker 命令与预期（--protocols --std --server-defaults --header；全量 -U 可选）、HSTS 两年粘性风险提示（换 HTTP 后浏览器仍强制 HTTPS 至过期）"
    - "全量收口六段式全绿：GOROOT gofmt/go vet/go test -race 全量/pnpm -C web build/裸 clone 编译测试/启动冒烟（冒烟覆盖三形态：默认拒绝断言 D-03 文案、--bind 127.0.0.1 裸跑放行、--credential+自签证书 TLS 起服）"
    - statement: "testssl.sh 扫描无弱项（协议下限/cipher 清单/安全头；docker run --rm -ti drwetter/testssl.sh 手动执行——D-07 裁决不进 CI，外部依赖重；Go 自动化矩阵已在 03-02 覆盖协议与 cipher 下限）"
      verification: backstop
    - statement: "浏览器 Basic 弹窗一次输入后，同源 fetch /api/attach 自动携带缓存凭据返回 200（A2 假设——浏览器通行行为，人工 UAT 必验；若假则 D-02 流程断裂需复盘）"
      verification: backstop
  prohibitions:
    - "README 与 03-UAT.md 禁止提供 InsecureSkipVerify/关闭证书校验类教程（RESEARCH deprecated 纪律：自签证书指引 mkcert/CA 方案）"
    - "UAT 脚本任何输出禁止打印凭据值或 ticket 值（日志红线延伸到测试输出——check detail 只打印状态码/布尔/耗时）"
    - "冒烟与 UAT 禁止以 --no-auth 掩盖 D-03 行为变更——默认 bind 的拒绝路径必须作为断言项显式覆盖"
  artifacts:
    - path: "web/uat/phase03.mjs"
      provides: "Phase 3 协议层自动化 UAT（6 场景）"
      contains: "scenarioAuthFlow"
    - path: "README.md"
      provides: "认证/TLS/Origin 文档、行为变更明示、协议节同步"
      contains: "--credential"
    - path: ".planning/phases/03-auth/03-UAT.md"
      provides: "浏览器与 testssl.sh 人工验证清单"
      contains: "testssl"
  key_links:
    - from: "web/uat/phase03.mjs"
      to: "cmd/wesh/main.go"
      via: "spawn 真实二进制驱动六场景（flag 契约 + 启动矩阵的端到端证据）"
      pattern: "--credential"
    - from: "README.md"
      to: ".planning/phases/03-auth/03-CONTEXT.md"
      via: "文档直译 D-01..D-13 决策（flag 语义/逃生门/行为变更）"
      pattern: "--no-auth"
---

<objective>
Phase 3 收口：协议层自动化 UAT（phase03.mjs 六场景）+ phase02.mjs 的 D-03 适配 + README 认证/TLS 文档与行为变更明示 + 03-UAT.md 人工验证清单 + 全量收口验证六段式。

Purpose: ROADMAP 三条成功准则的证据链在此汇总（准则 1 ticket 流程 / 准则 2 爆破节流+日志红线 / 准则 3 Origin+TLS+安全头）；D-03 行为变更的文档明示是"单次语义明示传统"的延续（P1 传统：行为变更必须对用户透明）。
Output: phase03.mjs 全绿 + README/03-UAT.md 落地 + 六段式验证记录 + human-check 清单。

规格探针标记（spec-less probe fallback）：SEC-01..SEC-05 探针报为 unclassified——按 §C 保持 unresolved，不自动核销；本 plan 汇总五需求的端到端证据链，testssl.sh 与浏览器凭据缓存两项人工验证以 backstop 标记（honest-verifier：无法自证则 human_needed，非静默 pass），三条 must-NOT 经 §B 回忆落入 prohibitions（descriptor-less），无静默丢弃。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth/03-CONTEXT.md
@.planning/phases/03-auth/03-RESEARCH.md
@.planning/phases/03-auth/03-VALIDATION.md
@README.md
@web/uat/phase02.mjs
@cmd/wesh/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: web/uat/phase03.mjs 六场景 + phase02.mjs 的 D-03 适配</name>
  <precondition>03-03/03-04/03-05 已合入（depends_on 保证）；openssl 可用（生成 TLS 场景自签证书夹具，环境清单已核实 1.1.1k）；docker 仅手动 testssl 需要，不阻塞本任务。</precondition>
  <files>web/uat/phase03.mjs, web/uat/phase02.mjs</files>
  <read_first>
    - web/uat/phase02.mjs（模板整文件：startWesh spawn+端口解析 32-47 行、dialHello 52-66 行、check/PASS-FAIL 累积 26-29 行、场景+finally kill 形态、node:http 手构 Upgrade 196-211 行）
    - .planning/phases/03-auth/03-RESEARCH.md（§Validation Architecture 准则映射 872-874 行；§Environment Availability 行 832-845；Pitfall 6 fetch 行为）
    - .planning/phases/03-auth/03-PATTERNS.md（§phase03.mjs 355-365 行——逐段复制要点，含凭据不入 check 输出的红线延伸）
    - cmd/wesh/main.go（flag 契约与启动行格式真源——spawn 参数与端口解析正则的断言依据）
  </read_first>
  <action>
    新建 `web/uat/phase03.mjs`（零依赖 Node ≥22 原生 WebSocket/fetch；头注释格式沿用：phase 名 + 运行命令 `node web/uat/phase03.mjs [wesh 二进制路径]`）：
    1. 基础设施复用 + 两处基建适配：startWesh（spawn + stdout 端口解析——**所有场景 spawn 参数显式加 --bind 127.0.0.1**，D-03 后的合法起服前提；**端口解析正则从 phase02.mjs 行 39 的 /listening on http:\/\/[^\s]+:(\d+)/ 放宽为 /listening on https?:\/\/[^\s]+:(\d+)/**——03-04 启动行 scheme 感知，场景 5 TLS 起服打印 https://，照抄 http-only 正则会 8s 超时）、dialHello 扩展 ticket 参数（Hello JSON 加 ticket 字段）、check/waitClose/outputText/sleep；**新增 spawnExpectExit(args) helper**（spawn + 'exit' 事件等待 + stderr 累积 + 3s 超时 SIGKILL，返回 {code, stderr}）——场景 6 拒绝路径的进程不打印 listening 行而是直接退出，startWesh 等端口必然 8s 超时，拒绝场景必须走此 helper。**红线：凭据与 ticket 值永不进入 check detail/日志输出**（PATTERNS 365 行纪律）。
    2. **场景 1 scenarioAuthFlow（准则 1 主链路）**：spawn [--bind 127.0.0.1 --credential uat:uat-pass-x9 --writable -- bash --norc --noprofile]。断言：GET / 无凭据 → 401 且 WWW-Authenticate 含 Basic realm="wesh"；POST /api/attach 无凭据 → 401；错凭据 → 401；正确凭据（Authorization: Basic base64，Buffer 构造）→ 200 且 JSON.ticket 为非空串且 Cache-Control 含 no-store；dialHello(ticket) → Welcome(mode=rw)；Hello 携非法 ticket（22 字符 'A' 重复）→ 收 Error 帧 code==auth_failed 且 close code==1008 reason==auth_failed（同进程第二次 WS 建连不可行——单会话约束，非法 ticket 用独立 spawn 实例）。
    3. **场景 2 scenarioThrottle（准则 2 节流）**：spawn 同形态凭据实例；快速连发 8 次错凭据 POST /api/attach（<1s 内完成）；断言序列中先出现 401、后续出现 429 且 429 响应带 Retry-After 头（生产 base 1s：fail#1 后 notBefore=+1s，快速连发必然触发——无需 sleep 确定性成立）。
    4. **场景 3 scenarioOrigin（SEC-04）**：spawn [--credential ... --origin https://portal.example]；断言：POST /api/attach 带 Origin: https://evil.example（正确凭据）→ 403；带 Origin: https://portal.example → 200；/ws 手构 Upgrade（node:http，phase02.mjs 场景 5 形态）带邪恶 Origin + wesh.v1 子协议 → 403；不带 Origin → 到达非 403 响应（400/101/409 任一，D-13 非浏览器放行证明）。
    5. **场景 4 scenarioNoAuth**：spawn [--bind 127.0.0.1 -- bash]（loopback 裸跑合法）；断言 POST /api/attach → 404；dialHello 无 ticket → Welcome(mode=ro)。
    6. **场景 5 scenarioTLS（SEC-05）**：mktemp 目录 openssl req -x509 -newkey rsa:2048 -keyout key.pem -out cert.pem -days 1 -nodes -subj /CN=localhost 生成夹具；spawn [--credential ... --tls-cert cert --tls-key key]；进程首部 `process.env.NODE_TLS_REJECT_UNAUTHORIZED = '0'`（自签夹具的测试内正当用途，注释注明非教程）；断言：fetch https://127.0.0.1:port/ 无凭据 → 401 且响应头 Strict-Transport-Security 含 max-age=63072000、X-Content-Type-Options==nosniff、Content-Security-Policy 含 frame-ancestors 'none'；正确凭据 POST /api/attach → 200 取 ticket；dialHello(wss://..., ticket) → Welcome。清理 mktemp 目录。
    7. **场景 6 scenarioStartupMatrix（D-03/D-05 冒烟）**：两子场景均经 spawnExpectExit（step 1 的 spawn-exit-stderr 捕获 helper——不走 startWesh）：默认 bind（不加 --bind）无凭据 → 进程 3s 内退出且 stderr 含 "refusing to listen on non-loopback"（拒绝路径端到端证据）；[--bind 0.0.0.0 --credential ... 无 TLS] → 3s 内退出且 stderr 含 "refusing to serve credentials over plaintext"（D-05）。
    8. 汇总 exit code 形态沿用 phase02.mjs（passed/total + 场景异常计数）。
    修改 `web/uat/phase02.mjs`：startWesh spawn 参数加 '--bind', '127.0.0.1'（一行适配 + 注释引 D-03——loopback 裸跑免凭据；协议断言语义零改动）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go build -o /tmp/wesh-uat/wesh ./cmd/wesh && node web/uat/phase03.mjs /tmp/wesh-uat/wesh && node web/uat/phase02.mjs /tmp/wesh-uat/wesh</automated>
  </verify>
  <acceptance_criteria>
    - phase03.mjs 六场景全 PASS（场景 1 含 401 同文/200+no-store/Welcome/auth_failed+1008；场景 2 429+Retry-After；场景 3 双端点 403；场景 4 404+直连；场景 5 wss+HSTS 三头；场景 6 双拒绝文案）
    - phase02.mjs 11/11 保持全过（--bind 适配无协议语义漂移）
    - 两脚本输出 grep 无凭据/ticket 值（红线延伸到 UAT 输出）
  </acceptance_criteria>
  <done>ROADMAP 准则 1/2/3 的协议层证据链自动化闭环；D-03/D-05 拒绝路径获真实二进制端到端断言。</done>
</task>

<task type="auto">
  <name>Task 2: README 认证/TLS 节 + 03-UAT.md + 全量收口六段式</name>
  <precondition>plan 03-01 至 03-05 已全部完成并合入（depends_on 保证）；CI 远端运行结果不在本任务断言范围（推送时机由用户决定）。</precondition>
  <files>README.md, .planning/phases/03-auth/03-UAT.md</files>
  <read_first>
    - README.md（被改文件现状全文：首屏无认证警示、单次语义节、用法 flag 表、构建节、安全说明节、wesh.v1 协议节——本任务在其上重构安全相关各节）
    - .planning/phases/03-auth/03-CONTEXT.md（D-01..D-13 决策原文；specifics 节「显式逃生门风格/日志红线/反代张力已闭合」）
    - .planning/phases/03-auth/03-RESEARCH.md（Pitfall 8 行 631-635 README 义务；testssl.sh 命令 行 775-784；Open Question 3 HSTS 粘性注明义务 行 827-830）
    - .planning/phases/02-protocol/02-UAT.md（UAT 文档结构参照）
    - .planning/phases/02-protocol/02-06-PLAN.md（Task 2 六段式收口模式；human-check 清单形态）
    - cmd/wesh/main.go（flag 帮助文案与默认值真源）
  </read_first>
  <action>
    1. 更新 `README.md`（简体中文为主、命令与标识符英文，沿用既有文档风格）：
       - 首屏警示区改写：Phase 3 已落地认证/TLS/Origin 白名单——警示语义从「无认证」改为「默认拒绝裸奔」：默认 bind 0.0.0.0 下无凭据拒绝启动（D-03）；凭据+明文+非 loopback 拒绝启动（D-05）；loopback 裸跑不受限。**行为变更单独醒目小段**：Phase 1/2 的 `wesh -- bash` 用法在非 loopback 下现需 `--no-auth` 或凭据。
       - 用法节 flag 表新增六行：--credential（可重复，user:pass；值对同机用户可见 ps，生产建议 WESH_CREDENTIAL env）/--tls-cert 与 --tls-key（成对启用 TLS）/--no-auth/--insecure-http（显式逃生门语义）/--origin（可重复，完整 origin，不配则同源校验）。
       - 安全说明节新增「认证与传输安全」小节：整站 Basic（浏览器原生弹窗，D-02）；POST /api/attach 换一次性 ticket（单次使用/60s TTL/绑权限，重放拒绝）；失败节流 per-IP 指数退避 1s 翻倍封顶 30s（100 次爆破 ≥47min）；凭据比较 crypto/subtle 常数时间；凭据/ticket 任何形态不进日志；Origin 白名单语义（无 Origin 放行/同源放行/--origin 列表，D-13）；TLS MinVersion 1.2 默认 1.3 + AEAD-only cipher + 安全头集合（HSTS 仅 TLS）；systemd 部署推荐 EnvironmentFile 600 形态示例（Pitfall 8 义务）；testssl.sh 手动验证命令（docker drwetter/testssl.sh，D-07）+ HSTS 两年粘性风险提示。自签证书指引只给 mkcert/CA 方向，**不给关闭证书校验教程**（deprecated 纪律）。
       - 协议节同步：Hello 载荷加可选 ticket 字段；Error code 表加 auth_failed（前端静默重试一次语义）；/api/attach 端点契约小段（POST-only 405/空 body 413 上限/200 {ticket} no-store/401/403/429+Retry-After）。
    2. 新建 `.planning/phases/03-auth/03-UAT.md`（结构参照 02-UAT.md）：自动化层说明（phase03.mjs 六场景覆盖表）+ 人工清单——(a) 浏览器打开 https/http 页面弹 Basic 原生对话框，输入一次后页面正常进终端（DevTools 观察 POST /api/attach 200 且后续同源 fetch 自动带凭据，A2 验证点）；(b) 页面放置 >60s 后操作触发重连：静默重取 ticket 成功无面板（D-10 前端半侧）；(c) 错凭据弹窗连续失败后观察 429 面板（节流用户感知面）；(d) testssl.sh 两条命令（协议/cipher/头快速组 + -U 全量可选）与「无弱项」预期；(e) HSTS 粘性提示（测试完换 HTTP 部署需清浏览器 HSTS 缓存或换端口）。
    3. 全量收口六段式（全程 ~/open_src/wesh）：(1) "$(go env GOROOT)/bin/gofmt" -l . 输出为空（/usr/bin/gofmt 陈旧教训）；go vet ./... 退出 0。(2) time go test -race -count=1 ./... 退出 0——确认 Ticket/Auth/Throttle/Origin/Redact/TLS/StartupMatrix 测试组在列。(3) pnpm -C web install --frozen-lockfile && pnpm -C web build 退出 0；test -f web/dist/index.html.gz。(4) 裸 clone：git archive --format=tar HEAD | (mkdir -p /tmp/wesh-clean && tar -x -C /tmp/wesh-clean) 后 cd /tmp/wesh-clean && go build ./... && go test ./... -count=1 退出 0；清理。(5) 启动冒烟三形态：go build -o /tmp/wesh-bin ./cmd/wesh；a) /tmp/wesh-bin -- /bin/cat 无 --bind → 非零退出 + stderr 含 refusing 文案（D-03 断言）；b) /tmp/wesh-bin --bind 127.0.0.1 --port 0 -- /bin/cat & → 启动单行 listening on http:// + curl / 200 + 无子协议 /ws 400 → kill 进程组；c) openssl 自签夹具 + --credential + --tls-cert/--tls-key → listening on https:// + curl -k / 401（Basic challenge 冒烟）→ kill；pgrep -f wesh-bin 确认无残留。(6) 全量验证结果逐条命令 + 退出码写入 SUMMARY。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go vet ./... && go test -race -count=1 ./... && pnpm -C web build && test -z "$("$(go env GOROOT)/bin/gofmt" -l .)" && grep -q -- '--credential' README.md && grep -q -- '--no-auth' README.md && grep -q 'auth_failed' README.md && grep -q 'testssl' .planning/phases/03-auth/03-UAT.md</automated>
    <human-check>
      浏览器与 TLS 手动确认清单（end-of-phase 汇总，对应 ROADMAP 三条准则；详细步骤见 03-UAT.md）：
      1. 凭据弹窗与缓存（准则 1 + A2 验证点）：wesh --bind 127.0.0.1 --credential user:pass -- bash 后浏览器打开——原生 Basic 弹窗输入一次即进终端；DevTools Network 可见 POST /api/attach 200（无新登录 UI）；刷新页面不再弹窗（凭据缓存生效）。
      2. ticket 过期静默重试（准则 1 前端半侧）：页面放置 >60s 后直接操作/刷新——无错误面板直进终端（auth_failed → 静默重取重试一次生效）。
      3. 爆破节流感知（准则 2）：弹窗连续输错凭据 3+ 次后页面落 Too many attempts 面板（429 语义）；stderr 事件行无凭据字样（红线人工复核）。
      4. Origin 拒绝（准则 3）：curl 带 -H "Origin: https://evil.example" 升级请求 → 403（或见 phase03.mjs 场景 3 输出）。
      5. TLS 与安全头（准则 3）：mkcert/自签起 TLS 实例，浏览器 https 访问正常；testssl.sh docker 快速组（--protocols --std --server-defaults --header）无弱项；明文 HTTP 实例响应无 HSTS（Pitfall 7 人工复核）。
    </human-check>
  </verify>
  <acceptance_criteria>
    - automated 命令链全部退出 0；裸 clone 目录 go build 与 go test 退出 0
    - 冒烟三形态断言齐全：默认拒绝文案/loopback 放行 200/TLS 401 challenge；无 wesh 进程残留
    - README 含六 flag、行为变更醒目段、/api/attach 契约、testssl.sh 命令与 HSTS 粘性提示；无 InsecureSkipVerify 类教程
    - 03-UAT.md 五项人工清单文档化；SUMMARY 记录全部验证命令与退出码；human-check 五项由用户在 end-of-phase 验证确认
  </acceptance_criteria>
  <done>Phase 3 全量验证绿、文档与 wire 一致、行为变更对用户透明、人工 UAT 清单就绪待确认。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档 → 部署者 | README/03-UAT.md 是部署安全认知的唯一来源：逃生门语义、HSTS 粘性、凭据 env 推荐形态属补偿控制 |
| UAT 脚本 → 被测二进制 | UAT 输出本身可能成为凭据/ticket 泄漏面（CI 日志/终端回滚缓冲） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-27 | Information Disclosure | 部署者按过时文档裸奔公网（Phase 1/2 用法惯性） | high | mitigate | D-03/D-05 行为变更首屏 + 用法节双重明示；冒烟场景 6 拒绝文案断言；UAT 脚本凭据/ticket 不入输出（prohibition） |
| T-03-28 | Tampering | 文档与 wire 漂移（README 写的契约与实现不符） | medium | mitigate | flag/协议节直译 main.go 与 proto.go 真源；grep 断言关键串（--credential/--no-auth/auth_failed） |
| T-03-29 | Information Disclosure | 自签证书教程把用户引向关闭证书校验 | medium | mitigate | prohibition 锁定 mkcert/CA 方向；testssl.sh 命令含 --header 组验证安全头（正向引导） |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | 零新增依赖（RESEARCH §Package Legitimacy Audit） |
</threat_model>

<verification>
- Task 1：phase03.mjs 与 phase02.mjs 双双全绿
- Task 2：automated 命令链退出 0 + 裸 clone 绿 + 冒烟三形态断言 + human-check 五项 end-of-phase 确认
- VALIDATION.md Sampling Rate 的 phase gate 命令（全量 + UAT）执行记录进 SUMMARY
</verification>

<success_criteria>
- ROADMAP Phase 3 三条成功准则全部有证据：准则 1（ticket 全流程 + 重放拒绝——Go 集成 + UAT 场景 1）、准则 2（爆破节流 429 + subtle 比较 + 日志红线——03-01/03-02/03-03 测试 + UAT 场景 2）、准则 3（Origin 403 + TLS 1.2+/1.3 + 安全头 + testssl.sh 路径——03-02 矩阵 + UAT 场景 3/5 + 人工清单）
- SEC-01..SEC-05 五需求在 REQUIREMENTS.md 可追溯至具体测试/UAT 场景
- 仓库处于可推送状态（gofmt/vet/-race/前端构建/双 UAT 全绿）
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `web/uat/phase03.mjs`：`scenarioAuthFlow`/`scenarioThrottle`/`scenarioOrigin`/`scenarioNoAuth`/`scenarioTLS`/`scenarioStartupMatrix`（新建）
- `web/uat/phase02.mjs`：startWesh 加 `--bind 127.0.0.1` 适配（修改；无新符号）
- `README.md`（更新；无新代码符号）
- `.planning/phases/03-auth/03-UAT.md`（新建文档）

<output>
Create `.planning/phases/03-auth/03-06-SUMMARY.md` when done
</output>
