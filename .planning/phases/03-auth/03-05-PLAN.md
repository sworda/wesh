---
phase: 03-auth
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-03"]
files_modified:
  - web/src/main.ts
  - web/dist/index.html
  - web/dist/index.html.gz
autonomous: true
requirements:
  - SEC-02

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "连接流程改造为 connect() 函数：先 fetch POST /api/attach（同源 fetch 自动携带浏览器缓存的 Basic 凭据，D-02 成立前提），按响应分派——200 取 ticket / 404 无认证模式直连（ticket=undefined）/ 401 与 429 各落 showStatus 人话面板 / 网络异常落 unreachable 面板"
    - "Hello JSON 携带 ticket 字段（undefined 时 JSON 自动省略该键——无认证模式与 03-01 契约兼容）；WS URL scheme 按 location.protocol 选 ws/wss（https 页面下 wss，TLS 部署可连）"
    - "auth_failed 静默重试仅一次：onclose 见 lastError?.code === 'auth_failed' 且 retriedAuth 未置位 → 置位后 void connect() 重取 ticket 重试（ticket 60s 过期是正常场景，D-10）；重试再失败落既有 1008 分支展示，非无限循环"
    - "每次 connect() 尝试重置 per-connection 状态（opened/helloSent/lastError）；ws 提升为模块级 let，onData/sendResize 等既有接线引用当前连接；showStatus 幂等（onerror/onclose 双触发不重复渲染）"
    - "帧常量注释区与 proto.go 对齐更新（D-16 互相指路纪律）：Hello 载荷加 ticket 可选字段说明、auth_failed 入 Error code 注释"
    - "401 面板文案引导重新加载页面（重新导航触发浏览器原生 Basic 弹窗——fetch 401 不弹窗是平台行为，Pitfall 6）；429 面板提示稍候重试"
    - "pnpm -C web build 重建后 web/dist/index.html 与 .gz 为新产物（仓库提交真实构建产物纪律——改前端源码必须重建，否则二进制内嵌旧产物）"
  prohibitions:
    - "禁止自建登录表单/凭据输入框等任何新 UI 组件（D-02：浏览器原生 Basic 弹窗是唯一直 creds 入口，showStatus 三态面板复用现状）"
    - "auth_failed 重试禁止无限循环（retriedAuth 单次置位；无限重试会把 60s TTL 正常过期打成重试风暴放大节流计数）"
    - "ticket 禁止写入 URL query/localStorage/console.log（泄漏面；仅存于闭包变量与 Hello 载荷）"
  artifacts:
    - path: "web/src/main.ts"
      provides: "connect() 连接流程（fetch ticket → WS → Hello{ticket} → auth_failed 重试一次）"
      contains: "retriedAuth"
    - path: "web/dist/index.html"
      provides: "重建后的单文件产物（含 gz 旁路）"
      contains: ""
  key_links:
    - from: "web/src/main.ts"
      to: "internal/server/server.go"
      via: "fetch POST /api/attach 消费 03-03 端点（200 ticket / 404 探测 / 401·429 分派）"
      pattern: "/api/attach"
    - from: "web/src/main.ts"
      to: "internal/proto/proto.go"
      via: "HelloPayload.Ticket 与 ErrAuthFailed 的前端消费侧（D-16 手工对齐）"
      pattern: "auth_failed"
---

<objective>
前端连接流程认证化改造：connect() 函数包裹现状直连逻辑，先 fetch 取 ticket 再建 WS，Hello 携带 ticket，auth_failed 按 D-10 静默重试一次；随后重建并提交 dist 产物。

Purpose: D-02 的「前端零新 UI 组件，仅加 fetch ticket → Hello 携带逻辑」与 D-10 的前端半侧在此落地；ROADMAP 准则 1 的浏览器可用性闭环。
Output: main.ts 改造 + dist 重建提交 + pnpm 构建绿。

规格探针标记（spec-less probe fallback）：SEC-02 探针报为 unclassified——按 §C 保持 unresolved，不自动核销；前端行为边界按 D-02/D-10 与 RESEARCH Code Examples 定稿落入 truths，三条 must-NOT（不自建登录 UI/不无限重试/ticket 不落存储）经 §B 回忆落入 prohibitions（descriptor-less，下游 dispose flagged-unverified），无静默丢弃。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth/03-CONTEXT.md
@.planning/phases/03-auth/03-RESEARCH.md
@.planning/phases/03-auth/03-PATTERNS.md
@web/src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: main.ts——connect() 认证感知连接流程 + auth_failed 重试一次</name>
  <files>web/src/main.ts</files>
  <read_first>
    - web/src/main.ts（被改文件现状全文：帧常量区 6-15 行、直连 73-74 行、onmessage 87-117、onData 122-126、sendResize 131-138、onopen 150-161、showStatus 165-181、onerror 183-192、onclose 按码分派 197-250）
    - .planning/phases/03-auth/03-RESEARCH.md（Code Examples §前端连接流程改造 行 644-719——connect() 定稿形态逐段对应；Pitfall 6 行 621-625 fetch 401 不弹窗）
    - .planning/phases/03-auth/03-PATTERNS.md（§main.ts 改 283-309 行——改造点与纪律沿用）
    - .planning/phases/03-auth/03-CONTEXT.md（D-02 零新 UI、D-10 重试一次、Claude's Discretion 404 探测顺序）
  </read_first>
  <action>
    改造 `web/src/main.ts`（RESEARCH Code Examples 定稿为蓝本，逐段对应现状接线）：
    1. 模块级状态调整：`const ws` 改为 `let ws: WebSocket`（connect() 内赋值）；新增 `let retriedAuth = false`（auth_failed 静默重试仅一次的门闩，注释引 D-10）；opened/helloSent/lastError 维持模块级。
    2. 新增 `async function connect(): Promise<void>` 包裹连接建立：
       - fetch('/api/attach', { method: 'POST' })——credentials 默认值同源即携带（HTTP auth 缓存条目随同源 fetch 自动附带，A2 假设；注释标明 UAT 验证点）。resp.ok → ticket = (await resp.json()).ticket；resp.status === 404 → ticket = undefined（无认证模式探测，注释引 RESEARCH Pattern 1 决策）；401 → showStatus('Authentication failed', 引导重新加载页面的文案——重新导航触发浏览器原生弹窗，Pitfall 6) 后 return；429 → showStatus('Too many attempts', 稍候重试文案) 后 return；其余非 ok 状态按 401 同款面板处理（通用认证失败，不细分——无 oracle 纪律延伸到前端文案）；catch → showStatus('Unable to connect', 既有 unreachable 文案) 后 return。
       - new WebSocket((location.protocol === 'https:' ? 'wss://' : 'ws://') + location.host + '/ws', [SUBPROTOCOL])（替换现状硬编码 ws://——TLS 部署必需）；binaryType='arraybuffer'。
       - 每次尝试开头重置 opened=false、helloSent=false、lastError=null（重试时不携带上次连接残留状态）。
       - onopen 内 Hello JSON 加 ticket 字段（`{ version: SUBPROTOCOL, cols: term.cols, rows: term.rows, ticket }`——ticket 为 undefined 时 JSON.stringify 自动省略该键，无认证模式与服务端核销跳过分支兼容，03-01 契约）；其余 fit.fit()/helloSent/term.focus() 现状不变。
       - onmessage/onerror 既有逻辑原样移入 connect 内（行为零改动）。
       - onclose 在 switch 之前插入 auth_failed 守卫：`lastError?.code === 'auth_failed' && !retriedAuth` → retriedAuth=true、lastError=null、void connect()、return（仅一次——重试再失败时 retriedAuth 已置位，落既有 switch 的 1008 分支展示 lastError.message）；既有按码分派（1000/1008/1009/1011/1013/default）逐字保留。
    3. 文件尾部以 `void connect();` 启动（替换现状顶层直连三行）。
    4. 帧常量注释区（6-15 行）更新：Hello 载荷说明加可选 ticket 字段（Phase 3 认证核销）；Error code 注释加 auth_failed（与 proto.go 互相指路，D-16 纪律）。
    5. 纪律自查：无新 UI 组件/无表单；ticket 只存闭包变量（不入 URL/localStorage/console）；showStatus 复用现状三态面板。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && pnpm -C web exec tsc --noEmit && pnpm -C web build</automated>
  </verify>
  <acceptance_criteria>
    - tsc --noEmit 与 vite 构建均退出 0（TS 严格性即前端静态验证）
    - main.ts 含 connect() 函数与 retriedAuth 门闩；Hello JSON 含 ticket 键；WS URL 按 location.protocol 分支
    - fetch 分派四路齐全（200/404/401·429/catch）；onclose 的 auth_failed 守卫在 switch 之前且仅重试一次
    - 无新增 DOM 元素/表单组件；ticket 未出现于 URL/localStorage/console 调用
  </acceptance_criteria>
  <done>前端认证感知连接流程落地：取 ticket → 携带核销 → 过期静默重试一次；无认证模式 404 探测直连零摩擦。</done>
</task>

<task type="auto">
  <name>Task 2: dist 重建与产物提交</name>
  <precondition>Task 1 已合入（dist 必须由内嵌新连接流程的源码构建——否则产物内嵌旧直连逻辑）。</precondition>
  <files>web/dist/index.html, web/dist/index.html.gz</files>
  <read_first>
    - README.md（构建节：「修改 web/ 前端源码后必须先重新 pnpm -C web build 再 go build」纪律与产物提交约定）
    - web/vite.config.ts（singlefile 构建形态确认）
  </read_first>
  <action>
    1. `pnpm -C web build`（含 tsc && vite build && gzip -k9 全链——P1 钉死形态）全量重建。
    2. 校验产物：web/dist/index.html 与 web/dist/index.html.gz 时间戳新于 main.ts 改动；grep 产物含 /api/attach 与 wss 分支字样（singlefile 内联后源码串可检索——新连接流程确实入包的证据）。
    3. `go build ./...` 确认 embed 链编译通过；git add 两个产物文件随本 plan 提交（仓库提交真实构建产物纪律，README 构建节）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && pnpm -C web build && test -f web/dist/index.html.gz && grep -q '/api/attach' web/dist/index.html && grep -q 'wss://' web/dist/index.html && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - 构建退出 0；dist/index.html 含 /api/attach 与 wss:// 检索串（新流程入包证据）
    - web/dist/index.html 与 .gz 双双更新且随提交入库；go build ./... 退出 0
  </acceptance_criteria>
  <done>dist 产物与新连接流程同步入库，裸 clone 构建嵌的是认证感知前端。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 服务端响应 → 前端分派 | /api/attach 响应与 WS Error 帧驱动前端状态机；恶意/异常响应不得使前端进入凭据泄露或重试风暴形态 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-24 | Information Disclosure | ticket 经前端泄漏（URL 历史/持久存储/控制台） | high | mitigate | prohibition 锁定：仅存闭包变量与 Hello 载荷；grep 级验收（无 localStorage/URL 写入） |
| T-03-25 | DoS | auth_failed 无限重试放大服务端节流计数 | medium | mitigate | retriedAuth 单次门闩（D-10）；重试失败落 1008 展示分支 |
| T-03-26 | Spoofing | 前端把 401 之外的异常响应当成功处理（假 ticket 入 Hello） | low | mitigate | 仅 resp.ok 取 ticket；404 显式直连；其余统一失败面板（无 oracle 延伸） |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | 零新增依赖（fetch/WebSocket 均浏览器内建，web/package.json 不动） |
</threat_model>

<verification>
- `pnpm -C web exec tsc --noEmit && pnpm -C web build` 退出 0
- dist 产物含新流程检索串；go build ./... 退出 0
- 浏览器人工确认（弹窗凭据缓存/fetch 携带/过期重试）在 03-06 的 human-check 清单汇总执行
</verification>

<success_criteria>
- D-02 前端半侧落地：零新 UI、浏览器原生弹窗 + 同源 fetch 携带凭据
- D-10 前端半侧落地：auth_failed 静默重试一次
- wss scheme 支持使 03-04 的 TLS 伺服可被浏览器真实使用
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `web/src/main.ts`：`connect()` 函数、`retriedAuth` 状态、Hello 载荷 `ticket` 字段、ws/wss scheme 分支（改造；无新导出符号——单文件应用）
- `web/dist/index.html` / `web/dist/index.html.gz`（重建产物）

<output>
Create `.planning/phases/03-auth/03-05-SUMMARY.md` when done
</output>
