---
phase: 01-pty
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - README.md
autonomous: true
requirements:
  - CORE-01
  - FE-01
  - FE-03

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "README 首屏含醒目警示：Phase 1 无认证，仅在可信网络使用（D-05 accepted 风险的补偿控制，CONTEXT specifics 强制）"
    - "README 明示单次语义：Phase 1 任意终结路径（子进程退出 / WS 断开）服务端整体退出；断线重连在 Phase 6——避免用户误以为 bug（CONTEXT specifics）"
    - "README 记录构建顺序硬依赖：pnpm -C web build 先于 go build（D-18），并列明 SEC-06 env 白名单变量集合"
    - "干净检出（裸 clone 模拟）下 go build ./... 与 go test ./... 成功——embed 占位链有效（D-18/Pitfall 6）"
    - "全量验证：go test -race -count=1 ./... 与 pnpm -C web build 退出 0"
    - statement: "WebGL 渲染器加载失败或 GPU 上下文丢失时自动回落 DOM 渲染器，终端仍渲染且可交互（DevTools 禁 WebGL 手动验证）"
      verification: backstop
  artifacts:
    - path: "README.md"
      provides: "用法 / 构建顺序 / 无认证警示 / 单次语义 / env 白名单说明"
      contains: "Phase 1 无认证"
  key_links:
    - from: "README.md"
      to: ".planning/phases/01-pty/01-CONTEXT.md"
      via: "警示语与单次语义说明直译自 CONTEXT specifics（行 99-105）"
      pattern: "无认证"
---

<objective>
Phase 1 收口：README（含 CONTEXT 强制的安全警示与单次语义说明）+ 全量验证（-race 全测试、前端构建、裸 clone embed 链）+ 浏览器三项手动确认（汇总 FE-01/FE-03 与整体可用性）。

Purpose: 把"文档补偿控制"（D-05 绑定 0.0.0.0 的 accepted 风险）与全绿证据一并落地，作为 /gsd:verify-work 前的最后一道闸。
Output: README.md + 全量验证记录 + 手动 checklist 结果。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pty/01-CONTEXT.md
@.planning/phases/01-pty/01-RESEARCH.md
@.planning/phases/01-pty/01-VALIDATION.md
@.planning/phases/01-pty/01-UI-SPEC.md
@.planning/phases/01-pty/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md（用法 + 构建 + 安全警示 + 单次语义）</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/01-pty/01-CONTEXT.md（specifics 行 99-105：警示与单次语义为强制内容；D-04/D-06/D-07；deferred 行 108-117）
    - .planning/phases/01-pty/01-RESEARCH.md（§安全域 V14 行 771：白名单集合；Pattern 4 构建顺序 行 426）
    - .planning/PROJECT.md（项目定位段）
    - .planning/phases/01-pty/01-PATTERNS.md（§No Analog Found README 行）
  </read_first>
  <action>
    在仓库根写 `README.md`（简体中文为主、命令与标识符英文），结构：
    1. 项目一句话：`wesh` — 通过 Web 分享终端的命令行工具（`wesh [flags] -- <cmd> [args...]`）。
    2. **首屏醒目警示段（位置仅次于标题，用引用块）："Phase 1 无认证：任何能访问监听地址的人都获得一个以你身份运行的 shell。仅在可信网络使用。"**（CONTEXT specifics 行 103 强制——D-05 默认 bind 0.0.0.0 的补偿控制；Phase 3 认证/TLS 到位后才是公网安全）。
    3. 单次语义说明："Phase 1 为单次语义：子进程退出或 WS 断开，服务端即整体退出（退出码 = 子进程退出码）。断线重连与 --once 等完整生命周期在后续阶段提供——WS 断开即退出是当前阶段的预期行为，不是 bug。"（CONTEXT specifics 行 104 直译，D-10/D-11）
    4. 用法：四个 flag（--port 默认 7681、0=随机并打印实际端口；--bind 默认 0.0.0.0；--help；--version）+ `--` 后原样传递任意命令及参数；启动仅打印单行 `listening on http://host:port`。
    5. 构建：`pnpm -C web install && pnpm -C web build && go build -o wesh ./cmd/wesh`——构建顺序硬依赖（D-18）：前端构建先于 go build；裸 clone 仅有占位 HTML，可直接 `go test ./...` 但运行需先构建前端。
    6. 安全说明：SEC-06 env 白名单——子进程只见 TERM=xterm-256color、COLORTERM=truecolor（固定）与 PATH/HOME/USER/LOGNAME/SHELL（继承）、LANG/LC_*（前缀继承），服务端其余环境变量一律不透传；协议帧形状一段（1 字节类型 + 载荷；'0' INPUT/OUTPUT、'1' RESIZE+JSON；未知类型 1002）。
    7. 测试：`go test -race -count=1 ./...`；CI 双平台（含 macOS 收割验证）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && test -f README.md && grep -q 'Phase 1 无认证' README.md && grep -q 'pnpm -C web build' README.md && grep -q 'TERM=xterm-256color' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md 含"Phase 1 无认证，仅在可信网络使用"警示且位于标题之后的首个段落区块
    - 含单次语义句（WS 断开即退出为预期行为；断线重连属后续阶段）
    - 含构建顺序（pnpm -C web build 先于 go build）与四个 flag 及默认值（7681 / 0.0.0.0）
    - 含 SEC-06 白名单变量集合（TERM/COLORTERM 固定 + PATH/HOME/USER/LOGNAME/SHELL/LANG/LC_*）
  </acceptance_criteria>
  <done>README 落地：安全警示与单次语义两项强制内容就位，构建/测试/协议说明完整。</done>
</task>

<task type="auto">
  <name>Task 2: 全量收口验证（-race 全量 + 前端构建 + 裸 clone embed 链）</name>
  <precondition>plan 01-01/01-02/01-03/01-04 已全部完成并合入（depends_on 保证）；CI 远端首次运行结果不在本任务断言范围（推送由用户决定时机）。</precondition>
  <files>README.md</files>
  <read_first>
    - .planning/phases/01-pty/01-VALIDATION.md（Sampling Rate 行 30-35；Wave 0 清单 行 57-64 复核无遗漏）
    - .planning/phases/01-pty/01-RESEARCH.md（Pitfall 5/6 行 540-551）
    - .planning/ROADMAP.md（Phase 1 成功准则三条）
  </read_first>
  <action>
    按 VALIDATION §Sampling Rate 的 phase gate 执行全量收口（全程在 ~/open_src/wesh）：
    1. `gofmt -l .` 输出为空（如有输出则 gofmt -w 修正后重跑）；`go vet ./...` 退出 0。
    2. `go test -race -count=1 ./...` 退出 0（不设 CGO_ENABLED=0，Pitfall 5）。
    3. `pnpm -C web install --frozen-lockfile && pnpm -C web build` 退出 0；`test -f web/dist/index.html.gz`。
    4. 裸 clone 模拟（D-18/Pitfall 6 的 embed 占位链证明）：`git archive --format=tar HEAD | (mkdir -p /tmp/wesh-clean && tar -x -C /tmp/wesh-clean)` 后 `cd /tmp/wesh-clean && go build ./... && go test ./... -count=1` 退出 0——干净检出仅凭 web/dist/index.html 占位即可编译测试；随后清理 /tmp/wesh-clean。
    5. 运行冒烟：`go build -o /tmp/wesh-bin ./cmd/wesh && /tmp/wesh-bin -- /bin/cat &`（记录 pid），断言启动输出恰一行且匹配 `listening on http://0.0.0.0:7681`（D-07）；`curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:7681/` 返回 200；`curl -s -H 'Accept-Encoding: gzip' -D - -o /dev/null http://127.0.0.1:7681/ | grep -i 'content-encoding: gzip'` 命中（Pattern 4 双路径）；`curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:7681/ws`（非升级请求）非 200；结束后 kill 该进程组并确认无残留（pgrep -f wesh-bin 为空）。
    6. 若第 3 步构建产物使 `git status` 显示 web/dist/index.html 被修改（真实产物覆盖占位），保留真实产物提交——README 已说明构建顺序；占位语义由第 4 步独立证明。
    7. 把全量验证结果写入 SUMMARY（逐条命令 + 退出码）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go vet ./... && go test -race -count=1 ./... && pnpm -C web build && test -z "$(gofmt -l .)"</automated>
    <human-check>
      浏览器手动三项（VALIDATION 1-01-08/09 + 成功准则整体确认，end-of-phase 汇总）：
      1. 交互可用性：`wesh -- bash` 后浏览器开 http://localhost:7681，键入命令有回显、输出实时；在 web shell 里执行 `env`，确认只见白名单变量（无服务端机密）；另开第二个浏览器标签访问同地址应见 "Unable to connect"（409 场景）。
      2. FE-03/CORE-02：`wesh -- vim` 打开后拖动浏览器窗口，远端 `stty size` 跟随变化且 vim 无闪屏错乱。
      3. FE-01：DevTools → Rendering 或 chrome://flags 禁用 WebGL 后刷新页面，终端仍渲染且可交互（DOM 回落）；重新启用 WebGL 刷新，渲染正常。
      4. 终结语义：在 web shell 里 `exit`，页面显示 "Session ended"；shell 侧 wesh 进程已退出且退出码为子进程退出码（`echo $?`）。
    </human-check>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./...` 与 `pnpm -C web build` 退出 0；`gofmt -l .` 无输出
    - 裸 clone 模拟目录中 `go build ./...` 与 `go test ./... -count=1` 退出 0
    - 冒烟：启动输出恰一行且匹配 `listening on http://0.0.0.0:7681`；/ 返回 200；gzip 协商命中 Content-Encoding: gzip；收尾后 pgrep 无 wesh 残留进程
    - SUMMARY 记录全部验证命令与退出码；human-check 四项结果由用户在 end-of-phase 验证中确认
  </acceptance_criteria>
  <done>Phase 1 全量验证绿、文档齐备、embed 占位链与 gzip 双路径经干净检出与冒烟双重证明。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档 → 部署者 | README 是 D-05 accepted 风险（无认证 + 0.0.0.0）的补偿控制面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | README.md（部署者误部署到不可信网络） | high | mitigate | 首屏醒目警示"Phase 1 无认证，仅在可信网络使用"（CONTEXT specifics 强制）+ 单次语义说明——T-01-09 的文档补偿控制落地 |
| T-05-02 | Information Disclosure | README.md（env 白名单可预期性） | low | mitigate | 列明 SEC-06 白名单变量集合，运维可据此判断哪些变量不会被带入 web shell |
</threat_model>

<verification>
- Task 2 的全部 automated 命令退出 0
- human-check 四项在 end-of-phase 人工验证中确认
- VALIDATION.md 九项（1-01-01..09）全部 green 或手动确认
</verification>

<success_criteria>
- ROADMAP Phase 1 三条成功准则全部有证据：交互终端（e2e + 手动）、resize 同步（TestResize + 手动 vim）、收割无僵尸 + env 零泄露（TestReap + TestEnvWhitelist + 手动 `env`）
- CI 双平台配置就位；README 两项强制内容就位
- 仓库处于可推送状态（gofmt/vet/test 全绿）
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `README.md`（新文件；无新代码符号）

<output>
Create `.planning/phases/01-pty/01-05-SUMMARY.md` when done
</output>
