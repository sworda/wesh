---
phase: 01-pty
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internal/server/e2e_test.go
  - cmd/wesh/main_test.go
autonomous: true
requirements:
  - CORE-01

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "第二个 WS 连接 attach 请求在 Accept 之前收到 HTTP 409（D-09，Phase 1 单客户端临时语义）"
    - "子进程以退出码 42 退出时，已 attach 的 WS 客户端先收到 1000 正常关闭帧，随后 exitf 被以 42 调用（D-10 退出码传递）"
    - "未知类型帧（如 '9'）导致 WS 以 1002 关闭（D-16 协议演化纪律；1006 永不写入）"
    - "WS 客户端断开后子进程进程组收到 SIGHUP 且 exitf 被调用（D-11）"
    - "无 `-- <cmd>` 时 run 返回非零且 stderr 含 `usage: wesh [flags] -- <cmd> [args...]`（D-03）；`--` 后以 `-` 开头的参数原样传递不被 flag 包吞掉（D-02）"
  artifacts:
    - path: "internal/server/e2e_test.go"
      provides: "生命周期 e2e 四测（在 plan 01-01 的 TestEchoPTY 基础上增量）"
      contains: "TestSecondClient409 / TestExitCodePropagation / TestUnknownFrame1002 / TestClientDisconnectSIGHUP"
    - path: "cmd/wesh/main_test.go"
      provides: "CLI 解析与无命令报错测试"
      contains: "TestParseArgs / TestNoCommandError"
  key_links:
    - from: "internal/server/e2e_test.go"
      to: "internal/server/server.go"
      via: "注入捕获桩 exitf 断言 D-09/D-10/D-11 生命周期语义"
      pattern: "New\\(|exitf"
    - from: "cmd/wesh/main_test.go"
      to: "cmd/wesh/main.go"
      via: "直接调用 parseArgs / run 断言 CLI 契约"
      pattern: "parseArgs\\(|run\\("
---

<objective>
为 plan 01-01 落地的生命周期与 CLI 决策（D-03/D-09/D-10/D-11 与关闭码纪律）补齐 e2e 与单元测试——这些是 Phase 1"单次语义"的全部边界行为，也是 Phase 6 完整生命周期语义重写前的回归基线。

Purpose: 单次语义是 Phase 1 的显式产品行为（不是 bug），四条路径（第二连接 409 / 退出码传递 / 未知帧 1002 / 断开 SIGHUP）必须自动化锁定。
Output: internal/server/e2e_test.go 增量四个测试 + cmd/wesh/main_test.go，`go test -race` 全绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pty/01-CONTEXT.md
@.planning/phases/01-pty/01-RESEARCH.md
@.planning/phases/01-pty/01-PATTERNS.md
@.planning/phases/01-pty/01-VALIDATION.md
@.planning/phases/01-pty/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: server 生命周期 e2e 四测（409 / 退出码 / 1002 / SIGHUP）</name>
  <files>internal/server/e2e_test.go</files>
  <read_first>
    - internal/server/e2e_test.go（plan 01-01 产物——在既有 TestEchoPTY 基础上增量，复用其 sess/Server/exitf 桩构造模式）
    - internal/server/server.go（被测对象当前实现：409 判定、1000 关闭、1002 关闭、SIGHUP 进程组、exitf 单次收口）
    - .planning/phases/01-pty/01-CONTEXT.md（D-09、D-10、D-11、D-16）
    - .planning/phases/01-pty/01-RESEARCH.md（Pattern 3 行 328-392；关闭码纪律 行 600-603）
    - .planning/phases/01-pty/01-PATTERNS.md（§No Analog Found 生命周期行）
  </read_first>
  <action>
    在 `internal/server/e2e_test.go` 增量四个测试（复用 plan 01-01 的构造：sess + New(sess, exitf 捕获桩) + 127.0.0.1:0 监听 + coder/websocket Dial；统一 5-10s 超时护栏；若现有构造不利于复用，抽一个 test helper 函数，保持单文件内聚）：
    1. `TestSecondClient409`（D-09）：sess 用长驻命令（如 /bin/cat）；第一个 Dial 成功建立 WS；随后对同一 /ws 发第二次握手（用 websocket.Dial 并断言返回的 *http.Response StatusCode == 409，或直接 http.Get/httptest 客户端断言 409）；断言第一条 WS 仍然存活可 echo；清理：关闭第一条连接，等待 exitf 触发。
    2. `TestExitCodePropagation`（D-10）：sess 用会自杀的命令——以 Go 惯例 TestHelperProcess 模式（os.Executable() + env 守卫 + args 标记）让子进程 `os.Exit(42)`，避免经 shell；Dial attach 后等待：先收到 WS close 帧且 CloseError.Code == websocket.StatusNormalClosure（1000），随后断言 exitf 捕获码 == 42。
    3. `TestUnknownFrame1002`（D-16 关闭码纪律）：sess 用 /bin/cat；Dial 后发一个首字节为 '9'（0x39）的 binary 帧；断言连接被关闭且 CloseError.Code == websocket.StatusProtocolError（1002）；额外断言整个交互中从未出现 1006（CloseError.Code != 1006——库层不会主动发 1006，本断言防回归 PITFALLS C9）。
    4. `TestClientDisconnectSIGHUP`（D-11）：sess 用可捕获 SIGHUP 的 TestHelperProcess（helper 安装 signal.Notify(SIGHUP) 处理器，收到后向 stdout 打一行标记再退出）；Dial 建立后主动 `c.CloseNow()` 断开；断言 helper 的 SIGHUP 标记经 drain 输出可见或 helper 进程消失（/proc/\<pid\> 不存在），且 exitf 被以 0 调用。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test ./internal/server -count=1 -v -run 'TestSecondClient409|TestExitCodePropagation|TestUnknownFrame1002|TestClientDisconnectSIGHUP' && go test -race -count=1 ./internal/server</automated>
  </verify>
  <acceptance_criteria>
    - 四个新测试全部通过且 `go test -race -count=1 ./internal/server` 退出 0
    - TestSecondClient409 断言第二次握手 HTTP 状态码恰为 409 且第一连接不受影响（D-09）
    - TestExitCodePropagation 断言先收 1000 关闭帧、exitf 捕获码恰为 42（D-10）
    - TestUnknownFrame1002 断言关闭码恰为 1002 且全程无 1006
    - TestClientDisconnectSIGHUP 断言子进程收到 SIGHUP 的证据（标记输出或进程消失）且 exitf(0) 被调用（D-11）
    - 既有 TestEchoPTY 不回归（同文件内全量绿）
  </acceptance_criteria>
  <done>D-09/D-10/D-11 与关闭码纪律四条生命周期边界全部自动化锁定，server 包 -race 全绿。</done>
</task>

<task type="auto">
  <name>Task 2: CLI 层测试——parseArgs 契约与无命令报错</name>
  <files>cmd/wesh/main_test.go</files>
  <read_first>
    - cmd/wesh/main.go（plan 01-01 产物——被测对象：parseArgs / run / 默认值）
    - .planning/phases/01-pty/01-CONTEXT.md（D-02、D-03、D-04、D-05、D-06）
    - .planning/phases/01-pty/01-PATTERNS.md（§No Analog Found main.go 行）
  </read_first>
  <action>
    新建 `cmd/wesh/main_test.go`（package main 内测）：
    1. `TestParseArgs` 表驱动：默认调用 `parseArgs(["--", "bash"])` → bind=="0.0.0.0"、port==7681、argv==["bash"]（D-05/D-06）；`parseArgs(["--port", "0", "--bind", "127.0.0.1", "--", "ls", "-la"])` → port==0、bind=="127.0.0.1"、argv==["ls", "-la"]——`-la` 以 `-` 开头仍原样进入 argv 不被 flag 包吞掉（D-02）；`parseArgs(["--port", "7682", "--", "/bin/echo", "--version"])` → argv==["/bin/echo", "--version"]（子命令自己的 flag 不被 wesh 解析）。
    2. `TestNoCommandError`（D-03）：`run([])` 与 `run(["--port", "0"])`（无 `--`）均返回非零退出码；捕获 stderr 断言含 `usage: wesh [flags] -- <cmd> [args...]`；断言两条路径都不会启动监听（快速返回，无端口占用）。
    3. `TestVersionFlag`：`run(["--version"])` 返回 0 且 stdout 含 `wesh` 与版本字符串（版本变量来自 main 包内 version const/var，构建期不强制注入）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test ./cmd/wesh -count=1 -v && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/wesh -count=1` 退出 0
    - TestParseArgs 覆盖：默认值（0.0.0.0:7681）、`--` 后以 `-` 开头参数原样传递、子命令 flag 不被吞
    - TestNoCommandError 断言非零返回与 stderr usage 行（D-03）
    - 整仓 `go test ./... -count=1` 退出 0
  </acceptance_criteria>
  <done>CLI 契约（D-02~D-06）自动化锁定；`--` 分隔与参数透传语义有回归保护。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试客户端 → server | 本 plan 以编程方式驱动恶意/边界输入（第二连接、未知帧、异常断开）验证既有缓解 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | DoS | internal/server/server.go 409 路径 | medium | mitigate | TestSecondClient409 固化 D-09：第二连接在 Accept 之前以 HTTP 409 拒绝，不消耗 PTY/WS 资源、不干扰已 attach 会话 |
| T-03-02 | Tampering | 关闭码路径 | low | mitigate | TestUnknownFrame1002 固化未知帧 → 1002 且全程无 1006（T-01-08 的可执行回归，PITFALLS C9） |
| T-03-03 | DoS | 生命周期退出路径（孤儿进程/退出码丢失） | medium | mitigate | TestExitCodePropagation 与 TestClientDisconnectSIGHUP 固化 D-10/D-11：退出码传递、SIGHUP 进程组、exitf 单次收口 |
| T-03-04 | Tampering | CLI 参数解析（flag 吞参改变用户意图） | low | mitigate | TestParseArgs 固化 D-02：`--` 后参数（含以 `-` 开头者）原样进 exec 数组，wesh 自身 flag 与子命令 flag 不串扰 |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/server ./cmd/wesh` 全绿
- 整仓 `go test ./... -count=1` 绿（含 plan 01-01/01-02 既有测试不回归）
</verification>

<success_criteria>
- D-09/D-10/D-11 与未知帧 1002 四条生命周期边界有自动化证明
- CLI 契约（D-02~D-06）有表驱动测试锁定
- Phase 1 单次语义的全部边界行为进入回归网
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `internal/server/e2e_test.go`（增量）：`TestSecondClient409`、`TestExitCodePropagation`、`TestUnknownFrame1002`、`TestClientDisconnectSIGHUP`（+ 可选 test helper 与 `TestHelperProcess`）
- `cmd/wesh/main_test.go`：`TestParseArgs`、`TestNoCommandError`、`TestVersionFlag`

<output>
Create `.planning/phases/01-pty/01-03-SUMMARY.md` when done
</output>
