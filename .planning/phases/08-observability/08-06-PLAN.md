---
phase: 08-observability
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - web/uat/phase08-journal.mjs
autonomous: true
gap_closure: true
gap_ids: [G-08-2]
requirements: [OPS-08]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "README 两则 journald jq 示例在 systemd 默认配置（StandardOutput=journal）下复制粘贴即可运行：合流流上 jq 零 parse error、检出预期事件行（G-08-2 truth）"
    - "README「结构化日志」节如实陈述 journald 合流机理（stdout 横幅 + stderr JSON 事件），grep 防护带一句理由，文案风格与既有 README 一致（中文、精炼、带理由）"
    - "wesh 源码与启动输出零改动——横幅保持 stdout 人读文本（D-14）、运行期事件恒 stderr JSON（D-15）、警告行保持文本（D-16）"
    - "合流模拟夹具自证不空转：未防护管道在合流流上必 parse error（负对照），防护后两则示例管道与全流纯度断言全绿"
  artifacts:
    - path: README.md
      contains: "两则 journald 示例均含 grep 防护段（滤 { 起始行）+ 合流机理说明句"
    - path: web/uat/phase08-journal.mjs
      provides: "G-08-2 回归夹具：journal 合流模拟（stdout+stderr 按时序拼接）+ 四组断言（负对照/全流纯度/示例一/示例二）"
  key_links:
    - "README 示例的 grep+jq 段 ≡ phase08-journal.mjs 断言管道（逐字一致——示例与脚本同步改防漂移）"
    - "grep '^\{' 预滤 ≡ parseEvents 滤 '{' 起始行约定（08-01 Go/JS 双侧既定解析形态）"
---

<objective>
闭合 G-08-2（08-UAT test 2，major）：README「结构化日志」节两则 jq 检索示例（`journalctl -o cat | jq 'select(...)'`）在 systemd 默认配置下被启动横幅打断——stdout 人读横幅经 `StandardOutput=journal` 与 stderr JSON 事件在 journal 合流，jq 遇非 JSON 行停流，后续事件检索不到。

修复取 README 侧（方向 3 + 合流说明）：示例统一补 `grep '^\{'` 预滤 + 引言句说明合流机理。**wesh 代码零改动**——方向 1 与锁定决策冲突：横幅改道/JSON 化破坏 D-14（启动行人读文本 + 01-05 冒烟/UAT 启动行解析既有消费者零破坏）；TTY 自动检测被 D-15 明文拒绝（隐式行为与显式哲学相悖）；横幅含 token JSON 化违反 P5 D-03（token 永不入事件流）；且横幅改走 stderr 也无法修复——journalctl 读取的是 unit 全部输出的合流，fd 归属无关，人读行照样混入。方向 2（StandardOutput=null）静默丢 share link（operator 在 journal 中的关键输出），无更优配方形态。示例防护与自动化测试的 parseEvents 滤行约定（滤 `{` 起始行，08-01 Go/JS 双侧）同款——README 与测试套件对齐。

Purpose: 恢复「人读走 jq」承诺在 systemd 主流部署形态下的可复现性（README 示例复制粘贴即用）。
Output: README.md 结构化日志节更新 + web/uat/phase08-journal.mjs 合流模拟回归脚本（负对照自证夹具不空转，防回归）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-observability/08-UAT.md
# Gap G-08-2（## Gaps 节）——root_cause/missing 已定，勿重诊断；Tests 节 test 2 为本 gap 的 UAT 源
@.planning/phases/08-observability/08-CONTEXT.md
# D-14（启动行人读文本保持）/ D-15（恒 JSON 零开关、拒绝 TTY 检测）/ D-16（警告行保持文本）= wesh 侧修复的否决依据
# D-18（msg=event schema）/ D-20（client_id 从 1 起）/ D-21（detach reason 四值）/ D-23（auth_failed 无用户名）= 断言依据
@README.md
# 编辑目标：「结构化日志（JSON 审计事件）」节（约 line 433-468）——引言句 + 两则 journald 示例 + 「红线重申」段
@web/uat/phase08.mjs
# 骨架件复用源：WESH 定位 / startWesh spawn 与端口解析 / waitEvent / dialHello（WebSocket headers+protocols 双形态）/ check / assertOutputClean
</context>

<tasks>

<task type="auto">
  <name>Task 1: README 结构化日志节——journald 示例补 grep 防护 + 合流机理说明</name>
  <files>README.md</files>
  <action>
    - 改写该节 journald 引言句（现文「journald + jq 检索示例（systemd 部署下 stderr 自动进 journal）：」）为合流机理说明，一到两句、中文精炼带理由，要点：①systemd 默认 `StandardOutput=journal` 会把 stdout 启动横幅（人读文本——分流决策见该节末「红线重申」段）与 stderr JSON 事件合流入 journal；②jq 遇非 JSON 行即中止整条管道；③故示例统一在 journalctl 段与 jq 段之间插入 `| grep '^\{'` 预滤 JSON 事件行——与自动化测试 parseEvents 的滤行约定（滤 `{` 起始行）同款。
    - 两则示例（认证失败审计 / 单连接生命周期关联）各插入同一防护段：引号内字符逐字为 `grep '^\{'`；示例的 `#` 注释、jq select 表达式、`-o cat`、`-c` 等其余内容逐字保留，仅插入防护段。
    - 禁止项（锁定决策，per D-14/D-15/D-16，勿做）：不改任何 Go 源码；不把横幅迁 stderr 或 JSON 化（D-14 启动行人读文本 + 既有启动行解析零破坏；fd 改道也不消除 journal 合流）；不加 TTY 检测（D-15 明文拒绝隐式行为）；不改「运行期事件恒为 stderr 单行 JSON」承诺表述（承诺域是 stderr 事件流，与 journal 合流正交，表述仍真）；不加 StandardOutput=null/file 配方改动（null 静默丢 share link 可见性，配方侧无更优形态——示例防护已是默认配置下最小完备修复）。
    - 完成后自检：该节其余段落（schema 说明、事件目录表、「红线重申」段）零改动；全文件风格一致。
  </action>
  <verify>
    <automated>test "$(grep -cF "grep '^\{'" README.md)" -ge 2 && test "$(grep -E '^[[:space:]]*journalctl ' README.md | grep -vcF "grep '^\{'")" -eq 0</automated>
  </verify>
  <done>README 两则 journald 示例均含统一 grep 防护段，引言句说明合流机理与防护理由；文件中不存在 journalctl 直连 jq 的未防护示例行；wesh 源码零改动。</done>
</task>

<task type="auto">
  <name>Task 2: 合流模拟回归脚本 web/uat/phase08-journal.mjs（负对照 + 两则示例管道端到端）</name>
  <files>web/uat/phase08-journal.mjs</files>
  <action>
    - 新建零依赖 Node UAT 脚本（Node ≥ 22 原生 fetch/WebSocket），逐字复用 phase08.mjs 骨架件（04-04 先例）：WESH 二进制定位、spawn stdio 分离捕获、端口从 stdout 启动行解析（D-14 既有消费者形态的活证据）、waitEvent 异步流同步（stderr 落流晚于决议点——05-01 纪律）、dialHello 的 WebSocket 装配双形态分支（headers+protocols）、check/detail 断言输出、assertOutputClean 红线（凭据/ticket 值永不入测试输出）。文件头注释登记血缘：G-08-2（2026-08-28 UAT 实测）+ 等价性假设——journalctl -o cat 的 unit 输出 ≡ stdout+stderr 按时序拼接（两路 fd 经 journal 合流，横幅行先于事件行）。
    - 夹具：spawn `--credential UAT_CREDENTIAL -- bash --norc --noprofile`（单次语义独立 spawn，04-06 先例）。事件制造两步：①错凭据 fetch POST /api/attach（Authorization: Basic 任意错值）→ 期待 401，产生 auth_failed 事件；②正确凭据 fetch 同端点 → ticket → 原生 WebSocket 携子协议 + Hello（cols/rows/ticket）→ 等 Welcome → 主动 close → attach/detach 事件对（实例唯一成功连接 ⇒ client_id == 1 确定，D-20）。
    - 进程收口（kill + waitExit 等价形态）后构造合流流：stdout 全文在前 + stderr 全文在后拼接（journal 时序：横幅在启动期、事件在运行期）。
    - 断言四组（child_process spawnSync 以 `/bin/sh -c` 执行，合流流经 stdin 喂入——journalctl 段被 stdin 等价替换，grep+jq 段与 README 新示例逐字一致）：
      ①负对照（夹具自证）：合流流直灌无防护的 jq select 管道 → 退出码非 0 且 stderr 含 parse error 字样——证明夹具确复现 G-08-2 原缺陷形态（非 JSON 横幅行确在流中），修复面不是空转通过。
      ②全流纯度：防护段 + `jq .` → 退出 0 且 stderr 空——grep 之后每行皆合法 JSON（「零 parse error」的机械证明）。
      ③示例一：防护段 + `jq -c 'select(.event=="auth_failed")'` → 退出 0、stdout 恰 1 行、解析后 event=="auth_failed" 且无 user/username 键（D-23）、stderr 空。
      ④示例二：防护段 + `jq -c 'select(.client_id==1)'` → 退出 0、stdout 恰 2 行（attach+detach）、client_id 均为 1、detach reason=="normal"（D-20/D-21）、stderr 空。
    - jq 缺失兜底：spawnSync('jq', ['--version']) 非 0 → 打印 skipped + reason 后退出 0（实机工具豁免纪律，与浏览器层 UAT 的 skipped 形态一致；Linux 开发机已有 jq——A2 实测用过）。
    - 若本脚本管道段与 Task 1 的 README 示例出现任何字面分歧（含引号形态），以 README 为准回改脚本——两处必须逐字一致（key_link 防漂移）。
  </action>
  <verify>
    <automated>node web/uat/phase08-journal.mjs</automated>
  </verify>
  <done>脚本四组断言全绿（或 jq 缺失 skipped）：负对照证明合流夹具不空转、防护后全流零 parse error、两则示例管道在合流流上各检出预期事件行且 reason/键集断言成立。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README 示例 → operator 终端 | 示例命令被 operator 复制执行（内容为固定管道，无注入面） |
| 测试夹具 → 合流流解析 | UAT 脚本持有 UAT_CREDENTIAL 与一次性 ticket（敏感值） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06-01 | Information Disclosure | phase08-journal.mjs 测试输出 | low | mitigate | assertOutputClean 红线复用（phase08.mjs 同款）——凭据/ticket 值永不入 detail 输出；grep 全流断言在管道 stdout 面进行，敏感值不回显 |
| T-08-06-SC | Tampering | 依赖引入（npm 等） | high | accept | 本计划零新增依赖：README 编辑 + Node stdlib（node:child_process/fetch/WebSocket）+ 系统既有 jq/grep——无供应链面 |
</threat_model>

<verification>
1. `node web/uat/phase08-journal.mjs` 四组断言全绿（负对照 / 全流纯度 / 示例一 / 示例二）
2. `node web/uat/phase08.mjs` 全绿（21 断言——零代码改动的回归确认，08 约束明示保持全绿）
3. `go test -race -count=1 ./...` 全绿（README/脚本改动不触 Go 面——确认零漂移）
4. 真实 journal 复测提示（供 UAT resume，非本 plan 阻塞项）：Linux 开发机 systemd 用户级 unit（wesh-uat.service，A2 既存事件可回溯）下以 sg systemd-journal 代跑 README 两则新示例——jq 零 parse error、检出既存 auth_failed 与 attach/detach 对（A2 blocked 点已随用户加组解除）
</verification>

<success_criteria>
- G-08-2 truth 的机械化等价面全绿：合流模拟上 README 新示例管道原样可用（零 parse error + 预期事件检出 + reason/键集断言成立）
- git diff 仅 README.md 与新增 web/uat/phase08-journal.mjs——wesh 源码零改动（D-14/D-15/D-16 保持）
- README 无未防护 journald 示例行，防护段与脚本管道逐字一致
- 全量回归（Go -race 五包 + phase08.mjs 21 断言）零破坏
</success_criteria>

<output>
Create `.planning/phases/08-observability/08-06-SUMMARY.md` when done（frontmatter 携 gap_ids: [G-08-2] 供 /gsd-verify-work 对账 resolved）
</output>
