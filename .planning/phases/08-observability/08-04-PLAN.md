---
phase: 08-observability
plan: 04
type: execute
wave: 4
depends_on:
  - "08-03"
files_modified:
  - internal/server/metrics.go
  - internal/server/server.go
  - internal/server/clients.go
  - internal/server/auth.go
  - cmd/wesh/main.go
  - internal/server/metrics_test.go
autonomous: true
requirements:
  - OPS-07

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GET /metrics 返回手写 Prometheus text 0.0.4 exposition（D-01）：Content-Type text/plain; version=0.0.4; charset=utf-8、UTF-8 \\n 行尾、末行恒带换行、每 series 按 HELP/TYPE/样本分组——stdlib 零依赖，go.mod/go.sum 逐字节不动"
    - "series 全量 17 条（D-02 聚合 gauge/D-03 runtime/D-04 字节双指标/D-05 会话口径+连接三件套/D-06 认证计数器+build_info）：wesh_clients_connected gauge / wesh_clients_total counter / wesh_clients_kicked_total counter / wesh_session_active gauge / wesh_outbox_depth_bytes_max gauge / wesh_outbox_depth_bytes_sum gauge / wesh_pty_output_bytes_total counter / wesh_ws_sent_bytes_total counter / wesh_ws_recv_bytes_total counter / wesh_auth_failed_total counter / wesh_auth_throttled_total counter / wesh_input_rate_dropped_total counter / wesh_input_queue_dropped_total counter / wesh_credit_gate_transitions_total counter / wesh_goroutines gauge / wesh_mem_alloc_bytes gauge / wesh_build_info{version} gauge=1"
    - "label 纪律（D-02/D-06）：全部 series 零身份 label（remote/remote_user/client_id 永不进 label）；outbox 深度为 max/sum 聚合 gauge；build_info 仅 version 单 label 且值过 escLabel 转义（\\→\\\\、\"→\\\"、\\n→\\\\n，反斜杠先行）"
    - "metrics handler 读 registry 状态锁序 hubMu > outbox.mu（R-07：hubMu 一趟内逐 outbox.mu 读深度，afterDrain 先例形态）——kicks/gateTransitions/clientsTotal 保持 hubMu 内 plain int 承载，绝不为读取改 atomic"
    - "/metrics 跟随认证闸（D-08）：凭据模式过 basicAuth（无/错凭据 401、正确凭据 200）、--no-auth 模式直通；根路径固定不受 --base-path 影响（D-09）；POST 405 + Allow: GET"
    - "version 经 Options.Version 单一通道（main.go var version 直传，零值兜底 \"dev\"）——发布构建 ldflags 注入属 Phase 9 既定，本 phase 只落 plumbing"
    - "收发字节双指标分开（D-04）：wesh_pty_output_bytes_total（PTY 源单计）与 wesh_ws_sent_bytes_total（fan-out ×N）——两 series 相除即吞吐放大比；ws_recv 含 Hello 首读与稳态循环两站点"
    - "全量 go test -race 绿（含并发 attach/detach + 连续采集的快照竞态压力）"
  artifacts:
    - path: "internal/server/metrics.go"
      provides: "metricsHandler + snapshotMetrics + writeGauge/writeCounter/writeBuildInfo/escLabel + metricsCounters 结构"
      contains: "snapshotMetrics"
    - path: "internal/server/metrics_test.go"
      provides: "TestMetricsExposition/TestMetricsAuth/TestBuildInfo/TestMetricsValues/TestMetricsSnapshotRace"
      contains: "TestMetricsValues"
    - path: "cmd/wesh/main.go"
      provides: "Options.Version 透传（version 变量 → server.New）"
      contains: "Version: version"
  key_links:
    - from: "onChunk（PTY 读循环）/ writer（每客户端写端）/ Attach 读循环"
      to: "wesh_pty_output_bytes_total / wesh_ws_sent_bytes_total / wesh_ws_recv_bytes_total"
      via: "三挂点 atomic.Int64 热路径无锁递增（inputDrops 先例形态）——onChunk 入口计 chunk、writer 成功 Write 后计 msg、读循环每消息计 data"
      pattern: "wsSentBytes|wsRecvBytes|ptyOutputBytes"
    - from: "basicAuth 401/429 站点 + Attach Hello 核销失败站点"
      to: "wesh_auth_failed_total / wesh_auth_throttled_total"
      via: "basicAuth 加末参 *metricsCounters（server.go:402/416 与 /metrics 注册三调用点同改）；WS 侧 auth_failed 在 server.go:773 事件点同址递增"
      pattern: "metricsCounters"
    - from: "cmd/wesh/main.go var version"
      to: "wesh_build_info{version}"
      via: "main.go server.New Options 字面量追加 Version 一个 kv（生产直传先例形态）→ s.version → escLabel → exposition"
      pattern: "Options.Version"
  prohibitions:
    - statement: "metrics label MUST NOT 携带 remote/remote_user/client_id/ticket 任何客户端身份（D-02/D-06——日志红线在 metrics 面的镜像；per-IP/每连接明细查日志事件，metrics 只看总量与聚合）"
      status: resolved
      verification: test
---

<objective>
落地 OPS-07：新 metrics.go 装手写 exposition 三件套（snapshotMetrics + writer + handler），Handler() 根路径注册（认证闸跟随 + 405 fallback），Options.Version plumbing，计数器挂点全量兑现（clientsTotal + 字节三件套 + auth 两计数器）——17 series 覆盖 ROADMAP SC2 五类指标（连接数/会话存活/收发字节/outbox 深度/踢出计数）+ 认证计数器 + 基础 runtime。

Purpose: ttyd 缺失的可运维性第二面——Prometheus scrape_config 原生 basic_auth 可采集、人工 curl 可读；慢客户端检测（outbox max）、踢出计数、吞吐放大比一眼可见。
Output: /metrics 全行为 + 计数器挂点 + metrics_test.go 行为锁。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-observability/08-CONTEXT.md
@.planning/phases/08-observability/08-RESEARCH.md
@.planning/phases/08-observability/08-PATTERNS.md
@.planning/phases/08-observability/08-03-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): /metrics 端到端——metrics.go exposition 三件套 + 注册（认证闸/405/bp 固定）+ Options.Version plumbing + 格式与认证两态测试</name>
  <reversibility rating="costly">D-01/D-08：端点格式与 series 命名是采集契约（改动破坏已部署 scrape 配置与看板）；认证闸形态是暴露面契约——用户已在 08-CONTEXT 锁定，记录评级不重复设卡。</reversibility>
  <files>internal/server/metrics.go, internal/server/server.go, cmd/wesh/main.go, internal/server/metrics_test.go</files>
  <read_first>
    - internal/server/server.go（Handler() L391-474 两分支——/metrics 注册插入点（08-03 /healthz 双注册同区）；Options 注释分档 L159-234 与 New 零值兜底先例 L260-297；Server struct 字段区）
    - internal/server/clients.go（afterDrain 锁序先例 L451-456；registry n/kicks/gateTransitions L250-268；inputQ.droppedInputs L197-200）
    - internal/server/auth.go（basicAuth 签名 L101——/metrics 包装直接复用现状四参形态，Task 2 才加计数器参）
    - cmd/wesh/main.go（var version L31-32；server.New Options 字面量 L1134——追加 Version 一个 kv 的落点）
    - .planning/phases/08-observability/08-RESEARCH.md（Pattern 2 规范 CITED 条款与 series 清单表、Pattern 3 快照形态、Pitfall 7 405）
  </read_first>
  <behavior>
    - 无认证实例：GET /metrics → 200，Content-Type 逐字 text/plain; version=0.0.4; charset=utf-8；body 每 series 按 # HELP/# TYPE/样本行三行组序；末行以 \n 收尾；wesh_clients_connected 样本值 0（dialHello 后 1）；wesh_session_active==1；wesh_goroutines>0；wesh_mem_alloc_bytes>0；wesh_build_info{version="dev"} 1
    - 凭据实例：无/错凭据 GET /metrics → 401（与 / 同链同文）；正确凭据 → 200 同形态（D-08）
    - bp=/wesh 实例：GET /metrics → 200；GET /wesh/metrics → 无认证 404/凭据 401（D-09 根路径固定）
    - POST /metrics → 405 + Allow: GET（两模式同文，fallback 不包认证——与 /api/attach 405 先例同形态）
    - escLabel 表驱动：`v"q` → `v\"q`、反斜杠先行翻倍、换行 → \n 两字符
  </behavior>
  <action>
    ① internal/server/metrics.go（新文件）：包声明 + 注释头登记 D-01/D-02/D-03/D-04/D-05/D-06/D-08/D-09 与 label 红线（零身份 label——日志红线的 metrics 镜像）。内容：metricsCounters struct（authFailed/authThrottled atomic.Int64——Task 2 递增点挂此）；metricsSnap struct（clientsConnected/clientsTotal/kicks/gateTransitions int64 + outboxMax/outboxSum int）；snapshotMetrics()（RESEARCH Pattern 3 逐字形态：hubMu 一趟内读 n/clientsTotal/kicks/gateTransitions + 遍历 registry.set 逐 outbox.mu 读 bytes 聚合 max/sum——clientsTotal 字段 Task 2 落地，本任务快照先读 0 值占位？否——同任务内一次性写全：本任务即落地 registry.clientsTotal 字段与 registerLocked 加点（clients.go 一并改，属本任务文件面），Task 2 只做字节/auth 计数器与数值测试）；writeGauge/writeCounter（HELP/TYPE/样本三行 fmt.Fprintf 形态，RESEARCH Pattern 2）/writeBuildInfo（label 值过 escLabel）/escLabel（三字符转义，反斜杠先行顺序敏感）；metricsHandler（snapshot → Content-Type → builder 逐 series 输出 17 条 → 末行恒 \n；runtime.NumGoroutine()/runtime.ReadMemStats(&m).Alloc 直采；sessionAlive 读 08-03 字段）。
    ② internal/server/server.go：Options 加 Version string 字段（生产直传注释分档先例：main var version 原样透传，零值兜底 "dev"）；New 加兜底与 s.version 字段直传；Handler() 凭据分支注册 mux.Handle("GET /metrics", basicAuth(http.HandlerFunc(s.metricsHandler), s.credentials, s.throttle, s.proxy))、无认证分支注册 mux.HandleFunc("GET /metrics", s.metricsHandler)——均不带 bp（D-09）；两分支之外注册 path-only 405 fallback（Allow: GET，与 /healthz fallback 同区）。
    ③ internal/server/clients.go：registry 加 clientsTotal int64 字段（hubMu 保护注释——与 kicks 同形态，R-07 下无需 atomic）；registerLocked 尾部 r.clientsTotal++（唯一加点，对称记账注释）。
    ④ cmd/wesh/main.go：server.New Options 字面量尾部追加 Version: version（L1134 先例形态——生产直传字段原样透传）。
    ⑤ internal/server/metrics_test.go（新文件）：TestMetricsExposition（白盒：构造最小 Server 直调 metricsHandler 经 httptest.ResponseRecorder——Content-Type 逐字断言、单 series 三行组序、末行 \n、17 条 series 名全在场清单）；TestMetricsAuth（黑盒：凭据实例 401→200 两态 + 无认证实例直通 + bp 实例 /metrics 200 与 /wesh/metrics 精确码 + POST 405）；TestBuildInfo（escLabel 表驱动 + exposition 中 build_info 行格式断言）。
    提交：feat(08-04): /metrics endpoint — hand-written Prometheus text exposition
  </action>
  <verify>
    <automated>go test ./internal/server/ -race -count=1 -run 'TestMetricsExposition|TestMetricsAuth|TestBuildInfo|TestHealthz' -v && go test ./internal/server/ -count=1 && go vet ./internal/server/ && git diff --exit-code go.mod go.sum</automated>
  </verify>
  <acceptance_criteria>
    - TestMetricsExposition/TestMetricsAuth/TestBuildInfo 全 PASS（Content-Type 逐字、17 series 在场、认证两态、bp 固定、405）
    - `grep -c 'func (s \*Server) snapshotMetrics' internal/server/metrics.go` == 1
    - `grep -c '"GET /metrics"' internal/server/server.go` == 2（凭据/无认证两分支各一）
    - `grep -c 'Version: version' cmd/wesh/main.go` == 1
    - `grep -c 'clientsTotal' internal/server/clients.go` ≥ 2（字段 + 加点）
    - `git diff --exit-code go.mod go.sum` 退出 0（零新依赖证据）
  </acceptance_criteria>
  <done>
    /metrics 端点全形态上线：17 series exposition 合法、认证闸两态、bp 固定、405 成对；version plumbing 单一通道；go.mod 零漂移。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 计数器挂点兑现——字节三件套 + auth 两计数器 + TestMetricsValues 数值正确性 + 快照竞态压力</name>
  <files>internal/server/metrics.go, internal/server/server.go, internal/server/clients.go, internal/server/auth.go, internal/server/metrics_test.go</files>
  <read_first>
    - internal/server/metrics.go（Task 1 落地的 metricsCounters/snapshotMetrics/metricsHandler——本任务递增点与快照读取扩展）
    - internal/server/clients.go（onChunk L354-368——pty_output 挂点入口；writer L633-653 成功 Write 后——ws_sent 挂点；inputDrops/droppedInputs 读取现状）
    - internal/server/server.go（Attach 读循环：Hello 首读 L740 与稳态循环 L880——ws_recv 两挂点；Hello 核销失败事件站点 L773——WS 侧 auth_failed 递增点；Server struct 字段区——mc metricsCounters 落位）
    - internal/server/auth.go（basicAuth L101-123——签名加末参与 401/429 两递增点；调用点 server.go:402/416 与 /metrics 注册三处同改）
    - internal/server/slowclient_test.go（stall 夹具形态——TestMetricsValues 踢出场景复用；小 OutboxBytes 覆写先例）
    - internal/server/auth_e2e_test.go（爬梯 sleep 驱动 429 的 pacing 形态）
  </read_first>
  <behavior>
    - 两客户端 attach + /bin/cat 回显驱动后：wesh_clients_connected==2、wesh_clients_total==2；wesh_pty_output_bytes_total>0、wesh_ws_recv_bytes_total>0、wesh_ws_sent_bytes_total ≥ wesh_pty_output_bytes_total（fan-out ×N 语义——双端窗口期下行单端不少于源）
    - 驱一次慢客户端踢出后：wesh_clients_kicked_total==1、wesh_clients_connected==1
    - 凭据实例错凭据打 /metrics：wesh_auth_failed_total ≥ 1；爬梯打满 429 后 wesh_auth_throttled_total ≥ 1（无 IP label——per-IP 明细查日志事件）
    - 并发 attach/detach 循环 + 连续 GET /metrics 在 -race 下干净（快照锁序回归）
  </behavior>
  <action>
    ① internal/server/metrics.go：metricsCounters 扩五字段（authFailed/authThrottled/ptyOutputBytes/wsSentBytes/wsRecvBytes atomic.Int64——热路径无锁递增，inputDrops 先例形态；注释挂 OPS-07）；snapshotMetrics 读取端接入五计数器（atomic Load——hubMu 外读合法，与快照内 plain int 读取并存，场景化选型注释）。
    ② internal/server/server.go：Server struct 加 mc metricsCounters 字段；onChunk 入口（hubMu 持锁内，atomic 递增无锁安全）s.mc.ptyOutputBytes.Add(int64(len(chunk)))；Attach Hello 首读与稳态读循环每消息 s.mc.wsRecvBytes.Add(int64(len(data)))（RESEARCH A4：两站点计入，忠实「WS 网络流量」字面）；Hello 核销失败事件站点（L773 区）s.mc.authFailed.Add(1)。
    ③ internal/server/clients.go：writer 内每条 msg 成功 Write 之后 s.mc.wsSentBytes.Add(int64(len(msg)))（写失败 return 路径不计——计成功送出字节）。
    ④ internal/server/auth.go：basicAuth 签名加末参 mc *metricsCounters（401 站点 mc.authFailed.Add(1)、429 站点 mc.authThrottled.Add(1)）；server.go 三调用点（:402 root、:416 attach 链、/metrics 凭据注册）同改传 &s.mc——注释登记「计数与事件同址递增（401/429 事件行既有，metrics 只加计数不打行）」。
    ⑤ internal/server/metrics_test.go：TestMetricsValues（黑盒无认证实例 + exposition 文本逐行解析小 helper：两 dialHello + INPUT 帧驱 /bin/cat 回显 → GET 断言 behavior 数值组；复用 slowclient_test.go stall 夹具驱一次踢出 → kicked==1/connected==1；凭据实例错凭据 + 爬梯 429 → auth 两计数器断言）；TestMetricsSnapshotRace（-race 压力：goroutine 组并发 attach/close 循环 2s + 主循环连续 GET /metrics——数据竞争即红）。
    提交：feat(08-04): wire byte/auth counters into /metrics, value-correctness tests
  </action>
  <verify>
    <automated>go test ./internal/server/ -race -count=1 -run 'TestMetrics' -v && go test -race -count=1 ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - TestMetricsValues 全 PASS（connected/total/kicked/字节三件套/auth 两计数器数值断言）
    - TestMetricsSnapshotRace -race 干净
    - `grep -c 'wsRecvBytes.Add' internal/server/server.go` == 2（Hello 首读 + 稳态循环两站点）
    - `grep -c 'ptyOutputBytes.Add\|wsSentBytes.Add' internal/server/clients.go` == 2（onChunk + writer 各一）
    - `grep -c 'mc \*metricsCounters' internal/server/auth.go` == 1（basicAuth 末参签名唯一）
    - `grep -c '&s.mc' internal/server/server.go` == 3（三调用点同改）
    - `go test -race -count=1 ./...` 全绿（auth_e2e/proxy_e2e 等 basicAuth 间接受影响面零回归）
  </acceptance_criteria>
  <done>
    五计数器挂点兑现：三字节/auth 两计数器数值行为锁定；basicAuth 签名扩展三调用点同改；-race 全量绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prometheus/curl → /metrics | 行为轮廓观测面（连接数/失败计数）——D-08 认证闸是唯一访问控制 |
| version 字符串 → exposition label | ldflags 注入值进 label——转义缺失即 exposition 注入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04a | Information Disclosure | metrics label 携身份（remote/client_id）→ 隐私泄漏 + label 基数爆炸 | high | mitigate | D-02/D-06：全 series 零身份 label（聚合 gauge + 总量计数）；prohibition 行为锁（exposition 全文无 remote 值串断言随 TestMetricsValues） |
| T-08-04b | Information Disclosure | /metrics 行为轮廓被未授权观测 | medium | mitigate | D-08 认证闸跟随（401 同文同链）；--no-auth 模式的暴露面 README 明示（08-05） |
| T-08-04c | Denial of Service | 采集器凭据错误触发自锁（429 节流打 Prometheus 目标） | medium | mitigate | README 配方明示 basic_auth 配置与「凭据错误触发全站节流」后果（08-05，RESEARCH Pitfall 6）；throttled 事件可观测（08-02 retry_after 字段） |
| T-08-04d | Tampering | exposition 注入（version label 含引号/换行伪造 series） | low | mitigate | escLabel 三字符转义单侧定义（规范逐字要求）；TestBuildInfo 表驱动锁定 |
| T-08-04e | Denial of Service | 采集锁序错误致 fan-out 冻结（ABBA 死锁） | high | mitigate | snapshotMetrics 单快照 hubMu > outbox.mu（RESEARCH Pattern 3 逐字形态）；TestMetricsSnapshotRace -race 压力回归 |
| T-08-SC | Tampering | npm/pip/cargo 包安装供应链 | high | mitigate | N/A——本 plan 零新依赖（D-01 手写 exposition 排除官方库；go.mod/go.sum 验收逐字节不动） |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V2 Authentication（/metrics 闸跟随） | ✓ D-08 basicAuth 包装 + 两态行为锁 |
| V7 Logging（身份不进指标面） | ✓ 零身份 label + build_info 单 version label 转义 |
| V9 Communications（安全头继承） | ✓ securityHeaders 包裹全部路由（server.go:473 既定）——TLS 部署下 /metrics 同享 HSTS |
</threat_model>

<verification>
- `go test -race -count=1 ./...` 全绿（快照竞态 + basicAuth 签名扩展全回归）
- 冒烟：真实二进制 `--port 0` 启动，`curl -s localhost:PORT/metrics` 首行 `# HELP wesh_clients_connected`、含 17 条 series、末行换行
</verification>

<success_criteria>
- `curl -s localhost:PORT/metrics` 输出可被任意 Prometheus text 0.0.4 解析器消费；双客户端场景 ws_sent ≥ 2×pty_output 的放大比可观测
- 凭据模式 `curl -u user:pass .../metrics` 200、无凭据 401；bp=/wesh 下采集路径恒为根 /metrics
- 慢客户端被踢后 wesh_clients_kicked_total 递增、outbox max 回落——运维信号闭环
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **OPS-07 (unclassified, unresolved)**：probe 无法从需求文本分类边界形态——本 plan 以 08-CONTEXT D-01..D-06 与 RESEARCH Validation Architecture 为验收来源（17 series/认证两态/数值正确性全显式验证）。人工复核项：无真实 Prometheus 实例验证（RESEARCH Environment Availability 登记的 fallback）——采集兼容性以 exposition 文本规范逐字断言代替，真实 scrape 随 08-05 人工清单复核；ws_recv 计入 Hello 首读（RESEARCH A4，~100B/连接，运维语义无碍）。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `func (s *Server) metricsHandler(w http.ResponseWriter, r *http.Request)` / `func (s *Server) snapshotMetrics() metricsSnap` / `func writeGauge` / `func writeCounter` / `func writeBuildInfo` / `func escLabel(string) string`（internal/server/metrics.go，新文件）
- `type metricsCounters struct{ authFailed, authThrottled, ptyOutputBytes, wsSentBytes, wsRecvBytes atomic.Int64 }` + `Server.mc metricsCounters` 字段
- `type metricsSnap struct`（internal/server/metrics.go）
- `registry.clientsTotal int64` 字段（internal/server/clients.go，hubMu 保护）
- `Options.Version string` + `Server.version string`（internal/server/server.go）；main.go Options 字面量 `Version: version` kv
- basicAuth 新签名 `func basicAuth(next http.Handler, creds []Credential, th *throttleStore, p proxyInfo, mc *metricsCounters) http.Handler`
- 17 条 series 名（wesh_ 前缀，must_haves.truths 清单逐字）
- Handler() 注册：`"GET /metrics"`（两分支）+ `"/metrics"` path-only 405 fallback
- `TestMetricsExposition` / `TestMetricsAuth` / `TestBuildInfo` / `TestMetricsValues` / `TestMetricsSnapshotRace`（internal/server/metrics_test.go，新文件）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/08-observability/08-04-SUMMARY.md` when done
</output>
