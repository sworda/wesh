---
phase: 08-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/server/log.go
  - internal/server/server.go
  - internal/server/log_test.go
  - internal/server/limits_test.go
  - internal/server/emptyexit_test.go
  - internal/server/auth_e2e_test.go
  - internal/server/proxy_e2e_test.go
  - internal/server/multi_test.go
  - web/uat/phase05.mjs
  - web/uat/phase07.mjs
  - web/uat/phase07-b2.mjs
autonomous: true
requirements:
  - OPS-08

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "运行期事件全部经 slog JSONHandler 输出单行 JSON：msg 恒 \"event\"、事件名走独立 event 字段、time/level 为 slog 默认键、level 恒 \"INFO\"（D-13/D-15/D-18）"
    - "logEvent 签名不变（remote, code, reason, remoteUser ...string），全部既有调用点零改动——迁移只换内部实现（D-13）"
    - "captureStderr 测试通道不失明：事件 writer 在每次 Write 时解析 os.Stderr 变量（动态 writer），05-01 同步纪律（startTrackedServerWith + waitHandlers）下全部 stderr 断言测试继续成立"
    - "启动行/分享链接行（含 token）与 wesh: warning: 警告行保持人读文本逐字节不变（D-14/D-16）——01-05 冒烟与 UAT 启动行解析消费者零破坏"
    - "5 个 Go 测试文件（limits/emptyexit/auth_e2e/proxy_e2e/multi）与 3 个 UAT 脚本（phase05 S6/phase07 S4/phase07-b2 B2b）的事件断言全部迁移为按行 JSON 解析字段断言，禁止子串/正则断言 JSON 行；凭据/ticket/token 红线负断言逐字保留子串形态"
    - "零新 CLI flag、零新外部依赖（log/slog 为 stdlib，go.mod 不动）（D-15/D-01 哲学）"
    - "全量 go test -race 绿 + 既有 UAT 脚本全量回归绿（回归面 = 08-05 Task 3 显式枚举清单，非裸计数）"
  artifacts:
    - path: "internal/server/log.go"
      provides: "stderrW 动态 writer + eventLog 包级单例 + emitEvent 底层出口 + logEvent（slog 实现）"
      contains: "slog.NewJSONHandler"
    - path: "internal/server/log_test.go"
      provides: "parseEvents/countByEvent 断言 helper + TestLogEventJSON 端到端"
      contains: "func parseEvents"
    - path: "internal/server/server.go"
      provides: "旧文本 logEvent 实现整体删除（注释随迁至 log.go 并去掉过渡形态表述）"
      contains: "logIfMessageTooBig"
  key_links:
    - from: "internal/server 全部 logEvent 调用点（18 处）"
      to: "internal/server/log.go eventLog（slog JSONHandler）→ stderrW → os.Stderr"
      via: "调用时解析 os.Stderr 变量——captureStderr 置换后事件行仍入管道（动态 writer 是捕获语义的唯一保真形态）"
      pattern: "stderrW"
    - from: "web/uat/phase05.mjs S6 / phase07.mjs S4 / phase07-b2.mjs B2b stderr 断言"
      to: "JSON 事件行（event/code/remote/remote_user 字段）"
      via: "按行 split → 滤 '{' 起始行 → JSON.parse → 字段断言（与子串断言正交）"
      pattern: "JSON.parse"
  prohibitions:
    - statement: "凭据/ticket/token/Authorization 头任何形态（含 base64、含用户名）MUST NOT 出现在 JSON 事件流任何字段（SEC-01/D-23 红线在 slog 形态的延续；auth_failed 事件不含用户名）"
      status: resolved
      verification: test
    - statement: "启动行/分享链接行的 token MUST NOT 迁入 JSON 事件结构化字段（D-14：stdout 人读文本与 stderr 机器事件两通道分流，token 的合法输出面仅启动打印与 URL 路径）"
      status: resolved
      verification: judgment
    - statement: "运行期事件 MUST NOT 双轨输出（同一事件既出旧文本行又出 JSON 行）——D-13 原子迁移，拒绝双轨漂移面"
      status: resolved
      verification: test
---

<objective>
落地 D-13 原子迁移：把唯一事件出口 logEvent（现 server.go:1071-1077 的 fmt.Fprintf 文本行）迁入新 log.go，内部换 slog JSONHandler（stdlib，STACK.md 定案），经动态 stderr writer 保持「调用时解析 os.Stderr」语义；随迁 5 个 Go 测试文件与 3 个 UAT 脚本（phase05 S6/phase07 S4/phase07-b2 B2b）的事件断言为按行 JSON 解析（parseEvents helper），凭据红线负断言逐字保留。本 plan 是 Phase 8 全部后续工作的基座——08-02 的审计事件目录、08-03/08-04 的端点事件都建立在 slog 形态之上。

Purpose: OPS-08 的第一刀——事件格式从单行文本切到 JSON，检索/索引能力（jq/Loki 直打字段）由此解锁；一次性原子切换杜绝双轨漂移。
Output: log.go（slog 基础设施）+ 全量断言迁移 + 回归绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-observability/08-CONTEXT.md
@.planning/phases/08-observability/08-RESEARCH.md
@.planning/phases/08-observability/08-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): log.go slog 基座端到端——logEvent 迁移 + 动态 writer + parseEvents helper + TestLogEventJSON 全链打穿</name>
  <reversibility rating="costly">D-13：全部 captureStderr 断言随迁移改写，回退需全量还原——用户已在 08-CONTEXT 锁定，记录评级不重复设卡。</reversibility>
  <files>internal/server/log.go, internal/server/server.go, internal/server/log_test.go</files>
  <read_first>
    - internal/server/server.go（logEvent 现状 L1047-1077 含红线注释块、logIfMessageTooBig L1079-1092 保持不动、import 块）
    - internal/server/limits_test.go（captureStderr L91-111 本体零改动、TestOversize1009 L117-152 同步边形态）
    - internal/server/e2e_test.go（startTrackedServerWith waitHandlers 同步纪律 L171-194）
    - internal/server/proxy.go（注释头 L3-39——新文件「注释头登记决策号 + 红线声明」组织纪律先例）
    - .planning/phases/08-observability/08-RESEARCH.md（Pattern 1 动态 writer 与 Pitfall 1 实证、Pattern 5 parseEvents 形态、Pitfall 4 float64 陷阱）
  </read_first>
  <behavior>
    - logEvent 调用点触发事件（如 message_too_big）→ stderr 恰一行合法 JSON：time（RFC3339 毫秒）/level="INFO"/msg="event"/event=<reason 串>/remote=<对端>/code=<int> 六键；remoteUser 非空才出 remote_user 键（空串/缺省不出键，与现状语义一致）
    - captureStderr 置换 os.Stderr 后事件行仍被捕获（动态 writer：每次 Write 调用时读 os.Stderr 变量——RESEARCH Pitfall 1：handler 构造时捕获旧 writer 则测试全部失明）
    - 并发 emit 行级原子：JSONHandler 内建 mutex 串行化记录，每记录恒完整一行（比现状 fmt.Fprintf 更强）
    - 禁止 kv 交替参数（奇数/非 string 键产出 !BADKEY）——一律 LogAttrs + slog.String/Int 类型化 attr
    - 测试：TestLogEventJSON 端到端——captureStderr → startTrackedServerWith（/bin/cat + Writable）→ dialHello → 写 proto.ReadLimitPostAuth+1 字节触发 1009 → readCloseErr == 1009 → waitHandlers 同步边 → restore → parseEvents：恰 1 条 event=="message_too_big"，msg=="event"、level=="INFO"、code 按 float64 比 1009、remote 前缀 "127.0.0.1:"、time 键存在非空
  </behavior>
  <action>
    ① 新建 internal/server/log.go：包声明 + 注释头登记 D-13/D-15/D-18 与迁移来源（server.go 原 L1047-1077），红线注释随函数迁移逐字保留语义（凭据/ticket/Authorization 任何形态禁止作为任何参数传入；remote_user 值必须经 sanitizeRemoteUser 清洗且来源只能是配置头名 HTTP 头——清洗在提取点完成，本函数不做二次清洗）。内容：stderrW 类型（Write 方法体内读 os.Stderr 变量写回，RESEARCH Pattern 1 逐字形态）；包级单例 eventLog = slog.New(slog.NewJSONHandler(stderrW{}, nil))；emitEvent(attrs ...slog.Attr) 底层出口（eventLog.LogAttrs(context.Background(), slog.LevelInfo, "event", attrs...)——08-02 扩展字段事件的挂点，本 plan 仅 logEvent 消费）；logEvent 签名不变迁入（attrs = event/remote/code 三键 + remoteUser 非空追加 remote_user，D-18 schema）。
    ② internal/server/server.go：删除旧 logEvent 函数体（两行 stderr 文本打印分支，L1071-1077）；注释块 L1047-1070 整体迁往 log.go，删去其中「Phase 8 升级 slog 结构化日志（OPS-08），本期为过渡形态」表述——迁移已完成）；logIfMessageTooBig 保持原位（其 logEvent 调用零改动）；import 块若 fmt/os 因删除变为未使用则同步清理（其余使用点仍在则不動）。
    ③ 新建 internal/server/log_test.go：parseEvents(t, captured string) []map[string]any（按 "\n" 切行、跳过非 '{' 起始行——D-16 警告行与 panic 栈等混合流成员不得使解析 FAIL、json.Unmarshal 到 map[string]any、单行非法 JSON 即 t.Fatalf 带行内容）；countByEvent(evs, name) int 与事件字段断言小件（注意 JSON 数字解码为 float64——RESEARCH Pitfall 4：code 按 float64 比或写 intOf helper）；TestLogEventJSON（behavior 端到端形态；同步边 waitHandlers 复用 e2e_test.go 既有夹具）。
    提交：feat(08-01): migrate logEvent to slog JSONHandler with dynamic stderr writer
  </action>
  <verify>
    <automated>go test ./internal/server/ -run 'TestLogEventJSON' -count=1 -v && go vet ./internal/server/ && time go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - TestLogEventJSON PASS（六键断言 + captureStderr 捕获成立）
    - `grep -c 'slog.NewJSONHandler' internal/server/log.go` == 1
    - `grep -c 'os.Stderr' internal/server/log.go` ≥ 1（动态 writer 调用时解析语义存在）
    - `grep -c 'func logEvent' internal/server/log.go` == 1 且 `grep -c 'func logEvent' internal/server/server.go` == 0（唯一出口迁至 log.go）
    - `grep -c 'Fprintf' internal/server/server.go` == 0（server.go 无遗留文本行打印——旧实现的格式化打印整体清零）
    - `go vet ./internal/server/` 退出 0
  </acceptance_criteria>
  <done>
    slog 基座落地：logEvent 签名不变、输出为单行 JSON、动态 writer 保 captureStderr 语义；TestLogEventJSON 端到端锁定；server.go 旧实现零残留。
  </done>
</task>

<task type="auto">
  <name>Task 2: 5 个 Go 测试文件事件断言迁移——子串/行尾锚定 → parseEvents 字段断言（红线负断言逐字保留）</name>
  <files>internal/server/limits_test.go, internal/server/emptyexit_test.go, internal/server/auth_e2e_test.go, internal/server/proxy_e2e_test.go, internal/server/multi_test.go</files>
  <read_first>
    - internal/server/log_test.go（Task 1 落地的 parseEvents/countByEvent helper——本任务全部断言改造的唯一消费形态）
    - internal/server/limits_test.go（TestOversize1009 L142-151：count + 三要素子串断言）
    - internal/server/emptyexit_test.go（L268-283：行尾锚定负/正断言——exiting 复查与 wait 启动行证据）
    - internal/server/auth_e2e_test.go（L450-474：红线负断言 L457-467 + 正向对照 L469-474）
    - internal/server/proxy_e2e_test.go（L82-96 remote_user 行断言、L320-348 双通道 max_clients/message_too_big + token/ticket 红线负断言）
    - internal/server/multi_test.go（L830-839：max_clients/code=503 存在性断言）
    - .planning/phases/08-observability/08-PATTERNS.md（「测试断言迁移」节——迁移清单与两陷阱：float64、行尾锚定消解）
  </read_first>
  <action>
    ① limits_test.go TestOversize1009：msg/event 面迁移——恰 1 条 event=="message_too_big"（countByEvent == 1），该事件 code 按 float64 比 1009、remote 前缀 "127.0.0.1:"；原 count 子串断言与三要素子串断言整体替换。
    ② emptyexit_test.go：负断言「触发行零次」改为 countByEvent(evs, "exit_when_empty") == 0（exiting 复查防线）；正断言「wait 启动行存在」改为 countByEvent(evs, "exit_when_empty_wait") == 1——行尾锚定在 JSON 字段精确相等语义下天然消解（exit_when_empty 与 exit_when_empty_wait 是两个独立 event 名，无前缀歧义）。
    ③ auth_e2e_test.go：红线负断言四行（b64 凭据/明文口令/ticket 值/authorization 大小写不敏感）逐字保留子串形态（与 JSON 化正交——断言「全文不含敏感串」）；正向对照两行升级为 countByEvent(evs, "auth_failed") ≥ 1 与 countByEvent(evs, "throttled") ≥ 1。
    ④ proxy_e2e_test.go：第一块（L86-95）——countByEvent(evs, "auth_failed") == 3；存在 remote_user=="alice" 的事件与 remote_user=="carol" 的事件各 ≥1；携 remote_user 键的事件总数 == 2（不携头行不出键语义保持）。第二块（L325-347）——countByEvent(evs, "max_clients") == 2 且两事件 remote_user 分别为 "bob"/"carol"（双通道同口径）；countByEvent(evs, "message_too_big") == 1 且该事件 remote_user=="dave"；token/ticket 红线负断言四行逐字保留。
    ⑤ multi_test.go L837：改为「存在 event=="max_clients" 且 code 按 float64 比 503 的事件 ≥1」（存在性即锁定，轮询重试次数不确定语义保持）。
    全部文件头部注释中描述事件行形态的措辞（如「stderr 单行事件」）同步改写为 JSON 事件行表述；captureStderr 本体与 waitHandlers 同步边零改动。
    提交：test(08-01): migrate stderr event assertions to JSON field parsing
  </action>
  <verify>
    <automated>go test ./internal/server/ -count=1 && go test ./internal/server/ -race -count=1 -run 'TestOversize1009|TestExitEmpty|TestAuth|TestProxy|TestMaxClients|TestClientCount' && go vet ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/server/ -count=1` 全绿（五文件迁移后无遗留子串断言失败）
    - `grep -rn 'reason=' internal/server/*_test.go` 退出码 1（旧文本事件行键格式在测试断言面清零——JSON 字段语义取代；注意 grep 命中注释也算命中，注释措辞同步清零后方可归零）
    - `grep -c 'parseEvents' internal/server/log_test.go` ≥ 1 且五迁移文件各自 `grep -c 'parseEvents' <file>` ≥ 1
    - auth_e2e_test.go 的 b64Wrong/明文口令/ticket/authorization 四负断言行与 proxy_e2e_test.go 的 roTok/rwTok/issued.Ticket 负断言行逐字仍在（`grep -c 'b64Wrong' internal/server/auth_e2e_test.go` ≥ 2、`grep -c 'roTok' internal/server/proxy_e2e_test.go` ≥ 2）
  </acceptance_criteria>
  <done>
    五文件事件断言全部 JSON 字段化（事件名精确相等、float64 数字纪律）；红线负断言子串形态逐字保留；包级测试全绿。
  </done>
</task>

<task type="auto">
  <name>Task 3: phase05.mjs S6、phase07.mjs S4 与 phase07-b2.mjs B2b UAT 断言迁移 JSON 行解析 + 全量 UAT 回归</name>
  <files>web/uat/phase05.mjs, web/uat/phase07.mjs, web/uat/phase07-b2.mjs</files>
  <read_first>
    - web/uat/phase05.mjs（S6 场景 L426-440：stderr 轮询 1013/slow_consumer 行断言；文件头红线纪律块与 check/detail 形态 L1-30；startWesh stderrText 通道）
    - web/uat/phase07.mjs（S4 场景 L537-600：remote=/remote_user=/reason= 子串断言组 + S4c NEL 线形探针 L570-579；sensitiveTokens/redactArgs 红线 L77-92）
    - web/uat/phase07-b2.mjs（B2b L130-132：remote_user 首值子串断言 + bob 缺席负断言；B2d L142-144：空值头不出 remote_user 键 + auth_failed 行存在——JSON 形态下幸存，零改动；先读本脚本确认 B2b/B2d 语义再动手）
    - .planning/phases/08-observability/08-PATTERNS.md（web/uat/phase08.mjs 节「JSON 事件行断言新形态」——按行 split → 滤 '{' 起始行 → JSON.parse → event 字段断言）
  </read_first>
  <action>
    ① 三脚本各自内联新增 parseEvents(stderrText) helper（零依赖纪律：各脚本自含不共享模块——按行 split、滤 '{' 起始行、JSON.parse、解析失败即抛带行号与行首 120 字符截断的 Error；detail 红线保持：事件值只作断言材料，detail 只打 event 名/布尔/计数）。
    ② phase05.mjs S6：stderr 轮询断言改为「10s 内出现 event=="slow_consumer" 且 code==1013 的 JSON 事件行」（08-02 将折入 detach reason=kick——届时本行二次迁移，08-02 plan 已登记该文件的二次触碰）；客户端侧 1013 关闭帧既有断言逐字不动。
    ③ phase07.mjs S4：全部 remote=/reason= 子串断言改 JSON 字段断言——存在 event=="exit_when_empty" 且 remote=="198.51.100.7" 且 remote_user=="alice" 的事件；S4c NEL 探针断言保持语义（注入 NEL 的输入经剥离后 remote_user=="alice" 保留，对照组无 remote_user 键）；detail 只打布尔与 event 名（现状逐字形态保持）。
    ④ phase07-b2.mjs B2b：迁移为按行 JSON 解析字段断言——parseEvents(tail1) 后断言「存在 event=="auth_failed" 且 remote_user 字段等于 "alice" 的事件」（多值头取首值语义）且「无一事件的 remote_user 字段含 "bob"」（第二值泄漏负断言，JSON 字段形态）；detail 只打两枚布尔（现状形态保持）。B2d 零改动——空值头不出 remote_user 键的负断言与 auth_failed 行存在断言在 JSON 单行形态下原样成立（checker 核实幸存，只迁移格式变化真实打破的 B2b）；B2a/B2c 关闭帧断言本就不消费事件行，逐字不动。
    ⑤ 真实二进制重建后三脚本全跑 + 其余 UAT 脚本回归（phase02/03/04/06 不消费事件行零改动——跑通即证）。
    提交：test(08-01): migrate phase05/07/07-b2 UAT event assertions to JSON line parsing
  </action>
  <verify>
    <automated>time go build -o /tmp/wesh-uat/wesh ./cmd/wesh && node web/uat/phase05.mjs && node web/uat/phase07.mjs && node web/uat/phase07-b2.mjs && node web/uat/phase02.mjs && node web/uat/phase03.mjs && node web/uat/phase04.mjs && node web/uat/phase06.mjs && go test -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - `node web/uat/phase05.mjs` 退出 0（S6 断言 JSON 事件行形态 PASS）
    - `node web/uat/phase07.mjs` 退出 0（S4 含 S4c NEL 剥离断言 JSON 字段形态 PASS）
    - `node web/uat/phase07-b2.mjs` 退出 0（B2b 迁移后 JSON 字段断言形态 PASS；B2a/B2c/B2d 零改动保持绿）
    - `grep -n 'remote_user=alice' web/uat/phase07-b2.mjs` 退出码 1（B2b 旧键子串断言清零——JSON 字段断言取代）
    - `grep -n "includes('reason=\|includes('remote=\|includes('remote_user=" web/uat/phase05.mjs web/uat/phase07.mjs` 退出码 1（迁移面旧键 includes 断言清零；dialHello 握手错误路径（phase05.mjs:115/phase07.mjs:191）与 S4 check detail 串（phase07.mjs:555）的 reason=/remote_user= 文本显式保留、不在迁移面。另注：两条 S4 迁移目标行不在本收窄静态 grep 覆盖内——phase07.mjs:575（前导空格变体 `includes(' remote_user=alice\n')`）与 phase07.mjs:594（正则形态 `/remote=127\.0\.0\.1:\d+/.test(...)`）；该两行的硬闭合由动态判据 `node web/uat/phase07.mjs` 退出 0 承担）
    - phase02/03/04/06.mjs 各自退出 0（零改动回归证据）
    - `go test -race -count=1 ./...` 全绿
  </acceptance_criteria>
  <done>
    UAT 断言面完成 JSON 迁移（phase05 S6/phase07 S4/phase07-b2 B2b）；回归面以 08-05 Task 3 显式枚举清单为准，未触及脚本零回归；-race 全量绿；D-13 原子迁移全链闭合（零遗留消费者）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端 → WS/HTTP（remote/remote_user 值来源） | 用户可控输入经事件字段进 stderr → journald 持久化——日志注入信任边界 |
| wesh → stderr/journald | 事件流是审计唯一事实源——格式完整性即审计完整性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01a | Tampering | 日志注入伪造事件行（C1/NEL 经 XFF/auth-header 注入） | high | mitigate | 本 plan：encoding/json 转义 C0（\n→\\n）消除换行伪造成员；C1 穿透（GOROOT encode.go:1023）由 08-02 D-19 sanitize 推广闭合——本 plan 不削弱既有 remote_user sanitize（proxy.go:55-67 保持） |
| T-08-01b | Information Disclosure | 日志成为凭据库（base64 凭据/ticket/token 进 stderr→journald） | critical | mitigate | SEC-01 红线注释随 logEvent 迁至 log.go 逐字保留；auth_e2e/proxy_e2e 红线负断言子串形态逐字保留（迁移后仍全文扫描敏感串） |
| T-08-01c | Tampering | 双轨输出致审计检索分裂（部分文本部分 JSON） | medium | mitigate | D-13 原子迁移单点切换：logEvent 唯一出口内部换实现，无双轨窗口；验收 grep 锁 server.go 无遗留格式化打印 |
| T-08-SC | Tampering | npm/pip/cargo 包安装供应链 | high | mitigate | N/A——本 phase 零新外部依赖（log/slog stdlib，go.mod 不动；RESEARCH Package Legitimacy Audit 显式 N/A），无安装任务 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V7 Error Handling and Logging（审计事件完整性与机密性） | ✓ JSON 单行原子写 + 红线负断言保留 + 无双轨 |
| V5 Input Validation（用户可控字段进日志） | ◐ C0 转义本 plan 闭合；C1 剥离 08-02 D-19 闭合 |
</threat_model>

<verification>
- `go test -race -count=1 ./...` 全绿（五迁移文件 + log_test.go 新增）
- `node web/uat/phase05.mjs && node web/uat/phase07.mjs && node web/uat/phase07-b2.mjs` 退出 0（S6/S4/B2b JSON 断言形态）
- phase02/03/04/06.mjs 各自退出 0
- 启动冒烟：真实二进制 `--port 0` 启动行解析得端口（D-14 消费者零破坏）
</verification>

<success_criteria>
- 任一事件（如 message_too_big）在 stderr 产出恰一行合法 JSON，`jq -c 'select(.event=="message_too_big")'` 可检索
- captureStderr 全部既有测试在 slog 形态下继续绿（动态 writer 语义）
- 启动行/分享链接行/警告行与迁移前逐字节一致（D-14/D-16）
- 凭据/ticket/token 红线负断言逐字保留且全绿
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **OPS-08 (unclassified, unresolved)**：probe 无法从需求文本分类边界形态——本 plan 以 RESEARCH.md Validation Architecture 为验收来源（JSON 六键/动态 writer/断言迁移面均为显式验证）。人工复核项：journald 实机 ingest 与 jq 检索属部署面（README 检索示例随 08-05 落地，人工清单复核）；slog time 键时区为 UTC（stdlib 固定，不可配——D-15 无配置面的必然推论）。
</flagged_assumptions>

<notes>
## Notes / Risks

- **scope_sanity 知情接受（checker WARNING，disposition: ACCEPT，按现状执行）：** files_modified=11 为知情接受——其中 8 个文件系同一机械断言迁移变换（5 个 Go 测试文件 + 3 个 UAT 脚本，子串→JSON 字段断言同构替换）；若 Task 2 执行中出现上下文压力，可将 5 个测试文件拆两组提交。不重构 plan 结构。
</notes>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `type stderrW struct{}` + `func (stderrW) Write`（internal/server/log.go，新文件）
- `var eventLog *slog.Logger`（internal/server/log.go）
- `func emitEvent(attrs ...slog.Attr)`（internal/server/log.go——08-02 扩展字段事件的底层出口）
- `func logEvent(remote string, code websocket.StatusCode, reason string, remoteUser ...string)`（迁入 internal/server/log.go，签名不变）
- `func parseEvents(t *testing.T, captured string) []map[string]any` + `func countByEvent(evs []map[string]any, name string) int`（internal/server/log_test.go，新文件）
- `TestLogEventJSON`（internal/server/log_test.go）
- web/uat/phase05.mjs、phase07.mjs、phase07-b2.mjs 各自内联 `parseEvents(stderrText)` helper
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/08-observability/08-01-SUMMARY.md` when done
</output>
