---
phase: 08-observability
plan: 05
type: execute
wave: 5
depends_on:
  - "08-04"
files_modified:
  - web/uat/phase08.mjs
  - README.md
  - .planning/phases/08-observability/08-UAT.md
autonomous: true
requirements:
  - OPS-06
  - OPS-07
  - OPS-08

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "phase08.mjs 六场景全绿：S1 healthz 免认证+四字段+405 / S2 metrics 认证闸两态 / S3 metrics exposition 指标与 bp 固定 / S4 关停中 503 draining（--stop-timeout 3 拉宽窗口 + SIGTERM 轮询）/ S5 审计事件 JSON 行检索 / S6 控制字符剥离回归（NEL 线形探针）——覆盖 CONTEXT Discretion 授权场景矩阵全量"
    - "phase08.mjs 遵守 UAT 红线：token/凭据值只作断言材料（sensitiveTokens 闭包），detail 只打 event 名/状态码/布尔/形状；assertOutputClean 运行时自净；PHASE08_ONLY 调试过滤提交形态恒全场景"
    - "README 新增「## 运维（Phase 8）」节：/healthz 免认证是整站 Basic 闸唯一例外的明示（D-07 防例外蔓延义务）、Prometheus scrape_config basic_auth 配方与「凭据错误触发全站节流 429」明示（RESEARCH Pitfall 6）、journald+jq 检索示例（event/client_id 字段直打）、--no-auth 模式 /metrics 暴露面明示"
    - "全量六段式绿：GOROOT gofmt -l 清零 / go vet ./... / go test -race -count=1 ./... / time pnpm -C web build 且 web/dist/index.html git 零漂移（本 phase 不动 web/）/ 裸 clone 归档编译测试 / 启动冒烟（--port 0 启动行解析 + /healthz 200 + /metrics 200）"
    - "既有 UAT 脚本全量回归绿（phase02/03/04/05/06/07/07-b2 及各 dom/width/dims 变体）+ phase08.mjs 全场景"
    - "08-UAT.md 人工清单落地：真实 Prometheus scrape 复核、journald 检索复核、draining 窗口观测复核（三条 flagged assumption 的复核路由）"
  artifacts:
    - path: "web/uat/phase08.mjs"
      provides: "phase08 协议层 UAT 六场景（harness 逐字复用 phase07.mjs 骨架 + parseEvents 内联 helper）"
      contains: "PHASE08_ONLY"
    - path: "README.md"
      provides: "## 运维（Phase 8）节——健康检查/指标/结构化日志三小节"
      contains: "运维（Phase 8）"
  key_links:
    - from: "phase08.mjs S4 SIGTERM → 真实关停序列"
      to: "/healthz 503 draining（08-03 D-11）"
      via: "--stop-timeout 3 拉宽 draining 窗口（07 stop-signal flag 先例夹具思路）→ SIGTERM 立即轮询断言 503——窗口时序确定性化"
      pattern: "stop-timeout"
    - from: "README 运维节 Prometheus 配方"
      to: "/metrics basicAuth 闸（08-04 D-08）"
      via: "scrape_config basic_auth 配方 + 429 自锁明示——采集器凭据错误的全站节流后果文档化（RESEARCH Pitfall 6 明示义务）"
      pattern: "basic_auth"
---

<objective>
收口 Phase 8：phase08.mjs 协议层 UAT 六场景（端点行为 + JSON 日志行解析全链断言）、README 运维节（免认证例外/Prometheus 配方/jq 检索三面文档化）、08-UAT.md 人工清单、全量六段式与全脚本回归。

Purpose: 三个端点（/healthz、/metrics、JSON 事件流）在真实二进制上的端到端证据 + 运维文档闭环——OPS-06/07/08 三需求达成交付态。
Output: phase08.mjs 全绿 + README 运维节 + 六段式全绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-observability/08-CONTEXT.md
@.planning/phases/08-observability/08-RESEARCH.md
@.planning/phases/08-observability/08-PATTERNS.md
@.planning/phases/08-observability/08-04-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): phase08.mjs harness 落地 + S1 健康检查场景端到端（免认证/四字段/bp 固定/405）</name>
  <files>web/uat/phase08.mjs</files>
  <read_first>
    - web/uat/phase07.mjs（harness 逐字复制源：文件头纪律块 L1-25、check/skip/emittedDetails L62-75、sensitiveTokens/redactArgs L77-92、startWesh L103-145、dialHello/waitClose/waitExit L171-219、assertOutputClean L759-764、场景表与 ONLY 过滤 L766-797）
    - .planning/phases/08-observability/08-PATTERNS.md（web/uat/phase08.mjs 节——harness 复制清单七件 + JSON 事件行断言形态）
    - .planning/phases/08-observability/08-CONTEXT.md（D-07/D-09/D-10——S1 断言面来源）
  </read_first>
  <action>
    ① 新建 web/uat/phase08.mjs：逐字复制 phase07.mjs 骨架七件（文件头纪律块改写为 phase08 覆盖矩阵注释 + 红线保持；check/skip/emittedDetails；sensitiveTokens/redactArgs；startWesh；dialHello/waitClose/waitExit/collectUntilClose；assertOutputClean；场景表 + PHASE08_ONLY 过滤 + 汇总退出码）。内联 parseEvents(stderrText) helper（08-01 在 phase05/07 落地的同款自含形态：按行 split、滤 '{' 起始行、JSON.parse、失败抛带截断行内容的 Error）。
    ② S1 场景（健康检查）：(a) 无认证实例 GET /healthz → 200 + Content-Type application/json + body 四键（status=="ok"、clients 为数字、max_clients==32、session_active==true）；(b) 凭据实例无 Authorization 头 GET /healthz → 200 同形态（D-07），对照 GET / → 401（例外不蔓延）；(c) bp=/wesh 实例 GET /healthz → 200、GET /wesh/healthz → 404（无认证模式精确码，D-09）；(d) POST /healthz → 405 + Allow: GET。detail 只打状态码/布尔/键名形状。
    提交：test(08-05): phase08.mjs harness + S1 healthz scenario
  </action>
  <verify>
    <automated>time go build -o /tmp/wesh-uat/wesh ./cmd/wesh && PHASE08_ONLY=S1 node web/uat/phase08.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `PHASE08_ONLY=S1 node web/uat/phase08.mjs` 退出 0 且 S1 四组断言全 PASS
    - `grep -c 'PHASE08_ONLY' web/uat/phase08.mjs` ≥ 1（调试过滤形态与先例同款）
    - `grep -c 'assertOutputClean' web/uat/phase08.mjs` ≥ 2（定义 + 汇总调用）
    - `grep -c 'parseEvents' web/uat/phase08.mjs` ≥ 1（JSON 事件行 helper 在场）
  </acceptance_criteria>
  <done>
    phase08.mjs harness 与 S1 场景端到端绿：healthz 免认证/四字段/bp 固定/405 在真实二进制上全链断言。
  </done>
</task>

<task type="auto">
  <name>Task 2: 场景矩阵 S2-S6——metrics 两态/exposition 指标/503 draining/审计事件检索/控制字符剥离回归</name>
  <files>web/uat/phase08.mjs</files>
  <read_first>
    - web/uat/phase08.mjs（Task 1 落地的 harness 与 S1——场景追加形态）
    - web/uat/phase07.mjs（S4 凭据模式 fetch 断言形态 L319-321；S4c NEL 线形探针构造 L570-579；S7 SIGTERM 驱动真实关停序列 L677-690）
    - web/uat/phase05.mjs（S6 stall 夹具与洪水驱动形态——S3 指标数值场景的输出灌注参考）
    - .planning/phases/08-observability/08-RESEARCH.md（OQ3：--stop-timeout 3 拉宽 draining 窗口建议；Pitfall 5 NEL 探针语义）
    - .planning/phases/08-observability/08-CONTEXT.md（D-18/D-20/D-21/D-22/D-23——S5 断言面来源；D-19——S6 断言面来源）
  </read_first>
  <action>
    ① S2（metrics 认证闸两态）：凭据实例——无凭据 GET /metrics → 401、正确凭据（basicHeader 复用 phase07 形态）→ 200 且 Content-Type 含 text/plain 与 version=0.0.4；--no-auth 实例直通 200。
    ② S3（metrics exposition 指标 + bp 固定）：无认证实例双客户端 dialHello + INPUT 帧驱 /bin/cat 回显 → GET /metrics 文本解析：17 条 series 名全在场（must_haves 清单逐字）、wesh_clients_connected==2、wesh_clients_total==2、wesh_pty_output_bytes_total>0、wesh_ws_sent_bytes_total ≥ pty_output、wesh_session_active==1、wesh_build_info 行含 version="dev"；bp=/wesh 实例 /metrics 200、/wesh/metrics 404。
    ③ S4（关停中 503 draining）：spawn 加 '--stop-timeout', '3' 拉宽窗口（RESEARCH OQ3 夹具思路）→ dialHello 建连 → SIGTERM → 立即轮询 GET /healthz 断言出现 503 且 body status=="draining"（窗口内）→ waitExit 进程退出（15s 护栏，phase07 S7 先例）。
    ④ S5（审计事件 JSON 行检索）：凭据实例——错凭据打 401 → parseEvents 存在 event=="auth_failed" 且无 user/username 键；爬梯打满 429 → event=="throttled" 且 retry_after ≥1；dialHello → close → event=="attach" 与 event=="detach" 各恰 1 条、client_id 相等、detach reason=="normal"；独立实例子进程退出（-- bash -c exit 42 形态或 exit 42 命令）→ event=="session_end" 且 exit_code==42 且 duration_seconds>0 且无 signal 键。detail 只打 event 名/布尔/数值形状（token/凭据值红线保持）。
    ⑤ S6（控制字符剥离回归）：trust 实例（--auth-header X-Remote-User）+ NEL 线形探针（phase07.mjs S4c 同款 'ali\u00C2\u0085ce' 双码点上线构造）→ 事件行 JSON 解析后 remote_user=="alice"（NEL 剥离证据）；XFF 链首注入同类探针 → remote 字段值无 C0/C1/DEL rune（逐码点断言，D-19 双闸语义）；对照组（未配置 auth-header）无 remote_user 键。
    提交：test(08-05): phase08.mjs S2-S6 scenario matrix
  </action>
  <verify>
    <automated>time go build -o /tmp/wesh-uat/wesh ./cmd/wesh && node web/uat/phase08.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node web/uat/phase08.mjs` 退出 0（S1-S6 全场景 PASS）
    - 输出汇总行含 S1..S6 各场景 PASS 记录（场景表全量执行形态）
    - S4 断言含 503 与 status=="draining" 证据；S5 断言含 client_id 关联与 session_end 三字段证据；S6 断言含 NEL 剥离证据
    - assertOutputClean 自净通过（detail 无敏感值）
  </acceptance_criteria>
  <done>
    六场景矩阵全绿：端点行为与 JSON 事件流在真实二进制上端到端闭环；CONTEXT Discretion 授权场景面全覆盖。
  </done>
</task>

<task type="auto">
  <name>Task 3: README 运维节 + 08-UAT.md 人工清单 + 全量六段式与全脚本回归</name>
  <files>README.md, .planning/phases/08-observability/08-UAT.md</files>
  <read_first>
    - README.md（## 部署与配置（Phase 7）节 L250-310 风格先例——节首总览 + ### 小节分专题 + 配方代码块；认证节 Basic 闸表述区——/healthz 例外明示的交叉引用落点）
    - .planning/phases/07-deployment/07-UAT.md（人工清单结构先例）
    - .planning/phases/08-observability/08-RESEARCH.md（Pitfall 6 配方明示义务、Code Examples jq 检索示例）
    - .planning/phases/08-observability/08-CONTEXT.md（D-07 明示义务、D-14/D-15/D-16 文档口径）
  </read_first>
  <action>
    ① README.md 新增「## 运维（Phase 8）」节（部署与配置节之后，风格先例保持）：### 健康检查（/healthz）——200 四字段 JSON 示例 + 503 draining 语义 + **免认证是整站 Basic 闸唯一例外**粗体明示（探活器结构性带不了凭据 + 端点零敏感信息双前提；/metrics 不在例外内）；### 指标（/metrics）——17 series 一览表（名/类型/语义）、认证闸跟随说明（--no-auth 模式暴露面明示）、Prometheus scrape_config basic_auth 配方代码块 + **凭据错误会触发全站节流（429）致采集目标自锁**粗体明示（throttled 事件可观测，排查走日志）；### 结构化日志——事件目录表（auth_failed/throttled/attach/detach/session_start/session_end/shutdown/exit_when_empty 族）+ 字段口径（msg="event"/event/client_id/reason/retry_after/exit_code/signal/duration_seconds）+ journald+jq 检索示例两则（select(.event=="auth_failed") 与 select(.client_id==N)）+ 凭据/token 永不入日志与 remote/remote_user 控制字符剥离的红线重申 + 启动行/分享链接行保持人读文本（D-14/D-15 恒 JSON 无开关）。
    ② 新建 .planning/phases/08-observability/08-UAT.md 人工清单（07-UAT.md 结构先例）：真实 Prometheus scrape 复核（配方起实例 → target up → series 可见）；journald 实机检索复核（systemd 部署下 jq 两示例）；draining 窗口观测复核（systemd restart 场景健康检查翻转）；标注 Linux 侧无浏览器（CODEBUDDY.md 拓扑），本 phase 无浏览器层人工项。
    ③ 全量六段式：(a) GOROOT gofmt -l 清零（纯排版差异 -w 修正后独立 style 提交先例；/usr/bin/gofmt 陈旧须用 GOROOT 版）；(b) go vet ./...；(c) go test -race -count=1 ./...；(d) time pnpm -C web build 且 git diff --exit-code web/dist/index.html（本 phase 不动 web/——零漂移证明）；(e) 裸 clone 归档编译测试（mktemp 归档 → go build + go test 计数比对先例）；(f) 启动冒烟：真实二进制 --port 0 启动行解析端口 → curl /healthz 200 四字段 → curl /metrics 200 含 17 series → SIGTERM 退出干净。
    ④ 全脚本回归：phase02/03/04/04-dom/04-t1-width/05/05-dom/05-dims/06/06-dom/07/07-b2/08.mjs 全部退出 0（13 脚本）。
    提交：docs(08-05): README observability section, 08-UAT checklist, full regression
  </action>
  <verify>
    <automated>go vet ./... && go test -race -count=1 ./... && time pnpm -C web build && git diff --exit-code web/dist/index.html && time go build -o /tmp/wesh-uat/wesh ./cmd/wesh && for s in phase02 phase03 phase04 phase05 phase06 phase07 phase07-b2 phase08; do node web/uat/$s.mjs || exit 1; done && for s in phase04-dom phase04-t1-width phase05-dom phase05-dims phase06-dom; do node web/uat/$s.mjs || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## 运维（Phase 8）' README.md` == 1 且三小节标题在场（健康检查/指标/结构化日志）
    - `grep -c '唯一例外' README.md` ≥ 1（D-07 明示义务落文）且 `grep -c 'basic_auth' README.md` ≥ 1（Prometheus 配方落文）且 `grep -c 'select(.event' README.md` ≥ 1（jq 示例落文）
    - `.planning/phases/08-observability/08-UAT.md` 存在且含 Prometheus/journald/draining 三复核项
    - 六段式全绿（gofmt -l 输出为空 / vet 0 / -race 全绿 / web build 后 index.html 零 diff / 裸 clone 编译测试通过 / 冒烟三断言）
    - 全部 UAT 脚本（协议八 phase02..08 含 phase07-b2 + dom/width/dims 五，共 13）退出 0
  </acceptance_criteria>
  <done>
    README 运维节三面文档化（例外明示/配方/检索）；08-UAT.md 人工清单落地；六段式与全脚本回归全绿——Phase 8 交付态。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 运维文档读者 → 部署面 | README 配方是部署行为入口——配方错误（缺 basic_auth/误开放 metrics）直接造成暴露面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05a | Information Disclosure | UAT 脚本 detail 泄漏 token/凭据值进 CI 日志 | high | mitigate | sensitiveTokens 闭包 + redactArgs + assertOutputClean 运行时自净（phase07 先例逐字沿用）；S5 detail 只打 event 名/布尔/数值形状 |
| T-08-05b | Information Disclosure | README 配方误导致 /metrics 公开暴露 | medium | mitigate | basic_auth 配方与 --no-auth 暴露面同节明示；D-07 例外范围限定 /healthz 单端点的防蔓延表述 |
| T-08-05c | Denial of Service | 采集器凭据错误自锁（429 打 Prometheus） | medium | mitigate | README 配方节粗体明示后果与排查通道（throttled 事件 retry_after 字段，08-02 落地）——RESEARCH Pitfall 6 明示义务兑现 |
| T-08-SC | Tampering | npm/pip/cargo 包安装供应链 | high | mitigate | N/A——本 plan 零新依赖（UAT 脚本 Node 原生 fetch/WebSocket 零依赖纪律） |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V7 Logging（文档与测试面红线保持） | ✓ UAT 自净 + README 红线重申 |
| V2 Authentication（例外边界文档化） | ✓ D-07 单端点例外明示 |
</threat_model>

<verification>
- 六段式全绿（见 Task 3 acceptance）
- 全 UAT 脚本（协议八 phase02..08 含 phase07-b2 + dom/width/dims 五，共 13）退出 0
- phase08.mjs 六场景全 PASS
</verification>

<success_criteria>
- ROADMAP Phase 8 三条 Success Criteria 全成立：/healthz 探活可用（S1/S4）、/metrics 五类指标暴露（S2/S3）、JSON 审计日志可检索且凭据零入日志 + 控制字符剥离（S5/S6）
- OPS-06/07/08 三需求达成交付态，REQUIREMENTS.md 勾选随 phase 完成流程更新
- 三条 flagged assumption（真实 scrape/journald/draining 观测率）有 08-UAT.md 复核路由
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **OPS-06/07/08 (unclassified, unresolved)**：三条 probe 未分类行的汇总路由——真实 Prometheus scrape 兼容性（无实例，exposition 文本断言代替）、journald 实机 ingest 与 jq 检索、draining 窗口编排观测率，均落 08-UAT.md 人工清单复核（Task 3 落地）；除此外本 phase 全部验收均为自动化显式验证（Go 单测 + 协议层 UAT）。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `web/uat/phase08.mjs`（新文件：六场景 S1-S6 + 内联 parseEvents + PHASE08_ONLY 过滤）
- README.md「## 运维（Phase 8）」节（### 健康检查（/healthz）/ ### 指标（/metrics）/ ### 结构化日志）
- `.planning/phases/08-observability/08-UAT.md`（新文件：三人工复核项）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/08-observability/08-05-SUMMARY.md` when done
</output>
