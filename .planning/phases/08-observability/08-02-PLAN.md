---
phase: 08-observability
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - internal/server/server.go
  - internal/server/clients.go
  - internal/server/auth.go
  - internal/server/proxy.go
  - internal/server/events_test.go
  - internal/server/proxy_test.go
  - internal/server/exitmsg_test.go
  - web/uat/phase05.mjs
autonomous: true
requirements:
  - OPS-08

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "事件目录全量（D-17）：认证（auth_failed/throttled）+ 连接（attach/detach）+ 会话生命周期（session_start/session_end/shutdown）+ exit_when_empty 族（wait/cancel/fire 现状事件名沿用）——ROADMAP 三面全覆盖"
    - "attach/detach 事件携 client_id（= c.attachSeq，进程内单调递增从 1 起，D-20）——同一连接的 attach/detach 事件流可按 client_id 关联检索"
    - "client 断开 = detach 单事件 + reason 字段四值（normal/kick/pong_timeout/shutdown，D-21）；kick/pong_timeout 不再单独打事件行；wire 关闭码与 close reason（1013 slow_consumer 等客户端可见形态）逐字节不变"
    - "detach reason 传递 -race 干净：kick 在 hubMu 内天然同步就地 emit；pong_timeout 由 pinger 取 hubMu 写 client 字段、detach 同锁读（禁止 plain 字段跨 goroutine 传递）；shutdown 复用 s.exiting 判定 + closeBroadcastCode（lifecycle 置 1000 / Shutdown 置 1001）"
    - "session_end 字段 = exit_code（与 EXIT 帧同源，信号死亡 -1）+ signal 名（仅信号死亡且 signalName 映射命中出键）+ duration_seconds（PTY spawn 到退出，D-22）；信号提取抽包级 helper 与 exitMessage 共用（单侧定义）"
    - "throttled 事件携 retry_after 秒数（auth.go 现成 retry 值，D-23）；auth_failed 事件不含用户名（SEC-01 红线结构性保持：logEvent 四参签名根本无用户名通道）"
    - "trust 模式 remote 字段（XFF 链首）过 sanitizeRemoteUser 同款清洗（D-19 纵深第二道——ParseIP 第一道闸保持，两道并存）"
    - "全量 go test -race 绿；phase05.mjs S6 完成二次迁移（detach reason=kick JSON 断言）"
  artifacts:
    - path: "internal/server/events_test.go"
      provides: "TestAttachDetachEvents/TestDetachReason（四 reason 子测）/TestSessionEnd/TestShutdownEvent/TestThrottledRetryAfter/TestAuthFailedNoUsername"
      contains: "TestDetachReason"
    - path: "internal/server/server.go"
      provides: "attach 事件打点 + pinger 换 *client 签名 + closeBroadcastCode/startedAt 字段 + session_start/session_end/shutdown 打点"
      contains: "closeBroadcastCode"
    - path: "internal/server/clients.go"
      provides: "detach 单事件 emit + client.pongTimedOut 字段 + kick 路径 detach reason=kick emit"
      contains: "pongTimedOut"
  key_links:
    - from: "pinger（pong_timeout 置位）"
      to: "detach() reason 读取"
      via: "pinger 取 hubMu 写 cl.pongTimedOut → CloseNow → reader 错误 → detach 同锁读——Go 内存模型同步边 = hubMu（RESEARCH Pattern 4 形态 b）"
      pattern: "pongTimedOut"
    - from: "lifecycle sess.Wait 返回 / Shutdown 入口"
      to: "detach() reason=shutdown 判定"
      via: "s.exiting 置位先于关闭广播（既有不变量），detach 在 hubMu 内读 exiting + closeBroadcastCode"
      pattern: "closeBroadcastCode"
    - from: "exitMessage（server.go）信号提取逻辑"
      to: "session_end signal 字段"
      via: "抽取包级 exitSignalNum helper 两消费点共用——信号死亡判定单侧定义"
      pattern: "exitSignalNum"
  prohibitions:
    - statement: "auth_failed 事件 MUST NOT 含用户名/凭据任何形态（SEC-01 红线：凭据任何形态含用户名永不入日志）"
      status: resolved
      verification: test
---

<objective>
在 08-01 落地的 slog 基座上补全 D-17 审计事件目录：新增 attach/detach（含 client_id 与 reason 四值）/session_start/session_end/shutdown 五个进程与连接事件；kick 与 pong_timeout 两现行独立事件行折入 detach reason（D-21）；throttled 事件携 retry_after（D-23）；trust 模式 remote 字段过 sanitize（D-19）。exit_when_empty 族与 auth_failed 等 logEvent 三要素事件名沿用零改动（D-17）。

Purpose: 「认证失败、连接建立/断开、会话生命周期」三面可检索（ROADMAP SC3）——同一连接的 attach→detach 经 client_id 关联，会话「活多久、怎么死的」单事件齐备。
Output: 事件目录全量上线 + events_test.go 行为锁 + phase05.mjs S6 终态迁移。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-observability/08-CONTEXT.md
@.planning/phases/08-observability/08-RESEARCH.md
@.planning/phases/08-observability/08-PATTERNS.md
@.planning/phases/08-observability/08-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): attach/detach 事件链端到端——client_id 关联 + reason 四值（normal/kick/pong_timeout/shutdown）+ phase05.mjs S6 终态迁移</name>
  <files>internal/server/server.go, internal/server/clients.go, internal/server/events_test.go, web/uat/phase05.mjs</files>
  <read_first>
    - internal/server/log.go（08-01 落地的 emitEvent 底层出口与 logEvent——本任务新事件的唯一 emit 通道）
    - internal/server/server.go（Attach 升档序列 L805-876——emit 插入点；pinger L1012-1045 签名与现行超时事件行；lifecycle exiting 置位 L1192-1198；Shutdown L1264-1271）
    - internal/server/clients.go（client struct 字段区、detach L690-713、kickSlowConsumerLocked L500-526——现行踢出事件行 L506 与 wire Close L523、registerLocked attachSeq 分配 L273-282）
    - internal/server/keepalive_test.go（pinger 测试夹具形态——短 interval/pongTimeout Options 覆写）
    - internal/server/slowclient_test.go（stall 夹具 assertKicked1013——wire 1013 断言保持）
    - internal/server/shutdown_test.go（startShutdownServerWith 夹具——srv 句柄直调 Shutdown 形态）
    - .planning/phases/08-observability/08-RESEARCH.md（Pattern 4 detach reason 三机制表 + Pitfall 2 数据竞争实证）
  </read_first>
  <behavior>
    - dialHello 成功 → 恰 1 条 event=="attach"：remote/client_id（=1 起单调递增）/mode（ro|rw，RESEARCH A6 登记的增强字段）键齐备，无 code 键；remote_user 非空才出键
    - 客户端主动关闭 → 恰 1 条 event=="detach"：reason=="normal"、code==1000、client_id 与该连接 attach 事件同值（关联检索锁）
    - stall 客户端 outbox 写满被踢 → 恰 1 条 event=="detach"：reason=="kick"、code==1013；wire 关闭帧 CloseError{Code:1013, Reason:"slow_consumer"} 逐字节不变（slowclient_test.go 既有断言不動）；不再有独立踢出事件行
    - stall 端不答 pong → 恰 1 条 event=="detach"：reason=="pong_timeout"、code==1006；pinger 内现行事件行删除
    - srv.Shutdown() → 已注册客户端 detach 事件 reason=="shutdown"、code==1001；lifecycle 子进程退出广播窗口的 detach reason=="shutdown"、code==1000
    - 恰好一次：kick 与 reader detach 互斥——removeLocked 返回 true 的路径 emit（既有成员判定所有权规则），同连接绝无两条 detach
  </behavior>
  <action>
    ① internal/server/clients.go：client struct 加 pongTimedOut bool 字段（注释：hubMu 保护——pinger 置位取锁、detach 同锁读，RESEARCH Pattern 4 形态 b；-race 防线）。detach() 在 removeLocked 返回 true 之后、close(c.done) 之前插入 detach 事件 emit：reason 判定序 = c.pongTimedOut → "pong_timeout"（code 1006）；s.exiting → "shutdown"（code 取 s.closeBroadcastCode）；否则 "normal"（code 1000）；经 emitEvent 发 event=="detach" + remote/client_id/code/reason（remote_user 非空追加）。kickSlowConsumerLocked 内 removeLocked true 后，将现行踢出事件行（L506）替换为同款 detach emit（reason=="kick"、code==1013）——wire 的 Close(1013, "slow_consumer") 调用与 kicks 计数逐字不动。
    ② internal/server/server.go：Server struct 加 closeBroadcastCode int 字段（hubMu 保护，0=未广播；lifecycle L1198 区 exiting 置位同点置 1000，Shutdown L1266 区同点置 1001）。pinger 签名改为 (ctx context.Context, cl *client, interval time.Duration)——体内 c.Ping/c.CloseNow 改经 cl.conn；DeadlineExceeded 分支：删去现行超时事件行，改为 s.hubMu.Lock() 置 cl.pongTimedOut=true、Unlock 后 cl.conn.CloseNow()；调用点（Attach 升档序列 go s.pinger(...)）改传 cl，remote/remoteUser 参数随签名删除（pinger 注释块同步改写：pong_timeout 不再单独打行，折入 detach reason，D-21）。Attach 升档序列 hubMu.Unlock() 之后、SetReadLimit 之前插入 attach 事件 emit（emitEvent：event=="attach" + cl.remote/cl.attachSeq/effMode（mode 键）+ cl.remoteUser 非空追加）——同一 goroutine 内 registerLocked 的 attachSeq 写 happens-before 本读。
    ③ internal/server/events_test.go（新文件）：TestAttachDetachEvents（dialHello → close → parseEvents：attach/detach 各恰 1 条、client_id 相等、detach reason=="normal" 且 code==1000）；TestDetachReason 四子测（normal：客户端主动 close；kick：复用 stall 洪水夹具驱 outbox 写满；pong_timeout：Options 覆写短 PingInterval/PongTimeout + stall 不读；shutdown：startShutdownServerWith 变体直调 srv.Shutdown()——各断言 reason/code/client_id 关联，同步边 waitHandlers 纪律保持）；注释头登记 D-17/D-20/D-21。
    ④ web/uat/phase05.mjs S6 二次迁移：08-01 中间形态断言（event=="slow_consumer"）改为「10s 内出现 event=="detach" 且 reason=="kick" 且 code==1013 的 JSON 事件行」；客户端侧 1013 关闭帧断言逐字不动。
    提交：feat(08-02): audit event catalog — attach/detach with client_id and reason
  </action>
  <verify>
    <automated>go test ./internal/server/ -race -count=1 -run 'TestAttachDetachEvents|TestDetachReason|TestPingKeepalive|TestSlowConsumerKick|TestSuccessionKickRace|TestShutdown' -v && go test ./internal/server/ -count=1 && time go build -o /tmp/wesh-uat/wesh ./cmd/wesh && node web/uat/phase05.mjs</automated>
  </verify>
  <acceptance_criteria>
    - TestAttachDetachEvents 与 TestDetachReason 四子测全 PASS（-race 干净）
    - `grep -c 'pongTimedOut' internal/server/clients.go` ≥ 2（字段声明 + detach 读取）且 `grep -c 'pongTimedOut' internal/server/server.go` ≥ 1（pinger 置位）
    - `grep -c 'logEvent(remote, websocket.StatusAbnormalClosure' internal/server/server.go` == 0（pinger 独立事件行已除）
    - `grep -c 'logEvent(c.remote, websocket.StatusTryAgainLater' internal/server/clients.go` == 0（kick 独立事件行已除）
    - `grep -c 'Close(websocket.StatusTryAgainLater, "slow_consumer")' internal/server/clients.go` == 1（wire 关闭形态逐字保持）
    - `node web/uat/phase05.mjs` 退出 0（S6 detach reason=kick 断言 PASS）
    - TestPingKeepalive/TestSlowConsumerKick/TestSuccessionKickRace/TestShutdown1001 全 PASS（既有行为零回归）
  </acceptance_criteria>
  <done>
    attach/detach 事件链落地：client_id 关联可检索；reason 四值行为锁定且 -race 干净；wire 关闭形态零漂移；phase05.mjs S6 终态绿。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 会话生命周期事件——startedAt + session_start/session_end/shutdown 打点 + exitSignalNum 单侧定义抽取</name>
  <files>internal/server/server.go, internal/server/events_test.go, internal/server/exitmsg_test.go</files>
  <read_first>
    - internal/server/server.go（New 尾部 goroutine 启动区 L358-368——session_start 与 startedAt 挂点；lifecycle L1167-1226——exit code 提取 L1168-1173 与 EXIT 帧组帧；exitMessage L1141-1162 信号提取内联 L1154-1157；signalName L1099-1129；Shutdown L1264-1286）
    - internal/server/exitmsg_test.go（exitMessage 白盒行为锁——exitSignalNum 抽取后行为逐字节回归）
    - internal/server/exit_test.go（exitf 桩实例夹具——子进程 exit 42 / kill -HUP 两形态）
    - .planning/phases/08-observability/08-RESEARCH.md（Code Examples「session_end 字段提取」节 + A7 假设）
  </read_first>
  <behavior>
    - server.New 完成 → 恰 1 条 event=="session_start"：pid 键 = sess.Cmd.Process.Pid（数字）；无 remote/code 键（进程级事件）
    - 子进程 exit 42 → 恰 1 条 event=="session_end"：exit_code==42、duration_seconds>0、无 signal 键
    - 子进程被 kill -HUP → 恰 1 条 event=="session_end"：exit_code==-1、signal=="SIGHUP"、duration_seconds>0（signal 键仅信号死亡且 signalName 映射命中出键——未命中不出键，类型恒 string，A7 裁决）
    - srv.Shutdown() 入口 → 恰 1 条 event=="shutdown"（进程级事件，无 remote/code 键）
    - exitMessage 行为逐字节不变（exitmsg_test.go 既有全绿）——exitSignalNum 抽取是纯重构
  </behavior>
  <action>
    ① internal/server/server.go：Server struct 加 startedAt time.Time 字段（注释：New 尾部记录，与 pty.Start 时刻差毫秒级审计语义无碍，D-22 duration 数据源）。抽取包级 helper exitSignalNum(err error) (int, bool)——exitMessage 的 WaitStatus 信号提取内联逻辑（L1154-1157）迁入，exitMessage 改调本 helper（行为逐字节不变，单侧定义纪律）；exitmsg_test.go 补 exitSignalNum 表白盒行（ExitError 信号形态命中/非信号退出未命中/非 ExitError 未命中）。New 尾部 goroutine 启动之前记录 startedAt = time.Now() 并 emitEvent(event=="session_start" + pid=sess.Cmd.Process.Pid)。
    ② lifecycle 内 sess.Wait 返回与 exit code 提取完成之后（EXIT 帧组帧之前）插入 session_end emit：emitEvent(event=="session_end" + exit_code=code + duration_seconds=time.Since(s.startedAt).Seconds()；exitSignalNum(err) 命中且 signalName 命中时追加 signal 键）。Shutdown 入口（hubMu 锁定前）插入 emitEvent(event=="shutdown")——与 draining 置位（08-03）同函数不同点，本任务只加事件。
    ③ internal/server/events_test.go：TestSessionEnd 两子测（exit 42 形态：exitf 桩实例跑真子进程 `sh -c 'exit 42'` 等价物——用 exit_test.go 既有 helperArgv 形态；kill -HUP 形态：TestHelperProcess sighup 分支先例）；断言 session_start（pid 键存在且与 sess.Cmd.Process.Pid 相等）+ session_end 三字段；captureStderr + parseEvents + waitHandlers 同步纪律。TestShutdownEvent：startShutdownServerWith 变体 → srv.Shutdown() → parseEvents 恰 1 条 event=="shutdown"。
    提交：feat(08-02): session lifecycle events — session_start/session_end/shutdown
  </action>
  <verify>
    <automated>go test ./internal/server/ -race -count=1 -run 'TestSessionEnd|TestShutdownEvent|TestExitMessage|TestExit' -v && go test ./internal/server/ -count=1 && go vet ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - TestSessionEnd 两子测 PASS（exit 42：exit_code==42 无 signal 键；HUP：exit_code==-1 signal=="SIGHUP"；duration_seconds>0）
    - TestShutdownEvent PASS（恰 1 条 event=="shutdown"）
    - exitmsg_test.go 全绿（exitMessage 行为逐字节回归 + exitSignalNum 白盒行）
    - `grep -c 'exitSignalNum' internal/server/server.go` ≥ 2（helper 定义 + exitMessage 消费）且 `grep -c 'func exitSignalNum' internal/server/server.go` == 1
    - `go test ./internal/server/ -count=1` 全绿（session_start 事件混入既有捕获面后 parseEvents 计数断言零漂移——本验收即其回归证据）
  </acceptance_criteria>
  <done>
    会话生命周期三面事件落地：session_start（pid）/session_end（exit_code/signal/duration_seconds）/shutdown；exitSignalNum 单侧定义；既有测试零回归。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 认证事件边界——throttled 携 retry_after + auth_failed 无用户名回归锁 + remote 字段 sanitize 推广（D-19/D-23）</name>
  <files>internal/server/auth.go, internal/server/proxy.go, internal/server/events_test.go, internal/server/proxy_test.go</files>
  <read_first>
    - internal/server/auth.go（basicAuth L101-123——throttled 站点 L104-110 retry 现成值 L106；红线注释 L89-100 保持）
    - internal/server/proxy.go（sanitizeRemoteUser L55-67 复用实现、clientIP ParseIP 闸 L89-103、remote L108-113——D-19 挂点；注释头 L30-39 红线）
    - internal/server/auth_e2e_test.go（爬梯 sleep 驱动 429 的既有场景形态——TestThrottledRetryAfter 复用）
    - internal/server/proxy_test.go（白盒表驱动先例）
    - .planning/phases/08-observability/08-RESEARCH.md（Pitfall 5：C1 穿透实证——sanitize 是唯一防线；D-19 纵深两道论证）
  </read_first>
  <behavior>
    - 凭据实例爬梯打满节流 → 429 时恰 1 条 event=="throttled"：retry_after 键为 ≥1 整数（= auth.go 现 retry 值，与 Retry-After 响应头同值）；remote/code==429 键保持
    - 错凭据（独特用户名）→ event=="auth_failed" 事件无 user/username 键，且事件行全文不含该用户名串（SEC-01 红线 JSON 形态回归锁）
    - trust 模式 remote() 返回值经 sanitizeRemoteUser 清洗（D-19 纵深第二道；ParseIP 第一道闸保持）——白盒属性断言：任意构造 XFF 输入下 remote() 返回值不含 C0/C1/DEL rune
  </behavior>
  <action>
    ① internal/server/auth.go：throttled 站点（L104-110）的 logEvent 调用替换为 emitEvent——event=="throttled" + remote + code==429 + retry_after=retry（L106 现成值，slog.Int64）+ remote_user 非空追加；auth_failed 站点保持 logEvent 零改动（四参签名结构性无用户名通道——红线注释保持并在注释中点名 D-23）。
    ② internal/server/proxy.go：remote() 的 trust 分支返回值改经 sanitizeRemoteUser(p.clientIP(r))（非 trust 分支 r.RemoteAddr 内核源值保持原样）；注释更新：D-19 推广落点 + 纵深两道论证（ParseIP 第一道结构性闸 + sanitize 第二道防取值路径放宽，RESEARCH Pitfall 5）；注释头红线块不动。
    ③ internal/server/events_test.go：TestThrottledRetryAfter（凭据实例 + 爬梯 sleep 驱动 429——auth_e2e 既有场景形态；parseEvents 断言 event=="throttled" 恰 1 条、retry_after 按 float64 比 ≥1、与响应 Retry-After 头同值）；TestAuthFailedNoUsername（错凭据独特用户名 "no-such-user-7f3a" → auth_failed 事件无 user/username 键 + 捕获全文不含该用户名串）。
    ④ internal/server/proxy_test.go：TestRemoteSanitize 白盒表驱动——proxyInfo{trust:true} 下：XFF 干净值透传相等；构造含 C1/NEL 的 XFF 首段与含 C1 的边界输入，属性断言 remote() 返回值逐 rune 无 ch<=0x1f / ch==0x7f / 0x80<=ch<=0x9f（注释登记：ParseIP 闸使注入值回落到 TCP 对端键，sanitize 是纵深第二道——两闸并存不冲突）。
    提交：feat(08-02): throttled retry_after, auth_failed username red-line, remote sanitize
  </action>
  <verify>
    <automated>go test ./internal/server/ -race -count=1 -run 'TestThrottledRetryAfter|TestAuthFailedNoUsername|TestRemoteSanitize|TestAuth|TestProxy' -v && go test ./internal/server/ -count=1 && go test -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - TestThrottledRetryAfter PASS（retry_after ≥1 且与 Retry-After 响应头同值）
    - TestAuthFailedNoUsername PASS（无 user/username 键 + 全文不含用户名字符串）
    - TestRemoteSanitize PASS（属性断言全表行无 C0/C1/DEL rune）
    - `grep -c 'sanitizeRemoteUser(p.clientIP(r))' internal/server/proxy.go` == 1（D-19 挂点唯一）
    - `grep -c 'retry_after' internal/server/auth.go` ≥ 1（事件字段落点；注释提及不计——以 attrs 构造行断言为准复核）
    - `go test -race -count=1 ./...` 全绿（auth_e2e/proxy_e2e 既有迁移断言零回归）
  </acceptance_criteria>
  <done>
    D-23 字段边界与 D-19 sanitize 推广落地：throttled 携 retry_after、auth_failed 无用户名结构性锁定、remote 字段双闸清洗；全量 -race 绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端 → XFF/auth-header（remote/remote_user 来源） | 反代追加式语义下客户端可注入首段——事件字段信任边界 |
| pinger goroutine → detach（reason 传递） | 跨 goroutine 写读——无同步边即数据竞争（-race 高危面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02a | Tampering | 日志注入伪造事件行（C1/NEL 经 XFF 首段注入 remote 字段） | high | mitigate | D-19：remote() trust 分支过 sanitizeRemoteUser（C0/C1/DEL 剥离 + 截断）——encoding/json 只转义 C0，清洗是唯一防线（RESEARCH Pitfall 5 GOROOT 实证）；TestRemoteSanitize 属性断言锁定 |
| T-08-02b | Information Disclosure | auth_failed 事件含用户名 → journald 持久化凭据库 | critical | mitigate | logEvent 四参签名结构性无用户名通道（D-23）；TestAuthFailedNoUsername 行为锁（无 user 键 + 全文不含用户名串） |
| T-08-02c | Tampering | pinger→detach plain 字段跨 goroutine 传递致数据竞争 | high | mitigate | RESEARCH Pattern 4 形态 b：pinger 取 hubMu 写 cl.pongTimedOut、detach 同锁读——Task 1 -race 验收强制 |
| T-08-02d | Information Disclosure | client_id 序号泄漏连接计数侧信道 | low | accept | D-20 已裁决：进程内序号无隐私面（非 IP），重启归零；关联检索收益大于侧信道 |
| T-08-SC | Tampering | npm/pip/cargo 包安装供应链 | high | mitigate | N/A——本 plan 零新依赖（stdlib only，go.mod 不动） |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V5 Input Validation（用户可控字段清洗） | ✓ remote 字段 D-19 双闸（ParseIP + sanitize） |
| V7 Error Handling and Logging（审计归因与红线） | ✓ client_id 关联 + auth_failed 无用户名 + retry_after |
</threat_model>

<verification>
- `go test -race -count=1 ./...` 全绿（detach reason 传递、pinger 签名变更、session 事件混入捕获面全回归）
- `node web/uat/phase05.mjs` 退出 0（S6 终态 detach reason=kick）
- phase07.mjs 退出 0（S4 零改动回归）
</verification>

<success_criteria>
- `journalctl -u wesh -o cat | jq -c 'select(.client_id==7)'` 形态可关联单连接 attach/detach 全事件流（README 示例随 08-05）
-  kick/pong_timeout 场景 stderr 无独立事件行，detach 单事件 reason 可检索
- session_end 单事件回答「会话活了多久、怎么死的」（exit_code/signal/duration_seconds）
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **OPS-08 (unclassified, unresolved)**：与 08-01 同条登记——本 plan 的事件字段集（attach 携 mode 属 RESEARCH A6 增强、signal 未命中不出键属 A7 裁决）以 RESEARCH.md 假设登记表为准，均为低风险且不影响任何锁定验收；人工复核项：事件名与字段名的最终检索体验随 08-05 README jq 示例人工清单复核。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `client.pongTimedOut bool` 字段（internal/server/clients.go，hubMu 保护）
- `Server.closeBroadcastCode int` / `Server.startedAt time.Time` 字段（internal/server/server.go）
- `func exitSignalNum(err error) (int, bool)`（internal/server/server.go——exitMessage 与 session_end 共用）
- pinger 新签名 `func (s *Server) pinger(ctx context.Context, cl *client, interval time.Duration)`（internal/server/server.go）
- 事件 schema：attach{remote,remote_user?,client_id,mode} / detach{remote,remote_user?,client_id,code,reason} / session_start{pid} / session_end{exit_code,signal?,duration_seconds} / shutdown{} / throttled 增 retry_after 键
- `TestAttachDetachEvents` / `TestDetachReason` / `TestSessionEnd` / `TestShutdownEvent` / `TestThrottledRetryAfter` / `TestAuthFailedNoUsername`（internal/server/events_test.go，新文件）
- `TestRemoteSanitize`（internal/server/proxy_test.go）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/08-observability/08-02-SUMMARY.md` when done
</output>
