---
phase: 05-multi-client
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/proto/proto.go
  - internal/proto/proto_test.go
  - internal/pty/spawn.go
  - internal/server/resize.go
  - internal/server/server.go
  - internal/server/clients.go
  - internal/server/clients_test.go
  - internal/server/multi_test.go
  - internal/server/resize_arb_test.go
autonomous: true
requirements: [MULTI-01, MULTI-04]
gap_closure: true
gap_ids: [G-05-1]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "attach Welcome 恒携会话 cols/rows，且数值 = 该客户端 attach 完成后生效的会话尺寸（参与端为重算后 min-rect/last-wins；旁观端为当前会话尺寸；无参与者的会话回落 spawn 80x24）"
    - "owner 窗口 resize 经 50ms 防抖重算后，全部在线客户端（含 ro 旁观者与上报者自身）收到携新会话尺寸的 'W' 推送"
    - "递补升格 Welcome 携新会话尺寸（owner 模式参与集 = {新 owner} 单员，故恒等于其 Hello 登记尺寸 cand.dims）"
    - "尺寸变化检测唯一挂点 = recalcNow 的 last 变化分支——目标尺寸不变时零推送（无放大无循环）"
    - "既有行为零回归：S1b 双端逐字节一致（广播路径不动）、ro 端零 RESIZE 上行双闸不动、升格 PTY 尺寸跟随不动"
  artifacts:
    - "internal/proto/proto.go：WelcomePayload 增 Cols/Rows（恒序列化，无 omitempty）；WelcomeFrame 签名携 cols/rows"
    - "internal/pty/spawn.go：SpawnCols=80/SpawnRows=24 导出常量（StartWithSize 引用，消除魔法数双写）"
    - "internal/server/resize.go：sessionDimsLocked()（last 零值回落 spawn 尺寸）+ pushSessionDimsLocked(target) 挂进 recalcNow"
    - "internal/server/server.go：attach 升档序列重排（addMember/recalcNow 前移至 Welcome 组帧之前）"
    - "internal/server/clients.go：promoteNextLocked 升格 Welcome 携 cand.dims"
    - "multi_test.go TestWelcomeSessionDims + resize_arb_test.go 推送子测 + resize_test.go sessionDimsLocked 白盒单测"
  key_links:
    - "recalcNow 的 last 更新点 → pushSessionDimsLocked（attach/detach/kick/升格/防抖五调用点全覆盖，单挂点）"
    - "attach：addMember+recalcNow（hubMu 内）→ sessionDimsLocked 取值 → Welcome 组帧入队 → registerLocked（Welcome 恒首帧不变量保持）"
    - "会话尺寸数据流：arbiter.last（recalcNow 维护）→ Welcome 载荷 → 前端 sessionDims（05-11 消费）"
---

<objective>
G-05-1 服务端半侧：修复 D-09「min-rect 不变量无需 S→C 尺寸下发」假设对相对寻址流（readline 行编辑按终端宽度生成环绕点/光标上行）不成立导致的异尺寸双端叠写。按用户裁决方向 A（2026-08-22）：Welcome 帧 JSON 载荷增带会话 cols/rows（加键 = P2 D-02 协议兼容增量），运行期尺寸变化复用 'W' 帧再推送（P2 D-01/D-02 纪律：既有帧类型运行期再推送不算动协议，零新类型字节；clients.go:497 升格推送先例）。

Purpose: 让每一端在任何时刻都知道当前会话尺寸（= PTY 目标尺寸），为 05-11 前端视口约束提供数据源——同 cols 渲染同字节流 = 两端逐屏严格一致。
Output: Welcome 三通道（attach/升格/运行期推送）全部携带会话尺寸；Go 行为测试组锁定。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-client/05-UAT.md

@internal/proto/proto.go
@internal/server/resize.go
@internal/server/clients.go
@internal/server/server.go
@internal/pty/spawn.go
@internal/server/e2e_test.go
@internal/server/multi_test.go
@internal/server/resize_arb_test.go
@internal/server/resize_test.go
</context>

<interfaces>
<!-- 跨 plan 契约（05-11/05-12 依赖，禁止漂移）：
- Welcome JSON 载荷新增两个恒在键：{"mode":"ro"|"rw","cols":N,"rows":N,"prefs":{...}?}
  cols/rows = 当前会话尺寸（服务端仲裁器产出，已 ClampDim [1,1000]，恒为正整数）。
- 三通道同形：attach Welcome（升档）/ 升格 Welcome（promoteNextLocked）/ 运行期推送 Welcome（recalcNow 尺寸变化时广播，每客户端按其当前生效 mode 与对应 prefs 档组帧）。
- 旧前端忽略未知键零漂移；新前端对缺席尺寸键的旧服务端不约束渲染（双向兼容）。
- pty 包导出：SpawnCols = 80, SpawnRows = 24（spawn 尺寸单一事实源）。 -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Welcome 契约增量（proto cols/rows + pty spawn 常量 + sessionDimsLocked）与三处组帧点原子适配</name>
  <files>internal/proto/proto.go, internal/proto/proto_test.go, internal/pty/spawn.go, internal/server/resize.go, internal/server/server.go, internal/server/clients.go, internal/server/clients_test.go</files>
  <reversibility rating="reversible">Welcome 增键是 P2 D-02 加字段兼容增量——旧前端忽略未知键、新前端容忍缺席键，两个方向都可回退</reversibility>
  <action>
    Go 包级编译是原子单位，本任务一次性完成签名变更与全部调用点适配（G-05-1 方向 A 的契约层）：

    1. proto.go：
       - WelcomePayload 增 `Cols int \`json:"cols"\`` 与 `Rows int \`json:"rows"\``——刻意不加 omitempty：会话尺寸恒在（含 80x24 回落），新前端靠「缺席 = 旧服务端」识别遗留形态；注释登记 G-05-1 根因（D-09 min-rect 不变量只覆盖绝对寻址流与纯文本流，不覆盖 readline 行编辑等相对寻址流——40 列 PTY 字节流在 120 列端换行点分叉产生叠写）与方向 A 裁决（2026-08-22）。
       - WelcomeFrame 签名改为 (mode string, prefs json.RawMessage, cols, rows int)，组帧注释补「运行期再推送用于尺寸下发（G-05-1）与递补升格（R-09）双通道」；'W' 常量注释（proto.go:25-27）同步补尺寸键语义。
    2. spawn.go：提取导出常量 SpawnCols = 80、SpawnRows = 24，StartWithSize 的 Winsize 字面量改引用常量（spawn.go:42 魔法数单一事实源化——服务端零参与者会话尺寸回落值与真实 PTY spawn 尺寸必须同源，防双写漂移）。
    3. resize.go：新增 sessionDimsLocked() dims——`s.arbiter.last == (dims{})`（无参与者哨兵：首个参与端 attach 前 PTY 保持 spawn 尺寸，recalcNow 零值分支不动 PTY 的既有语义不变）时返回 dims{pty.SpawnCols, pty.SpawnRows}，否则返回 last。注释写明「last 零值 ≠ 会话尺寸未知，PTV 实际尺寸 = spawn 值」的论证（spawn.go StartWithSize 钉死 80x24，零参与者期间无任何 Resize 路径触达——recalcNow 零值提前返回）。
    4. server.go attach 升档序列重排（现时序 server.go:704 Welcome 组帧 → 705 registerLocked → 714-717 addMember/recalcNow，Welcome 会携带过时的 pre-attach 尺寸）：新时序为 hubMu 内 decideModeLocked → 构造 cl（dims=Hello 首尺寸，不变）→ `if s.participates(effMode) { s.addMember(cl, cl.dims); s.recalcNow() }` → sd := s.sessionDimsLocked() → prefs 按生效 mode 选档（不变）→ `cl.outbox.trySend(proto.WelcomeFrame(effMode, prefs, sd.cols, sd.rows))` → registerLocked → becomeOwner 立 owner（不变）→ hubCond.Broadcast。
       - 不变量保持：Welcome 入队仍先于 registerLocked——onChunk 遍历注册表扇出，cl 未登记前绝无 OUTPUT 夹入，「Welcome 恒为 S→C 首帧」（P2 D-02 时序纪律）不受影响；addMember/recalcNow 不触注册表，提前执行无扇出面。
       - 锁序保持：recalcNow → sess.Resize 取 sess.fdMu，hubMu > sess.fdMu 同序（resize.go:8-9 纪律）不变。
       - 更新 server.go:660-675 与 695-699 两段注释为新时序（逐字描述新顺序与不变量论证）。
    5. clients.go promoteNextLocked（clients.go:531）：升格 Welcome 改携 cand.dims——`proto.WelcomeFrame(proto.ModeRW, s.clientPrefsRW, cand.dims.cols, cand.dims.rows)`。论证注释：owner 模式升格后参与集 = {cand} 单员（旧 owner 已在 detach/kick 的 removeMember 移除），arbitrate 单员 = cand.dims，故升格 Welcome 尺寸恒等于升格后 recalcNow 的 last——**保持 trySend 在前、失败踢出重扫的既有形态**（不得为重算尺寸把 addMember/recalcNow 前移，否则 trySend 失败路径要回滚参与集，复杂度换取的值相同）；cand.dims 为 Hello 登记尺寸（运行期不更新，clients.go:97-100 既定），旁观期缩窗的瞬态偏差由前端升格分支 refit→RESIZE 上报纠正（05-08 纠正链，05-11 保持），随后 recalcNow 推送收口。
    6. 测试适配（编译必要项）：clients_test.go:96-97 两处 WelcomeFrame 调用补尺寸实参（mergeBatch 形状测试，任取如 80,24）；proto_test.go TestWelcomeFrameErrorFrame 三处调用点（:65/:95/:106）同补，并新增 dims round-trip 断言：组帧 WelcomeFrame(ModeRW, nil, 132, 43) → unmarshal WelcomePayload → Cols/Rows 精确相等；再加「prefs nil 时 cols/rows 键恒在」subtest（unmarshal 到 map[string]any 断言 "cols"/"rows" 键存在——恒序列化契约锁）。

    禁止：不得给 cols/rows 加 omitempty；不得新增帧类型字节；不得改动 Hello/Error/RESIZE 任何既有形状；不得动 onChunk/mergeBatch 广播路径（S1b 逐字节一致回归面）。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && go build ./... && go vet ./... && go test ./internal/proto/ ./internal/server/ -run 'TestWelcomeFrameErrorFrame|TestProtocolConstants|TestMergeBatch' -count=1 && go test ./... -count=1 2>&1 | tail -20</automated>
  </verify>
  <done>go build/vet 干净；WelcomeFrame 新签名三调用点（attach/升格/测试）全部携尺寸；proto 单测含 dims round-trip 与恒在键断言；既有全量测试零回归（本任务未挂推送，行为变化仅限 Welcome 载荷增键与 attach 组帧时序，全部既有断言不受尺寸键影响——map/struct 解码均容忍新键）。</done>
</task>

<task type="auto">
  <name>Task 2: recalcNow 推送挂点（运行期尺寸下发）+ Go 行为测试组（attach/升格/推送三通道断言）</name>
  <files>internal/server/resize.go, internal/server/multi_test.go, internal/server/resize_arb_test.go, internal/server/resize_test.go</files>
  <action>
    1. resize.go 新增 pushSessionDimsLocked(target dims)（hubMu 内调用）：遍历 registry.set，每客户端按其当前生效 mode 组帧——`mode := c.mode.Load().(string)`（atomic.Value 恒存 proto.ModeRO/ModeRW 字符串，allWritableBlockedLocked 同款读法先例）；prefs 按 mode 选档（rw → clientPrefsRW / ro → clientPrefsRO，D-13 双档纪律在推送通道不漂移：ro 端推送永不含 osc52）；frame := proto.WelcomeFrame(mode, prefs, target.cols, target.rows) → `if !c.outbox.trySend(frame) { s.kickOrCreditLocked(c, frame) }`（R-08 分工复用：连 ~100B 推送都容不下的端 = 事实 stalled）。
       - 推送挂点 = recalcNow 内 `s.arbiter.last = target` 与 sess.Resize 之后——尺寸变化唯一检测点即唯一推送点，attach/detach/kick/升格/防抖五调用点自动全覆盖；目标不变（含零值哨兵提前返回）零推送。
       - range 内踢出安全性注释登记：removeLocked 的 map delete 在 range 期间为 Go spec 安全（未到达的被删条目不再产出），onChunk→kickOrCreditLocked（clients.go:354-358）同形态先例；踢出若触发 promoteNextLocked → 嵌套 recalcNow → 嵌套推送，每次踢出永久移除一端保证有界终止（≤ max-clients）。
       - attach 调用点（Task 1 重排后）的天然性质写入注释：recalcNow 执行时新客户端尚未 registerLocked，推送循环不触达 attach 者自身——其会话尺寸由 Welcome 承载，零重复帧。
    2. resize_test.go（package server 白盒，arbitrate 表测同文件纪律）新增 TestSessionDimsLocked：`s := &Server{}` 零值 → sessionDimsLocked() == dims{pty.SpawnCols, pty.SpawnRows}（spawn 回落锁）；置 s.arbiter.last = dims{100, 30} → 返回该值（恒等锁）。
    3. multi_test.go 新增 TestWelcomeSessionDims（dialHelloPayload 返回完整 Welcome map，e2e_test.go:266 既有 helper 零新造）：
       - owner 模式（Writable: true 默认策略）：A = dialHelloPayload(40, 10) → wm["mode"]==rw 且 wm["cols"]==float64(40) && wm["rows"]==float64(10)（map[string]any JSON 解码数值为 float64）；B = dialHelloPayload(120, 40) → mode==ro（D-07 降级）且 cols/rows == 40/10——**旁观者 Welcome 携会话尺寸而非自身窗口尺寸**（G-05-1 核心 carriage 断言）。
       - A.CloseNow() → readUntilWelcome(cB) 5s 窗口收升格 Welcome：mode==rw 且 cols/rows == 120/40（升格携新 owner 尺寸 = cand.dims，G-05-1「升格恢复自渲染」服务端半侧）。
       - all 模式子测（WritePolicyAll）：A(132,43) → cols/rows 132/43（单员 last-wins）；B(80,24) → 80/24（min-rect 即时重算后的会话尺寸，非自身 80x24 恰好相等——改用 B(60,20) 使 min(132,60)xmin(43,24)=60/24 ≠ B 自身 60/20，rows 维产生区分度断言）。
    4. resize_arb_test.go 新增子测「运行期尺寸变化推送」（黑盒，startResizeServer/sendResize/ptySize 既有夹具）：owner 模式 A(80,24)/B(60,20) 降级 ro；B 的 attach Welcome 被 dialHello 消费后武装 readUntilWelcome(cB) 与 readUntilWelcome(cA)；A sendResize(100, 30) → 两端各在 5s 窗口收到第二帧 Welcome：B 的 mode==ro 且 cols/rows==100/30（推送按各端当前 mode 组帧 + 旁观者收到推送 = 约束渲染的数据通道）；A（上报者自身）同收 100/30。pollSize(t, sess, 100, 30) 附带锁定 PTY 跟随（既有断言形态）。
       - 禁止负向静默窗口断言（「resize 前 B 无第二帧 Welcome」类——防抖/调度时序引入flaky面，省）。
    5. 回归自检（写进测试注释，不断言）：S1 形态（owner 模式 A rw + B ro 降级，B 不参与）attach/无 resize → recalcNow 的 last 不变 → 零推送，accumPayload 的「窗口内无第二帧 Welcome」断言（multi_test.go:37-38）不受影响；TestResizeArbitration 既有三子测的 dialHello 尺寸组合（132x43/80x24 等）触发的推送帧均落在未被读流的连接上，Getsize 断言面不受影响。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && go test ./internal/server/ -run 'TestSessionDimsLocked|TestWelcomeSessionDims|TestResizeArbitration' -race -count=1 -v 2>&1 | grep -E '^(=== RUN|--- (PASS|FAIL)|PASS|FAIL|ok)' | head -30 && time go test ./... -race -count=1 2>&1 | tail -10</automated>
  </verify>
  <done>三个新测试/子测全过且全量 -race 零回归；推送挂点单点化（recalcNow 内），五调用点行为一致；owner resize 后 ro 旁观者在协议层可观测地收到新会话尺寸——G-05-1 服务端数据通道闭合。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server RESIZE | rw 端上报尺寸入仲裁（既有面，ClampDim [1,1000] 钳制 + D-09 ro 双闸不变） |
| server→client Welcome | 会话尺寸下发（本 plan 新增载荷键——纯整数，无敏感信息） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05G-01 | Tampering | arbiter 尺寸数据源 | low | mitigate | 尺寸值只能来自仲裁器（participates 矩阵 + ClampDim 既有闸），客户端无法注入未参与仲裁的尺寸；ro 端 RESIZE 双闸不变 |
| T-05G-02 | Denial of Service | 尺寸推送放大 | low | accept | rw 端 RESIZE 洪水经 50ms 防抖合并 + last 变化检测（不变零推送）收敛；持续洪水使防抖永不触发 = 重算饿死而非放大（PITFALLS Pitfall 10 既有裁决接受的形态）；推送帧 ~100B × ≤32 客户端，有界 |
| T-05G-03 | Information Disclosure | Welcome 尺寸键 | low | accept | cols/rows 为纯布局整数，无凭据/token/内容信息（logEvent 红线不涉及） |
| T-05G-SC | Tampering | 依赖供应链 | low | mitigate | 本 plan 零新依赖（无 npm/pip/cargo 安装任务），Package Legitimacy Gate 不触发 |
</threat_model>

<verification>
- `go build ./... && go vet ./...` 干净
- `go test ./... -race -count=1` 全量零回归（含既有 multi/resize/handshake/clients 全部测试——Welcome 增键对 map/struct 解码均透明）
- 新断言组：TestSessionDimsLocked（回落/恒等）+ TestWelcomeSessionDims（attach 双模式 + 升格尺寸）+ resize_arb 推送子测（双端收推送 + PTY 跟随）
- 接口契约自检：Welcome JSON 必含 cols/rows 且为 [1,1000] 正整数（ proto round-trip 锁）
</verification>

<success_criteria>
- 任意时刻任意在线客户端持有的最近一帧 Welcome 尺寸 == 服务端 arbiter.last（无参与者期 == spawn 80x24）
- owner resize → 50ms 防抖 → 全体在线端（含旁观者）协议层可观测收到新会话尺寸
- 升格 Welcome 尺寸 == 新 owner Hello 登记尺寸（120/40 断言锁定）
- 广播/权限/背压既有断言零回归（S1b 等价物 accumPayload 形态不受影响）
</success_criteria>

<output>
Create `.planning/phases/05-multi-client/05-10-SUMMARY.md` when done
</output>
