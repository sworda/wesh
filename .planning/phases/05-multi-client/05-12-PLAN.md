---
phase: 05-multi-client
plan: 12
type: execute
wave: 2
depends_on: ["05-10", "05-11"]
files_modified:
  - web/uat/phase05.mjs
  - web/uat/phase05-dom.mjs
  - web/uat/phase05-dims.mjs
  - README.md
  - .planning/phases/05-multi-client/05-UAT.md
autonomous: true
requirements: [MULTI-01, MULTI-04]
gap_closure: true
gap_ids: [G-05-1]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "协议层可断言：异尺寸双端 attach 后 Welcome 携会话尺寸（旁观者收会话尺寸而非自身窗口尺寸）；owner resize 后旁观者 50ms 防抖窗后收到携新尺寸的 'W' 推送；升格 Welcome 携新 owner 尺寸"
    - "DOM 逻辑面可断言：jsdom 宽端页在会话尺寸 < 窗口时 xterm 渲染被约束（rows 数 = 会话 rows；长行输出在会话 cols 处折行）；owner 断开升格后约束解除恢复窗口尺寸渲染"
    - "headless 层可断言：同一 40 列 PTY 字节流喂 40 列 headless 两端逐行一致（约束渲染 ≡ 窄端原生渲染的等价锁）；同流喂 120 列产生分叉（负对照证明断言区分度——G-05-1 叠写机理的回归锁）"
    - "既有断言零回归：phase05.mjs S1b（双端逐字节一致）/S9b（升格 PTY 跟随）、phase05-dom.mjs D1b/D1d（ro 提示恰一次/零 RESIZE 上行）、phase02/03/04 全套"
    - "README 协议表 'W' 行与 resize 行为节描述新形态；05-UAT.md 自动化执行说明更新复跑命令与覆盖描述"
  artifacts:
    - "web/uat/phase05.mjs：S10 场景（S10a 异尺寸 Welcome 尺寸 / S10b resize 推送 / S10c 升格尺寸）"
    - "web/uat/phase05-dom.mjs：D6 场景（D6a 约束 rows / D6b 会话 cols 折行 / D6c 升格解除约束）"
    - "web/uat/phase05-dims.mjs：新建 headless 等价 + 负对照断言（probe10.mjs 探针机制转正）"
    - "README.md：协议表 + resize 行为节；05-UAT.md：自动化执行说明段"
  key_links:
    - "S10/D6 共用基建：dialHello/welcomeOf/readUntilWelcome（phase05.mjs 既有）与 loadTerminal/waitReady/dialHello（phase05-dom.mjs 既有）零新造"
    - "D6 观测通道：.xterm-rows childElementCount === 会话 rows（DOM 渲染器行数恒等于 term.rows）；折行断言 = 全同字符长行在相邻两 div 各恰会话 cols 字符"
    - "复跑链：node web/uat/phase05.mjs ./wesh && node web/uat/phase05-dom.mjs ./wesh && node web/uat/phase05-dims.mjs ./wesh"
---

<objective>
G-05-1 验证与文档收口：为 05-10（服务端尺寸下发）+ 05-11（前端视口约束）补齐三层自动化断言（协议 / jsdom DOM / @xterm/headless），把用户实测发现的叠写场景固化为永久回归锁，并同步 README 与 05-UAT.md 描述。

Purpose: gap 闭合的客观证据层；headless 硬约束（根 CODEBUDDY.md：永无浏览器/playwright）下三层策略是本类修复的最强可验证形态。
Output: 三脚本新断言全过 + 全套 UAT 回归 + 文档同步。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-multi-client/05-UAT.md
@.planning/phases/05-multi-client/05-10-PLAN.md
@.planning/phases/05-multi-client/05-11-PLAN.md

@web/uat/phase05.mjs
@web/uat/phase05-dom.mjs
@web/uat/probe10.mjs
@web/uat/phase04-t1-width.mjs
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase05.mjs S10——会话尺寸下发协议层断言（Welcome carriage / resize 推送 / 升格尺寸）</name>
  <files>web/uat/phase05.mjs</files>
  <action>
    新增 S10 场景函数 s10SessionDims() 并挂入 scenarios 数组尾（既有风格：中文场景名 + check(id, name, ok, detail) 三要素；token/凭据值永不入 detail 红线不变）。实例形态：`startWesh(['--writable', '--', 'bash', '--norc', '--noprofile'])`（owner 模式默认）。既有夹具直接复用：dialHello(port, {cols, rows}) / welcomeOf(frames) / waitClose / outputText / sleep。

    - S10a（attach carriage，G-05-1 核心断言）：A = dialHello(40, 10) → welcomeOf(a.frames)：mode==='rw' 且 cols===40 && rows===10；B = dialHello(120, 40) → welcomeOf(b.frames)：mode==='ro'（D-07 降级）且 **cols===40 && rows===10**——旁观者 Welcome 携会话尺寸（owner 的 40x10）而非自身窗口 120x40，数值不相等即证明 carriage 语义在场（若实现误把自身 Hello 尺寸回填，cols 会是 120——断言有区分度）。
    - S10b（运行期推送）：A 发 RESIZE 帧（0x31 + JSON {"cols":60,"rows":15}）→ 轮询 b.frames（50ms 防抖 + 余量，5s 上限）出现第二帧 WELCOME 且解码 cols===60 && rows===15、mode==='ro'（推送按接收端当前 mode 组帧）；同窗口断言 a.frames 同样收到该推送（上报者自身也在广播集内）。
    - S10c（升格尺寸）：a.ws.close() + waitClose → 轮询 b.frames 新 WELCOME（升格推送与可能的尺寸推送同值同向，取最后一帧 WELCOME 解码）：mode==='rw' 且 cols===120 && rows===40（= B 的 Hello 登记尺寸 cand.dims，owner 模式升格后参与集单员）。

    时序注释登记：S10b 断言前无需等待——50ms 防抖经轮询吸纳（S9b stty 同款形态）；S10c 的「取最后一帧」容忍升格 Welcome 与紧随的 recalcNow 推送两帧相继到达（同值幂等）。S1-S9 既有断言零改动（广播路径未动，S1b 逐字节一致天然回归）。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && pnpm -C web build >/dev/null 2>&1; time go build -o wesh ./cmd/wesh && node web/uat/phase05.mjs ./wesh 2>&1 | grep -E 'S10|S1b|S9|结果' </automated>
  </verify>
  <done>S10a/b/c 三断言 PASS；S1-S9 全量 PASS（结果行通过数 = 改造前 +3）；无场景异常。</done>
</task>

<task type="auto">
  <name>Task 2: phase05-dom.mjs D6（约束渲染/折行/升格解除）+ 新建 phase05-dims.mjs（headless 等价 + 负对照）</name>
  <files>web/uat/phase05-dom.mjs, web/uat/phase05-dims.mjs</files>
  <action>
    1. phase05-dom.mjs 新增 d6ViewportConstraint() 并挂入执行序列（D1-D5 之后）：
       - 场景装配：`startWesh(['--writable', '--', 'bash', '--norc', '--noprofile'])`；owner 配角 = dialHello(inst.port, { cols: 40, rows: 10 })（窄 owner 把会话尺寸钉在 40x10）；jsdom 页 = loadTerminal(..., { path: pathFromUrl(inst.shareRW) })（布局桩 720x408 → fit 80x24，宽端；rw 链接第二端降级 ro——旁观宽端即 G-05-1 用户场景）。waitReady 落定。
       - D6a（约束 rows 断言）：`ctx.document.querySelector('.xterm-rows').childElementCount === 10`——DOM 渲染器行 div 数恒等于 term.rows，会话 rows=10 约束生效（无约束时是 24，区分度在场；phase04-dom.mjs:387 `.xterm-rows > div` 行列表先例通道）。
       - D6b（会话 cols 折行断言 = 叠写回归的 DOM 层等价物）：owner.ws 发 INPUT 帧注入 `printf 'A%.0s' {1..80}\n`（bash --norc 支持花括号展开；80 个 'A' 恰两倍会话列宽）→ waitFor 输出落定后，在 `.xterm-rows > div` 列表中找到 textContent 为恰 40 个 'A' 的行，断言其存在且相邻下一 div 同样恰 40 个 'A'（折行点 = 会话 cols 而非窗口 80——无约束时 80 个 'A' 单行呈现，断言必败，区分度锁定）。注释登记机理：readline/printf 输出按 PTY 40 列生成，约束 40 列渲染 = 换行点一致 = 不叠写。
       - D6c（升格解除约束，设计约束 4 端到端）：owner.ws.close() → waitFor 标题 [ro] 前缀消失（升格信号，D2b 同款）→ 随后 `.xterm-rows` childElementCount 回到 24（升格 Welcome 携 cand.dims = 本页 Hello 80x24 → sessionDims 更新 → refit 解除约束）。waitFor 轮询吸纳升格+推送双帧相继到达（同值幂等）。
       - 收尾 cleanup(ctx, inst) 同款；owner.ws 二次 close try/catch（D2 先例）。
    2. 新建 web/uat/phase05-dims.mjs（probe10.mjs 探针机制转正为门禁断言；运行：`node web/uat/phase05-dims.mjs [wesh 路径]`，零依赖传统之外的 @xterm/headless 6.0.0 已在 web/uat/package.json 依赖内，`allowProposedApi: true` 纪律见根 CODEBUDDY.md）：
       - 流程照 probe10.mjs：spawn（--bind 127.0.0.1 --port 0 --writable -- bash --norc --noprofile，端口解析复用其正则）→ A=dial(40,10) owner / B=dial(120,40) 旁观 → prompt 落定 → A 注入超 40 列长命令（echo 标记串 + 长尾巴）→ 收齐后取 A/B 各自 base 切片后的 OUTPUT 字节流。
       - D6H-1（等价锁）：B 流喂 40x10 headless（约束渲染等价物）与 A 流喂另一 40x10 headless（窄端原生），buffer.active 逐行 translateToString(true) 比对全等——「同 cols 渲染同字节流 = 逐屏严格一致」的终端核心层锁定。
       - D6H-2（负对照）：B 流喂 120x40 headless → 断言与 40 列渲染**逐行不全等**（换行点分叉 = G-05-1 叠写机理复现；phase04-t1-width.mjs U6 对照组先例——证明 D6H-1 的断言有区分度而非恒真）。
       - 结果汇总/退出码形态照 phase04-t1-width.mjs（check(name, actual, expected) + process.exit）。
    3. 不改 package.json（依赖在场）；probe10.mjs 保留原位（探针档案），新文件头部注释指路其血缘。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && node web/uat/phase05-dom.mjs ./wesh 2>&1 | grep -E 'D6|D1|D2|结果' && node web/uat/phase05-dims.mjs ./wesh 2>&1 | tail -5</automated>
  </verify>
  <done>D6a/b/c PASS 且 D1-D5 零回归（ro 提示门闩在推送形态下仍恰一次 = D1b 回归顺带覆盖）；phase05-dims.mjs D6H-1 等价 PASS + D6H-2 负对照 PASS（不全等）；两脚本退出码 0。</done>
</task>

<task type="auto">
  <name>Task 3: README/05-UAT.md 文档同步 + 全量回归（go -race + 六段式 UAT）</name>
  <files>README.md, .planning/phases/05-multi-client/05-UAT.md</files>
  <action>
    1. README.md 两处同步（行号为改造前参照）：
       - 协议表 'W' 行（L109）：载荷描述改为 `{"mode":"ro"\|"rw","cols":N,"rows":N,"prefs":{...}?}`，补注「cols/rows 恒在 = 当前会话尺寸；运行期尺寸变化经 'W' 帧再推送（递补升格推送同通道先例），前端据以约束视口渲染」。L104 正文内联 Welcome JSON 描述同步（同节形状一致性纪律，04-06 先例）。
       - resize 行为节（L181-183）：首句「各端按自己窗口渲染、多余面积留白」改写为新形态——会话尺寸 = 参与集 min-rect，经 Welcome 下发、运行期 'W' 推送更新；窗口大于会话矩形的端约束视口到会话尺寸渲染（超出面积为页面背景留白，行编辑回显等相对寻址流逐屏一致）；窗口小于会话尺寸的轴按窗口渲染（裁剪语义不变）；减员恢复（2→1 last-wins + 推送解除约束）。纯 ro 会话段（Hello 首尺寸参与/运行期缩放不上报/reattach 恢复）逐字保持。
       - 禁止改动：关闭码表、Error 码契约段、部署注意节——零波及面。
    2. 05-UAT.md 仅更新「自动化执行说明」段（L14-20 区）：复跑命令补 `&& node web/uat/phase05-dims.mjs ./wesh`；覆盖描述补 S10/D6/D6H 三句（会话尺寸下发协议断言 / 约束渲染 DOM 断言 / headless 等价+负对照）；通过计数更新为复跑实际输出值（以本任务验证命令实测为准，不预写死）。**Test 1 的 result/note 与 Gaps 区 G-05-1 状态字段不动**——gap 状态翻牌由 /gsd:verify-work 重跑后对账（gap_ids 链接）。
    3. 全量回归六段式（改造合并态最终门）：
       - `/usr/bin/gofmt -l .` 改用 GOROOT gofmt（01-03 登记的陈旧 /usr/bin/gofmt CJK 注释规则差异陷阱——`$(go env GOROOT)/bin/gofmt -l .` 输出须为空；若本 plan 动的 Go 文件为零则跳过即可，05-10 已覆盖）
       - `go vet ./...`
       - `time go test ./... -race -count=1`（编译超时 20 分钟纪律，实测远小于此）
       - `time pnpm -C web build`
       - UAT 全脚本连跑：`node web/uat/phase02.mjs ./wesh && node web/uat/phase03.mjs ./wesh && node web/uat/phase04.mjs ./wesh && node web/uat/phase05.mjs ./wesh && node web/uat/phase05-dom.mjs ./wesh && node web/uat/phase05-dims.mjs ./wesh && node web/uat/phase04-t1-width.mjs`（前序脚本回归面：Welcome 增键对各 phase 的 map 解码断言透明——phase04.mjs prefs 断言为键存在性形态，不受新键影响；如有意外失败即为本 plan 回归证据，禁止跳过来收口）
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && time go test ./... -race -count=1 2>&1 | tail -5 && node web/uat/phase02.mjs ./wesh >/tmp/r2.log 2>&1; node web/uat/phase03.mjs ./wesh >/tmp/r3.log 2>&1; node web/uat/phase04.mjs ./wesh >/tmp/r4.log 2>&1; node web/uat/phase05.mjs ./wesh >/tmp/r5.log 2>&1; node web/uat/phase05-dom.mjs ./wesh >/tmp/r5d.log 2>&1; node web/uat/phase05-dims.mjs ./wesh >/tmp/r5h.log 2>&1; node web/uat/phase04-t1-width.mjs >/tmp/r4t.log 2>&1; tail -2 /tmp/r2.log /tmp/r3.log /tmp/r4.log /tmp/r5.log /tmp/r5d.log /tmp/r5h.log /tmp/r4t.log</automated>
  </verify>
  <done>go 全量 -race 过；七个 UAT 脚本结果行全 PASS（phase02/03/04 通过数与改造前一致，phase05 系 +S10/+D6/+D6H）；README 两节与 05-UAT.md 说明段落地；Test 1 result 留待 verify-work 对账。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT 脚本 → spawn 实例 | 测试夹具直连真实二进制（既有形态）；token/凭据值永不进断言输出（既有红线） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05G-07 | Information Disclosure | UAT 输出/log | low | mitigate | 沿用 phase05 既有红线：token 只存闭包变量作断言材料，detail/控制台零 token 零凭据（check detail 审查） |
| T-05G-08 | Denial of Service | 测试夹具资源 | low | accept | 全部实例 SIGKILL 收口 + jsdom 统一 cleanup（既有纪律）；phase05-dims.mjs 字节流量级同 probe10（KB 级） |
| T-05G-SC | Tampering | 依赖供应链 | low | mitigate | 零新依赖（@xterm/headless 6.0.1→6.0.0 已在 web/uat 锁文件内，jsdom 既有），Package Legitimacy Gate 不触发 |
</threat_model>

<verification>
- S10/S10a-c、D6a-c、D6H-1/2 全部 PASS（三层断言闭合 G-05-1）
- phase02/03/04/05 全脚本 + dom/headless 变体连跑零回归
- go test ./... -race 全量过
- README 协议表/resize 节、05-UAT.md 自动化说明段与实现一致
</verification>

<success_criteria>
- G-05-1 的三个断言层（协议 carriage+推送 / DOM 约束渲染+升格解除 / headless 等价+负对照）全部自动化锁定，复跑命令三条即可重现
- 用户实测场景「A 小 B 大，A 内输入」在自动化中等价覆盖（D6b 折行点一致 = 不叠写的可断言等价物；像素层一致性仍属既有豁免）
- 全部既有断言（S1b 逐字节、D1b/D1d、S9b 等）零回归
</success_criteria>

<output>
Create `.planning/phases/05-multi-client/05-12-SUMMARY.md` when done
</output>
