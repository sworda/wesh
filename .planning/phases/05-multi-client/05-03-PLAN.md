---
phase: 05-multi-client
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - internal/server/clients.go
  - internal/server/server.go
  - internal/proto/proto.go
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
  - internal/server/multi_test.go
autonomous: false
requirements:
  - MULTI-02

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "--writable 保持总闸（不给 = 全员只读、只打印 ro 链接，现状语义零漂移）；--write-policy=owner|all 默认 owner（D-05 安全默认哲学：旁观是被动场景、协作主动开启；P2 D-15 全名无短选项）"
    - "owner = 首个以 rw 身份完成 attach 的客户端（D-06——无显式指定通道，operator 也是浏览器客户端）；owner 断线后按 attach 顺序 FIFO 递补：order 中首个 rwEligible 且仍在线者升格"
    - "owner 在位时后续 rw attach 降级 ro + 进递补队列（D-07：Welcome mode=ro 但 client.rwEligible=true）；升格 = 服务端向其 outbox 入队 Welcome{mode:\"rw\", prefs: rw 档}（R-09 复用 'W' 帧运行期推送，零新类型字节——P2 D-01/D-02 纪律）"
    - "模式判定矩阵（RESEARCH Pattern 5 逐字）：rw ticket × --writable × owner × owner 不在位 → rw（成为 owner）；rw × owner × owner 在位 → ro（rwEligible，FIFO 尾）；rw × all → rw（无递补概念）；ro ticket × 任意 → ro（永不递补）；无 --writable × 任意 → ro（总闸）"
    - "prefs 按客户端 mode 分化双档 blob（D-13 + P5-6）：ro 档（旁观者+降级递补者）不含 osc52 键——即使全局 --osc52 开启；rw 档按全局 --osc52 下发；attach Welcome 按生效 mode 选档、升格 Welcome 必携 rw 档（升格即获 osc52，D-13 的另一半）"
    - "osc52 双档由 main 聚合期产出（aggregateClientPrefs 产 ro/rw 两变体，osc52 是服务端专有键）——服务端对 prefs 保持不透明透传，不做运行期 JSON 手术（P5-6 纪律）"
    - "升格路径接入 P5-7 统一 Broadcast：递补升格后 hubCond.Broadcast() 重估信用门（新 rw 端进信用集）；仲裁参与集切换挂点在 resize.go 注释登记（05-04 落地：新 owner 尺寸接管）"
    - "--write-policy 与 --writable 组合校验：用户显式设置 --write-policy 却未给 --writable 时启动拒绝 exit 2（配置矛盾 fail-fast，P3 校验矩阵纪律；fs.Visit 判定显式设置，默认 owner 不给 --writable 属正常 ro 会话不拒）"
    - "permission_denied code 保持占位注释不硬用（CONTEXT 裁断：owner 降级走 Welcome 非 Error，无真实使用场景）"
    - "升格信号 = 标题前缀消失 + 键盘激活，无 toast/面板/badge（UI-SPEC ## UI Considerations covered：promotion-transition——D-07 零新 UI 组件，前端翻转在 05-08 落地）"
  prohibitions:
    - "权限不得由客户端请求获得——ro→rw 唯一通道是服务端 FIFO 递补后的 Welcome 推送；任何『客户端发帧申请写权限』的机制都是越权面（owner 权限抢夺 T-05-08），禁止出现"
  artifacts:
    - path: "internal/server/clients.go"
      provides: "registry owner 指针 + client.rwEligible + 模式判定矩阵 + 递补升格（promoteNextLocked）"
      contains: "rwEligible"
    - path: "internal/server/server.go"
      provides: "升档 mode 判定走矩阵 + Welcome 按生效 mode 与 prefs 双档选档"
      contains: "writePolicy"
    - path: "cmd/wesh/main.go"
      provides: "--write-policy flag + 显式设置判定 + 组合校验 + prefs 双档聚合（ClientPrefsRO/ClientPrefsRW）"
      contains: "write-policy"
    - path: "internal/server/multi_test.go"
      provides: "TestOwnerPolicy/TestAllPolicy/TestSuccession（VALIDATION 05-01-02）"
      contains: "TestSuccession"
  key_links:
    - from: "internal/server/server.go (升档)"
      to: "internal/server/clients.go (模式判定矩阵)"
      via: "ticket mode × writable × writePolicy × owner 在位？ → 生效 mode + rwEligible + owner 指针登记"
      pattern: "rwEligible"
    - from: "internal/server/clients.go (detach owner)"
      to: "internal/server/clients.go (promoteNextLocked)"
      via: "order FIFO 首个 rwEligible 在线者 → outbox 入队 Welcome{mode:rw, prefs:rw档} + owner 指针切换 + Broadcast"
      pattern: "promoteNext"
    - from: "cmd/wesh/main.go (aggregateClientPrefs)"
      to: "internal/server/server.go (Welcome 选档)"
      via: "Options.ClientPrefsRO/ClientPrefsRW 双 blob；attach Welcome 按生效 mode 选档、升格 Welcome 携 rw 档"
      pattern: "ClientPrefsR"
---

<objective>
MULTI-02 写权限体系：--write-policy=owner|all（默认 owner）+ owner FIFO 递补（首个 rw attach 为 owner，断线按 attach 顺序递补）+ owner 在位时 rw 降级 ro 进递补队列 + 升格 Welcome 运行期推送（R-09 复用 'W' 帧）+ prefs 按 mode 双档分化（D-13 旁观端 osc52 强制不下发，兑现 P4 deferred）。

Purpose: all 模式全员可写服务协作排障、owner 模式仅 owner 可写服务演示旁观（ROADMAP 成功准则 1 后半）；权限真边界在服务端（P2 D-13/D-14 纪律延伸），前端只做 UX 翻转。prefs 双档堵住共享终端 OSC52 劫持全体旁观者剪贴板的面（PITFALLS C5 对策的另一半）。
Output: 注册表 owner/FIFO/矩阵 + server 升档判定 + main.go flag 与双档聚合 + 三权限测试，含 D-05 one-way 确认门。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-client/05-CONTEXT.md
@.planning/phases/05-multi-client/05-RESEARCH.md
@.planning/phases/05-multi-client/05-PATTERNS.md
@.planning/phases/05-multi-client/05-VALIDATION.md
@internal/server/clients.go
@internal/server/server.go
@cmd/wesh/main.go
@internal/proto/proto.go
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 确认门——--write-policy CLI 公开契约（D-05，one-way）</name>
  <read_first>
    - .planning/phases/05-multi-client/05-CONTEXT.md（decisions 节 D-05/D-06/D-07 逐字）
  </read_first>
  <decision>--write-policy=owner|all、默认 owner——CLI flag 公开契约（one-way door：发布后改名将破坏用户脚本与文档）</decision>
  <context>
    D-05 在 discuss-phase 已锁定（--writable 保持总闸；新增 --write-policy=owner|all 默认 owner，安全默认哲学）。此 flag 一经发布即成为公开 CLI 契约（P2 D-15 纪律：全名无短选项），撤回或改名需要major 级别变更。本门是实现前最后一次确认，不是重新决策。
  </context>
  <options>
    <option id="as-locked">
      <name>按锁定值落地（推荐）</name>
      <pros>--write-policy=owner|all 默认 owner；与 CONTEXT.md D-05 逐字一致；零重新讨论成本</pros>
      <cons>无</cons>
    </option>
    <option id="revise">
      <name>调整 flag 名/默认值</name>
      <pros>若发现命名冲突可在此刻低成本修正</pros>
      <cons>推翻 CONTEXT.md 锁定项，需重新走 discuss 流程并回填全部引用此决策的文档</cons>
    </option>
  </options>
  <resume-signal>选择 as-locked（继续实现）或 revise（说明调整点）</resume-signal>
  <acceptance_criteria>
    - 用户明确选择 as-locked 后才进入 Task 2；revise 时停止执行并回填 CONTEXT.md
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 模式判定矩阵 + owner/FIFO 递补 + 升格 Welcome + --write-policy flag + prefs 双档</name>
  <reversibility rating="one-way">--write-policy flag 是公开 CLI 契约（D-05）——发布后改名/改默认即破坏用户脚本与文档，属 one-way door；确认门见 Task 1。</reversibility>
  <files>internal/server/clients.go, internal/server/server.go, internal/proto/proto.go, cmd/wesh/main.go, cmd/wesh/main_test.go</files>
  <read_first>
    - internal/server/clients.go（05-01/05-02 产出全文——registry/client/信用门现状，owner 指针与递补挂载点）
    - internal/server/server.go（升档 mode 判定区现状——checkTicket 返回 mode 的消费点；85-133 Options/New 形态）
    - cmd/wesh/main.go（53-118 parseArgs flag 形态与 client-option 记录式先例、166-179 aggregateClientPrefs、199-216 validateStartup 校验矩阵、272-278 启动打印）
    - internal/proto/proto.go（23 行 Welcome 注释、44 行 permission_denied 占位、85-121 WelcomePayload/WelcomeFrame）
    - .planning/phases/05-multi-client/05-RESEARCH.md（Pattern 5 判定矩阵逐字表、裁断 R-09、Pitfall P5-6 双档时序）
    - .planning/phases/05-multi-client/05-PATTERNS.md（server.go 节升档序列改造行、main.go 节 flag 形态与 prefs 双档纪律）
  </read_first>
  <action>
    1. `internal/server/clients.go`：
       - client 结构加 `rwEligible bool`（注释：rw ticket/候选资格——D-06『以 rw 身份』；ro ticket 恒 false 永不递补）；registry 加 `owner *client`。
       - 模式判定函数（Pattern 5 表逐字落地，输入：ticket 绑定 mode、writable、writePolicy、owner 是否在位；输出：生效 mode + rwEligible + 是否成为 owner）：rw × owner 模式 × owner 不在位 → ModeRW + 立 owner；rw × owner × 在位 → ModeRO + rwEligible=true（D-07 降级，FIFO 尾——order 切片天然 FIFO）；rw × all → ModeRW；ro × 任意 → ModeRO（rwEligible=false）；无 writable → ModeRO（D-05 总闸）。
       - `promoteNextLocked()`：detach 移除的若是 owner → 遍历 order 取首个 rwEligible 且仍在线者 → 置 owner + c.mode = ModeRW + c.rwEligible 保留（语义注释）→ 其 outbox 入队 `proto.WelcomeFrame(proto.ModeRW, s.clientPrefsRW)`（R-09 升格推送；P5-6：升格必携 rw 档 prefs——osc52 的另一半）→ s.hubCond.Broadcast()（P5-7 升格挂点：新 rw 端进信用集）→ 注释登记『仲裁参与集切换挂点：05-04 resize.go 落地时新 owner 尺寸接管』。无可递补者 → owner=nil（下一个 rw attach 按矩阵成为新 owner）。
    2. `internal/server/server.go`：
       - Server/Options 加 writePolicy 与 clientPrefsRO/clientPrefsRW 两字段（Options.ClientPrefs 单字段分裂为 ClientPrefsRO/ClientPrefsRW json.RawMessage——注释：D-13 双档，聚合期产出，服务端不透明透传不做运行期 JSON 手术）；原 ClientPrefs 字段移除，04 装配调用点（main.go）同步迁移。
       - 升档序列：原 mode 判定改为调用 clients.go 矩阵；Welcome 帧按生效 mode 与对应 prefs 档组帧（ro → clientPrefsRO，rw → clientPrefsRW）入队 outbox 首条。
    3. `cmd/wesh/main.go`：
       - parseArgs：`fs.StringVar(&cfg.writePolicy, "write-policy", "owner", "write policy when --writable is on: owner|all (default owner)")`（全名无短选项 P2 D-15）；parse 期枚举校验——值非 owner/all 即时报错 exit 2（枚举值非敏感，直接 return error 即可，clientOptErr 记录式仅用于值含敏感内容的场景）。
       - validateStartup 扩展：经 fs.Visit 判定 write-policy 被显式设置且 cfg.writable 为 false → 返回错误（配置矛盾 fail-fast：显式要求写策略却没开写总闸；默认 owner + 无 --writable 是纯 ro 会话正常形态不拒）；纯函数零副作用纪律不变。
       - aggregateClientPrefs（166-179）产双档：ro 档 = 现聚合结果剔除 osc52 键（osc52 是服务端专有键，聚合期已知——ro 档永远不含，即使全局 --osc52 开启，D-13）；rw 档 = 现聚合结果原样（按全局 --osc52）；两 blob 经 Options.ClientPrefsRO/ClientPrefsRW 传入。
    4. `cmd/wesh/main_test.go`：TestStartupMatrix 扩展（main_test.go:303 表测直调 validateStartup 先例——config 结构需加 writePolicy 值字段与 writePolicySet 显式设置标记字段，后者由 parseArgs 经 fs.Visit 填充、validateStartup 消费）：
       - 新增拒绝行：writePolicySet=true 且 writable=false（任意 bind 安全形态如 127.0.0.1）→ wantErrSub 含 "write-policy" 与 "writable"（组合校验拒绝文案）；
       - 新增放行行：writePolicy 取默认 owner、writePolicySet=false、writable=false → 空 err 空 warn（默认 owner 无 --writable 是纯 ro 会话正常形态不拒）；
       - 既有行零改动（writePolicySet 零值 false 语义 = 未显式设置，与现状行为兼容）。
    5. `internal/proto/proto.go`：23 行 Welcome 注释补充『运行期再推送用于递补升格（R-09——P2 D-01/D-02：既有帧类型再推送不算动协议）』；44 行 permission_denied 占位注释保持（CONTEXT 裁断不硬用——附一行注释说明 owner 降级走 Welcome 非 Error）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -race -count=1 ./internal/server/ && go test -race -count=1 -run 'TestParseArgs|TestStartupMatrix' ./cmd/wesh/</automated>
  </verify>
  <acceptance_criteria>
    - go build/vet 退出 0；server 包 -race 全绿（05-01/05-02 测试零回归）
    - grep -c 'rwEligible\|promoteNextLocked' internal/server/clients.go >= 3
    - grep -c 'ClientPrefsRO\|ClientPrefsRW' internal/server/server.go cmd/wesh/main.go >= 4（Options 两字段 + 聚合产出 + Welcome 选档）
    - grep -c 'write-policy' cmd/wesh/main.go >= 2（flag 注册 + 校验）
    - TestParseArgs 绿（write-policy 畸形值 exit 2）；TestStartupMatrix 扩展行绿：显式 write-policy 无 --writable 拒绝行 + 默认 owner 无 --writable 放行行
    - 升格 Welcome 复用 proto.WelcomeFrame（grep -c 'WelcomeFrame(proto.ModeRW' internal/server/clients.go >= 1），无新类型字节（proto.go 类型字节表零新增）
  </acceptance_criteria>
  <done>owner/all 判定矩阵、FIFO 递补升格、--write-policy flag 与组合校验、prefs 双档全部落码且编译测试绿；行为矩阵断言由 Task 3 锁定。</done>
</task>

<task type="auto">
  <name>Task 3: TestOwnerPolicy / TestAllPolicy / TestSuccession 权限测试组</name>
  <files>internal/server/multi_test.go</files>
  <read_first>
    - internal/server/multi_test.go（05-01/05-02 产出——dialHello 复用形态与既有测试装配）
    - internal/server/e2e_test.go（474-563 ro/rw 两半侧测的可写配置先例——startTestServerWith opts 形态）
    - .planning/phases/05-multi-client/05-VALIDATION.md（05-01-02 行：三测试命名与断言面）
    - .planning/phases/05-multi-client/05-RESEARCH.md（Pattern 5 矩阵逐字表——测试覆盖的行）
  </read_first>
  <action>
    1. `internal/server/multi_test.go` 追加（无认证模式即可覆盖矩阵核心行——ticket mode 由全局 writable 派生；认证模式 token 绑定通道由 05-06 sharetoken_test.go 与既有 dialHelloTicket 覆盖）：
       - `TestOwnerPolicy`（VALIDATION 05-01-02）：opts Writable=true + write-policy owner（Options 直传字段）→ A attach → Welcome mode=rw（成为 owner）；B attach → Welcome mode=ro（D-07 降级）；断言 B 的 INPUT 被丢弃（发 INPUT 后子进程 cat 无回显——行为断言经 A 端输出视角或超时无回显）且 A 的 INPUT 生效；--osc52 变体：A 的 Welcome prefs 含 osc52 键、B 的不含（D-13 双档断言——prefs JSON 键存在性断言，不解析值）。
       - `TestAllPolicy`：write-policy all → A/B 均 Welcome mode=rw；两端 INPUT 均生效（cat 回显）；无递补概念（A 断开后 B 保持 rw，无升格帧——B 端读到的下一帧不是 Welcome）。
       - `TestSuccession`（VALIDATION 05-01-02）：owner 模式 A(owner rw)/B(降级 ro) → A CloseNow → B 在轮询窗口内收到第二帧 Welcome mode=rw（R-09 升格推送）且 prefs 含 osc52 键（P5-6 升格携 rw 档）→ B 的 INPUT 此后生效；C 场景：owner 断开且无可递补者 → 后续新 rw attach 直接成为 owner（Welcome rw）。
       - 全部 -race；Read 永不带 deadline ctx（Pitfall 2 竞速形态）。
  </action>
  <verify>
    <automated>go test -race -count=1 -run 'TestOwnerPolicy|TestAllPolicy|TestSuccession' ./internal/server/ && go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - 三测试全绿且命名与 VALIDATION 05-01-02 逐字对齐（grep -c 'func TestOwnerPolicy\|func TestAllPolicy\|func TestSuccession' internal/server/multi_test.go == 3）
    - TestOwnerPolicy 含：A=rw/B=ro Welcome 断言 + B INPUT 丢弃 + D-13 prefs 双档（osc52 键存在/缺席）断言
    - TestSuccession 含：升格 Welcome mode=rw + 升格 prefs 携 osc52 键 + 递补后 INPUT 生效断言
    - server 包 -race 全量绿（零回归）
  </acceptance_criteria>
  <done>MULTI-02 行为锁定：owner 模式升降级与 FIFO 递补升格、all 模式全员可写、ro 永不递补、prefs 按 mode 双档（旁观者永不获 osc52）。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

edge-probe 对本条需求的分类为 unclassified（中文文本启发式局限，非无边缘），按 #1110 保持 unresolved 显式记录：

- requirement: MULTI-02 ｜ probe: "unclassified — review manually" ｜ status: flagged-unverified ｜ 处置：边角面由 CONTEXT D-05/D-06/D-07/D-13 与 RESEARCH Pattern 5 矩阵、R-09、P5-6 实质覆盖；断言已提升为 must_haves.truths（判定矩阵、FIFO 递补、升格携 rw 档 prefs、双档 osc52），由 TestOwnerPolicy/TestAllPolicy/TestSuccession 实证。

## Artifacts this plan produces

- `internal/server/clients.go`：`client.rwEligible` 字段、`registry.owner` 指针、模式判定函数（ticket mode × writable × writePolicy × owner 在位 → 生效 mode）、`promoteNextLocked()`（FIFO 递补 + 升格 Welcome 推送 + Broadcast）
- `internal/server/server.go`：`Options.ClientPrefsRO`/`ClientPrefsRW` 双字段（替代 ClientPrefs 单字段）、`writePolicy` 字段、升档 Welcome 按 mode 选档
- `cmd/wesh/main.go`：`--write-policy=owner|all` flag（默认 owner）、fs.Visit 显式设置判定 + 组合校验、aggregateClientPrefs 双档产出
- `cmd/wesh/main_test.go`：TestStartupMatrix 扩展行（显式 write-policy 无 --writable 拒绝 / 默认 owner 未显式设置放行）
- `internal/proto/proto.go`：Welcome 运行期再推送注释（R-09）
- `internal/server/multi_test.go`：`TestOwnerPolicy`、`TestAllPolicy`、`TestSuccession`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端→写权限 | 不可信客户端不得自行获得写权限——ro→rw 唯一通道是服务端 FIFO 递补推送 |
| rw 输出→ro 旁观者 | 共享终端内容（含 OSC52 剪贴板写）扇出到旁观者——旁观者环境不得被劫持 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Elevation of Privilege | owner 断线后的权限真空/抢夺 | high | mitigate | D-06 FIFO 递补（attach 顺序确定，无『先抢先得』竞态）；升格经服务端 Welcome 推送非客户端请求；TestSuccession 锁定递补顺序与升格帧 |
| T-05-07 | Tampering | OSC52 劫持旁观者剪贴板 | high | mitigate | D-13 prefs 双档：ro 端（旁观者+降级者）永不下发 osc52 键（即使全局 --osc52 开启）；聚合期产双 blob 零运行期手术；TestOwnerPolicy 断言 ro Welcome prefs 无 osc52 键 |
| T-05-08b | Elevation of Privilege | --write-policy=all 误开协作面 | medium | mitigate | 默认 owner（D-05 安全默认）；显式 write-policy 无 --writable 启动拒绝（配置矛盾 fail-fast）；flag 值 parse 期枚举校验 |
</threat_model>

<verification>
- go build/vet 退出 0；go test -race -count=1 ./internal/server/ ./cmd/wesh/ 全绿（含三权限测试）
- 源码断言：rwEligible/promoteNextLocked/双档 prefs 字段/write-policy flag 齐备；proto 类型字节零新增
</verification>

<success_criteria>
- owner 模式：首个 rw attach 可写、后续 rw 降级旁观、owner 断线按 attach 顺序自动升格（ROADMAP 成功准则 1：演示旁观形态）
- all 模式：全员可写（协作排障形态）；ro ticket 持有者永不递补
- 旁观者永不收到 osc52 能力（D-13 兑现 P4 deferred）
</success_criteria>

<output>
创建 `.planning/phases/05-multi-client/05-03-SUMMARY.md`：记录确认门结果（as-locked/revise）、判定矩阵落码形态、递补升格的 Welcome 推送与 Broadcast 挂点、prefs 双档装配、三测试锁定行为、05-04 的仲裁参与集切换挂点登记。
</output>
