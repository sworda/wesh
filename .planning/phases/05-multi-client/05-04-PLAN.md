---
phase: 05-multi-client
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - internal/server/resize.go
  - internal/server/clients.go
  - internal/server/server.go
  - internal/server/resize_arb_test.go
autonomous: true
requirements:
  - MULTI-04

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "resize 仲裁参与集按写权限分层（D-09 矩阵逐字）：--writable --write-policy=owner → 仅 owner 尺寸参与（递补后新 owner 尺寸接管）；--writable --write-policy=all → 全部生效 rw 端；无 --writable 纯 ro 会话 → 全部 ro 端的 Hello 首尺寸（否则会话冻结 80x24）；含可写端会话中的 ro 旁观者 → 不参与（服务端直接忽略其 RESIZE，第二闸）"
    - "仲裁算法（MULTI-04）：参与集 0 人 → 不动 PTY 保持现状；1 人 → last-wins（该成员尺寸）；≥2 人 → min(cols)×min(rows) 最小公共矩形；2→1 恢复 last-wins（剩余者尺寸）"
    - "RESIZE 上报走 50ms 防抖（单 time.Timer reset——Options.ResizeDebounce 测试可覆写，AfterFunc 先例 server.go:377-386）；attach/detach/递补升格即时重算不防抖（无风暴风险，RESEARCH Pattern 4）"
    - "目标尺寸变化才调 sess.Resize（P5-3：同尺寸 TIOCSWINSZ 内核不发 SIGWINCH，且避免无谓 ioctl）；入参尺寸已经 proto.ClampDim 钳制 [1,1000]（proto.go:167-176 既有，零改动）；sess.Resize(cols, rows) 参数序陷阱注释锁定（io_test.go:24-25 既有纪律——切勿按 (rows, cols) 误传）"
    - "ro 端 RESIZE 服务端直接忽略（D-09 第二闸——前端 ro 不发是第一闸，05-08 落地）；P2 D-13『ro 放行 RESIZE』为单客户端语境已被 D-09 修订（注释逐字登记修订来源）"
    - "min-rect 保证任何参与端窗口 ≥ PTY 尺寸，各端按自己窗口渲染、多余面积留白——无 S→C 尺寸下发帧（D-09 推论）；纯 ro 会话旁观者运行期窗口缩放不上报、缩到小于 PTY 尺寸者看到裁剪画面、重新 attach 恢复（RESEARCH Pattern 4 行为推论，README 明示在 05-09 落地）"
  prohibitions:
    - "resize 仲裁不得把 PTY 尺寸撑到超出参与集中任何客户端的窗口——任何参与端窗口 < PTY 尺寸即渲染破碎（全屏 TUI 画到可视区外）；min-rect 不变量（PTY ≤ min(参与端窗口)）在任何仲裁路径下不得破坏"
    - "ro 旁观者尺寸不得以任何形式影响可写端 PTY 尺寸（D-09：旁观者永不影响可写端）——包括纯 ro 会话中 ro 端运行期 RESIZE（忽略）与含可写端会话中的 ro 端（不参与）"
  artifacts:
    - path: "internal/server/resize.go"
      provides: "dims 类型 + arbitrate 纯函数 + arbiter（sizes map + 50ms 防抖 timer + 参与集供给 + sess.Resize 挂点）"
      contains: "func arbitrate"
    - path: "internal/server/clients.go"
      provides: "client 尺寸字段（Hello 首尺寸登记）+ 递补升格时参与集切换调用点"
      contains: "arbiter"
    - path: "internal/server/server.go"
      provides: "RESIZE case 改造（ro 忽略 + 参与集上报）+ Hello 首尺寸入 arbiter"
      contains: "reportResize"
    - path: "internal/server/resize_arb_test.go"
      provides: "TestArbitrate 纯函数表测 + TestResizeArbitration Getsize 集成（VALIDATION 05-01-04）"
      contains: "TestArbitrate"
  key_links:
    - from: "internal/server/server.go (RESIZE case)"
      to: "internal/server/resize.go (arbiter)"
      via: "ro → 直接忽略；参与集成员 → arbiter.report(c, cols, rows)：更新 sizes → timer.Reset(50ms) → 到期重算 → 尺寸变化才 sess.Resize"
      pattern: "reportResize"
    - from: "internal/server/clients.go (register/detach/promote)"
      to: "internal/server/resize.go (arbiter)"
      via: "attach 登记 Hello 首尺寸 + 即时重算；detach 移除 + 即时重算；递补升格 → 参与集切换（owner 模式 sizes 只留新 owner）"
      pattern: "arbitrate"
    - from: "internal/server/resize.go (arbiter)"
      to: "internal/pty/io.go (Session.Resize)"
      via: "目标尺寸 ≠ 当前才调用；参数序 (cols, rows)——io_test.go:24-25 锁定"
      pattern: "sess.Resize"
---

<objective>
MULTI-04 resize 仲裁器：arbitrate 纯函数（0/1/N 参与者的 last-wins 与 min-rect）+ D-09 参与集分层（owner 仅 owner / all 全部 rw / 纯 ro 全体 ro Hello 尺寸 / 旁观者不参与）+ 50ms 防抖与即时重算双通道 + ro RESIZE 服务端忽略第二闸。

Purpose: 异尺寸多客户端共存的渲染正确性（ROADMAP 成功准则 3 前半）；ARCHITECTURE §2.9『owner 跟随』表述已作废，算法以 MULTI-04 需求与 D-09 矩阵为准。仲裁器是纯函数 + 薄装配，表测 + Getsize 集成双锁定。
Output: resize.go（新）+ server/clients 挂点 + resize_arb_test.go 两测试，全量 -race 绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-client/05-CONTEXT.md
@.planning/phases/05-multi-client/05-RESEARCH.md
@.planning/phases/05-multi-client/05-PATTERNS.md
@.planning/phases/05-multi-client/05-VALIDATION.md
@internal/server/clients.go
@internal/server/server.go
@internal/pty/io.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: resize.go 仲裁器（arbitrate 纯函数 + 参与集分层 + 防抖/即时双通道）+ server/clients 挂点</name>
  <files>internal/server/resize.go, internal/server/clients.go, internal/server/server.go</files>
  <read_first>
    - internal/server/server.go（RESIZE case 现状 475-480 区、升档 Hello 尺寸消费点、377-386 AfterFunc 防抖先例）
    - internal/server/clients.go（05-03 产出——register/detach/promoteNextLocked 的即时重算挂点）
    - internal/pty/io.go（33-40 Resize fdMu 纪律；io_test.go:24-25 参数序注释）
    - internal/proto/proto.go（158-176 DecodeResize/ClampDim——尺寸入仲裁前已钳 [1,1000]）
    - .planning/phases/05-multi-client/05-RESEARCH.md（Pattern 4 仲裁器完整形态与参与集矩阵、Code Examples arbitrate 逐字、裁断表）
    - .planning/phases/05-multi-client/05-PATTERNS.md（resize.go 节——timer 先例/Resize 纪律/纯函数形态）
  </read_first>
  <action>
    1. 新建 `internal/server/resize.go`（同包 server）：
       - `type dims struct{ cols, rows int }`（注释：入仲裁前已经 proto.ClampDim 钳制 [1,1000]，uint16 转换安全）。
       - `func arbitrate(members []dims) dims`（RESEARCH Code Examples 逐字形态）：0 → dims{}（不动 PTY 保持现状的哨兵——调用方对零值不发起 Resize）；1 → members[0]（last-wins）；≥2 → 逐成员 min(cols)×min(rows)。注释引 MULTI-04 与 D-09。
       - `arbiter` 结构：sizes map[*client]dims（仅参与集成员）+ timer *time.Timer + last dims（当前 PTY 目标尺寸，变化检测用）；方法：
         - `report(c *client, cols, rows int)`（hubMu 内调用）：c 在 sizes 中 → 更新 → timer.Reset(s.resizeDebounce)（50ms 默认，Options.ResizeDebounce 覆写——防抖防 SIGWINCH 风暴，PITFALLS Pitfall 10）；c 不在 → 忽略（D-09 第二闸——ro 旁观者与纯 ro 会话运行期 RESIZE 均落此）。
         - `addMember(c, cols, rows)` / `removeMember(c)` + `recalcNow()`：attach/detach/递补即时重算不防抖。
         - 重算：members 收集 → arbitrate → 结果非零且 ≠ last → s.sess.Resize(cols, rows)（参数序注释：io_test.go:24-25 锁定 (cols, rows) 序；目标尺寸变化才调——P5-3 同尺寸不发 SIGWINCH 且避免无谓 ioctl）→ 更新 last。
       - 参与集供给（D-09 矩阵，经 Server 的 writePolicy/writable 判定）：owner 模式 → sizes 仅 owner 一员；all 模式 → 全部生效 mode==rw 端；无 writable 纯 ro 会话 → 全部 ro 端（尺寸来源 = Hello 首尺寸——注释：ro 运行期 RESIZE 被 report 忽略，运行期缩放不上报、画面裁剪风险 README 明示 05-09）；含可写端会话的 ro 旁观者 → 不入 sizes。
    2. `internal/server/server.go`：
       - Server 加 arbiter 字段（New 装配，timer 初始化为 stopped 态）。
       - 升档完成点：Hello 首尺寸（DecodeHello 已 ClampDim）按参与集规则 addMember + recalcNow（注释：attach 即时重算——新 owner/新 rw 端立即参与仲裁）。
       - RESIZE case（475-480 区）改造：`if c.mode == proto.ModeRO { continue }`（D-09 第二闸——注释逐字登记『P2 D-13 ro 放行 RESIZE 为单客户端语境，已被 D-09 修订』）；rw → s.arbiter.report(c, cols, rows)。
    3. `internal/server/clients.go`：detach（removeLocked 后）→ arbiter.removeMember + recalcNow；promoteNextLocked → owner 模式下参与集切换（sizes 只保留新 owner——removeMember 旧 owner 已在 detach 完成；若递补发生在位切换则同步矫正）+ recalcNow（注释：D-09 递补后新 owner 尺寸接管）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - go build/vet 退出 0；server 包既有测试 -race 全绿（零回归）
    - grep -c 'func arbitrate' internal/server/resize.go == 1（纯函数落位）
    - grep -c 'func.*report\|func.*addMember\|func.*removeMember\|func.*recalcNow' internal/server/resize.go >= 4
    - grep -c 'resizeDebounce\|ResizeDebounce' internal/server/resize.go internal/server/server.go >= 2（Options 字段消费点）
    - server.go RESIZE case 含 ro 忽略闸（grep -A3 'case proto.Resize' internal/server/server.go 含 ModeRO 判定）
    - 防抖经单 time.Timer reset（grep -c '\.Reset(' internal/server/resize.go >= 1）；无 time.AfterFunc 新装配点之外的 goroutine 计时循环（Don't Hand-Roll 纪律）
  </acceptance_criteria>
  <done>仲裁器与三个挂点（report/addMember/removeMember+recalcNow）落码；D-09 参与集矩阵与防抖/即时双通道就位；编译测试绿。仲裁行为断言由 Task 2 锁定。</done>
</task>

<task type="auto">
  <name>Task 2: resize_arb_test.go——TestArbitrate 表测 + TestResizeArbitration Getsize 集成</name>
  <files>internal/server/resize_arb_test.go</files>
  <read_first>
    - internal/server/resize.go（Task 1 产出——被测对象）
    - internal/server/tickets_test.go（1-20, 104-114 同包白盒 + t.Run 子测形态——exact 先例）
    - internal/server/multi_test.go（异尺寸 dialHello 参数化先例——e2e_test.go:131-132 注释）
    - .planning/phases/05-multi-client/05-PATTERNS.md（resize_arb_test.go 节——表测形态与 Getsize 集成断言通道）
    - .planning/phases/05-multi-client/05-VALIDATION.md（05-01-04 行：TestArbitrate/TestResizeArbitration 命名与断言面）
  </read_first>
  <action>
    1. 新建 `internal/server/resize_arb_test.go`：
       - `TestArbitrate`（package server 同包白盒——内部类型不导出，tickets_test.go 先例；t.Run 子测表测）：空切片 → 零值 dims（不动 PTY 哨兵）；单成员 → 原样返回（last-wins）；两成员 {132,43}/{80,24} → {80,24}（min-rect）；三成员含极小值 → 极小矩形；2→1 序列（先两成员取 min、再仅余者）→ 余者尺寸（last-wins 恢复）；输入已钳语义注释（ClampDim 在上游，纯函数不重复钳制）。
       - `TestResizeArbitration`（package server_test 黑盒集成）：write-policy all 会话，A(132x43)/B(80x24) 双 dialHello → 轮询断言 pty.Getsize 读回 == min(80,24)（RESEARCH 核实 winsize.go:18 通道；Getsize 返回 (rows, cols)——断言时注意序）；B CloseNow → 轮询断言 Getsize 恢复 132x43（2→1 last-wins）；防抖合并：Options.ResizeDebounce 覆写 200ms → A 在 100ms 内连发 3 次异尺寸 RESIZE → 100ms 时点断言 Getsize 未变（防抖窗口内未应用）→ 轮询至 2s 断言 Getsize == 最后上报值（合并为一次应用）。
       - owner 模式参与集断言（并本子测）：owner 模式 A(owner 132x43)/B(降级 ro 80x24) → Getsize 跟随 A（132x43 而非 min）——D-09『ro 旁观者永不影响可写端尺寸』；A CloseNow → B 升格 → Getsize 切到 B 尺寸 80x24（递补后新 owner 尺寸接管）。
       - ro RESIZE 忽略闸（并本子测）：B（降级 ro）发 RESIZE 帧 → Getsize 不变（D-09 第二闸）。
       - 集成测试轮询形态沿用 e2e 先例（5s 上限、100ms 步进）；Getsize 断言经本文件局部 helper——resize_arb_test.go 内自建 `startResizeServer(t, argv, opts) (exitCh, wsURL, sess *pty.Session)`：pty.Start + server.New + net.Listen + http.Serve 装配形态照抄 e2e_test.go:105-122 startTestServerWith，但返回值加挂 sess。**不改 startTestServerWith 签名**（e2e_test.go/multi_test.go 31 处调用点零波及——爆炸半径裁断：checker W3 方案 b）；尺寸读回经 creack/pty `pty.Getsize(sess.Master)`（RESEARCH 核实 winsize.go:18 通道；返回 (rows, cols)——断言时注意序，与 sess.Resize(cols, rows) 入参序相反）。
  </action>
  <verify>
    <automated>go test -race -count=1 -run 'TestArbitrate|TestResizeArbitration' ./internal/server/ && go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - TestArbitrate 表测全绿（0/1/2/3 成员 + 2→1 恢复行齐备）
    - TestResizeArbitration 绿：all 模式 min-rect Getsize 读回 + 2→1 恢复 + 200ms 防抖合并（窗口内未应用 + 窗口后应用为最后值）
    - owner 模式子测绿：旁观者尺寸不影响 PTY + 递补后新 owner 尺寸接管
    - ro RESIZE 忽略子测绿：降级 ro 发 RESIZE 后 Getsize 不变
    - grep -c 'func TestArbitrate\|func TestResizeArbitration' internal/server/resize_arb_test.go == 2（VALIDATION 05-01-04 命名逐字对齐）
    - server 包 -race 全量绿（零回归）
  </acceptance_criteria>
  <done>MULTI-04 行为锁定：异尺寸双客户端 min-rect（Getsize 实证）、2→1 last-wins 恢复、50ms 防抖合并、D-09 参与集分层与 ro 忽略双闸。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

edge-probe 对本条需求的分类为 unclassified（中文文本启发式局限，非无边缘），按 #1110 保持 unresolved 显式记录：

- requirement: MULTI-04 ｜ probe: "unclassified — review manually" ｜ status: flagged-unverified ｜ 处置：边角面由 CONTEXT D-09（参与集矩阵 + min-rect 推论）与 RESEARCH Pattern 4、P5-3、A3 实质覆盖；断言已提升为 must_haves.truths（min-rect/防抖/钳制/忽略闸），由 TestArbitrate 表测与 TestResizeArbitration Getsize 集成实证；纯 ro 会话运行期缩放裁剪风险已在 truths 与 README 任务（05-09）显式化。

## Artifacts this plan produces

- 新文件 `internal/server/resize.go`：`dims` 类型、`func arbitrate(members []dims) dims`、`arbiter` 结构（sizes map/time.Timer/last）、`report`/`addMember`/`removeMember`/`recalcNow` 方法
- `internal/server/clients.go`：detach 与 promoteNextLocked 的仲裁即时重算挂点
- `internal/server/server.go`：RESIZE case ro 忽略闸（D-09 第二闸）+ Hello 首尺寸 addMember；`Options.ResizeDebounce` 消费点（50ms 默认）
- 新文件 `internal/server/resize_arb_test.go`：`TestArbitrate`（同包白盒表测）、`TestResizeArbitration`（Getsize 集成：min-rect/2→1/防抖合并/owner 参与集/ro 忽略）、局部 helper `startResizeServer`（自起 pty+server 保留 sess 供 creack/pty Getsize 读回，不触碰 startTestServerWith 31 处调用点）

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端 RESIZE 上报→PTY 全局尺寸 | 不可信客户端的尺寸请求影响全体渲染——仲裁必须保证任何参与端窗口 ≥ PTY 尺寸 |
| RESIZE 频率→SIGWINCH 风暴 | 高频 resize 上报驱动 TIOCSWINSZ/SIGWINCH——防抖与钳制是仅有防线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-11 | Denial of Service | RESIZE 风暴（高频上报致 SIGWINCH 洪泛子进程） | medium | mitigate | 50ms 单 timer 防抖合并（PITFALLS Pitfall 10 防线）+ ClampDim [1,1000] 既有钳制 + 目标尺寸变化才 Resize（P5-3）；TestResizeArbitration 防抖合并子测锁定 |
| T-05-11b | Tampering | 仲裁把 PTY 撑出某客户端窗口（渲染破碎） | medium | mitigate | min-rect 不变量（≥2 一律 min(cols)×min(rows)）+ D-09 参与集分层（旁观者不参与、不拖累）；TestArbitrate 表测 + owner 参与集子测锁定；prohibitions 登记 |
</threat_model>

<verification>
- go test -race -count=1 ./internal/server/ 全绿（含 TestArbitrate/TestResizeArbitration）
- 源码断言：arbitrate 纯函数落位、防抖 timer reset、ro 忽略闸、Options.ResizeDebounce 消费
</verification>

<success_criteria>
- 异尺寸两客户端按 min(cols)×min(rows) 渲染，2→1 时恢复 last-wins（ROADMAP 成功准则 3 前半，Getsize 实证）
- owner 模式旁观者尺寸永不影响 PTY；ro 端 RESIZE 服务端忽略；50ms 防抖合并风暴上报
</success_criteria>

<output>
创建 `.planning/phases/05-multi-client/05-04-SUMMARY.md`：记录 arbitrate 纯函数形态、参与集矩阵落码、防抖/即时双通道、Getsize 断言的 (rows, cols) 序注意点、纯 ro 会话运行期裁剪行为（README 移交 05-09）。
</output>
