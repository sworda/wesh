---
phase: 05-multi-client
plan: 08
type: execute
wave: 8
depends_on: ["05-02", "05-03", "05-04", "05-06", "05-07"]
files_modified:
  - web/src/main.ts
  - web/dist/index.html
autonomous: true
requirements:
  - MULTI-02
  - MULTI-03
  - MULTI-04
  - MULTI-05

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "分享链接进入流程（§Share Link Entry Contract 逐字）：location.pathname 匹配 ^/s/([^/]+)/$ 提取 token → fetch('/api/attach', { method: 'POST', headers Content-Type: application/json, body: JSON.stringify({ token }) }) → 后续 ticket → Hello 链路不变；mode 唯一来源是 Welcome.mode（前端不解析不分支 token 种类）；无尾斜杠 /s/abc 由服务端 301 补斜杠前端无需兼容"
    - "token 处理红线（T-03-24 泄漏面延伸逐字纪律）：token 只存 connect() 闭包变量与 POST body——禁止 console/localStorage/sessionStorage/任何日志调用；禁止 history.replaceState 剥离 URL token（D-03 路径段是分享/书签契约，1013 后手动刷新必须凭原 URL 重新 attach）"
    - "fetch 响应分派矩阵（UI-SPEC 逐字）：ok → ticket 继续 WS；404 → 无认证探测直连（未携 token，既有形态）；401 携 token → C-3 Invalid share link 专版；401 未携 token → P3 Authentication failed 不变；429 → P3 Too many attempts 不变；503 → C-2 Server is full 专版（OQ2）；fetch throw → C-4 Unable to connect（改写后）"
    - "1013 专版（D-10/C-1 逐字）：title Disconnected / body This client was disconnected because it could not keep up with the session output. The session itself is unaffected. / hintPrefix To reattach from the latest output, → [Reload this page].——onclose 只认 ev.code 不渲染 reason 内容（防伪造钓鱼）；不做任何自动重连（Phase 6 CORE-05 边界）；beforeunload listener 移除联动不变"
    - "文案清扫（UI-SPEC §Revisions R1/R2/R3 + C-4/C-5/C-6 逐字）：三处 another client is already attached (wesh currently allows a single client)（main.ts:365-371/519-525/544-549）→ It may have exited, or it is refusing new connections (for example, because it is full).；Connection lost body → The connection closed unexpectedly. + hintPrefix The session may still be running. To reattach,；1008/1009/1011 三条 hintPrefix → If the problem persists, restart wesh from your shell, then；Session ended (1000) 文案不变"
    - "ro 不发 RESIZE（D-09 第一闸）：sendResize 在 !helloSent 门之后加 if (isRO) return——Hello 携首尺寸不受影响（helloSent 门先于 isRO 生效——isRO 仅在 WELCOME 到达后才可能为 true）"
    - "升格 rw 分支（§RO Mode & Promotion Contract 逐字五步）：运行期 Welcome mode === 'rw' → isRO=false + term.options.disableStdin=false → setTitle() 去 [ro] 前缀（单一写口 main.ts:209-211）→ fit.fit()（触发 onResize→sendResize 上报当前尺寸——owner 尺寸接管的前端半侧）→ prefs 应用段照常（升格携 rw 档含 osc52，queryKeys 跳过幂等可重入）→ beforeunload 条件重注册（addEventListener 同参去重幂等）；降级路径不存在不实现"
    - "OSC52 一次性门闩：osc52Loaded 模块级 flag——prefs.osc52 === true 才加载 ClipboardAddon 且仅加载一次（升格 Welcome 重放 prefs 防二次注册 OSC52 handler）；ro 端永远收不到 osc52:true → 永不加载（D-13 前端自然落点，无需 ro 特判代码）"
    - "升格信号 = 标题前缀消失 + 键盘激活，无 toast/面板/badge（UI-SPEC covered：promotion-transition 零新 UI）；粘贴 isRO 门随升格自动放行（04 契约不变）；选中即复制 ro 下生效不变"
    - "既有契约不变项（UI-SPEC covered lift）：空黑终端即加载态无 spinner 无白闪；状态面板结构（标题→正文 8px→提示行 16px、内边距 24px、max-width 480px）与 showStatus 幂等形态不变；resize 浮层行为逐字不变（1000x1000 上界由服务端 ClampDim 钳制既定——浮层为纯本地 UX 辅助，R4）；link tooltip max-width 480px + break-all 不变；标题 sanitize + 128 code point 截断不变（[ro] 前缀经单一写口随 isRO 翻转增删）"
    - "web/dist/index.html 重建入库（含本 plan 全部检索串；.gz 按 .gitignore 既定策略不入库——Phase 03-05 决策）；构建带 time 前缀且构建后验证产物时间戳（项目约束）"
  prohibitions:
    - "token 不得以任何形式离开 connect() 闭包与 POST body（禁 URL query 重写/localStorage/sessionStorage/console/日志/全局变量）——T-03-24 ticket 红线的 token 同款延伸"
    - "onclose 1013 分支不得做任何自动重连（含静默 setTimeout 重连）——后台标签页重连→再被踢循环比手动刷新更差（D-10 边界纪律，Phase 6 一次性做对）"
    - "1013/503 等面板不得渲染 close reason 或服务端下发的任何动态串——文案全部硬编码（防伪造引导钓鱼，onclose 只认 code 既有纪律）"
  artifacts:
    - path: "web/src/main.ts"
      provides: "/s/ token 提取与 fetch body + 响应分派矩阵 + 1013/503/无效链接三专版 + 文案清扫 + 升格 rw 分支 + sendResize isRO 门 + osc52Loaded 门闩"
      contains: "/s/"
    - path: "web/dist/index.html"
      provides: "重建后的单文件产物（含全部新文案与逻辑检索串）"
      contains: ""
  key_links:
    - from: "web/src/main.ts (connect)"
      to: "internal/server (POST /api/attach)"
      via: "/s/([^/]+)/$ 提取 token → body JSON{token} → 分派矩阵（ok/404/401×2/429/503/throw）"
      pattern: "JSON.stringify({ token })"
    - from: "web/src/main.ts (WELCOME 分支)"
      to: "internal/server (升格 Welcome 推送)"
      via: "运行期 mode === 'rw' → disableStdin 翻转 + setTitle 去前缀 + fit.fit() 尺寸上报（R-09 前端半侧）"
      pattern: "disableStdin"
    - from: "web/src/main.ts (onclose 1013)"
      to: "internal/server (1013 踢出)"
      via: "ev.code === 1013 → C-1 硬编码文案 + Reload this page 手动入口——只认 code 不渲染 reason"
      pattern: "1013"
---

<objective>
前端多客户端改造（05-UI-SPEC 行为与文案契约落地）：/s/{token}/ 进入流程与响应分派矩阵、onclose 1013/503/无效链接三专版与单客户端语义文案清扫（R1-R3）、ro 不发 RESIZE 第一闸、WELCOME 升格 rw 分支五步、OSC52 一次性门闩、dist 重建入库。

Purpose: 本 phase 前端零新可视组件（D-07）——契约主体是行为与文案：旁观者/递补者复用既有 ro 形态，升格无通知组件，文案消除多客户端后的事实错误。可视元素零新增零改版，01/04-UI-SPEC 未修订条目逐字有效。
Output: main.ts 改造 + dist 重建（时间戳验证），tsc/build 绿，node --test 既有套件零回归。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-client/05-CONTEXT.md
@.planning/phases/05-multi-client/05-RESEARCH.md
@.planning/phases/05-multi-client/05-PATTERNS.md
@.planning/phases/05-multi-client/05-UI-SPEC.md
@web/src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: /s/ token 进入流程 + fetch 响应分派矩阵 + onclose 三专版与文案清扫</name>
  <files>web/src/main.ts</files>
  <read_first>
    - web/src/main.ts（330-372 connect() 开头与 fetch 段、386-397 WELCOME 分支、500-560 onerror/onclose 三处单客户端文案、575-595 onclose 按码分派与 1013 占位、309-325 showStatus 形态、328-329 ticket 红线注释）
    - .planning/phases/05-multi-client/05-UI-SPEC.md（§Share Link Entry Contract 响应分派矩阵逐字、§Connection & Status Dispatch Contract、§Copywriting C-1..C-6 逐字、§Revisions R1-R3）
    - .planning/phases/05-multi-client/05-PATTERNS.md（main.ts 节改造映射表逐行 + 保留纪律节）
  </read_first>
  <action>
    1. `web/src/main.ts` connect() 开头（330-372 区）：
       - token 提取：`const shareMatch = location.pathname.match(/^\/s\/([^/]+)\/$/);` `const shareToken = shareMatch ? shareMatch[1] : undefined;`——模块级或 connect 闭包内（红线注释：token 只存闭包变量与 POST body——禁 console/localStorage/sessionStorage/日志；禁经 URL 重写 API 剥离 token，D-03 路径段是分享/书签契约，1013 后手动刷新凭原 URL 重新 attach）。
       - fetch 改造：携 token → `fetch('/api/attach', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ token: shareToken }) })`；无 token → 现状空 body 形态不变。UI-SPEC 注释：前端不解析不分支 token 种类——ro/rw 判定唯一来源 Welcome.mode。
       - 响应分派矩阵（UI-SPEC §Share Link Entry Contract 逐字七行）：resp.ok → ticket = (await resp.json()).ticket 继续；resp.status === 404 → 无认证探测（仅未携 token 时可到此分支）ticket = undefined 直连；401 → shareToken !== undefined 走 C-3 专版（title Invalid share link / body This share link is invalid or has expired. Share links are regenerated each time wesh restarts. / hintPrefix Ask the operator for a new link, then）return，否则既有 P3 Authentication failed 不变 return；429 → P3 Too many attempts 不变 return；503 → C-2 专版（title Server is full / body The server has reached its maximum number of attached clients. / hintPrefix Wait for a slot to free up, then）return；fetch throw → C-4（下条）return。
       - auth_failed 静默重试一次（P3 既定）不变——注释：携 token 流程同样适用（重试经 connect() 同一入口重新 POST 携 token）。
    2. onclose 与文案清扫（UI-SPEC §Copywriting 逐字）：
       - 1013 分支（581-587 区）占位文案替换为 C-1：title Disconnected / body This client was disconnected because it could not keep up with the session output. The session itself is unaffected. / hintPrefix To reattach from the latest output, → showStatus + Reload this page 链接形态（location.reload() 既有）；注释：onclose 只认 ev.code 不渲染 reason（slow_consumer 是机器串，防伪造钓鱼）；不做任何自动重连（D-10——Phase 6 CORE-05 边界）；beforeunload 移除联动不变。
       - C-4 三处（365-371、519-525、544-549——PATTERNS.md main.ts 节映射表给出三处行号与旧文）的单客户端语义旧句替换为 It may have exited, or it is refusing new connections (for example, because it is full).（R1 逐字；title 与 hintPrefix 不变）。
       - C-5（onclose default 分支）：body 改 The connection closed unexpectedly.（R2）；hintPrefix 改 The session may still be running. To reattach, → 渲染 ...To reattach, [Reload this page].。
       - C-6（1008/1009/1011 三条共用 hintPrefix）：改 If the problem persists, restart wesh from your shell, then（R3 逐字；title/body 不变——1008/1011 body 仍以 lastError.message 优先）。
       - Session ended (1000) 文案零改动（多客户端下 1000 广播仅子进程退出——语义保持精确为真）。
    3. 本任务不改 WELCOME 分支与 sendResize（Task 2）；pnpm -C web exec tsc --noEmit 与既有 node --test 套件验证。
  </action>
  <verify>
    <automated>pnpm -C web exec tsc --noEmit && cd web && node --test src/lib/*.test.ts && cd ..</automated>
  </verify>
  <acceptance_criteria>
    - pnpm -C web exec tsc --noEmit 退出 0
    - grep -c "JSON.stringify({ token" web/src/main.ts >= 1（fetch body 携 token 落位）
    - grep -c 'history.replaceState' web/src/main.ts == 0（URL token 剥离禁令——红线断言）
    - grep -c 'Invalid share link\|Server is full\|Disconnected' web/src/main.ts >= 3（C-3/C-2/C-1 三专版 title 落位）
    - grep -c 'another client is already attached' web/src/main.ts == 0（单客户端语义文案清扫完成，R1）
    - grep -c 'To reattach from the latest output,' web/src/main.ts == 1（C-1 hintPrefix 逐字）
    - grep -c 'If the problem persists, restart wesh from your shell, then' web/src/main.ts == 1（C-6 共用 hintPrefix 逐字——单写口或三处同源常量）
    - 既有 node --test 套件（prefs/title 等）退出 0（零回归）
  </acceptance_criteria>
  <done>进入流程与分派矩阵、三专版、文案清扫落码；tsc 与既有测试绿；token 红线（闭包+body only、零 replaceState）源码可断。</done>
</task>

<task type="auto">
  <name>Task 2: WELCOME 升格 rw 分支 + sendResize isRO 门 + OSC52 门闩 + dist 重建入库</name>
  <files>web/src/main.ts, web/dist/index.html</files>
  <read_first>
    - web/src/main.ts（386-482 WELCOME 分支全段——ro 单分支现状与 prefs 应用段 queryKeys 跳过、235-242 sendResize、168-186 helloSent/isRO/welcomeDone 状态区、209-211 setTitle 单一写口、7 行 ClipboardAddon 条件加载注释、474-482 welcomeDone 与 beforeunload 注册点）
    - .planning/phases/05-multi-client/05-UI-SPEC.md（§RO Mode & Promotion Contract 五步逐字、§OSC52 Boundary 门闩行、§Multi-Client First-Screen & Title Boundary）
    - .planning/phases/05-multi-client/05-PATTERNS.md（main.ts 节升格分支与 isRO 门行）
  </read_first>
  <action>
    1. `web/src/main.ts`：
       - sendResize（235-242 区）：!helloSent 门之后、readyState 检查之前加 `if (isRO) return;`（D-09 第一闸——注释：Hello 携首尺寸不受影响，helloSent 门先于 isRO 生效——isRO 仅在 WELCOME 到达后才可能为 true；服务端忽略为第二闸 05-04 已落）。
       - WELCOME 分支补 rw 路径（386-397 区，现状 if (w.mode === 'ro') 单分支之后补 else-if/并列分支——UI-SPEC 五步逐字）：运行期 Welcome 且 w.mode === 'rw' → ① isRO = false; term.options.disableStdin = false; ② setTitle()（去 [ro] 前缀——remoteTitle 不含前缀，04 契约单一写口）③ fit.fit()（触发既有 onResize→sendResize 链路——isRO 门已开，自动上报当前尺寸纠正排队期间可能过期的值，owner 尺寸接管的前端半侧）④ prefs 应用段照常执行（升格 Welcome 携 rw 档含 osc52——queryKeys 跳过机制幂等可重入，xterm 键/behavior 键/theme 重放同值无害，注释登记）⑤ beforeunload 条件重注册照既有条件分支（addEventListener 同参重复注册 DOM 规范去重——幂等无泄漏，RESEARCH Pattern 9 核实注释）。注释：握手 Welcome（attach 即 rw）走同一分支无害（isRO 本就 false，disableStdin 重复赋 false，fit 重复触发幂等）；降级路径不存在不实现（owner 只在位到断线）。
       - OSC52 门闩：模块级 `let osc52Loaded = false;`——prefs 应用段的 osc52 === true 加载点改为 `if (prefs.osc52 === true && clipboardOK && !osc52Loaded) { osc52Loaded = true; loadAddon(...) }`（一次性门闩——注释：升格 Welcome 重放 prefs 防 ClipboardAddon 二次注册 OSC52 handler；ro 端永远收不到 osc52:true 故永不加载，D-13 前端自然落点无需 ro 特判）。
    2. dist 重建入库：`time pnpm -C web build`（项目约束 time 前缀 + 全量构建）→ 构建后验证产物时间戳（ls -l --time-style=full-iso web/dist/ 确认 index.html mtime 为本次构建时刻）→ git add web/dist/index.html（.gz 按 .gitignore 既定策略不入库——Phase 03-05 决策注释）。
    3. 验证：tsc --noEmit + 既有 node --test 套件 + grep 检索串确认 dist 含新逻辑（isRO 门/osc52Loaded/三专版文案——单文件内联产物可直接 grep）。
  </action>
  <verify>
    <automated>pnpm -C web exec tsc --noEmit && time pnpm -C web build && ls -l --time-style=full-iso web/dist/ && grep -c 'osc52Loaded' web/dist/index.html && grep -c 'Invalid share link' web/dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - pnpm -C web exec tsc --noEmit 退出 0；time pnpm -C web build 退出 0
    - web/dist/index.html mtime 为本次构建时刻（ls 输出确认——项目时间戳验证约束）
    - grep -c 'osc52Loaded' web/src/main.ts web/dist/index.html 两处均 >= 1（门闩入源码且入产物）
    - grep -c 'if (isRO) return' web/src/main.ts >= 1（sendResize 第一闸——粘贴门既有 isRO 检查另计）
    - grep -c 'disableStdin = false' web/src/main.ts >= 1（升格 rw 分支落位）
    - dist 含三专版文案检索串（Disconnected / Server is full / Invalid share link）
    - 既有 node --test 套件退出 0（零回归）
  </acceptance_criteria>
  <done>升格五步、ro RESIZE 第一闸、OSC52 门闩落码；dist 重建入库且时间戳验证通过；tsc/build/测试全绿——前端多客户端契约完整。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

本 plan 兑现的 4 条需求的 probe 项已在其后端归属 plan（05-01/02/03/04/06）逐条显式记录（no-silent-drop equality 已满足，此处不重复mint）；前端半侧的边角面由 05-UI-SPEC.md 行为/文案契约与 ## UI Considerations（10 covered / 1 backstop / 2 dismissed）实质覆盖——covered 行已全部提升为本 plan must_haves.truths，backstop 行移交 05-09，dismissed 两行（无 form/list 元素、瞬态元素无加载/零态）结构性不适用。

## Artifacts this plan produces

- `web/src/main.ts`：`shareMatch`/`shareToken` 提取（^/s/([^/]+)/$ 正则）、fetch body 携 token 分支、七行响应分派矩阵、C-1/C-2/C-3 三专版 showStatus 调用、C-4/C-5/C-6 文案改写、sendResize `if (isRO) return` 门、WELCOME 升格 rw 分支（五步）、`osc52Loaded` 模块级门闩
- `web/dist/index.html`：重建产物（含全部新逻辑与文案检索串；.gz 不入库既定）

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL→token→POST body | token 经 URL 进入前端——任何额外持久化/日志化都是泄露面 |
| 服务端关闭帧→状态面板 | 关闭码/reason 来自服务端（可被中间人/服务端伪造）——前端文案不得渲染远端内容 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-09 | Spoofing | 1013/关闭帧伪造引导钓鱼（伪造 reason 诱导用户操作） | medium | mitigate | onclose 只认 ev.code 不渲染 reason（既有纪律延伸）；1013/503/无效链接文案全部硬编码英文（C-1/C-2/C-3 逐字契约）；Reload this page 链接仅 location.reload() 无外链 |
| T-05-06b | Information Disclosure | token 经前端面泄露（console/存储/URL 重写） | high | mitigate | token 只存 connect() 闭包变量与 POST body（T-03-24 红线延伸）；禁 history.replaceState 剥离（D-03 书签契约 + D-10 刷新入口）；acceptance 断言 replaceState 零调用；prohibitions 登记 |
| T-05-09b | Tampering | 升格 Welcome 伪造（中间人推送 rw 帧越权） | low | accept | 传输层 TLS+Origin+ticket 链路既有（P3）；且升格不扩大服务端真边界——服务端 INPUT 门按注册表 mode 独立判定（P2 D-13/D-14 纪律），前端 disableStdin 只是 UX 层；伪造帧无法让服务端接受 INPUT |
</threat_model>

<verification>
- pnpm -C web exec tsc --noEmit && time pnpm -C web build 退出 0；dist 时间戳验证通过
- 既有 node --test 套件零回归；dist 检索串断言（osc52Loaded/三专版/isRO 门）
- 源码断言：replaceState 零调用、单客户端文案零残留、C 系文案逐字
</verification>

<success_criteria>
- /s/{token}/ 链接打开即进入对应 mode 终端；无效链接/满员/被踢三条专版文案精确引导手动操作
- 递补者升格时键盘激活 + 标题前缀消失 + 尺寸自动纠正；旁观者永不产生 RESIZE 上行、永不加载 OSC52
- 单客户端语义文案全清扫；dist 产物入库
</success_criteria>

<output>
创建 `.planning/phases/05-multi-client/05-08-SUMMARY.md`：记录分派矩阵落码、文案清扫明细（逐字对照表）、升格五步与门闩、dist 构建耗时与时间戳、遗留（phase05.mjs 协议层验证 → 05-09）。
</output>
