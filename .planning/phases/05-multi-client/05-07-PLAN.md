---
phase: 05-multi-client
plan: 07
type: execute
wave: 7
depends_on: ["05-01", "05-06"]
files_modified:
  - internal/server/server.go
  - internal/server/clients.go
  - internal/server/clients_test.go
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
  - internal/server/multi_test.go
autonomous: false
requirements:
  - RES-03

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "--max-clients flag（D-08：容量策略是部署关切开 flag，与 P2 D-10 攻击面上限常量不同类）；默认 32（defaultMaxClients，05-01 已立——ARCHITECTURE §6『10–100 连接=团队围观/教学』区间下沿；注释挂 Phase 9 负载标定回填）"
    - "满员拒绝在 /ws Accept 之前以 HTTP 503（Attach 守卫区既有形态延伸——⓪Origin 403 → ①子协议 400 → ②halfOpen 429 → ③max-clients 503，05-01 拆除 409 后的③位重建）；拒绝路径 release() 恰好一次纪律不变（P5-5：被 503 拒的连接已持有半开名额，先释放不残留计数——02-03 局部 sync.Once + defer 兜底先例）"
    - "计数口径（R-06）：注册成功后计数（atomic int——registerLocked +1、detach -1）；半开连接不计入（halfOpenCounter 正交两闸）；守卫区③位用 atomic load 判定；并发握手竞态最坏瞬时超编 ≤ per-IP 半开帽 8，可接受且注释明示（容量策略非安全边界）"
    - "计数对称不变量（review #7）：计数器内嵌 registry（`n atomic.Int64`——③位闸在 hubMu 外 atomic load 故须 atomic），registerLocked/removeLocked 内对称 Add——运行期全部移除路径（reader-error detach / 1013 kickOrCreditLocked / pinger CloseNow→reader 错误→detach）收口于 removeLocked 单点，漂移结构上不可能；lifecycle 广播后进程即退出（计数无后续读者）、panic 按 Go 语义进程崩溃（无恢复面）——两路径无漂移窗口；TestClientCountInvariant 白盒逐步锁定 n == len(set)"
    - "满员事件落 logEvent(remote, 503, \"max_clients\")（R-10 命名族——HTTP 层事件 code 复用 HTTP 状态码值，P3 既定裁决；snake_case 与 hello_timeout/pong_timeout/message_too_big 同构）"
    - "/api/attach 503 容量早闸（OQ2 用户 2026-08-19 裁决）：attachHandler（Basic 链与 token 分支同查）在签发 ticket 前 atomic load 满员 → 503——前端 fetch 阶段可给 Server is full 专版文案（05-08 UI-SPEC C-2）；早闸后竞态窗口（ticket 已签但 WS 满员）语义无害，WS 侧③位 503 兜底"
  prohibitions:
    - "max-clients 计数不得单调增长——任何 attach 失败/断开/踢出路径都必须对称减计数（Pitfall 4：计数只增不减 = 运行时间增长静默降低可用容量直至全员 503）；503 拒绝不得消耗 halfOpen 名额（release 恰好一次）"
  artifacts:
    - path: "internal/server/server.go"
      provides: "③位 503 闸 + 注册计数器（atomic）+ /api/attach 503 早闸"
      contains: "max_clients"
    - path: "cmd/wesh/main.go"
      provides: "--max-clients flag（默认 32）→ Options.MaxClients 接线"
      contains: "max-clients"
    - path: "internal/server/multi_test.go"
      provides: "TestMaxClients503（VALIDATION 05-02-02）"
      contains: "TestMaxClients503"
  key_links:
    - from: "internal/server/server.go (Attach ③位)"
      to: "internal/server/clients.go (registry 计数)"
      via: "atomic load >= s.maxClients → release() + http.Error 503 + logEvent；registerLocked +1 / detach -1 对称记账"
      pattern: "maxClients"
    - from: "internal/server/server.go (attachHandler)"
      to: "internal/server/server.go (③位计数)"
      via: "OQ2 早闸：同一 atomic 计数读取，签发 ticket 前检查——一处检查两处用"
      pattern: "StatusServiceUnavailable"
---

<objective>
RES-03 最大并发客户端数：--max-clients flag（默认 32）+ 注册计数器（R-06 口径）+ Attach ③位 503 闸（Accept 前拒绝，release 恰好一次）+ /api/attach 503 早闸（OQ2）+ max_clients 事件。

Purpose: 连接数耗尽防护（ROADMAP 成功准则外但 RES-03 硬需求）；与 per-IP halfOpen 429 构成正交双闸（SEC-08 半开面 vs RES-03 稳态容量面）。
Output: server.go ③位闸与计数 + main.go flag + TestMaxClients503，含 D-08 one-way 确认门。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-client/05-CONTEXT.md
@.planning/phases/05-multi-client/05-RESEARCH.md
@.planning/phases/05-multi-client/05-PATTERNS.md
@.planning/phases/05-multi-client/05-VALIDATION.md
@internal/server/server.go
@cmd/wesh/main.go
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 确认门——--max-clients CLI 公开契约（D-08，one-way）</name>
  <read_first>
    - .planning/phases/05-multi-client/05-CONTEXT.md（decisions 节 D-08 逐字 + Claude's Discretion max-clients 计数口径倾向）
  </read_first>
  <decision>--max-clients flag 默认 32、满员 Accept 前 HTTP 503——CLI flag 公开契约（one-way door：发布后改名/改默认破坏部署脚本与文档）</decision>
  <context>
    D-08 在 discuss-phase 已锁定（容量策略开 flag，与攻击面常量分类不同；503 落 Attach 守卫区既有形态；默认初值负载测试标定 Phase 9 回填）。本门是实现前最后一次确认，不是重新决策。
  </context>
  <options>
    <option id="as-locked">
      <name>按锁定值落地（推荐）</name>
      <pros>--max-clients 默认 32 + ③位 503 + R-06 注册后计数；与 CONTEXT.md D-08 逐字一致</pros>
      <cons>并发握手瞬时超编 ≤8（R-06 已裁决接受，容量策略非安全边界）</cons>
    </option>
    <option id="revise">
      <name>调整 flag 名/默认值/计数口径</name>
      <pros>若有部署侧新信息可低成本修正</pros>
      <cons>推翻 CONTEXT.md 锁定项，需重新 discuss 并回填 RESEARCH 裁断 R-06</cons>
    </option>
  </options>
  <resume-signal>选择 as-locked（继续实现）或 revise（说明调整点）</resume-signal>
  <acceptance_criteria>
    - 用户明确选择 as-locked 后才进入 Task 2；revise 时停止执行并回填 CONTEXT.md
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: ③位 503 闸 + 注册计数器 + --max-clients flag + /api/attach 早闸</name>
  <reversibility rating="one-way">--max-clients flag 是公开 CLI 契约（D-08）——发布后改名/改默认破坏部署脚本与文档；确认门见 Task 1。</reversibility>
  <files>internal/server/server.go, cmd/wesh/main.go, cmd/wesh/main_test.go</files>
  <read_first>
    - internal/server/server.go（守卫区现状 300-334 区——05-01 拆 409 后的③位占位注释；release 恰好一次机制 320-327；attachHandler 现状——05-06 token 分支之后形态；214-232 区）
    - internal/server/clients.go（registerLocked/removeLocked——计数对称挂点）
    - cmd/wesh/main.go（53-78 parseArgs IntVar 先例——maxHalfOpen 类数值 flag 形态）
    - .planning/phases/05-multi-client/05-RESEARCH.md（裁断 R-06 计数口径、Pitfall P5-5 闸顺序与 halfOpen 泄漏、Shared Patterns logEvent 节 R-10）
    - .planning/phases/05-multi-client/05-PATTERNS.md（server.go 节③位改造行与 P5-5 纪律）
  </read_first>
  <action>
    1. `internal/server/server.go` + `internal/server/clients.go`：
       - 注册计数器内嵌 registry：`n atomic.Int64`（clients.go——③位闸在 hubMu 外 atomic load 故须 atomic；注释 R-06 口径：registerLocked 成功后 +1、removeLocked -1——半开不计入与 halfOpenCounter 正交；并发握手竞态瞬时超编 ≤ per-IP 半开帽 8，容量策略非安全边界可接受）。**对称不变量逐字注释（review #7）：运行期全部移除路径——reader-error detach / kickOrCreditLocked 1013 踢出 / pinger CloseNow 后 reader 错误 detach——均收口于 removeLocked 单点，加减排它性对称；lifecycle 广播后进程即退出（计数无后续读者）、panic 按 Go 语义进程崩溃（无恢复面）——两路径无漂移窗口。**
       - Attach ③位重建（05-01 占位注释处）：`if s.registry.n.Load() >= int64(s.maxClients) { release(); logEvent(remote, 503, "max_clients"); http.Error(w, "server is full", http.StatusServiceUnavailable); return }`——P5-5 顺序纪律：⓪Origin → ①子协议 → ②halfOpen 429 → ③503（注释逐字登记：503 必须在 halfOpen acquire 之后——满员时攻击者不得占半开名额；拒绝路径 release() 恰好一次，02-03 sync.Once + defer 兜底先例）；logEvent 的 code 参数 503 复用 HTTP 状态码值（R-10/P3 HTTP 层事件裁决——logEvent 签名为 websocket.StatusCode，强转注释说明）。
       - /api/attach 早闸（OQ2）：attachHandler 内（token peek 之后、Basic/token 任一签发路径之前）`if s.registry.n.Load() >= int64(s.maxClients) { http.Error(w, "server is full", http.StatusServiceUnavailable); return }`——注释：一处检查两处用（WS ③位兜底竞态窗口：ticket 已签但满员 → WS 侧 503，语义无害）；早闸不计 logEvent（HTTP 层常规拒绝，与 401/429 同纪律——事件只落 WS 侧③位？不——P3 纪律：HTTP 层 401/429 经 basicAuth/throttle 内 logEvent 落事件。503 早闸同款落 logEvent(remote, 503, "max_clients") 保持可观测一致性——注释说明双点位同 reason 串）。
    2. `cmd/wesh/main.go`：parseArgs `fs.IntVar(&cfg.maxClients, "max-clients", 32, "maximum simultaneous attached clients (default 32)")`（全名无短选项 P2 D-15；注释：容量策略部署关切 D-08，Phase 9 负载标定回填）；≤0 值经 validateStartup 校验拒绝（exit 2——配置校验矩阵形态，拒绝文案含 "max-clients"）；cfg.maxClients → Options.MaxClients 接线（05-01 已立字段与零值兜底）。
    3. `cmd/wesh/main_test.go`：TestStartupMatrix 扩展（main_test.go:303 表测直调 validateStartup 先例）：
       - 既有行基线同步：validateStartup 新增 ≤0 拒绝后，既有行 config 零值 maxClients=0 会被误拒——全部既有行注入 maxClients: 32 基值（或抽共享基底 config 注入，保持 wantErr/wantWarn 断言语义不变）；
       - 新增拒绝行：maxClients=0 与 maxClients=-1（bind 127.0.0.1 隔离其他校验维度）→ wantErrSub 含 "max-clients"；
       - TestStartupRefusalNoResource 等经 run() 全链路的测试不受影响（parseArgs 默认 32 先填充）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -race -count=1 ./internal/server/ ./cmd/wesh/</automated>
  </verify>
  <acceptance_criteria>
    - go build/vet 退出 0；server 与 cmd/wesh 包既有测试 -race 全绿（零回归）
    - grep -c 'max_clients' internal/server/server.go >= 2（③位 + 早闸双点位 logEvent，R-10）
    - grep -c 'n atomic.Int64' internal/server/clients.go >= 1 且 grep -c 'registry.n.Load' internal/server/server.go >= 2（计数器内嵌 registry + ③位/早闸双读取点，review #7）
    - grep -c '\.n\.Add(1)\|\.n\.Add(-1)' internal/server/clients.go >= 2（registerLocked/removeLocked 对称记账——唯一收口点）
    - grep -c 'max-clients' cmd/wesh/main.go >= 1
    - ③位拒绝路径含 release() 调用（grep -B3 -A3 'StatusServiceUnavailable' internal/server/server.go 可见 release 与 logEvent）
    - TestStartupMatrix 扩展行绿（max-clients ≤0 拒绝两行 + 既有行基值注入后零回归）
  </acceptance_criteria>
  <done>③位 503 闸与对称计数器落码；--max-clients flag 接线；/api/attach 早闸双点位；既有套件零回归。满员行为断言由 Task 3 锁定。</done>
</task>

<task type="auto">
  <name>Task 3: TestMaxClients503（满员 503 + halfOpen 无泄漏 + 槽位释放含 kick 路径）+ TestClientCountInvariant 白盒不变量</name>
  <files>internal/server/multi_test.go, internal/server/clients_test.go</files>
  <read_first>
    - internal/server/multi_test.go（05-01..05-05 产出——装配形态）
    - internal/server/handshake_test.go（HTTP 层拒绝场景断言形态——429/400 先例，503 同款）
    - internal/server/tickets_test.go（1-20, 104-114 同包白盒 t.Run 形态——TestClientCountInvariant 的 exact 先例）
    - internal/server/clients.go（05-01..05-06 产出——registry/registerLocked/removeLocked 被测对象；stall 夹具参照 slowclient_test.go）
    - .planning/phases/05-multi-client/05-VALIDATION.md（05-02-02 行：TestMaxClients503 断言面——满员 503 + halfOpen 计数不泄漏）
  </read_first>
  <action>
    1. `internal/server/multi_test.go` 追加 `TestMaxClients503`（VALIDATION 05-02-02）：
       - 装配：opts MaxClients=2 覆写（测试覆写先例）→ A/B 两 dialHello 成功（计数=2）。
       - 满员拒绝：第三人 WS 握手（websocket.Dial）→ 断言 HTTP 503（Accept 前拒绝——handshake_test.go HTTP 层断言形态；Dial 返回的 *http.Response 状态码断言）；③位拒绝后 halfOpen 不泄漏（行为断言：拒绝后立即可再发起握手到达③位而非被 429 截——或白盒计数归零，按 handshake_test.go 可见性选形态）；stderr 事件含 max_clients reason（captureStderr 先例——limits_test.go/auth_test.go 既有 helper 复用）。
       - /api/attach 早闸：凭据模式实例 MaxClients=1 → 占满后 POST /api/attach（Basic 正确）→ 503（OQ2）；token 通道实例同款（body 携 rw token → 503）。
       - 槽位释放：A CloseNow → detach -1 → 第三人 attach 成功（计数对称断言的行为化——Pitfall 4 防线）。
       - kick 路径槽位释放子场景（review #7）：MaxClients=2，A 正常 + B stall（Options.OutboxBytes 覆写小值 + seq 输出洪水——slowclient_test.go 夹具形态）→ B 被 1013 踢出（removeLocked -1，第二移除路径）→ 第三人 attach 成功——踢出路径计数对称的行为化。
       - -race 必跑。
    2. 新建 `internal/server/clients_test.go`（package server 同包白盒——内部类型不导出，tickets_test.go 先例）`TestClientCountInvariant`（review #7）：
       - 直构造 registry（零值可用——无需 Server/pty 装配），以 &client{} 假客户端序列执行 N 次 registerLocked 与交错 removeLocked；每步断言 `r.n.Load() == int64(len(r.set))`——逐步不变量。
       - 幂等防御断言：对非成员 client 调 removeLocked 为无操作且计数不变（map delete 缺失键 + slice 成员检查——Pitfall 4 防线注释）。
       - -race 必跑（计数器 atomic 与 hubMu 纪律的竞态面）。
  </action>
  <verify>
    <automated>go test -race -count=1 -run 'TestMaxClients503|TestClientCountInvariant' ./internal/server/ && go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - TestMaxClients503 绿：满员 WS 503 + /api/attach 早闸 503（Basic 与 token 双通道）+ halfOpen 无泄漏 + 槽位释放后 attach 成功 + kick 路径槽位释放（review #7 第二移除路径）
    - TestClientCountInvariant 绿（grep -c 'func TestClientCountInvariant' internal/server/clients_test.go == 1）：register/remove 交错序列逐步 n == len(set) + 非成员移除幂等零计数变化
    - grep -c 'func TestMaxClients503' internal/server/multi_test.go == 1（VALIDATION 05-02-02 命名逐字对齐）
    - server 包 -race 全量绿（零回归）
  </acceptance_criteria>
  <done>RES-03 行为锁定：满员 Accept 前 503（WS 与 /api/attach 双点位）、计数对称无泄漏（detach 与 kick 双移除路径行为化 + 白盒逐步不变量——review #7）、槽位释放即可复用。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

edge-probe 对本条需求的分类为 unclassified（中文文本启发式局限，非无边缘），按 #1110 保持 unresolved 显式记录：

- requirement: RES-03 ｜ probe: "unclassified — review manually" ｜ status: flagged-unverified ｜ 处置：边角面由 CONTEXT D-08 与 RESEARCH R-06（计数口径）、P5-5（闸顺序/halfOpen 泄漏）、A5（瞬时超编接受）实质覆盖；断言已提升为 must_haves.truths，由 TestMaxClients503 实证。

## Artifacts this plan produces

- `internal/server/server.go`：Attach ③位 503 闸（release + logEvent(remote, 503, "max_clients") + http.Error）、/api/attach 503 早闸（OQ2）
- `internal/server/clients.go`：registry `n atomic.Int64` 内嵌注册计数器、registerLocked/removeLocked 内计数对称加减（唯一收口点——review #7）
- 新文件 `internal/server/clients_test.go`：`TestClientCountInvariant`（白盒逐步不变量 + 非成员移除幂等——review #7）
- `cmd/wesh/main.go`：`--max-clients` flag（IntVar 默认 32，≤0 拒绝）→ Options.MaxClients 消费
- `cmd/wesh/main_test.go`：TestStartupMatrix max-clients ≤0 拒绝行 + 既有行 maxClients 基值注入
- `internal/server/multi_test.go`：`TestMaxClients503`（含 kick 路径槽位释放子场景——review #7）

## Review 发现处置（05-REVIEWS.md 交叉评审）

| # | 发现（严重度） | 处置 | 说明 |
|---|---------------|------|------|
| #7 | 计数漂移风险——须验证所有退出路径（错误/panic/cancel）经唯一收口点（HIGH） | **采纳** | 计数器内嵌 registry（n atomic.Int64），加减只在 registerLocked/removeLocked 内——运行期全部移除路径（reader-error detach / 1013 kickOrCreditLocked / pinger CloseNow→detach）收口于 removeLocked 单点，漂移结构上不可能（truths 论证 + 代码注释）；lifecycle 广播后进程即退出（无后续读者）、panic 按 Go 语义进程崩溃（无恢复面）——两路径无漂移窗口，显式登记。锁定证据三层：TestClientCountInvariant 白盒逐步不变量（含非成员移除幂等）+ TestMaxClients503 槽位释放（detach 路径）+ kick 路径槽位释放子场景（第二移除路径行为化） |
| — | 瞬时超编行为应向 operator 文档化（MEDIUM） | **采纳** | README 容量段补『注册后计数、并发握手瞬时超编 ≤8』说明（05-09 落地）；T-05-04c accept 行既有论证 |
| — | ro/rw 分别限额（如 10 rw + 100 ro）（MEDIUM 建议） | **延期** | REQUIREMENTS 无 per-mode 容量条目（RES-03 仅要求最大并发数限制）；属 v2 候选特性，如需走新需求流程——不在本 phase 范围 |
| — | admin 端点查看当前客户端数（Suggestion） | **拒绝** | Phase 8 OPS-07 /metrics 是 ROADMAP 既定观测通道（含连接数指标）；新增 ad-hoc admin 端点与本 phase 范围及 OPS-07 设计重叠 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 未认证/已认证客户端→连接槽位 | 连接数是有限容量——耗尽即合法用户不可用；计数器自身不得成为泄漏面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Denial of Service | 连接数耗尽（脚本批量 attach 占满槽位） | high | mitigate | --max-clients ③位 503（Accept 前零 WS 资源分配）+ per-IP halfOpen 429（SEC-08 既有）两闸正交；/api/attach 早闸减少 ticket 空转；TestMaxClients503 锁定 |
| T-05-04b | Denial of Service | 计数器泄漏（运行时间增长静默降容量） | high | mitigate | R-06 对称记账：计数器内嵌 registry（n atomic.Int64），registerLocked/removeLocked 唯一收口点加减——运行期全部移除路径（detach/kick/pinger-CloseNow→detach）经 removeLocked 单点（review #7）；503 拒绝 release() 恰好一次（P5-5）；TestClientCountInvariant 白盒逐步不变量 + 槽位释放与 kick 路径释放双行为断言 |
| T-05-04c | Denial of Service | 瞬时超编竞态（并发握手越过 503） | low | accept | R-06 已裁决：容量策略非安全边界，超编幅度 ≤ per-IP 半开帽 8（默认 32+8=40 账面，内存/goroutine 开销微小）；注释明示，Phase 9 负载标定时复核 |
</threat_model>

<verification>
- go test -race -count=1 ./internal/server/ ./cmd/wesh/ 全绿（含 TestMaxClients503）
- 源码断言：双点位 max_clients 事件、计数对称、flag 接线、release 恰好一次
</verification>

<success_criteria>
- 第 N+1 个客户端（N=max-clients）在 Accept 前收到 HTTP 503，已连接客户端不受影响（RES-03）
- 断开即释放槽位；运行期计数不泄漏；/api/attach 阶段即可获得 503（前端 Server is full 专版的服务端半侧）
</success_criteria>

<output>
创建 `.planning/phases/05-multi-client/05-07-SUMMARY.md`：记录确认门结果、R-06 计数口径落码、双点位 503 形态、flag 默认值论证、测试锁定的泄漏防线。
</output>
