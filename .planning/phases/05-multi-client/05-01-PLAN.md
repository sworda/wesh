---
phase: 05-multi-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/server/clients.go
  - internal/server/server.go
  - internal/proto/proto.go
  - internal/server/e2e_test.go
  - internal/server/multi_test.go
autonomous: true
requirements:
  - MULTI-01

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "双客户端 attach 同一服务端各自收到 Welcome，且实时收到同一 OUTPUT 字节流（两端累积 payload 逐字节一致）——MULTI-01 主干，tracer 切片的核心断言（D-11 单次语义终结后同进程多 WS 建连即本 phase 特性）"
    - "任何客户端断开不再触发 exitf/SIGHUP：服务端进程存活、会话仍可 echo、注册表移除该客户端——CONTEXT.md domain 节必然推论（P1 D-11 单次语义终结；服务端生命周期只随子进程，无客户端时子进程继续运行且新客户端仍可 attach）"
    - "子进程退出是唯一终结路径（P1 D-10）：Wait → Drain → 并行广播 1000 关闭全部客户端（Close 自带 5s+5s 上界故并行等待自然有界）→ exitf(code)，退出码传递语义不变"
    - "hub 每 chunk 只组帧一次（frame := make([]byte, 1+len(chunk))，frame[0]=proto.Output），全部客户端 outbox 共享该只读帧引用——outbox 绝不直接存 onChunk 的 chunk（P5-1：pty/io.go:13-14 明示读循环复用底层缓冲，跨帧持有必须自行拷贝）"
    - "hub 绝不内联 c.Close：outbox.trySend 满 → 注册表同步移除 + cancel() + logEvent(remote, 1013, \"slow_consumer\") + go c.Close(websocket.StatusTryAgainLater, \"slow_consumer\")（P5-2：Close 内联会对 stall 客户端阻塞 ReadLoop 最长 ~10s；close.go 幂等自界且解除全部 goroutine 阻塞）"
    - "Welcome 帧经 outbox 首条入队——每客户端 writer goroutine 是全程唯一 WS 写端，FIFO 保证 Welcome 恒为 S→C 首帧；握手期 Error 帧（version_mismatch/auth_failed）发生在注册前，维持直写不变（P2 D-02 时序纪律）"
    - "服务端 OUTPUT 路径零拷贝不跑 OSC 状态机，'T' TITLE 帧终局不实现（D-12，proto.go 类型字节保留注释；标题恢复靠 Plan 02 的 SIGWINCH 重绘与纯前端解析）"
    - "Options 新增测试可覆写字段 OutboxBytes/MaxClients/InputRate/InputBurst/ResizeDebounce——零值各取默认常量（HelloTimeout/MaxHalfOpenPerIP 先例，server.go:85-133 形态）；默认常量 defaultOutboxBytes=512KiB（R-01）、defaultMaxClients=32、defaultInputRate=32KiB/s、defaultInputBurst=64KiB、defaultResizeDebounce=50ms，全部挂『Phase 9 负载标定回填』注释（P2 D-10 常量纪律）"
    - "INPUT 门由全局 s.writable 改为 per-client c.mode == proto.ModeRO 判定（ro 静默丢，P2 D-13/D-14 的多客户端映射；本 plan 无降级路径故语义等价，owner 降级在 05-03 落地）"
    - "多客户端扇出对前端透明（UI-SPEC ## UI Considerations covered：同一 OUTPUT 帧路径，前端零改动）"
  prohibitions:
    - "多客户端化不得让单客户端路径退化——hub 临界区只含非阻塞 trySend 遍历；无客户端/单客户端时不得引入任何锁等待、cond 等待或逐客户端帧拷贝（单客户端内存与延迟形态与 Phase 4 等价）"
    - "输出帧不得被静默丢弃——trySend 失败的唯一处置是 1013 踢出该客户端，绝不丢帧保连接（有序字节流丢一段转义序列 = 客户端画面静默损坏，RESEARCH Anti-Pattern 2）"
  artifacts:
    - path: "internal/server/clients.go"
      provides: "client 结构 + registry（set map + FIFO order 切片）+ outbox（字节有界队列 + cap 1 信号 channel）+ 每客户端 writer goroutine + hub onChunk 扇出 + kickSlowConsumerLocked"
      contains: "func (s *Server) onChunk"
    - path: "internal/server/server.go"
      provides: "Attach 升档登记（409 原子门拆除→注册表）+ lifecycle 广播 1000 + wsDisconnected→detach + Options 五个新字段零值兜底"
      contains: "defaultOutboxBytes"
    - path: "internal/proto/proto.go"
      provides: "1013 关闭码注释启用（websocket.StatusTryAgainLater 发送路径已活）"
      contains: "1013"
    - path: "internal/server/multi_test.go"
      provides: "TestMultiClientFanout + TestDetach + TestExitBroadcast（VALIDATION 05-W0-01/05-01-01）"
      contains: "TestMultiClientFanout"
    - path: "internal/server/e2e_test.go"
      provides: "单次语义迁移后的全量套件（P5-4：waitExit 断言改写、409 整测替换、信号送达证据两测删除）"
      contains: ""
  key_links:
    - from: "internal/server/server.go (Attach 升档)"
      to: "internal/server/clients.go (registry)"
      via: "checkTicket/mode 判定后构造 client（conn/mode/attachSeq/outbox/cancel），Welcome 入队 outbox，登记进 set+order，启动 writer+pinger"
      pattern: "registerLocked|outbox"
    - from: "internal/pty/io.go (ReadLoop)"
      to: "internal/server/clients.go (hub)"
      via: "sess.ReadLoop(s.onChunk) 唯一读者不变；onChunk 组共享帧后逐客户端 trySend"
      pattern: "trySend"
    - from: "internal/server/clients.go (writer)"
      to: "github.com/coder/websocket (Conn.Write)"
      via: "每客户端 writer 阻塞消费 notEmpty 信号，drain 批量合并单帧写出；写失败直接返回由该客户端 reader 路径收口"
      pattern: "notEmpty"
    - from: "internal/server/server.go (lifecycle)"
      to: "internal/server/clients.go (registry)"
      via: "子进程退出 → Drain → 遍历注册表并行 go c.Close(1000) → WaitGroup 等待 → exitf"
      pattern: "lifecycle"
---

<objective>
MULTI-01 主干 tracer：把单客户端服务端升级为多客户端 fan-out——客户端注册表替代 409 原子门、hub（onChunk）对每客户端有界 outbox 做非阻塞 trySend、每客户端专属 writer 独占 WS 写端、trySend 满即 1013 踢出；同步完成生命周期改造（客户端断开=注册表移除，子进程退出=广播 1000+exitf 唯一终结路径）与 e2e 测试套件单次语义迁移（P5-4，阻塞一切后续新测试）。

Purpose: 这是全 phase 的架构证明切片——注册表/hub/writer 拓扑与 detach/广播生命周期是后续一切（权限/仲裁/限速/分享/容量）的地基；先打通双客户端收同一 OUTPUT 流，后续 plan 全部是在此骨架上的水平扩展。e2e 迁移必须与语义变更同 plan 落地（分两处提交则中间态全红）。
Output: clients.go（新）+ server.go 改造 + proto.go 1013 注释启用 + e2e_test.go 迁移 + multi_test.go 三测试，全量 go test -race 绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-client/05-CONTEXT.md
@.planning/phases/05-multi-client/05-RESEARCH.md
@.planning/phases/05-multi-client/05-PATTERNS.md
@.planning/phases/05-multi-client/05-VALIDATION.md
@internal/server/server.go
@internal/proto/proto.go
@internal/pty/io.go
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): clients.go 注册表/hub/writer + server.go 多客户端化主干改造</name>
  <files>internal/server/clients.go, internal/server/server.go, internal/proto/proto.go</files>
  <read_first>
    - internal/server/server.go（全文 657 行——改造面：61-63 attached/conn 字段、90-103 Options、124-170 New、320-334 守卫区③位 409、405-485 升档与读循环、517-541 onChunk、609-656 lifecycle/wsDisconnected/terminate）
    - internal/pty/io.go（1-30 行 chunk 别名注释与 ReadLoop 契约；33-55 Resize/Close fdMu 纪律）
    - internal/proto/proto.go（1-60 行常量注释区：1013 占位注释 proto.go:11、ModeRO/ModeRW、permission_denied 占位 proto.go:44）
    - .planning/phases/05-multi-client/05-RESEARCH.md（Pattern 1 hub 拓扑与关键纪律、Pattern 2 outbox/共享帧、Pattern 5 注册表结构、Code Examples 1013 踢出逐字形态）
    - .planning/phases/05-multi-client/05-PATTERNS.md（clients.go 节五处先例拼装、server.go 节改造映射表逐行）
  </read_first>
  <action>
    1. 新建 `internal/server/clients.go`（同包 server，新文件头部注释格式参照 tickets.go/throttle.go 既有形态——文件名 + 职责一句话 + 决策引用）：
       - `client` 结构：conn *websocket.Conn、remote string、mode string（proto.ModeRO/ModeRW）、attachSeq int64、outbox *outbox、done chan struct{}、cancel context.CancelFunc。注释：writer goroutine 是该连接唯一 WS 写端（pinger 的控制帧经库 writeFrameMu 与数据写串行化，既有 02-04 纪律）。
       - `outbox` 结构（RESEARCH Pattern 2 逐字形态）：mu、q [][]byte（共享只读帧引用）、bytes int、cap int、notEmpty chan struct{}（cap 1）。方法：`trySend(frame []byte) bool`——o.bytes+len(frame) > o.cap 返回 false，否则 append + 记账 + 非阻塞信号（select default）；`drain() (batch [][]byte, bytes int)`——swap 出整队重置计数。
       - `registry` 结构（R-07）：set map[*client]struct{}、order []*client（attach FIFO）、seq 计数器；方法 registerLocked/removeLocked（remove 必须同时清理 map 项与 slice 项——Pitfall 4 双容器防单调增长）；注册表与 hub 共用 s.hubMu。
       - hub：`func (s *Server) onChunk(chunk []byte)` 迁入本文件——s.hubMu.Lock 内：组帧一次（frame := make([]byte, 1+len(chunk))，frame[0]=proto.Output，copy；注释引 pty/io.go:13-14 别名红线），遍历 set 逐客户端 trySend；失败 → `kickSlowConsumerLocked(c)`：removeLocked + close(c.done)/cancel + logEvent(c.remote, websocket.StatusTryAgainLater, "slow_consumer") + go 匿名函数内 _ = c.conn.Close(websocket.StatusTryAgainLater, "slow_consumer")（R-10 命名族；注释：Close 永不内联——P5-2，close.go:87-96 幂等自界）。
       - `func (s *Server) writer(c *client)`（RESEARCH Pattern 2 逐字形态）：select c.done / outbox.notEmpty → drain → conn.Write(context.Background(), websocket.MessageBinary, 合并后单帧)；写失败静默 return（终结由该客户端 reader 路径收口，注释引 D-11 纪律的多客户端映射）。批量合并 = bytes.Join 或自拼，注释引 ARCHITECTURE §2.5 写合并。
    2. `internal/server/server.go` 改造：
       - 删除 61-62 行的 `attached` 与 `conn` 两字段（整体退役，RESEARCH State of the Art 表）与 63 行 frame 字段（hub 每 chunk 自组帧，不再需要预分配组帧缓冲）；New 内对应初始化行（142 frame、166 frame[0]）同步删除。
       - Server 结构加：hubMu sync.Mutex、registry（clients.go 定义）、五个新参数字段 outboxBytes/maxClients/inputRate/inputBurst/resizeDebounce。
       - Options（90-103）加 OutboxBytes/MaxClients/InputRate/InputBurst/ResizeDebounce 五字段（注释：测试可覆写，零值取默认常量——server.go:85-86 注释形态同款）；New 零值兜底（defaultOutboxBytes = 512*1024、defaultMaxClients = 32、defaultInputRate = 32*1024、defaultInputBurst = 64*1024、defaultResizeDebounce = 50*time.Millisecond；每常量注释挂『Phase 9 负载测试标定回填，P2 D-10 纪律』；InputRate/InputBurst/ResizeDebounce/MaxClients 本 plan 仅立字段与常量，消费点分别在 05-05/05-04/05-07——注释逐字段标明）。
       - 守卫区③位（329-334）：409 原子门与 attached.Store(false) 两处复位（348、359）整体拆除；③位注释改为『max-clients 503 闸由 05-07 落地』占位。② halfOpen 429 与 release 恰好一次纪律零改动。
       - 升档序列（418-465 区）：checkTicket 返回的 mode（tickets.go 既有 mode 字段兑现——认证模式）或无认证模式的全局派生 mode 写入 client；构造 client + outbox（cap=s.outboxBytes）→ Welcome 帧（proto.WelcomeFrame(mode, s.clientPrefs)，组帧函数零改动复用）作为 outbox 首条入队 → registerLocked → go s.writer(client) + go s.pinger(ctx, conn, remote, interval)（pinger 装配形态不变，server.go:449 先例）；原 Welcome 直写与 conn.Store 删除。
       - 读循环 INPUT case（469-483 区）：全局 s.writable 门改为 per-client `if c.mode == proto.ModeRO { continue }`（ro 静默丢，语义等价注释）；Master.Write 直写本 plan 保持（CR-01 输入队列在 05-05）。RESIZE case 保持直接 sess.Resize（注释：多客户端仲裁与 ro 忽略闸在 05-04 落地，本 plan 为 last-wins 延续）。
       - 读循环错误收口（455-465 区）：s.wsDisconnected() 改为 s.detach(client)——hubMu 内 removeLocked + cancel；不进 exitf、不发任何信号（CONTEXT domain 必然推论：P1 D-11 单次语义终结）。
       - lifecycle（619-634 区）：Wait → Drain（200ms 既有）→ hubMu 取快照后并行 go c.Close(websocket.StatusNormalClosure, ...) 广播全部客户端 + sync.WaitGroup 等待（Close 自带 5s+5s 上界，等待自然有界）→ terminate 唯一 exitf 路径（D-10）。terminate(true, 0) 的子进程信号发送分支与 childExited 竞态置位注释随 wsDisconnected 消亡同步清理（路径消亡，注释引 CONTEXT domain 必然推论）。
       - proto.go:11 注释更新：『1013 背压踢出 Phase 5 启用』占位表述改为已启用说明（发送路径 = clients.go kickSlowConsumerLocked，库常量 websocket.StatusTryAgainLater）；proto.go:44 permission_denied 保持占位注释不硬用（CONTEXT 裁断：owner 降级走 Welcome 非 Error，无真实使用场景）。
    3. 本任务提交后 e2e_test.go 旧生命周期断言预期变红（单次语义已终结）——Task 2 同 plan 迁移收口，不要在本任务修测试。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -race -count=1 -run 'TestHelloWelcome|TestVersionMismatch|TestProtocolConstants' ./internal/server/ ./internal/proto/</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... 与 go vet ./... 退出 0
    - grep -c 'func (s \*Server) onChunk' internal/server/clients.go == 1（hub 落位）
    - grep -v '^//' internal/server/server.go | grep -c 'atomic\.' == 0（单客户端原子门与连接指针字段整体退役，注释外零残留）
    - grep -c 'slow_consumer' internal/server/clients.go >= 2（logEvent 与 Close reason 双点位，R-10）
    - grep -c 'go func()' internal/server/clients.go >= 1 且 Close 调用在异步块内（P5-2：踢出绝不内联——以 TestHelloWelcome 等握手套件绿佐证回归）
    - TestHelloWelcome / TestVersionMismatch / TestProtocolConstants 在 -race 下全绿
    - grep -c 'defaultOutboxBytes\|defaultMaxClients\|defaultInputRate\|defaultInputBurst\|defaultResizeDebounce' internal/server/server.go == 5（五常量齐备）
  </acceptance_criteria>
  <done>双客户端 attach 主干在代码层打通：注册表登记、hub 共享帧扇出、writer 独占写端、断开不退出、子进程退出广播 1000 + exitf；编译/vet/握手套件绿。端到端行为断言由 Task 2 的 TestMultiClientFanout/TestDetach/TestExitBroadcast 锁定。</done>
</task>

<task type="auto">
  <name>Task 2: e2e_test.go 单次语义迁移（P5-4）+ multi_test.go fan-out/断开/广播三测试</name>
  <files>internal/server/e2e_test.go, internal/server/multi_test.go</files>
  <read_first>
    - internal/server/e2e_test.go（全文 564 行——迁移面逐处：68-76 TestEchoPTY 断开断言、160-163 waitExit、306-322 helper 信号分支、326-372 第二连接 409 整测、374-402 TestExitCodePropagation、405-433 关闭握手、436-473 信号送达证据测、474-563 ro/rw 两半侧收口）
    - .planning/phases/05-multi-client/05-PATTERNS.md（e2e_test.go 节迁移清单 + multi_test.go 节装配形态——startTestServerWith/dialHello 复用、异尺寸参数化注释 e2e_test.go:131-132）
    - .planning/phases/05-multi-client/05-RESEARCH.md（Pitfall P5-4 迁移面与逐文件核实清单、Validation Architecture 05-W0-01/05-01-01 行）
    - internal/server/clients.go（Task 1 产出——注册表/hub 形态，测试断言的直接对象）
  </read_first>
  <action>
    1. `internal/server/e2e_test.go` 迁移（断言形态与 helper 全部保留，只改生命周期语义）：
       - TestEchoPTY（68-76 区）：客户端断开后的 waitExit(0) 断言改为 exitCh 静默反证（select + 200ms time.After 收不到退出码）+ 服务端存活断言（同实例再 dialHello 成功并 echo 一致）。
       - 第二连接 409 整测（326-372 区）整测替换为『双 attach 成功 + 输出一致』——两 dialHello 同实例，子进程输出后两端各自累积收齐同一 payload；第三人 503 断言不在此做（max-clients 闸 05-07 才落地，由该 plan 的 TestMaxClients503 覆盖）。
       - 信号送达证据两测（436-473 区）与 helper 的 wesh-helper-sighup 分支（306-322 区）整体删除——D-11 信号路径消亡（CONTEXT domain 必然推论）；helperArgv 机制与其他 helper 分支保留。
       - TestExitCodePropagation（374-402）保留核心：子进程 exit 42 → 客户端收到 1000 关闭 → waitExit(42)；注释更新为广播形态（多客户端唯一终结路径，D-10）。
       - 关闭握手测（405-433）：客户端正常关闭后 waitExit(0) 改为 exitCh 静默 + 会话存活（同 TestEchoPTY 新形态）。
       - ro/rw 两半侧测（474-563）的 waitExit(0) 收口全部改为 exitCh 静默反证 + 断开即移除（再 attach 成功）形态。
    2. 新建 `internal/server/multi_test.go`（package server_test 黑盒，helper 复用 e2e_test.go 零改动——PATTERNS exact 先例）：
       - `TestMultiClientFanout`（VALIDATION 05-01-01）：startTestServerWith 起 cat（或 seq 1 100000）→ dialHello 两客户端（e2e_test.go:133 形态，异尺寸参数 80x24 与 132x43）→ 两端各自 Welcome mode 一致（无认证 --writable 均 rw）→ 子进程输出驱动后两端各自累积收齐同一 payload（TestEchoPTY 累积模式 e2e_test.go:52-66 双份；strings.Fields 切分免疫 ONLCR 的既定纪律沿用）→ 两端 payload 逐字节一致断言。
       - `TestDetach`（VALIDATION 05-W0-01）：A/B 双 attach → A CloseNow → exitCh 200ms 静默 + B 继续 echo 正常 + A 立即重新 attach 成功（注册表移除断言的行为化）。
       - `TestExitBroadcast`（VALIDATION 05-W0-01）：A/B 双 attach → 子进程 exit 3 → A/B 两端均收到 1000 关闭（CloseError.Code == websocket.StatusNormalClosure）→ waitExit(3)。
       - 全部测试 -race 必跑；客户端 Read 永不带 deadline ctx（Pitfall 2 回归锁——goroutine Read + 缓冲 channel + select time.After 竞速形态，e2e 既有先例）。
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/server/ ./internal/proto/ ./internal/pty/</automated>
  </verify>
  <acceptance_criteria>
    - go test -race -count=1 ./internal/server/ ./internal/proto/ ./internal/pty/ 全绿（迁移后套件 + 三新测试）
    - grep -c 'func TestMultiClientFanout\|func TestDetach\|func TestExitBroadcast' internal/server/multi_test.go == 3（VALIDATION 05-W0-01/05-01-01 命名逐字对齐）
    - grep -v '^//' internal/server/e2e_test.go | grep -c 'SIGHUP' == 0（信号路径消亡，代码零残留）
    - grep -c 'func TestSecondClient409' internal/server/e2e_test.go == 0（409 整测已替换为双 attach 形态）
    - TestMultiClientFanout 含两端 payload 逐字节一致断言；TestDetach 含 exitCh 静默 + 再 attach 成功断言；TestExitBroadcast 含双端 1000 + exitf(3) 断言
  </acceptance_criteria>
  <done>e2e 套件全部反映多客户端生命周期（断开不退出、子进程退出唯一终结），三新测试锁定 fan-out 一致性/断开移除/广播终结；server+proto+pty 三包 -race 全绿——后续 plan 的一切新测试从此地基上叠。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

本 phase 无 plain *-SPEC.md；edge-probe 对本条需求的分类为 unclassified（中文需求文本未被英文启发式命中，分类器局限——不代表需求无边角），按 #1110 规则保持 unresolved，作为 flagged assumption 显式记录（no-silent-drop）：

- requirement: MULTI-01 ｜ probe: "unclassified — review manually" ｜ status: flagged-unverified ｜ 处置：边角面已由 CONTEXT.md decisions（domain 必然推论/D-11/D-12）与 05-RESEARCH.md（Pattern 1/2、P5-1/P5-2、R-01/R-07/R-10）实质覆盖；具体可验证断言已提升为 must_haves.truths（共享帧只读、hub 绝不内联 Close、断开不退出、广播终结）与任务 acceptance_criteria，执行期由 TestMultiClientFanout/TestDetach/TestExitBroadcast 实证。

## Artifacts this plan produces

- 新文件 `internal/server/clients.go`：`client` 结构（conn/remote/mode/attachSeq/outbox/done/cancel）、`outbox` 类型（trySend/drain/notEmpty）、`registry` 类型（set/order/registerLocked/removeLocked）、`func (s *Server) onChunk`（hub 扇出）、`func (s *Server) writer`、`kickSlowConsumerLocked`、`detach`
- 新文件 `internal/server/multi_test.go`：`TestMultiClientFanout`、`TestDetach`、`TestExitBroadcast`
- `server.Options` 新字段：`OutboxBytes`、`MaxClients`、`InputRate`、`InputBurst`、`ResizeDebounce` + 默认常量 `defaultOutboxBytes`(512KiB)/`defaultMaxClients`(32)/`defaultInputRate`(32KiB/s)/`defaultInputBurst`(64KiB)/`defaultResizeDebounce`(50ms)
- `internal/server/server.go`：`attached`/`conn`/`frame` 三字段退役；③位 409 门拆除（503 闸 05-07 重建）；lifecycle 广播 1000；`terminate(true,0)` SIGHUP 路径消亡
- `internal/proto/proto.go`：1013 关闭码注释启用（`websocket.StatusTryAgainLater` 发送路径激活）
- e2e_test.go：`wesh-helper-sighup` helper 分支与信号送达两测移除；`waitExit(0)` 断开收口断言全量改写

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PTY→WS fan-out | 子进程输出（可信）经 hub 扇出到多个不可信客户端——慢/死客户端不得反向拖死生产者 |
| ReadLoop↔outbox 缓冲边界 | chunk 底层缓冲复用（pty/io.go:13-14）——跨帧持有未拷贝即数据损坏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Denial of Service | hub onChunk 扇出 | high | mitigate | trySend 非阻塞 + 满即 go c.Close(1013) 踢出（P5-2：hub 绝不内联 Close，close.go 幂等自界）；ReadLoop 永不因客户端阻塞（TestMultiClientFanout + Plan 02 TestSlowConsumerKick 锁定） |
| T-05-10 | Tampering | chunk 别名跨帧持有 | high | mitigate | hub 组帧一次 make+copy，outbox 只存共享只读帧引用（P5-1）；两客户端高压 fan-out 逐字节一致断言 + -race 强制 |
| T-05-01b | Denial of Service | writer 写阻塞 | medium | mitigate | writer 允许阻塞（阻塞=慢客户端信号）但写端隔离在 per-client goroutine；终结由 reader 路径收口；pinger 自带 5s 写超时兜底全死连接（02-04 精确分类纪律保留） |
</threat_model>

<verification>
- go build ./... && go vet ./... 退出 0
- go test -race -count=1 ./internal/server/ ./internal/proto/ ./internal/pty/ 全绿（含三新测试）
- 源码断言：hub 落位 clients.go、atomic 字段零残留、SIGHUP 零残留、五默认常量齐备
</verification>

<success_criteria>
- 两个 WS 客户端 attach 同一服务端，各自实时收到逐字节一致的 OUTPUT 流（MULTI-01 成功准则 1 的前半）
- 任一客户端断开服务端不退出、其他客户端无感知；子进程退出时全部客户端收 1000 且 exitf 以子进程退出码被调用
- e2e 套件迁移完成，全量 -race 绿——Wave 2 一切工作的地基
</success_criteria>

<output>
创建 `.planning/phases/05-multi-client/05-01-SUMMARY.md`：记录注册表/hub/writer 拓扑落地形态、生命周期改造决策、e2e 迁移明细（哪些断言改写成什么）、三新测试名称与锁定行为、遗留事项（如③位 503 占位注释）。
</output>
