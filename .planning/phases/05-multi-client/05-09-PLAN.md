---
phase: 05-multi-client
plan: 09
type: execute
wave: 9
depends_on: ["05-05", "05-08"]
files_modified:
  - web/uat/phase05.mjs
  - web/uat/phase02.mjs
  - web/uat/phase03.mjs
  - README.md
  - .planning/phases/05-multi-client/05-UAT.md
autonomous: true
requirements:
  - MULTI-01
  - MULTI-03
  - MULTI-05
  - RES-03

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "phase05.mjs 协议层 UAT 全过（VALIDATION 05-02-01 UAT 侧）：stdout 解析 share read-only/read-write 两行链接（startWesh 正则扩展）→ ro 链接全链（GET 200 无 Basic → POST body 携 token → ticket → WS → Welcome mode=ro）→ rw 链接全链（--writable spawn，Welcome mode=rw）→ 错 token → Basic 矩阵（GET 401 challenge / POST 401 无 oracle）→ 双客户端输出一致（同 port 两 dialHello 收同一 OUTPUT 流）→ 满员 503（--max-clients 小值 spawn：/api/attach 早闸 + WS ③位双点位）→ S6 1013 踢出协议层断言（raw-socket stall 夹具：net.Socket 手工 WS 握手 + masked Hello 后 socket.pause() 真 stall → stderr 断言 code=1013 reason=slow_consumer + 第二正常客户端输出持续推进——review #8：恢复为活跃场景，『Go 测试已覆盖』不构成 UAT 层豁免）"
    - "UAT 红线同守（phase04.mjs:6-9 纪律）：token 值只作断言材料，永不进 check detail/控制台输出——detail 只打状态码/布尔/形状"
    - "phase02.mjs/phase03.mjs 生命周期断言适配多客户端语义（P5-4 UAT 侧）：任何『客户端断开后服务端退出/进程期待退出』断言改为『服务端存活、可再 attach』；单次语义独立 spawn 纪律可放松但不必改（注释更新——多客户端下同进程多 WS 建连即本 phase 特性）；全部原断言面零丢失"
    - "README 多客户端节：分享链接用法（ro/rw 两行、重启即废）+ 反代访问日志记录 /s/ 路径的脱敏建议（D-03 specifics 锁定项——含具体 nginx 配置示例，review #6）+ **分享链接暴露面清单**（review #6：浏览器历史/地址栏可见含屏幕共享旁观、浏览器扩展、桌面索引、AV URL 扫描、明文 http:// 部署 PCAP 可见完整 URL → 不可信网络务必 TLS；吊销=重启）+ --write-policy=owner|all 语义与递补行为 + 纯 ro 会话运行期窗口缩放不上报的裁剪行为（RESEARCH Pattern 4 推论——重新 attach 恢复）+ all 模式多写者输入交错不做排序承诺（Pattern 8）+ 输入限速丢弃语义（review #5：持续输入 >32KiB/s 或单次粘贴超 64KiB burst 的部分被丢弃、连接不受影响、大粘贴建议分段）+ 1013 被踢后手动刷新（无自动重连，Phase 6）+ --max-clients flag（含并发握手瞬时超编 ≤8 说明——review #7 operator 文档）+ 单客户端→多客户端行为变更明示（断开不再退出）+ **默认参数与 Phase 9 标定方法论段**（review #9：五项初值 outbox 512KiB / 输入 32KiB/s+64KiB / max-clients 32 / 防抖 50ms / 信用半水位 50% 与一阶推算依据；Phase 9 标定方法 = 负载矩阵 × 验收标准，详见 <output> 对 SUMMARY 的要求）"
    - "全量验证六段式全绿（VALIDATION full suite command）：GOROOT gofmt 零差异（/usr/bin/gofmt 陈旧须用 GOROOT 版本——Phase 01-03 决策）+ go vet + go test -race -count=1 ./... + time pnpm -C web build + go build -o /tmp/wesh-uat/wesh ./cmd/wesh + node web/uat/phase05.mjs && phase02.mjs && phase03.mjs && phase04.mjs 全过"
    - { statement: "多客户端输出渲染一致性（MULTI-01 渲染层）：协议层 web/uat/phase05.mjs 双客户端 OUTPUT 流一致性自动化断言（headless 策略既定）；浏览器像素层多端一致性按 headless 硬约束豁免——任何自动化（含 playwright）均不可测，UAT 中以 skipped + reason 记录并风险接受（CODEBUDDY.md 显式豁免条款）；人工核对清单落 05-UAT.md（review #8——与 01/02/03/04-UAT.md 同位置同形态，外部浏览器可执行）", verification: backstop }
  prohibitions:
    - "UAT 脚本与 README 不得泄露任何真实 token 值（测试输出/detail/文档示例）——文档示例链接用占位符（如 <ro-token>），spawn 实例的 stdout 链接只作断言材料"
    - "phase02/03/04.mjs 适配不得削弱原断言面——适配只允许『生命周期预期改写』一类变更；任何借机删除/放宽的安全断言（握手拒绝/认证矩阵/上限）都是回归"
  artifacts:
    - path: "web/uat/phase05.mjs"
      provides: "MULTI-05 链接全链 + 双客户端一致 + 错 token + 满员 503 协议层断言"
      contains: "share read-only:"
    - path: "web/uat/phase02.mjs"
      provides: "多客户端生命周期语义适配"
      contains: ""
    - path: "web/uat/phase03.mjs"
      provides: "多客户端生命周期语义适配"
      contains: ""
    - path: "README.md"
      provides: "多客户端节（分享链接/权限模式/仲裁行为/容量/行为变更）+ 反代日志脱敏建议"
      contains: "write-policy"
  key_links:
    - from: "web/uat/phase05.mjs"
      to: "cmd/wesh (stdout)"
      via: "startWesh stdout 正则解析 listening on + share read-only: + share read-write: 三行——链接即断言材料"
      pattern: "share read-"
    - from: "web/uat/phase05.mjs"
      to: "internal/server (/s/ + /api/attach + /ws)"
      via: "GET 链接 → POST token → ticket → dialHelloTicket → Welcome mode 断言（全链模式绑定）"
      pattern: "/s/"
---

<objective>
Phase 5 收口：phase05.mjs 协议层 UAT（分享链接全链/双客户端一致/错 token/满员 503）+ phase02/03.mjs 生命周期断言适配 + README 多客户端节（含 D-03 反代脱敏建议）+ 全量六段式验证。

Purpose: headless 硬约束下协议层 UAT 是多客户端行为的最终端到端证据（CODEBUDDY.md 分层测试策略层 1）；README 兑现 D-03 的反代日志脱敏明示与全部行为变更告知。
Output: phase05.mjs（新）+ 两 UAT 适配 + README 节 + 全量绿——phase 验收就绪（/gsd:verify-work 前置）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-client/05-CONTEXT.md
@.planning/phases/05-multi-client/05-PATTERNS.md
@.planning/phases/05-multi-client/05-VALIDATION.md
@.planning/phases/05-multi-client/05-UI-SPEC.md
@web/uat/phase04.mjs
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: web/uat/phase05.mjs——链接全链 + 双客户端一致 + 错 token + 满员 503 + 1013 踢出（S6 活跃）</name>
  <files>web/uat/phase05.mjs</files>
  <read_first>
    - web/uat/phase04.mjs（全文 264 行——startWesh :44-60 stdout 正则、spawnExpectExit :64-76、dialHello :82-96、waitClose :98-101、check :37-40、场景数组与汇总 :251-264、红线注释 :6-9、单次语义注释 :11-12——全骨架复用 exact 先例）
    - web/uat/phase03.mjs（dialHelloTicket 与凭据模式 spawn 先例——token 全链场景的骨架来源）
    - .planning/phases/05-multi-client/05-PATTERNS.md（phase05.mjs 节——复用件清单与 phase05 特有场景逐字）
    - .planning/phases/05-multi-client/05-VALIDATION.md（05-02-01 行 UAT 断言面 + Sampling Rate 节 full suite command）
  </read_first>
  <action>
    1. 新建 `web/uat/phase05.mjs`（Node ≥22 原生 WebSocket/fetch 零依赖；phase04.mjs 骨架逐字复用——startWesh/spawnExpectExit/dialHello/waitClose/check/场景数组/串行循环/结果汇总）：
       - startWesh stdout 解析扩展：除 listening on 行外解析 "share read-only:  " 与 "share read-write: " 两行的 URL（正则锚行首前缀；token 值只存变量——红线：永不进 check detail/控制台输出，detail 只打状态码/布尔/形状，phase04.mjs:6-9 纪律逐字沿用）。
       - 场景 S1 双客户端输出一致：spawn（--writable）→ 同 port 两次 dialHello（80x24 与 132x43）→ 断言以两端 Welcome 实际 mode 为准（owner 模式默认下 A=rw / B=ro 即 D-07 形态；输出一致断言不受 mode 影响）→ 子进程输出驱动（seq 类命令）→ 两端各自累积收齐同一 payload 逐字节一致。
       - 场景 S2 ro 链接全链（MULTI-05）：spawn 带凭据（--credential，P3 形态）→ 解析 stdout ro 链接 → GET 链接（不带 Authorization）→ 200 且无 WWW-Authenticate challenge → POST /api/attach body JSON{token}（ro token）→ 200 出 ticket → WS Hello{ticket} → Welcome mode=ro；负面对照：GET / （无 token）无 Basic → 401 challenge（Basic 矩阵不变）。
       - 场景 S3 rw 链接全链：同实例（--writable）解析 rw 链接 → 同 S2 链路 → Welcome mode=rw（owner 空位时）；无 --writable spawn 实例 → stdout 无 share read-write 行（D-05 总闸断言——负向 grep stdout）。
       - 场景 S4 错 token：GET /s/{22 字符随机串}/ → 凭据模式 401 challenge；POST /api/attach body 携错 token → 401（响应形态与无 token 401 同文——无 oracle 断言形状级）。
       - 场景 S5 满员 503（RES-03）：spawn --max-clients 1（+--credential）→ 占槽 1 客户端 → POST /api/attach → 503（OQ2 早闸）→ 第二 WS 直连 → 503（③位——WS 握手 HTTP 层状态码经 fetch 升级请求或 Dial 失败响应断言，按 phase02.mjs 握手拒绝断言先例选形态）。
       - 场景 S6 1013 踢出（review #8 恢复为活跃场景——UAT 测全二进制真网络层，『Go 测试已覆盖』不构成豁免）：raw-socket stall 夹具——Node `net.Socket` 直连后手工完成 WS 升级（发送 GET /ws 请求：Host/Upgrade: websocket/Connection: Upgrade/Sec-WebSocket-Key/Sec-WebSocket-Version: 13/Sec-WebSocket-Protocol: wesh.v1 头，读至 \r\n\r\n 收 101），随后发一帧 masked Hello（0x82 + mask 位 + 4 字节 mask XOR 'H'+JSON 载荷，<126 字节单帧）完成注册，立即 `socket.pause()` 停止一切读取——TCP 接收缓冲填满 → 服务端 writer 阻塞 → 默认 512KiB outbox 涨满 → 1013 踢出。spawn 无 --writable（全 ro → 满即踢，信用门不介入）+ 子进程 `seq 1 3000000`（约 20MB 输出洪水）。断言三件套：① spawn stderr 轮询 10s 内出现 logEvent 行含 `code=1013` 且 `reason=slow_consumer`（startWesh 扩展 stderr 捕获缓冲——logEvent 三要素无敏感串，红线不破）；② 同实例第二正常 WS 客户端（dialHello）持续收流（累积字节在踢出前后窗口内单调增长——他人无卡顿）；③ 断言①②后 socket.resume()，10s 内触发 end/close 事件（服务端 Close 完成后 TCP 终结）。注释登记：stall 必须用 raw socket 而非 Node WebSocket 客户端——undici 实现会持续 drain TCP，无法制造内核级 stall。
       - 场景 S7 像素层多端一致性：标记 skipped + reason（headless 硬约束——浏览器渲染层任何自动化均不可测，CODEBUDDY.md 豁免条款；must_haves backstop 行的 UAT 落点）；reason 串指向人工清单 `.planning/phases/05-multi-client/05-UAT.md`（Task 2 产出，外部浏览器可执行）。
       - 单次语义注释（phase04.mjs:11-12 同款位置）更新：多客户端下同进程多 WS 建连即本 phase 特性，独立 spawn 纪律仅保留给需隔离凭据/容量配置的场景。
  </action>
  <verify>
    <automated>go build -o /tmp/wesh-uat/wesh ./cmd/wesh && node web/uat/phase05.mjs</automated>
  </verify>
  <acceptance_criteria>
    - go build 退出 0；node web/uat/phase05.mjs 退出 0 且汇总行全 pass（仅 S7 以 skipped + reason 记录不计失败——S6 已恢复活跃，review #8）
    - S2/S3 全链断言覆盖：GET 200 无 challenge → POST 出 ticket → Welcome mode 正确（ro/rw 各一）
    - S1 双客户端 payload 逐字节一致断言存在且通过
    - S5 双点位 503（/api/attach 早闸 + WS ③位）断言通过
    - S6 三断言通过：stderr 轮询命中 `code=1013` 且 `reason=slow_consumer` + 第二客户端累积字节单调增长 + stall 端 resume 后 10s 内 end/close；grep -c 'socket.pause()' web/uat/phase05.mjs >= 1（raw-socket stall 夹具落位）；grep -c 'Sec-WebSocket-Protocol' web/uat/phase05.mjs >= 1（手工握手头）
    - grep -c 'share read-only:' web/uat/phase05.mjs >= 1（stdout 解析锚点）；脚本内 token 值不出现在任何 check detail 字面（人工核读 + 红线注释存在）
    - S3 含无 --writable 实例 stdout 无 share read-write 行的负向断言
  </acceptance_criteria>
  <done>phase05.mjs 全过：MULTI-05 链接全链（ro/rw/错 token/D-05 总闸）、MULTI-01 双客户端一致、RES-03 双点位 503、MULTI-03 1013 踢出（S6 活跃三断言——review #8）协议层端到端锁定。</done>
</task>

<task type="auto">
  <name>Task 2: phase02/03.mjs 生命周期适配 + 05-UAT.md 人工清单 + README 多客户端节 + 全量六段式验证</name>
  <files>web/uat/phase02.mjs, web/uat/phase03.mjs, README.md, .planning/phases/05-multi-client/05-UAT.md</files>
  <read_first>
    - web/uat/phase02.mjs（全文——握手断言面与生命周期相关断言/注释逐处）
    - web/uat/phase03.mjs（全文——场景 3 无 Origin 400 形态与单次语义注释、S1f 独立 spawn 纪律注释）
    - .planning/phases/04-frontend/04-UAT.md（人工清单形态先例——review #8 S7 清单的同形态参照）
    - README.md（现状结构——协议节/认证 TLS 节/前端体验节，多客户端节插入点）
    - .planning/phases/05-multi-client/05-CONTEXT.md（specifics 节 token 反代日志取舍、D-10 手动刷新纪律）
    - .planning/phases/05-multi-client/05-RESEARCH.md（Pattern 4 纯 ro 裁剪推论、Pattern 8 输入交错不排序、Open Question 3 滴漏振荡接受、R-01 参数表——标定方法论素材）
  </read_first>
  <action>
    1. `web/uat/phase02.mjs` / `web/uat/phase03.mjs` 适配（只改生命周期预期，原断言面零丢失——prohibitions 红线）：
       - 逐处找出『客户端断开 → 服务端退出/进程终止』类断言与注释（phase02 握手违规路径的收口注释、phase03 场景注释中单次语义表述）→ 改为多客户端语义：断开后服务端存活（可再握手/再 attach 断言替换退出断言）；spawnExpectExit 类辅助仅保留给启动校验拒绝路径（exit 2 形态不变——与 WS 生命周期无关）。
       - phase03.mjs 场景 3（无 Origin 400 形态）注释更新：不建 WS 连接不触发任何会话状态变更（原『单次语义退出』表述失效）；S1f 非法 ticket 独立 spawn 纪律保留（隔离 throttle 计数，与单次语义无关——注释改写）。
       - 适配后两脚本全过（断言面数量只增不减——断开类断言改为存活断言，数量守恒）。
    2. 新建 `.planning/phases/05-multi-client/05-UAT.md`（review #8 S7 人工清单——04-UAT.md 同形态，外部浏览器手动执行；headless 硬约束下本机不可自动化，phase05.mjs S7 skipped reason 指向本文件）：
       - 双客户端视觉一致：两浏览器窗口 attach 同一会话 → 输出逐屏一致；异尺寸窗口按 min-rect 渲染、多余面积留白；2→1 后剩余端恢复自身尺寸渲染。
       - 新客首屏：会话运行 vim/htop 时新客户端 attach → 秒见重绘画面（D-11 SIGWINCH），非黑屏等下次输出。
       - ro 形态三要素：`[ro] ` 标题前缀、键盘不可输入、窗口拖动不触发上行（DevTools Network WS 帧面板无 RESIZE 帧）；console 有一次性 read-only mode 提示（review #4）。
       - 递补升格：owner 模式双 rw attach → 第二端 ro 前缀 → 关闭 owner 标签页 → 第二端标题前缀消失、键盘激活、可输入（无 toast/badge）。
       - 1013 专版：（人为慢网/节流工具）stall 后被踢 → Disconnected 面板 + Reload this page → 刷新凭原 URL 重新 attach 成功（D-10 手动入口 + D-03 token 保留）。
       - 503 专版：--max-clients 1 实例第二客户端 attach → Server is full 面板。
       - 无效链接：错误 token /s/ URL → 凭据模式 Basic 框（无自绘错误页）；POST 失败后 Invalid share link 面板。
    3. `README.md` 多客户端节（插入协议节之后/认证节相邻，风格沿用既有章节）：
       - 分享链接：启动打印 ro/rw 两行、复制即用、重启即废全部旧链接（D-02）；rw 行仅 --writable 打印（D-05）。
       - **反代访问日志脱敏建议（D-03 specifics 锁定明示项 + review #6 具体化）**：/s/{token}/ 路径段会被 nginx/Cloudflare/Caddy 等反代访问日志记录——附具体 nginx 示例（map $uri $sanitized 对 ~*^/s/ 重写为 /s/&lt;redacted&gt;/ 后 log_format 引用，或 location /s/ { access_log off; } 任选其一的注释形态），token 即访问凭证。
       - **分享链接暴露面清单（review #6 逐向量）**：浏览器历史/地址栏可见（含屏幕共享与截屏旁观）——演示时注意；浏览器扩展、桌面搜索索引、杀毒软件 URL 扫描可能读取历史中的链接；明文 http:// 部署时网络监听（PCAP）可见完整 URL——不可信网络务必 TLS（--tls-cert/--tls-key）；怀疑泄露时重启 wesh 即废全部旧链接（吊销=重启，D-02）。
       - 权限模式：--write-policy=owner|all（默认 owner）——owner 模式首个 rw attach 可写、后续 rw 旁观自动递补；all 模式全员可写（协作排障）；all 模式多写者输入交错不做排序承诺（RESEARCH Pattern 8）。
       - resize 行为：≥2 客户端最小公共矩形、2→1 恢复；纯 ro 会话旁观者运行期窗口缩放不上报——缩到小于 PTY 尺寸者看到裁剪画面，重新 attach 恢复（Pattern 4 推论明示）。
       - 输入限速（review #5 明示）：每客户端持续输入超过约 32KiB/s 或单次粘贴超过 64KiB burst 的部分会被丢弃（连接不受影响、不会收到错误提示）——大粘贴建议分段进行。
       - 慢客户端：接收过慢被 1013 断开属预期保护（他人不受影响）——手动刷新即可从最新输出看起；当前版本无自动重连（Phase 6）。
       - 容量：--max-clients（默认 32），满员 503；并发握手瞬时超编 ≤8（注册后计数口径，容量策略非安全边界——review #7 operator 文档）。
       - 默认参数与 Phase 9 标定（review #9 方法论）：五项初值（outbox 512KiB/客户端、信用恢复水位 50%、输入 32KiB/s+burst 64KiB、--max-clients 32、resize 防抖 50ms）为一阶推算合理值，Phase 9 负载标定回填——标定方法 = 客户端数（1/4/16/32）× 输出速率 × 慢链路注入的负载矩阵，验收 = 合法慢端零误踢 + 内存上界 + 门开闭频率，数据源 = 本 phase 计数器 stub（kicks/gateTransitions/inputDrops/droppedInputs/registry.n）。
       - 行为变更明示：多客户端化后客户端断开不再使服务端退出（旧版单次语义终结）；子进程退出仍正常关闭全部连接并退出。
    4. 全量六段式验证（VALIDATION full suite command 逐字 + 项目纪律）：
       - GOROOT gofmt -l .（零差异——/usr/bin/gofmt 陈旧，用 $(go env GOROOT)/bin/gofmt，Phase 01-03 决策）
       - go vet ./... && go test -race -count=1 ./...
       - time pnpm -C web build（构建后 ls -l --time-style=full-iso web/dist/ 验证时间戳）
       - go build -o /tmp/wesh-uat/wesh ./cmd/wesh
       - node web/uat/phase05.mjs && node web/uat/phase02.mjs && node web/uat/phase03.mjs && node web/uat/phase04.mjs 全过
  </action>
  <verify>
    <automated>test -z "$($(go env GOROOT)/bin/gofmt -l .)" && go vet ./... && go test -race -count=1 ./... && time pnpm -C web build && go build -o /tmp/wesh-uat/wesh ./cmd/wesh && node web/uat/phase05.mjs && node web/uat/phase02.mjs && node web/uat/phase03.mjs && node web/uat/phase04.mjs</automated>
  </verify>
  <acceptance_criteria>
    - gofmt -l 输出为空（GOROOT 版本）；go vet 退出 0；go test -race -count=1 ./... 全绿
    - time pnpm -C web build 退出 0 且 dist 时间戳验证通过
    - 四个 UAT 脚本依次退出 0（phase02/03 适配后全过、断言数量不少于适配前）
    - README.md 含 write-policy 与 max-clients 文档串（grep -c 'write-policy\|max-clients' README.md >= 2）
    - README.md 含反代访问日志脱敏建议段（grep -ci 'access.*log\|脱敏' README.md >= 1——D-03 明示项）且含具体 nginx 示例（grep -c 'access_log\|log_format' README.md >= 1——review #6 具体化）
    - README.md 含暴露面清单（grep -ci 'PCAP\|屏幕共享\|桌面' README.md >= 2——review #6）与 TLS 部署建议（grep -c 'tls-cert' README.md >= 1）
    - README.md 含输入限速丢弃语义（grep -c '32KiB' README.md >= 1——review #5）、瞬时超编说明（grep -c '超编' README.md >= 1——review #7）、Phase 9 标定段（grep -c '标定' README.md >= 1——review #9）
    - .planning/phases/05-multi-client/05-UAT.md 存在且含七组清单项（grep -c '双客户端视觉一致\|新客首屏\|ro 形态\|递补升格\|1013 专版\|503 专版\|无效链接' .planning/phases/05-multi-client/05-UAT.md >= 7——review #8 S7 人工清单）
    - README.md 含『断开不再使服务端退出』行为变更明示（grep -c '断开不再使服务端退出' README.md >= 1）
  </acceptance_criteria>
  <done>前序 UAT 适配零断言丢失；05-UAT.md 人工清单落地（S7 豁免闭环）；README 多客户端节覆盖 D-03 脱敏（含 nginx 示例）/暴露面清单/权限/仲裁/输入限速/容量/标定方法论/行为变更；六段式全绿——phase 验收就绪。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

本 plan 为验证与文档收口：8 条需求的 probe flagged assumptions 已在 05-01..05-07 逐条显式记录（no-silent-drop equality 满足）。本 plan 新增的显式假设：

- UAT S6（1013 踢出）经 review #8 恢复为活跃协议层场景：raw-socket stall 夹具（Node net.Socket 手工握手 + masked Hello + socket.pause() 制造内核级 stall）+ stderr logEvent `code=1013 reason=slow_consumer` 可观测通道 + 第二客户端推进断言。原『Go 集成测试已覆盖故 skipped』处置被 reviewer 驳回——UAT 测全二进制真网络层，层级不等价，本 plan 采纳并完成改造。S7（像素层一致性）维持 skipped + reason：headless 硬约束豁免（CODEBUDDY.md 平台原生行为豁免条款），人工清单落 05-UAT.md 完成缓解闭环——两者均非静默丢弃。
- reviewer 建议的错误恢复场景映射（review MEDIUM 处置——全部已有覆盖层，无需新增测试）：owner 崩溃中途退出 = detach → FIFO 递补（TestSuccession/TestSuccessionKickRace）；客户端网络分区 = pinger pong_timeout ≤(interval+5s) 收口（02-04 keepalive 套件 + TestGlobalCredit CloseNow 子场景）；服务端重启 = 分享链接全部失效（D-02 吊销语义，README 明示）；过期 ticket 重连 = P3 认证矩阵 401 同文同码（auth_e2e 套件回归）。

## Artifacts this plan produces

- 新文件 `web/uat/phase05.mjs`：S1 双客户端一致 / S2 ro 链接全链 / S3 rw 链接全链 + D-05 总闸负向 / S4 错 token / S5 满员双点位 503 / **S6 1013 踢出活跃场景（raw-socket stall 夹具 + stderr code=1013 断言 + 第二客户端推进 + resume 终结——review #8）** / S7 skipped 豁免记录（reason 指向 05-UAT.md）；startWesh stdout 三行解析 + stderr 捕获缓冲扩展
- 新文件 `.planning/phases/05-multi-client/05-UAT.md`：多客户端人工核对清单七组（双客户端视觉一致/新客首屏/ro 形态/递补升格/1013 专版/503 专版/无效链接——review #8 S7 闭环，04-UAT.md 同形态）
- `web/uat/phase02.mjs` / `web/uat/phase03.mjs`：多客户端生命周期语义适配（断开 → 存活断言）
- `README.md`：多客户端节（分享链接 + 反代脱敏建议含 nginx 示例 + **暴露面清单 + TLS 建议**（review #6）+ write-policy + resize 行为 + **输入限速丢弃语义**（review #5）+ 慢客户端 1013 + max-clients 含**瞬时超编说明**（review #7）+ **默认参数与 Phase 9 标定方法论**（review #9）+ 单次语义终结行为变更）

## Review 发现处置（05-REVIEWS.md 交叉评审）

| # | 发现（严重度） | 处置 | 说明 |
|---|---------------|------|------|
| #8 | S6（1013 踢出）与 S7（多客户端视觉一致）skipped 理由偏弱（HIGH） | **采纳** | S6 恢复为活跃协议层场景：raw-socket stall 夹具（net.Socket 手工 WS 握手 + masked Hello + socket.pause() 内核级 stall）+ stderr `code=1013 reason=slow_consumer` 断言 + 第二客户端推进断言——层级论证成立：UAT 测全二进制真网络，『Go 测试已覆盖』不构成豁免；S7 维持 headless 豁免（CODEBUDDY.md 硬约束，任何自动化含 playwright 均不可测），以 05-UAT.md 人工清单七组完成缓解闭环 |
| #6 | token 暴露面文档不足（HIGH，与 05-06/05-08 共同处置） | **采纳** | README 暴露面清单（浏览器历史/扩展/桌面索引/AV/截屏共享/明文 PCAP）+ nginx 脱敏具体示例（map $uri $sanitized 或 access_log off）+ TLS 部署建议 + 吊销=重启说明 |
| #5 | 静默输入丢弃（HIGH，与 05-05 共同处置） | **采纳（文档缓解）** | README 输入限速丢弃语义明示（>32KiB/s 或超 burst 部分丢弃、连接不受影响、大粘贴建议分段）；运行期协议反馈帧维持延期（05-05 冲突记录） |
| #9 | 参数标定推迟 Phase 9 且无方法论（MEDIUM，跨切面） | **采纳** | README『默认参数与 Phase 9 标定』段：五项初值与依据 + 标定方法（客户端数 × 输出速率 × 慢链路注入负载矩阵）+ 验收标准（合法慢端零误踢/内存上界/门开闭频率）+ 数据源（本 phase 计数器 stub）；SUMMARY 同步记录（见 output） |
| #7 | 瞬时超编 operator 文档（MEDIUM，与 05-07 共同处置） | **采纳** | README 容量段『并发握手瞬时超编 ≤8（注册后计数口径）』明示 |
| #10 | 观测性缺口（MEDIUM，跨切面） | **采纳（stub 清单交接）** | SUMMARY 记录五处计数器 stub 清单（kicks/gateTransitions/inputDrops/droppedInputs/registry.n）与消费方式，供 Phase 8 OPS-07 直接接线 |
| — | 错误恢复场景缺失（owner 崩溃/网络分区/服务端重启/过期 ticket）（MEDIUM） | **拒绝（已有覆盖层）** | Flagged Assumptions 映射表：四场景各有既有测试或文档覆盖（TestSuccession 系/pinger 套件/D-02 吊销/P3 认证矩阵）——新增场景属重复覆盖 |
| — | 无负载/压力测试（MEDIUM） | **延期** | Phase 9 负载标定是 ROADMAP 既定边界；方法论由 review #9 段落地，本 phase 计数器 stub 提供数据源 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT 输出/文档→token | 测试控制台与 README 是持久面——真实 token 落盘即泄露样本 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06c | Information Disclosure | token 经测试输出/README 泄露 | high | mitigate | UAT 红线（token 只作断言材料，detail 只打状态码/布尔/形状——phase04.mjs:6-9 纪律）；README 示例链接用占位符；acceptance 含红线注释与负向核读 |
| T-05-06d | Information Disclosure | 反代访问日志记录 /s/{token}/ 路径 | medium | mitigate | D-03 已裁决接受的残余面——README 显式脱敏建议（nginx log_format 过滤/关闭 access_log）为本 plan 强制交付项；acceptance grep 断言 |
</threat_model>

<verification>
- 六段式：GOROOT gofmt 零差异 / go vet / go test -race -count=1 ./... / time pnpm -C web build / go build / 四 UAT 脚本全过
- README 串断言：write-policy / max-clients / 脱敏建议 / 行为变更明示
</verification>

<success_criteria>
- phase05.mjs 全过且 phase02/03/04.mjs 回归全过——VALIDATION.md phase gate 条件满足，可进 /gsd:verify-work
- README 使用户无需读代码即可正确使用分享链接与权限模式，并知晓反代日志暴露面与全部行为变更
</success_criteria>

<output>
创建 `.planning/phases/05-multi-client/05-09-SUMMARY.md`：记录 phase05.mjs 场景矩阵（含 S6 raw-socket 夹具形态与三断言、S7 豁免与 05-UAT.md 指针）、前序 UAT 适配明细（逐处改写对照）、README 新节结构、六段式各项耗时与产物时间戳、**观测性 stub 清单（review #10 Phase 8 交接）：registry.kicks / registry.gateTransitions / s.inputDrops / droppedInputs / registry.n 五处计数器的名称、位置、语义与 Phase 8 OPS-07 消费方式**、**Phase 9 标定方法论记录（review #9）：负载矩阵维度与验收标准**、phase 验收建议。
</output>
