---
phase: 05-multi-client
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/server/resize.go
  - internal/server/clients.go
  - internal/server/resize_test.go
  - internal/server/clients_test.go
autonomous: true
requirements: [MULTI-04, RES-04]
gap_closure: true
gap_ids: [WR-01, WR-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WR-01：pushSessionDimsLocked 在 kickOrCreditLocked 返回后复检 `s.arbiter.last != target` 则中止——推送循环内踢出经 removeMember→嵌套 recalcNow（clients.go:479-480）把仲裁推进到 T2 的交织下，嵌套推送已把 T2 送达全部留存端，外层 stale 扇出中止后任何留存端持有的最近一帧 Welcome 尺寸恒 == 最新仲裁尺寸（白盒测试逐帧锁定该交织）"
    - "WR-02：afterDrain 清位开门时向恢复端补发一帧当前 sessionDimsLocked() 的 Welcome（creditPending 重投之后、hubCond.Broadcast 之前，用户 2026-08-22 裁决 option (a)）——creditBlocked 期间被 `if !c.creditBlocked` 守卫静默丢弃的尺寸推送在恢复时收敛，该端 sessionDims 不再过期至下次尺寸事件"
    - "resize.go 推送安全性注释论证覆盖真实可达的 removeMember→嵌套 recalcNow 路径（不再只论证推送循环内不可达的 promoteNextLocked 路径）；「触发帧不丢」承诺收窄为首帧暂存语义 + afterDrain 补发收敛"
    - "既有行为零回归：全量 go test -race 5/5 包绿 + go vet 干净 + phase05 三层 UAT（28/28+1skip、19/19、3/3）全绿——两修复不引入可观测协议行为变化（复用既有 'W' 帧：WR-01 中止的是 stale 扇出，WR-02 补发的是已滞后端错过的 Welcome）"
  artifacts:
    - "internal/server/resize.go：pushSessionDimsLocked 增 `arbiter.last != target` 复检 + 文档注释安全性段落改写（removeMember→嵌套 recalcNow 路径论证）"
    - "internal/server/clients.go：afterDrain 补发当前会话尺寸 Welcome（05-REVIEW WR-02 option (a) 逐字补丁形态）+ kickOrCreditLocked/afterDrain 文档注释同步"
    - "internal/server/resize_test.go：TestPushSessionDimsKickRecalc（推送循环内踢出改变仲裁结果的交织白盒复现）"
    - "internal/server/clients_test.go：TestAfterDrainResendsDims（blocked 端恢复时收到当前尺寸 Welcome 补发 + blocked 期后续推送帧不覆写暂存的守卫语义锁）"
  key_links:
    - "kickOrCreditLocked→kickSlowConsumerLocked→removeMember→嵌套 recalcNow（clients.go:479-480）→arbiter.last 推进→外层复检中止（WR-01 收敛链）"
    - "afterDrain→creditBlocked=false→sessionDimsLocked→按 c.mode 选档 prefs（D-13 双档在补发通道不漂移）→WelcomeFrame→trySend→hubCond.Broadcast（WR-02 收敛链）"
---

<objective>
闭合 05-VERIFICATION.md（2026-08-22 复验，status=gaps_found）gaps 块仅剩的两条缺陷——G-05-1 闭合缝合面上的两条代码核读确认的可达路径，同属「端 sessionDims ≠ PTY 实际尺寸 → 相对寻址流叠写」缺陷类：

- **WR-01（Blocker）**：pushSessionDimsLocked(target) 遍历 registry.set 时，若循环内 kickOrCreditLocked 踢出某客户端，removeMember 触发嵌套 recalcNow 把 arbiter.last 推进到 T2 并把 W(T2) 送达全部留存端，外层循环恢复后仍用捕获的 T1 向未访问端投递——终值倒挂（端终值 = 过期 T1 而 PTY = T2，map 遍历序下约半数留存端命中），G-05-1 症状类在窄窗口内瞬态复发。修复 = 05-REVIEW WR-01 逐字补丁：kickOrCreditLocked 返回后复检 `if s.arbiter.last != target { return }`（嵌套推送已送达更新值，stale 外层扇出直接中止）。
- **WR-02（Warning，用户裁决 option (a)）**：已 creditBlocked 端的尺寸推送帧被 `if !c.creditBlocked` 守卫跳过暂存而静默丢弃，afterDrain 恢复只重投 creditPending 的 OUTPUT 帧、永不补发会话尺寸——该端 sessionDims 过期至下次尺寸事件。修复 = 05-REVIEW option (a) 逐字补丁：afterDrain 在 `c.creditBlocked = false` 之后、Broadcast 之前向该端补发一帧当前 sessionDimsLocked() 的 Welcome。

Purpose: 让 05-10 的 success criterion「任意时刻任意在线客户端持有的最近一帧 Welcome 尺寸 == 服务端会话尺寸」在全部已核读交织下成立，复验可判 gaps_closed。
Output: 两处服务端小补丁 + 两个白盒回归测试 + 全量回归证据。两修复均为内部逻辑 + 注释——proto.go 不动、WelcomeFrame 4 参签名（05-10 既定）不变、前端零改动、无新导出符号。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-client/05-CONTEXT.md
@.planning/phases/05-multi-client/05-VERIFICATION.md
@.planning/phases/05-multi-client/05-REVIEW.md

@internal/server/resize.go
@internal/server/clients.go
@internal/server/resize_test.go
@internal/server/clients_test.go
@internal/proto/proto.go
@internal/pty/spawn.go
</context>

<interfaces>
<!-- 无新增跨 plan 契约：WelcomeFrame(mode, prefs, cols, rows) 4 参签名为 05-10 既定形态，
本 plan 两个修复均复用该签名组帧（WR-02 补发帧与 pushSessionDimsLocked 推送帧同形同语义）。
proto.go / 前端 / UAT 三脚本零漂移；旧断言面（S10/D6/D6H）即回归锁。 -->
</interfaces>

<gate_assessments>
本 plan 对四项 plan:pre capability contribution 的适用性评估（逐项记录，不静默跳过）：

- **security（ASVS L1，block_on=high）**：适用——已随附 <threat_model>。两修复为服务端内部可用性/一致性加固，不新增外部输入面、不触碰认证/凭据/token 路径；全部威胁评级 low，无 high 阻断项。
- **ai-integration（API Coverage 决策点）**：不适用——确定性检测器对 phase scope（05-10/11/12-PLAN + ROADMAP Phase 5 节）实测返回 `{"detected":false,"signals":[]}`。本 plan 为 WS 会话内部修复，零外部 API/SDK 集成，按其契约跳过且不向用户上报。
- **assumption-delta**：不适用——`assumption-delta scan 05 --json` 实测返回 `detected: false`。本 plan 不引入单→复数/必需→可选/派生→选择的身份模型迁移（修复对象是既有机制的正确性，非抽象边界变化）。
- **schema-gate**：不适用——phase scope 无任何 ORM schema 文件模式（无 Payload/Prisma/Drizzle/Supabase/TypeORM 路径）；两修复不修改 proto.go，WelcomeFrame 签名与 Welcome JSON schema 恒不变，确认记录。
- **SPEC/UI-SPEC**：本 phase 无 SPEC（EDGE_ABSENT）；05-UI-SPEC.md 存在但本 gap 闭合**零前端改动**（两修复均在 internal/server），不提取 UI 考量，显式记录。
- **§B prohibition 两阶段回想**（scope = 推送循环内踢出交织 + creditBlocked 丢帧）：留存候选（「端终值恒为最新仲裁尺寸」「blocked 端恢复时尺寸收敛」）均为常规工程正确性，已由本 plan 修复与回归测试所有——按协议 DROP，无 values/safety 级 prohibition 存活，must_haves 不增 prohibitions 块。既有 phase prohibition「绝不丢帧保连接」（05-02 锁定）不受削弱：WR-02 修复恰是其收敛性补强。
</gate_assessments>

<flagged_assumptions>
- **假设 FA-13-1（specless 探针合并登记，8 行合一）**：确定性 edge 探针对本 phase 8 条需求返回 8 行 unclassified/unresolved（分类器无法归类中文需求文本，报告 /tmp/p5-edge-coverage.json）——**MULTI-01、MULTI-02、MULTI-03、MULTI-04、MULTI-05、RES-02、RES-03、RES-04** 逐条枚举在此。该 8 条全部已由 05-01..05-12 十二个已执行 plan 的 must_haves 承载，并经 05-VERIFICATION.md Requirements Coverage 表逐条判 SATISFIED（附证据：测试名 + UAT 场景 + 本次独立复跑记录）；本 gap-closure plan 按协议不重复验证这 8 条，仅以修复触及面申报 requirements=[MULTI-04, RES-04]（WR-01 = resize 仲裁推送正确性 → MULTI-04；WR-02 = 信用门/背压交界面 → RES-04）。8 surfaced == 8 flagged（本条合并登记），无静默丢弃。
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: WR-01 — pushSessionDimsLocked 嵌套重算复检中止 stale 扇出 + 注释改写 + TestPushSessionDimsKickRecalc 白盒回归</name>
  <files>internal/server/resize.go, internal/server/resize_test.go</files>
  <reversibility rating="reversible">局部小补丁：一处复检 + 注释 + 一个测试文件新增函数，git 单提交可整体回退，无公开契约变化</reversibility>
  <read_first>
    - .planning/phases/05-multi-client/05-REVIEW.md — WR-01 Fix 块（逐字补丁与「同时修正注释」指令的唯一事实源，照抄形态勿自由发挥）
    - .planning/phases/05-multi-client/05-VERIFICATION.md — gaps 块（WR-01 缺陷链、可达性证明、「不可 defer」裁定原文）
    - internal/server/resize.go — 当前 pushSessionDimsLocked（:156-168）与其文档注释（:143-155，stale 论证在 :152-155）；recalcNow（:129-141，last 变化分支唯一推送挂点）；sessionDimsLocked（:180-185）
    - internal/server/clients.go — kickOrCreditLocked（:397-416，ro 即踢 / rw 离群即踢 / 全体满则信用三分支）、kickSlowConsumerLocked（:469-491，removeLocked→close(done)→removeMember(:479)→嵌套 recalcNow(:480)→owner 递补→Broadcast→异步 Close+cancel 的完整踢出链）
    - internal/server/resize_test.go — TestSessionDimsLocked 白盒先例（&Server{} 直构造、同文件纪律）
    - internal/server/clients_test.go — 白盒夹具先例（registry 零值可用、newOutbox、mergeBatch 表测形态）
    - internal/pty/spawn.go — Session 结构（Cmd/Master 导出字段、fdMu+closed 私有字段零值可用：白盒夹具以 creack/pty.Open() 供 Master 即可，无需子进程）
  </read_first>
  <action>
    1. **修复（05-REVIEW WR-01 逐字补丁）**：internal/server/resize.go 的 pushSessionDimsLocked 循环内，`s.kickOrCreditLocked(c, frame)` 调用之后立即插入复检——`if s.arbiter.last != target { return }`，并带补丁同款两行注释（语义：踢出可能经 removeMember→嵌套 recalcNow 把 arbiter.last 推进到更新值，嵌套推送已向全部留存客户端送达新值——本循环的 target 已过时，中止防旧值反超）。插入点精确：在 `if !c.outbox.trySend(frame) { ... }` 块内、kickOrCreditLocked 调用行之后。行为逻辑（补丁注释已论证，写进代码注释）：被踢但仲裁结果不变时嵌套 recalcNow 因 target==last 提前返回、last 不变，外层循环正确继续；信用路径无成员变动同样继续。
    2. **注释改写（resize.go :143-155 文档注释区）**：把安全性段落（现 :152-155）的嵌套路径论证从 promoteNextLocked 换成真实可达的 removeMember→嵌套 recalcNow 路径。改写后段落必须覆盖四要素：(a) removeLocked 的 map delete 在 range 期间 Go spec 安全（保留既有句）；(b) 循环内踢出经 clients.go:479-480 removeMember→嵌套 recalcNow 可达（被踢者是参与集成员且其持有某轴最小值时仲裁结果改变——纯 ro 会话全部 ro 端均为成员、all 模式被踢 rw 端亦为成员），嵌套推送把 W(T2) 送达全部留存端后外层捕获的 T1 已 stale——复检中止即此防线；(c) 踢出不改仲裁（嵌套 recalcNow 提前返回）或走信用路径时 last==target，外层正确继续；(d) 每次踢出永久移除一端保证嵌套有界终止（≤ max-clients）。原段落关于 promoteNextLocked 不可达性的判断（owner 模式唯一可写端恒走信用、all 模式 owner 恒 nil）可压缩为一个从句保留或删除——主论证必须落在 removeMember 路径上。同时把 :148-150 的「触发帧不丢」句收窄为首帧暂存语义 + Task 2 补发收敛（语义：首触发帧暂存 creditPending 不丢；已 blocked 期间的后续尺寸推送帧不暂存，由 afterDrain 开门时按当前 sessionDimsLocked() 补发收敛，WR-02 见 clients.go）。注释引用 D-13 双档与 R-08 分工的既有句保持不动。
    3. **白盒回归测试（resize_test.go 新增 TestPushSessionDimsKickRecalc，package server，TestSessionDimsLocked 同文件纪律）**——白盒必要性：缺陷窗口 = map 遍历序 × 推送循环内踢出 × 仲裁改变三者交织，黑盒不可控；白盒直调 pushSessionDimsLocked 复现。夹具与场景（每轮迭代全新装配）：
       - sess：creack/pty.Open() 取 master/tty，`sess := &pty.Session{Master: master}`（recalcNow→sess.Resize 需要活 master fd 吸收 TIOCSWINSZ；无子进程即无收割负担）；cleanup = sess.Close() + tty.Close()。
       - Server：`s := &Server{}`；s.sess = sess；`s.hubCond = sync.NewCond(&s.hubMu)`（kick 链 Broadcast 需要非 nil cond）；`s.arbiter = arbiter{sizes: make(map[*client]dims)}`。
       - 客户端 conn 夹具：测试起点一个 httptest.NewServer（handler 内 `websocket.Accept(w, r, nil)` 后 `c.Read(context.Background())` 读至出错即返回——承接踢出 goroutine 的 1013 Close 握手，防 goroutine 泄漏）；每轮迭代 `websocket.Dial(ctx, "ws://"+addr, nil)` 两条新 conn 分别挂 A/B fixture，迭代末 CloseNow 双 conn（幂等，B 的 conn 已被踢出 goroutine 关过）。**conn 必须真实**——kickSlowConsumerLocked 的异步 goroutine 调 `c.conn.Close(1013, ...)`，nil conn 会 panic 炸掉整个测试进程。
       - A（留存端）：mode.Store(proto.ModeRW)、outbox = newOutbox(1<<20)（trySend 恒成）、done、cancel = func(){}、remote="A"、真实 conn；B（被踢端）：同形但 outbox = newOutbox(1)（~100B Welcome trySend 恒败）。registerLocked(A)、registerLocked(B)。
       - 仲裁态：sizes = {A: dims{60,50}, B: dims{80,24}}（B 持 rows 轴最小值）；`s.arbiter.last = dims{60,24}`（= min-rect T1，模拟外层 recalcNow 已置 last 并 Resize 完毕）。调用 `s.pushSessionDimsLocked(dims{60,24})`。
       - 交织机制：B 被访问时 trySend 败 → kickOrCreditLocked(B) → B 为 rw 且 A 为未 blocked rw → 踢出 → removeMember(B) → 嵌套 recalcNow：sizes={A:60x50} → T2=60x50 ≠ last=60x24 → last=T2 + Resize(60,50) + 嵌套推送 W(60x50) 给 A → 复检命中（修复后）→ return。
       - 断言（每轮迭代）：drain A.outbox 得全部推送帧，逐帧断言 frame[0]==proto.Welcome 并解码 cols/rows。遍历序两态判据：len==1 ⟺ B-first（危险序，修复后外层被复检中止，A 只收到嵌套推送的 W(60x50)）；len==2 ⟺ A-first（[W(60x24), W(60x50)]）。**普适不变量：末帧 == s.arbiter.last（60x50）**——修复前 B-first 态为 [W(60x50), W(60x24)]、末帧 60x24 ≠ last，断言必败（测试牙齿）；B-first 态 len==1 且唯一帧 == 60x50。
       - 循环至命中：for 至多 32 轮迭代直至观测到 len==1（B-first）；32 轮未命中则 t.Fatal（map 随机序 2^-32 概率，等同确定性——不静默 skip，防测试形同虚设）。每轮同时断言普适不变量（A-first 轮也是有意义的回归样本）。
       - import 纪律：creackpty "github.com/creack/pty"（resize_arb_test.go 同款别名先例）、net/http/httptest、coder/websocket、sync、encoding/json、context、testing、time（Dial ctx 超时）。
    4. 禁止：不得改 proto.go / WelcomeFrame 签名 / 帧类型字节；不得动 onChunk、mergeBatch、kickOrCreditLocked 分工表三分支；不得把复检移到循环外或 trySend 成功路径（复检唯一挂点 = kickOrCreditLocked 返回后——信用路径 last 不变，复检自然通过零代价）；不得引入新导出符号；不得为「确定性」给 registry.set 换有序结构（map 遍历随机性是测试覆盖交织的来源，非缺陷）。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && go test ./internal/server/ -run 'TestPushSessionDimsKickRecalc' -race -count=1 -v 2>&1 | tail -5 && go vet ./... && time go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - 源码断言：`grep -c 'if s.arbiter.last != target {' internal/server/resize.go` 输出 ≥ 1（复检落位）
    - 注释断言：改写后 pushSessionDimsLocked 文档注释区含 removeMember 字样（`awk '/pushSessionDimsLocked 向全部在线客户端推送/,/^func \(s \*Server\) pushSessionDimsLocked/' internal/server/resize.go | grep -c 'removeMember'` ≥ 1）
    - 行为断言：TestPushSessionDimsKickRecalc 在 -race -count=1 下 PASS，且测试结构保证命中 B-first 交织（32 轮未命中即 FAIL，不放行空转绿）
    - go vet ./... exit 0；go build ./... exit 0
  </acceptance_criteria>
  <done>复检与注释改写落位；TestPushSessionDimsKickRecalc 复现「推送循环内踢出改变仲裁结果」交织并锁定留存端终值 == 最新仲裁尺寸；vet/build 干净。</done>
</task>

<task type="auto">
  <name>Task 2: WR-02 — afterDrain 开门补发当前会话尺寸 Welcome（option (a)）+ TestAfterDrainResendsDims 白盒回归</name>
  <files>internal/server/clients.go, internal/server/clients_test.go</files>
  <reversibility rating="reversible">局部小补丁：afterDrain 一处补发 + 注释 + 一个测试文件新增函数，git 单提交可整体回退；补发复用既有 'W' 帧类型，前端处理重复 Welcome 幂等（05-11 既定 roNotified/refit 门闩链）——无公开契约变化</reversibility>
  <read_first>
    - .planning/phases/05-multi-client/05-REVIEW.md — WR-02 option (a) 逐字补丁块（用户 2026-08-22 裁决选定 (a)，(b) 注释收窄兜底方案作废勿混用）
    - .planning/phases/05-multi-client/05-VERIFICATION.md — gaps 块（WR-02 缺陷链：blocked 端丢推送 + afterDrain 不补发 → sessionDims 过期）
    - internal/server/clients.go — afterDrain 现状（:429-447，重投 creditPending→清位→gateTransitions++→Broadcast 的精确语句序）、kickOrCreditLocked（:397-416，`if !c.creditBlocked` 守卫 :411-415）
    - internal/server/resize.go — sessionDimsLocked（:180-185，hubMu 内调用、last 零值回落 spawn 尺寸）；pushSessionDimsLocked 的 mode/prefs 选档形态（:157-162，补发帧选档逐字同款）
    - internal/proto/proto.go — WelcomeFrame 签名与 WelcomePayload 形状（mode/cols/rows/prefs 键，补发帧解码断言依据）
    - internal/server/clients_test.go — TestClientCountInvariant/TestWriterMergeControlFramesOnly（白盒夹具与逐字节断言惯例）
  </read_first>
  <action>
    1. **修复（05-REVIEW WR-02 option (a) 逐字补丁）**：internal/server/clients.go 的 afterDrain 中，`c.creditBlocked = false` 之后、`s.hubCond.Broadcast()` 之前插入补发段——`sd := s.sessionDimsLocked()`；`mode := c.mode.Load().(string)`；prefs 按 mode 选档（mode == proto.ModeRW → s.clientPrefsRW，否则 s.clientPrefsRO——与 pushSessionDimsLocked :157-162 逐字同形态，D-13 双档纪律在补发通道不漂移）；`_ = c.outbox.trySend(proto.WelcomeFrame(mode, prefs, sd.cols, sd.rows))` 带行注释（语义：补发阻塞期错过的尺寸推送）。补丁的 `_ =` 形态逐字保持——重投后 cur < cap/2 的数学保证（既有注释 :425-428）下 ~100B Welcome 入队必成，失败分支属配置错误不在此兜底。插入后精确语句序：重投 creditPending → `c.creditBlocked = false` → `s.registry.gateTransitions++`（原位置不动）→ 补发 Welcome → `s.hubCond.Broadcast()`。有序性论证（写进 afterDrain 文档注释）：补发帧在清位后、Broadcast 前入队——门仍闭合（onChunk 在 allWritableBlockedLocked 判定下无法夹入新帧），补发帧经 outbox FIFO 排在重投的 creditPending 之后，客户端字节流严格有序保持。
    2. **注释同步**：afterDrain 文档注释补 WR-02 语义一句（恢复开门时向该端补发一帧当前 sessionDimsLocked() 的 Welcome——creditBlocked 期间被 `if !c.creditBlocked` 守卫跳过的尺寸推送帧由此收敛，该端 sessionDims 不过期至下次尺寸事件）；kickOrCreditLocked 文档注释「触发帧不丢」段（:388-390 区）补一句首帧暂存边界（幂等置位守卫下已 blocked 的后续帧不覆写暂存——防二次暂存覆写首帧的既有语义；尺寸推送类帧的收敛出口 = afterDrain 补发，两函数注释互相指路）。
    3. **白盒回归测试（clients_test.go 新增 TestAfterDrainResendsDims，package server，TestClientCountInvariant 同文件纪律）**，两子测：
       - 子测 1「blocked 期后续推送帧不覆写暂存」（守卫语义锁——WR-02 缺陷链前半的行为固定）：`s := &Server{}` + hubCond 装配；c = rw 客户端（mode.Store(proto.ModeRW)、outbox=newOutbox(4096)、creditBlocked=true、creditPending=首触发 OUTPUT 帧字节、done/cancel/remote 夹具），registerLocked(c)（注册表仅 c 一端——剔除 c 后无其他未 blocked rw，kickOrCreditLocked 必走信用分支，结构保证不触发踢出路径）；调 `s.kickOrCreditLocked(c, proto.WelcomeFrame(proto.ModeRW, nil, 100, 30))`；断言 c.creditPending 仍逐字节 == 首触发帧（未被覆写——守卫语义的机械证据）、c.creditBlocked 仍 true、registry.kicks == 0。
       - 子测 2「afterDrain 开门补发当前会话尺寸」（WR-02 收敛锁）：`s := &Server{}` + hubCond 装配；s.arbiter.last = dims{100, 30}（sessionDimsLocked 非零分支）；s.clientPrefsRW = json.RawMessage(`{"fontSize":14}`)、s.clientPrefsRO = json.RawMessage(`{"fontSize":13}`)（选档区分度夹具）。表驱动两行（rw / ro）：c 按行置 mode、creditBlocked=true、creditPending=OUTPUT 帧、outbox=newOutbox(4096)（bytes=0 < cap/2，恢复判定必过）；调 `s.afterDrain(c)`；断言：(a) c.creditBlocked == false 且 c.creditPending == nil（既有恢复语义不变）；(b) drain c.outbox 恰得 2 帧——[0] 逐字节 == 原 creditPending（重投有序性：补发帧排在暂存帧之后）、[1] frame[0]==proto.Welcome 且解码 map 后 mode==该行期望档、cols==float64(100)、rows==float64(30)（补发帧携当前会话尺寸 = arbiter.last）、prefs.fontSize==rw 行 14 / ro 行 13（按 c.mode 选档，D-13 在补发通道不漂移的机械证据）。ro 行同构验证旁观者恢复路径（ro 永不持信用——ro 行进信用集是防御性夹具，断言补发选档正确性即可，测试注释写明此行为夹具驱动非生产可达路径）。
    4. 禁止：不得改 `if !c.creditBlocked` 守卫本身（首帧暂存单槽纪律是 05-02 锁定形态，守卫注释 :409-410 语义保留）；不得把补发挪到 Broadcast 之后（开门后 onChunk 新帧可能夹入，破坏有序性论证）；不得给补发加错误处理/重试分支（`_ =` 是补丁逐字形态，数学保证见上）；不得改 proto.go / 引入新导出符号 / 新增帧类型字节；不得动 gateTransitions 两递增点（Phase 8 OPS-07 挂点）。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && go test ./internal/server/ -run 'TestAfterDrainResendsDims' -race -count=1 -v 2>&1 | tail -8 && go vet ./... && time go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - 源码断言：`grep -c 'sessionDimsLocked()' internal/server/clients.go` 输出 ≥ 1（补发落位）
    - 行为断言：TestAfterDrainResendsDims 两子测在 -race -count=1 下全 PASS；补发帧序（creditPending 重投在前、Welcome 补发在后）由测试帧序断言锁定
    - 选档断言：rw/ro 两行的 prefs 断言区分 clientPrefsRW/clientPrefsRO 夹具值（选档错误必败）
    - go vet ./... exit 0；go build ./... exit 0
  </acceptance_criteria>
  <done>afterDrain 补发落位且语句序正确；kickOrCreditLocked/afterDrain 注释与实现一致；TestAfterDrainResendsDims 锁定「blocked 丢推送 → 恢复补发收敛」全链；vet/build 干净。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client Welcome | 会话尺寸下发（05-10 既有通道——WR-01 收敛其乱序、WR-02 补发其丢失；载荷纯整数无敏感信息，通道形状不变） |
| hub/arbiter 内部交织 | 推送循环内踢出触发嵌套重算（hubMu 单锁内，调用方均受信内部路径） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05WR-01 | Denial of Service | 踢出→嵌套 recalcNow→嵌套推送 递归深度 | low | mitigate | 每次踢出永久移除一端（removeLocked 恰好一次），嵌套深度 ≤ 注册表规模（≤ --max-clients 32）；复检 return 同时削减冗余扇出帧 |
| T-05WR-02 | Tampering | 端 sessionDims 终值一致性（G-05-1 缺陷类） | low | mitigate | 本 plan 修复本体：WR-01 复检中止 stale 扇出 + WR-02 补发收敛丢失——白盒测试逐帧锁定终值 == arbiter.last |
| T-05WR-03 | Denial of Service | afterDrain 补发放大 | low | accept | 补发仅在信用恢复点（drain 至半水位后）每端每开门周期一帧 ~100B，有界且低频；数学保证入队必成（重投后余量 ≥ cap/2+1 ≫ 帧长） |
| T-05WR-SC | Tampering | 依赖供应链 | low | mitigate | 本 plan 零新依赖（仅 stdlib httptest + 既有 go.mod 依赖入测试），Package Legitimacy Gate 不触发 |

ASVS L1 评估：两修复为服务端内部可用性/一致性加固，不新增外部输入面、不改认证/凭据/token/Origin 任一安全边界；全部威胁 low < block_on=high，无阻断项。
</threat_model>

<verification>
- 新测试组：`go test ./internal/server/ -run 'TestPushSessionDimsKickRecalc|TestAfterDrainResendsDims' -race -count=1` 全过
- 全量回归：`time go test ./... -race -count=1` 5/5 包绿（零回归——两修复不改变任何已锁定行为路径的预期输出）
- 静态检查：`go vet ./...` 干净
- 三层 UAT 回归锁（`time go build -o wesh ./cmd/wesh` 后连跑，须全绿、断言计数不变）：
  - `node web/uat/phase05.mjs ./wesh` → 28/28 + 1 skipped
  - `node web/uat/phase05-dom.mjs ./wesh` → 19/19
  - `node web/uat/phase05-dims.mjs ./wesh` → 3/3
  - 无新 UAT 断言需求：WR-01 中止的是 stale 扇出（协议面只少不多）、WR-02 补发复用既有 'W' 帧（前端重复 Welcome 幂等，05-11 门闩链既定）——两修复不引入可观测协议行为变化
- 前序 UAT 回归（05-VALIDATION 全量套件惯例）：`node web/uat/phase02.mjs ./wesh && node web/uat/phase03.mjs ./wesh && node web/uat/phase04.mjs ./wesh && node web/uat/phase04-t1-width.mjs` 全绿（本修复不触及其断言面）
</verification>

<success_criteria>
- 05-VERIFICATION.md gaps 块 missing 两条全部落地：WR-01 复检 + 注释改写 + 白盒交织测试；WR-02 option (a) 补发 + 白盒收敛测试
- 05-10 truth #5「owner resize 防抖后全部在线客户端收到携新会话尺寸的推送」在已核读的两条交织下同样成立——复验可由 gaps_found 转 gaps_closed
- 全量 -race 5/5 包绿 + vet 干净 + phase05 三层 UAT（28+1skip / 19 / 3）零回归
- 范围红线：IN-01..IN-04 打磨项未混入（REVIEW 挂账不动）；proto.go/前端/UAT 脚本零改动；无新导出符号、无新帧类型
</success_criteria>

## Artifacts this phase produces

- `internal/server/resize_test.go`：**TestPushSessionDimsKickRecalc**（新测试函数，package server 白盒）
- `internal/server/clients_test.go`：**TestAfterDrainResendsDims**（新测试函数，package server 白盒，含两子测）
- 无新导出符号、无新文件、无协议/前端产物变化（两修复均为 internal/server 内部逻辑 + 注释）
- `.planning/phases/05-multi-client/05-13-SUMMARY.md`（执行后由 executor 产出）

<output>
Create `.planning/phases/05-multi-client/05-13-SUMMARY.md` when done
</output>
