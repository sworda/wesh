---
phase: 10-mode-assembly
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/wesh/main.go            # 仅补缺分支：目标态已随 189d081/0ec37cb 在树，断言失败才动
  - cmd/wesh/main_test.go       # 仅补缺分支：WR-01 三行已随 189d081 在树；append-only 纪律
autonomous: true
gap_closure: true
gap_ids: [WR-01, WR-02]
requirements: [PC-01]
tags: [startup-preflight, cwd-semantics, validate-options, gap-closure]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "per-client × --cwd × 相对路径 argv0（存在且可执行）启动被接受——进程级 listening on + session_start（VERIFICATION truth #5 由 FAILED 转 VERIFIED；WR-01 missing (a) 放行面）"
    - "per-client × --cwd × 相对路径 argv0 缺失/无执行位 → exit 2 且 stderr 文案为 not executable 语义（WR-01 missing (c)：带斜杠形态不经 PATH 解析，不再声称 PATH 解析失败）"
    - "per-client × 裸命令名缺失 → exit 2 not found in PATH（SC4 既有拒绝面零漂移）；shared × 同参数不预检、照常 spawn（零回归对照面）"
    - "TestStartupMatrix 含 --cwd × 相对路径 argv0 放行面一行 + 拒绝面两行（缺失/无执行位）并全绿（WR-01 missing (b)）"
    - "run() 中 server.ValidateOptions 在装配分岔块尾部、pty.Start 之前被恰好调用一次——守卫触发时零资源占用（无 sess/ln/socket 残留可回滚；WR-02 missing 前移形态）"
    - "零回归收口闸：-race 五包全 ok + web/uat/phase02-09.mjs 八脚本原样 exit 全 0 + 五测试文件相对 781af48 零删除行（append-only）"
  artifacts:
    - "cmd/wesh/main.go — validateStartup SC4 预检块（--cwd 感知 os.Stat 可执行探测 + 分形态文案，现行 :1036-1063）与 run() ValidateOptions 前移调用点（现行 :1321-1331）"
    - "cmd/wesh/main_test.go — cwdCmdDir fixture（:699-708）与 TestStartupMatrix 三 WR-01 行（:832-834），append-only"
    - ".planning/phases/10-mode-assembly/10-05-SUMMARY.md — 闭合证据（进程级六形态冒烟矩阵 + 收口闸数字）"
  key_links:
    - "预检相对路径解析基准 ≡ spawn 侧 child chdir(cfg.cwd) 后 execve 基准（internal/pty/spawn.go cmd.Dir = opts.Dir 语义）——filepath.Join(cfg.cwd, argv0) 单点对齐，无双写"
    - "run() 启动序：validateStartup → 10-01 分岔块（spawnFunc 确定）→ server.ValidateOptions（最小字面量 Options{SessionMode, SpawnFunc}）→ pty.Start → net.Listen/listenSocket——任一 fail-fast 拒绝路径零资源占用"
    - "TestStartupMatrix 三 WR-01 行 → cwdCmdDir fixture（run.sh 0755 / noexec.sh 0644，t.TempDir 运行时材料——cwdOK/indexOK 同形态先例）"
  prohibitions:
    - "既有测试行 append-only——禁止删除/修改/放宽任何既有断言（含把期望值放宽成「两模式都接受」的形态，SUMMARY 方法论警告）"
    - "shared 模式行为逐字节零漂移——禁止给 shared 分支加任何预检/警告/文案/行为变动"
    - "新错误文案禁止含凭据/token/文件内容值（SEC-01 启动面红线延伸到本闭合新增/改动的文案面）；argv0 命令名回显限 %q 形态（D-04 枚举/路径回显豁免先例）"
    - "禁止为同一规则新增第二条校验闸位（单一闸纪律——SC4 预检只在 validateStartup 一处，装配契约校验只在 run() 分岔块尾部一处）"
    - "禁止引入 WESH_SESSION_MODE env 键（D-03 裁决）与 phase10.mjs 等任何新协议 UAT 脚本（D-06 裁决——冒烟矩阵以临时命令形态留在 SUMMARY 证据，不入库）"
    - "禁止新增任何 per-client 运行期行为（Phase 10 全部 inert 边界——本闭合只动启动期校验语义与校验点位序）"
    - "零新增依赖——go.mod/go.sum 一行不动（v1.1 里程碑锁定）"
---

<objective>
闭合 10-VERIFICATION.md frontmatter `gaps:` 的两条缺口（WR-01 Blocker / WR-02 Warning）——
Phase 10 复验 18/19，唯二未闭合项。

Purpose: WR-01 是 Phase 10 自身交付缺陷——SC4 预检 exec.LookPath(cfg.argv0) 在服务端进程
cwd 下解析，而实际 spawn 在子进程 chdir(--cwd) 之后解析相对路径，per-client × --cwd ×
相对路径 argv0 合法配置被结构性误拒；Phase 11 spawn 移至 attach 期后该预检是唯一启动闸，
误拒将永久固化（不可 deferred）。WR-02 是 ValidateOptions 调用点在 pty.Start/net.Listen
之后、失败路径零回滚，违反其注释自引的零资源占用纪律（当前不可达，但守卫存在意义即拦截
未来漂移，触发时清理路径即错）。

**规划期实证（2026-09-03，本 plan 落笔前核实）**：两缺口的修复已随 10-REVIEW-FIX 轮提交落
main——WR-01 = 189d081（预检 --cwd 感知对齐 + 分形态文案 + TestStartupMatrix 三行），
WR-02 = 0ec37cb（ValidateOptions 前移至分岔块尾部、pty.Start 之前）。当前 HEAD（c709930）
实测：六形态进程级冒烟全中（VERIFICATION 复现场景由 exit 2 误拒转为正常 listening），
TestStartupMatrix/TestValidateOptions 全绿，-race 五包全 ok。因此本 plan 三任务形态 =
**闭合断言优先 + 补缺兜底**（断言失败即按 action 内目标态规格修复——规格原文来自
VERIFICATION missing 清单与 10-REVIEW 推荐方案）+ **零回归收口闸**（fix 提交后的全量双
证据尚未在 main HEAD 重跑，本 plan 补齐）。

Output: WR-01/WR-02 闭合的进程级与测试级证据、收口闸全绿数字、10-05-SUMMARY.md。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-mode-assembly/10-VERIFICATION.md
@.planning/phases/10-mode-assembly/10-REVIEW-FIX.md
@cmd/wesh/main.go
@cmd/wesh/main_test.go
@internal/server/server.go
@internal/server/options_test.go
</context>

<flagged_assumptions>
- **PC-01 spec-less 边界行保持 unclassified/unresolved**（EDGE_ABSENT=1，本 phase 无 SPEC）——
  两条 VERIFICATION gap 即该行的具体未闭合边界实例，本 plan 闭合之；最终分类归本 plan 执行
  后的 phase 复验所有。不自动 resolve、不静默丢弃。
- **修复先于本 plan 存在**：WR-01/WR-02 代码修复经 10-REVIEW-FIX 轮（189d081/0ec37cb）已
  在 main HEAD。本 plan 任务为断言优先；若执行时目标态缺失/回退，Task 1/2 action 内的目标
  态规格即修复契约（逐条映射 VERIFICATION missing 清单），executor 按之补齐后重跑断言。
- **PATH 含相对项（如 `.`）× 裸命令名**的残余错位面不在本批闭合范围：VERIFICATION missing
  清单（本 plan 契约）只锁定带斜杠形态；裸名形态下 Go exec.LookPath 对相对解析结果返回
  ErrDot 走拒绝通道（安全方向，无误放），Phase 11 attach 期 spawn 语义收口前复核即可——
  登记为显式假设，非静默丢项。
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: WR-01 闭合——SC4 预检 --cwd 感知对齐的端到端冒烟矩阵（tracer：build → 六形态 spawn → 断言）</name>
  <files>cmd/wesh/main.go, cmd/wesh/main_test.go（均仅补缺分支才动；目标态已在树）</files>
  <read_first>
    - cmd/wesh/main.go — validateStartup SC4 预检块（现行 :1036-1063，含 10-review WR-01 注释与分形态文案）
    - cmd/wesh/main_test.go — cwdCmdDir fixture（:699-708）与 TestStartupMatrix 三 WR-01 行（:832-834）及表结构（:709-715 wantErrSub/wantWarnSub 通道）
    - internal/pty/spawn.go — Start/StartWithSize 的 cmd.Dir = opts.Dir 语义（spawn 侧解析基准，预检对齐的对照事实源）
    - .planning/phases/10-mode-assembly/10-VERIFICATION.md — gaps #1 的 missing 三条（本任务契约原文）
    - .planning/phases/10-mode-assembly/10-REVIEW-FIX.md — WR-01 节（189d081 已提交修复形态记录）
  </read_first>
  <action>
    闭合断言（目标态全部已在树，逐项核实）：
    1. missing (a) 预检与 spawn 语义对齐——validateStartup 的 SC4 块满足：仅
       cfg.sessionMode == server.SessionModePerClient 且 cfg.argv0 非空触发（shared/零值
       模式与空 argv0 不预检，shared 零漂移）；argv0 含 '/' 且非绝对路径且 cfg.cwd 非空时
       probe 改为 filepath.Join(cfg.cwd, cfg.argv0)（与 child chdir 后 execve 解析基准对齐），
       含 '/' 形态走 os.Stat 可执行探测（不存在/是目录/无 0111 执行位同拒），不含 '/' 的
       裸名形态保持 exec.LookPath 通道不变。
    2. missing (c) 文案准确——含 '/' 形态拒绝文案为 invalid command %q: not executable
       (per-client startup preflight)（%q 回显 argv0，D-04 豁免面）；裸名形态保持
       not found in PATH (per-client startup preflight) 逐字不变（SC4 既有面零漂移）。
    3. missing (b) 测试行——TestStartupMatrix 表内含三行命名行：per-client cwd-relative
       executable allowed (WR-01)（放行面：cwdCmdDir + argv0 "./run.sh"，三 want 全空）、
       per-client cwd-relative missing refused (WR-01)（wantErrSub "not executable"）、
       per-client cwd-relative non-executable refused (WR-01)（wantErrSub "not executable"）；
       运行时材料 cwdCmdDir = t.TempDir() + run.sh 0755 + noexec.sh 0644（cwdOK/indexOK
       同形态先例）。append-only 纪律：既有行逐字不动。
    补缺分支（仅当上述任一断言失败——修复被回退/残缺时）：按 1-3 目标态补齐——预检改动
    落点 = validateStartup 的 SC4 块（--cwd 预检行同位、loopback 早退之前、只读探测纪律
    内，函数头注释纪律遵守）；补 path/filepath import（若缺）；测试行追加到 TestStartupMatrix
    表尾（既有行逐字不动），fixture 置于表构造前（cwdOK/indexOK 同位）；新增中文注释引用
    WR-01 与「与 spawn 语义对齐」理据（风格对齐文件既有注释）。修复后重跑本任务全部断言。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && D=/tmp/wesh-v10-close && rm -rf "$D" && mkdir -p "$D/appdir" "$D/home" && time go build -o "$D/wesh" ./cmd/wesh && printf '#!/bin/sh\nexec /bin/bash\n' > "$D/appdir/run.sh" && chmod 755 "$D/appdir/run.sh" && printf '#!/bin/sh\nexit 0\n' > "$D/appdir/noexec.sh" && chmod 644 "$D/appdir/noexec.sh" && cd "$D/home" && A=$(timeout 3 "$D/wesh" --session-mode=per-client --bind 127.0.0.1 --port 0 --cwd="$D/appdir" -- ./run.sh 2>&1) && echo "$A" | grep -q "listening on" && B=$("$D/wesh" --session-mode=per-client --bind 127.0.0.1 --port 0 --cwd="$D/appdir" -- ./no-such.sh 2>&1; echo "rc=$?") && echo "$B" | grep -q "rc=2" && echo "$B" | grep -q "not executable" && C=$("$D/wesh" --session-mode=per-client --bind 127.0.0.1 --port 0 --cwd="$D/appdir" -- ./noexec.sh 2>&1; echo "rc=$?") && echo "$C" | grep -q "rc=2" && echo "$C" | grep -q "not executable" && E=$(cd "$D/home" && "$D/wesh" --session-mode=per-client --bind 127.0.0.1 --port 0 -- ./run.sh 2>&1; echo "rc=$?") && echo "$E" | grep -q "rc=2" && echo "$E" | grep -q "not executable" && F=$("$D/wesh" --session-mode=per-client --bind 127.0.0.1 --port 0 -- wesh-no-such-cmd-7f3a 2>&1; echo "rc=$?") && echo "$F" | grep -q "rc=2" && echo "$F" | grep -q "not found in PATH" && D2=$(cd "$D/home" && timeout 3 "$D/wesh" --session-mode=shared --bind 127.0.0.1 --port 0 --cwd="$D/appdir" -- ./run.sh 2>&1) && echo "$D2" | grep -q "listening on" && go test ./cmd/wesh -run 'TestStartupMatrix' -count=1 && test "$(go test ./cmd/wesh -run 'TestStartupMatrix/per-client_cwd-relative' -count=1 -v 2>/dev/null | grep -c -- '--- PASS')" = "3" && echo WR01_CLOSURE_PASS</automated>
  </verify>
  <done>六形态进程级矩阵全中：A per-client×--cwd×./run.sh（可执行存在）→ listening on（VERIFICATION 复现场景由误拒转放行）；B 缺失 / C 无执行位 → exit 2 + not executable；E 无 --cwd 且服务端 cwd 无该文件 → exit 2 + not executable；F 裸名缺失 → exit 2 + not found in PATH（SC4 既有面）；D shared 对照 → listening on（零漂移）。TestStartupMatrix 全绿且三 WR-01 子测逐 PASS。</done>
  <acceptance_criteria>
    - 冒烟 A：输出含 "listening on"（放行实证——exit 码为 timeout 的 124 或正常退出，绝不为 2）
    - 冒烟 B/C/E：各自 rc=2 且 stderr 含 "not executable (per-client startup preflight)"
    - 冒烟 F：rc=2 且 stderr 含 "not found in PATH (per-client startup preflight)"
    - 冒烟 D（shared 对照）：输出含 "listening on"
    - go test ./cmd/wesh -run 'TestStartupMatrix' -count=1 退出码 0
    - go test -run 'TestStartupMatrix/per-client_cwd-relative' -v 的 "--- PASS" 计数 == 3
    - 源码断言：cmd/wesh/main.go 含 "not executable (per-client startup preflight)" 至少 1 处、含 filepath.Join(cfg.cwd, 至少 1 处（grep -c 正计数门）
  </acceptance_criteria>
  <reversibility rating="reversible">断言为只读/临时目录冒烟；补缺修复 = 重放已评审已提交的目标态（189d081），git 可回退，无一-way 面。</reversibility>
</task>

<task type="auto">
  <name>Task 2: WR-02 闭合——ValidateOptions 前移位序断言（分岔块尾部 < pty.Start < listen）+ 装配契约测试</name>
  <files>cmd/wesh/main.go（仅补缺分支才动；目标态已在树）</files>
  <read_first>
    - cmd/wesh/main.go — run() 启动序（现行 :1260-1372：validateStartup → TLS 预检 → customIndex → 10-01 分岔块 :1314-1320 → ValidateOptions :1321-1331 → pty.Start :1334 → listen 分岔 :1342-1349 → Options 字面量 :1370）
    - internal/server/server.go — ValidateOptions 本体（:324-348）与其零资源占用自引注释
    - internal/server/options_test.go — TestValidateOptions 三态契约
    - .planning/phases/10-mode-assembly/10-VERIFICATION.md — gaps #2 的 missing 一条（本任务契约原文）
    - .planning/phases/10-mode-assembly/10-REVIEW-FIX.md — WR-02 节（0ec37cb 已提交修复形态记录）
  </read_first>
  <action>
    闭合断言（目标态已在树，逐项核实）：
    1. 单一调用点——cmd/wesh/main.go 全文件恰好 1 处 server.ValidateOptions( 调用（verr :=
       形态），调用实参为最小字面量 server.Options{SessionMode: cfg.sessionMode, SpawnFunc:
       spawnFunc}（ValidateOptions 只读这两字段，最小字面量与完整 opts 语义等价——
       internal/server/server.go:336-348 核实）。
    2. 位序——调用点位于 10-01 分岔块尾部之后、pty.Start(argv, ...) 之前、listen 分岔
       （var ln net.Listener）之前；守卫触发时 spawn/listen 均未发生，无 sess/ln 可回滚，
       零资源占用纪律恢复（与 validateStartup 拒绝路径同构）；失败通道 = fmt.Fprintf(os.Stderr,
       "wesh: %v\n", verr) + return 2。
    3. 旧调用点（完整 opts 构造之后、New 之前）不存在第二处 ValidateOptions 调用；opts 注释
       同步登记前移事实（现行 :1321-1327 与 :1366-1369 注释形态）。
    补缺分支（仅当断言失败——调用点仍在资源获取之后或存在第二调用点）：将 verr 调用前移到
    分岔块尾部与 pty.Start 之间（两输入 cfg.sessionMode/spawnFunc 在该点已完全确定），移除
    旧调用点，补中文注释引用 WR-02 与「与 validateStartup 拒绝路径零资源占用同构」表述；
    失败通道保持 exit 2 逐字形态。修复后重跑本任务全部断言。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && test "$(grep -c 'verr := server.ValidateOptions(' cmd/wesh/main.go)" = "1" && V=$(grep -n 'verr := server.ValidateOptions(' cmd/wesh/main.go | cut -d: -f1) && P=$(grep -n 'sess, err := pty.Start(' cmd/wesh/main.go | cut -d: -f1) && L=$(grep -n 'var ln net.Listener' cmd/wesh/main.go | cut -d: -f1) && test "$V" -lt "$P" && test "$P" -lt "$L" && go test ./internal/server -run TestValidateOptions -count=1 && go test ./cmd/wesh -count=1 && go vet ./... && test -z "$("$(go env GOROOT)"/bin/gofmt -l cmd internal web)" && echo WR02_CLOSURE_PASS</automated>
  </verify>
  <done>单一调用点且位序 V(ValidateOptions) < P(pty.Start) < L(listen 分岔) 数值断言成立；TestValidateOptions 三态全 PASS；cmd/wesh 包全量测试绿；vet/gofmt 零输出。</done>
  <acceptance_criteria>
    - grep -c 'verr := server.ValidateOptions(' cmd/wesh/main.go == 1（正计数门，注释不含该调用形态）
    - 行号数值断言：V < P 且 P < L（V=ValidateOptions 调用行，P=pty.Start 调用行，L=var ln net.Listener 声明行）
    - go test ./internal/server -run TestValidateOptions -count=1 退出码 0
    - go test ./cmd/wesh -count=1 退出码 0（含 TestStartupMatrix 全表）
    - go vet ./... 无输出；GOROOT gofmt -l cmd internal web 零输出
  </acceptance_criteria>
  <reversibility rating="reversible">断言为只读；补缺修复 = 重放已评审已提交的目标态（0ec37cb），git 可回退，无一-way 面。</reversibility>
</task>

<task type="auto">
  <name>Task 3: 零回归收口闸 + 闭合证据归集（fix 提交后 main HEAD 的全量双证据首跑）</name>
  <files>cmd/wesh/main_test.go, cmd/wesh/config_test.go, cmd/wesh/fuzz_test.go, internal/pty/spawn_test.go, internal/server/options_test.go, web/uat/phase02.mjs..phase09.mjs（全部只读断言，零修改）</files>
  <read_first>
    - .planning/phases/10-mode-assembly/10-04-SUMMARY.md — 10-04 收口闸数字基线（UAT 八脚本 PASS 计数：12/12、18/18、10/10、28/28、23/23、34/34、21/21、18/18）
    - .planning/phases/10-mode-assembly/10-VERIFICATION.md — ## Behavioral Spot-Checks 全表（复跑口径）
    - cmd/wesh/main_test.go — captureFd 纪律（进程级冒烟与测试的 stderr/stdout 捕获先例）
  </read_first>
  <action>
    在 main HEAD（含 189d081/0ec37cb/23f2df2/c709930）上重跑 Phase 10 既定收口闸，全部数字
    落 10-05-SUMMARY.md：
    1. 静态面与全量测试：time go build ./...、go vet ./...、GOROOT gofmt -l cmd internal web
       零输出、go test -race -count=1 ./... 五包全 ok。
    2. 协议 UAT 回归：node web/uat/phase02.mjs .. phase09.mjs 八脚本逐个跑，exit 全 0 且
       PASS 计数与 10-04 基线逐脚本一致；git status --short -- web/uat/ 零输出（脚本原样
       零修改，D-06 零新 UAT 脚本纪律）。
    3. append-only 闸：git diff -U0 781af48..HEAD 对五测试文件的删除行计数 == 0
       （781af48 = Phase 10 基线提交；10-VERIFICATION truth #9 同口径）。
    4. 冒烟三命令（SC1/SC2/SC19 既有面快速复核）：CLI --session-mode=banana → exit 2 且文案
       含 invalid --session-mode "banana": must be shared or per-client；TOML 源
       session-mode = "banana"（/tmp 临时配置，printf 生成）→ exit 2 同文案；
       --session-mode=per-client -- bash → listening on。
    5. 证据归集：Task 1 六形态矩阵结果 + Task 2 位序数字 + 本任务 1-4 全部门数字写入
       10-05-SUMMARY.md（按 summary.md 模板；gaps 闭合对账表——WR-01/WR-02 各自 missing
       逐项 → 证据链映射）。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh && time go build ./... && go vet ./... && test -z "$("$(go env GOROOT)"/bin/gofmt -l cmd internal web)" && go test -race -count=1 ./... && for s in 02 03 04 05 06 07 08 09; do node web/uat/phase$s.mjs > /tmp/wesh-v10-close/uat-$s.log 2>&1 || { echo "phase$s UAT FAILED"; exit 1; }; done && test -z "$(git status --short -- web/uat/)" && test "$(git diff -U0 781af48..HEAD -- cmd/wesh/main_test.go cmd/wesh/config_test.go cmd/wesh/fuzz_test.go internal/pty/spawn_test.go internal/server/options_test.go | grep -c '^-[^-]' || true)" = "0" && B=$(timeout 3 ./wesh --session-mode=banana -- bash 2>&1; echo "rc=$?"; true) ; echo "$B" | grep -q 'invalid --session-mode "banana": must be shared or per-client' ; echo CLOSURE_GATE_CORE_PASS</automated>
  </verify>
  <done>-race 五包全 ok；八 UAT 脚本 exit 全 0 且 PASS 计数与基线一致；web/uat/ 零修改；五测试文件零删除行；CLI banana 拒绝文案逐字命中。10-05-SUMMARY.md 含 WR-01/WR-02 missing 逐项 → 证据对账表与全部收口闸数字。</done>
  <acceptance_criteria>
    - go test -race -count=1 ./... 输出五行 "ok"（cmd/wesh、internal/proto、internal/pty、internal/server、web）
    - web/uat/phase02-09.mjs 八脚本逐个 exit 0；SUMMARY 记录各脚本 PASS 计数 == 12/18/10/28/23/34/21/18 基线
    - git status --short -- web/uat/ 输出为空
    - git diff -U0 781af48..HEAD 五测试文件删除行计数 == 0（过滤后计数门，^--- 头行已排除）
    - CLI banana 冒烟：输出含 invalid --session-mode "banana": must be shared or per-client 且 rc=2；TOML banana 同文案；per-client 接受形态输出含 listening on（此三行由 executor 跑并记录于 SUMMARY，不绑死在 automated 单行链退出码上——wesh 二进制路径以 go build -o 临时产物为准）
    - .planning/phases/10-mode-assembly/10-05-SUMMARY.md 存在且含「WR-01」「WR-02」对账节
  </acceptance_criteria>
  <reversibility rating="reversible">全任务只读断言 + 证据落盘；零代码改动面，无一-way 面。</reversibility>
</task>

</tasks>

## Artifacts this phase produces

本闭合 plan 产生的符号/文件全集（plan-review-convergence source-grounding 排除清单）：

**生产代码符号：零新增导出符号。** 补缺分支只恢复既有符号的目标态（validateStartup 函数体内
SC4 块的 probe 局部变量与 filepath.Join 调用；run() 内 server.ValidateOptions 调用点位序）——
两函数签名均不变。

**测试符号（已随 189d081 在树，本 plan 断言其存在；补缺分支重建时同名）：**
- `cmd/wesh/main_test.go` 局部 fixture 变量 `cwdCmdDir`（t.TempDir + run.sh 0755 + noexec.sh 0644）
- TestStartupMatrix 表行三行（子测名）：`per-client cwd-relative executable allowed (WR-01)`、
  `per-client cwd-relative missing refused (WR-01)`、
  `per-client cwd-relative non-executable refused (WR-01)`

**规划/证据文件：**
- `.planning/phases/10-mode-assembly/10-05-SUMMARY.md`（执行输出）

**临时产物（不入库）：** `/tmp/wesh-v10-close/` 下的 wesh 二进制、appdir fixture（run.sh/
noexec.sh）、home 目录、uat-*.log、bad.toml——冒烟矩阵运行时材料，D-06 纪律不入库。

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv / TOML 配置 → 启动校验 | operator 提供的命令行/cwd 值跨入启动行为判定面（本地信任域，但决定 spawn 面） |
| 预检 os.Stat 探测 → 文件系统 | 只读探测 operator 指定路径（纯函数纪律允许面） |
| 进程 stderr → 操作员/日志收集 | 启动错误文案跨出进程边界，SEC-01 红线适用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05-01 | Tampering | validateStartup SC4 预检（解析基准发散——WR-01 本体） | high | mitigate | 预检与 spawn 同基准对齐（--cwd join + os.Stat 可执行探测）；三行 TestStartupMatrix 行为锁 + 进程级六形态冒烟双向（放行/拒绝）实证；Phase 11 唯一启动闸前固化正确语义 |
| T-10-05-02 | Information Disclosure | 预检错误文案（stderr） | low | mitigate | 文案仅含 %q argv0 + 固定类别语（D-04 枚举/路径回显豁免面）；TestStartupMatrix 红线断言纪律覆盖启动面，禁含凭据/token/文件内容值 |
| T-10-05-03 | Denial of Service | run() 启动序资源获取（守卫触发零回滚——WR-02 本体） | medium | mitigate | ValidateOptions 前移至资源获取之前恢复零资源占用语义（无 sess/ln/socket 残留）；单调用点计数门 + 位序数值断言锁定 |
| T-10-05-SC | Tampering | npm/pip/cargo 安装 | — | not applicable | 本 plan 零包安装（零新增依赖锁定；go.mod/go.sum 不动）——Package Legitimacy Gate 不触发 |
</threat_model>

<verification>
- Task 1：WR01_CLOSURE_PASS——六形态进程级矩阵 + TestStartupMatrix 三 WR-01 子测全绿
- Task 2：WR02_CLOSURE_PASS——单调用点 + V<P<L 位序 + TestValidateOptions + cmd/wesh 全量 + vet/gofmt
- Task 3：CLOSURE_GATE_CORE_PASS + SUMMARY 落盘——-race 五包 ok、UAT 八脚本 exit 全 0 且计数对齐基线、append-only 零删除行、冒烟三命令命中
- 两缺口 missing 清单逐项有证据映射（SUMMARY 对账表）：WR-01 (a)(b)(c) → Task 1；WR-02 前移 → Task 2
</verification>

<success_criteria>
10-VERIFICATION.md gaps #1（WR-01）与 #2（WR-02）的 missing 清单全部落地并有进程级/测试级
证据；truth #5（per-client × 可执行命令 → 放行）由 FAILED 转为可复验通过；fix 提交后的
main HEAD 零回归双证据（-race 全量 + 八 UAT 原样）首次重跑全绿。Phase 10 可进入复验，
WR-01/WR-02 可由验证器对账关闭。
</success_criteria>

<output>
Create `.planning/phases/10-mode-assembly/10-05-SUMMARY.md` when done
</output>
