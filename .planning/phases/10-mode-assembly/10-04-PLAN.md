---
phase: 10-mode-assembly
plan: 04
type: execute
wave: 3
depends_on:
  - "10-02"
  - "10-03"
files_modified:
  - docs/CONFIGURATION.md
  - README.md
autonomous: true
requirements:
  - PC-01

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CONFIGURATION.md 五处落地（D-05 最小明示）：TOML 键表 session-mode 行、显式位键清单追加 session-mode、校验矩阵值域/枚举行追加 --session-mode、「放行但警告」清单追加 write-policy×per-client 行、默认值表 session-mode 行；两处键计数 29→30；全部注记「per-client 行为装配中，当前版本与 shared 等价」"
    - "README.md 含一句同旨明示（默认 shared + 装配中等价注记）；`wesh --help` 输出含 session-mode 行且与文档口径一致"
    - "gofmt -l 零输出、go vet 干净、go build 成功（time 前缀项目纪律）；go test -race -count=1 ./... 全绿"
    - "既有协议 UAT 八脚本（web/uat/phase02-09.mjs）默认 shared 模式原样重跑全过——脚本零修改（D-06 零回归第二证据腿）"
    - "冒烟三命令进程级证据：CLI banana exit 2 + D-04 文案；TOML banana exit 2 同文案（一闸双覆盖）；--session-mode=per-client 启动接受打印 listening on（SC1/SC2）"
  artifacts:
    - path: "docs/CONFIGURATION.md"
      provides: "session-mode 键表行/显式位清单/校验矩阵行/warn 清单行/默认值行 + 29→30 键计数"
      contains: "session-mode"
    - path: "README.md"
      provides: "session-mode 一句最小明示（D-05）"
      contains: "session-mode"
  key_links:
    - from: "docs/CONFIGURATION.md 各表行"
      to: "cmd/wesh/main.go --help 文案与 parseArgs 行为"
      via: "文档即被测物纪律——文档口径与 --help/校验矩阵行为一致（装配中注记防误用预期）"
      pattern: "session-mode"
    - from: "docs/CONFIGURATION.md 「放行但警告」清单行"
      to: "cmd/wesh/main.go validateStartup warn 行（10-02 落地）"
      via: "warn 行为契约的文档化——D-01/D-02 处置对用户可见"
      pattern: "write-policy"
  prohibitions:
    - statement: "MUST NOT 把 per-client 未实现行为写成既成事实（文档先行于实现 = 漂移源——D-05「装配中」注记为强制口径；完整模式语义段归 Phase 15 PC-12）"
    - statement: "MUST NOT 在收口验证中放宽任何既有断言（「两模式都接受」式改写禁止——SUMMARY.md 方法论警告：最大风险 = 破坏既有不变量而不自知；既有测试与 UAT 脚本零修改重跑）"
---

<objective>
Phase 10 收口：D-05 文档最小明示（CONFIGURATION.md 五处 + README.md 一句 + --help 核对）与每阶段收口闸全量验证——全量 -race、既有协议 UAT 原样重跑、冒烟三命令，形成「Go 测试新增面全绿 + 既有套件原样全绿」的零回归双证据（D-06）。

Purpose: flag 公开即文档义务（每 phase 收口先例）；「装配中」注记防用户开了发现无新行为误以为 bug；完整模式语义段归 Phase 15（PC-12），不写超前文档（漂移源）。
Output: 文档三面落地 + 收口证据（六段式适配形态）入 SUMMARY。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-mode-assembly/10-CONTEXT.md
@.planning/phases/10-mode-assembly/10-01-SUMMARY.md
@.planning/phases/10-mode-assembly/10-02-SUMMARY.md
@.planning/phases/10-mode-assembly/10-03-SUMMARY.md
@docs/CONFIGURATION.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: D-05 文档最小明示——CONFIGURATION.md 五处 + README.md 一句 + --help 核对</name>
  <files>docs/CONFIGURATION.md, README.md</files>
  <read_first>
    - docs/CONFIGURATION.md（键计数 :33/:49；TOML 键表 write-policy 行 :56；显式位清单 :100；校验矩阵表 :108-124（值域/枚举行 :124）；「放行但警告」清单 :126-130；默认值表 :146-163）
    - README.md（使用示例区 :74-94——可写协作示例块 :90-94）
    - cmd/wesh/main.go（10-01 落地的 --session-mode flag 注册与 help 文案）
    - cmd/wesh/main.go validateStartup warn 行（10-02 落地文案——warn 清单行的口径源）
    - .planning/phases/10-mode-assembly/10-CONTEXT.md（D-05 决策全文）
    - .planning/phases/10-mode-assembly/10-PATTERNS.md（§13 三处表行先例）
  </read_first>
  <action>
    ① docs/CONFIGURATION.md：
    - :33「共 29 键：27 个长期运行 flag 同名键」→「共 30 键：28 个长期运行 flag 同名键」；:49「### 全部 29 个配置键」→「### 全部 30 个配置键」。
    - TOML 键表 write-policy 行（:56）后插入：`| session-mode | 字符串 | "shared" | shared（多客户端共享同一进程，默认）或 per-client（每 WS 客户端独立 PTY 进程——行为装配中，当前版本与 shared 等价） |`（行内键名按表内既有反引号格式书写；语义注记 D-05 强制口径）。
    - :100 显式位清单（port/bind/socket-mode/socket-owner/write-policy）追加 session-mode（D-02 机制文档化——配置中出现即视为显式设置）。
    - 校验矩阵表值域/枚举行（:124）的枚举 flag 清单追加 --session-mode（与 --write-policy/--stop-signal 同列；「值可回显，非敏感」口径不变——D-04 与 CONFIGURATION.md:124 既有口径一致）。
    - 「放行但警告」清单（:126-130）追加一行：显式 --write-policy × --session-mode=per-client——owner/all 仲裁与递补语义在 per-client 下不装配，stderr 警告放行（ro/rw 权限级别仍按 ticket 生效；D-01/D-02）。
    - 默认值表（:146-163 值行区）追加：`| session-mode | shared | per-client 行为装配中，当前与 shared 等价 |`。
    ② README.md 加一句（建议落点：使用示例区可写协作示例块 :90-94 之后——随既有 flag 叙述带；精确落点与措辞 Discretion，强制要素 = flag 名、两枚举、默认 shared、装配中等价注记）：形如「`--session-mode=shared|per-client` 选择会话模式（默认 `shared`；per-client 行为装配中，当前版本与 `shared` 等价）。」
    ③ --help 核对：`go run ./cmd/wesh --help` 输出含 session-mode 行，且枚举名单/默认值/装配中注记与 ①② 文档口径一致（不一致则回改 flag 注册文案——文档与 help 同一事实叙述）。
    提交：`docs(10-04): session-mode minimal documentation — CONFIGURATION.md rows + README note (PC-01, D-05)`
  </action>
  <verify>
    <automated>grep -c 'session-mode' docs/CONFIGURATION.md && grep -c 'session-mode' README.md && go run ./cmd/wesh --help 2>&1 | grep -c 'session-mode'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'session-mode' docs/CONFIGURATION.md` ≥ 5（键表行/显式位清单/校验矩阵行/warn 清单行/默认值行五处）
    - `grep -c '共 30 键' docs/CONFIGURATION.md` ≥ 1 且 `grep -c '全部 30 个配置键' docs/CONFIGURATION.md` == 1
    - `grep -c 'session-mode' README.md` ≥ 1
    - `go run ./cmd/wesh --help` 输出含 `session-mode` 行（grep 计数 ≥ 1）
    - 三处文档均含「装配中」等价注记语义（grep `装配中` docs/CONFIGURATION.md ≥ 1）
  </acceptance_criteria>
  <done>CONFIGURATION.md 五处 + README.md 一句落地且计数 29→30；--help 与文档口径一致；无超前于实现的行为描述。</done>
</task>

<task type="auto">
  <name>Task 2: 收口全量验证——静态面 + 全量 -race + 既有协议 UAT 原样重跑 + 冒烟三命令（D-06 零回归双证据）</name>
  <files>（验证任务——零文件改动；证据入 SUMMARY）</files>
  <read_first>
    - web/uat/phase02.mjs（脚本调用形态：node web/uat/phaseNN.mjs [二进制路径]，默认 /tmp/wesh-uat/wesh；spawn --bind 127.0.0.1 --port 0 形态）
    - .planning/phases/10-mode-assembly/10-CONTEXT.md（D-06 验证面决策——零新 UAT 脚本，既有脚本原样重跑口径）
    - CODEBUDDY.md（项目纪律：go test -race CI 强制；构建命令 time 前缀；Linux 开发机 headless 禁浏览器——本任务全部在 Linux 侧协议层）
  </read_first>
  <action>
    ① 静态面：`gofmt -l .` 零输出（GOROOT 纪律）；`go vet ./...` 干净；`time go build ./...` 成功（time 前缀项目纪律）。
    ② Go 全量：`go test -race -count=1 ./...` 全绿（v1.0 既有测试 + 本 phase 新增面）。
    ③ 既有协议 UAT 原样重跑（D-06 第二证据腿——脚本零修改、默认 shared 模式）：`time go build -o /tmp/wesh-uat/wesh ./cmd/wesh` 后依次执行 `node web/uat/phase02.mjs`、`phase03.mjs`、`phase04.mjs`、`phase05.mjs`、`phase06.mjs`、`phase07.mjs`、`phase08.mjs`、`phase09.mjs` 八脚本——全部 PASS（任一 FAIL 即停止排查，不得改脚本适配）。fuzz 种子回归已含于 ②（FuzzDecodeFileConfig 随套件运行）。
    ④ 冒烟三命令（二进制直证进程级行为）：
    - CLI 源：`/tmp/wesh-uat/wesh --session-mode=banana -- bash` → exit 2 且 stderr 含 `invalid --session-mode "banana": must be shared or per-client`。
    - TOML 源：`printf 'session-mode = "banana"\n' > /tmp/wesh-uat/bad.toml && /tmp/wesh-uat/wesh --config /tmp/wesh-uat/bad.toml -- bash` → exit 2 且同文案（一闸双覆盖进程级证据）。
    - 接受面：`timeout 3 /tmp/wesh-uat/wesh --session-mode=per-client --bind 127.0.0.1 --port 0 -- bash` 日志含 `listening on`（SC1）；同形态缺省（不带 --session-mode）冒烟对照。
    ⑤ 证据落 SUMMARY：六段结果（gofmt/vet/build、-race 全量、八 UAT 脚本、冒烟三命令）逐项记录 PASS/FAIL 与关键输出摘录；声明「既有测试与 UAT 脚本零修改」（git status 佐证——web/uat/ 与既有 *_test.go 无本 phase 改动行，10-01/10-02/10-03 均为 append-only 追加）。
    （本任务零提交——验证证据入 SUMMARY；若 ①-④ 暴露问题则修复后重跑并记录）
  </action>
  <verify>
    <automated>gofmt -l . | wc -l && go vet ./... && time go build ./... && go test -race -count=1 ./... && time go build -o /tmp/wesh-uat/wesh ./cmd/wesh && for s in phase02 phase03 phase04 phase05 phase06 phase07 phase08 phase09; node web/uat/$s.mjs > /tmp/wesh-uat/$s.log 2>&1; echo "$s=$status"; end</automated>
  </verify>
  <acceptance_criteria>
    - `gofmt -l . | wc -l` == 0；`go vet ./...` 无输出；`go build ./...` 成功
    - `go test -race -count=1 ./...` 全绿
    - phase02-09 八脚本退出码全为 0（$s=0 逐行）；各脚本日志尾行 PASS 计数与 v1.0 基线一致（无 FAIL 行）
    - 冒烟：CLI banana exit 2 + 文案全文匹配；TOML banana exit 2 + 同文案；per-client 与缺省两形态日志含 `listening on`
    - `git status --short web/uat/` 零输出（UAT 脚本零修改证据）
  </acceptance_criteria>
  <done>零回归双证据成立（Go 全量新增面绿 + 既有套件原样全绿），双源非法值拒绝与 per-client 启动接受经进程级冒烟直证，证据完整落入 SUMMARY。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档（CONFIGURATION/README）→ 用户 | 文档是用户配置行为的依据——超前描述不存在的 per-client 行为会诱导误配置与错误 bug 报告 |
| 既有测试/UAT 套件 → 零回归证据 | 收口闸的完整性依赖「原样重跑」——任何断言放宽都使证据失效且不自知 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04a | Information Disclosure | 文档把未实现行为写成事实（用户错误预期 → 误配置/误报） | low | mitigate | D-05「装配中，当前版本与 shared 等价」注记为强制口径（键表/默认值表/README 三处同文）；完整语义段归 Phase 15（PC-12） |
| T-10-04b | Tampering | 收口验证断言放宽（「两模式都接受」式改写破坏零回归证据链） | high | mitigate | 期望值逐字未动纪律：既有测试与 UAT 脚本零修改重跑（git status/diff 佐证）；任一 FAIL 停止排查不得改脚本适配；SUMMARY.md 方法论警告登记 |
| T-10-SC | Tampering | 依赖供应链 | high | accept | 零新依赖；本 plan 无包安装任务 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V5 Input Validation（冒烟双源拒绝证据） | ✓ CLI/TOML banana exit 2 进程级直证 |
| V7 Error Handling（文档口径与错误面一致） | ✓ --help 与文档同一事实叙述核对 |
</threat_model>

<verification>
- gofmt/vet/build 静态面干净；`go test -race -count=1 ./...` 全绿
- web/uat/phase02-09.mjs 八脚本默认 shared 原样重跑全过（脚本零修改）
- 冒烟三命令：CLI banana exit 2（D-04 全文）；TOML banana exit 2（同文案）；per-client/缺省均 listening on
- 文档五处 + README 一句 + --help 核对一致
</verification>

<success_criteria>
- Phase 10 成功准则 SC1-SC4 全证：SC1 双源接受 + 零回归双证据（Go 全量 + 八 UAT 脚本原样全绿）；SC2 非法值 CLI/TOML 双源 exit 2 且文案不泄露红线面；SC3 三层优先级链（10-03 子测锁定）；SC4 per-client LookPath 启动预检（10-02 矩阵行锁定）
- 全部接缝 inert 证明：本 phase 无任何 per-client 运行期行为（SpawnFunc 零调用方）；PC-01 需求文本「缺省 shared，v1.0 全部行为逐字节不变」成立
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- docs/CONFIGURATION.md：session-mode 键表行 / 显式位清单追加 / 校验矩阵枚举行追加 / 「放行但警告」清单行 / 默认值表行 + 29→30 键计数两处
- README.md：session-mode 一句最小明示
- 收口验证证据（入 10-04-SUMMARY.md——六段结果与「既有套件零修改」声明）
- （零新代码符号——文档与验证 plan）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/10-mode-assembly/10-04-SUMMARY.md` when done
</output>
