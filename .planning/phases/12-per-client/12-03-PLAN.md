---
phase: 12-per-client
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - internal/server/clients.go
  - internal/server/server.go
  - internal/server/perclient.go
  - internal/server/export_test.go
  - internal/server/perclient_test.go
autonomous: true
requirements: [PC-10, PC-11]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "per-client 慢客户端 trySend 失败后读循环闭包阻塞持帧——输出积压于内核缓冲、子进程写阻塞而非丢数据（ttyd pty_pause/resume parity，per D-01；探针行 PC-11 经 D-01/D-02 裁决落定）"
    - "outbox drain 至非满即恢复信号——持帧闭包收到信号后重试同一帧成功并自动续读（cap-1 信号量形态，clients.go:189 notEmpty 先例）"
    - "停读态连续无恢复超过 dwell 阈值（默认 10s 内部常量，Options 测试可覆写，零 CLI flag/TOML 键）→ 1013 slow_consumer 踢出（per D-02/D-03）"
    - "每次续读重置 dwell 计时——慢但在前进的客户端永不被踢（D-02 判据核心语义）"
    - "停读/续读两点各递增 registry.gateTransitions（D-05——既有 series wesh_credit_gate_transitions_total 多两个递增点，metrics.go 零改动）"
    - "阻塞持帧期间 P5-1 别名红线保持——帧在闭包栈上，每帧 make+copy 纪律逐字不动；CR-01 读循环零同步写保持（阻塞点只在输出闭包，INPUT 路径零改动）"
    - "dwell 到期踢出走 hubMu → kickSlowConsumerLocked 既有序列——1013 wire 形态/kicks 计数/detach 事件/teardown 挂点全部零改动复用"
  artifacts:
    - path: "internal/server/perclient.go"
      provides: "ReadLoop 闭包阻塞持帧 + dwell 武装/重置/到期踢出"
      contains: "dwellTimer"
    - path: "internal/server/clients.go"
      provides: "outbox.notFull 恢复信号 + drain 挂点 + defaultSlowDwell 常量"
      contains: "notFull"
    - path: "internal/server/server.go"
      provides: "Options.SlowDwell 字段 + New 零值兜底 + Server.slowDwell"
      contains: "SlowDwell"
    - path: "internal/server/perclient_test.go"
      provides: "停读续读/dwell 四新测"
      contains: "TestPerClientDwellKick"
  key_links:
    - from: "internal/server/perclient.go"
      to: "internal/server/clients.go"
      via: "闭包 select 等 outbox.notFull / cl.done 逃逸；dwell 到期 hubMu 内 kickSlowConsumerLocked"
      pattern: "notFull"
    - from: "internal/server/clients.go"
      to: "internal/server/metrics.go"
      via: "gateTransitions 停读/续读递增点 → snapshotMetrics → wesh_credit_gate_transitions_total（零改动流经）"
      pattern: "gateTransitions\\+\\+"
  prohibitions:
    - requirement_id: PC-10
      category: safety
      statement: "背压路径绝不丢帧保连接——阻塞持帧期间与 dwell 到期前，任何已读 PTY 输出不得被静默丢弃（有序字节流丢段 = 客户端画面静默损坏）"
    - requirement_id: PC-11
      category: values
      statement: "慢但在前进（持续消费）的客户端不得被 dwell 踢出——dwell 只在「连续无恢复」窗口耗尽后触发，每次续读必须重置计时"
---

<objective>
PC-10/PC-11 背压语义：per-client 输出闭包从「trySend 失败直踢 1013」改造为「阻塞持帧 + dwell 看门狗」——停读（内核缓冲积压、子进程写阻塞不丢数据）→ 恢复自动续读 → 持续过载 dwell 到期才 1013 踢出（D-01/D-02/D-03）。

**WR-01 闭合回指（D-04 硬性要求，显式登记）**：STATE.md:99「[Phase 11 REVIEW WR-01 → Phase 12] per-client 输出闭包 trySend 失败直踢 1013，丢失 05-13 attach 宽限与信用门暂存层」一项，按本 plan 以「dwell 涵盖不复刻」形态闭合——dwell 10s 从停读起点武装，结构性涵盖 500ms attach 宽限与一切瞬态满箱（×20 余量，CONTEXT specifics 量化论证）；阻塞持帧即暂存（帧在闭包栈上 ≡ shared 暂存字段语义等价，单消费者自由度下复刻即死代码面）。shared 侧 dwell 第 5 轮实证废弃记录（clients.go:473-475）为反面教材而非母本——per-client 无信用集语义冲突，注释显式回指该段。

Purpose: ttyd pty_pause/resume parity——慢链路新端瞬态满箱不再循环丢会话；持续过载仍按 PC-10 踢出，服务端与其他客户端不受影响。
Output: outbox 恢复信号 + 闭包阻塞持帧 + dwell 计时器 + gateTransitions 两递增点 + Options.SlowDwell + 四新测。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-per-client/12-CONTEXT.md
@.planning/phases/12-per-client/12-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 机制——outbox 恢复信号 + 闭包阻塞持帧 + dwell 看门狗 + gateTransitions 两递增点 + Options.SlowDwell（D-01/D-02/D-03/D-04/D-05）</name>
  <reversibility rating="costly">D-01/D-04：阻塞持帧机制被停读/续读/dwell 三层 Go 测试与 phase12.mjs 端到端锁定后，改形态需同步重写两 face 断言；WR-01 闭合登记随本机制成立——按 CONTEXT 既定评级执行，不加门。</reversibility>
  <files>internal/server/clients.go, internal/server/server.go, internal/server/perclient.go, internal/server/export_test.go</files>
  <read_first>
    - internal/server/perclient.go:243-271（startSessionGoroutines 与 ReadLoop 闭包现状逐字——trySend 失败直踢改造点）与 :62-93（pcSession 字段区）与 :332-359（teardownPCLocked——dwell 计时器随会话消亡的 Stop 挂点评估）
    - internal/server/clients.go:176-221（outbox/trySend/drain 全文——notFull 信号挂点）与 :33-68（常量区——defaultSlowDwell 落位与 defaultGateDwell 废弃记录 :59-61 反面教材）与 :311-315（gateTransitions 字段）与 :515-519/:558-559（两递增点先例）与 :592-631（kickSlowConsumerLocked 零改动复用面）与 :738-761（writer drain→afterDrain 恢复点先例）与 :934-953（AfterFunc 自引用三件套母本）
    - internal/server/server.go:249-317（Options 结构）与 :382-395（New 零值兜底先例）与 :460-475（Server 字段装配区 sessionMode: opts.SessionMode 同位）
    - internal/server/export_test.go:15-52（ForTest 四件套纪律：hubMu 内读写/调用方不得持 hubMu/仅服务测试声明）
    - internal/pty/io.go:11-14（P5-1 别名红线纪律源）
    - .planning/phases/12-per-client/12-PATTERNS.md §3/§4/Shared Patterns（组合形态与锁序约束全部锚点）
  </read_first>
  <behavior>
    - trySend 失败 → 闭包进入持帧阻塞：hubMu 内递增 gateTransitions（停读点）+ 武装 dwell 计时器（AfterFunc 三件套：var 预声明自引用/回调首句取 hubMu/身份比对+状态复查）→ select 等 outbox.notFull 或 cl.done
    - notFull 到达 → 重试同一帧：成功 → hubMu 内递增 gateTransitions（续读点）+ dwell 计时器 Stop 置 nil（续读重置=计时器只在停读窗口存活，再停重新武装完整 10s）→ 闭包返回正常读循环；失败（防御分支，单消费者下数学上不可达）→ 继续 select 等待
    - dwell 到期回调：hubMu 内身份比对（pc.dwellTimer != t 即陈旧不动作）+ select cl.done 早退 → kickSlowConsumerLocked(cl) 既有序列（1013 slow_consumer/kicks++/detach 事件/teardown 挂点全部既有）
    - cl.done 逃逸（detach/kick/teardown）→ 闭包 return 丢帧合法（客户端已消亡）；dwell 计时器随 cl.done 关闭由闭包 defer Stop 兜底 + 回调身份比对双防线
    - outbox 增 notFull chan struct{}（cap 1，newOutbox 初始化）——drain() swap 重置字节后非阻塞发送（select default 不阻塞，已有信号在飞则接收方必会被唤醒）
    - defaultSlowDwell = 10 * time.Second（clients.go 常量区，注释载 D-03 不暴露公开配置面与量级依据：attach 宽限 ×20 余量/后台标签节流风险接受段引 CONTEXT specifics）；Options.SlowDwell + New 零值兜底（OutboxBytes :382-384 三段式先例）；Server.slowDwell 字段装配
    - 观测出口：export_test.go 增 GateTransitionsForTest（hubMu 内读，ForTest 四件套逐字形态）
  </behavior>
  <action>
    1. clients.go：outbox 加 notFull 字段与 newOutbox 初始化（notEmpty :189 同形）；drain() 尾部非阻塞信号；常量区加 defaultSlowDwell（10s）——注释显式回指 :59-61 defaultGateDwell 废弃记录与 :473-475 第 5 轮实证（shared 信用集冲突在 per-client 单消费者下结构性不存在，D-02/D-04 论证摘要）；gateTransitions 字段注释补「per-client 停读/续读两递增点（12-03，D-05）」。
    2. server.go：Options 加 SlowDwell time.Duration（注释分档先例：测试可覆写通道，OutboxBytes 同档）；New 零值兜底 opts.SlowDwell <= 0 → defaultSlowDwell；Server 字段区加 slowDwell 并在装配区赋值（sessionMode :468 同位形态）。main.go 与 TOML 配置面零改动（D-03）。
    3. perclient.go：pcSession 加 dwellTimer *time.Timer 字段（注释载 hubMu 保护——运行期可写字段与 exitCode/reaped 同锁先例）；ReadLoop 闭包（:253-268）改造为 D-01 形态——detach 门提前返回与每帧 make+copy 两行逐字保持（P5-1：持帧期间帧在闭包栈上，读循环自然停摆无别名窗口）；trySend 失败分支替换为「停读点递增+武装 → select notFull/cl.done → 重试/逃逸 → 续读点递增+Stop 置 nil」；dwell 到期回调形态见 behavior；闭包 defer dwellTimer Stop 兜底。注释载：阻塞点绝不持 hubMu/任何锁（单消费者自由度——阻塞在闭包栈上，锁全放，clients.go:420-422 信用门 Wait 先例的反向约束）；shared 信用门暂存/重投两形态一律不复刻进本路径（D-04，STATE.md:99 WR-01 按 dwell 涵盖闭合）。
    4. export_test.go：GateTransitionsForTest（PCSationsLenForTest :48-52 同款）。
    5. metrics.go 零改动（D-05 红线）；go build ./... && go vet ./... && time go test -race ./internal/server/ -count=1 全绿（既有 slowclient/multi 测试组为 shared 信用门零回归证据）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && time go test -race ./internal/server/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - outbox 含 notFull cap-1 信号量且 drain() 尾部非阻塞发送；defaultSlowDwell == 10s 常量存在于 clients.go 常量区
    - Options.SlowDwell 字段 + New 零值兜底 + Server.slowDwell 装配三锚点 grep 可证；`git diff --quiet HEAD -- cmd/wesh/main.go`（公开配置面零改动，D-03）
    - `git diff --quiet HEAD -- internal/server/metrics.go`（D-05 红线）
    - perclient.go 闭包不再含「trySend 失败直踢」形态（kickSlowConsumerLocked 调用在闭包内零命中——region 限定 startSessionGoroutines 函数体）；dwell 到期路径调用 kickSlowConsumerLocked（hubMu 内）
    - `grep -c 'creditPending' internal/server/perclient.go` == 0（D-04 不复刻形态源码断言）
    - 闭包持帧期间不持锁：startSessionGoroutines 函数体内 select 等待段不出现 hubMu.Lock 嵌套（region grep 断言 + -race 全量佐证）
    - 全量 go test -race ./internal/server/ 绿（shared 信用门/backpressure 既有测试组原样绿 = 零回归）
  </acceptance_criteria>
  <done>阻塞持帧 + dwell 机制装配完成且 shared 零回归；WR-01（STATE.md:99）按 dwell 涵盖形态在代码与注释双侧闭合。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Go 断言组——停读续读不丢帧 / dwell 到期 1013 / 慢但前进不踢 / gateTransitions 计数（D-11/D-12，PC-10/PC-11 收口）</name>
  <files>internal/server/perclient_test.go</files>
  <read_first>
    - internal/server/perclient_test.go:40-135（harness + readOutputUntil）与文件头 :1-14（夹具纪律红线）
    - internal/server/slowclient_test.go:7-60（stall 夹具纪律 + seqFlood 平台分支 :32-49——同包 server_test 直接调用）与 :95-135（assertKicked1013 两合法终结形态 + readUntilError 竞速封装）
    - internal/server/perclient_test.go:1115-1153（TestPerClientStopTimeoutKillFallback 时序双断言形态——「到期前静默窗 / 到期后护栏内收码」镜像到 dwell 断言）
    - internal/server/export_test.go（Task 1 新增的 GateTransitionsForTest）
  </read_first>
  <behavior>
    - TestPerClientStallBlocksAndResumes：spawnFn 注入 seqFlood argv → dialHello 后不 Read（stall 夹具）→ 等 outbox 涨满停读（gateTransitions +1 轮询观测）→ 静默窗内连接不被踢（≥2×停读确认时长但 ≪ dwell）→ 开始读 → 续读（gateTransitions 再 +1）→ seq 字节连续性断言（解析整数严格递增无缺口 = 停读期输出零丢失）
    - TestPerClientDwellKick：mutate 覆写 Options.SlowDwell 短值（500ms 级，o.StopTimeout :1119 先例形态）→ stall 恒不读 → assertKicked1013 护栏内收 1013 slow_consumer（两合法终结形态逐字复用）
    - TestPerClientDwellNoKickWhileProgressing：短 dwell 覆写 + 滴漏读（每 ~dwell/3 读一小批持续 ≥3×dwell 时长）→ 连接存活零 1013（慢但在前进永不踢，D-02 语义）
    - TestPerClientStallGateTransitions：起止快照 GateTransitionsForTest 差值断言（停读+续读一轮 = +2；hubMu 内读纪律由出口承载）
    - 时序纪律：禁精确时点断言；静默窗一律 select + time.After 竞速；统护 ctx 只做护栏； dwell 覆写经 Options（零测试钩子进二进制，D-12）
  </behavior>
  <action>
    1. 四测落 internal/server/perclient_test.go（D-11 单一家）；seqFlood/assertKicked1013/readUntilError 同包直接复用，平台分支（darwin/Linux 阈值）逐字继承。
    2. TDD 顺序：四测先写先红（机制 Task 1 已落地，本任务断言收口——测试与机制同 phase 两提交：Task 1 feat 提交 + 本任务 test 提交，11-01/11-03 TDD 提交收口先例）。
    3. stall 停读观测：gateTransitions +1 轮询（GateTransitionsForTest，间隔 10-20ms，统护 5s）替代固定 sleep（STATE.md Blockers Phase 9 flake 教训——轮询替代固定 sleep）。
    4. 字节连续性：resume 后 readOutputUntil 累积 seq 载荷，剥提示符/回显行后解析整数序列，断言连续段单调 +1 无缺口（seqFlood 末位序号平台分支已到顶）。
    5. time go test -race -run 'TestPerClientStall|TestPerClientDwell' ./internal/server/ -count=1 -v 全绿 → 全量 time go test -race ./internal/server/ -count=1 回归。
  </action>
  <verify>
    <automated>time go test -race -run 'TestPerClientStall|TestPerClientDwell' ./internal/server/ -count=1 -v && time go test -race ./internal/server/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 四个新测试函数全部存在且 -race 全绿（含 CI ubuntu 形态——darwin 阈值分支经 seqFlood/assertKicked1013 既有平台分支继承）
    - 新测零 per-read deadline、零固定 sleep 时点断言（region grep：新增函数体无 SetReadDeadline，sleep 仅作滴漏节奏非断言依据）
    - gateTransitions 停读/续读 +2 经 GateTransitionsForTest 差值断言锁定
    - perclient_test.go 既有测试断言行零改动（git diff 白名单=仅新增函数）
    - PC-10（持续过载 1013）与 PC-11（停读续读不丢数据、慢但前进不踢）经行为断言锁定
  </acceptance_criteria>
  <done>PC-10/PC-11 服务端语义全绿锁定：停读续读 ttyd parity、dwell 到期 1013、慢但前进永不踢、观测计数两点递增。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server (消费速率) | 慢/stall 客户端是背压语义的不可信输入面——资源占用上界由 dwell + maxClients 双闸收口 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07 | DoS | 阻塞持帧（stall 驻留面） | medium | mitigate | dwell 10s 到期 1013 踢出（D-02）+ 每 stall 端资源确定（1 outbox ≤512KiB + 1 闭包）+ 并发上界 maxClients 硬顶（Phase 11 D-02/D-03 已落地）——最坏驻留 32×512KiB 有界 |
| T-12-08 | Tampering | dwell 计时器（陈旧回调误踢） | medium | mitigate | AfterFunc 三件套（var 预声明/回调首句取 hubMu/身份比对 pc.dwellTimer != t 不动作）+ cl.done 早退 + removeLocked 幂等兜底三层防线；Pitfall 4 恰好一次先例逐字 |
| T-12-09 | DoS | 恢复信号通道（信号风暴） | low | mitigate | cap-1 信号量 select default 非阻塞发送——drain 频率上界 = writer 吞吐；持帧闭包单消费者，无广播放大面 |
| T-12-10 | Information Disclosure | 1013 踢出事件 | low | accept | detach reason=kick code=1013 既有 schema 零新字段（审计窗口期 Phase 11→13 已明示接受） |
| T-12-SC | Tampering | 依赖面 | high | mitigate | 零新依赖红线（CONTEXT 已锁定）——go.mod/go.sum 零 diff；无安装任务，legitimacy 门不触发 |
</threat_model>

<verification>
- time go test -race ./internal/... -count=1 全绿（新四测 + shared 背压既有组零回归）
- 锁序纪律：-race 全量 + 闭包持帧不持锁源码断言 + dwell 回调 hubMu 三件套
- D-03/D-05 红线：cmd/wesh/main.go 与 internal/server/metrics.go 零 diff
</verification>

<success_criteria>
1. stall → 停读（gateTransitions +1）→ 恢复 → 续读（+1）→ seq 字节连续零丢失
2. 短 dwell 覆写下 stall 恒停 → 1013 slow_consumer 护栏内到达；滴漏前进 → 永不踢
3. shared 全量测试原样绿、metrics.go/main.go 零 diff、WR-01 闭合注释回指落码
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `clients.go`：`outbox.notFull` 字段 + `newOutbox` 初始化 + `drain()` 恢复信号发送点；常量 `defaultSlowDwell`（10s）
- `server.go`：`Options.SlowDwell` 字段 + New 零值兜底 + `Server.slowDwell` 字段与装配行
- `perclient.go`：`pcSession.dwellTimer` 字段；ReadLoop 闭包阻塞持帧/dwell 武装/续读重置/到期踢出改造（startSessionGoroutines 函数体重写段）
- `export_test.go`：`GateTransitionsForTest` 观测出口
- 新测试：`TestPerClientStallBlocksAndResumes` / `TestPerClientDwellKick` / `TestPerClientDwellNoKickWhileProgressing` / `TestPerClientStallGateTransitions`（均 perclient_test.go）

<output>
Create `.planning/phases/12-per-client/12-03-SUMMARY.md` when done
</output>
