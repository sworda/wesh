---
phase: 12-per-client
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - internal/server/resize.go
  - internal/server/server.go
  - internal/server/perclient.go
  - internal/server/perclient_test.go
  - web/src/main.ts
  - web/dist/index.html
  - web/uat/phase12-dom.mjs
autonomous: true
requirements: [PC-05, PC-07]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "per-client 客户端 RESIZE 直通本会话 PTY 的 TIOCSWINSZ——DecodeResize 钳制 [1,1000] 与 50ms 防抖保留（per D-06/ROADMAP「含」；探针行 PC-05 经 D-06 裁决落定）"
    - "resize 直通仅取 sess.fdMu 不持 hubMu（锁序三规则 §5）；每会话 debouncer 与 arbiter 共用同一组件与同一时长源（defaultResizeDebounce/Options.ResizeDebounce，防双写漂移，Pitfall 7）"
    - "ro 客户端 RESIZE 在 per-client 分支放行直通自己 PTY（D-06 服务端第二闸不生效），shared 分支 ro 丢弃闸逐字保留"
    - "per-client 运行期线上无 'W' 约束帧——Welcome 仅 attach 一次，resize 仲裁/owner 递补/fan-out/信用门在 per-client 分支不装配（arbiter 零值天然 no-op，Phase 11 结构性继承）"
    - "双 per-client 客户端尺寸互不影响——A resize 后 B 的 PTY winsize 不变"
    - "ro 客户端 attach 后照常获得独立进程，其 INPUT 被服务端丢弃、对自身进程同样无效；每客户端输入限速保留（per PC-07；探针行经 CONTEXT 已锁定 INPUT 零分支裁决落定）"
    - "前端 ro 第一闸按模式位放开：per-client ro 端恢复 fit 上报 RESIZE，shared ro 保持不发（D-07 与 D-06 同 plan 配对落地）"
    - "jsdom 断言：per-client ro 端 resize 事件后 sentFrames 含 RESIZE（0x31）；shared ro 对照零 RESIZE（D-13）"
  artifacts:
    - path: "internal/server/resize.go"
      provides: "debouncer 共用件（newDebouncer/Reset/Stop），arbiter 改持之（行为等价）"
      contains: "newDebouncer"
    - path: "internal/server/server.go"
      provides: "RESIZE case per-client 直通分支（cl.pc != nil 门）"
      contains: "cl.pc != nil"
    - path: "internal/server/perclient.go"
      provides: "pcSession 每会话防抖三字段（resizeMu/pendingResize/resizeDeb）+ teardown Stop 挂点"
      contains: "resizeDeb"
    - path: "internal/server/perclient_test.go"
      provides: "resize 直通/隔离/ro INPUT/限速五新测"
      contains: "TestPerClientResizePassthroughRW"
  key_links:
    - from: "internal/server/server.go"
      to: "internal/pty/io.go"
      via: "RESIZE case per-client 分支 → 每会话 debouncer 回调 → sess.Resize（仅 fdMu）"
      pattern: "cl\\.pc != nil"
    - from: "web/src/main.ts"
      to: "internal/server/server.go"
      via: "sendResize 第一闸按 sessionMode 放行（D-07）↔ 服务端第二闸 per-client 不生效（D-06）注释互指"
      pattern: "sessionMode !== 'per-client'"
  prohibitions:
    - requirement_id: PC-05
      category: values
      statement: "per-client 放行不得渗入 shared 路径——shared 的 ro RESIZE 服务端丢弃闸与前端第一闸逐字保留（v1.0 零回归承诺，收口闸双证据覆盖）"
    - requirement_id: PC-07
      category: safety
      statement: "ro 客户端 INPUT 永不到达任何 PTY 进程（含其自有进程）——per-client 独占进程不构成放开输入面的理由；权限不得由客户端请求获得（T-05-08 越权面保持）"
---

<objective>
PC-05 + PC-07 服务端与前端配对落地：per-client RESIZE 直通本会话 TIOCSWINSZ（钳制/防抖保留、无仲裁无 'W' 帧），ro 端按 D-06/D-07 配对放行（服务端第二闸 per-client 不生效 + 前端第一闸按模式位放开——两决策同 plan 落地，CONTEXT specifics 硬性配对）；ro INPUT 门控与每客户端限速以新断言锁定（PC-07 服务端语义 Phase 11 已免费成立，本 phase 断言收口）。

Purpose: herdr 场景 ro 移动端转屏/拖窗后自身 area 渲染尺寸正确（PC-13 driving scenario 前半），且任何客户端尺寸永不影响他人。
Output: debouncer 共用件 + RESIZE 直通分支 + 前端闸放开 + dist + 5 Go 新测 + jsdom D2 断言。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-per-client/12-CONTEXT.md
@.planning/phases/12-per-client/12-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 服务端直通——resize.go debouncer 共用件抽取 + server.go RESIZE per-client 分支 + pcSession 防抖字段（D-06，§3.4）</name>
  <reversibility rating="costly">D-06：per-client ro RESIZE 放行为 wire 行为变更，被 Go 测试与 phase12.mjs 锁定后回收即行为破坏——按 CONTEXT 既定评级执行，不加门。</reversibility>
  <files>internal/server/resize.go, internal/server/server.go, internal/server/perclient.go</files>
  <read_first>
    - internal/server/resize.go:52-112（arbiter 字段/timer 形态/initArbiter/reportResize 全文——抽取母本）与 :114-141（recalcNow）
    - internal/server/server.go:1181-1194（RESIZE case 现状逐字——D-09 第二闸与 rw 入 arbiter 路径；per-client 直通分支插入点）与 :1026-1031（cl.pc != nil 分岔先例）
    - internal/server/perclient.go:62-93（pcSession 字段区与写一次纪律注释）与 :332-359（teardownPCLocked 快半段——debouncer Stop 挂点）
    - internal/pty/io.go:34-41（sess.Resize fdMu 纪律）
    - internal/server/clients.go:55-58（defaultResizeDebounce 时长源——双写漂移防线，不新增第二份常量）
    - .planning/phases/12-per-client/12-PATTERNS.md §2/§5（直通形态与抽取方向逐字段锚点）
  </read_first>
  <behavior>
    - resize.go 抽取共用件：type debouncer 封装「time.AfterFunc 创建即 Stop 为 stopped 态 + 首次 Reset 才武装 + 回调入内先取锁」形态（resize.go:65-80 既有注释论证随件迁移）；arbiter.timer 字段改持 *debouncer，initArbiter/reportResize 行为等价（既有 resize_arb_test.go/resize_test.go 零改动全绿为等价证据）
    - pcSession 增三字段：resizeMu sync.Mutex（叶锁——持有期间不取任何其他锁）+ pendingResize dims + resizeDeb *debouncer；attach 升档时装配 debouncer，回调：resizeMu 内取 pendingResize → 放锁 → sess.Resize（仅 fdMu，os.ErrClosed 忽略——Attach 读循环既有纪律同款）
    - server.go RESIZE case 分支形态：case 入口判 cl != nil && cl.pc != nil → per-client 直通（DecodeResize 成功 → resizeMu 内写 pendingResize + resizeDeb.Reset(s.resizeDebounce)）；ro/rw 同形直通（D-06——mode 闸只在 shared 半侧）；else 分支 = shared 现状逐字保留（ro 丢弃 continue + reportResize 入 arbiter）
    - teardownPCLocked 快半段加 resizeDeb.Stop()（计时器随会话消亡，AfterFunc 回调对 closed 会话 Resize 返回 os.ErrClosed 静默——双防线）
  </behavior>
  <action>
    1. resize.go：抽 debouncer 共用件（newDebouncer(d time.Duration, fn func()) 构造即 Stop / (*debouncer).Reset(d) / (*debouncer).Stop()——Reset 并发安全语义沿用 Go 1.23+ timer 注释论证）；arbiter 改持共用件，initArbiter/reportResize 调用形态同步（时长源仍 s.resizeDebounce，唯一消费点纪律保持）。
    2. perclient.go：pcSession 加三字段（注释载锁序：resizeMu 叶锁不嵌套；sess.Resize 仅 fdMu，hubMu > sess.fdMu 全序不受影响）；upgradePerClient 装配 resizeDeb（回调取 pendingResize 后 Resize）；teardownPCLocked 快半段 SignalGroup 之前加 resizeDeb.Stop()。
    3. server.go RESIZE case：per-client 直通分支插在 D-09 第二闸之前（ro 放行结构性生效——分支先判 cl.pc != nil 直通，shared 才走 ro 丢弃闸）；分支头注释载 D-06 裁决（ttyd parity：protocol.c 只门 INPUT 不门 RESIZE；ro 独占进程无旁观对象）与前端第一闸（main.ts sendResize）互指。shared 分支一行不动。
    4. 直通路径零 Welcome 再推送——per-client 分支不调 recalcNow/pushSessionDimsLocked（arbiter 零值本就 no-op，注释明示「Welcome cols/rows 即自有尺寸，G-05-1 契约 per-client 下退化为恒等式」perclient.go:211-212 既有注释互指）。
    5. go build ./... && go vet ./... && time go test -race ./internal/server/ -run 'Resize|Arbit' -count=1（既有仲裁/resize 测试组等价证据）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && time go test -race ./internal/server/ -run 'Resize|Arbit' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - resize.go 含 debouncer 共用件且 arbiter 经它驱动；既有 resize_arb_test.go/resize_test.go 零改动全绿
    - server.go RESIZE case 含 cl.pc != nil 直通分支；shared 半侧（ro 丢弃 continue + s.reportResize 调用）源码逐字保持——`git diff` 该区段只有新增行无删除/改写行
    - per-client 直通回调锁序可证：回调内不出现 hubMu（源码断言：perclient.go debouncer 回调函数体 grep hubMu 零命中）
    - pcSession 三字段与 teardown Stop 挂点存在（grep 可证）
    - 全量 go test -race ./internal/server/ 绿（等价性 + 零回归一并覆盖）
  </acceptance_criteria>
  <done>per-client RESIZE 直通机制装配完成，shared 仲裁路径逐字保留；debouncer 单组件双消费（arbiter + 每会话）无双写漂移面。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 前端第一闸放开（D-07）+ dist 重建 + phase12-dom.mjs D2 断言扩展</name>
  <files>web/src/main.ts, web/dist/index.html, web/uat/phase12-dom.mjs</files>
  <read_first>
    - web/src/main.ts:325-341（sendResize 现状——:332 isRO 闸与注释）与 :311-321（四状态注释区）
    - web/uat/phase12-dom.mjs（12-01 落地的夹具与 D1/D3 形态——D2 扩展点）
    - web/uat/phase06-dom.mjs:160-175（SpyWebSocket sentFrames 首字节记录通道——RESIZE 0x31 断言材料）
    - internal/server/server.go RESIZE case（Task 1 落地后的 D-06 分支——注释互指对象）
  </read_first>
  <behavior>
    - sendResize 闸改为按模式位：isRO && sessionMode !== 'per-client' → return（shared ro 不发逐字保留）；per-client ro → 照常发送（去重/正整数/helloSent 各门不变）
    - jsdom D2：per-client 实例（--writable 关闭 → ro）→ window resize 事件（jsdom dispatchEvent + 100ms debounce 真实等待）→ sentFrames 首字节含 0x31；shared 无 --writable 实例对照 → 同操作 sentFrames 零 0x31
  </behavior>
  <action>
    1. main.ts sendResize（:325-341）：isRO 闸改写为模式位条件（per-client ro 放行）；闸注释重写——载 D-07 裁决、与服务端 D-06 分支互指（两侧闸注释互指纪律与 proto.go:6/main.ts:12 同款）、05-08 shared 语义逐字保留声明。
    2. cd web && time pnpm build → dist/index.html 重建纳管。
    3. phase12-dom.mjs 增 D2 场景：per-client ro 实例（无 --writable 启动）jsdom 加载 → WELCOME 后 dispatchEvent(new Event('resize')) → 等待 100ms debounce + 余量 → SpyWebSocket sentFrames 断言含 RESIZE；shared ro 实例对照同操作断言不含。隔离纪律同 D1/D3（独立 spawn + 独立 jsdom）。
    4. 红线保持：detail 零 token/凭据/pid 数值（phase06-dom.mjs:38-41 逐字沿用）；assertOutputClean() 兜底。
  </action>
  <verify>
    <automated>cd web && time pnpm build && node uat/phase12-dom.mjs</automated>
  </verify>
  <acceptance_criteria>
    - main.ts sendResize 内 isRO 闸为模式位条件形态（grep 'sessionMode' web/src/main.ts 命中 ≥3：声明/解析/闸三处）
    - phase12-dom.mjs 退出码 0，D1/D2/D3 全过
    - shared 回归：phase06-dom.mjs 与 phase05-dom.mjs 零修改重跑全绿
    - dist/index.html 为本次构建产物
  </acceptance_criteria>
  <done>D-06/D-07 配对生效——per-client ro 端拖窗/转屏真实上行 RESIZE 且服务端直通（herdr ro 移动端场景前半打通）；shared ro 行为逐字节不变。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Go 断言组——resize 直通两 case + 尺寸隔离 + ro INPUT 门控 + 限速保留（D-11，PC-05/PC-07 服务端收口）</name>
  <files>internal/server/perclient_test.go</files>
  <read_first>
    - internal/server/perclient_test.go:40-135（harness startPerClientServerWithSpawn 四返回值形态 + readOutputUntil）
    - internal/server/slowclient_test.go:7-13（stall 夹具纪律——本任务只用其纪律不 stall）
    - internal/server/resize_arb_test.go 与 resize_test.go（表测/行为测形态先例）
    - internal/pty/io.go:34-41（Resize fdMu/closed 语义——观测面用 TIOCGWINSZ 直读 winsize 的合法性依据）
    - golang.org/x/sys/unix 既有使用（pty/io.go SignalForegroundGroup TIOCGPGRP 先例——IoctlGetWinsize 同族 API）
  </read_first>
  <behavior>
    - TestPerClientResizePassthroughRW：rw 端 dialHello(111x44) → spawnedSessions[0] TIOCGWINSZ == 44x111；RESIZE 120x50 → 防抖窗后（轮询非固定 sleep，STATE.md Blockers Phase 9 flake 教训）TIOCGWINSZ == 50x120
    - TestPerClientResizePassthroughRO：无 --writable harness（mutate 覆写 Writable:false）ro 端 RESIZE 133x55 → 同一观测面 == 55x133（D-06 放行直通证据）
    - TestPerClientResizeIsolation：双端 attach，A RESIZE → A winsize 变、B winsize 恒为各自 Hello 值；且 attach Welcome 之后双端零 'W' 帧（静默窗断言，select + time.After 竞速形态）
    - TestPerClientROInputDropped：ro 端发 INPUT（"echo SHOULD_NOT_ECHO\n"）→ 静默窗内 OUTPUT 零 MARKER 命中；对端 rw（另实例或 writable 实例 rw 端）echo 对照正常
    - TestPerClientInputRateLimitKept：rw 端超速率 flood INPUT（>32KiB/s 持续）→ 连接不被踢不关闭、后续正常输入回显（RES-02 丢弃语义 per-client 保留证据）
    - 夹具红线逐字保持：客户端 Read 永不带 per-read deadline；静默窗一律 select + time.After 竞速；统护 ctx 只做护栏（perclient_test.go:13-14 文件头）
  </behavior>
  <action>
    1. 五测全部落 internal/server/perclient_test.go（D-11 单一家纪律），harness 复用 startPerClientServerWithSpawn/startPerClientServer（:56-114）——ro 形态经 mutate 覆写 Writable:false（Options 既有字段，零新装配面）。
    2. winsize 观测面：spawnedSessions() 访问器取 *pty.Session → unix.IoctlGetWinsize(int(sess.Master.Fd()), unix.TIOCGWINSZ) 直读（x/sys/unix 既有依赖，零新增）；防抖等待一律轮询直至匹配或统护到期，禁固定 sleep 时点断言。
    3. TDD 顺序：先写五测（此时直通分支已落地——本任务为断言收口，测试先行编写并与实现同 PR 提交）。
    4. time go test -race -run 'TestPerClientResize|TestPerClientROInput|TestPerClientInputRate' ./internal/server/ -count=1 -v 全绿后，全量 time go test -race ./internal/server/ -count=1 回归。
  </action>
  <verify>
    <automated>time go test -race -run 'TestPerClientResize|TestPerClientROInput|TestPerClientInputRate' ./internal/server/ -count=1 -v && time go test -race ./internal/server/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 五个新测试函数全部存在且全绿（-race）
    - 测试内零 per-read deadline（grep 断言：新增区段无 SetReadDeadline/ReadDeadline——region 限定 perclient_test.go 新增函数体）
    - 全量 ./internal/server -race 绿；perclient_test.go 既有 Phase 11 测试断言行零改动（git diff 白名单=仅新增函数）
    - PC-05 成功准则 1 与 PC-07 全条经 Go 行为断言锁定（直通/钳制/防抖/隔离/无 'W'/ro INPUT 丢/限速保留）
  </acceptance_criteria>
  <done>PC-05/PC-07 服务端语义全绿锁定：resize 各归各会话无仲裁无串扰，ro 独立进程输入门控与限速保留。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server (RESIZE 帧) | ro 客户端在 per-client 新获 RESIZE 生效面——输入经 DecodeResize 钳制 [1,1000] + 每会话 50ms 防抖两防线 |
| server→client ('W' 帧) | per-client 运行期零推送——无新 S→C 面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04 | DoS | RESIZE 直通路径（ro 放行面） | low | mitigate | 钳制 [1,1000] 在 Decode 层既有（proto.go:203-209）；50ms 每会话防抖合并风暴（Pitfall 7 SIGWINCH 防线）；资源上界 = maxClients 硬顶（Phase 11 已落地） |
| T-12-05 | Tampering | 锁序（resizeMu/fdMu/hubMu） | medium | mitigate | resizeMu 叶锁不嵌套 + 直通仅 fdMu 不持 hubMu（§5 三规则）；acceptance 含回调函数体 hubMu 零命中源码断言 + 全量 -race |
| T-12-06 | Elevation of Privilege | ro INPUT 门 | high | mitigate | ro INPUT 丢弃闸零改动（server.go:1157-1159 现状逐字保持）；TestPerClientROInputDropped 行为断言锁定；权限不得由客户端请求获得（decideModeLocked 既有红线，per-client 不引入申请通道） |
| T-12-SC | Tampering | pnpm build（dist 重建） | high | mitigate | 零新依赖红线——pnpm-lock.yaml 零 diff；无安装任务，legitimacy 门不触发 |
</threat_model>

<verification>
- time go test -race ./internal/... -count=1 全绿
- cd web && time pnpm build && node uat/phase12-dom.mjs 退出码 0（D1/D2/D3）
- 零回归：phase05-dom/phase06-dom 零修改重跑；shared RESIZE 语义逐字保持（git diff 区段审查）
</verification>

<success_criteria>
1. per-client 双端 resize 互不影响、ro 直通生效、运行期零 'W' 帧（Go 断言组绿）
2. ro INPUT 丢弃 + rw 限速保留（Go 断言组绿）
3. 前端 per-client ro 第一闸放开、shared 保持不发（jsdom D2 双向断言绿）
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `resize.go`：`debouncer` 类型 + `newDebouncer(d, fn)` + `(*debouncer).Reset` + `(*debouncer).Stop`（arbiter 改持之）
- `perclient.go`：`pcSession.resizeMu` / `pcSession.pendingResize` / `pcSession.resizeDeb` 三字段；teardownPCLocked 快半段 `resizeDeb.Stop()` 挂点
- `server.go`：RESIZE case per-client 直通分支（`cl.pc != nil` 门）
- `main.ts`：sendResize isRO 闸模式位条件化（无新符号，既有函数改写）
- `web/uat/phase12-dom.mjs`：场景 D2（ro RESIZE 发送恢复 + shared 对照）
- 新测试：`TestPerClientResizePassthroughRW` / `TestPerClientResizePassthroughRO` / `TestPerClientResizeIsolation` / `TestPerClientROInputDropped` / `TestPerClientInputRateLimitKept`（均 perclient_test.go）

<output>
Create `.planning/phases/12-per-client/12-02-SUMMARY.md` when done
</output>
