---
phase: 06-session-lifecycle
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - internal/pty/signal_linux.go
  - internal/pty/signal_darwin.go
  - internal/pty/io_test.go
  - internal/server/server.go
  - internal/server/clients.go
  - internal/server/emptyexit_test.go
autonomous: false
requirements:
  - SESS-01
  - SESS-02

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "断开退出统一收口（D-13）：注册表非空→空迁移（detach 与 kickSlowConsumerLocked 两移除点 removeLocked 返回 true 后 len(set)==0 判定——事件=迁移而非状态，启动期恒空天然免疫，RESEARCH Pitfall 2）且 ExitWhenEmpty 开启 → SIGHUP 子进程进程组（syscall.Kill(-sess.Cmd.Process.Pid, syscall.SIGHUP)——setsid 使子进程为组长、pgid==pid，P1 git 历史 cc03c79~1 逐字形态复活）→ 子进程死亡 → 既有 lifecycle 单一路径收口（exitf+sync.Once 零新分支，两模式零分支差异；Phase 7 OPS-04 信号可配化不提前设计）"
    - "--exit-when-empty 宽限（D-14 服务端半侧）：grace>0 时迁移点启动 time.AfterFunc 计时器（回调捕获最后离开者 remote）；attach（registerLocked 成功后）恰好一次取消（Stop + 置 nil）；计时内任一端 attach 成功则退出取消、会话继续；回调到期取 hubMu 复查『仍空且未 exiting』才发 SIGHUP（Pitfall 4：复查是恰好一次的兜底；SIGHUP 幂等——kill 已死 pgid 收 ESRCH 静默忽略；timer 随会话消亡）"
    - "lifecycle 终结广播不触发断开退出（planner 推导不变量）：lifecycle 在注册表快照前（hubMu 内）置 s.exiting=true，空触发判定检查该位——广播 Close(1000) 引发的 detach 致空属正常终结序列，不得再生 SIGHUP/计时器（事件流可信度与信号竞态防线；无此门则自然退出 exit 42 路径有 SIGHUP 竞态翻码风险）"
    - "pty.Session.SignalHangup() 平台方法（signal_linux.go/signal_darwin.go 构建标签纪律，reap_linux.go/reap_darwin.go 同签名调用点零平台分支先例）：不触 Master fd 故不取 fdMu（与 SignalForegroundGroup 的 TIOCGPGRP 路径不同——调用方在 hubMu 内调用安全，锁序 hubMu > sess.fdMu 不受影响）；全部错误静默（ESRCH 幂等，Pitfall 4）"
    - "宽限取消后绝不退出：计时内 attach → 会话继续（echo 可验）→ 再次断开后重新计时——TestExitWhenEmptyGraceCancel 锁定；到期仍空 → exitf 恰好一次——TestExitWhenEmptyGraceExpire；立即形态（grace=0）迁移点直接发 SIGHUP 无计时器——TestExitWhenEmptyImmediate；kick 移除路径同挂点——TestExitWhenEmptyKickTrigger；自然退出（客户端在位）exitf 以子进程原码恰好一次收口——TestExitWhenEmptyLifecycleGate；宽限计时器在 lifecycle 启动后到期不得再触发（回调 exiting 复查守卫 + 无误导触发日志）——TestExitWhenEmptyTimerAfterLifecycle（review #5 吸收）"
    - "Options 新增 ExitWhenEmpty bool + ExitWhenEmptyGrace time.Duration 两测试可覆写字段（D-14）：grace=0 是合法显式值（立即退出）——set 位由 bool 承载与 grace 分离，禁止 <=0 零值兜底吞掉显式 0（PATTERNS §3 注意项；main.go writePolicySet 显式设置判定先例）；默认不开启（零值 = 现状保持：无客户端时子进程继续运行，P5 推论）；New 装配直传，grace<0 防御性钳 0"
    - "新事件落 logEvent 三要素单行（D-12② 延伸，CONTEXT Established Patterns）：立即/到期触发 reason=exit_when_empty、宽限启动 exit_when_empty_wait、宽限取消 exit_when_empty_cancel（code 统一 1000 收口桶，reason 区分语义）；token/ticket/凭据值永不入参（SEC-01 红线）；退出码/信号名非敏感"
  prohibitions:
    - statement: "默认行为不得漂移——不给 --exit-when-empty/--once 时，客户端断开（含全部断开）不得触发任何终结动作（无 SIGHUP、无 exitf、无计时器）；P5 确立的『断开不退出』默认是产品承诺（D-14 默认不开启）"
      status: resolved
      verification: judgment
    - statement: "SIGHUP 目标恒为本会话子进程进程组（-sess.Cmd.Process.Pid）——绝不读取外部 pid、绝不对其他进程组发信号（误发 = 杀无关进程）"
      status: resolved
      verification: judgment
    - statement: "断开退出触发绝不经由新 exitf 分支或旁路 terminate 收口——只发 SIGHUP，终结由既有 lifecycle 单一路径到达（D-13 硬约束）"
      status: resolved
      verification: judgment
  artifacts:
    - path: "internal/pty/signal_linux.go"
      provides: "Session.SignalHangup()（linux 构建标签）"
      contains: "SignalHangup"
    - path: "internal/pty/signal_darwin.go"
      provides: "Session.SignalHangup()（darwin 构建标签，同签名）"
      contains: "SignalHangup"
    - path: "internal/pty/io_test.go"
      provides: "TestSignalHangup（信号送达与 WaitStatus 断言）"
      contains: "TestSignalHangup"
    - path: "internal/server/server.go"
      provides: "Options.ExitWhenEmpty/ExitWhenEmptyGrace + Server 字段装配 + exiting 门（lifecycle 快照前置位）"
      contains: "ExitWhenEmpty"
    - path: "internal/server/clients.go"
      provides: "maybeExitWhenEmptyLocked（detach/kick 两调用点）+ registerLocked 宽限取消点 + exitEmptyTimer 字段"
      contains: "exit_when_empty"
    - path: "internal/server/emptyexit_test.go"
      provides: "TestExitWhenEmptyImmediate/GraceCancel/GraceExpire/KickTrigger/LifecycleGate/TimerAfterLifecycle 六测"
      contains: "TestExitWhenEmptyImmediate"
  key_links:
    - from: "internal/server/clients.go (detach / kickSlowConsumerLocked)"
      to: "internal/pty (Session.SignalHangup)"
      via: "removeLocked 成功后 len(set)==0 且 !exiting 且 ExitWhenEmpty → grace==0 直接 SIGHUP / grace>0 启动 exitEmptyTimer（AfterFunc 回调捕获 remote）"
      pattern: "maybeExitWhenEmptyLocked|SignalHangup"
    - from: "internal/server/clients.go (registerLocked)"
      to: "internal/server/server.go (exitEmptyTimer)"
      via: "attach 登记成功后 Stop + 置 nil + logEvent exit_when_empty_cancel——宽限恰好一次取消"
      pattern: "exitEmptyTimer"
    - from: "internal/server/server.go (lifecycle)"
      to: "internal/server/clients.go (空触发)"
      via: "快照前 hubMu 内置 exiting=true——广播期 detach 致空被门控抑制"
      pattern: "exiting"
    - from: "internal/server/clients.go (timer 回调)"
      to: "internal/server/server.go (lifecycle)"
      via: "到期复查仍空 → SIGHUP → 子进程死亡 → sess.Wait 返回 → 既有 lifecycle 收口（exitf 唯一触发源不变）"
      pattern: "AfterFunc"
---

<objective>
SESS-01/02 服务端半侧：注册表空触发断开退出——pty.Session.SignalHangup（SIGHUP 进程组，P1 D-11 语义复活）+ Options.ExitWhenEmpty（set/grace 分离）+ detach/kick 两移除点空迁移检测 + 宽限计时器（hubMu 纪律）+ lifecycle exiting 门 + 六行为测试。

Purpose: --once 与 --exit-when-empty 的服务端语义核心（06-04 的 CLI 只是本 plan 能力的 flag 投影）；空迁移检测/计时器启停恰好一次/exiting 门三个时序不变量是本 plan 的全部难点——全部挂点均有逐字先例（PATTERNS §4/§5）。exitf+sync.Once 单一收口纪律是硬约束：新路径只加触发源（SIGHUP），不加终结分支。
Output: pty 平台信号方法 + server 空触发与宽限机制 + emptyexit_test.go 六测 + io_test.go 一测，-race 全绿。含 OQ1 退出状态确认门（门裁决值驱动测试断言常量与 06-06/06-07 的断言与文档）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-session-lifecycle/06-CONTEXT.md
@.planning/phases/06-session-lifecycle/06-RESEARCH.md
@.planning/phases/06-session-lifecycle/06-PATTERNS.md
@.planning/phases/06-session-lifecycle/06-VALIDATION.md
@.planning/phases/06-session-lifecycle/06-01-SUMMARY.md
@internal/server/server.go
@internal/server/clients.go
@internal/pty/io.go
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 确认门——断开退出收口路径的 wesh 进程退出状态（RESEARCH OQ1，未裁决项）</name>
  <read_first>
    - .planning/phases/06-session-lifecycle/06-RESEARCH.md（Open Questions OQ1 逐字 + Pitfall 8 机制论证 + Assumptions A4）
    - .planning/phases/06-session-lifecycle/06-CONTEXT.md（D-13 逐字——SIGHUP → Drain → exitf 以子进程退出码收口）
  </read_first>
  <decision>--once/--exit-when-empty 收口路径：SIGHUP 杀子进程 → ExitCode()=-1（GOROOT 语义）→ exitf(-1) → os.Exit(-1) 被 Unix 截断为退出状态 255——与 Phase 1 旧单次语义的 exit 0（git 历史 terminate(true, 0)）不一致。wesh 自身退出状态是部署脚本可见的公开行为，需一行裁决</decision>
  <context>
    机制已 GOROOT 级确定（exec_posix.go:155-157：信号死亡 ExitCode=-1）；D-09 已确立「信号死亡 exit_code=-1」先例（EXIT 帧载荷）。待定的是 wesh 进程退出状态的产品语义：0 = 正常收口 vs 255 = 信号收口（照单收）vs 129 = 128+SIGHUP shell 惯例。本门裁决值驱动：本 plan 测试断言常量、06-06 phase06.mjs S3/S4/S5 的进程退出状态断言、06-07 README 明示文案。06-REVIEWS（OpenCode）将本阻塞链确认为全 phase 最高优先项（Agreed Concern #1）——门的关键性获独立评审佐证；review 同时关切『门未指明不同选择的下游影响』——三处下游消费点已在各自 plan 参数化（Task 3 三形态断言值随门裁决、06-06 S3-S5 同、06-07 README 同），无遗漏点。
  </context>
  <options>
    <option id="accept-255">
      <name>照单收 255（推荐）</name>
      <pros>lifecycle 零分支改动（D-13 字面形态）；与 D-09「信号死亡=-1」语义同源；README 明示「--once/--exit-when-empty 收口 = 子进程被 SIGHUP 终结，wesh 退出状态 255」</pros>
      <cons>与 Phase 1 旧单次语义 exit 0 不一致（P5 已终结该语义，无兼容承诺）</cons>
    </option>
    <option id="map-to-0">
      <name>映射 -1→0（保 P1 语义）</name>
      <pros>断开退出路径 wesh 以 0 干净退出，部署脚本语义直白</pros>
      <cons>需触发源置标记位 + lifecycle 映射分支（空触发致死专属）——D-13『零分支差异』字面被打破，EXIT 帧 -1 与进程状态 0 不同源</cons>
    </option>
    <option id="map-to-129">
      <name>映射 128+SIGHUP=129（shell 惯例）</name>
      <pros>与 shell 报告信号死亡的惯例一致</pros>
      <cons>同 map-to-0 的分支成本，且 wesh 不是 shell——惯例移植收益存疑</cons>
    </option>
  </options>
  <resume-signal>选择 accept-255 / map-to-0 / map-to-129</resume-signal>
  <acceptance_criteria>
    - 本门必须停止等待用户：用户明确选择后才进入 Task 2；测试断言常量与后续 plan 的断言/文档按裁决值落地（先例不得作为自动通过依据）
    - 门裁决值的三处下游消费点（Task 3 TestExitWhenEmptyImmediate 断言常量 / 06-06 phase06.mjs S3-S5 进程退出状态断言 / 06-07 README 明示文案）已在各自 plan 参数化——按裁决值单点落地，无需二次裁决（review OQ1 阻塞链关切闭合）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: pty.SignalHangup + Options.ExitWhenEmpty + 注册表空触发与宽限计时器 + exiting 门</name>
  <files>internal/pty/signal_linux.go, internal/pty/signal_darwin.go, internal/server/server.go, internal/server/clients.go</files>
  <read_first>
    - internal/server/clients.go（detach 677-696 与 kickSlowConsumerLocked 491-513 两移除点逐字、registerLocked 264-273 取消挂点、registry 结构 232-260）
    - internal/server/server.go（Options 151-173、New 零值兜底 199-226 先例、lifecycle 955-1004——06-01 改造后形态：EXIT 广播段已落位、terminate 997-1004 不可动硬约束）
    - internal/pty/io.go（SignalForegroundGroup 50-61 幂等/降级纪律先例；fdMu 注释 22-28 锁范围纪律）
    - internal/pty/spawn.go（StartWithSize 调用链——setsid 内建论证，pgid==子进程 pid）
    - internal/pty/reap_linux.go / reap_darwin.go（构建标签与同签名纪律逐字先例）
    - internal/server/resize.go（initArbiter 72-80——AfterFunc + Stop 初始态 + 回调取 hubMu 计时器先例）
    - .planning/phases/06-session-lifecycle/06-RESEARCH.md（Pattern 4 注册表空触发逐字形态 + git 历史 SIGHUP 恢复件 / Pitfall 2 状态当事件 / Pitfall 4 计时器泄漏）
    - .planning/phases/06-session-lifecycle/06-PATTERNS.md（§4/§5 逐字先例拼装）
  </read_first>
  <action>
    1. 新建 `internal/pty/signal_linux.go` 与 `internal/pty/signal_darwin.go`（reap_* 同款构建标签 //go:build linux / darwin，两平台同签名——调用点零平台分支）：
       - `func (s *Session) SignalHangup()`：`_ = syscall.Kill(-s.Cmd.Process.Pid, syscall.SIGHUP)`——注释逐字登记论证：负 pid = 进程组；setsid（creack/pty StartWithSize 内建，spawn.go 调用链）使子进程为组长，pgid == 子进程 pid（P1 git 历史 cc03c79~1 逐字形态复活，D-13）；Start 成功后 Cmd.Process 必非 nil；错误全部静默（ESRCH 幂等——已死进程组重复发送无害，RESEARCH Pitfall 4）；不触 Master fd 故不取 fdMu（与 SignalForegroundGroup 的 TIOCGPGRP 路径不同——io.go:50-61 对照注释），调用方可在 hubMu 内调用（锁序 hubMu > sess.fdMu 不受影响）。
    2. `internal/server/server.go`：
       - Options 加两字段（注释按 Phase 分组成段既有形态）：ExitWhenEmpty bool（D-14：所有客户端断开后退出——set 位；默认 false = 现状保持，无客户端时子进程继续运行）+ ExitWhenEmptyGrace time.Duration（断开退出宽限——0 是合法显式值 = 最后一个客户端断开立即退出；仅在 ExitWhenEmpty 为 true 时有意义；set/grace 分离禁止 <=0 兜底，PATTERNS §3 注意项）。
       - Server 加字段：exitWhenEmpty bool、exitWhenEmptyGrace time.Duration（New 装配直传，grace<0 防御性钳 0 注释）；exitEmptyTimer *time.Timer（hubMu 保护——启停全在 hubMu 内，注释引 resize.go initArbiter 先例）；exiting bool（hubMu 保护——lifecycle 终结广播门）。
       - lifecycle 内：hubMu 快照段（06-01 形态）取锁后立即 `s.exiting = true` 再取快照——注释：广播 Close 引发的 detach 致空属正常终结序列，空触发检查该位抑制（事件流可信度与信号竞态防线；无此门自然退出路径有 SIGHUP 翻码竞态）。
    3. `internal/server/clients.go`：
       - 新增 `func (s *Server) maybeExitWhenEmptyLocked(c *client)`（注释：调用方必须已持 hubMu 且刚 removeLocked(c) 成功——事件=非空→空迁移，RESEARCH Pitfall 2 启动期恒空天然免疫）：判定 `!s.exitWhenEmpty || s.exiting || len(s.registry.set) != 0` 任一成立即 return；grace==0（立即形态）→ logEvent(c.remote, websocket.StatusNormalClosure, "exit_when_empty") + s.sess.SignalHangup()；grace>0 → 既有 timer 先 Stop（幂等重启——再次断开重新计时）→ `s.exitEmptyTimer = time.AfterFunc(s.exitWhenEmptyGrace, ...)`（回调捕获 c.remote；回调体内：hubMu.Lock + defer Unlock → `s.exiting || len(s.registry.set) != 0` 复查仍空守卫 → logEvent(remote, 1000, "exit_when_empty") + s.sess.SignalHangup()——注释：复查是恰好一次的兜底，Pitfall 4；不调 exitf——零新 exitf 分支，D-13 硬约束）→ logEvent(c.remote, websocket.StatusNormalClosure, "exit_when_empty_wait")。
       - detach 尾部（removeMember/recalcNow/promoteNextLocked 之后、hubCond.Broadcast 之前）与 kickSlowConsumerLocked 尾部（同位）各插一行 s.maybeExitWhenEmptyLocked(c)——两调用点注释同款（注册表空 = 触发源，SESS-01/02）。
       - registerLocked 尾部加宽限取消点：`if s.exitEmptyTimer != nil { s.exitEmptyTimer.Stop(); s.exitEmptyTimer = nil; logEvent(c.remote, websocket.StatusNormalClosure, "exit_when_empty_cancel") }`——注释：宽限内任一端 attach 成功即取消退出（D-14），恰好一次（置 nil 防重复 Stop 与重复事件）。
    4. logEvent 三要素纪律：remote 取触发客户端 c.remote（timer 回调用创建时捕获值）；code 恒 websocket.StatusNormalClosure（1000 收口桶——reason 区分语义，注释登记该口径）；token/ticket/凭据值永不入参（SEC-01 红线注释保持）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -race -count=1 ./internal/server/ ./internal/pty/</automated>
  </verify>
  <acceptance_criteria>
    - go build/vet 退出 0；server/pty 两包既有套件 -race 全绿（零回归——本任务不改测试）
    - grep -c 'func (s \*Session) SignalHangup' internal/pty/signal_linux.go == 1 且 internal/pty/signal_darwin.go == 1（双平台同签名）
    - grep -c 'go:build linux' internal/pty/signal_linux.go == 1 且 grep -c 'go:build darwin' internal/pty/signal_darwin.go == 1（构建标签纪律）
    - grep -c 'ExitWhenEmpty' internal/server/server.go >= 4（Options 两字段 + Server 字段 + New 装配）
    - grep -c 'maybeExitWhenEmptyLocked' internal/server/clients.go >= 3（定义 + detach/kick 两调用点）
    - grep -c 'exitEmptyTimer' internal/server/clients.go >= 3（触发点启动 + registerLocked 取消 + 置 nil）
    - grep -c 'exiting' internal/server/server.go >= 2（字段 + lifecycle 置位）且 grep -c 'exiting' internal/server/clients.go >= 2（两处置守卫——触发函数与 timer 回调）
    - lifecycle 函数体内行号序断言（review #5 吸收——exiting 须先于注册表快照置位）：`awk '/func \(s \*Server\) lifecycle/,/^}/' internal/server/server.go` 区间内 `s.exiting = true` 行号 < `range s.registry.set` 快照循环行号（同一 hubMu 临界区内先置位再快照）
    - 核读（review #5 全部代码路径覆盖核实）：registry.removeLocked 返回值语义 = 成员在 set 中删除成功才 return true（kick 路径 kickSlowConsumerLocked 消费正确）；maybeExitWhenEmptyLocked 调用点恰好两处（detach 尾部 + kickSlowConsumerLocked 尾部）且均在 removeLocked(c) 返回 true 之后
    - grep -c 'exit_when_empty' internal/server/clients.go >= 3（exit_when_empty 触发 + _wait + _cancel 三 reason 串）
    - grep -v '^\s*//' internal/server/server.go | grep -c 'termOnce.Do' == 1（exitf 仍单点收口——terminate 零改动，D-13）
  </acceptance_criteria>
  <done>断开退出服务端机制落码：SIGHUP 进程组方法双平台就位、空迁移检测挂两移除点、宽限计时器启停恰好一次、exiting 门抑制终结广播误触发；exitf 单点收口不变量保持。行为断言由 Task 3 五测锁定。</done>
</task>

<task type="auto">
  <name>Task 3: emptyexit_test.go 六测（立即/宽限取消/宽限到期/kick 触发/exiting 门/计时器晚于 lifecycle）+ io_test.go TestSignalHangup</name>
  <files>internal/server/emptyexit_test.go, internal/pty/io_test.go</files>
  <read_first>
    - internal/server/e2e_test.go（startTestServerWith 121-137 装配 + waitExit 先例 + dialHello 形态 + startTrackedServerWith stderr 捕获装配——05-01 WaitGroup happens-before 先例，TestExitWhenEmptyTimerAfterLifecycle 用）
    - internal/server/keepalive_test.go（assertNoExit 静默反证先例——exitCh select + time.After）
    - internal/server/slowclient_test.go（stall 夹具形态——OutboxBytes 覆写 + 输出洪水驱动踢出；kick 路径测试的直接先例）
    - internal/pty/io_test.go（既有测试的包声明与夹具形态——同包追加）
    - internal/server/clients.go（Task 2 产出——被测对象形态）
    - .planning/phases/06-session-lifecycle/06-PATTERNS.md（§8 exitf 捕获桩先例）
  </read_first>
  <action>
    1. 新建 `internal/server/emptyexit_test.go`（package server_test 黑盒，helper 复用 e2e_test.go；全部 -race 必跑；客户端 Read 永不带 deadline ctx——Pitfall 2 回归锁）：
       - TestExitWhenEmptyImmediate：opts{Writable:true, ExitWhenEmpty:true, ExitWhenEmptyGrace:0} + argv /bin/cat → dialHello → c.Close(1000) → exitCh 5s 内收码恰好一次（码值 = Task 1 门裁决值——accept-255 形态断言 -1（exitf 捕获桩收 ExitError ExitCode 原值，os.Exit 截断 255 只在真实二进制出现，06-06 S3/S4 断言进程级）；map-to-0 形态断言 0；map-to-129 形态断言 129——断言常量以门裁决注释标明）。
       - TestExitWhenEmptyGraceCancel：grace=2s → attach → close → 300ms 后再 dialHello 成功（取消生效）→ echo 验证会话存活（INPUT→OUTPUT 一致）→ 1.5s 时点 exitCh 静默（assertNoExit 形态——旧 timer 若未取消此时已到期）→ close 第二端 → exitCh 在 grace+2s 余量内收码（再次断开重新计时后到期）。
       - TestExitWhenEmptyGraceExpire：grace=400ms → attach → close → 不再 attach → close 后 100ms 时点 exitCh 静默（不过早退出）→ 3s 内收码。
       - TestExitWhenEmptyKickTrigger：opts ExitWhenEmpty grace=0 + OutboxBytes 小值覆写（slowclient_test.go 夹具形态）+ argv seq 洪水 → stall 客户端被 1013 踢出（第二移除路径）→ 注册表空 → exitCh 5s 内收码。
       - TestExitWhenEmptyLifecycleGate：opts ExitWhenEmpty grace=0 + argv `sh -c 'sleep 1; exit 42'` → dialHello（sleep 保 attach 窗口）→ 客户端收 EXIT{exit_code:42} + 1000（06-01 形态）→ exitCh 收 42 恰好一次——exiting 门抑制广播期 detach 的空触发（无门则 SIGHUP 竞态可能翻码 -1；本测试锁定门存在性）。
       - TestExitWhenEmptyTimerAfterLifecycle（review #5 吸收——宽限计时器在 lifecycle 启动后到期不得再触发；装配用 startTrackedServerWith，stderr 捕获的 WaitGroup happens-before 形态，05-01 决策先例）：opts{Writable:true, ExitWhenEmpty:true, ExitWhenEmptyGrace:2s} + argv `sh -c 'sleep 1; exit 42'` → dialHello → 客户端 close（宽限计时器启动，到期点 ≈close+2s）→ 子进程 ~1s 时点自然退出先于计时器到期 → lifecycle 置 exiting + exitf(42) → 断言 exitCh 收 42 恰好一次 → 等待越过计时器到期点（close+3s 护栏）→ 断言 exitCh 无第二次收码且 stderr 不含触发事件行 "reason=exit_when_empty\n"（行尾锚定区分——exit_when_empty_wait 启动行允许存在；回归形态 = 计时器回调缺 exiting 复查时向已终结会话补发 SIGHUP 并打误导性触发日志，review『confusing log』关切的可执行闭合）。
    2. `internal/pty/io_test.go` 追加 TestSignalHangup（包声明随既有文件）：pty.Start([]string{"sleep", "600"}) → sess.SignalHangup() → sess.Wait() 返回 err → errors.As 取 *exec.ExitError → ee.Sys().(syscall.WaitStatus) 断言 Signaled()==true 且 Signal()==syscall.SIGHUP；10s 护栏（既有测试统一超时纪律）。
    3. 全量回归：server/pty/proto/cmd 四包 -race 全绿。
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/server/ ./internal/pty/ ./internal/proto/ ./cmd/wesh/</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'func TestExitWhenEmptyImmediate\|func TestExitWhenEmptyGraceCancel\|func TestExitWhenEmptyGraceExpire\|func TestExitWhenEmptyKickTrigger\|func TestExitWhenEmptyLifecycleGate\|func TestExitWhenEmptyTimerAfterLifecycle' internal/server/emptyexit_test.go == 6
    - grep -c 'func TestSignalHangup' internal/pty/io_test.go == 1
    - TestExitWhenEmptyGraceCancel 断言面含：宽限内再 attach 成功 + echo 会话存活 + 旧 timer 到期时点 exitCh 静默 + 再次断开后收码
    - TestExitWhenEmptyLifecycleGate 断言面含：EXIT{42} + 1000 + exitf(42) 恰好一次
    - TestExitWhenEmptyTimerAfterLifecycle 断言面含（review #5 吸收）：exitf(42) 恰好一次 + 越过计时器到期点后无第二次收码 + stderr 无 "reason=exit_when_empty\n" 触发事件行
    - 四包 -race 全绿（含既有套件零回归）
  </acceptance_criteria>
  <done>SESS-01/02 服务端行为全锁：立即退出/宽限取消/宽限到期/kick 触发/exiting 门/计时器晚于 lifecycle 六路径 + 信号送达单测；-race 全绿。CLI flag 投影（--once 语法糖与 --exit-when-empty 解析）由 06-04 落地，协议层证据由 06-06 phase06.mjs S3-S5 补齐。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

本 phase 无 plain *-SPEC.md；edge-probe 对两条需求的分类均为 unclassified（中文需求文本未被英文启发式命中，分类器局限），按 #1110 规则保持 unresolved，作为 flagged assumption 显式记录（no-silent-drop）：

- requirement: SESS-01 ｜ probe: "unclassified — review manually" ｜ status: flagged-unverified ｜ 处置：边角面已由 CONTEXT.md D-12/D-13（语法糖展开与统一收口路径）、06-RESEARCH Pattern 4（空迁移检测挂点）与 Pitfall 2（启动期恒空免疫）实质覆盖；断言已提升为 must_haves.truths，执行期由 TestExitWhenEmpty* 五测与 06-06 S3 实证；CLI 投影面（--once 展开与冲突矩阵）由 06-04 锁定。
- requirement: SESS-02 ｜ probe: "unclassified — review manually" ｜ status: flagged-unverified ｜ 处置：边角面已由 CONTEXT.md D-14（三形态语义）、06-RESEARCH Pitfall 4（计时器泄漏/双触发防线）实质覆盖；宽限取消/到期两路径由 TestExitWhenEmptyGraceCancel/GraceExpire 锁定，协议层由 06-06 S4/S5 实证。

## Review Dispositions (06-REVIEWS, OpenCode)

| 发现 | 处置 |
|------|------|
| HIGH #1：OQ1 阻塞链（门未指明不同选择的下游影响） | 维持门设计（review 确认其关键性）；Task 1 context 注入评审佐证 + acceptance 补三处下游消费点参数化闭合条目 |
| HIGH #5：exiting 置位须先于注册表快照；kick 路径 removeLocked 返回值可靠性待验 | 已吸收——Task 2 acceptance 加 lifecycle 体内行号序断言（awk 区间）+ removeLocked 返回值语义/调用点覆盖核读条目；Task 3 加 TestExitWhenEmptyTimerAfterLifecycle 行为锁定 |
| MEDIUM：timer 回调不查 SIGHUP 是否已发，建议 sighupSent 标志位 | 驳回标志位——Pitfall 4 设计已闭合：AfterFunc 单次触发 + registerLocked 恰好一次取消（置 nil）+ 回调 exiting/空复查 + SIGHUP 幂等（ESRCH 静默）+ exitf sync.Once 兜底，标志位是冗余状态（多一位即多一处漂移面）；其『误导日志』底层关切由新测试的 stderr 触发事件零次断言闭合 |
| Suggestion：为 exiting 门防 double-SIGHUP 加显式测试（计时器晚于 lifecycle） | 已吸收——TestExitWhenEmptyTimerAfterLifecycle（Task 3 第六测） |
| Suggestion：README 按 OQ1 裁决记录退出状态 | 已覆盖——06-07 README 条款随门裁决值落地（Task 1 acceptance 已登记该消费点） |

## Artifacts this plan produces

- 新文件 `internal/pty/signal_linux.go` / `internal/pty/signal_darwin.go`：`func (s *Session) SignalHangup()`（平台构建标签，同签名）
- `internal/pty/io_test.go`：`TestSignalHangup`
- `internal/server/server.go`：`Options.ExitWhenEmpty bool` + `Options.ExitWhenEmptyGrace time.Duration`；Server 字段 `exitWhenEmpty`/`exitWhenEmptyGrace`/`exitEmptyTimer *time.Timer`/`exiting bool`；lifecycle 快照前 exiting 置位
- `internal/server/clients.go`：`func (s *Server) maybeExitWhenEmptyLocked(c *client)`（detach/kickSlowConsumerLocked 两调用点）；registerLocked 宽限取消点；logEvent reason 串 `exit_when_empty`/`exit_when_empty_wait`/`exit_when_empty_cancel`
- 新文件 `internal/server/emptyexit_test.go`：`TestExitWhenEmptyImmediate`、`TestExitWhenEmptyGraceCancel`、`TestExitWhenEmptyGraceExpire`、`TestExitWhenEmptyKickTrigger`、`TestExitWhenEmptyLifecycleGate`、`TestExitWhenEmptyTimerAfterLifecycle`（review #5 吸收）

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 注册表状态→子进程进程组 | 客户端断开事件驱动 OS 级信号——信号目标必须恒为本会话进程组，误发即杀无关进程 |
| 计时器 goroutine↔hubMu 状态 | 宽限回调并发访问注册表——泄漏/双触发/竞态会把活会话静默杀死 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-02a | Tampering | SIGHUP 误发无关进程组 | high | mitigate | kill 目标恒为 -sess.Cmd.Process.Pid（setsid 组长，pgid 恒等自身 pid，spawn.go 调用链 + P1 注释论证）；不读外部 pid；无用户可配信号面（Phase 7 OPS-04 才引入，届时评审）；TestSignalHangup 锁定送达语义 |
| T-06-02b | Denial of Service | 计时器泄漏/双触发杀死活会话 | high | mitigate | 启停全在 hubMu 内（触发=两移除点、取消=registerLocked，置 nil 防重复）；回调取 hubMu 复查『仍空且未 exiting』（arbiter timer 同款纪律）；SIGHUP 幂等（ESRCH 静默）；exitf sync.Once 天然兜底；TestExitWhenEmptyGraceCancel/Expire 行为锁定 |
| T-06-02c | Denial of Service | 启动即退出（注册表启动期恒空被当触发） | medium | mitigate | 事件=非空→空迁移——检测只挂 detach/kick 两移除点（removeLocked 成功后才判定），启动期零调用结构性免疫（RESEARCH Pitfall 2）；严禁轮询/状态式检测 |
| T-06-02d | Information Disclosure | logEvent 新事件泄露敏感值 | low | mitigate | 三要素 remote/code/reason 单行——token/ticket/凭据值永不入参（SEC-01 红线）；退出码/信号名非敏感；reason 为静态机器串（R-10 命名族） |
| T-06-SC | Tampering | 依赖安装供应链 | low | accept | 本 plan 零新依赖零安装（06-RESEARCH Package Legitimacy Audit：无输入） |
</threat_model>

<verification>
- go build ./... && go vet ./... 退出 0
- go test -race -count=1 ./internal/server/ ./internal/pty/ ./internal/proto/ ./cmd/wesh/ 全绿（含七新测试）
- 源码断言：双平台 SignalHangup、Options 两字段、两移除点挂点、宽限取消点、exiting 门（行号序断言）、exitf 单点收口不变量
</verification>

<success_criteria>
- --once 语义的服务端核心成立：唯一客户端断开 → SIGHUP 进程组 → lifecycle 收口退出（SESS-01 服务端半侧）
- --exit-when-empty 三形态成立：立即 / 宽限到期退出 / 宽限内 attach 取消（SESS-02 服务端半侧）
- 默认不开启零漂移；exitf 单点收口与 lifecycle 唯一终结路径不变量保持（D-13）
</success_criteria>

<output>
创建 `.planning/phases/06-session-lifecycle/06-02-SUMMARY.md`：记录 OQ1 门裁决值（accept-255/map-to-0/map-to-129）与其对测试断言常量的驱动、SIGHUP 复活形态与 git 历史对照、空触发/计时器/exiting 门三不变量落码、六测试名称与锁定行为、遗留事项。
</output>
