---
phase: 06-session-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/proto/proto.go
  - internal/proto/proto_test.go
  - internal/server/server.go
  - internal/server/exit_test.go
  - internal/server/exitmsg_test.go
  - web/src/main.ts
  - web/dist/index.html
autonomous: false
requirements:
  - SESS-03

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "'X'（0x58）EXIT 类型字节三侧手工对齐（D-08/D-16）：proto.go 常量区补 `Exit = 'X'`（proto.go:32 占位注释兑现改写为已启用说明）+ web/src/main.ts 帧常量区加 `EXIT = 0x58`——两侧注释互相指路纪律不变；TestProtocolConstants 帧字节表加 {Exit, 'X'} 行逐字钉死（proto_test.go:196-212 表先例）"
    - "EXIT 载荷形状公开契约（D-09，P2 D-02 演化纪律约束后续）：ExitPayload{ExitCode int `json:\"exit_code\"`, Message string `json:\"message\"`}（snake_case tag，ErrorPayload 同构）；信号死亡 exit_code=-1；ExitFrame(exitCode int, message string) []byte 组帧与 ErrorFrame 逐字同构（append([]byte{Exit}, b...)，固定 schema 下 json.Marshal 不会失败）；round-trip 单测锁定（含 exit_code=-1 行）"
    - "广播序列写序安全（D-10 + RESEARCH Pitfall 1）：lifecycle 内 Drain 后组帧一次 exitFrame（全客户端共享只读引用，P5-1 纪律），hubMu 快照注册表后每客户端 goroutine 内【先同步 conn.Write(2s 超时 ctx, MessageBinary, exitFrame) 再 conn.Close(1000)】——写序靠库帧级串行化保证；禁止经 outbox.trySend 异步入队（writer drain 与 Close 关闭帧竞态，关闭帧超车则客户端收 1000 却无退出码）；Write 失败不补救直接 Close（进程已退出场景无需保帧，CONTEXT discretion 授权）；并行等待自然有界（Close 内建 5s+5s 上界）"
    - "message 三形态服务端唯一写口（UI-SPEC §Session Ended Contract 文案表逐字 + Pitfall 3 红线）：正常退出 `The process exited with code {N}.`；信号死亡 `The process was killed by signal {SIGNAME}.`——SIGNAME 走显式大写名映射（SIGHUP/SIGINT/SIGQUIT/SIGKILL/SIGTERM 等常见集合），禁止裸用 Signal.String()（GOROOT zerrors 表产出 'hangup' 式小写描述词）；未知名回退 `The process was killed by signal {N}.`；Wait 返回非 ExitError 错误兜底 exit_code=-1 + `The process terminated.`；exitf 的进程退出码提取逻辑（code 变量）零漂移；大写名映射抽为独立 signalName(sig) (string, bool) helper 供 exitMessage 消费（review 吸收——映射表独立白盒可测），signalName 逐行与 exitMessage 两可构造分支（nil err→code 0 文案、非 ExitError→The process terminated.）由 exitmsg_test.go 白盒锁定（resize_test.go 白盒先例）"
    - "'E' Error 与 'X' EXIT 类型字节区分锁定（D-08 语义独立——终结 ≠ 错误，exit 0 不挤占 Error code 空间；review E/X 共存核实建议吸收）：TestExitFrame 含 Exit != Error 断言行；proto.go 'X' 常量注释登记与 'E' 的语义边界"
    - "双端在线（rw+ro 混合）收逐字节一致的同一 EXIT 帧（终结无权限语义，ro/rw 全员同帧无分档）后各自收到 1000 正常关闭，EXIT 必先于 onclose 到达——TestExitFrameBroadcast 锁定；信号死亡 exit_code=-1 且 message 含大写信号名——TestExitFrameSignal 锁定"
    - "前端承接（D-10/UI-SPEC R2 逐字契约）：onmessage switch 新增 `case EXIT`——JSON.parse 载荷暂存 lastExit（lastError 同款通道同款容错：parse 失败 console.warn 静默丢弃，main.ts:643-648 先例）；onclose case 1000 正文 = `lastExit?.message ?? 既有硬编码文案`（旧服务端/异常路径回退逐字不变，前向兼容 P2 D-02）；title 恒 'Session ended'、hintPrefix 恒 'Start wesh again from your shell, then' 不变"
    - "EXIT message 为契约约束短文本（三形态均 ≤~45 字符；UI-SPEC long-text/overflow status-panel covered）——480px 面板 max-width 内自然折行；message 经 textContent 渲染（showStatus 全程无 innerHTML，注入面结构性不存在）"
  prohibitions:
    - statement: "EXIT 帧的 exit_code 必须忠实传递子进程真实退出状态——信号死亡不得粉饰为 exit 0 或任何正常退出码（exit_code=-1 + 信号名文案是唯一合法形态，D-09 语义同源 GOROOT ExitCode()=-1）"
      status: resolved
      verification: judgment
    - statement: "EXIT message 的唯一写口是服务端组文案——前端不得自维护信号文案表、不得改写/拼接 message（textContent 直显，D-09 纪律）"
      status: resolved
      verification: judgment
  artifacts:
    - path: "internal/proto/proto.go"
      provides: "Exit 常量（'X'）+ ExitPayload + ExitFrame（D-08/D-09）"
      contains: "func ExitFrame"
    - path: "internal/proto/proto_test.go"
      provides: "TestProtocolConstants 'X' 行 + ExitFrame round-trip"
      contains: "Exit"
    - path: "internal/server/server.go"
      provides: "lifecycle EXIT 广播段（组帧一次 + 同步 Write→Close 写序）+ 退出码/信号提取与 message 组装"
      contains: "ExitFrame"
    - path: "internal/server/exit_test.go"
      provides: "TestExitFrameBroadcast + TestExitFrameSignal（SESS-03 行为锁定）"
      contains: "TestExitFrameBroadcast"
    - path: "internal/server/exitmsg_test.go"
      provides: "TestSignalName + TestExitMessage（白盒——signalName 映射表逐行 + exitMessage 可构造分支，review 吸收）"
      contains: "TestSignalName"
    - path: "web/src/main.ts"
      provides: "EXIT 常量 + lastExit 暂存 + case EXIT + onclose 1000 正文改造"
      contains: "0x58"
    - path: "web/dist/index.html"
      provides: "重建产物（前端 EXIT 承接随构建入库）"
      contains: ""
  key_links:
    - from: "internal/server/server.go (lifecycle)"
      to: "internal/proto/proto.go (ExitFrame)"
      via: "sess.Wait 返回 → code/message 提取组装 → 组帧一次 → hubMu 快照 → 每客户端 goroutine 同步 Write(EXIT) 后接 Close(1000)"
      pattern: "ExitFrame"
    - from: "internal/server/server.go (lifecycle)"
      to: "os/exec + syscall (退出码/信号)"
      via: "errors.As ExitError → ExitCode()；信号死亡 WaitStatus.Signaled()/Signal() 取信号 → 大写名映射"
      pattern: "WaitStatus|ExitError"
    - from: "web/src/main.ts (onmessage case EXIT)"
      to: "web/src/main.ts (onclose case 1000)"
      via: "EXIT 载荷暂存 lastExit → 1000 分支正文 lastExit?.message ?? 硬编码回退"
      pattern: "lastExit"
---

<objective>
SESS-03 端到端 tracer：子进程退出后全部在线客户端收到含退出码的类型化终结帧（新 S→C 类型字节 'X'，载荷 {"exit_code":N,"message":M}）再以 1000 正常关闭——proto 契约 + lifecycle 写序安全广播 + Go 行为测试 + 前端暂存承接 + dist 重建。

Purpose: EXIT 帧是本 phase 唯一的新协议面与最大机制风险点（EXIT↔Close 写序竞态，RESEARCH Pitfall 1）——先在 agent 最佳上下文里把这根脊柱打通并锁死，后续 plan（断开退出/CLI/重连/UAT）全部在它证明的终结语义上叠加。前端承接同 plan 落地（vertical slice：帧契约 → 服务端广播 → 前端展示一链到底）。
Output: proto.Exit/ExitPayload/ExitFrame + lifecycle EXIT 广播 + exit_test.go 两测试 + main.ts case EXIT 与 1000 正文 + dist 重建，go test -race 与前端构建全绿。含 D-08/D-09 one-way 确认门。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-session-lifecycle/06-CONTEXT.md
@.planning/phases/06-session-lifecycle/06-RESEARCH.md
@.planning/phases/06-session-lifecycle/06-PATTERNS.md
@.planning/phases/06-session-lifecycle/06-UI-SPEC.md
@.planning/phases/06-session-lifecycle/06-VALIDATION.md
@internal/proto/proto.go
@internal/server/server.go
@web/src/main.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 确认门——EXIT 帧类型字节与载荷公开契约（D-08/D-09，one-way）</name>
  <read_first>
    - .planning/phases/06-session-lifecycle/06-CONTEXT.md（decisions 节 D-08/D-09/D-10 逐字 + Claude's Discretion 类型字节建议 'X'）
    - .planning/phases/06-session-lifecycle/06-UI-SPEC.md（§Session Ended Contract 文案三形态定稿）
  </read_first>
  <decision>EXIT 帧 = 新 S→C 类型字节 'X'（0x58），载荷 {"exit_code":N,"message":M}（信号死亡 exit_code=-1，message 含大写信号名）——前后端公开协议契约（one-way door：发布后改值/改义破坏全部已部署客户端，P2 D-01/D-02 纪律）</decision>
  <context>
    D-08/D-09 在 discuss-phase 已锁定（终结语义独立于错误语义故不复用 'E' Error 帧；exit_code 结构化供测试断言、message 前端直显；服务端组文案唯一写口）；proto.go:32 注释已为 'X' 预留占位（本 session 逐字核实）；UI-SPEC 已定稿 message 三形态文案表。本门是实现前最后一次确认，不是重新决策。
  </context>
  <options>
    <option id="as-locked">
      <name>按锁定值落地（推荐）</name>
      <pros>'X'/0x58 + {"exit_code":N,"message":M} 三形态文案与 CONTEXT.md D-08/D-09、UI-SPEC §Session Ended Contract 逐字一致；proto.go:32 预留位兑现</pros>
      <cons>发布后类型字节空间少一位（'T'/'P' 占位纪律同款，已计价）</cons>
    </option>
    <option id="revise">
      <name>调整类型字节值/载荷形状</name>
      <pros>若有协议侧新信息可低成本修正</pros>
      <cons>推翻 CONTEXT.md 锁定项与 UI-SPEC 定稿，需重新 discuss 并回填 06-RESEARCH</cons>
    </option>
  </options>
  <resume-signal>选择 as-locked（继续实现）或 revise（说明调整点）</resume-signal>
  <acceptance_criteria>
    - 本门必须停止等待用户：用户明确选择 as-locked 后才进入 Task 2；revise 时停止执行并回填 CONTEXT.md/UI-SPEC（先例不得作为自动通过依据）
  </acceptance_criteria>
</task>

<task type="tracer" tdd="true">
  <name>Task 2 (tracer): proto EXIT 帧契约 + lifecycle 写序安全广播 + exit_test.go 端到端两测</name>
  <reversibility rating="one-way">EXIT 类型字节与载荷形状是前后端公开协议契约（D-08/D-09）——发布后改值/改义破坏全部已部署客户端；确认门见 Task 1。</reversibility>
  <files>internal/proto/proto.go, internal/proto/proto_test.go, internal/server/server.go, internal/server/exit_test.go, internal/server/exitmsg_test.go</files>
  <read_first>
    - internal/proto/proto.go（全文——常量区 19-34（32 行 'X' 占位注释）、ErrorPayload 112-117、ErrorFrame 165-170 逐字先例）
    - internal/proto/proto_test.go（TestProtocolConstants 帧字节表 196-212、TestWelcomeFrameErrorFrame round-trip 64-136 先例）
    - internal/server/server.go（lifecycle 955-1004 全文——Wait 959 / code 提取 960-964 / Drain 965 / close(inputDone) 969 / hubMu 快照 974-979 / 并行 Close 981-988 / terminate 994）
    - internal/server/multi_test.go（TestExitBroadcast 195-228——广播场景装配与『读到 CloseError 为止途中帧丢弃』形态；本 plan 新测试的直接编排先例）
    - .planning/phases/06-session-lifecycle/06-RESEARCH.md（Pattern 1 组帧同构 / Pattern 2 写序论证 / Pattern 3 GOROOT 退出码信号提取配方 / Pitfall 1 写序竞态 / Pitfall 3 信号名陷阱）
    - .planning/phases/06-session-lifecycle/06-PATTERNS.md（§1/§2/§3 逐字先例与落法）
    - .planning/phases/06-session-lifecycle/06-UI-SPEC.md（§Session Ended Contract message 三形态文案表——逐字定稿）
  </read_first>
  <behavior>
    - TestProtocolConstants：帧字节表新增行断言 Exit == 'X'（既有五行逐字不动）
    - TestExitFrame round-trip：ExitFrame(42, "The process exited with code 42.") → [0]=='X' → Unmarshal → ExitCode==42 且 Message 逐字等；ExitFrame(-1, "The process was killed by signal SIGHUP.") 行同款；含类型字节区分断言 Exit != Error（'X' 与 'E' 不得同值——终结语义独立于错误语义，D-08；review 建议吸收）
    - TestSignalName（新建 exitmsg_test.go，package server 白盒——resize_test.go 先例，Go 单文件单 package 约束）：映射表逐行断言（SIGHUP/SIGINT/SIGQUIT/SIGKILL/SIGTERM 等常见集合 → 大写名且 ok==true；未在映射表的信号如 SIGWINCH → ok==false 回退位）
    - TestExitMessage（同文件白盒）：exitMessage(nil, 0) == "The process exited with code 0."；exitMessage(errors.New 合成的非 ExitError, 0) == "The process terminated."（兜底分支白盒直测——review 兜底核实建议吸收；ExitError 两分支由 TestExitFrameBroadcast/Signal 真实进程路径覆盖，exec.ExitError 无公开构造器不可合成）
    - TestExitFrameBroadcast：Writable:true 起 `bash --norc --noprofile`，dialHello 双客户端（80x24 / 100x40）→ A 写 INPUT "exit 42\r" → 两端各自读至 CloseError：最后到达的数据帧为 EXIT 帧（帧序断言——EXIT 必先于 onclose/1000）且两端 EXIT 帧体逐字节一致（exit_code==42、message=="The process exited with code 42."），CloseError.Code==1000；waitExit(exitCh, 42)（退出码传递语义不变）
    - TestExitFrameSignal：argv `sh -c 'sleep 1; kill -HUP $$'`（sleep 保 attach 窗口，kill -HUP $$ 自发信号死亡）→ 客户端收 EXIT{exit_code:-1} 且 message 含大写 "SIGHUP"（Pitfall 3 映射断言）→ 1000；exitf 捕获桩收到 -1（ExitError ExitCode 语义；os.Exit 截断 255 只在真实二进制出现，UAT 层 06-06 断言）
  </behavior>
  <action>
    1. `internal/proto/proto.go`：
       - 常量区 32 行占位注释兑现改写：'X' EXIT 已启用（SESS-03，Phase 6 落地——终结帧先于 1000 广播；与 'E' Error 帧的语义边界——终结 ≠ 错误，exit 0 是正常终结不挤占 Error code 空间，D-08 用户裁决，review E/X 共存核实吸收）；补 `Exit = 'X' // 0x58, S→C, JSON {"exit_code":N,"message":M}` 常量行；文件头『前端 web/src/main.ts 帧常量手工对齐、两侧注释互相指路（D-16）』纪律保持。
       - ExitPayload 结构（ErrorPayload 同构，显式 snake_case json tag 防字段名漂移）：ExitCode int（信号死亡 = -1——exec ExitCode 语义同源，D-09）+ Message string（英文人话前端直显，服务端组文案唯一写口）。
       - ExitFrame(exitCode int, message string) []byte（ErrorFrame 逐字同构：json.Marshal + append([]byte{Exit}, b...)，注释『固定 schema 下 json.Marshal 不会失败』同款）。
    2. `internal/proto/proto_test.go`：帧字节表加 {"Exit", Exit, 'X'} 行；新增 TestExitFrame round-trip（组帧 → ef[0] != Exit 断言 → json.Unmarshal(ef[1:]) → ExitCode/Message 逐字段相等；两形态行：exit 42 与 -1/SIGHUP）。
    3. `internal/server/server.go` lifecycle() 改造（D-10 广播序列）：
       - 新增两个包级未导出函数（lifecycle 调用点紧邻）：
         - `signalName(sig syscall.Signal) (string, bool)`（review 吸收——映射表抽为独立 helper，白盒可测）：switch syscall.Signal 大写名映射（SIGHUP/SIGINT/SIGQUIT/SIGILL/SIGABRT/SIGKILL/SIGSEGV/SIGPIPE/SIGALRM/SIGTERM/SIGUSR1/SIGUSR2/SIGCHLD 常见集合）→ ("SIGHUP", true) 形态；未在表 → ("", false)；注释『Pitfall 3：Signal.String() 产出小写描述词（"hangup"），禁止裸用——显式映射是唯一合法形态』。
         - `exitMessage(err error, code int) string`：err==nil → "The process exited with code 0."；ExitError 且 code>=0 → fmt.Sprintf("The process exited with code %d.", code)；ExitError 且 code<0（信号死亡）→ WaitStatus 提取（ee.Sys().(syscall.WaitStatus) 类型断言 + Signaled() 守卫——注释登记：unix 上 ExitError.Sys() 恒为 WaitStatus，断言失败分支为防御性兜底，真实进程不可达）→ signalName 命中组 "The process was killed by signal SIGHUP." 形态；未命中/断言失败回退 fmt.Sprintf("The process was killed by signal %d.", int(sig))；非 ExitError（Wait 返回其他错误）→ "The process terminated."。三形态文案与 UI-SPEC 文案表逐字一致。
       - lifecycle 内：code 提取（959-964 既有）零改动；close(inputDone) 之后组帧一次 `exitFrame := proto.ExitFrame(code, exitMessage(err, code))`（注释：全客户端共享只读引用，P5-1 纪律）；快照循环的每客户端 goroutine 改为——先 `wctx, cancel := context.WithTimeout(context.Background(), 2*time.Second)` + `_ = c.conn.Write(wctx, websocket.MessageBinary, exitFrame)` + cancel()，后 `_ = c.conn.Close(websocket.StatusNormalClosure, "")`（注释逐字登记写序论证：库帧级写串行化保同 goroutine 先写先发；Write 失败不补救——进程已退出无需保帧；2s 常量注释挂『Phase 9 标定』——RESEARCH OQ3 定值，并显式登记权衡（review LOW 吸收——拒绝可配化，P2 D-10 常量纪律）：stall/慢链路 2s 未写完 ~100B EXIT 帧即放弃直写，该端退化为 1000 + 前端硬编码回退文案（R2 回退路径既有，非致命），2s ≪ Close 内建 5s+5s 上界；Close 内建 5s+5s 上界并行等待自然有界）；lifecycle 头注释『广播 1000』更新为『广播 EXIT 帧 + 1000』（D-10 序列）。
       - exitf 的进程退出码提取逻辑（code 变量与 terminate(code) 调用）零改动——EXIT 帧载荷的 -1 语义与进程退出状态解耦（信号死亡自然路径既有 exitf(-1) 行为保持）。
    4. 新建 `internal/server/exit_test.go`（package server_test 黑盒，startTestServerWith/dialHello/waitExit 复用 e2e_test.go 零改动——PATTERNS exact 先例）：
       - TestExitFrameBroadcast 与 TestExitFrameSignal 按 <behavior> 逐条落地；客户端 Read 永不带 deadline ctx（Pitfall 2 回归锁——goroutine Read + 缓冲 channel + select time.After 竞速形态或 10s 统护 ctx，e2e 既有先例）；两端读循环收集数据帧至 CloseError，断言最后一帧 [0]==proto.Exit 且帧体逐字节一致。
       - 全量 Go 套件回归：既有广播/退出码测试（TestExitBroadcast/TestExitCodePropagation 读到 CloseError 丢弃途中帧的形态）预期零适配通过；若有个别帧序严格断言因新增 EXIT 帧变红，适配 = 预期序列补 EXIT 帧（Rule 1 同 plan 收口，绝不删断言面）。
    5. 新建 `internal/server/exitmsg_test.go`（package server 白盒——resize_test.go 白盒先例，黑盒 exit_test.go 与本文档分文件满足 Go 单文件单 package 约束；review signalName helper/兜底核实两建议的锁定载体）：
       - TestSignalName：映射表逐行断言（常见集合每个 → 大写名且 ok==true；SIGWINCH 等未在表信号 → ok==false）。
       - TestExitMessage：exitMessage(nil, 0) 与 errors.New 合成非 ExitError 两可构造分支逐字断言（<behavior> 值）。
    6. 提交纪律：proto 契约 + server 广播 + 三测试文件同任务同提交。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -race -count=1 ./internal/proto/ ./internal/server/ ./internal/pty/</automated>
  </verify>
  <acceptance_criteria>
    - go build/vet 退出 0；proto/server/pty 三包 -race 全绿（含两新测试与既有套件零回归）
    - grep -c "Exit = 'X'" internal/proto/proto.go == 1（类型字节钉死）
    - grep -c 'func ExitFrame' internal/proto/proto.go == 1 且 grep -c 'exit_code' internal/proto/proto.go >= 1（载荷 json tag）
    - grep -c 'ExitFrame' internal/server/server.go >= 1（lifecycle 组帧调用点）
    - grep -c 'func signalName' internal/server/server.go == 1（review 吸收——映射表独立 helper）
    - grep -c 'func TestExitFrameBroadcast\|func TestExitFrameSignal' internal/server/exit_test.go == 2
    - grep -c 'func TestSignalName\|func TestExitMessage' internal/server/exitmsg_test.go == 2（白盒锁定——映射表逐行 + 两可构造分支）
    - TestExitFrame 断言面含 Exit != Error 类型字节区分行（review E/X 区分吸收）
    - TestExitFrameBroadcast 断言面含：EXIT 帧序先于 CloseError、两端帧体逐字节一致、exit_code==42、close code==1000、waitExit(42)
    - TestExitFrameSignal 断言面含：exit_code==-1、message 含大写 SIGHUP（grep -c 'SIGHUP' internal/server/exit_test.go >= 1）
    - grep -c 'The process exited with code' internal/server/server.go >= 1（正常退出文案存在——nil err 字面量与 ExitError Sprintf 两分支均含此形态，message 三形态写口 1/3）
    - grep -c 'The process was killed by signal' internal/server/server.go >= 2（信号文案双分支并存——signalName 命中 %s 形态 + 未命中/断言失败 %d 回退，message 三形态写口 2/3）
    - grep -c 'The process terminated\.' internal/server/server.go == 1（非 ExitError 兜底文案唯一存在，message 三形态写口 3/3）
  </acceptance_criteria>
  <done>SESS-03 脊柱打通：'X' 帧契约三侧之 Go 两侧钉死，lifecycle 以写序安全形态广播 EXIT→1000，双端广播/帧序/退出码/信号死亡四行为由两测试锁定；前端承接由 Task 3 落地。</done>
</task>

<task type="auto">
  <name>Task 3: 前端 EXIT 承接——帧常量 + lastExit 暂存 + case EXIT + onclose 1000 正文 + dist 重建</name>
  <files>web/src/main.ts, web/dist/index.html</files>
  <read_first>
    - web/src/main.ts（帧常量区 11-26；per-connection 重置块 391-404；ERROR 暂存通道 643-648 先例；onclose case 1000 分支 707-713；showStatus 365-381——textContent 无 innerHTML 形态）
    - .planning/phases/06-session-lifecycle/06-UI-SPEC.md（§Session Ended Contract 前端承接逐字契约 + R2 修订行 + §Copywriting C-7）
    - .planning/phases/06-session-lifecycle/06-PATTERNS.md（§9 Analog A/D——常量区与暂存通道先例）
  </read_first>
  <action>
    1. `web/src/main.ts`：
       - 帧常量区加 `EXIT = 0x58`（与 proto.go 'X' 手工对齐，注释互相指路纪律——proto.go 侧指路注释已在 Task 2 落位，本侧注释回指 proto.go 常量区）。
       - 模块级新增 `let lastExit: { exit_code: number; message: string } | null = null;`——声明位置 = lastError 模块级声明邻位（执行期 grep -n 'let lastError' web/src/main.ts 定位——review placement 显式化建议吸收）；lastError 同款注释形态：最近一帧 EXIT{exit_code,message}，onclose 1000 展示用，D-10。
       - connect() per-connection 重置块（391-404）加 `lastExit = null;`（IN-01 防漂移登记同款——auth_failed 重试/重连不携带上连接残留）。
       - onmessage switch 新增 `case EXIT:`（ERROR 暂存同款）：try 内 JSON.parse(new TextDecoder().decode(buf.subarray(1))) 赋 lastExit；catch 内 console.warn('discard malformed EXIT frame')（容错文案与 ERROR 帧同族）；注释：D-09/D-10 暂存，onclose 1000 分支正文显示 message。
       - onclose `case 1000:` 正文改为 `lastExit?.message ?? 'The process exited and the wesh server has stopped.'`（未收 EXIT——旧服务端/异常路径——回退既有硬编码文案逐字不变，前向兼容 P2 D-02；title 'Session ended' 与 hintPrefix 'Start wesh again from your shell, then' 逐字不动）。
       - onclose 上方 685-687『1006 永不作为分派依据』注释本 plan 不动（1006 重连触发由 06-03 落地时同步改写——此处保持逐字）。
    2. dist 重建：`time pnpm -C web build`（构建 = tsc && vite build && gzip -k9；tsc 不过则构建失败即本任务红灯）；web/dist/index.html 产物变更随本任务提交（dist 已提交真实构建产物既定策略；.gz 不入库既有 .gitignore 纪律不变）。
    3. `node --test web/src/lib/*.test.ts` 回归（lib 纯函数零改动预期全绿）。
  </action>
  <verify>
    <automated>time pnpm -C web build && node --test web/src/lib/*.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - time pnpm -C web build 退出 0（tsc 严格模式过 + vite 产物 + gzip）
    - node --test web/src/lib/*.test.ts 全绿（零回归）
    - grep -c '0x58' web/src/main.ts >= 1（EXIT 帧常量）
    - grep -c 'case EXIT' web/src/main.ts == 1 且 grep -c 'lastExit' web/src/main.ts >= 4（声明 + 重置块 + 暂存赋值 + 1000 分支消费）
    - grep -c "lastExit?.message ?? 'The process exited and the wesh server has stopped.'" web/src/main.ts == 1（R2 正文改造逐字形态）
    - git status 显示 web/dist/index.html 已修改并随任务提交
  </acceptance_criteria>
  <done>前端 EXIT 承接落地：EXIT 帧暂存 → 1000 正文显示服务端组文案（退出码/信号人话），旧服务端回退零漂移；dist 重建入库。SESS-03 三侧（proto/server/前端）全链接通，协议层端到端证据由 06-06 phase06.mjs 补齐。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

本 phase 无 plain *-SPEC.md；edge-probe 对本条需求的分类为 unclassified（中文需求文本未被英文启发式命中，分类器局限——不代表需求无边角），按 #1110 规则保持 unresolved，作为 flagged assumption 显式记录（no-silent-drop）：

- requirement: SESS-03 ｜ probe: "unclassified — review manually" ｜ status: flagged-unverified ｜ 处置：边角面已由 CONTEXT.md D-08/D-09/D-10（帧类型/载荷/广播序列）、06-RESEARCH Pattern 1/2/3（组帧同构/写序论证/GOROOT 提取配方）与 Pitfall 1/3（写序竞态/信号名陷阱）实质覆盖；可验证断言已提升为 must_haves.truths（写序安全、三形态文案、双端同帧、前端回退），执行期由 TestExitFrameBroadcast/TestExitFrameSignal 与 06-06 phase06.mjs S1/S2 实证。
- canon-referral（ADR-550 D6）：EXIT message 的 HTML 注入面属 OWASP canon——由 threat_model T-06-01b（textContent 渲染 + 服务端唯一写口）与既有 showStatus 无 innerHTML 纪律覆盖，不立 prohibition 条目。

## Review Dispositions (06-REVIEWS, OpenCode)

| 发现 | 处置 |
|------|------|
| HIGH：'E' 与 'X' 共存需核实（含 proto.go:32 占位注释核实） | 已吸收——占位注释核实为 Task 2 既定步骤；E/X 区分入 proto 常量注释 + TestExitFrame 断言行（must_haves 新增 truth） |
| MEDIUM：信号名映射内联，建议抽 signalName() helper | 已吸收——Task 2 抽 `signalName(sig) (string, bool)`，exitMessage 消费；白盒 TestSignalName 逐行锁定 |
| Suggestion：exitMessage 的 WaitStatus 断言失败兜底需核实 | 已吸收——白盒 TestExitMessage 锁定两可构造分支；断言失败分支注释登记 unix 上 Sys() 恒 WaitStatus（防御性兜底真实进程不可达） |
| Suggestion：加 Error vs EXIT 前端区分测试 | 部分吸收——协议字节区分由 TestExitFrame 锁定；前端区分由 06-05 D7（EXIT→Session ended 全链）与既有 phase03/05 系列 ERROR 路径共同覆盖，不新增专项 |
| LOW：2s Write 超时取值任意，建议可配或显式记录权衡 | 部分吸收——拒绝可配化（P2 D-10 常量纪律 + Phase 9 标定挂账既定）；采纳权衡显式文档化（Task 2 注释规格补强） |

## Artifacts this plan produces

- `internal/proto/proto.go`：`Exit = 'X'`（0x58）类型字节常量（含与 'E' Error 语义边界注释）、`ExitPayload{ExitCode, Message}`（json tag exit_code/message）、`func ExitFrame(exitCode int, message string) []byte`
- `internal/proto/proto_test.go`：TestProtocolConstants 新行、`TestExitFrame`（含 Exit != Error 区分断言）
- `internal/server/server.go`：`func signalName(sig syscall.Signal) (string, bool)`（大写名映射表）、`func exitMessage(err error, code int) string`、lifecycle EXIT 广播段（exitFrame 组帧 + 同步 Write(2s)→Close(1000)，2s 权衡注释）
- 新文件 `internal/server/exit_test.go`：`TestExitFrameBroadcast`、`TestExitFrameSignal`
- 新文件 `internal/server/exitmsg_test.go`（白盒）：`TestSignalName`、`TestExitMessage`
- `web/src/main.ts`：`EXIT = 0x58` 帧常量、`lastExit` 模块级暂存变量（声明于 lastError 邻位）、onmessage `case EXIT`、onclose 1000 正文 `lastExit?.message ?? ...`、connect() 重置块 lastExit 清零行
- `web/dist/index.html`：重建产物

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 子进程退出状态→全部在线客户端 | 退出码/信号信息经服务端组帧广播给不可信客户端——文案内容与写序都不得被利用 |
| lifecycle↔每客户端连接 | EXIT 直写与 Close 关闭帧共享同一 WS 写路径——stall 客户端不得拖延全局终结 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01a | Spoofing | EXIT 帧伪造（客户端注入终结提示误导其他用户） | low | accept | 结构性不可能：EXIT 为 S→C 类型字节，C→S 类型空间不变（'0'/'1'/'H'），未知 C→S 字节 1002 直关（server.go 读循环 default 闸既有）——注册表注记 |
| T-06-01b | Tampering | EXIT message 渲染注入（服务端文案进前端面板） | medium | mitigate | message 三形态全部服务端硬编码模板（唯一写口，UI-SPEC 文案表逐字）；前端 textContent 直显（showStatus 全程无 innerHTML，main.ts:365-381 结构性无注入面）；JSON.parse 失败静默丢弃 |
| T-06-01c | Denial of Service | stall 客户端拖延 exitf（EXIT 直写阻塞） | high | mitigate | 同步 Write 带 2s 超时 ctx（超时即放弃该端直写，Close 仍把 1000 送上）+ Close 内建 5s+5s 上界 + 每客户端并行 goroutine + WaitGroup 有界等待——TestExitFrameBroadcast 与 06-06 S1/S2 锁定 |
| T-06-SC | Tampering | 依赖安装供应链 | low | accept | 本 plan 零新依赖零安装（06-RESEARCH Package Legitimacy Audit：无输入，Gate 无需运行） |
</threat_model>

<verification>
- go build ./... && go vet ./... 退出 0
- go test -race -count=1 ./internal/proto/ ./internal/server/ ./internal/pty/ 全绿（含 TestExitFrame/TestExitFrameBroadcast/TestExitFrameSignal）
- time pnpm -C web build 退出 0 且 dist/index.html 变更入库
- node --test web/src/lib/*.test.ts 全绿
- 源码断言：'X' 常量钉死、ExitFrame 组帧调用点、message 三形态写口、前端 0x58/lastExit/case EXIT 落位
</verification>

<success_criteria>
- 子进程退出后所有在线客户端先收到含退出码的 EXIT 帧（信号死亡 exit_code=-1 + 信号名人话）再以 1000 正常关闭——ROADMAP 成功准则 2 全链成立（协议层 UAT 证据 06-06 补齐）
- 旧前端收 EXIT 落 default 静默跳过、1000 面板显示既有硬编码文案——前向兼容成立（P2 D-02）
- 写序安全：EXIT 恒先于 1000 到达，stall 客户端不拖延 exitf
</success_criteria>

<output>
创建 `.planning/phases/06-session-lifecycle/06-01-SUMMARY.md`：记录确认门结果（D-08/D-09 as-locked 或 revise）、EXIT 帧契约落码形态、写序安全论证落地、信号名映射表取舍、两新测试名称与锁定行为、前端承接改造点、既有套件适配明细（如有）。
</output>
