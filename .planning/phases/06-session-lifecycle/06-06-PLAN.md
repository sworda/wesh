---
phase: 06-session-lifecycle
plan: 06
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-04"]
files_modified:
  - web/uat/phase06.mjs
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - CORE-05

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "S1 EXIT 双端广播（SESS-03 协议层终证）：--writable + --credential 实例，客户端 A 经 Basic ticket（rw）+ 客户端 B 经 shareRO token（ro）双 attach → A 写 INPUT 'exit 42\r' → 双端同收 EXIT 帧（exit_code==42 且 message 逐字 == 'The process exited with code 42.'，帧体逐字节一致——终结无权限语义，ro/rw 全员同帧）且 EXIT 先于 onclose 到达（帧序断言：收齐数据帧后 waitClose 得 code==1000）→ wesh 进程 exit 事件码==42（退出码传递进程级锁定）"
    - "S2 EXIT 信号死亡：spawn `sh -c 'sleep 1; kill -HUP $$'`（sleep 保 attach 窗口）→ dialHello → EXIT{exit_code:-1} 且 message 含大写 'SIGHUP'（Pitfall 3 显式映射断言——'killed by signal hangup' 式小写描述词为回归）→ waitClose code==1000"
    - "S3 --once 全链（SESS-01 协议层终证）：--once + --credential 实例 → A dialHelloTicket 成功 → 第二客户端双点位拒绝（POST /api/attach Basic → 503 早闸 + WS 直连升级 → 503 ③位——phase05.mjs S5 双点位先例逐字可抄；503 = 既有 max-clients 计数路径，409 不复活）→ A ws.close() → wesh 进程 exit 事件（退出状态按 06-02 Task 1 门裁决值断言——accept-255 形态为 255，os.Exit(-1) Unix 截断；map-to-0/129 形态随裁决）→ stderr 无异常栈"
    - "S4 --exit-when-empty 立即形态（SESS-02）：无认证实例携裸 flag（spawn 参数 '--exit-when-empty' 不带值——IsBoolFlag 形态实证）→ attach 前 400ms 守候窗断言进程无 exit 事件（启动期注册表恒空不触发——Pitfall 2 的协议层显式证据，review 建议吸收）→ attach → close → 进程 exit 事件（退出状态同门裁决值）"
    - "S5 --exit-when-empty=1500ms 宽限两子场景（时序容差规格，review #6 吸收：取消窗 400ms ≪ 标称 1500ms 留 1100ms 调度余量；到期断言 5s 护栏轮询而非精确时点）：① attach → close → 400ms 后再 attach 成功（宽限取消——会话存活以 INPUT 唯一标记回读 OUTPUT 含标记验证）→ close → 宽限到期进程 exit；② 独立实例 attach → close → 无人归 → 到期（1500ms+余量窗口内）进程 exit——D-14 三形态之宽限与取消的协议层证据"
    - "S6 断连重接同一 PTY（CORE-05 协议层等价物）：--writable 无认证实例 → attach → INPUT 写标记（'X=weshmark42\r' 后 'echo $X\r' 回读确认落账）+ 'echo S6PID=$$\r' 解析 pidPre（/S6PID=(\\d+)/ 正则——终端回显含键入命令原文，数字锚定只命中结果行）→ ws.close() 断开（注释：abrupt/正常断开在服务端同归 reader 终结 → detach；1006 触发面由 phase06-dom.mjs 合成事件覆盖，分工登记）→ 新 attach（dialHello 成功即共享进程模型下接回原 PTY 的结构性证据）→ 'echo S6PID=$$\r' 解析 pidPost == pidPre（**进程 ID 相等 = 同一进程强证据**——新 bash 进程必持不同 pid，review #4 吸收：shell 变量证据升级为进程 ID 主证据）+ 'echo $X\r' 输出含 weshmark42（shell 状态跨断连存活——次级佐证）→ 全程未收 EXIT 帧、新 Welcome 正常到达、断开期间服务端不退出（默认不开启断言的顺带锁定，D-14）"
    - "S7 真实断网栈/浏览器原生事件序列：skipped + reason（headless 硬约束豁免条款，CODEBUDDY.md；reason 指向 .planning/phases/06-session-lifecycle/06-UAT.md 人工清单——06-07 产出）"
  prohibitions:
    - statement: "spawn 实例 stdout 的链接/token 只作断言材料——任何 token 值进入 check detail/控制台输出/汇总行即违规（phase04.mjs:6-9 红线沿用，测试输出可能进 CI 日志）"
      status: resolved
      verification: judgment
    - statement: "宽限/退避类场景必须真实等待（时钟不 mock、不缩短服务端等待制造假绿）——超时上限只做护栏，断言窗与标称宽限的余量关系须在注释写明"
      status: resolved
      verification: judgment
  artifacts:
    - path: "web/uat/phase06.mjs"
      provides: "phase 6 协议层 UAT 七场景（EXIT 双端/信号死亡/--once/--exit-when-empty 立即与宽限/断连重接同一 PTY）+ 豁免记录"
      contains: "--exit-when-empty"
  key_links:
    - from: "web/uat/phase06.mjs (startWesh)"
      to: "cmd/wesh (真实二进制)"
      via: "spawn --bind 127.0.0.1 --port 0 + 场景 flag（--once/--exit-when-empty[=duration]/--credential/--writable）→ stdout 三行解析 + stderr 捕获 + child 'exit' 事件断言"
      pattern: "startWesh"
    - from: "web/uat/phase06.mjs (帧收集)"
      to: "internal/proto (EXIT 'X'=0x58)"
      via: "帧常量区加 EXIT=0x58（phase05.mjs:27-29 对齐位同款）→ exitOf 解码断言 exit_code/message"
      pattern: "0x58"
    - from: "web/uat/phase06.mjs (S6 重接)"
      to: "internal/server (共享进程模型)"
      via: "断开后服务端存活（默认不开启）→ 新 attach 同 PTY——shell PID 相等（主证据，review #4）+ 变量跨断连存活（次级佐证）"
      pattern: "S6PID|weshmark42"
---

<objective>
phase 6 协议层 UAT：phase06.mjs 七场景——EXIT 双端广播（ro/rw 同帧 + 帧序 + 进程退出码）、信号死亡、--once 全链（双点位 503 + 断开退出）、--exit-when-empty 立即与宽限、断连重接同一 PTY。

Purpose: headless 硬约束下本 phase 服务端行为的最终端到端证据（CODEBUDDY.md 分层测试策略层 1，phase02/03/04/05.mjs 先例）——把 06-01/06-02/06-04 三个 plan 的机制转成对真实二进制的全链断言。
Output: web/uat/phase06.mjs（新）七场景全绿（S7 豁免 skipped 记录）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-session-lifecycle/06-CONTEXT.md
@.planning/phases/06-session-lifecycle/06-RESEARCH.md
@.planning/phases/06-session-lifecycle/06-VALIDATION.md
@.planning/phases/06-session-lifecycle/06-01-SUMMARY.md
@.planning/phases/06-session-lifecycle/06-02-SUMMARY.md
@.planning/phases/06-session-lifecycle/06-04-SUMMARY.md
@web/uat/phase05.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase06.mjs 骨架（五件套复用 + EXIT 常量 + waitExit）+ S1/S2（EXIT 双端广播与信号死亡）</name>
  <files>web/uat/phase06.mjs</files>
  <read_first>
    - web/uat/phase05.mjs（全文 655 行——五件套逐字复用源：startWesh 68-93（stdout 三行解析 + stderr 捕获 + child 句柄）、dialHello 98-117、waitClose 119-122、check/skip 52-60、场景数组与汇总 640-655；帧常量区 27-29；S5 双点位 503 先例 380-407；红线注释 11-13）
    - web/uat/phase03.mjs（dialHelloTicket 凭据模式握手先例——S1/S3 的 Basic ticket 链路来源）
    - .planning/phases/06-session-lifecycle/06-RESEARCH.md（Code Examples——EXIT 断言形态 exitOf 与 --once 全链注释）
    - .planning/phases/06-session-lifecycle/06-PATTERNS.md（§12 复用件清单）
    - .planning/phases/06-session-lifecycle/06-01-SUMMARY.md（EXIT 帧契约落码形态——message 文案逐字）
  </read_first>
  <action>
    1. 新建 `web/uat/phase06.mjs`（Node ≥22 原生 WebSocket/fetch 零依赖；phase05.mjs 骨架逐字沿用）：
       - 文件头注释（phase05.mjs:1-17 同款位置）：覆盖范围（SESS-01/02/03 + CORE-05 协议面）、红线沿用（token 值永不进 check detail/控制台输出——phase04.mjs:6-9 纪律）、单次语义纪律更新（--once/--exit-when-empty 场景的服务端退出是特性不是回归——exit 事件为断言通道）；运行方式行 `node web/uat/phase06.mjs [wesh 二进制路径]`（默认 /tmp/wesh-uat/wesh）。
       - 帧常量区加 `EXIT = 0x58`（proto.go 'X' 对齐注释——phase05.mjs:27-29 对齐位同款）。
       - startWesh 按 phase05.mjs 形态复用（--bind 127.0.0.1 --port 0 + stdout 三行解析 + stderr 捕获缓冲 + child 句柄 + kill: SIGKILL）；新增 waitExit(child, timeoutMs) helper——child 'exit' 事件决议 {code, signal}（S1/S3/S4/S5 进程退出断言通道）。
       - exitOf(frames) helper（RESEARCH Code Examples 逐字形态）：JSON.parse(dec.decode(frames.find((f) => f[0] === EXIT).subarray(1)))。
       - 帧收集器 collectUntilClose(ws)：onmessage 累积二进制帧 + waitClose 决议 close {code, reason}——返回 {frames, close}（EXIT 帧序断言 = frames 最后一帧 [0]===EXIT 且 close.code===1000）。
    2. S1 EXIT 双端广播：spawn ['--writable', '--credential', UAT_CREDENTIAL, '--', 'bash', '--norc', '--noprofile'] → A 经 POST /api/attach（Basic）取 ticket 后 dialHelloTicket（rw）→ B 经 stdout 解析的 shareRO 链接 token POST body 取 ticket 后 dialHelloTicket（ro）→ A 发 INPUT 'exit 42\r' → 双端 collectUntilClose：断言 ① 末帧 EXIT 且 exit_code==42 且 message 逐字 == 'The process exited with code 42.'（06-01 服务端组文案唯一写口）；② 两端 EXIT 帧体逐字节一致（终结无权限语义——ro/rw 同帧）；③ close.code===1000（帧序：EXIT 先于 1000 到达）；④ waitExit(child) code===42。
    3. S2 信号死亡：spawn ['--', 'sh', '-c', 'sleep 1; kill -HUP $$']（无认证无写——ro 直连即可，sleep 保 attach 窗口）→ dialHello → collectUntilClose：EXIT exit_code===-1 且 message 含 'SIGHUP'（大写——Pitfall 3 显式映射断言；注释登记 'killed by signal hangup' 小写描述词为回归形态）且 close.code===1000。
  </action>
  <verify>
    <automated>go build -o /tmp/wesh-uat/wesh ./cmd/wesh && node web/uat/phase06.mjs</automated>
  </verify>
  <acceptance_criteria>
    - go build 退出 0；node web/uat/phase06.mjs 退出 0 且 S1/S2 全 pass
    - grep -c 'EXIT = 0x58\|EXIT=0x58' web/uat/phase06.mjs == 1（帧常量对齐位）
    - grep -c 'waitExit' web/uat/phase06.mjs >= 2（helper 定义 + S1 断言调用）
    - S1 断言面含：双端 exit_code==42 + message 逐字 + 帧体逐字节一致 + 帧序（EXIT 先于 1000）+ 进程 exit 42
    - S2 断言面含：exit_code==-1 + message 含大写 SIGHUP + close 1000
    - 脚本内 token 值不出现在任何 check detail 字面（人工核读 + 红线注释存在）
  </acceptance_criteria>
  <done>SESS-03 协议层终证：EXIT 双端同帧（ro/rw）+ 帧序先于 1000 + 退出码/信号死亡两形态 + 进程级退出码传递。S3-S7 由 Task 2 补齐。</done>
</task>

<task type="auto">
  <name>Task 2: S3-S7（--once 全链 / --exit-when-empty 立即与宽限 / 断连重接同一 PTY / 豁免记录）+ 汇总收口</name>
  <files>web/uat/phase06.mjs</files>
  <read_first>
    - web/uat/phase06.mjs（Task 1 产出——骨架与 S1/S2 形态，同文件追加）
    - web/uat/phase05.mjs（S5 双点位 503 先例 380-407 逐字可抄——/api/attach 早闸 + WS ③位）
    - .planning/phases/06-session-lifecycle/06-CONTEXT.md（D-12/D-13/D-14 逐字——S3-S5 断言语义源）
    - .planning/phases/06-session-lifecycle/06-02-SUMMARY.md（OQ1 门裁决值——S3/S4/S5 进程退出状态断言常量）
    - .planning/phases/06-session-lifecycle/06-VALIDATION.md（Wave 0 phase06.mjs 行 + Manual-Only Verifications 表——S7 豁免登记形态）
  </read_first>
  <action>
    1. S3 --once 全链：spawn ['--once', '--credential', UAT_CREDENTIAL, '--', 'bash', '--norc', '--noprofile'] → A dialHelloTicket（Basic）成功 → 第二客户端双点位拒绝（POST /api/attach Basic → 503 + WS 直连 Dial → 失败响应 503——phase05.mjs S5 双点位先例逐字形态；断言注释：503 = 既有 max-clients 计数路径，409 不复活，D-12）→ A ws.close(1000) → waitExit(child, 10s) 断言进程已退出且退出状态 == 06-02 Task 1 门裁决值（accept-255 → 255；注释：os.Exit(-1) Unix 截断——SIGHUP 致子进程信号死亡 ExitCode=-1 的进程级投影，OQ1 裁决）→ stderr 捕获缓冲无 panic/异常栈断言（strings 不含 'panic'）。
    2. S4 --exit-when-empty 立即：spawn ['--exit-when-empty', '--', 'bash', '--norc', '--noprofile']（裸 flag 不传值——IsBoolFlag 形态实证，D-14）→ **attach 前 400ms 守候窗断言进程无 exit 事件**（启动期注册表恒空不触发——Pitfall 2 的协议层显式证据，review 建议吸收；守候窗 ≪ 任何合理误触发时延）→ dialHello → close → waitExit 断言退出（退出状态同门裁决值）；对照注释：无 flag 实例断开不退出已由 S6 顺带锁定。
    3. S5 --exit-when-empty=1500ms 宽限（时序容差规格——review #6 吸收：标称 1500ms，取消窗口取 400ms ≪ 标称留 1100ms 调度余量；到期断言窗 5s 护栏轮询，禁精确时点断言）：
       - 子场景① 取消：spawn ['--exit-when-empty=1500ms', ...] → attach → close → 400ms 后再 attach 成功（宽限内 attach 取消退出）→ 新连接 echo 验证会话存活（INPUT 写唯一标记回读 OUTPUT 含标记——echo 形态显式化）→ close → waitExit 断言宽限到期后退出（5s 护栏）。
       - 子场景② 到期：独立实例 attach → close → 无人归 → waitExit 断言 5s 护栏内退出（到期 ≈1500ms）。
    4. S6 断连重接同一 PTY（CORE-05 协议层等价物；review #4 吸收——进程 ID 主证据）：spawn ['--writable', '--', 'bash', '--norc', '--noprofile']（无认证）→ dialHello → INPUT 写 'X=weshmark42\r' → 'echo $X\r' 回读确认输出含 weshmark42 → INPUT 'echo S6PID=$$\r' 以 /S6PID=(\d+)/ 正则解析 pidPre（注释：终端回显含键入命令原文 'echo S6PID=$$'——正则数字锚定只命中结果行）→ ws.close() → 500ms 后新 dialHello（服务端存活 = 默认不开启断开退出的顺带断言——D-14 默认值零漂移）→ 新连接 INPUT 'echo S6PID=$$\r' 解析 pidPost → 断言 pidPost == pidPre（进程 ID 相等 = 接回同一 PTY 进程的强证据——新 bash 进程必持不同 pid）→ 新连接 INPUT 'echo $X\r' → 输出含 weshmark42（shell 变量跨断连存活——次级佐证；注释：共享进程模型下重连 = 新 attach 同一 sess，无会话 ID 协商）→ 断言首连接全程未收 EXIT 帧（frames 无 0x58——子进程未退出无终结帧）。
    5. S7 豁免：skip(id, '浏览器原生断网/恢复事件序列与 tmux/herdr 屏幕重绘观感', 'headless 硬约束——真实断网栈任何自动化均不可测（CODEBUDDY.md 豁免条款）；协议层等价物 = S6；人工清单见 .planning/phases/06-session-lifecycle/06-UAT.md')。
    6. 输出自净断言（review #7 吸收——红线由注释纪律升级为运行时自证）：check/skip 包装收集全部已发 detail 入数组；汇总行前执行 assertOutputClean()——遍历 detail 数组断言不含 UAT_CREDENTIAL 值与 S1/S3 stdout 解析的两条 share token 值（含 '/s/' 链接形态串），命中即 FAIL（phase04.mjs:6-9 红线的可执行形态，防未来回归静默破线）。
    7. 汇总行与退出码（phase05.mjs 640-655 逐字形态：300ms 场景间隔 + 场景异常计数 + 汇总 + process.exit(failed===0?0:1)）；全脚本七场景全绿（S7 skipped 不计失败）后提交。
  </action>
  <verify>
    <automated>go build -o /tmp/wesh-uat/wesh ./cmd/wesh && node web/uat/phase06.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node web/uat/phase06.mjs 退出 0：S1-S6 全 pass + S7 以 skipped+reason 记录
    - S3 断言面含：双点位 503（/api/attach + WS 直连）+ A 断开后进程退出 + 退出状态按 OQ1 门裁决值 + stderr 无 panic
    - S4 断言面含：attach 前 400ms 守候窗进程无 exit 事件（启动期恒空不触发，review 吸收）+ close 后 waitExit 退出
    - S5 断言面含：宽限内再 attach 成功 + echo 存活（INPUT 标记回读 OUTPUT 含标记）+ 再次断开后到期退出（子场景①）与无人归到期退出（子场景②）——断言窗 5s 护栏轮询形态
    - S6 断言面含：断开期间服务端存活（再 attach 成功）+ pidPost==pidPre（echo S6PID=$$ 进程 ID 主证据，review #4 吸收）+ echo $X 含 weshmark42（次级佐证）+ 首连接无 EXIT 帧
    - grep -c "'--once'" web/uat/phase06.mjs >= 1 且 grep -c "'--exit-when-empty" web/uat/phase06.mjs >= 2（裸写与 =1500ms 两形态）
    - grep -c 'weshmark42' web/uat/phase06.mjs >= 2（写入与回读两断言点）且 grep -c 'S6PID' web/uat/phase06.mjs >= 2（pidPre/pidPost 两解析点）
    - grep -c 'assertOutputClean' web/uat/phase06.mjs >= 2（review #7——输出自净断言定义 + 汇总前调用）
    - grep -c 'skip(' web/uat/phase06.mjs >= 1（S7 豁免记录）
  </acceptance_criteria>
  <done>phase 6 协议层 UAT 全绿：SESS-01/02/03 与 CORE-05 协议面全部端到端锁定（七场景 + 一豁免记录）——phase 验收的协议层证据齐备。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

SESS-01/02/03 与 CORE-05 的 probe 状态（unclassified — review manually）已分别在 06-02/06-01/06-03 显式登记（flagged-unverified）；本 plan 承接四条的协议层实证面——06-VALIDATION.md Wave 0 phase06.mjs 行的全部能力点（--once 单接/断开退出、--exit-when-empty 全员断开退出、EXIT 帧含退出码与 SIGHUP 形态与 1000 关闭、重连接回原 PTY）逐一落为 S1-S6 断言。

## Review Dispositions (06-REVIEWS, OpenCode)

| 发现 | 处置 |
|------|------|
| HIGH #4：S6 同-PTY 证据偏弱（shell 变量断言），建议 /tmp marker 文件或进程 ID 证据 | 已吸收——主证据升级为 `echo S6PID=$$` 进程 ID 相等断言（pidPre/pidPost，正则数字锚定防回显误命中）；weshmark42 变量存活降为次级佐证（双证据互证） |
| HIGH #6：S5 时序敏感（400ms 宽限窗/再 attach 抖动/echo 验证形态未明示） | 已吸收——取消窗 400ms ≪ 标称 1500ms 的 1100ms 余量论证入注释；echo = INPUT 唯一标记回读 OUTPUT 含标记；到期断言 5s 护栏轮询，精确时点断言零残留 |
| MEDIUM #7：token 红线检查未实现 | 已吸收——Task 2 第 6 步 assertOutputClean() 运行时自净断言（detail 数组不含 UAT_CREDENTIAL/share token/'/s/' 链接形态串） |
| MEDIUM：S3 断言依赖 OQ1 裁决，需文档化影响 | 已覆盖——S3/S4/S5 退出状态断言随 06-02 Task 1 门裁决值参数化（既定形态），06-02 Task 1 acceptance 已登记本消费点 |
| Suggestion：S5 用 setTimeout/Atomics.wait 精确计时 | 部分吸收——setTimeout 真实等待既定（时钟不 mock 为 prohibition）；Atomics.wait 拒绝（主线程阻塞语义与异步 WS 驱动相斥；轮询+容差窗为既定形态） |
| LOW：waitExit 需 watchdog 防 wesh 挂死 | 已覆盖——waitExit(child, timeoutMs) helper 恒带超时（Task 1 规格既定），护栏到期即 FAIL 而非无限等待 |

## Artifacts this plan produces

- 新文件 `web/uat/phase06.mjs`：帧常量 `EXIT = 0x58`；helper `waitExit(child, timeoutMs)`/`exitOf(frames)`/`collectUntilClose(ws)`/`assertOutputClean()`（review #7）；场景 S1（EXIT 双端广播）/S2（信号死亡）/S3（--once 全链）/S4（--exit-when-empty 立即 + 启动守候断言）/S5（宽限取消与到期两子场景，容差窗形态）/S6（断连重接同一 PTY——PID 主证据 + 变量佐证）/S7（豁免 skip）

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试驱动→真实二进制 | UAT spawn 真实 wesh 实例全链断言——测试流量覆盖 06-01..06-04 全部新面 |
| 敏感值→测试输出 | 凭据/share token 经 stdout 解析为断言材料——输出面红线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06a | Information Disclosure | token/凭据值进测试输出 | high | mitigate | 红线注释沿用（phase04.mjs:6-9）：detail 只打状态码/布尔/形状/退出码/文案常量；链接与 token 只存闭包变量作断言材料；人工核读入 acceptance_criteria |
| T-06-06b | Denial of Service | 宽限/退出场景时序假绿或挂死 | medium | mitigate | 余量关系注释明示（标称 1500ms vs 400ms 取消窗 + 5s 护栏）；waitExit 全带超时；spawn 实例全部 SIGKILL 收口；挂死 = 护栏到期 FAIL 而非无限等待 |
| T-06-SC | Tampering | 依赖安装供应链 | low | accept | 零新依赖（Node 原生 WebSocket/fetch 零依赖脚本） |
</threat_model>

<verification>
- go build -o /tmp/wesh-uat/wesh ./cmd/wesh 退出 0
- node web/uat/phase06.mjs 退出 0（S1-S6 pass + S7 skipped 记录）
</verification>

<success_criteria>
- ROADMAP 成功准则 1：--once 单客户端断开退出 + --exit-when-empty 最后一端断开触发退出（立即/宽限/取消三形态）协议层全证
- ROADMAP 成功准则 2：子进程退出 → 全部在线客户端收 EXIT 帧（含退出码/信号人话）→ 1000 正常关闭——协议层全证
- ROADMAP 成功准则 3 服务端半侧：断开后新 attach 接回同一 PTY 进程、输入输出一致（shell 变量跨断连存活）
</success_criteria>

<output>
创建 `.planning/phases/06-session-lifecycle/06-06-SUMMARY.md`：记录七场景断言面与余量关系、OQ1 裁决值在进程退出状态断言的兑现、S6 同一进程证据形态、豁免登记、与 Go 测试层的分工（哪些断言只在协议层存在）。
</output>
