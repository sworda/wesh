---
phase: 14-herdr-uat
plan: 04
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - internal/server/resize_test.go
  - internal/server/resize_arb_test.go
  - internal/server/clients_test.go
autonomous: true
requirements: [PC-13]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "resize_test.go 三测双模式断言分叉表：shared 列=单端 last-wins/异尺寸 min-rect 仲裁（原断言逐字）；per-client 列=RESIZE 直通本会话 TIOCSWINSZ（[1,1000] 钳制与 50ms 防抖保留）+ 无 min-rect 约束 + 线上零 'W' 约束帧（蓝本 resize 行 + PC-05 语义）"
    - "resize_arb_test.go 一测落 D-03 形态：shared 列=仲裁器 min-rect 行为原断言逐字；per-client 列=显式断言仲裁器未装配——异尺寸双端各自尺寸直通、无 min-rect 收敛、线上零 'W' 帧，可证伪（有人误在 per-client 装配仲裁器时测试翻红），否决 t.Skip"
    - "clients_test.go 三测落 D-03 形态：owner 递补/写权限仲裁 shared 列原断言逐字；per-client 列=显式断言 owner 概念不装配（各端各自 rw/ro 门控归 PC-07 语义，无递补事件）——write-policy=owner × per-client 组合的 validateStartup warn 已在 Phase 10 落地，本列锁运行期未装配面"
    - "resize_test.go 自含装配（无装配族调用）——收编时按实况个别处理：统一经 newTestServer 装配为基准形态；若个别测的自含装配与 harness 语义不等价，保持自含并登记偏差（D-02 蓝本为准 + 偏差登记纪律）"
    - "shared 列全部期望值字面与改造前逐字一致（D-02 红线；git diff 自审白名单 = 三文件结构包裹与未装配断言新增）"
  artifacts:
    - path: "internal/server/resize_test.go"
      provides: "resize 直通/仲裁双模式断言分叉表"
      contains: "mode=per-client"
    - path: "internal/server/resize_arb_test.go"
      provides: "D-03 仲裁器未装配显式断言（per-client 分支）"
      contains: "mode=per-client"
    - path: "internal/server/clients_test.go"
      provides: "D-03 owner 递补未装配显式断言（per-client 分支）"
      contains: "mode=per-client"
  key_links:
    - from: "internal/server/resize_arb_test.go + clients_test.go"
      to: "internal/server/resize.go + internal/server/clients.go"
      via: "per-client 列断言「未装配」的可证伪面 = 这些组件在 per-client 分支不进入装配路径（运行期零 'W' 帧/无递补事件为 wire 级观测）"
      pattern: "arbiter|owner"
  prohibitions:
    - requirement_id: PC-13
      category: values
      statement: "mode-exclusive 测试的 per-client 分支禁止 t.Skip 或空分支——「不装配」必须做成可证伪断言（D-03 否决 t.Skip 的既定裁决：腐化无信号）；禁止断言放宽成「两模式都接受」"
---

<objective>
resize 直通分叉 + mode-exclusive 未装配断言批：resize_test（3 测，mode-mapped）双模式分叉表 + resize_arb/clients（4 测，shared-only）的 D-03「显式断言未装配」per-client 分支——三维归类第三维（mode-exclusive）的落地承载。

Purpose: 「不装配」若无断言承载，未来有人误在 per-client 装配仲裁器/owner 递补时无任何测试翻红——D-03 把组件缺席做成可证伪防线，这是三维归类相对 naive 参数化的核心增量。
Output: 三文件双模式改造 + 未装配断言可证伪形态 + -race 双模式逐名绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-herdr-uat/14-CONTEXT.md
@.planning/phases/14-herdr-uat/14-RESEARCH.md
@.planning/phases/14-herdr-uat/14-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: resize_test.go 三测双模式断言分叉表改造（D-01/D-02，自含装配个别处理）</name>
  <reversibility rating="reversible">测试文件改造，git 可回退；无公开契约面。</reversibility>
  <files>internal/server/resize_test.go</files>
  <read_first>
    - internal/server/resize_test.go 全文（3 测，0 处装配族调用——自含装配现状，逐测看清装配形态）
    - internal/server/harness_test.go（newTestServer 装配入口——收编基准形态）
    - internal/server/perclient_test.go（Phase 12 RESIZE 直通/防抖既有测——per-client 列断言对照面，防双写漂移）
    - internal/server/resize.go（arbiter 防抖与变化检测机制——shared 列事实源）
    - .planning/research/PITFALLS.md:378-399（蓝本 resize 行：「per-client=直通 + 防抖（P7），无 min-rect」）
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §Architecture Patterns 1（改造面实测盘点表 resize_test 行：「无装配族调用——自含装配，收编时个别看」）
  </read_first>
  <behavior>
    - shared 列：单端 last-wins、异尺寸 min-rect 收敛、2→1 恢复等原断言逐字
    - per-client 列：RESIZE 帧直通自身会话 TIOCSWINSZ（钳制 [1,1000] 与 50ms 防抖保留）、他端尺寸不受影响、线上无 'W' 约束帧
    - 装配形态：能经 newTestServer 表达者收编；自含装配语义不等价者保持自含并在测试体注释与 SUMMARY 双登记
    - go test -v 输出三测 mode=shared / mode=per-client 子测试双绿
  </behavior>
  <action>
    1. 逐测审读自含装配形态：判定能否直换 newTestServer(t, mode, argv, mutate)；可换者包裹 for-mode + t.Run 分叉表，不可换者保持自含装配但同样补 per-client 分支断言（注释载明「自含装配保持 + 偏差登记」）。
    2. per-client 列断言面：直通语义观测经同包 ptySize/pollSize 既有 helper（12-02 决策：creack/pty Getsize 即 TIOCSWINSZ 直读，零新代码）；他端不受影响经双端对照；零 'W' 帧经帧收集面类型字节扫描。
    3. 与 perclient_test.go 既有 Phase 12 直通测的边界：本列锁「分叉点」（直通 vs 仲裁），resize 风暴/防抖细节归 perclient_test.go 单一承载，不重复造同义断言。
    4. time go test -race -count=1 -v -run 'TestResize' ./internal/server/ 双模式全绿 → time go test -race -count=1 ./internal/server/ 全量回归；gofmt/vet 零输出；单提交收口（test(14-04) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v -run 'TestResize' ./internal/server/ && time go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - resize_test.go 三测均含 mode=shared / mode=per-client 子测试且 -v 输出全 PASS
    - per-client 列含直通/无 min-rect/零 'W' 帧断言（grep 可证观测面存在）
    - shared 列期望值字面与改造前逐字一致（git diff 自审：零期望值字面改动）
    - 自含装配保持的测（若有）含注释说明且登记进 SUMMARY
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
  </acceptance_criteria>
  <done>resize 三测双模式分叉表落地，直通 vs 仲裁分叉显式成表。</done>
</task>

<task type="auto">
  <name>Task 2: mode-exclusive 批——resize_arb_test + clients_test 的 D-03 显式断言未装配（D-03）</name>
  <reversibility rating="reversible">测试断言新增，git 可回退；无公开契约面。</reversibility>
  <files>internal/server/resize_arb_test.go, internal/server/clients_test.go</files>
  <read_first>
    - internal/server/resize_arb_test.go 全文（1 测 2 处 startTrackedServerWith 调用——仲裁器 min-rect 现状断言）
    - internal/server/clients_test.go 全文（3 测 0 处装配族调用——owner 递补/写权限仲裁现状）
    - internal/server/harness_test.go（newTestServer 装配入口）
    - internal/server/resize.go（arbiter 机制——「未装配」断言的对照本体）
    - internal/server/clients.go（owner 递补/WritePolicy 机制——「未装配」断言的对照本体）
    - .planning/phases/14-herdr-uat/14-CONTEXT.md D-03（显式断言未装配的既定形态：resize 直通无 min-rect 约束、线上零 'W' 帧；否决 t.Skip）
    - internal/server/perclient_test.go（Phase 12 ro 放行 RESIZE/直通既有测——per-client 分支断言的互补面）
  </read_first>
  <behavior>
    - resize_arb per-client 列：异尺寸两端（如 100x30 / 60x20）attach 后各自 RESIZE 直通生效（各自 TIOCSWINSZ 等于自报值），无 min-rect 收敛（两端尺寸不互相压缩），运行期帧流零 'W' 类型字节——任一反向出现即 FAIL（可证伪）
    - clients per-client 列：两端 attach 后无 owner 指派/递补事件面（各端权限由自身 ticket 决定，ro 门控归 PC-07）；owner 断开不触发任何递补行为（他端权限无变化）
    - shared 列：仲裁器/owner 递补原断言逐字
  </behavior>
  <action>
    1. 两文件套用 for-mode + t.Run + newTestServer 包裹形态；shared 列原断言逐字搬入。
    2. resize_arb per-client 列实现：双端异尺寸 attach（Hello 携不同 cols/rows），各自发 RESIZE 后经 ptySize/pollSize 观测各自会话 winsize == 各自自报值（钳制后），帧收集面断言零 'W' 类型字节；测试体注释载明 D-03「显式断言未装配——误装配仲裁器时本列翻红」。
    3. clients per-client 列实现：rw+rw 两端 attach，一端断开后断言他端权限/行为零变化（无升格/递补事件帧）；注释载明 D-03 与「write-policy=owner × per-client 启动期 warn 已在 Phase 10 落地，本列锁运行期未装配」。
    4. time go test -race -count=1 -v -run 'TestArbitrate|TestResizeArbitration|TestOwner|TestSuccession|TestWritePolicy' ./internal/server/（按实际测名调整）双模式全绿 → time go test -race -count=1 ./internal/server/ 全量回归；gofmt/vet 零输出；单提交收口（test(14-04) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v ./internal/server/ 2>&1 | grep -E '^(--- (PASS|FAIL)).*mode=' | head -40; time go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - resize_arb_test.go 与 clients_test.go 均含 mode=per-client 子测试且 -v 输出全 PASS（零 t.Skip——grep 两文件无 t.Skip 命中）
    - resize_arb per-client 列含零 'W' 帧断言与各自 winsize 直通断言（grep 可证）
    - clients per-client 列含 owner 断开后他端零变化断言（grep 可证）
    - shared 列期望值字面与改造前逐字一致（git diff 自审：零期望值字面改动）
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
  </acceptance_criteria>
  <done>D-03 显式断言未装配两文件落地：仲裁器/owner 递补在 per-client 的缺席成为可证伪防线；三维归类第三维收口。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试改造面→零回归证据链与装配漂移防线 | shared 列是 v1.0 仲裁语义证据本体；per-client 列是「组件不装配」的防腐防线——两面都怕字面漂移与空断言 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07 | Tampering | mode-exclusive 的 per-client 分支写成 t.Skip/空断言，未来误装配仲裁器时无信号（D-03 否决形态本体） | high | mitigate | 可证伪断言强制（零 'W' 帧/各自直通/无递补事件三观测面）+ 验收闸 grep 零 t.Skip + 14-12 终审 |
| T-14-08 | Tampering | resize 分叉表「和稀泥」（直通与仲裁两语义都被接受） | high | mitigate | 断言分叉表显式成表 + shared 列逐字未动 git diff 自审 + 14-12 收口闸 diff 白名单终审 |
| T-14-SC | Tampering | 依赖面（go.mod/go.sum/lockfile） | high | mitigate | 零新依赖红线——本 plan 无装包任务，legitimacy 门不触发；零 diff 由 14-12 收口闸断言 |
</threat_model>

<verification>
- time go test -race -count=1 -v ./internal/server/ 三文件 7 测双模式子测试逐名全绿
- grep 两文件零 t.Skip；per-client 列未装配断言三观测面在场
- git diff 自审：shared 列期望值逐字未动
</verification>

<success_criteria>
1. resize 三测直通 vs 仲裁分叉显式成表（PC-05 语义对照面）
2. resize_arb/clients 四测 per-client 分支未装配断言可证伪（D-03 否决 t.Skip 兑现）
3. shared 列逐字未动，-race 双模式全绿
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `internal/server/resize_test.go`：3 测改造为 `t.Run("mode=shared"/"mode=per-client")` 双跑 + resize 直通/仲裁断言分叉表
- `internal/server/resize_arb_test.go`：1 测改造为双跑 + per-client 分支仲裁器未装配显式断言（零 'W' 帧 + 各自直通）
- `internal/server/clients_test.go`：3 测改造为双跑 + per-client 分支 owner 递补未装配显式断言

<output>
Create `.planning/phases/14-herdr-uat/14-04-SUMMARY.md` when done
</output>
