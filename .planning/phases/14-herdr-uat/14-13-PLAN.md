---
phase: 14-herdr-uat
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-mermaid.mjs
  - scripts/package.json
  - scripts/pnpm-lock.yaml
  - docs/ARCHITECTURE.md
autonomous: false
requirements: [PC-12]
gap_closure: true
gap_ids: [G-14-34]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "node scripts/check-mermaid.mjs 对 docs/*.md 全部 mermaid 块（ARCHITECTURE×2 + TESTING×1）parse PASS 且 exit 0"
    - "修复前负对照成立：同一脚本对当前含词法错误的组件图输出块级 FAIL 且 exit 1（判别力自证，非恒绿）"
    - "docs/ARCHITECTURE.md 双模式拓扑图块（第二块）与两个围栏之外的正文零字节改动（git diff 审查）"
    - "用户在 GitHub 或 mermaid 渲染器目检组件图渲染正常：4 个 subgraph 框成形、边箭头完整、边标签完整显示、无错误占位框（G-14-34 truth 达成）"
  artifacts:
    - "scripts/check-mermaid.mjs + scripts/package.json + scripts/pnpm-lock.yaml（jsdom ^30.0.1 + mermaid ^11.17.2，dev 校验工具面，不进 CI/生产）"
    - "docs/ARCHITECTURE.md 组件图（第一块）：4 个 subgraph 改合法 id + 引号标题（CMDWESH/SRV/PTYLAYER/WEBPKG），带标签边统一引号形态"
  key_links:
    - "check-mermaid.mjs 的 jsdom 注入链（window/document/navigator → dynamic import mermaid → 逐块 parse）——Linux 禁浏览器约束下渲染合法性的 Node 端等价物（/tmp/mermaid-check/check.mjs 先例形态）"
    - "负对照（修复前 exit 1）→ 修复（exit 0）红绿链——载具判别力先于修复落地（14-08 D3 exit-code 门禁负对照先例）"
---

<objective>
闭合 UAT gap G-14-34：docs/ARCHITECTURE.md 组件图（第一个 mermaid 块，L11-56）mermaid 词法错误修复，使该图在 GitHub/渲染器正常渲染。

Purpose: UAT test 34（14-11 D3 递延的 mermaid 渲染目检项）用户报告组件图无法渲染。诊断（Node 端 mermaid.parse 11.17.2 实证）确认两类词法错误：(1) L15/L19/L29 三处 subgraph id 含非法字符 `/`（词法错误 Unrecognized text）；(2) L41 边标签 `GET / · /s/{token}/` 中 `{` 被解析为 DIAMOND_START 触发 parse error。第二块双模式拓扑图（L98-132）用规范形式 parse PASS 无问题。root_cause 含校验面漏检：14-11 的结构化校验只查结构完整性（节点数/边目标/subgraph-end 配对/围栏成对），不查 mermaid 词法合法性——本 plan 同时固化词法校验载具堵住该漏检面，防复发。

Output: 修复后的 docs/ARCHITECTURE.md（仅第一块内部行变动）+ scripts/check-mermaid.mjs 词法校验载具（含依赖声明与 lockfile）。

修复方案采用 UAT missing 字段钦定形态（已经 /tmp/mermaid-check/fix-check.mjs 草案 v2 预验证 parse PASS flowchart-v2），与第二块 L98 的 `subgraph SH["..."]` + `-->|"..."|` 规范风格统一。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-herdr-uat/14-UAT.md
@docs/ARCHITECTURE.md
</context>

<environment_constraints>
- Linux 开发机禁浏览器/playwright/X11（CODEBUDDY.md 双机拓扑硬约束）：mermaid 语法验证只能走 Node 端 mermaid.parse（jsdom 注入 window/document 后 dynamic import mermaid），**禁止 mmdc/mermaid-cli**（依赖 puppeteer/chromium）。
- 包管理用 pnpm（禁 npm）。
- 不进 CI：ci.yml 零 diff（14-12 收口闸已定，本 plan 不重开验证面；check-mermaid.mjs 为手动可重跑工具 + 本次复验载具）。
- /tmp/mermaid-check/（jsdom ^30.0.1 + mermaid ^11.17.2 + check.mjs/fix-check.mjs）是诊断阶段预验证环境——同机会话内可用作参照；若已被清理，本 plan 自包含全部改动清单，不依赖其存在。
</environment_constraints>

<tasks>

<task type="auto">
  <name>Task 1: 固化 mermaid 词法校验载具 scripts/check-mermaid.mjs（含负对照自证）</name>
  <files>scripts/check-mermaid.mjs, scripts/package.json, scripts/pnpm-lock.yaml</files>
  <action>
    收编 /tmp/mermaid-check 预验证形态为仓库内可重跑工具（root_cause 漏检面的防复发件——14-11 结构化校验不查词法，此后词法校验有了常驻载具）。

    1. 新建 scripts/package.json：private、type: module，dependencies 仅 mermaid ^11.17.2 + jsdom ^30.0.1（与 /tmp/mermaid-check 预验证环境逐字同版——依赖路线为 UAT 诊断/planning_context 钦定，非本 plan 新选型）。在 scripts/ 目录 pnpm install 产出 lockfile（根 .gitignore 的 node_modules/ 已覆盖 scripts/node_modules，无需新增忽略规则）。注意 scripts/release.sh 已在该目录——新工程文件与其并存互不干扰。

    2. 新建 scripts/check-mermaid.mjs，逐字沿用 /tmp/mermaid-check/check.mjs 的加载链（若在）：jsdom 构造 DOM 后注入 globalThis.window/document（navigator 缺省补齐），再 dynamic import('mermaid')——顺序不可颠倒（mermaid.parse 内部经 DOMPurify 需要 window/document，静态顶层 import 会先于注入求值而崩）。脚本契约：
       - 参数：argv 为 .md 文件列表（相对 cwd）；无参时默认扫描仓库 docs/ 目录全部 .md。
       - 提取正则 /```mermaid\n([\s\S]*?)```/g 逐块调用 mermaid.parse。
       - 输出：每块一行判定——文件名 + 块序号 + PASS/FAIL + diagramType；FAIL 行附加错误消息首行。零 mermaid 块的文件输出跳过说明，不计失败。
       - 退出码：任一块 FAIL → exit 1；全部 PASS（或无块）→ exit 0。
       - 扫描默认域 docs/*.md 当前共 3 块：ARCHITECTURE.md×2 + TESTING.md×1。

    3. 负对照自证（判别力证明，14-08 D3 exit-code 门禁负对照先例）：对当前未修复文档运行脚本，必须得到 ARCHITECTURE.md 块 1 FAIL（Lexical error / DIAMOND_START 类词法错误）+ 块 2 PASS + TESTING.md PASS + exit 1——证明脚本对坏输入真实报红，排除恒绿假阳实现。TESTING.md 单独跑一遍 exit 0 作正向冒烟。
  </action>
  <verify>
    <automated>cd scripts && pnpm install --silent; cd ..; node scripts/check-mermaid.mjs > /tmp/cm-neg.txt 2>&1; ec=$?; grep -q 'FAIL' /tmp/cm-neg.txt && grep -q 'PASS' /tmp/cm-neg.txt && test $ec -eq 1 && node scripts/check-mermaid.mjs docs/TESTING.md && echo NEGATIVE-CONTROL-OK</automated>
  </verify>
  <done>scripts/check-mermaid.mjs 对当前文档报 ARCHITECTURE 块 1 FAIL 且 exit 1（负对照成立）；对 docs/TESTING.md 全块 PASS exit 0；依赖与 lockfile 落库 scripts/（mermaid ^11.17.2 + jsdom ^30.0.1）。</done>
</task>

<task type="auto">
  <name>Task 2: 按预验证草案 v2 修复 ARCHITECTURE.md 组件图（红→绿）</name>
  <files>docs/ARCHITECTURE.md</files>
  <action>
    对组件图（第一个 mermaid 块，L11-56）执行 UAT missing 钦定、mermaid.parse 11.17.2 预验证 PASS 的修复（草案逐字真值源：/tmp/mermaid-check/fix-check.mjs 的 fixed 模板字符串，若该文件仍在可逐行比对）。全部改动如下（共 16 行）：

    一、4 处 subgraph 改合法 id + 引号标题（id 是 mermaid 内部标识，渲染显示文本来自引号标题串——语义零漂移；新 id 不与现有节点 id 冲突）：
    - L15 `subgraph cmd/wesh（CLI 装配层）` 改为 `subgraph CMDWESH["cmd/wesh（CLI 装配层）"]`
    - L19 `subgraph internal/server（网关层）` 改为 `subgraph SRV["internal/server（网关层）"]`
    - L29 `subgraph internal/pty（数据面）` 改为 `subgraph PTYLAYER["internal/pty（数据面）"]`
    - L33 `subgraph web（前端装配）` 改为 `subgraph WEBPKG["web（前端装配）"]`

    二、12 处带标签边统一加引号 `-->|"..."|`（与第二块 L98 双模式拓扑图风格统一；其中 L41 是 `{` 被 DIAMOND_START 误解析的主修复点，其余 11 处为风格统一+特殊字符免疫）：
    - L39 `CLI -->|spawn| PTY` → `CLI -->|"spawn"| PTY`
    - L40 `CLI -->|Options 装配| HTTP` → `CLI -->|"Options 装配"| HTTP`
    - L41 `FE -->|GET / · /s/{token}/| EMBED` → `FE -->|"GET / · /s/{token}/"| EMBED`
    - L42 `FE -->|POST /api/attach 换 ticket| HTTP` → `FE -->|"POST /api/attach 换 ticket"| HTTP`
    - L43 `FE -->|WS /ws（wesh.v1）| ATTACH` → `FE -->|"WS /ws（wesh.v1）"| ATTACH`
    - L45 `ATTACH -->|注册| HUB` → `ATTACH -->|"注册"| HUB`
    - L46 `PTY -->|ReadLoop 32KiB chunk| HUB` → `PTY -->|"ReadLoop 32KiB chunk"| HUB`
    - L47 `HUB -->|'0' OUTPUT 扇出| CLIENTS` → `HUB -->|"'0' OUTPUT 扇出"| CLIENTS`
    - L48 `CLIENTS -->|WS 下行| FE` → `CLIENTS -->|"WS 下行"| FE`
    - L49 `FE -->|'0' INPUT / '1' RESIZE| ATTACH` → `FE -->|"'0' INPUT / '1' RESIZE"| ATTACH`
    - L52 `INPUTQ -->|独占 Master.Write| PTY` → `INPUTQ -->|"独占 Master.Write"| PTY`
    - L53 `ARB -->|TIOCSWINSZ| PTY` → `ARB -->|"TIOCSWINSZ"| PTY`
    （L39-L53 区间内全部 12 条带标签边即清单全部——4 subgraph + 12 边标签共 16 行，与开头计数自洽。）

    三、零改动面（diff 审查基准）：
    - 块内全部节点声明行零改动（FE/CLI/HTTP/ATTACH/HUB/CLIENTS/INPUTQ/ARB/OBS/PTY/EMBED/CHILD 的 id 与方括号文本形态本就合法，含 CHILD 的 &lt;cmd&gt; HTML 实体）。
    - 5 条无标签边零改动（L44 `HTTP --> ATTACH`、L50 `ATTACH --> INPUTQ`、L51 `ATTACH --> ARB`、L54 `PTY --- CHILD`、L55 `HTTP --- OBS`）。
    - 第二块双模式拓扑图（L98-132）零改动。
    - 两个 ```mermaid 围栏与全部围栏外正文零改动。

    完成后运行 scripts/check-mermaid.mjs：3 块全 PASS（两块 flowchart-v2 diagramType + TESTING 1 块）exit 0——Task 1 的红在此转绿。verify 的零回归门按上述基准自动断言：git diff -U0 全部 hunk 的旧/新侧行域均落在 L11-56 且删/增行各恰 16——任何块外改动（含纯删除侧逃逸）即红。
  </action>
  <verify>
    <automated>node scripts/check-mermaid.mjs && git diff -U0 docs/ARCHITECTURE.md | awk '/^--- /{next} /^\+\+\+ /{next} /^-/{d++} /^\+/{a++} /^@@/{ if (match($0,/-[0-9]+(,[0-9]+)?/)) { split(substr($0,RSTART+1,RLENGTH-1),o,","); oc=(o[2]==""?1:o[2]+0); oe=(oc==0?o[1]:o[1]+oc-1); if(11>o[1]+0||oe>56) bad=1 } if (match($0,/\+[0-9]+(,[0-9]+)?/)) { split(substr($0,RSTART+1,RLENGTH-1),n,","); nc=(n[2]==""?1:n[2]+0); ne=(nc==0?n[1]:n[1]+nc-1); if(11>n[1]+0||ne>56) bad=1 } } END{ if(d!=16||a!=16||bad) exit 1 }' && echo FIXED-GREEN</automated>
  </verify>
  <done>node scripts/check-mermaid.mjs 全块 PASS exit 0（组件图 flowchart-v2）；git diff -U0 docs/ARCHITECTURE.md 删/增行各恰 16（4 subgraph + 12 边标签）且全部 hunk 旧/新侧行域落在 L11-56 组件图块内——无标签边/节点行/第二块/围栏外零出现于 diff（增删两侧对称断言，纯删除侧改动同样报红）。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 渲染目检人工门——GitHub/渲染器确认组件图正常渲染（UAT test 34 闭合）</name>
  <what-built>ARCHITECTURE.md 组件图 mermaid 词法修复：4 个 subgraph 合法 id + 引号标题、带标签边统一引号化（含 L41 `{token}` DIAMOND_START 主修复点）。Node 端 mermaid.parse 11.17.2 全部 3 块 PASS（词法合法性自动化等价物已绿）。</what-built>
  <how-to-verify>
    两条路径任选其一（Linux 开发机禁浏览器——目检在 Windows 工作站或任意有渲染器处进行）：
    1. GitHub 远端：将当前分支/提交 push 到远端，浏览器打开仓库 docs/ARCHITECTURE.md 渲染页。
    2. 本地渲染器：VS Code 装 Markdown Preview Mermaid Support 插件后预览 docs/ARCHITECTURE.md（或任意 mermaid 渲染器粘贴组件图块）。

    目检组件图（第一个图）四点：
    - 4 个 subgraph 框成形（cmd/wesh（CLI 装配层）/internal/server（网关层）/internal/pty（数据面）/web（前端装配）标题显示正常，含全角括号）
    - 边箭头完整，无断线/缺边
    - `GET / · /s/{token}/` 边标签完整显示（花括号不再触发解析错误）
    - 无 mermaid 语法错误占位（红框/Syntax error 提示）
    顺带确认第二块双模式拓扑图渲染如常（本就正常，防意外回归）。
  </how-to-verify>
  <resume-signal>输入 "approved"（或描述渲染问题及截图）</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 开发机 pnpm ↔ npm registry | scripts/ 局部安装 mermaid/jsdom（dev 校验工具面，不进生产二进制/CI/web 构建） |
| GitHub 渲染器 ↔ ARCHITECTURE.md 内容 | 公开架构文档渲染（无敏感数据——文档本为发布面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-SC | Tampering | pnpm install mermaid/jsdom（scripts/） | medium | mitigate | 依赖路线为 UAT 诊断/planning_context 钦定（非本 plan 新选型），且已于诊断阶段在用户环境实装实测（/tmp/mermaid-check 同版产出预验证结论）；版本钉死 ^11.17.2/^30.0.1 + lockfile 冻结 + 仅 scripts/ 局部安装 + 不进 CI/生产/web 构建（dev 工具面，最坏影响=校验脚本输出失真，Task 3 人工目检门兜底最终产出） |
| T-14-35 | Tampering | scripts/check-mermaid.mjs 恒绿假阳 | low | mitigate | Task 1 负对照自证：脚本对当前含词法错误的块必须 FAIL exit 1（14-08 D3 exit-code 门禁负对照先例）——try/catch 吞错或 parse 结果未判定的实现过不了负对照门 |
| T-14-36 | Information Disclosure | docs/ARCHITECTURE.md | low | accept | 公开架构文档（v1.0 起随仓库发布），修复仅动 mermaid 块内部语法形态，零新增敏感信息 |
</threat_model>

<verification>
1. **词法合法性（自动化）**：node scripts/check-mermaid.mjs → docs/ 三块全部 PASS（ARCHITECTURE×2 flowchart-v2 + TESTING×1），exit 0。
2. **判别力（负对照）**：修复前同脚本对块 1 报 FAIL exit 1（Task 1 已证；SUMMARY 记录负对照输出）。
3. **改动纯净性（diff 审查）**：git diff -U0 docs/ARCHITECTURE.md 删/增行各恰 16（4 subgraph + 12 带标签边）、全部 hunk 旧/新侧行域落在 L11-56（Task 2 verify 已自动断言）；第二块、无标签边、节点行、围栏外正文零字节改动；ci.yml 零 diff；web/ 与 internal/ 零 diff。
4. **渲染目检（人工门）**：Task 3 checkpoint approved——GitHub/渲染器中组件图 subgraph 成形、边完整、标签正常显示、无语法错误占位。
</verification>

<success_criteria>
- G-14-34 truth 达成：docs/ARCHITECTURE.md 中 mermaid 图在 GitHub/渲染器正常渲染（用户目检 approved）。
- 词法校验载具常驻：scripts/check-mermaid.mjs 可重跑（负对照判别力已自证），root_cause 指出的 14-11 结构化校验漏检面（不查词法）已被堵住——后续文档 mermaid 改动有复验通道。
- 零意外回归：第二块双模式拓扑图与全部围栏外正文逐字节不变；无 CI/生产面改动。
</success_criteria>

<output>
Create `.planning/phases/14-herdr-uat/14-13-SUMMARY.md` when done
</output>
