---
phase: 14-herdr-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/server/harness_test.go
  - internal/server/exit_test.go
  - internal/server/stopseq_test.go
  - internal/server/health_test.go
  - internal/server/slowclient_test.go
autonomous: true
requirements: [PC-13]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "newTestServer(t, mode, argv, mutate) 单一装配点落地于 internal/server/harness_test.go（package server_test）：shared 分支直传 startTrackedServerWith（opts 基线 {Writable: true} + mutate 覆写，waitHandlers 返回值丢弃保持薄包装零改动），per-client 分支直传 startPerClientServer（Cleanup 逐一 Kill+Close 追踪纪律保留），未知 mode 走 t.Fatalf——per D-01"
    - "t.Run 子测试名逐字为 \"mode=shared\" / \"mode=per-client\"（D-01 形态；模式常量值 server.SessionModeShared=\"shared\" / server.SessionModePerClient=\"per-client\"，clients.go:110-114）"
    - "exit_test.go EXIT 广播两测完成双模式分叉表改造：shared 列=全员广播（v1.0 期望值逐字未动），per-client 列=属主私有化 + 他端零感知（PC-04 语义）——tracer 薄片经 -race 双模式绿实证装配点可用"
    - "stopseq/health/slowclient 三文件完成同类改造：stopseq per-client 列=N 进程组各执行一遍 stop-signal 序列；health per-client 列=session_alive 恒 true（13-05 裁决口径）；slowclient per-client 列=无全局信用门、outbox 满即 1013（R-08 1:1 退化，PITFALLS 蓝本表行）"
    - "shared 列全部期望值字面与改造前逐字一致——禁止断言放宽成「两模式都接受」（D-02 红线；git diff 自审白名单 = 仅结构包裹与分叉表新增）"
    - "CI 结构零改动：.github/workflows/ci.yml 零 diff，go test -race -count=1 -v ./... 单 step 经 t.Run 天然双模式覆盖（D-01）"
  artifacts:
    - path: "internal/server/harness_test.go"
      provides: "newTestServer 单一装配点（两族收编）"
      contains: "func newTestServer("
    - path: "internal/server/exit_test.go"
      provides: "EXIT 广播双模式断言分叉表"
      contains: "mode=per-client"
    - path: "internal/server/slowclient_test.go"
      provides: "信用门 shared-only / per-client 满即踢分叉"
      contains: "mode=shared"
  key_links:
    - from: "internal/server/harness_test.go"
      to: "internal/server/e2e_test.go + internal/server/perclient_test.go"
      via: "newTestServer 两分支分别直传 startTrackedServerWith 与 startPerClientServer——收编而非改写，两族母本函数体零改动"
      pattern: "startTrackedServerWith|startPerClientServer"
  prohibitions:
    - requirement_id: PC-13
      category: values
      statement: "禁止断言放宽成「两模式都接受」——shared 列期望值被改写即零回归证据毁灭（PITFALLS P11 本体；本 phase 全部改造 plan 的共同红线）"
---

<objective>
D-01 执行机械落地 + phase tracer：新建 newTestServer 单一装配点（收编 shared/per-client 两装配族），并以最小 mode-mapped 文件批（exit/stopseq/health/slowclient，共 8 测）端到端验证「装配点 → t.Run 双跑 → 断言分叉表 → -race 双模式绿」全链——后续 5 个改造 plan 全部建立在该薄片实证之上。

Purpose: 三维归类改造是 40+ 文件面，先以最薄切片证明收编架构两族直传零偏差，再批量推开（tracer-first：架构死路在第一批提交暴露，而非第十批之后）。
Output: harness_test.go（新）+ 四文件双模式改造 + -race 双模式逐名绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-herdr-uat/14-CONTEXT.md
@.planning/phases/14-herdr-uat/14-RESEARCH.md
@.planning/phases/14-herdr-uat/14-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 【phase tracer】newTestServer 装配点（harness_test.go 新建）+ exit_test.go 双模式分叉表改造（D-01/D-02）</name>
  <reversibility rating="costly">D-01 执行机械（newTestServer + t.Run 双跑）被 40+ 文件改造与 CI 双模式绿锁定后，改执行机械需逐文件重写——CONTEXT 已评级 costly，确认门免除（非 one-way）。</reversibility>
  <files>internal/server/harness_test.go, internal/server/exit_test.go</files>
  <read_first>
    - internal/server/e2e_test.go:171-194（startTrackedServerWith shared 族母本逐字签名与装配序列——pty.Start 启动期 spawn + server.New(sess,...) + net.Listen + t.Cleanup{killServer} + waitHandlers）
    - internal/server/e2e_test.go:120-134（killServer 收口与泄漏级联减速教训注释——收编后必须保留的纪律）
    - internal/server/perclient_test.go:55-135（startPerClientServerWithSpawn:60 全形态与 startPerClientServer:114 薄包装——attach 期 spawn + spawned 逐一 Kill+Close Cleanup 追踪纪律 + 头注释 Pitfall 11 红线 :3-14）
    - internal/server/clients.go:110-114（SessionModeShared/SessionModePerClient 模式常量逐字值）
    - internal/server/exit_test.go 全文（2 测 3 处 startTrackedServerWith 调用——改造对象现状）
    - .planning/research/PITFALLS.md:255-275（Pitfall 11 三维归类蓝本本体——共享 harness + 断言分叉表 + 禁止断言放宽红线）
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §Architecture Patterns 1/2 与 §Code Examples（newTestServer 收编骨架与 mode-mapped 断言分叉表形态——蓝本已给出建议骨架，按落地实况微调）
  </read_first>
  <behavior>
    - newTestServer(t, "shared", argv, mutate) 返回的 wsURL 可用且行为与 startTrackedServerWith 直调等价（opts 基线 Writable: true，mutate 非 nil 时先覆写再装配）
    - newTestServer(t, "per-client", argv, mutate) 等价于 startPerClientServer 直调（Cleanup 追踪纪律保留，序列洪水测试不泄漏）
    - newTestServer(t, 未知 mode, ...) 立即 t.Fatalf
    - exit_test.go 改造后：go test -v 输出含 TestExit*/mode=shared 与 TestExit*/mode=per-client 子测试名且双绿
    - shared 子测试断言的期望值字面与改造前逐字一致；per-client 子测试断言 B 端零感知（无 EXIT 帧、连接保持）
  </behavior>
  <action>
    1. 新建 internal/server/harness_test.go（package server_test，imports 沿用 e2e_test.go:1-23 外部包形态）：实现 newTestServer(t *testing.T, mode string, argv []string, mutate func(*server.Options)) (exitCh chan int, wsURL string)，t.Helper() 开头；switch mode 两分支——shared：opts := server.Options{Writable: true}，mutate 非 nil 则调用，exitCh, wsURL, _ = startTrackedServerWith(t, argv, opts) 丢弃 waitHandlers 返回；per-client：直接 return startPerClientServer(t, argv, mutate)；default：t.Fatalf("unknown mode %q", mode)。函数头注释载 D-01 收编论证（两族不对称点：shared 启动期 spawn vs per-client attach 期 spawn，直传即保留两侧 Cleanup 纪律；蓝本 PITFALLS :264）。
    2. exit_test.go 两测改造为 for _, mode := range []string{server.SessionModeShared, server.SessionModePerClient} { t.Run("mode="+mode, func(t *testing.T) {...}) } 包裹形态，装配调用统一换 newTestServer(t, mode, argv, nil)（或保现状 mutate）；断言分叉点显式成表（map[mode]expected 形态）：shared 列 = 改造前原断言逐字搬入（EXIT 帧广播全员、1000 关闭、exit_code 值——v1.0 语义）；per-client 列 = 属主端收私有 EXIT + 1000，他端在静默窗口内零帧到达且连接保持（PC-04；静默窗口用 perclient_test.go:3-14 头注释纪律的 select + time.After 竞速形态，客户端 Read 永不带 per-read deadline）。
    3. 检查点备份纪律：改造前 git status 干净确认；本任务单提交收口（test(14-01) 前缀——本 phase 全为测试面改动）。
    4. time go test -race -count=1 -v -run 'TestExit' ./internal/server/ 双模式子测试逐名全绿 → time go test -race -count=1 ./internal/server/ 全量回归（其余文件未改造应原样绿——tracer 不破坏任何既有面）。
    5. gofmt（GOROOT gofmt，go1.26.3 现代 doc-comment 规则）与 go vet 零输出。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v -run 'TestExit' ./internal/server/ && time go test -race -count=1 ./internal/server/ && go vet ./... && "$(go env GOROOT)/bin/gofmt" -l internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - internal/server/harness_test.go 存在且含 func newTestServer( 恰一处；go test -v 输出含 mode=shared 与 mode=per-client 子测试行且全 PASS
    - exit_test.go 内含 t.Run("mode="+mode 包裹与断言分叉表；shared 列期望值字面与改造前逐字一致（git diff 自审：无期望值字面改动行）
    - startTrackedServerWith / startPerClientServer 两族母本函数体零改动（git diff 白名单 = harness_test.go 新增 + exit_test.go 改造两文件）
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
    - .github/workflows/ci.yml 零 diff（D-01 结构零改动）
  </acceptance_criteria>
  <done>装配点经 tracer 薄片实证：exit 两测双模式 -race 绿，shared 列逐字未动，全量回归零破坏；后续改造 plan 可直接复用 newTestServer。</done>
</task>

<task type="auto">
  <name>Task 2: 小型 mode-mapped 批改造——stopseq_test + health_test + slowclient_test（D-01/D-02，蓝本 PITFALLS :378-399 对应行）</name>
  <reversibility rating="reversible">测试文件改造，git 可回退；无公开契约面。</reversibility>
  <files>internal/server/stopseq_test.go, internal/server/health_test.go, internal/server/slowclient_test.go</files>
  <read_first>
    - internal/server/harness_test.go（Task 1 落地的 newTestServer——本批装配入口）
    - internal/server/stopseq_test.go 全文（2 测 3 调用——stop-signal 序列现状）
    - internal/server/health_test.go 全文（2 测 6 调用——session_alive 语义现状）
    - internal/server/slowclient_test.go 全文（2 测 2 调用——信用门/1013 现状；吸收带纪律注释为 PC-10 对照面）
    - internal/server/perclient_test.go（Phase 11/12 已落地的 per-client 对照断言——断开 SIGHUP/停读续读/dwell 1013 既有测，避免双写漂移：per-client 列断言与之一致或互补，不重复造同义断言）
    - .planning/research/PITFALLS.md:378-399（蓝本表行：shutdown/stopseq=「per-client=N 组信号+有界 join」、health/metrics=「session_active 语义分叉」、slowclient=「per-client 无信用门：满即踢（R-08 1:1 退化）」）
    - internal/server/metrics.go（13-05 落地的 session_active 模式分支与 healthz 恒 true 裁决口径——health per-client 列期望值的事实源）
  </read_first>
  <behavior>
    - stopseq per-client 列：断开/终结形态下信号序列发到该会话自身进程组（N=1 形态即每客户端各一遍）；shared 列原断言逐字
    - health per-client 列：/healthz session_alive 恒 true（13-05 裁决）；shared 列原断言逐字
    - slowclient per-client 列：无全局信用门（他端 PTY 读不暂停），outbox 写满以 1013 踢出；shared 列原断言逐字
    - 全部经 newTestServer 装配 + t.Run 双跑；shared 列期望值逐字未动
  </behavior>
  <action>
    1. 三文件逐文件套用 Task 1 同款包裹形态（for-mode + t.Run + newTestServer + 断言分叉表）。
    2. stopseq_test：per-client 列断言 detach/终结触发该会话进程组的 stop-signal 序列（信号形态沿用 11-04 勘误先例——信号夹具用 HUP/trap 形态）；与 perclient_test.go 既有断开 SIGHUP 测的边界：本列聚焦「序列」语义（stop-signal 可配 + 组语义），不断言 teardown 竞态面。
    3. health_test：per-client 列断言 /healthz 四字段中 session_alive 恒 true（含零会话形态——13-05「恒 true」裁决）；shared 列保持生命周期跟随语义原断言。
    4. slowclient_test：per-client 列断言停读客户端不触发全局信用门（对照端 PTY 输出持续到达）+ 持续过载以 1013 收尾； dwell/停读续读细节已在 perclient_test.go（Phase 12）覆盖，本列只锁「无信用门 + 满即踢」分叉点（蓝本 R-08 行）——发现蓝本归属与实测行为不符时按 D-02 登记偏差，不静默跟改。
    5. time go test -race -count=1 -v -run 'TestStopSeq|TestHealth|TestSlow|TestGlobalCredit|TestSlowConsumer' ./internal/server/（按实际测名调整模式）双模式全绿 → time go test -race -count=1 ./internal/server/ 全量回归；gofmt/vet 零输出；单提交收口（test(14-01) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v ./internal/server/ 2>&1 | grep -E '^(=== RUN|--- (PASS|FAIL)).*mode=' | head -60; time go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - 三文件均含 t.Run("mode="+mode 包裹且经 newTestServer 装配（grep 三文件各命中）
    - -v 输出可见三文件全部测试的 mode=shared / mode=per-client 子测试双 PASS
    - shared 列期望值字面与改造前逐字一致（git diff 自审：白名单 = 本任务三文件结构包裹与分叉表新增，零期望值字面改动）
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
  </acceptance_criteria>
  <done>小型 mode-mapped 批双模式绿，蓝本三行（stopseq/health/slowclient）归属落地；偏差（若有）已登记进 SUMMARY。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试改造面→零回归证据链 | shared 列期望值是 v1.0 行为契约的证据本体——改造期的任何字面漂移都是证据篡改面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | mode-mapped 断言改造被「和稀泥」成两模式都接受，shared 零回归证据毁灭（PITFALLS P11 本体） | high | mitigate | D-02 断言分叉表强制（mode → expected 显式表）+ shared 列逐字未动 git diff 自审 + 14-12 收口闸 diff 白名单逐行终审 |
| T-14-02 | Tampering | newTestServer 收编点薄包装引入装配行为偏差（两族不对称点：启动期 spawn vs attach 期 spawn） | medium | mitigate | 两分支直传零改写（Task 1 步 1）+ tracer 薄片双模式 -race 绿先证后推 + 全量回归零破坏断言 |
| T-14-SC | Tampering | 依赖面（go.mod/go.sum/lockfile） | high | mitigate | 零新依赖红线——本 plan 无装包任务，legitimacy 门不触发；零 diff 由 14-12 收口闸断言 |
</threat_model>

<verification>
- time go test -race -count=1 -v ./internal/server/ 双模式子测试逐名全绿（exit/stopseq/health/slowclient 四文件）
- time go test -race -count=1 ./... 全量零回归
- git diff 白名单审查：harness_test.go 新增 + 四文件结构包裹，两族母本与 ci.yml 零 diff，shared 列期望值逐字未动
</verification>

<success_criteria>
1. newTestServer 单一装配点落地且两族直传零改写（D-01）
2. 四文件 8 测双模式 -race 逐名绿，shared 列期望值逐字未动（D-02）
3. ci.yml 零改动，单 step 天然双模式覆盖（D-01）
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `internal/server/harness_test.go`（新文件）：`newTestServer(t, mode, argv, mutate)` 单一装配点函数
- `internal/server/exit_test.go`：两测改造为 `t.Run("mode=shared"/"mode=per-client")` 双跑 + EXIT 广播/私有化断言分叉表
- `internal/server/stopseq_test.go`：双跑 + stop-signal 序列 N 组分叉表
- `internal/server/health_test.go`：双跑 + session_alive 语义分叉表（per-client 恒 true）
- `internal/server/slowclient_test.go`：双跑 + 无信用门/满即 1013 分叉表

<output>
Create `.planning/phases/14-herdr-uat/14-01-SUMMARY.md` when done
</output>
