---
phase: 14-herdr-uat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/server/harness_test.go
  - internal/server/perclient_test.go
  - internal/server/exit_test.go
  - internal/server/stopseq_test.go
  - internal/server/health_test.go
  - internal/server/slowclient_test.go
  - internal/server/limits_test.go
autonomous: true
requirements: [PC-13]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "newTestServer 小族装配点落地于 internal/server/harness_test.go（package server_test，per D-01——二值签名无法承载在册特殊调用点，修订期实证扩展为小族）：newTestServer 二值普通形（shared 分支直传 startTestServerWith，opts 基线 {Writable: true} + mutate 覆写；per-client 分支直传 startPerClientServer，Cleanup 追踪纪律保留）；newTrackedTestServer 三值形带 waitHandlers（shared 直传 startTrackedServerWith；per-client 直传 startPerClientServerTrackedWithSpawn——perclient_test.go 新增姊妹变体补齐 per-client 族 handler 追踪形态，wg 包裹镜像 startTrackedServerWith :186-193）；newHandleTestServer 四值形带 waitHandlers+srv（shared 直传 startTrackedServerHandle；per-client 经姊妹变体弃 spawnedSessions）；newSessTestServer 三值形带 sessions 访问器（shared 直传 startResizeServer + 单例包装；per-client 直传 startPerClientServerWithSpawn 取 spawnedSessions）；未知 mode 一律 t.Fatalf"
    - "小族承载修订期实证的特殊调用点登记：waitHandlers 同步边面=limits_test.go:132 / auth_e2e_test.go:403 / proxy_e2e_test.go:46,218,281 / multi_test.go:756 / emptyexit_test.go:250；waitHandlers+srv 面=emptyexit_test.go:322；srv 直调面=health_test.go:251 / shutdown_test.go:107,137；sess PTY 读回面=resize_arb_test.go:111,144,190,248"
    - "t.Run 子测试名逐字为 \"mode=shared\" / \"mode=per-client\"（D-01 形态；模式常量值 server.SessionModeShared=\"shared\" / server.SessionModePerClient=\"per-client\"，clients.go:110-114）"
    - "exit_test.go EXIT 广播两测完成双模式分叉表改造：shared 列=全员广播（v1.0 期望值逐字未动），per-client 列=属主私有化 + 他端零感知（PC-04 语义）——tracer 薄片经 -race 双模式绿实证装配点可用"
    - "stopseq/health/slowclient/limits 四文件完成同类改造：stopseq per-client 列=N 进程组各执行一遍 stop-signal 序列；health per-client 列=session_alive 恒 true（13-05 裁决口径）且 :251 Shutdown 直调面收编至 newHandleTestServer；slowclient 的 TestSlowConsumerKick per-client 列=outbox 满即 1013（R-08 1:1 退化）、TestGlobalCredit per-client 列=D-03 显式断言信用门未装配（蓝本 shared-only 行）；limits 的 TestOversize1009（:132）经 newTrackedTestServer 双跑（stderr 同步边两列同构——tracked 形态的运行期首证）、TestReadLimitBoundary（:198 startRawCatServer pre-listen stty 形态）保持 shared 单跑并登记 D-02 偏差"
    - "shared 列全部期望值字面与改造前逐字一致——禁止断言放宽成「两模式都接受」（D-02 红线；git diff 自审白名单 = 仅结构包裹与分叉表新增）"
    - "CI 结构零改动：.github/workflows/ci.yml 零 diff，go test -race -count=1 -v ./... 单 step 经 t.Run 天然双模式覆盖（D-01）"
  artifacts:
    - path: "internal/server/harness_test.go"
      provides: "newTestServer 小族装配点（四形态：普通/tracked/handle/sess）"
      contains: "func newTestServer("
    - path: "internal/server/perclient_test.go"
      provides: "startPerClientServerTrackedWithSpawn 姊妹变体（per-client 族 handler 追踪补齐——既有 36 测零改动）"
      contains: "func startPerClientServerTrackedWithSpawn("
    - path: "internal/server/exit_test.go"
      provides: "EXIT 广播双模式断言分叉表"
      contains: "mode=per-client"
    - path: "internal/server/slowclient_test.go"
      provides: "信用门 shared-only D-03 列 / per-client 满即踢分叉"
      contains: "mode=shared"
    - path: "internal/server/limits_test.go"
      provides: "上限五测双跑（TestOversize1009 经 newTrackedTestServer）+ TestReadLimitBoundary 单跑偏差登记"
      contains: "mode=per-client"
  key_links:
    - from: "internal/server/harness_test.go"
      to: "internal/server/e2e_test.go + internal/server/perclient_test.go + internal/server/resize_arb_test.go"
      via: "小族四形态分别直传 startTestServerWith / startTrackedServerWith / startTrackedServerHandle / startResizeServer 与 startPerClientServer / startPerClientServerWithSpawn / startPerClientServerTrackedWithSpawn——收编而非改写，两族母本函数体零改动（唯一新增 = per-client tracked 姊妹变体）"
      pattern: "startTestServerWith|startTrackedServerWith|startTrackedServerHandle|startResizeServer|startPerClientServer"
  prohibitions:
    - requirement_id: PC-13
      category: values
      statement: "禁止断言放宽成「两模式都接受」——shared 列期望值被改写即零回归证据毁灭（PITFALLS P11 本体；本 phase 全部改造 plan 的共同红线）"
---

<objective>
D-01 执行机械落地 + phase tracer：新建 newTestServer 小族装配点（收编 shared/per-client 两装配族——修订期实证：二值签名无法承载 waitHandlers 同步边/srv 直调/sess 读回三类在册特殊调用点，扩展为四形态小族 + per-client 族补 tracked 姊妹变体），并以最小 mode-mapped 文件批（exit/stopseq/health/slowclient/limits，共 13 测）端到端验证「装配点 → t.Run 双跑 → 断言分叉表 → -race 双模式绿」全链——后续 5 个改造 plan 全部建立在该薄片实证之上。

Purpose: 三维归类改造是 30+ 文件面，先以最薄切片证明收编架构两族直传零偏差，再批量推开（tracer-first：架构死路在第一批提交暴露，而非第十批之后）；limits:132 的 stderr 同步边测使最险的新成员（per-client tracked 姊妹变体）在 tracer 期即获运行期实证，而非留到 14-05。
Output: harness_test.go（新，四形态小族）+ perclient_test.go（姊妹变体新增，既有 36 测零改动）+ 五文件双模式改造 + -race 双模式逐名绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-herdr-uat/14-CONTEXT.md
@.planning/phases/14-herdr-uat/14-RESEARCH.md
@.planning/phases/14-herdr-uat/14-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 【phase tracer】newTestServer 小族装配点（harness_test.go 新建）+ per-client tracked 姊妹变体 + exit_test.go 双模式分叉表改造（D-01/D-02）</name>
  <reversibility rating="costly">D-01 执行机械（newTestServer 小族 + t.Run 双跑）被 30+ 文件改造与 CI 双模式绿锁定后，改执行机械需逐文件重写——CONTEXT 已评级 costly，确认门免除（非 one-way）。</reversibility>
  <files>internal/server/harness_test.go, internal/server/perclient_test.go, internal/server/exit_test.go</files>
  <read_first>
    - internal/server/e2e_test.go:136-161（startTestServerWith/startTestServer 普通二值族母本——newTestServer shared 分支直传目标；pty.Start 启动期 spawn + server.New(sess,...) + net.Listen + t.Cleanup{killServer} + go http.Serve）
    - internal/server/e2e_test.go:164-221（startTrackedServerWith 三值母本 + startTrackedServerHandle 四值母本——newTrackedTestServer/newHandleTestServer shared 分支直传目标；wg 包裹 handler 的 happens-before 同步边纪律注释逐字核读）
    - internal/server/e2e_test.go:120-134（killServer 收口与泄漏级联减速教训注释——收编后必须保留的纪律）
    - internal/server/perclient_test.go:55-135（startPerClientServerWithSpawn:60 全形态与 startPerClientServer:114 薄包装——attach 期 spawn + spawned 逐一 Kill+Close Cleanup 追踪纪律 + 头注释 Pitfall 11 红线 :3-14；姊妹变体插点在 :106 后与 :108 注释之间）
    - internal/server/resize_arb_test.go:33-54（startResizeServer 本地 sess 变体母本——newSessTestServer shared 分支直传目标）
    - internal/server/clients.go:110-114（SessionModeShared/SessionModePerClient 模式常量逐字值）
    - internal/server/exit_test.go 全文（2 测 2 处 startTestServerWith 调用 :67/:116——改造对象现状）
    - 特殊调用点登记（修订期实证，小族容量依据）：limits_test.go:132 / auth_e2e_test.go:403 / proxy_e2e_test.go:46,218,281 / multi_test.go:756 / emptyexit_test.go:250（waitHandlers 同步边——stderr 捕获类）；emptyexit_test.go:322（waitHandlers+srv）；health_test.go:251 与 shutdown_test.go:107,137（srv 直调——startShutdownServerWith 本地变体）；resize_arb_test.go:111,144,190,248（sess 读回——startResizeServer 本地变体）
    - .planning/research/PITFALLS.md:255-275（Pitfall 11 三维归类蓝本本体——共享 harness + 断言分叉表 + 禁止断言放宽红线）
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §Architecture Patterns 1/2 与 §Code Examples（newTestServer 小族骨架与 mode-mapped 断言分叉表形态——修订后蓝本，按落地实况微调）
  </read_first>
  <behavior>
    - newTestServer(t, mode, argv, mutate) (exitCh, wsURL)：shared = startTestServerWith 直传（opts 基线 Writable: true，mutate 非 nil 时先覆写）；per-client = startPerClientServer 直传（Cleanup 追踪纪律保留）；未知 mode 立即 t.Fatalf
    - newTrackedTestServer(t, mode, argv, mutate) (exitCh, wsURL, waitHandlers)：shared = startTrackedServerWith 直传；per-client = startPerClientServerTrackedWithSpawn 直传后丢弃 srv/spawnedSessions——stderr 捕获类测（restore() 前需与 handler 内 logEvent 建立同步边）的双模式装配形态
    - newHandleTestServer(t, mode, argv, mutate) (exitCh, wsURL, waitHandlers, srv)：shared = startTrackedServerHandle 直传；per-client = startPerClientServerTrackedWithSpawn 直传后丢弃 spawnedSessions——Shutdown 直调面与白盒 srv 面的双模式装配形态（不需要同步边的调用点忽略 waitHandlers 即可，wg 惰性无害）
    - newSessTestServer(t, mode, argv, mutate) (exitCh, wsURL, sessions func() []*pty.Session)：shared = startResizeServer 直传 + 单例访问器包装（func() []*pty.Session { return []*pty.Session{sess} }）；per-client = startPerClientServerWithSpawn 直传取 spawnedSessions——PTY 读回面（ptySize/pollSize 消费）的双模式统一访问器形态；本 plan 期零消费方（首个消费方 = 14-04 resize_arb 四子测试），编译期在场 + 14-04 运行期首证
    - exit_test.go 改造后：go test -v 输出含 TestExit*/mode=shared 与 TestExit*/mode=per-client 子测试名且双绿
    - shared 子测试断言的期望值字面与改造前逐字一致；per-client 子测试断言 B 端零感知（无 EXIT 帧、连接保持）
  </behavior>
  <action>
    1. perclient_test.go 新增 startPerClientServerTrackedWithSpawn(t *testing.T, spawnFn func(cols, rows int, remoteUser string) (*pty.Session, error), mutate func(*server.Options)) (exitCh chan int, wsURL string, srv *server.Server, spawnedSessions func() []*pty.Session, waitHandlers func())：装配序列与 startPerClientServerWithSpawn 逐字同构，唯一差异 = http.Serve 的 handler 经 sync.WaitGroup 包裹（wg.Add(1)/defer wg.Done() 镜像 startTrackedServerWith :186-193 形态）并返回 wg.Wait——per-client 族补齐 handler 追踪（stderr 捕获类测双跑的结构前提）；函数头注释载明「startPerClientServerWithSpawn 的 handler 追踪姊妹变体（14-01 新增，同步边纪律镜像 startTrackedServerWith 注释）」。既有函数与 36 测零改动。
    2. 新建 internal/server/harness_test.go（package server_test，imports 沿用 e2e_test.go:1-23 外部包形态 + pty 包）：实现 behavior 四形态，均 t.Helper() 开头 + switch mode 两分支直传 + default t.Fatalf("unknown mode %q", mode)；per-client 分支默认 spawnFn 闭包逐字镜像 perclient_test.go:116-120（pty.StartWithSize + RemoteUser 赋值局部复制形态——母本零改动红线下的小段复写，注释载明镜像来源）。族头注释载 D-01 收编论证：两族不对称点（shared 启动期 spawn vs per-client attach 期 spawn）直传即保留两侧 Cleanup 纪律；四形态容量依据 = 修订期实证的特殊调用点登记（read_first 清单逐条）；蓝本 PITFALLS :264。
    3. exit_test.go 两测改造为 for _, mode := range []string{server.SessionModeShared, server.SessionModePerClient} { t.Run("mode="+mode, func(t *testing.T) {...}) } 包裹形态，装配调用统一换 newTestServer(t, mode, argv, nil)；断言分叉点显式成表（map[mode]expected 形态）：shared 列 = 改造前原断言逐字搬入（EXIT 帧广播全员、1000 关闭、exit_code 值——v1.0 语义）；per-client 列 = 属主端收私有 EXIT + 1000，他端在静默窗口内零帧到达且连接保持（PC-04；静默窗口用 perclient_test.go:3-14 头注释纪律的 select + time.After 竞速形态，客户端 Read 永不带 per-read deadline）。
    4. 检查点备份纪律：改造前 git status 干净确认；本任务单提交收口（test(14-01) 前缀——本 phase 全为测试面改动）。
    5. time go test -race -count=1 -v -run 'TestExit' ./internal/server/ 双模式子测试逐名全绿 → time go test -race -count=1 ./internal/server/ 全量回归（其余文件未改造应原样绿——tracer 不破坏任何既有面）。
    6. gofmt（GOROOT gofmt，go1.26.3 现代 doc-comment 规则）与 go vet 零输出。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v -run 'TestExit' ./internal/server/ && time go test -race -count=1 ./internal/server/ && go vet ./... && "$(go env GOROOT)/bin/gofmt" -l internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - internal/server/harness_test.go 存在且含 func newTestServer( / func newTrackedTestServer( / func newHandleTestServer( / func newSessTestServer( 各恰一处；perclient_test.go 含 func startPerClientServerTrackedWithSpawn( 恰一处
    - go test -v 输出含 mode=shared 与 mode=per-client 子测试行且全 PASS；exit_test.go 内含 t.Run("mode="+mode 包裹与断言分叉表
    - exit_test.go shared 列期望值字面与改造前逐字一致（git diff 自审：无期望值字面改动行）
    - startTestServerWith / startTrackedServerWith / startTrackedServerHandle / startPerClientServer / startPerClientServerWithSpawn / startResizeServer 母本函数体与既有 36 测零改动（git diff 白名单 = harness_test.go 新增 + perclient_test.go 姊妹变体新增 + exit_test.go 改造三文件）
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
    - .github/workflows/ci.yml 零 diff（D-01 结构零改动）
  </acceptance_criteria>
  <done>小族装配点经 tracer 薄片实证：exit 两测双模式 -race 绿，shared 列逐字未动，全量回归零破坏；tracked 形态由 Task 2 limits:132 运行期首证，sess 形态 14-04 首证（编译期在场）——后续改造 plan 可直接复用小族四形态。</done>
</task>

<task type="auto">
  <name>Task 2: 小型 mode-mapped 批改造——stopseq_test + health_test + slowclient_test + limits_test（D-01/D-02，蓝本 PITFALLS :378-399 对应行；limits 迁入使 tracked 形态 tracer 期获运行期实证）</name>
  <reversibility rating="reversible">测试文件改造，git 可回退；无公开契约面。</reversibility>
  <files>internal/server/stopseq_test.go, internal/server/health_test.go, internal/server/slowclient_test.go, internal/server/limits_test.go</files>
  <read_first>
    - internal/server/harness_test.go（Task 1 落地的小族四形态——本批装配入口）
    - internal/server/stopseq_test.go 全文（2 测 2 处普通调用 :79/:108——stop-signal 序列现状）
    - internal/server/health_test.go 全文（2 测 7 处调用：6 处普通 + :251 startShutdownServerWith 本地 srv 变体——TestHealthzDraining 的 Shutdown 直调面，收编至 newHandleTestServer）
    - internal/server/slowclient_test.go 全文（2 测 2 处普通调用 :162/:253——信用门/1013 现状；吸收带纪律注释为 PC-10 对照面；TestGlobalCredit:243 按蓝本 shared-only 行处理）
    - internal/server/limits_test.go 全文（5 测：3 处普通调用 + :132 startTrackedServerWith（TestOversize1009 stderr 捕获同步边——收编至 newTrackedTestServer）+ :198 startRawCatServer（TestReadLimitBoundary pre-listen stty raw 自含装配））
    - internal/server/perclient_test.go（Phase 11/12 已落地的 per-client 对照断言——断开 SIGHUP/停读续读/dwell 1013 既有测，避免双写漂移：per-client 列断言与之一致或互补，不重复造同义断言）
    - .planning/research/PITFALLS.md:378-399（蓝本表行：shutdown/stopseq=「per-client=N 组信号+有界 join」、health/metrics=「session_active 语义分叉」、slowclient=「per-client 无信用门：满即踢（R-08 1:1 退化）」、resize_arb/TestGlobalCredit/owner 递补=shared-only 行、handshake/limits/keepalive=mode-agnostic 同断言行）
    - internal/server/metrics.go（13-05 落地的 session_active 模式分支与 healthz 恒 true 裁决口径——health per-client 列期望值的事实源）
  </read_first>
  <behavior>
    - stopseq per-client 列：断开/终结形态下信号序列发到该会话自身进程组（N=1 形态即每客户端各一遍）；shared 列原断言逐字
    - health per-client 列：/healthz session_alive 恒 true（13-05 裁决）；shared 列原断言逐字；TestHealthzDraining 的 :251 装配收编至 newHandleTestServer（srv 直调 Shutdown 语义两列同构）
    - slowclient：TestSlowConsumerKick per-client 列=outbox 写满以 1013 踢出（无全局信用门，R-08 1:1 退化）；TestGlobalCredit per-client 列=D-03 显式断言未装配形态（停读端不触发全局信用门——对照端 PTY 输出持续到达，信用门机制在 per-client 不装配的可证伪防线）；shared 列原断言逐字
    - limits（蓝本 mode-agnostic 同断言行）：TestOversize1009 经 newTrackedTestServer 双跑（stderr 捕获同步边两列同构——waitHandlers 在 restore() 前调用形态保留）；TestFragmentedFlood1009/TestEmptyFragmentFloodResilience/TestPreHelloReadLimit 经 newTestServer 同断言双跑；TestReadLimitBoundary 保持 shared 单跑（startRawCatServer 的 pre-listen stty raw 装配与 per-client attach 期 spawn 结构性不等价——spawn 发生在 Listen 之后无 pre-listen 窗口，D-02 偏差登记）
    - 全部经小族装配 + t.Run 双跑；shared 列期望值逐字未动
  </behavior>
  <action>
    1. stopseq/health/slowclient 三文件逐文件套用 Task 1 同款包裹形态（for-mode + t.Run + newTestServer + 断言分叉表）；health_test.go:251 调用点换 newHandleTestServer(t, mode, argv, nil)（srv 保留，waitHandlers 忽略——注释载明 wg 惰性）。
    2. stopseq_test：per-client 列断言 detach/终结触发该会话进程组的 stop-signal 序列（信号形态沿用 11-04 勘误先例——信号夹具用 HUP/trap 形态）；与 perclient_test.go 既有断开 SIGHUP 测的边界：本列聚焦「序列」语义（stop-signal 可配 + 组语义），不断言 teardown 竞态面。
    3. health_test：per-client 列断言 /healthz 四字段中 session_alive 恒 true（含零会话形态——13-05「恒 true」裁决）；shared 列保持生命周期跟随语义原断言。
    4. slowclient_test：TestSlowConsumerKick per-client 列断言持续过载以 1013 收尾（dwell/停读续读细节已在 perclient_test.go Phase 12 覆盖，本列只锁「无信用门 + 满即踢」分叉点——蓝本 R-08 行）；TestGlobalCredit 按蓝本 shared-only 行走 D-03 形态：per-client 列断言一端停读期间对照端 PTY 输出持续到达（信用门不装配的可证伪断言），测试体注释载明蓝本行归属。
    5. limits_test：TestOversize1009 包裹双跑、装配换 newTrackedTestServer（waitHandlers 同步边两列保留——stderr 捕获断言两列同值）；TestFragmentedFlood1009/TestEmptyFragmentFloodResilience/TestPreHelloReadLimit 换 newTestServer 同断言双跑（洪水类测双跑后 CI 时长翻倍——保持原量不缩水，时长影响登记 SUMMARY）；TestReadLimitBoundary 保持 shared 单跑原样，文件头或测试体注释载明「pre-listen stty 装配结构性 shared-only——per-client attach 期 spawn 无 pre-listen 窗口，D-02 偏差登记（蓝本 limits 行=同断言双跑，本测除外）」并登记 SUMMARY。
    6. time go test -race -count=1 -v -run 'TestStopseq|TestStop|TestHealth|TestSlow|TestGlobalCredit|TestSlowConsumer|TestOversize|TestReadLimit|TestFragmented|TestEmptyFragment|TestPreHello' ./internal/server/（按实际测名调整模式）双模式全绿 → time go test -race -count=1 ./internal/server/ 全量回归；gofmt/vet 零输出；单提交收口（test(14-01) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v ./internal/server/ 2>&1 | grep -E '^(=== RUN|--- (PASS|FAIL)).*mode=' | head -80; time go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - 四文件均含 t.Run("mode="+mode 包裹且经小族装配（grep 四文件各命中；limits_test.go 的 TestReadLimitBoundary 为登记的 shared-only 例外）
    - health_test.go:251 原 startShutdownServerWith 调用点已收编至 newHandleTestServer（grep 可证）；limits_test.go:132 原 startTrackedServerWith 调用点已收编至 newTrackedTestServer（grep 可证）
    - -v 输出可见四文件全部双跑测试的 mode=shared / mode=per-client 子测试双 PASS
    - shared 列期望值字面与改造前逐字一致（git diff 自审：白名单 = 本任务四文件结构包裹与分叉表新增，零期望值字面改动）
    - TestReadLimitBoundary 单跑保持与 D-02 偏差登记注释在场（grep 可证）
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
  </acceptance_criteria>
  <done>小型 mode-mapped 批 + limits 上限批双模式绿，蓝本四行（stopseq/health/slowclient/limits）归属落地；newTracked/newHandle 形态获运行期首证；TestGlobalCredit D-03 列与 TestReadLimitBoundary 偏差登记完毕。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试改造面→零回归证据链 | shared 列期望值是 v1.0 行为契约的证据本体——改造期的任何字面漂移都是证据篡改面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | mode-mapped 断言改造被「和稀泥」成两模式都接受，shared 零回归证据毁灭（PITFALLS P11 本体） | high | mitigate | D-02 断言分叉表强制（mode → expected 显式表）+ shared 列逐字未动 git diff 自审 + 14-12 收口闸 diff 白名单逐行终审 |
| T-14-02 | Tampering | newTestServer 小族收编点薄包装引入装配行为偏差（两族不对称点：启动期 spawn vs attach 期 spawn；per-client tracked 姊妹变体为唯一新装配代码） | medium | mitigate | 四形态两分支直传零改写（Task 1 步 1/2）+ tracked 形态经 limits:132 stderr 同步边测 tracer 期运行期实证 + tracer 薄片双模式 -race 绿先证后推 + 全量回归零破坏断言 |
| T-14-SC | Tampering | 依赖面（go.mod/go.sum/lockfile） | high | mitigate | 零新依赖红线——本 plan 无装包任务，legitimacy 门不触发；零 diff 由 14-12 收口闸断言 |
</threat_model>

<verification>
- time go test -race -count=1 -v ./internal/server/ 双模式子测试逐名全绿（exit/stopseq/health/slowclient/limits 五文件）
- time go test -race -count=1 ./... 全量零回归
- git diff 白名单审查：harness_test.go 新增 + perclient_test.go 姊妹变体新增 + 五文件结构包裹，两族母本与 ci.yml 零 diff，shared 列期望值逐字未动
</verification>

<success_criteria>
1. newTestServer 小族装配点落地且两族直传零改写（D-01）；tracked/handle/sess 三特殊形态覆盖全部在册特殊调用点类别
2. 五文件 13 测双模式 -race 逐名绿，shared 列期望值逐字未动（D-02）；TestReadLimitBoundary 单跑偏差登记
3. ci.yml 零改动，单 step 天然双模式覆盖（D-01）
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `internal/server/harness_test.go`（新文件）：`newTestServer` / `newTrackedTestServer` / `newHandleTestServer` / `newSessTestServer` 小族装配点函数
- `internal/server/perclient_test.go`：`startPerClientServerTrackedWithSpawn` 姊妹变体新增（既有 36 测零改动）
- `internal/server/exit_test.go`：两测改造为 `t.Run("mode=shared"/"mode=per-client")` 双跑 + EXIT 广播/私有化断言分叉表
- `internal/server/stopseq_test.go`：双跑 + stop-signal 序列 N 组分叉表
- `internal/server/health_test.go`：双跑 + session_alive 语义分叉表（per-client 恒 true）+ Shutdown 直调面收编 newHandleTestServer
- `internal/server/slowclient_test.go`：双跑 + 满即 1013 分叉表 + TestGlobalCredit 信用门未装配 D-03 列
- `internal/server/limits_test.go`：上限四测双跑（TestOversize1009 经 newTrackedTestServer）+ TestReadLimitBoundary 单跑偏差登记注释

<output>
Create `.planning/phases/14-herdr-uat/14-01-SUMMARY.md` when done
</output>
