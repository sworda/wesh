---
phase: 14-herdr-uat
plan: 09
type: execute
wave: 2
depends_on: [14-08]
files_modified:
  - web/uat/pw/phase14-pw.mjs
autonomous: false
requirements: [PC-13]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "phase14-pw.mjs 落地 D-07 断言面：Windows 工作站 Playwright 双 tab（桌面视口 ~1600x1000 / 移动视口小屏双 context），经 TCP 转发器连接 Linux 侧 wesh（--session-mode=per-client --writable --insecure-http --credential 形态 + herdr --session wesh-uat-p14-pw-<ts> 独立命名会话）；断言以 xterm buffer 文本/光标位置为主 + 截图留档人工复核，像素视觉豁免（CODEBUDDY.md §5，skipped+reason 记录）"
    - "T0 per-client 模式自证：两 tab shell PID 不等（phase12-pw T1b :165-174 先例形态——假绿防线）"
    - "T1 核心观感：大屏 tab 的 herdr 面板边框列位置（.xterm-rows 行文本逐字比对）在小屏 tab attach 后逐字不变"
    - "T2 小屏 resize（视口变更模拟转屏/拖窗）后，大屏 tab 边框列位置仍逐字不变"
    - "cols/rows 以 stty 实测回读为准，不硬编码视口到终端行列的映射（RESEARCH A5；phase12-pw V0/V_UP/V_DOWN 阶梯先例已实证 fit 链路）"
    - "否决截图 diff 回归（herdr 渲染含时钟/状态闪烁面 flake 风险——D-07 既定）；截图仅留档人工复核"
    - "双机拓扑纪律：脚本在 Windows 侧运行，经 ssh 管理 Linux 侧 wesh 起停（lib/server.mjs 载具）；Linux 侧零浏览器零 playwright；断网模拟（若涉及）一律转发器 kill/restore"
    - "清理：browser.close() → fwd.stop() → stopWesh() → herdr session stop（Linux 侧经 ssh）——命名会话零残留（D-09）"
  artifacts:
    - path: "web/uat/pw/phase14-pw.mjs"
      provides: "herdr driving Windows 浏览器层观感断言（T0 自证/T1 attach 不压缩/T2 resize 不压缩）"
      contains: "phase14-pw"
  key_links:
    - from: "web/uat/pw/phase14-pw.mjs"
      to: "web/uat/phase14.mjs"
      via: "同一 driving scenario 的浏览器层对应——协议层翻转链（14-08 已绿）标定视口/时序形态，pw 层只承载观感断言（D-07 分工）"
      pattern: "wesh-uat-p14"
  prohibitions:
    - requirement_id: PC-13
      category: safety
      statement: "双机拓扑红线：Linux 开发机侧不得安装/运行浏览器或 playwright（headless 硬约束）；Windows 侧不得操作真实网卡/系统网络配置模拟断网（一律 TCP 转发器 kill/restore）"
    - requirement_id: PC-13
      category: safety
      statement: "不得触碰用户日常 herdr default 会话——pw 场景同样走 wesh-uat-* 命名会话 + 跑后 session stop 核验（D-09 零污染纪律）"
---

<objective>
PC-13 浏览器面收口：新建 web/uat/pw/phase14-pw.mjs——Windows 工作站 Playwright 双 tab 真实 Chromium 观感断言：移动端小屏 tab attach/resize 后，桌面端大屏 tab 的 herdr 面板边框列位置逐字不变（D-07）。脚本在 Linux 侧编写（仓库共享），执行在 Windows 侧（人工确认门）。

Purpose: 协议层（14-08）证明 wire 与 herdr API 面正确；pw 层证明真实浏览器渲染面下用户观感正确——v1.1 里程碑 driving scenario 的最终用户体验证据（phase06-pw 46/46 先例的双机全链形态）。
Output: phase14-pw.mjs + Windows 侧执行全绿 + 截图留档人工复核记录。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-herdr-uat/14-CONTEXT.md
@.planning/phases/14-herdr-uat/14-RESEARCH.md
@.planning/phases/14-herdr-uat/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase14-pw.mjs 编写——双 tab 观感断言三测（D-07/D-09，phase12-pw 载具全复用）</name>
  <reversibility rating="reversible">pw 脚本新增，git 可回退；herdr 命名会话一次性。</reversibility>
  <files>web/uat/pw/phase14-pw.mjs</files>
  <read_first>
    - web/uat/pw/phase12-pw.mjs 全文（resize 观感先例：:29-34 lib 载具 imports、:37-39 视口阶梯、:50-60 renderRows buffer 观测、:120-136 Check 收集 + 双机拓扑 startWesh/Forwarder、:165-174 T1b per-client 自证形态、:200-201 行文本断言、:280-286 收口 exit code）
    - web/uat/pw/lib/check.mjs / forwarder.mjs / server.mjs / browser.mjs（Check/sleep/Forwarder/TARGET_HOST/ssh/ensureRunSh/startWesh/stopWesh/launch/CRED/openSession/runCmd/waitTermText/panel——可复用面逐件核对）
    - web/uat/phase14.mjs（14-08 落地——driving 场景时序/视口/herdr 命名会话形态的协议层标定）
    - .planning/phases/14-herdr-uat/14-CONTEXT.md D-07（断言面与否决面）与 D-09（命名会话纪律）
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §Architecture Patterns 3.3（pw 层对应断言：边框列位置逐字不变）与 §Assumptions A5（视口选型/stty 回读）
  </read_first>
  <behavior>
    - 拓扑：Forwarder(PORT_BASE, TARGET_HOST, TARGET_PORT) + ensureRunSh + startWesh('--session-mode per-client --writable --insecure-http --credential ${CRED} -- <herdr 路径> --session wesh-uat-p14-pw-<ts>') + fwd.start + launch
    - T0：桌面 tab（context viewport 1600x1000 量级）openSession → shellPid；移动 tab（独立 context 小屏视口）openSession → shellPid；pidA≠pidB 落 check（模式自证）
    - T1：桌面 tab herdr 渲染稳定后（waitTermText 锚定面板边框特征行），抓取 .xterm-rows 目标行文本快照；移动 tab attach（openSession 完成 + 自身渲染稳定）后，桌面 tab 同序位行文本逐字一致
    - T2：移动 context 视口变更（如竖屏 390x700 → 横屏 700x390 阶梯）后，桌面 tab 同序位行文本仍逐字一致
    - 截图：T0/T1/T2 三时点 screenshots/p14-*.png 留档（像素视觉豁免——skipped+reason 记录于 Check 汇总）
    - 清理 finally：browser.close → fwd.stop → stopWesh → ssh 侧 herdr session stop wesh-uat-p14-pw-<ts> + session list 核验
  </behavior>
  <action>
    1. 新建 web/uat/pw/phase14-pw.mjs：imports 沿用 phase12-pw :29-34 四件套；Check 实例三件（P14-T0/T1/T2）+ mkdirSync('screenshots')。
    2. 拓扑装配按 behavior；wesh argv 中的 herdr 路径与会话名经 ssh 侧命令展开（herdr 在 Linux 侧——与 phase14.mjs 同机约定 ~/.local/bin/herdr，或用 which 解析）；命名会话带时间戳后缀保证唯一。
    3. 双 context 双 tab：browser.newContext({viewport: 桌面}) 与 browser.newContext({viewport: 移动小屏}) 各自 newPage + openSession；stty 回读（runCmd 'stty size' 类通道——lib 先例）记录实际 cols/rows 进 Check detail（A5 标定材料）。
    4. 行文本快照/比对通道：page.evaluate 读 .xterm-rows 子 div textContent（phase12-pw :200-201 形态）；目标行定位经 herdr 面板边框特征（waitTermText 正则锚定后取同序位行组）；比对为逐字 equal。
    5. T2 视口变更：移动 context 的 page.setViewportSize 阶梯变更，等待移动端自身重排稳定（waitTermText 或固定护栏轮询），再比对桌面 tab。
    6. 收口：截图三时点 → 清理序列 → Check 汇总 + process.exit（phase12-pw :280-286 形态）。
    7. Linux 侧静态核验：node --check web/uat/pw/phase14-pw.mjs 语法零错；脚本内无 playwright 安装动作（依赖 Windows 侧既有 1.62.1 钉版）；单提交收口（test(14-09) 前缀）。Windows 实跑归 Task 2 确认门。
  </action>
  <verify>
    <automated>node --check web/uat/pw/phase14-pw.mjs && grep -c 'screenshots/p14-' web/uat/pw/phase14-pw.mjs</automated>
  </verify>
  <acceptance_criteria>
    - web/uat/pw/phase14-pw.mjs 存在且 node --check 语法零错
    - 脚本含 T0 pid 不等自证 + T1/T2 行文本逐字比对三测（grep 可证断言行存在）
    - 拓扑为 Forwarder + ssh startWesh 形态（grep 可证）；herdr 会话名含 wesh-uat-p14-pw- 前缀；清理序列含 session stop
    - 无截图 diff 断言形态（grep 可证截图仅留档）；像素视觉豁免以 skipped+reason 形态记录
    - 脚本无任何 Linux 侧浏览器/playwright 安装或网卡操作动作（grep 自审）
  </acceptance_criteria>
  <done>phase14-pw.mjs 编写完成并通过静态核验；Windows 实跑待 Task 2 人工确认门。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Windows 工作站执行 phase14-pw.mjs + 截图人工复核（D-07 观感终验）</name>
  <what-built>web/uat/pw/phase14-pw.mjs（Task 1 编写并静态核验）：Windows Playwright 双 tab 真实 Chromium，经 TCP 转发器连接 Linux 侧 per-client 模式 wesh（herdr 命名会话），T0 per-client 自证（双 tab shell pid 不等）+ T1 小屏 tab attach 后大屏 tab herdr 面板边框列逐字不变 + T2 小屏 resize 后仍逐字不变 + 三时点截图留档。</what-built>
  <how-to-verify>
    1. Windows 工作站侧进入仓库 web/uat/pw/ 目录（依赖已装：playwright 1.62.1 钉版，Chromium 缓存就绪——CODEBUDDY.md 既定）。
    2. 执行：node web/uat/pw/phase14-pw.mjs（如需指定目标机参数，沿用 phase12-pw 同款环境变量/参数形态）。
    3. 预期：T0/T1/T2 全部 PASS，退出码 0；Linux 侧 herdr session list 无 wesh-uat-p14-pw-* 残留。
    4. 截图复核：打开 screenshots/p14-*.png 三时点截图，人工确认大屏 tab herdr 面板观感在小屏 attach/resize 前后一致（像素视觉豁免项——观感级确认即可）。
    5. 若 FAIL：先核 herdr --version（Linux 侧须为 0.8.100，D-09 版本钉定纪律）与 wesh 二进制新鲜度（go build -o /tmp/wesh-uat/wesh ./cmd/wesh 重构建后重跑）；仍 FAIL 则记录失败 check 行回报。
  </how-to-verify>
  <resume-signal>Type "approved"（全绿 + 截图复核通过）或描述失败 check 行与现象</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Windows 工作站→Linux 开发机 | pw 脚本经 ssh 管理 Linux 侧 wesh/herdr——凭据与命令通道面 |
| 浏览器→TCP 转发器→wesh | 真实 Chromium 全链——测试凭据（CRED）在页面/截图中的可见面 |
| pw 场景→用户日常 herdr 环境 | Linux 侧命名会话隔离（同 14-08 防线） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-18 | Tampering | 双机拓扑越界（Linux 侧装浏览器/Windows 侧动真实网卡） | high | mitigate | CODEBUDDY.md 硬约束 + 脚本 grep 自审（无安装/网卡动作）+ 转发器 kill/restore 先例形态 |
| T-14-19 | Information Disclosure | 测试凭据 CRED 泄入截图/日志 | low | accept | CRED 为一次性测试凭据（lib/browser.mjs 头注释纪律继承）；截图人工复核顺带覆盖 |
| T-14-20 | DoS | pw 场景 herdr 命名会话/Chromium 进程残留 | medium | mitigate | finally 清理序列（browser.close → fwd.stop → stopWesh → session stop → list 核验） |
| T-14-SC | Tampering | 依赖面（playwright 1.62.1 钉版既有，零新增） | high | mitigate | 零新依赖红线——复用 web/uat/pw 既有钉版依赖，无装包任务，legitimacy 门不触发；lockfile 零 diff 由 14-12 收口闸断言 |
</threat_model>

<verification>
- node --check web/uat/pw/phase14-pw.mjs 语法零错（Linux 侧静态闸）
- Windows 侧 node web/uat/pw/phase14-pw.mjs 全绿 + 退出码 0（Task 2 人工确认门）
- 截图三时点留档 + 人工复核记录；herdr session list 零残留
</verification>

<success_criteria>
1. T0/T1/T2 Windows 侧全绿：移动端 attach/resize 后桌面端观感逐字不变（PC-13 浏览器面）
2. 截图留档人工复核通过（像素视觉豁免按 CODEBUDDY §5 记录）
3. 双机拓扑零越界，命名会话零残留
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `web/uat/pw/phase14-pw.mjs`（新文件）：P14-T0/T1/T2 三 Check 实例与场景主流程；会话名前缀 `wesh-uat-p14-pw-`；截图产物 `screenshots/p14-*.png`（运行期产物）

<output>
Create `.planning/phases/14-herdr-uat/14-09-SUMMARY.md` when done
</output>
