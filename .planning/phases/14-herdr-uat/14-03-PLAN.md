---
phase: 14-herdr-uat
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - internal/server/emptyexit_test.go
  - internal/server/shutdown_test.go
  - internal/server/metrics_test.go
autonomous: true
requirements: [PC-13]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "emptyexit_test.go 九测归一：--once/--exit-when-empty 双模式断言分叉表——per-client 列=注册表空迁移第二终结源 terminate 路径（P1，Phase 13 已落地的 pcSupervisor/pcExitReq 语义），shared 列=原断言逐字（蓝本 emptyexit 行）"
    - "shutdown_test.go 六测归一：Shutdown N 进程组各一遍 stop-signal 序列 + 有界 join（P10）双模式分叉表，shared 列原断言逐字（蓝本 shutdown 行）"
    - "metrics_test.go 六测归一：session_active 语义分叉（shared=会话存活跟随生命周期 / per-client 按 13-05 裁决口径）+ series 清单双模式镜像断言（21 series 零身份 label 红线），shared 列原断言逐字（蓝本 health/metrics 行）"
    - "归一形态 = 既有 per-client 测（Phase 13 混入：emptyexit 2 处 startPerClientServer :415/:455 / shutdown 4 处 startPerClientServerWithSpawn :205/:265/:330/:387 / metrics 1 处 startPerClientServerWithSpawn :593）与 shared 测经 newTestServer 小族收编到同一测试体双 t.Run 分支，而非新写或删除重造——既有断言证据链保持（D-01/D-02，RESEARCH §Pattern 2 先例形态）"
    - "per-client-only 测（无 shared 对照面，如 spawn_throttled/spawn_failed 事件面）保持单模式原样不动——它们本身就是 per-client 断言本体（蓝本「新增 per-client-only」行）；保持不动的事实登记进 SUMMARY 偏差段（RESEARCH Open Question 2 裁决：默认不动）"
    - "shared 列全部期望值字面与改造前逐字一致（D-02 红线；git diff 自审白名单 = 三文件结构归一与分叉表新增）"
  artifacts:
    - path: "internal/server/emptyexit_test.go"
      provides: "--once/--exit-when-empty 双模式断言分叉表（第二终结源列）"
      contains: "mode=per-client"
    - path: "internal/server/shutdown_test.go"
      provides: "Shutdown N 组信号 + 有界 join 双模式断言分叉表"
      contains: "mode=shared"
    - path: "internal/server/metrics_test.go"
      provides: "session_active 语义与 series 镜像双模式断言分叉表"
      contains: "mode=per-client"
  key_links:
    - from: "internal/server/emptyexit_test.go + shutdown_test.go + metrics_test.go"
      to: "internal/server/harness_test.go"
      via: "shared/per-client 两族装配调用归一至 newTestServer 双 t.Run 分支——Phase 13 混入形态的结构归一"
      pattern: "newTestServer"
  prohibitions:
    - requirement_id: PC-13
      category: values
      statement: "归一不得删除或改写既有 per-client 测的断言证据（Phase 13 落地的事件/退出码/series 断言链）——结构归一是搬运不是削弱；禁止断言放宽成「两模式都接受」"
---

<objective>
mode-mapped 终结/观测批：emptyexit（9 测）/ shutdown（6 测）/ metrics（6 测）三文件的双模式归一——这三文件在 Phase 13 已混入 per-client 测（两装配族并存），本 plan 把它们收编为 newTestServer 单一装配 + 双 t.Run 分叉表的统一形态（D-01 执行机械的最终形态）。

Purpose: 两装配族并存的混合形态是 D-01 要消除的散点结构；归一后「同一需求两模式断言分叉」在单一测试体内显式可读，shared 列继续充当零回归证据。
Output: 三文件归一改造 + -race 双模式逐名绿 + 偏差登记（per-client-only 保持不动清单）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-herdr-uat/14-CONTEXT.md
@.planning/phases/14-herdr-uat/14-RESEARCH.md
@.planning/phases/14-herdr-uat/14-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: emptyexit_test.go 九测双模式归一（D-01/D-02，第二终结源列）</name>
  <reversibility rating="reversible">测试文件归一改造，git 可回退；无公开契约面。</reversibility>
  <files>internal/server/emptyexit_test.go</files>
  <read_first>
    - internal/server/emptyexit_test.go 全文（9 测 9 处装配调用：5 处 startTestServerWith 普通 :42/:69/:134/:180/:211 + 1 处 startTrackedServerWith :250（TestExitWhenEmptyTimerAfterLifecycle stderr 同步边——收编至 newTrackedTestServer）+ 1 处 startTrackedServerHandle :322（TestExitWhenEmptyPromoteKickOnce waitHandlers+srv——收编至 newHandleTestServer）+ 2 处 startPerClientServer :415/:455（Phase 13 混入现状））
    - internal/server/harness_test.go（newTestServer 小族装配入口）
    - .planning/research/PITFALLS.md:378-399（蓝本 emptyexit 行：「per-client 退出路径=空迁移 terminate（P1）」）与 :340-349 Looks-Done 清单 exit-when-empty 行（宽限到期形态须验证）
    - internal/server/perclient.go（pcSupervisor/pcExitReq 第二终结源机制——per-client 列期望值的事实源）
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §Architecture Patterns 1/2
  </read_first>
  <behavior>
    - --once 分叉：shared 列=唯一客户端断开后服务端退出（原断言逐字）；per-client 列=同触发条件，终结目标为该客户端进程组 + 退出状态 255 对齐
    - --exit-when-empty 分叉：含「注册表已空且无子进程可等」形态第二终结源生效（不会永不退出）——per-client 列既有 Phase 13 测归位；宽限到期形态两模式各自断言（Looks-Done 清单行）
    - 既有 per-client-only 测（无 shared 对照面者）保持单模式原样，清单登记 SUMMARY
    - go test -v 输出归一测试含 mode=shared / mode=per-client 子测试且双绿
  </behavior>
  <action>
    1. 逐测判定归属：同需求双模式者归一为 for-mode + t.Run + 小族装配双跑分叉表——普通调用点（:42/:69/:134/:180/:211）换 newTestServer，:250 换 newTrackedTestServer（waitHandlers 同步边保留），:322 换 newHandleTestServer（waitHandlers+srv 全保留）；per-client-only 者保持原样（测试体不动，仅文件级注释或 SUMMARY 登记归属判定）。
    2. 归一时 shared 列 = 原 shared 测断言逐字搬入；per-client 列 = 原 Phase 13 per-client 测断言逐字搬入——归一是结构搬运，两列期望值字面均不改写。
    3. 宽限到期形态（grace>0 + 无重连 → 退 255）按 Looks-Done 清单确认两模式各有断言覆盖；缺漏按蓝本补一行断言（注释载明出处），无法补齐者登记偏差。
    4. time go test -race -count=1 -v -run 'TestOnce|TestExitWhenEmpty|TestEmpty|TestGrace' ./internal/server/（按实际测名调整）双模式全绿 → time go test -race -count=1 ./internal/server/ 全量回归；gofmt/vet 零输出；单提交收口（test(14-03) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v ./internal/server/ 2>&1 | grep -E '--- (PASS|FAIL): .*mode=' | head -60; time go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - emptyexit_test.go 归一测试含 t.Run("mode="+mode 包裹且经小族装配；:250 经 newTrackedTestServer、:322 经 newHandleTestServer（grep 可证）；per-client-only 测清单与「保持不动」判定登记进 SUMMARY
    - -v 输出归一测试 mode=shared / mode=per-client 子测试全 PASS（子测试 PASS 行 4 空格缩进——grep 模式不带 ^ 锚）
    - 两列期望值字面与归一前各自原测逐字一致（git diff 自审：零期望值字面改动）
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
  </acceptance_criteria>
  <done>--once/--exit-when-empty 双模式归一落地，第二终结源列显式成表；per-client-only 保持面登记完毕。</done>
</task>

<task type="auto">
  <name>Task 2: shutdown_test.go 六测双模式归一（D-01/D-02，N 组信号 + 有界 join 列）</name>
  <reversibility rating="reversible">测试文件归一改造，git 可回退；无公开契约面。</reversibility>
  <files>internal/server/shutdown_test.go</files>
  <read_first>
    - internal/server/shutdown_test.go 全文（6 测 6 处装配调用：2 处 startShutdownServerWith 本地 srv 变体 :107/:137（TestShutdown1001/TestShutdownStopTimeout——收编至 newHandleTestServer，本批为该本地 helper 的最后调用点）+ 4 处 startPerClientServerWithSpawn :205/:265/:330/:387（Phase 13 混入现状））
    - internal/server/harness_test.go（newTestServer 小族装配入口）
    - .planning/research/PITFALLS.md:378-399（蓝本 shutdown 行：「per-client=N 组信号 + 有界 join（P10）」）与 :340-349 Looks-Done 清单 Shutdown 行（3 客户端 Shutdown 后全部 pgid ESRCH 且 session_end 事件数=N）
    - internal/server/server.go（Shutdown 快照逐组信号与 join 机制——per-client 列事实源）
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §Architecture Patterns 1/2
  </read_first>
  <behavior>
    - Shutdown 分叉：shared 列=1001 广播 + 单进程组信号序列（原断言逐字）；per-client 列=全部存活进程组各执行一遍 stop-signal 序列 + 有界 join + session_end 事件数==N（Phase 13 既有断言归位）
    - 断开未收割残留形态（Shutdown 侧 D-state 兜底 terminate，13-04 落地）所在测按归属归一或保持 per-client-only
    - go test -v 输出归一测试双模式子测试双绿
  </behavior>
  <action>
    1. 同 Task 1 归一手法：逐测判定归属，双模式者 for-mode + t.Run + 小族装配归一——:107/:137 两处 startShutdownServerWith 调用点换 newHandleTestServer（srv 保留直调 Shutdown，waitHandlers 忽略，注释载明 wg 惰性），两列断言逐字搬入；per-client-only 保持原样并登记。
    2. startShutdownServerWith 本地 helper 删除（:107/:137 为其最后调用点——14-01 已迁出 health_test.go:251；删除后全仓零引用，D-01 散点消除）；文件头相关登记注释同步更新。
    3. 多客户端 Shutdown 形态（N 组信号 + session_end 数==N）确认在 per-client 列有断言承载（Phase 13 既有）；shared 列对应测保持单组语义原断言。
    4. time go test -race -count=1 -v -run 'TestShutdown|TestGraceful|Test1001|TestDrain|TestPerClientShutdown' ./internal/server/（按实际测名调整）双模式全绿 → time go test -race -count=1 ./internal/server/ 全量回归；gofmt/vet 零输出；单提交收口（test(14-03) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v ./internal/server/ 2>&1 | grep -E '--- (PASS|FAIL): .*mode=' | head -60; time go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - shutdown_test.go 归一测试含 t.Run("mode="+mode 包裹且经小族装配；:107/:137 经 newHandleTestServer（grep 可证）
    - startShutdownServerWith 本地 helper 已删除且全仓零引用（grep -rn 零命中）
    - -v 输出归一测试 mode=shared / mode=per-client 子测试全 PASS
    - 两列期望值字面与归一前各自原测逐字一致（git diff 自审：零期望值字面改动）
    - per-client 列含 N 组信号 + session_end 事件数==N 断言（grep 可证）
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
  </acceptance_criteria>
  <done>Shutdown 双模式归一落地，N 组信号 + 有界 join 列显式成表。</done>
</task>

<task type="auto">
  <name>Task 3: metrics_test.go 六测双模式归一（D-01/D-02，session_active 语义 + series 镜像列）</name>
  <reversibility rating="reversible">测试文件归一改造，git 可回退；无公开契约面。</reversibility>
  <files>internal/server/metrics_test.go</files>
  <read_first>
    - internal/server/metrics_test.go 全文（6 测 16 处装配调用：15 处 startTestServerWith 普通 + 1 处 startPerClientServerWithSpawn :593（TestMetricsPerClient——Phase 13 混入现状））
    - internal/server/harness_test.go（newTestServer 小族装配入口）
    - internal/server/metrics.go（21 series 契约与 session_active 模式分支、HELP 双模式文案——per-client 列事实源，13-05 落地）
    - .planning/research/PITFALLS.md:378-399（蓝本 health/metrics 行：「session_active 语义 + series 清单扩展（P10）」）与 :340-349 Looks-Done 清单观测面行（series 镜像、零身份 label 红线、client_id 关联键）
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §Architecture Patterns 1/2
  </read_first>
  <behavior>
    - session_active 分叉：shared 列=跟随会话生命周期（原断言逐字）；per-client 列=13-05 裁决口径（模式分支语义）
    - series 镜像：两模式各自断言 /metrics exposition 含该模式应见的 series 集合（17 shared / 21 per-client 含 spawn 四计数器——按 metrics.go 现状核定），零身份 label 红线两模式同断言
    - HELP 文案双模式分叉按现状归位；go test -v 输出归一测试双绿
  </behavior>
  <action>
    1. 同归一手法：逐测判定归属，双模式者 for-mode + t.Run + newTestServer 归一，两列断言逐字搬入；per-client-only 保持原样并登记。
    2. series 清单断言以 metrics.go 现状为准核定两列期望值（镜像测既有形态沿用）；零身份 label 红线断言保持两模式同值。
    3. time go test -race -count=1 -v -run 'TestMetrics|TestSessionActive|TestSeries|TestSpawn' ./internal/server/（按实际测名调整）双模式全绿 → time go test -race -count=1 ./internal/server/ 全量回归；gofmt/vet 零输出；单提交收口（test(14-03) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v ./internal/server/ 2>&1 | grep -E '--- (PASS|FAIL): .*mode=' | head -60; time go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - metrics_test.go 归一测试含 t.Run("mode="+mode 包裹且经小族装配
    - -v 输出归一测试 mode=shared / mode=per-client 子测试全 PASS
    - 两列期望值字面与归一前各自原测逐字一致（git diff 自审：零期望值字面改动）
    - series 清单两模式分叉断言与 metrics.go 现状一致；零身份 label 红线断言在场
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
  </acceptance_criteria>
  <done>metrics 双模式归一落地，session_active 语义与 series 镜像列显式成表；三文件归一批收口。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 归一改造面→Phase 13 既有证据链 | emptyexit/shutdown/metrics 的 per-client 断言是 Phase 13 需求的收口证据——归一搬运中的字面漂移即证据篡改面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05 | Tampering | 归一搬运中改写既有 per-client/shared 断言字面（Phase 13 证据链毁灭） | high | mitigate | 两列逐字搬入纪律 + git diff 自审零期望值字面改动 + 14-12 收口闸 diff 白名单逐行终审 |
| T-14-06 | Tampering | per-client-only 测被误删或强行加 shared 分支（蓝本「新增 per-client-only」行未要求双跑） | medium | mitigate | 归属逐测判定 + 保持不动清单登记 SUMMARY（RESEARCH OQ2 裁决：默认不动） |
| T-14-SC | Tampering | 依赖面（go.mod/go.sum/lockfile） | high | mitigate | 零新依赖红线——本 plan 无装包任务，legitimacy 门不触发；零 diff 由 14-12 收口闸断言 |
</threat_model>

<verification>
- time go test -race -count=1 -v ./internal/server/ 三文件归一测试双模式子测试逐名全绿
- git diff 自审：两列期望值逐字未动，白名单 = 三文件结构归一与分叉表新增
- SUMMARY 含 per-client-only 保持不动清单与偏差登记
</verification>

<success_criteria>
1. 三文件 21 测完成归一：同需求双模式者双 t.Run 分叉表，per-client-only 保持原样并登记
2. 第二终结源/N 组信号/session_active 三分叉面显式成表
3. shared 列与 Phase 13 per-client 断言两列字面均逐字保持（双证据链零削弱）
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `internal/server/emptyexit_test.go`：--once/--exit-when-empty 测归一为 `t.Run("mode=shared"/"mode=per-client")` 双跑 + 第二终结源断言分叉表
- `internal/server/shutdown_test.go`：Shutdown 测归一为双跑 + N 组信号/有界 join/session_end 数==N 断言分叉表
- `internal/server/metrics_test.go`：metrics 测归一为双跑 + session_active 语义/series 镜像/HELP 文案断言分叉表

<output>
Create `.planning/phases/14-herdr-uat/14-03-SUMMARY.md` when done
</output>
