---
phase: 14-herdr-uat
plan: 06
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - internal/server/sharetoken_test.go
  - internal/server/tls_test.go
  - internal/server/customindex_test.go
  - internal/server/proxy_e2e_test.go
  - internal/server/proxy_test.go
  - internal/server/basepath_test.go
autonomous: true
requirements: [PC-13]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "sharetoken/tls/customindex 三文件 4 测完成 mode-agnostic 双跑改造：分享链接门禁/TLS 配置/自定义首页与进程模型无关（蓝本 sharetoken/tls/customindex 行），两列同一断言体期望值逐字未动"
    - "proxy_e2e/proxy/basepath 三文件 10 测完成同类改造：auth-header/XFF/base-path 面 mode-agnostic（蓝本 proxy*/basepath 行）"
    - "改造完成后 server 包三维归类收尾核对：32 个 _test.go 文件中除 perclient_test.go（per-client-only 保持单模式）、load_test.go（14-07 扩展面）、events/log/exitmsg/options（纯函数/事件 schema 已 Phase 13 扩展锁定）外，全部 server 装配测均经 newTestServer 双跑——收口核对清单登记 SUMMARY（D-01/D-02 蓝本全覆盖的证据）"
    - "全部经 newTestServer(t, mode, ...) 装配 + t.Run(\"mode=shared\"/\"mode=per-client\") 双跑（D-01）；期望值两列同值且与改造前逐字一致"
    - "CI 结构零改动确认：.github/workflows/ci.yml 零 diff，全量 go test -race -count=1 -v ./... 单 step 双模式覆盖达成（D-01 验收形态）"
  artifacts:
    - path: "internal/server/proxy_e2e_test.go"
      provides: "auth-header/XFF e2e 四测双模式同断言双跑"
      contains: "mode=per-client"
    - path: "internal/server/basepath_test.go"
      provides: "base-path 三测双模式同断言双跑"
      contains: "mode=shared"
    - path: "internal/server/sharetoken_test.go"
      provides: "分享链接门禁测双模式同断言双跑"
      contains: "mode=shared"
  key_links:
    - from: "internal/server/sharetoken_test.go 等六文件"
      to: "internal/server/harness_test.go"
      via: "server 装配调用收编至 newTestServer——部署面经同装配点双跑，防接缝重构误碰"
      pattern: "newTestServer"
  prohibitions:
    - requirement_id: PC-13
      category: values
      statement: "同断言双跑不得退化为断言削弱——部署面红线（XFF 信任闸/remote_user sanitize/base-path 规范化）两列同值且逐字未动；禁止「两模式都接受」形态"
---

<objective>
mode-agnostic 部署面批 + 三维归类收尾核对：sharetoken（1）/tls（2）/customindex（1）+ proxy_e2e（4）/proxy（3）/basepath（3）六文件 14 测双模式同断言双跑，并对 server 包 32 个测试文件做三维归类全覆盖核对（SC1 的完成判定面）。

Purpose: 部署面（auth-header/XFF/base-path/分享链接/TLS/自定义首页）是 Phase 7/9 落地的事故高发面，接缝重构等价性必须经双跑持续证明；收尾核对把「哪些文件双跑、哪些保持单模式、为什么」做成显式清单，杜绝静默漏网。
Output: 六文件双跑改造 + 三维归类收口核对清单 + -race 双模式逐名绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-herdr-uat/14-CONTEXT.md
@.planning/phases/14-herdr-uat/14-RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 部署面前批——sharetoken_test + tls_test + customindex_test 双模式同断言双跑（D-01）</name>
  <reversibility rating="reversible">测试文件改造，git 可回退；无公开契约面。</reversibility>
  <files>internal/server/sharetoken_test.go, internal/server/tls_test.go, internal/server/customindex_test.go</files>
  <read_first>
    - internal/server/sharetoken_test.go 全文（1 测——分享链接 /s/{token}/ 门禁现状装配形态）
    - internal/server/tls_test.go 全文（2 测——TLS 配置/握手面现状）
    - internal/server/customindex_test.go 全文（1 测——自定义首页校验/伺服现状）
    - internal/server/harness_test.go（newTestServer 装配入口）
    - .planning/research/PITFALLS.md:378-399（蓝本 sharetoken/tls/customindex 归属行）
  </read_first>
  <behavior>
    - 三文件 server 装配测经 newTestServer 双跑，两列同一断言体期望值逐字未动
    - 纯函数/配置测（无 server 装配者，如 TLS 配置构造单测）保持单跑原样，登记 SUMMARY
    - go test -v 输出装配测 mode=shared / mode=per-client 子测试双绿
  </behavior>
  <action>
    1. 三文件逐测判定装配形态：server 装配者包裹 for-mode + t.Run("mode="+mode) + newTestServer 双跑；纯函数体保持单跑（注释载明归属判定）。
    2. 断言体原样搬入两列（字面零改写）；注释一行载明「蓝本部署面行——mode-agnostic 同断言双跑」。
    3. time go test -race -count=1 -v -run 'TestShareToken|TestTLS|TestCustomIndex' ./internal/server/（按实际测名调整）双模式全绿 → time go test -race -count=1 ./internal/server/ 全量回归；gofmt/vet 零输出；单提交收口（test(14-06) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v ./internal/server/ 2>&1 | grep -E '^(--- (PASS|FAIL)).*mode=' | head -40; time go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - 三文件 server 装配测均含 t.Run("mode="+mode 包裹且经 newTestServer 装配
    - -v 输出装配测 mode=shared / mode=per-client 子测试全 PASS
    - 断言期望值字面与改造前逐字一致（git diff 自审：零期望值字面改动）
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
  </acceptance_criteria>
  <done>sharetoken/tls/customindex 4 测双模式同断言双跑落地。</done>
</task>

<task type="auto">
  <name>Task 2: 部署面后批——proxy_e2e_test + proxy_test + basepath_test 双跑 + 三维归类收口核对（D-01/D-02）</name>
  <reversibility rating="reversible">测试文件改造 + 核对清单文档化（SUMMARY），git 可回退；无公开契约面。</reversibility>
  <files>internal/server/proxy_e2e_test.go, internal/server/proxy_test.go, internal/server/basepath_test.go</files>
  <read_first>
    - internal/server/proxy_e2e_test.go 全文（4 测 6 处 startTrackedServerWith 调用——auth-header/XFF e2e 现状）
    - internal/server/proxy_test.go 全文（3 测 1 处调用——sanitize/extract 面现状）
    - internal/server/basepath_test.go 全文（3 测——base-path 挂载/307 现状）
    - internal/server/harness_test.go（newTestServer 装配入口）
    - .planning/research/PITFALLS.md:378-399（蓝本 proxy*/basepath 归属行 + 全表——收口核对的对照基准）
    - internal/server/（ls *_test.go 全清单 32 文件——收口核对枚举源；14-RESEARCH §Pattern 1 改造面实测盘点表为分类参照）
  </read_first>
  <behavior>
    - 三文件 server 装配测经 newTestServer 双跑，两列同一断言体期望值逐字未动；XFF 信任闸/remote_user sanitize 红线断言两列同值
    - 收口核对：32 文件逐一归类（双跑已改造 / per-client-only 单模式 / load 面 / 纯函数单跑），与蓝本表逐行对照，全部命中或登记偏差
    - go test -v 输出装配测 mode=shared / mode=per-client 子测试双绿
  </behavior>
  <action>
    1. 三文件套用同款包裹形态（for-mode + t.Run + newTestServer），断言体原样搬入两列；proxy 面红线断言（不可信 XFF 不采信/头值 sanitize/remote_user 审计归因）两列同值零缩水。
    2. 收口核对执行：ls internal/server/*_test.go 得 32 文件清单，逐一标注归类（① 已双跑改造——14-01..14-06 各 plan 文件名对照；② per-client-only 单模式——perclient_test.go；③ load 面——load_test.go（14-07 扩展）；④ 纯函数/事件 schema 单跑——events/log/exitmsg/options（Phase 13 扩展已锁定）；⑤ cmd/wesh 与 pty 包文件——蓝本「双模式同断言」行的落地形态说明（无 server 装配者天然 mode-agnostic 单跑，config/fuzz 已 Phase 10 扩展））；与 PITFALLS :378-399 蓝本表逐行对照，任何蓝本行未落地或归属实测不符按 D-02 登记偏差，不静默跟改。
    3. time go test -race -count=1 -v -run 'TestProxy|TestAuthHeader|TestXFF|TestBasePath' ./internal/server/（按实际测名调整）双模式全绿 → time go test -race -count=1 ./... 全量全绿（含 cmd/wesh 与 pty 包）；gofmt/vet 零输出；核对清单写进 SUMMARY；单提交收口（test(14-06) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v ./internal/server/ 2>&1 | grep -cE '^(--- PASS).*mode='; time go test -race -count=1 ./... && go vet ./... && "$(go env GOROOT)/bin/gofmt" -l internal/ cmd/</automated>
  </verify>
  <acceptance_criteria>
    - 三文件 server 装配测均含 t.Run("mode="+mode 包裹且经 newTestServer 装配
    - -v 输出装配测 mode=shared / mode=per-client 子测试全 PASS
    - 断言期望值字面与改造前逐字一致（git diff 自审：零期望值字面改动）
    - SUMMARY 含 32 文件三维归类收口核对清单（每文件归类 + 蓝本行对照 + 偏差登记）
    - time go test -race -count=1 ./... 全量全绿（5 包）；go vet 与 GOROOT gofmt -l 零输出
    - .github/workflows/ci.yml 零 diff（D-01 结构零改动终态确认）
  </acceptance_criteria>
  <done>部署面 10 测双跑落地；server 包三维归类收口核对清单完成——SC1 双模式 CI 门形态达成（单 step t.Run 双跑全覆盖）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试改造面→零回归证据链 | 部署面红线断言（XFF 信任闸/sanitize/base-path）是 SEC-07/OPS 语义证据本体——漂移即证据篡改面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-11 | Tampering | 同断言双跑退化为期望值削弱 | high | mitigate | 两列同值逐字未动 git diff 自审 + 14-12 收口闸 diff 白名单逐行终审 |
| T-14-12 | Tampering | 三维归类静默漏网（某 server 装配测未双跑且无登记） | medium | mitigate | Task 2 收口核对：32 文件逐一归类 + 蓝本表逐行对照 + 偏差登记（D-02 形态化） |
| T-14-SC | Tampering | 依赖面（go.mod/go.sum/lockfile） | high | mitigate | 零新依赖红线——本 plan 无装包任务，legitimacy 门不触发；零 diff 由 14-12 收口闸断言 |
</threat_model>

<verification>
- time go test -race -count=1 ./... 全量全绿（双模式经 t.Run 覆盖，CI 同款命令）
- SUMMARY 收口核对清单：32 文件归类全命中或偏差登记
- git diff 自审：期望值逐字未动；ci.yml 零 diff
</verification>

<success_criteria>
1. 部署面 14 测双模式同断言双跑落地
2. 三维归类收口核对完成——SC1「-race 双模式全量 Go 测试 CI 门」形态达成（单 step 零结构改动）
3. 期望值零改写，全量 -race 五包绿
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `internal/server/sharetoken_test.go` / `tls_test.go` / `customindex_test.go`：装配测改造为 `t.Run("mode=shared"/"mode=per-client")` 同断言双跑
- `internal/server/proxy_e2e_test.go` / `proxy_test.go` / `basepath_test.go`：装配测改造为同断言双跑
- SUMMARY 产物：server 包 32 文件三维归类收口核对清单（文档化产物，非代码符号）

<output>
Create `.planning/phases/14-herdr-uat/14-06-SUMMARY.md` when done
</output>
