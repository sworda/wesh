---
phase: 14-herdr-uat
plan: 06
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - internal/server/sharetoken_test.go
  - internal/server/tls_test.go
  - internal/server/customindex_test.go
  - internal/server/proxy_e2e_test.go
  - internal/server/proxy_test.go
  - internal/server/basepath_test.go
autonomous: true
requirements: [PC-13]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "sharetoken/customindex 两文件 2 测完成 mode-agnostic 双跑改造：分享链接门禁/自定义首页与进程模型无关（蓝本 sharetoken/customindex 行），两列同一断言体期望值逐字未动——sharetoken_test.go 为 package server 白盒文件（harness 小族在 server_test 包，包墙结构性不可达），双跑形态 = 本地 startShareServer 加 mode 参数的白盒镜像（shared=New(sess,...) 现状 / per-client=New(nil,...)+SpawnFunc+SessionModePerClient+spawn 追踪 Cleanup）；tls_test.go 实测为 httptest/中间件直测零 wesh server 装配（2 测纯函数形态），保持单跑并登记"
    - "proxy_e2e/basepath 两文件 7 测完成同类改造：auth-header/XFF/base-path 面 mode-agnostic（蓝本 proxy*/basepath 行）——proxy_e2e 5 处 startTrackedServerWith 调用点按用途分流（:46/:218/:281 用 waitHandlers → newTrackedTestServer；:127/:179 丢弃 → newTestServer）；proxy_test.go 3 测为纯函数（sanitize/extract——零装配），保持单跑并登记"
    - "改造完成后 server 包三维归类收尾核对：33 个 _test.go 文件（修订期实测修正——含 export_test.go 纯 helper 零测试桶）中除 perclient_test.go（per-client-only 保持单模式）、load_test.go（14-07 扩展面）、events/log（server 装配测保持单跑——D-02 蓝本偏差登记，见 Task 2）、exitmsg/options/auth/origin/throttle/tickets/proxy/tls/clients/resize（纯白盒/纯函数单跑）外，全部 server 装配测均经 newTestServer 小族双跑——收口核对清单登记 SUMMARY（D-01/D-02 蓝本全覆盖的证据）"
    - "全部经 newTestServer 小族装配 + t.Run(\"mode=shared\"/\"mode=per-client\") 双跑（D-01）；期望值两列同值且与改造前逐字一致"
    - "CI 结构零改动确认：.github/workflows/ci.yml 零 diff，全量 go test -race -count=1 -v ./... 单 step 双模式覆盖达成（D-01 验收形态）"
  artifacts:
    - path: "internal/server/proxy_e2e_test.go"
      provides: "auth-header/XFF e2e 四测双模式同断言双跑"
      contains: "mode=per-client"
    - path: "internal/server/basepath_test.go"
      provides: "base-path 三测双模式同断言双跑"
      contains: "mode=shared"
    - path: "internal/server/sharetoken_test.go"
      provides: "分享链接门禁测双模式同断言双跑"
      contains: "mode=shared"
  key_links:
    - from: "internal/server/sharetoken_test.go 等六文件"
      to: "internal/server/harness_test.go"
      via: "server 装配调用收编至 newTestServer——部署面经同装配点双跑，防接缝重构误碰"
      pattern: "newTestServer"
  prohibitions:
    - requirement_id: PC-13
      category: values
      statement: "同断言双跑不得退化为断言削弱——部署面红线（XFF 信任闸/remote_user sanitize/base-path 规范化）两列同值且逐字未动；禁止「两模式都接受」形态"
---

<objective>
mode-agnostic 部署面批 + 三维归类收尾核对：sharetoken（1）/customindex（1）+ proxy_e2e（4）/basepath（3）四文件 9 测双模式同断言双跑（tls/proxy 两文件 5 测实测为纯函数形态保持单跑并登记），并对 server 包 33 个测试文件做三维归类全覆盖核对（SC1 的完成判定面，含 events/log 单跑的 D-02 偏差登记与 export_test.go 零测试桶）。

Purpose: 部署面（auth-header/XFF/base-path/分享链接/TLS/自定义首页）是 Phase 7/9 落地的事故高发面，接缝重构等价性必须经双跑持续证明；收尾核对把「哪些文件双跑、哪些保持单模式、为什么」做成显式清单，杜绝静默漏网。
Output: 四文件双跑改造 + 三维归类收口核对清单（33 文件含偏差登记）+ -race 双模式逐名绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-herdr-uat/14-CONTEXT.md
@.planning/phases/14-herdr-uat/14-RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 部署面前批——sharetoken_test（白盒镜像双跑）+ tls_test（纯函数判定登记）+ customindex_test 双模式同断言双跑（D-01/D-02）</name>
  <reversibility rating="reversible">测试文件改造，git 可回退；无公开契约面。</reversibility>
  <files>internal/server/sharetoken_test.go, internal/server/tls_test.go, internal/server/customindex_test.go</files>
  <read_first>
    - internal/server/sharetoken_test.go 全文（1 测 4 处本地 startShareServer 白盒装配调用 :207/:276/:310/:356——package server 文件，harness 小族（server_test 包）结构性不可达；分享链接 /s/{token}/ 门禁现状）
    - internal/server/tls_test.go 全文（2 测——httptest.NewUnstartedServer + securityHeaders 中间件直测，零 wesh server 装配：实测纯函数形态）
    - internal/server/customindex_test.go 全文（1 测 9 处装配调用：8 处 startTestServerWith + 1 处 startBasePathServer :214——自定义首页校验/伺服现状）
    - internal/server/harness_test.go（newTestServer 小族装配入口——customindex 消费面）
    - internal/server/perclient_test.go:60-122（startPerClientServerWithSpawn 装配序列——sharetoken 白盒镜像 per-client 分支的参照母本）
    - .planning/research/PITFALLS.md:378-399（蓝本 sharetoken/tls/customindex 归属行）
  </read_first>
  <behavior>
    - sharetoken_test：startShareServer 加 mode 参数（白盒镜像 newTestServer 双分支——shared=现有 New(sess,...) 形态逐字保留；per-client=New(nil, exitf, opts) + Options.SessionMode=SessionModePerClient + SpawnFunc 默认 pty.StartWithSize 闭包 + spawned 逐一 Kill+Close Cleanup 追踪，镜像 perclient_test.go:60-106 母本）；4 处调用点所在子测试包 for-mode + t.Run 双跑，两列同一断言体期望值逐字未动
    - tls_test：实测零 wesh server 装配（httptest/中间件直测）——保持单跑原样，判定依据注释 + SUMMARY 登记（蓝本 tls 行的偏差：蓝本按「双模式同断言」归类，实测无装配面可参数化）
    - customindex_test：9 处调用点经 newTestServer / startBasePathServer（mode 参数化，见 Task 2）双跑；两列同一断言体
    - go test -v 输出装配测 mode=shared / mode=per-client 子测试双绿
  </behavior>
  <action>
    1. sharetoken_test.go：startShareServer(t, mode, opts) 参数化改造（白盒双分支镜像，per-client 分支 Cleanup 追踪纪律照 perclient_test.go:88-98 形态）；测试体包 for-mode + t.Run("mode="+mode)，4 处调用点传 mode——注释载明「package server 白盒文件，harness 小族（server_test 包）不可达，本地镜像双分支（D-01 同构形态）」。
    2. tls_test.go：保持单跑零改动或仅文件头注释一行载明判定（纯函数/中间件形态，无装配可参数化）；登记 SUMMARY。
    3. customindex_test.go：8 处 startTestServerWith 调用点换 newTestServer；:214 startBasePathServer 调用点随 Task 2 的 helper mode 参数化传 mode（两任务同 plan 串行，本任务落地时一并改）。
    4. 断言体原样搬入两列（字面零改写）；注释一行载明「蓝本部署面行——mode-agnostic 同断言双跑」。
    5. time go test -race -count=1 -v -run 'TestShareToken|TestTLS|TestCustomIndex|TestSecurityHeaders' ./internal/server/（按实际测名调整）双模式全绿 → time go test -race -count=1 ./internal/server/ 全量回归；gofmt/vet 零输出；单提交收口（test(14-06) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v ./internal/server/ 2>&1 | grep -E '--- (PASS|FAIL): .*mode=' | head -40; time go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - sharetoken_test.go 的 startShareServer 含 mode 参数与 per-client 白盒分支（grep 可证）；4 处调用点所在子测试含 t.Run("mode="+mode 包裹
    - customindex_test.go 装配测含 t.Run("mode="+mode 包裹且经小族装配
    - tls_test.go 保持单跑判定登记进 SUMMARY（文件零改动或仅注释行）
    - -v 输出装配测 mode=shared / mode=per-client 子测试全 PASS
    - 断言期望值字面与改造前逐字一致（git diff 自审：零期望值字面改动）
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
  </acceptance_criteria>
  <done>sharetoken 白盒镜像双跑 + customindex 双跑落地；tls 纯函数判定登记；部署面前批收口。</done>
</task>

<task type="auto">
  <name>Task 2: 部署面后批——proxy_e2e_test + basepath_test 双跑 + proxy_test 纯函数登记 + 三维归类收口核对（D-01/D-02）</name>
  <reversibility rating="reversible">测试文件改造 + 核对清单文档化（SUMMARY），git 可回退；无公开契约面。</reversibility>
  <files>internal/server/proxy_e2e_test.go, internal/server/proxy_test.go, internal/server/basepath_test.go</files>
  <read_first>
    - internal/server/proxy_e2e_test.go 全文（4 测 5 处 startTrackedServerWith 调用——:46/:218/:281 使用 waitHandlers（stderr 同步边）收编至 newTrackedTestServer，:127/:179 丢弃 waitHandlers 收编至 newTestServer；auth-header/XFF e2e 现状）
    - internal/server/proxy_test.go 全文（3 测——TestSanitizeRemoteUser/TestProxyClientIP/TestRemoteSanitize 纯函数零装配；修订期实证 :16 的 startTrackedServerWith 串为注释提及非调用）
    - internal/server/basepath_test.go 全文（3 测 4 处装配调用：2 处 startTestServerWith + 2 处 startBasePathServer 本地变体 :61/:190——base-path 挂载/307 现状）
    - internal/server/harness_test.go（newTestServer 小族装配入口）
    - .planning/research/PITFALLS.md:378-399（蓝本 proxy*/basepath 归属行 + 全表——收口核对的对照基准）
    - internal/server/（ls *_test.go 全清单 33 文件——收口核对枚举源；14-RESEARCH §Pattern 1 改造面实测盘点表（修订后）为分类参照）
  </read_first>
  <behavior>
    - proxy_e2e/basepath server 装配测经小族双跑，两列同一断言体期望值逐字未动；XFF 信任闸/remote_user sanitize 红线断言两列同值；startBasePathServer 加 mode 参数直传 newTestServer（本地 URL 映射 wrapper 保留，:61/:190 与 customindex:214 三调用点传 mode）
    - proxy_test 纯函数三测保持单跑原样，判定登记 SUMMARY
    - 收口核对：33 文件逐一归类（双跑已改造 / per-client-only 单模式 / load 面 / 纯函数·白盒单跑 / 事件面单跑偏差 / 纯 helper 零测试桶），与蓝本表逐行对照，全部命中或登记偏差
    - go test -v 输出装配测 mode=shared / mode=per-client 子测试双绿
  </behavior>
  <action>
    1. 两文件套用同款包裹形态（for-mode + t.Run + 小族装配），断言体原样搬入两列；proxy_e2e 五调用点按 behavior 用途分流（:46/:218/:281 → newTrackedTestServer，waitHandlers 同步边保留；:127/:179 → newTestServer）；proxy 面红线断言（不可信 XFF 不采信/头值 sanitize/remote_user 审计归因）两列同值零缩水；startBasePathServer(t, mode, opts) 参数化直传 newTestServer。
    2. proxy_test.go 保持单跑零改动或仅注释行载明判定；登记 SUMMARY。
    3. 收口核对执行：ls internal/server/*_test.go 得 33 文件清单，逐一标注归类（① 已双跑改造——14-01..14-06 各 plan 文件名对照；② per-client-only 单模式——perclient_test.go（36 测，per-client 断言本体）；③ load 面——load_test.go（14-07 扩展）；④ 纯白盒/纯函数单跑——clients/resize/auth/origin/throttle/tickets/exitmsg/options/proxy/tls（零 server 装配可参数化，14-04/14-05/本 plan 判定登记）；⑤ 事件面单跑（D-02 蓝本偏差登记）——events_test.go（9 测 server 装配测：蓝本「exitmsg/log/events 双模式同断言（扩展后）」行的实测处置 = 保持单跑，per-client 事件 schema 面已由 Phase 13 同文件三测 TestPerClientSessionEnd/TestPerClientSessionStart/TestSpawnEventsSchema 承载（蓝本「扩展后锁定」的落地产物），shared 事件测双跑仅复跑既有断言无增量——与 14-04 登记面一致引用）、log_test.go（1 测 startTrackedServerWith :69 同形态登记）；⑥ 纯 helper 零测试桶——export_test.go（package server 白盒导出桩，0 测）；⑦ cmd/wesh（3 文件 22 测）/pty（5 文件 21 测）/proto（2 文件 6 测）包文件——蓝本「双模式同断言」行的落地形态说明（无 server 装配者天然 mode-agnostic 单跑，config/fuzz 已 Phase 10 扩展））；与 PITFALLS :378-399 蓝本表逐行对照，任何蓝本行未落地或归属实测不符按 D-02 登记偏差（14-04 已登三则引用之，不重复登记），不静默跟改。
    4. time go test -race -count=1 -v -run 'TestProxy|TestAuthHeader|TestXFF|TestBasePath' ./internal/server/（按实际测名调整）双模式全绿 → time go test -race -count=1 ./... 全量全绿（含 cmd/wesh 与 pty 包）；gofmt/vet 零输出；核对清单写进 SUMMARY；单提交收口（test(14-06) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v ./internal/server/ 2>&1 | grep -cE '--- PASS: .*mode='; time go test -race -count=1 ./... && go vet ./... && "$(go env GOROOT)/bin/gofmt" -l internal/ cmd/</automated>
  </verify>
  <acceptance_criteria>
    - proxy_e2e/basepath 装配测均含 t.Run("mode="+mode 包裹且经小族装配；:46/:218/:281 经 newTrackedTestServer（grep 可证）
    - -v 输出装配测 mode=shared / mode=per-client 子测试全 PASS（grep -cE 计数非 0——子测试 PASS 行 4 空格缩进，模式不带 ^ 锚）
    - 断言期望值字面与改造前逐字一致（git diff 自审：零期望值字面改动）
    - SUMMARY 含 33 文件三维归类收口核对清单（每文件归类 + 蓝本行对照 + 偏差登记；含 export_test.go 零测试桶与 events/log 单跑偏差登记）
    - time go test -race -count=1 ./... 全量全绿（5 包）；go vet 与 GOROOT gofmt -l 零输出
    - .github/workflows/ci.yml 零 diff（D-01 结构零改动终态确认）
  </acceptance_criteria>
  <done>部署面 9 测双跑 + proxy 纯函数登记落地；server 包 33 文件三维归类收口核对清单完成（含 events/log 偏差与 export 桶）——SC1 双模式 CI 门形态达成（单 step t.Run 双跑全覆盖）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试改造面→零回归证据链 | 部署面红线断言（XFF 信任闸/sanitize/base-path）是 SEC-07/OPS 语义证据本体——漂移即证据篡改面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-11 | Tampering | 同断言双跑退化为期望值削弱 | high | mitigate | 两列同值逐字未动 git diff 自审 + 14-12 收口闸 diff 白名单逐行终审 |
| T-14-12 | Tampering | 三维归类静默漏网（某 server 装配测未双跑且无登记） | medium | mitigate | Task 2 收口核对：33 文件逐一归类（含 export_test.go 零测试桶与 events/log 偏差登记）+ 蓝本表逐行对照 + 偏差登记（D-02 形态化） |
| T-14-SC | Tampering | 依赖面（go.mod/go.sum/lockfile） | high | mitigate | 零新依赖红线——本 plan 无装包任务，legitimacy 门不触发；零 diff 由 14-12 收口闸断言 |
</threat_model>

<verification>
- time go test -race -count=1 ./... 全量全绿（双模式经 t.Run 覆盖，CI 同款命令）
- SUMMARY 收口核对清单：33 文件归类全命中或偏差登记
- git diff 自审：期望值逐字未动；ci.yml 零 diff
</verification>

<success_criteria>
1. 部署面 9 测双模式同断言双跑落地（tls/proxy 5 测纯函数登记保持单跑）
2. 三维归类收口核对完成——SC1「-race 双模式全量 Go 测试 CI 门」形态达成（单 step 零结构改动）
3. 期望值零改写，全量 -race 五包绿
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `internal/server/sharetoken_test.go`：`startShareServer` 加 mode 参数（package server 白盒镜像双分支）+ 4 调用点子测试 `t.Run("mode=shared"/"mode=per-client")` 双跑
- `internal/server/tls_test.go`：纯函数判定（httptest/中间件直测零 wesh 装配）保持单跑——零改动或仅判定注释行
- `internal/server/customindex_test.go`：装配测改造为同断言双跑（8 处普通 + 1 处 startBasePathServer :214）
- `internal/server/proxy_e2e_test.go`：5 处 tracked 调用点按用途分流（:46/:218/:281 → `newTrackedTestServer`；:127/:179 → `newTestServer`）+ 同断言双跑
- `internal/server/proxy_test.go`：纯函数判定保持单跑——零改动或仅判定注释行
- `internal/server/basepath_test.go`：`startBasePathServer` 加 mode 参数直传 newTestServer + 3 测同断言双跑
- SUMMARY 产物：server 包 33 文件三维归类收口核对清单（文档化产物，非代码符号；含 export_test.go 零测试桶与 events/log 单跑 D-02 偏差登记）

<output>
Create `.planning/phases/14-herdr-uat/14-06-SUMMARY.md` when done
</output>
