---
phase: 14-herdr-uat
plan: 05
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - internal/server/handshake_test.go
  - internal/server/limits_test.go
  - internal/server/keepalive_test.go
  - internal/server/auth_e2e_test.go
  - internal/server/auth_test.go
  - internal/server/origin_test.go
  - internal/server/throttle_test.go
  - internal/server/tickets_test.go
autonomous: true
requirements: [PC-13]
estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "handshake/limits/keepalive 三文件 15 测完成 mode-agnostic 双跑改造：协议守卫/三层上限/保活与进程模型无关，两模式跑同一断言体（同断言双跑——蓝本 handshake/limits/keepalive 行）"
    - "auth_e2e/auth/origin/throttle/tickets 五文件 15 测完成同类改造：认证/Origin/节流/ticket 面 mode-agnostic（跑两遍成本低，防接缝误碰——蓝本 auth*/origin/throttle/tickets 行）"
    - "同断言双跑的语义红线：两列跑同一断言体是「该行为与进程模型无关」的显式判定，每列都执行完整原断言集——不是省略断言的「两模式都接受」（PITFALLS P11 红线辨析）"
    - "全部经 newTestServer(t, mode, ...) 装配 + t.Run(\"mode=shared\"/\"mode=per-client\") 双跑（D-01）；期望值两列同值且与改造前逐字一致（零改写）"
    - "纯函数测（无 server 装配的测试体，如 auth_test.go 的凭据比较单测）保持单跑原样——无装配可参数化；逐文件判定清单登记 SUMMARY（蓝本 auth* 行含两类形态）"
  artifacts:
    - path: "internal/server/handshake_test.go"
      provides: "握手守卫七测双模式同断言双跑"
      contains: "mode=per-client"
    - path: "internal/server/auth_e2e_test.go"
      provides: "认证 e2e 九测双模式同断言双跑"
      contains: "mode=shared"
    - path: "internal/server/limits_test.go"
      provides: "三层上限五测双模式同断言双跑"
      contains: "mode=shared"
  key_links:
    - from: "internal/server/handshake_test.go 等八文件"
      to: "internal/server/harness_test.go"
      via: "server 装配调用收编至 newTestServer——mode-agnostic 面经同装配点双跑，防接缝重构误碰（PITFALLS P11 反向陷阱防线）"
      pattern: "newTestServer"
  prohibitions:
    - requirement_id: PC-13
      category: values
      statement: "同断言双跑不得退化为断言削弱——任何「两模式都能过就行」的期望值改写即零回归证据毁灭；纯函数测保持单跑是形态判定不是覆盖缺口"
---

<objective>
mode-agnostic 协议守卫+认证批：handshake（7）/limits（5）/keepalive（3）+ auth_e2e（9）/auth（2）/origin（2）/throttle（1）/tickets（1）八文件 30 测的双模式同断言双跑改造——三维归类第一维（mode-agnostic）的前半批。

Purpose: 协议守卫与认证面与进程模型无关，但 shared 路径被接缝重构碰过的每个函数都必须被证明等价（PITFALLS P11 反向陷阱）——双跑即该等价的持续防线。
Output: 八文件双跑改造 + -race 双模式逐名绿 + 期望值零改写自审。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-herdr-uat/14-CONTEXT.md
@.planning/phases/14-herdr-uat/14-RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 协议守卫批——handshake_test + limits_test + keepalive_test 双模式同断言双跑（D-01）</name>
  <reversibility rating="reversible">测试文件改造，git 可回退；无公开契约面。</reversibility>
  <files>internal/server/handshake_test.go, internal/server/limits_test.go, internal/server/keepalive_test.go</files>
  <read_first>
    - internal/server/handshake_test.go 全文（7 测——子协议双闸/4KiB/5s 超时/抢跑/version_mismatch 等现状装配形态）
    - internal/server/limits_test.go 全文（5 测——17KiB 1009/边界/分片洪水/空消息洪水现状）
    - internal/server/keepalive_test.go 全文（3 测——pinger 存活/pong 超时/禁用反证现状）
    - internal/server/harness_test.go（newTestServer 装配入口）
    - .planning/research/PITFALLS.md:378-399（蓝本 handshake/limits/keepalive 行：「协议守卫/上限/保活与进程模型无关」）
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §Architecture Patterns 1/2（同断言双跑形态与红线）
  </read_first>
  <behavior>
    - 三文件全部 server 装配测经 newTestServer 双跑：两列执行同一断言体且期望值与改造前逐字一致
    - 无 server 装配的测试体（若存在）保持单跑原样，登记 SUMMARY
    - go test -v 输出每测含 mode=shared / mode=per-client 子测试且双绿
  </behavior>
  <action>
    1. 三文件逐测包裹 for-mode + t.Run("mode="+mode) 形态，装配调用换 newTestServer(t, mode, argv, mutate)；断言体原样搬入（两列同值，字面零改写）。
    2. 测试体注释一行载明归属：「蓝本 handshake/limits/keepalive 行——mode-agnostic 同断言双跑」。
    3. 洪水类测（1 字节分片/0 字节空消息）双跑后 CI 时长翻倍——保持原量不缩水（防线强度不因双跑降低）；时长影响登记 SUMMARY（A1 估算面）。
    4. time go test -race -count=1 -v -run 'TestHandshake|TestHello|TestLimits|TestKeepalive|TestPong|TestSubprotocol' ./internal/server/（按实际测名调整）双模式全绿 → time go test -race -count=1 ./internal/server/ 全量回归；gofmt/vet 零输出；单提交收口（test(14-05) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v ./internal/server/ 2>&1 | grep -E '^(--- (PASS|FAIL)).*mode=' | head -60; time go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - 三文件 server 装配测均含 t.Run("mode="+mode 包裹且经 newTestServer 装配（grep 命中）
    - -v 输出 15 测 mode=shared / mode=per-client 子测试全 PASS
    - 断言期望值字面与改造前逐字一致（git diff 自审：仅结构包裹与注释新增，零期望值字面改动）
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
  </acceptance_criteria>
  <done>协议守卫 15 测双模式同断言双跑落地；洪水防线强度零缩水。</done>
</task>

<task type="auto">
  <name>Task 2: 认证面批——auth_e2e_test + auth_test + origin_test + throttle_test + tickets_test 双模式同断言双跑（D-01）</name>
  <reversibility rating="reversible">测试文件改造，git 可回退；无公开契约面。</reversibility>
  <files>internal/server/auth_e2e_test.go, internal/server/auth_test.go, internal/server/origin_test.go, internal/server/throttle_test.go, internal/server/tickets_test.go</files>
  <read_first>
    - internal/server/auth_e2e_test.go 全文（9 测 2 处 startTrackedServerWith 调用——认证 e2e 现状）
    - internal/server/auth_test.go 全文（2 测——凭据比较纯函数或装配形态逐测判定）
    - internal/server/origin_test.go 全文（2 测）、internal/server/throttle_test.go 全文（1 测）、internal/server/tickets_test.go 全文（1 测）
    - internal/server/harness_test.go（newTestServer 装配入口）
    - .planning/research/PITFALLS.md:378-399（蓝本 auth*/origin/throttle/tickets 行：「认证传输面 mode-agnostic（跑两遍成本低，防接缝误碰）」）
  </read_first>
  <behavior>
    - server 装配测经 newTestServer 双跑，两列同一断言体期望值逐字未动
    - 纯函数测（凭据 subtle 比较/Origin 规范化/throttleStore/ticketStore 单测族——无 server 装配者）保持单跑原样，逐文件判定清单登记 SUMMARY
    - go test -v 输出装配测 mode=shared / mode=per-client 子测试双绿
  </behavior>
  <action>
    1. 五文件逐测判定装配形态：含 startTrackedServerWith/http.Server 装配者包裹 for-mode + t.Run + newTestServer 双跑；纯函数体保持单跑（注释载明「纯函数 mode-agnostic，无装配可参数化」）。
    2. 日志红线类断言（凭据不出现在日志）双跑两列同值——红线强度零缩水。
    3. time go test -race -count=1 -v -run 'TestAuth|TestOrigin|TestThrottle|TestTicket' ./internal/server/（按实际测名调整）双模式全绿 → time go test -race -count=1 ./internal/server/ 全量回归；gofmt/vet 零输出；单提交收口（test(14-05) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v ./internal/server/ 2>&1 | grep -E '^(--- (PASS|FAIL)).*mode=' | head -60; time go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - 五文件 server 装配测均含 t.Run("mode="+mode 包裹且经 newTestServer 装配
    - -v 输出装配测 mode=shared / mode=per-client 子测试全 PASS
    - 纯函数测保持单跑清单登记进 SUMMARY
    - 断言期望值字面与改造前逐字一致（git diff 自审：零期望值字面改动）
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
  </acceptance_criteria>
  <done>认证面 15 测双模式同断言双跑落地；纯函数保持面登记完毕；mode-agnostic 前半批收口。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试改造面→零回归证据链 | 协议守卫/认证断言是 v1.0 SEC/RES 语义证据本体——同断言双跑中的期望值漂移即证据篡改面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-09 | Tampering | 同断言双跑退化为期望值削弱（「两模式都能过」形态） | high | mitigate | 两列同值且与改造前逐字一致 git diff 自审 + 14-12 收口闸 diff 白名单逐行终审 |
| T-14-10 | DoS | 洪水类测双跑致 CI 时长超阈值（估算 3-4min/leg，A1） | low | accept | 时长实测登记 SUMMARY；若超 CI 时限按 D-02 偏差登记流程再议分批（形态不变） |
| T-14-SC | Tampering | 依赖面（go.mod/go.sum/lockfile） | high | mitigate | 零新依赖红线——本 plan 无装包任务，legitimacy 门不触发；零 diff 由 14-12 收口闸断言 |
</threat_model>

<verification>
- time go test -race -count=1 -v ./internal/server/ 八文件 30 测双模式子测试逐名全绿
- git diff 自审：期望值逐字未动，白名单 = 八文件结构包裹与注释新增
- SUMMARY 含纯函数保持单跑清单
</verification>

<success_criteria>
1. 协议守卫 15 测 + 认证面 15 测双模式同断言双跑落地（D-01 执行机械覆盖 mode-agnostic 前半批）
2. 期望值零改写，-race 双模式全绿
3. 纯函数测形态判定清单登记（无静默缺口）
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `internal/server/handshake_test.go`：7 测改造为 `t.Run("mode=shared"/"mode=per-client")` 同断言双跑
- `internal/server/limits_test.go`：5 测改造为同断言双跑
- `internal/server/keepalive_test.go`：3 测改造为同断言双跑
- `internal/server/auth_e2e_test.go`：9 测改造为同断言双跑
- `internal/server/auth_test.go` / `origin_test.go` / `throttle_test.go` / `tickets_test.go`：装配测改造为同断言双跑（纯函数测保持单跑）

<output>
Create `.planning/phases/14-herdr-uat/14-05-SUMMARY.md` when done
</output>
