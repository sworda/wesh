---
phase: 14-herdr-uat
plan: 10
type: execute
wave: 2
depends_on: [14-08]
files_modified:
  - web/uat/run-all.mjs
autonomous: true
requirements: [PC-13]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "web/uat/run-all.mjs 一键矩阵落地（D-04）：枚举 13-08 收口闸十五脚本基线（协议 11 + jsdom 4——phase02/03/04/05/05-dims/06/07/08/09 + phase04-dom/05-dom/06-dom/12-dom + per-client 三脚本 phase11/12/13）+ phase14.mjs 共 16 项，串行 spawn('node', [script]) 逐脚本聚合 exit code，末尾汇总表（脚本/结果/耗时）输出"
    - "每脚本 exit code 0/1 门禁语义逐脚本既有（phase13.mjs 尾 process.exit 形态）——runner 零断言逻辑只聚合，不引入任何测试框架（Don't Hand-Roll：15 脚本全部自带 check()/exit code 门禁）"
    - "前置保障：wesh 二进制存在性检查（默认 /tmp/wesh-uat/wesh，缺席则先 go build -o /tmp/wesh-uat/wesh ./cmd/wesh 或明确报错指引——phase13.mjs 头注释约定形态）"
    - "pw 层不纳入矩阵：汇总表末尾注明 phase14-pw.mjs 独立执行入口（Windows 工作站 node web/uat/pw/phase14-pw.mjs）——双机拓扑硬约束（RESEARCH OQ3 裁决），收口闸人工两段式（Linux runner 全绿 + Windows pw 全绿）"
    - "runner 自身 exit code = 全绿 0 / 任一失败 1；单脚本挂死防护：逐脚本超时护栏（如 10min/脚本，herdr 场景含真实等待——超时 kill 并记 FAIL，不阻断后续脚本）"
    - "UAT 红线继承：runner 透传子脚本输出（stdio inherit 或管道转发），自身不新增任何 token/pid 打印面；assertOutputClean 职责归各子脚本既有面"
    - "SC2 证据形态（D-13）：phase11/12/13 三脚本经本 runner 重跑全绿即「协议层 per-client 全链六项」矩阵证据——零新断言双写"
    - "协议 UAT 不进 CI（D-04 既定：真实二进制 spawn 的环境敏感度/flake 面/时长不合算）——run-all.mjs 为人工/收口闸执行入口，.github/workflows/ 零 diff"
  artifacts:
    - path: "web/uat/run-all.mjs"
      provides: "16 项 UAT 矩阵 runner（串行聚合 + 超时护栏 + 汇总表 + exit code 门禁）"
      contains: "phase14.mjs"
  key_links:
    - from: "web/uat/run-all.mjs"
      to: "web/uat/phase02-14.mjs + *-dom.mjs"
      via: "串行 spawn 聚合各脚本自带 exit code 门禁——13-08 十五脚本基线 + phase14.mjs 增量的形式化（D-04 零修改重跑形式化本体）"
      pattern: "spawn"
  prohibitions:
    - requirement_id: PC-13
      category: values
      statement: "矩阵不得以跳过/豁免换绿——脚本失败必须修复或显式登记，runner 不提供 skip 开关（skipped 仅各脚本内平台豁免类且带 reason，CODEBUDDY.md §5 既定口径）"
---

<objective>
D-04 落地：新建 web/uat/run-all.mjs——把 13-08 收口闸人工逐跑的十五脚本 + phase14.mjs 串成一键矩阵（串行 spawn + exit code 聚合 + 汇总表），作为「phase02-09 既有协议 UAT 默认 shared 零修改重跑全过」零回归双证据与 SC2 三脚本重跑证据的形式化执行入口。

Purpose: 零回归双证据此前靠人工逐脚本跑（13-08 十七轮）；runner 把它形式化为单命令矩阵，消除漏跑/错序面，并为 14-12 收口闸提供统一执行载具。
Output: run-all.mjs + 全矩阵首跑汇总表（各脚本基线计数核对）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-herdr-uat/14-CONTEXT.md
@.planning/phases/14-herdr-uat/14-RESEARCH.md
@.planning/phases/14-herdr-uat/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: run-all.mjs 编写——16 项枚举 + 串行聚合 + 超时护栏 + 汇总表（D-04）</name>
  <reversibility rating="reversible">runner 脚本新增，git 可回退；纯读取面零环境副作用（子脚本自身清理归各脚本）。</reversibility>
  <files>web/uat/run-all.mjs</files>
  <read_first>
    - web/uat/phase13.mjs:748-769（收口段母本：串行执行 + 异常计数 + 汇总行 + process.exit 门禁语义——runner 把它提升到跨脚本层）
    - web/uat/phase13.mjs:1-44（四段式头注释母本与二进制路径约定）
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §Architecture Patterns 5（十五脚本基线表与时长量级 ~3.5min + phase14 增量 1-2min）与 §Open Questions 3（pw 不纳入裁决）
    - .planning/phases/14-herdr-uat/14-PATTERNS.md §No Analog Found（run-all.mjs 无精确类比的设计约束说明）
    - web/uat/（ls 核对 16 项脚本文件实际文件名——phase05-dims/phase04-dom 等命名逐字核对）
  </read_first>
  <behavior>
    - 头注释四段式：矩阵清单 16 项 + 红线（runner 零断言零敏感值打印）+ 时序纪律（串行 + 逐脚本超时护栏）+ 运行方式（node web/uat/run-all.mjs [wesh 二进制路径]）
    - 枚举数组：15 项既有 + phase14.mjs，按协议→jsdom→per-client→herder 场景序排列（phase14.mjs 含真实等待排末位）
    - 逐脚本：spawn('node', [script, WESH]) 串行 await，stdio 管道转发前缀化（[phaseNN] 行前缀）或 inherit；超时护栏 10min kill 记 FAIL；聚合 {script, ok, durMs} 结果数组
    - 汇总表：逐脚本 PASS/FAIL/耗时行 + 总计行；pw 独立入口注记；process.exit(全绿 ? 0 : 1)
    - 二进制前置：fs.existsSync(WESH) 缺席时尝试 go build（或报错退出并打印构建指引——择一实现并注释）
  </behavior>
  <action>
    1. 新建 web/uat/run-all.mjs（零依赖 Node>=22）：头注释 + WESH 常量（argv[2] 覆盖，默认 /tmp/wesh-uat/wesh）+ SCRIPTS 枚举数组（文件名经 web/uat/ ls 逐字核对）。
    2. 主流程：二进制存在性前置 → for 循环串行 spawn（timeout 护栏 + kill 转 FAIL）→ 结果聚合 → 汇总表打印（列：脚本/结果/耗时秒）→ pw 注记行 → exit code 门禁。
    3. 输出前缀化或 inherit 择一（可读性优先）；runner 自身不解析不断言不打印子脚本敏感值（红线注释载明）。
    4. node --check 语法零错；node web/uat/run-all.mjs 干跑语法面（可先以单脚本子集验证聚合逻辑）——本任务以编写 + 静态核验 + 子集冒烟收口；单提交（test(14-10) 前缀）。
  </action>
  <verify>
    <automated>node --check web/uat/run-all.mjs && node web/uat/run-all.mjs --help 2>/dev/null; node -e "console.log('syntax ok')"</automated>
  </verify>
  <acceptance_criteria>
    - web/uat/run-all.mjs 存在且 node --check 语法零错
    - SCRIPTS 枚举含 16 项（grep 逐文件名核对：phase02/03/04/05/05-dims/06/07/08/09/11/12/13/14 + phase04-dom/05-dom/06-dom/12-dom——实际 16 项以 ls 核对为准）
    - 串行 spawn + 超时护栏 + 汇总表 + exit code 门禁四面 grep 可证
    - pw 独立执行入口注记在场（grep phase14-pw 可证）
    - .github/workflows/ 零 diff（D-04 不进 CI）
  </acceptance_criteria>
  <done>runner 编写完成并通过静态核验与子集冒烟；16 项枚举与门禁形态就位。</done>
</task>

<task type="auto">
  <name>Task 2: 全矩阵首跑 + 基线计数核对（D-04/D-13 证据面成型）</name>
  <reversibility rating="reversible">执行性任务——结果记录入 SUMMARY，零代码面变更（若脚本暴露真实缺陷则停下登记而非放行）。</reversibility>
  <files>web/uat/run-all.mjs</files>
  <read_first>
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §Architecture Patterns 5（十三-08 基线计数表：12/18/10/28+DIMS/23/34/21/18 + 21+1skip/20/29 + 37/19/40+2skip/17）
    - .planning/phases/13-resource-defense/13-08-SUMMARY.md（UAT 矩阵基线原始记录 :172-193 表——计数核对基准）
    - web/uat/run-all.mjs（Task 1 落地的 runner）
  </read_first>
  <behavior>
    - go build -o /tmp/wesh-uat/wesh ./cmd/wesh（time 前缀）后 node web/uat/run-all.mjs 全矩阵跑通
    - 逐脚本结果与 13-08 基线计数一致（12/18/10/28+1skip/DIMS PASS/23+1skip/34+1skip/21/18 + 21+1skip/20/29 + 37/19/40+2skip/17）；phase14.mjs 全绿（S1+S2 check 数按 14-08 实际）
    - 任一脚本低于基线或 FAIL：不修脚本不放宽，停下登记（真实缺陷信号——按 D-02 同纪律登记偏差并上报）
    - 汇总表形态完整（16 行 + 总计 + pw 注记）；全程耗时记录（对照 ~3.5min+1-2min 量级，A6）
  </behavior>
  <action>
    1. time go build -o /tmp/wesh-uat/wesh ./cmd/wesh 构建新鲜二进制。
    2. time node web/uat/run-all.mjs 全矩阵首跑；逐脚本 PASS/FAIL 与计数摘录。
    3. 基线核对：逐脚本断言计数与 13-08-SUMMARY :172-193 表一致（ skipped 项带 reason 形态保持）；phase14.mjs 计数按 14-08-SUMMARY 记录核对。
    4. 不一致处置：先核二进制新鲜度与环境（herdr --version 0.8.100），复跑确认非 flake 后仍不一致 → 停下登记偏差（SUMMARY 记录脚本名/基线值/实测值），不修脚本不放行。
    5. 汇总表与耗时入 SUMMARY；herdr session list 与进程残留核验（pgrep wesh 零残留——注意 Pitfall 3 自匹配陷阱不适用于 wesh 名）；单提交收口（test(14-10) 前缀，若零文件改动则提交仅含 SUMMARY）。
  </action>
  <verify>
    <automated>time go build -o /tmp/wesh-uat/wesh ./cmd/wesh && time node web/uat/run-all.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - node web/uat/run-all.mjs 退出码 0（16/16 全绿）
    - SUMMARY 含逐脚本计数核对表（基线 vs 实测）且全部一致或偏差登记
    - 汇总表含 16 行 + 总计 + pw 注记；全程耗时记录
    - herdr session list 零 wesh-uat-* 残留；wesh 进程零残留
  </acceptance_criteria>
  <done>全矩阵首跑 16/16 全绿且计数与基线一致——D-04 零修改重跑形式化与 D-13 SC2 矩阵证据成型；runner 可供 14-12 收口闸直接复用。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner→16 个真实二进制 spawn 子脚本 | 矩阵长跑（~5min 量级）的进程残留与挂死面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-21 | DoS | 子脚本挂死致矩阵永挂 / 中途失败致进程残留 | medium | mitigate | 逐脚本 10min 超时护栏 kill 转 FAIL + 串行异常不跳后续 + 跑后 herdr session list 与 wesh 进程残留核验（Task 2 步 5） |
| T-14-22 | Information Disclosure | runner 透传面新增 token/pid 打印 | low | accept | runner 零断言零敏感值打印红线（头注释载明）；敏感值自净归各子脚本既有 assertOutputClean 面 |
| T-14-SC | Tampering | 依赖面（Node 原生零依赖） | high | mitigate | 零新依赖红线——无装包任务，legitimacy 门不触发；lockfile 零 diff 由 14-12 收口闸断言 |
</threat_model>

<verification>
- node --check + 子集冒烟（Task 1）；全矩阵 node web/uat/run-all.mjs 16/16 全绿 + 基线计数一致（Task 2）
- 残留核验零命中；.github/workflows/ 零 diff
</verification>

<success_criteria>
1. 一键矩阵 16/16 全绿（D-04 零修改重跑形式化）
2. 逐脚本计数与 13-08 基线一致（SC2 三脚本重跑证据，D-13）
3. runner 超时护栏/汇总表/exit code 门禁就位，14-12 可直接复用
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `web/uat/run-all.mjs`（新文件）：`SCRIPTS` 16 项枚举数组 + 主流程（串行 spawn 聚合/超时护栏/汇总表/exit code 门禁）+ 常量 `WESH`

<output>
Create `.planning/phases/14-herdr-uat/14-10-SUMMARY.md` when done
</output>
