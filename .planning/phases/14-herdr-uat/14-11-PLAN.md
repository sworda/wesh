---
phase: 14-herdr-uat
plan: 11
type: execute
wave: 2
depends_on: [14-07, 14-08]
files_modified:
  - README.md
  - docs/ARCHITECTURE.md
  - docs/CONFIGURATION.md
autonomous: true
requirements: [PC-12]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "README.md 新增「会话模式」节（:96-98 session-mode 单段的同位扩展，D-14 主承载）：模式语义表三行（分享链接=按权限级别的独立进程入场券 / ro=自有进程输入门控 / 配合 herdr·tmux 经多路复用汇聚）+ herdr 配方示例（wesh --session-mode=per-client --writable -- herdr --session <name>——与 phase14.mjs 实证形态逐字一致，文档即被测物）+ 资源义务段含实测标定表（14-07 LOADDATA pc_flood/pc_resident 数据回填，09-09 D-13 表格形态）+ 按部署形态 maxClients 建议值表（D-11：默认 32 不动，如「低配 VPS 建议 --max-clients=8」类建议行）"
    - "tmux 对照句措辞红线：tmux 3.6 默认 window-size=latest——表述为「默认 window-size=latest 下小屏客户端活动时窗口收缩，桌面端观感被压缩；window-size 可配但均为单窗口尺寸语义，不存在 per-client 独立渲染」，不得出现「默认取最小值」字面失实（RESEARCH §State of the Art 本机实测修正）"
    - "docs/ARCHITECTURE.md 补双模式架构段（D-14）：goroutine 拓扑 mermaid 图（CODEBUDDY.md 文档规则——技术文档用 mermaid；:9-21 graph TD + subgraph 分层 + <br/> 节点文案形态沿用）+ 双模式分岔说明（装配期一次分岔、运行期零分岔，6-7 显式分支点）+ keep/degrade/vanish 映射素材（FEATURES :111-135）"
    - "docs/ARCHITECTURE.md:7 GoTTY 误记修正（D-14）：「GoTTY 式共享进程模型」表述修正为 GoTTY 实为 per-connection spawn（源码已核实）+ wesh 双模式分岔说明——误记原文逐字核读后改写，语义不失真"
    - "docs/CONFIGURATION.md 补 max-clients per-client 语义行（D-14）：默认值表（:165-166 区域）补行——per-client 下兼任并发进程上限（握手 503 闸 + spawn 前 hubMu 内复检计数，PC-08 硬不变量）；stop-timeout 双默认值行（:176 区域）同表邻居不动"
    - "REQUIREMENTS.md:50 D-15 历史注记不改写（D-14 既定：裁决档案保持当时语境）"
    - "PC-12 并发/中断保障（specless 探针 concurrency 行，resolved-explicit）：三文档改动集中于本 plan 串行落地，files_modified 与同 wave plan 零重叠（wave 2 扫描保证无并行写者）；中断恢复单元为 git 提交边界——未提交改动直接丢弃，无部分持久化中间态；并行执行保障由 wave 结构结构性提供"
  artifacts:
    - path: "README.md"
      provides: "「会话模式」节（模式语义表 + herdr 配方 + 资源义务段标定表 + 建议值表）"
      contains: "会话模式"
    - path: "docs/ARCHITECTURE.md"
      provides: "双模式架构段（mermaid goroutine 拓扑图）+ :7 GoTTY 误记修正"
      contains: "per-connection"
    - path: "docs/CONFIGURATION.md"
      provides: "max-clients per-client 语义行"
      contains: "per-client"
  key_links:
    - from: "README.md 标定表"
      to: "internal/server/load_test.go"
      via: "14-07 LOADDATA cell=pc_flood/pc_resident 实测行回填（09-06 → 09-09 先例链）——数据未实测不填，填即实测值"
      pattern: "pc_flood|pc_resident"
    - from: "README.md herdr 配方"
      to: "web/uat/phase14.mjs"
      via: "配方形态与 14-08 实证 argv 逐字一致（文档即被测物——配方可复现性由 UAT 担保）"
      pattern: "session-mode=per-client"
  prohibitions:
    - requirement_id: PC-12
      category: values
      statement: "不得改写 REQUIREMENTS.md:50 D-15 历史注记（裁决档案完整性——历史语境保持，任何「顺手修正」即档案篡改）"
    - requirement_id: PC-12
      category: values
      statement: "tmux 对照句不得出现「默认取最小值」字面失实表述（tmux 3.6 实测默认 window-size=latest——文档失实会误导用户拿 tmux 复测误判 wesh 失效）"
    - requirement_id: PC-12
      category: values
      statement: "文档段不得描述未经验证面实证的行为——herdr 配方必须经 phase14.mjs/pw 实证、资源数据必须来自 14-07 LOADDATA 实测（文档即被测物，防文档说谎；账面推算值入场必须标注推算属性）"
    - requirement_id: PC-12
      category: values
      statement: "不得削弱或删除既有 shared 模式语义表述（v1.0 文档承诺保持——per-client 段为增量而非改写；shared 是差异化本体的文档面）"
---

<objective>
PC-12 落地：模式语义文档三件套——README 主承载（「会话模式」节 + 资源义务段标定表回填）+ docs/ARCHITECTURE.md 双模式段（mermaid goroutine 拓扑 + :7 GoTTY 误记修正）+ docs/CONFIGURATION.md max-clients per-client 语义行（D-14 分布形态）。标定数据依赖 14-07 LOADDATA 实测，herdr 配方依赖 14-08 实证形态。

Purpose: per-client 是 v1.1 核心卖点，其语义（独立进程入场券/ro 门控/多路复用汇聚）必须在主文档一次说清——二级文档降低可达性（D-14 否决 SESSION-MODES.md 的既定裁决）；GoTTY 误记是 v1.0 遗留的事实性错误，随本里程碑修正。
Output: 三文档段落落地 + 标定表实测回填 + 误记修正。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-herdr-uat/14-CONTEXT.md
@.planning/phases/14-herdr-uat/14-RESEARCH.md
@.planning/phases/14-herdr-uat/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README「会话模式」节 + 资源义务段标定表回填（D-14 主承载 + D-11 建议值表 + D-05 tmux 对照句）</name>
  <reversibility rating="reversible">文档编辑，git 可回退；无公开契约变更（maxClients 默认 32 不动——建议值表是文档建议非契约）。</reversibility>
  <files>README.md</files>
  <read_first>
    - README.md:90-110（session-mode 单段 :96-98 与保活先杀段的同位邻区——新节插点与段形态母本；单段致密中文叙事 + flag 值 inline code 形态）
    - README.md 既有标定表区域（09-09 D-13 先例表格形态——grep 定位标定/负载相关表）
    - .planning/phases/14-herdr-uat/14-07-SUMMARY.md（LOADDATA pc_flood/pc_resident 实测行——标定表数据源；若 14-07 未完成本任务阻塞，不得编造数据）
    - web/uat/phase14.mjs（herdr 配方实证 argv 形态——配方示例逐字来源）
    - .planning/research/FEATURES.md :57-66/:131（裁决 6/7：ro=自有进程输入门控 / 分享链接=独立进程入场券——语义表叙事源）与 :111-135（keep/degrade/vanish 映射素材）
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §State of the Art（tmux 3.6 window-size=latest 实证与措辞红线）
  </read_first>
  <behavior>
    - 新「会话模式」节含：模式语义表（三行：shared=多人同屏共享进程（差异化本体，默认）/ per-client=每客户端独立 PTY 进程（ttyd 式）/ 选择方式 --session-mode 或 TOML session_mode）+ 分享链接/ro/herdr·tmux 汇聚三语义行 + herdr 配方 sh 代码块 + tmux 对照句（latest 措辞）
    - 资源义务段：标定表（N=1/4/16/32 行 × 内存/fd/goroutine/吞吐列，pc_flood/pc_resident 实测值）+ 「32 并发 shell 是重负载」义务说明 + 建议值表（部署形态 → 建议 --max-clients 值；默认 32 不动明示）
    - 既有 shared 语义表述零削弱（新节为增量）；README 其他区域零 diff
  </behavior>
  <action>
    1. README.md 在 session-mode 单段（:96-98 区域）同位扩展为「会话模式」节：模式语义表三行 + 三语义行（分享链接=按权限级别的独立进程入场券；ro=自有进程输入门控；配合 herdr/tmux 时经多路复用汇聚——各配一句机理说明，叙事源 FEATURES 裁决 6/7）。
    2. herdr 配方示例 sh 代码块：wesh --session-mode=per-client --writable -- herdr --session <name>（与 phase14.mjs 实证 argv 形态逐字一致）；附一句 herdr is_foreground + per-client area 仲裁恢复的效果说明（移动端 attach 不再压缩桌面端面板）。
    3. tmux 对照句：按 behavior 的 latest 措辞书写（红线——不得出现「默认取最小值」字面）。
    4. 资源义务段：从 14-07-SUMMARY 摘 LOADDATA 实测值填标定表（09-09 D-13 表格形态沿用：格名/会话数/内存/fd/goroutine/吞吐列）；「32 并发 shell 是重负载」义务句 + 建议值表（如低配 VPS 建议 --max-clients=8 类分档，默认值 32 经 14-07 数据验证成立则明示「实测可承载」口径；若 14-07 证伪升级则按 one-way 门结果书写——不得与 14-07 结论矛盾）。
    5. 自审：既有 shared 段零改动（git diff 白名单 = 新节与资源义务段新增）；文档内命令/数据与实证源逐字核对一遍；单提交收口（docs(14-11) 前缀）。
  </action>
  <verify>
    <automated>grep -n '会话模式' README.md && grep -c 'window-size' README.md && grep -n 'max-clients' README.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - README.md 含「会话模式」节（grep 命中）且节内含模式语义表三语义行与 herdr 配方 sh 代码块
    - 配方 argv 含 --session-mode=per-client 与 herdr --session（与 phase14.mjs 形态一致）
    - tmux 对照句含 window-size=latest 表述且全文无「默认取最小值」字面（grep 负断言——该字面不得出现在本任务新增段）
    - 标定表数值与 14-07-SUMMARY LOADDATA 行一致（逐项核对）；建议值表在场且默认 32 不动明示
    - git diff 白名单 = README 新节与资源义务段新增（既有段零改动）
  </acceptance_criteria>
  <done>README 主承载落地：模式语义/herdr 配方/标定表/建议值表四位一体，数据全部实测可溯。</done>
</task>

<task type="auto">
  <name>Task 2: docs/ARCHITECTURE.md 双模式段 + :7 误记修正 + docs/CONFIGURATION.md max-clients 行（D-14）</name>
  <reversibility rating="reversible">文档编辑，git 可回退。</reversibility>
  <files>docs/ARCHITECTURE.md, docs/CONFIGURATION.md</files>
  <read_first>
    - docs/ARCHITECTURE.md:1-30（:7 GoTTY 误记原文逐字核读 + :9-21 mermaid graph TD 形态母本）
    - docs/ARCHITECTURE.md 全文结构（双模式段插点选定——会话生命周期/组件拓扑相关章节邻居）
    - docs/CONFIGURATION.md:160-180（默认值表 :165-166 区域与 stop-timeout 双默认值行 :176 区域——max-clients 行同表补位）
    - .planning/research/ARCHITECTURE.md §10 :475-486（资源账面表——双模式段引用的账面口径）与 §12 PC-4 :529（负载矩阵/文档义务枚举）
    - .planning/research/FEATURES.md :111-135（keep/degrade/vanish 映射——双模式分岔说明素材）
    - internal/server/clients.go:110-114（模式常量）与 internal/server/perclient.go（五 goroutine 装配/pcSupervisor——mermaid 拓扑图的事实源）
  </read_first>
  <behavior>
    - ARCHITECTURE.md 双模式段含：装配期一次分岔/运行期零分岔说明（6-7 显式分支点枚举指引）+ goroutine 拓扑 mermaid 图（shared 单会话五 goroutine vs per-client 每会话五 goroutine + pcSupervisor 对比形态）+ keep/degrade/vanish 摘要（哪些机制仅 shared 装配）
    - :7 误记修正：原「GoTTY 式共享进程模型」表述改写为——v1.0 采用共享进程模型（GoTTY 实为 per-connection spawn，此前表述有误）；v1.1 起 wesh 自身支持双模式（shared 默认 / per-client opt-in）
    - CONFIGURATION.md 默认值表补 max-clients per-client 语义行：per-client 下兼任并发进程上限（握手 503 + spawn 前复检，并发子进程数 ≤ max-clients 硬不变量）
    - mermaid 图语法零错误（```mermaid 围栏 + graph TD 形态）
  </behavior>
  <action>
    1. docs/ARCHITECTURE.md 新增双模式架构段（插点按全文结构选定，注释/行文沿用既有段致密中文形态）：分岔说明 + mermaid goroutine 拓扑图（节点含 ReadLoop/inputWriter/writer/pinger/sessionWatcher 五件 + shared 单实例 vs per-client ×N 实例 + pcSupervisor 终结源——事实源 perclient.go）+ keep/degrade/vanish 摘要表（仲裁器/信用门/owner 递补仅 shared 装配）。
    2. :7 误记修正：定位「GoTTY」原句逐字改写为 behavior 所述修正语义（GoTTY 实为 per-connection spawn + 双模式分岔说明）；上下文句连贯性自审。
    3. docs/CONFIGURATION.md 默认值表（:165-166 区域）max-clients 行补 per-client 语义列/行（表格形态沿用现状）；必要时加一行 bullet 分岔说明（:170-178 段形态）。
    4. 自审：REQUIREMENTS.md 零 diff（D-15 注记不改写红线）；mermaid 围栏配对正确；git diff 白名单 = 两文件新增段与 :7 改写行；单提交收口（docs(14-11) 前缀）。
  </action>
  <verify>
    <automated>grep -n 'per-connection' docs/ARCHITECTURE.md && grep -c 'mermaid' docs/ARCHITECTURE.md && grep -n 'max-clients' docs/CONFIGURATION.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/ARCHITECTURE.md 含双模式段与 mermaid goroutine 拓扑图（grep mermaid 命中数增加）与 per-connection spawn 修正表述（grep 命中）
    - :7 区域不再含「GoTTY 式共享进程模型」作为现状事实的表述（误记原文已改写为修正语义——grep 该区段核对）
    - docs/CONFIGURATION.md 默认值表含 max-clients per-client 语义行（grep 命中 per-client）
    - REQUIREMENTS.md 零 diff（D-15 注记不改写）
    - git diff 白名单 = 两文件新增段与误记改写行（既有段零改动）
  </acceptance_criteria>
  <done>ARCHITECTURE 双模式段 + 误记修正 + CONFIGURATION 语义行落地；PC-12 三件套收口。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档面→用户行为 | 模式语义/配方/标定表是用户部署决策的依据面——失实文档即误导面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-23 | Information Disclosure | 文档失实（未实证配方/编造标定数据/失实 tmux 对照）误导用户部署与复测结论 | medium | mitigate | 文档即被测物：配方与 phase14.mjs argv 逐字核对 + 标定表与 14-07 LOADDATA 逐项核对 + tmux 措辞红线 grep 负断言（Task 1/2 验收闸） |
| T-14-24 | Tampering | 误记修正引入新失真（改写 :7 时上下文语义断裂） | low | mitigate | 原文逐字核读后改写 + 上下文连贯性自审（Task 2 步 2/步 4） |
| T-14-SC | Tampering | 依赖面 | high | mitigate | 零新依赖红线——文档面零装包；零 diff 由 14-12 收口闸断言 |
</threat_model>

<verification>
- README「会话模式」节 + 标定表实测回填 + 建议值表（grep 验收闸）
- ARCHITECTURE 双模式段 mermaid + :7 修正；CONFIGURATION max-clients 行（grep 验收闸）
- REQUIREMENTS.md 零 diff；git diff 白名单自审
</verification>

<success_criteria>
1. 模式语义三行 + herdr 配方 + tmux 对照句（latest 措辞）README 落地（PC-12 主承载）
2. 标定表 14-07 实测回填 + 建议值表（D-11 两段式结论一致）
3. GoTTY 误记修正 + 双模式 mermaid 段 + max-clients 语义行（D-14 三件套）
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `README.md`：「会话模式」节（模式语义表/herdr 配方示例/tmux 对照句）+ 资源义务段（pc_flood/pc_resident 标定表 + maxClients 建议值表）
- `docs/ARCHITECTURE.md`：双模式架构段（mermaid goroutine 拓扑图）+ :7 GoTTY 误记修正表述
- `docs/CONFIGURATION.md`：max-clients per-client 语义行

<output>
Create `.planning/phases/14-herdr-uat/14-11-SUMMARY.md` when done
</output>
