---
phase: 14-herdr-uat
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - web/uat/phase14.mjs
autonomous: true
requirements: [PC-13]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "phase14.mjs S1 driving 场景全绿：wesh --session-mode=per-client --writable -- <herdr> --session wesh-uat-p14-<pid> 起独立命名会话（D-09）；桌面端 WS Hello 120x40 + 移动端 40x12 双 attach；wesh 层断言双 session_start 事件双 pid（pid 不等）+ 两端 Welcome session:\"per-client\" + 桌面端 OUTPUT 流几何特征宽于移动端（关系断言）——D-06 wesh 层"
    - "S1 herdr 行为层断言翻转链（spike 标定产物，D-06）：就绪门轮询 herdr api snapshot 至 layouts 非空（禁固定 sleep）；移动 attach 后 area 翻转为移动几何派生（紧凑布局：area.width == 移动端 cols 且 area.x == 0）；桌面端经 wesh INPUT 注入活动后 area 翻回桌面几何派生（全量布局：area.x > 0 且 area.width > 移动端 cols）；移动端 RESIZE 50x20 后 area 为新几何——关系断言形态，禁止硬编码 94/26/39 等 chrome 常量（Pitfall 2；DEFAULT_MOBILE_WIDTH_THRESHOLD=64 为几何分岔钉定点）"
    - "S1 流层互证：移动端 attach 后桌面端只收小增量帧（增量字节数 << 其初始全量帧字节数，关系断言——spike 实测 55B vs 96625B 量级）；移动 resize 后桌面端仍为桌面几何增量更新（非压缩重渲染）"
    - "phase14.mjs S2 ro 汇聚场景全绿（D-08②）：ro 分享链接移动端 attach 经 herdr 汇聚同会话——POST /api/attach 携 share token 换 ticket → Hello JSON 携 ticket 键（唯一通道，Pitfall 8）→ Welcome.mode==='ro' 自检落 check 行（防假绿）；ro 端 INPUT 后 herdr pane read w1:p1 --source visible 内容逐字不变 + 标记串缺席；rw 对照端 echo 标记串后 pane 可见（wesh 门控生效的实证防线——FEATURES 裁决 7 文档叙事防说谎）"
    - "UAT 红线沿用：token/pid 只作断言材料永不进 check detail/控制台输出，assertOutputClean() 运行时自净（phase13.mjs :73-85/:740-746 三代先例同形态）；时序纪律真实等待 + 护栏上限，禁精确时点断言"
    - "清理序列 finally 兜底：关 WS → wesh SIGTERM → herdr session stop <name> → herdr session list 核验（wesh 死后 herdr server 存活是特性，必须显式 stop——RESEARCH §3.5）；用户日常 default 会话零触达"
    - "启动时 execFileSync herdr --version 打进日志（诊断材料非断言——版本漂移先核版本纪律，D-09/RESEARCH OQ1）；标记命令只用最简 echo MARKER_STR 形态（fish/bash/sh 三通，Pitfall 6）"
    - "SC2 不重复断言纪律（D-13）：双 pid/EXIT 不串台/resize 隔离/ro 门控/--once 255/spawn 失败 1011 六项已被 phase11/12/13.mjs 覆盖，本脚本只做 herdr 独有增量 + wesh 层复核，六项重跑证据归 run-all（14-10）承载"
  artifacts:
    - path: "web/uat/phase14.mjs"
      provides: "herdr driving + ro 汇聚协议层 UAT（S1/S2 两场景 + assertOutputClean + exit code 门禁）"
      contains: "wesh-uat-p14-"
  key_links:
    - from: "web/uat/phase14.mjs"
      to: "herdr 0.8.100 socket API"
      via: "HERDR_SOCKET_PATH=~/.config/herdr/sessions/<name>/herdr.sock 定向的 herdr api snapshot（layouts[].area 翻转链）+ herdr pane read（ro 门控观测）——spike 标定形态（RESEARCH §Pattern 3）"
      pattern: "HERDR_SOCKET_PATH"
    - from: "web/uat/phase14.mjs"
      to: "web/uat/phase13.mjs"
      via: "check/skip 门禁/startWesh/dialHello/waitScanPid/pollESRCH/assertOutputClean 同构母本复用（第三代）"
      pattern: "assertOutputClean"
  prohibitions:
    - requirement_id: PC-13
      category: safety
      statement: "不得触碰用户日常 herdr default 会话——全部断言与清理只经 wesh-uat-* 命名会话 + HERDR_SOCKET_PATH 定向（D-09 零污染纪律；default 会话运行中，误停即用户环境事故）"
    - requirement_id: PC-13
      category: safety
      statement: "token/pid 等测试凭据不得进入 check detail 或任何控制台输出——detail 只打状态码/布尔/形状/退出码/文案常量（UAT 红线，违反即信息泄漏面）"
    - requirement_id: PC-13
      category: safety
      statement: "UAT 结束不得残留 herdr server/wesh/洪水子进程——清理序列 finally 兜底 + session list 核验 stopped（资源泄漏即 DoS 面）"
    - requirement_id: PC-13
      category: safety
      statement: "双机拓扑红线：Linux 开发机侧不得安装/运行浏览器或 playwright（headless 硬约束）；Windows 侧不得操作真实网卡模拟断网（一律 TCP 转发器 kill/restore）"
    - requirement_id: PC-13
      category: values
      statement: "tmux 不得充当「恢复正确行为」的证据也不建 tmux 对照 UAT——tmux 压缩是其自身 window-size 语义而非 wesh 缺陷（D-05 既定裁决；对照漂移即 flake 与误判源）"
---

<objective>
PC-13 协议面收口：新建 web/uat/phase14.mjs——herdr driving scenario 协议层双层断言（wesh 层双 pid/winsize 隔离 + herdr 行为层 api snapshot 翻转链）+ ro 汇聚场景（分享链接独立进程入场券 + 输入门控实证）。全部断言行以 14-RESEARCH §Pattern 3 spike 标定产物为准（两轮 live spike + 一轮 wesh×herdr 全链 smoke 已实证形态）。

Purpose: 「per-client 模式下 herdr is_foreground + per-client area 仲裁恢复生效」是 v1.1 里程碑的存在意义（driving scenario）；本脚本是该声明的可证伪协议层证据。
Output: phase14.mjs 两场景全绿 + exit code 门禁 + 清理零残留。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-herdr-uat/14-CONTEXT.md
@.planning/phases/14-herdr-uat/14-RESEARCH.md
@.planning/phases/14-herdr-uat/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase14.mjs 骨架 + S1 driving 场景（wesh 层 + herdr 行为层翻转链，D-05/D-06/D-08①/D-09）</name>
  <reversibility rating="reversible">UAT 脚本新增，git 可回退；herdr 命名会话一次性，清理后零残留。</reversibility>
  <files>web/uat/phase14.mjs</files>
  <read_first>
    - web/uat/phase13.mjs:1-44（四段式头注释母本）与 :67-98（check/skip/emittedDetails/sensitiveTokens/sensitivePids 红线件）与 :106-134（startWesh 真实二进制 spawn：--bind 127.0.0.1 --port 0 + stdout 两行解析 + stderr 持续捕获 + kill 恒 SIGKILL）与 :143-203（dialHello/dialAttach——binaryType='arraybuffer' 纪律已在位）与 :213-218（waitExit）与 :253-297（waitScanPid/pgroupAlive/pollESRCH）与 :740-769（assertOutputClean + 串行场景 + exit code 收口）
    - web/uat/phase05.mjs:251-274（ro ticket 全链：POST /api/attach 携 token 换 ticket → Hello 携 ticket——S2 备用）
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §Architecture Patterns 3（spike 标定产物全文：3.1 会话隔离/清理序列、3.2 翻转链断言表 S1-S5、3.3 流层互证字节量级、3.5 进程形态/pgrep 陷阱、3.6 就绪门）与 §Code Examples phase14.mjs herdr 会话夹具段与 §Pitfalls 2/3/4/6
    - .planning/phases/14-herdr-uat/14-PATTERNS.md §web/uat/phase14.mjs（模式分配与 herdr 增量面）
  </read_first>
  <behavior>
    - 骨架：四段式头注释（场景清单 S1 driving / S2 ro 汇聚 + 红线 + 时序纪律 + 运行方式）；WESH 默认 /tmp/wesh-uat/wesh（argv[2] 覆盖）；HERDR 默认 ~/.local/bin/herdr（argv[3] 覆盖）；SESSION=wesh-uat-p14-<process.pid>；SOCK=~/.config/herdr/sessions/<SESSION>/herdr.sock；启动打 herdr --version 进日志
    - S1：wesh spawn（--writable --session-mode=per-client -- HERDR --session SESSION）→ 桌面端 dialHello 120x40 → 轮询 snapshot 至 layouts 非空（护栏 ~15s）→ 移动端 dialHello 40x12 → 轮询至 area==移动几何派生（紧凑：width==40 且 x==0）→ 桌面端 INPUT 注入活动 → 轮询至 area 翻回桌面几何派生（全量：x>0 且 width>40）→ 移动端 RESIZE 50x20 → 轮询至 area==新几何派生
    - S1 wesh 层：stderr JSON 双 session_start 事件 client_id 关联 + pid 不等；两端 Welcome session==='per-client'；桌面端初始全量帧字节数 >> 移动 attach 后桌面增量帧字节数（关系断言）
    - S1 流层：移动 attach 后桌面增量 << 其初始全量帧（spike 55B vs 96625B 形态——断言增量 < 全量×5% 类关系阈值，禁绝对常量）
    - 清理 finally：关 WS ×2 → wesh SIGTERM → execFileSync(HERDR, ['session','stop',SESSION]) → session list 核验 SESSION 缺席或 stopped；任一步失败不阻断后续清理步
  </behavior>
  <action>
    1. 新建 web/uat/phase14.mjs（零依赖 Node>=22 原生 WebSocket/fetch/child_process/os）：头注释四段式 + imports + WESH/HERDR/SESSION/SOCK 常量装配 + check/skip/emittedDetails/sensitiveTokens/sensitivePids 红线件（phase13.mjs 形态复用）。
    2. helper 层：startWesh（phase13 :106-134 母本，args 尾部加 '--', HERDR, '--session', SESSION）；dialHello（:143-163 母本，binaryType 纪律）；herdrSnap()=JSON.parse(execFileSync(HERDR,['api','snapshot'],{env:{...process.env,HERDR_SOCKET_PATH:SOCK},encoding:'utf8'}))；pollUntil(fn, guardMs) 通用轮询（禁固定 sleep 精确时点，护栏上限只作 FAIL 转换）；areaOf(snap)=snap.result.snapshot.layouts[0].area。
    3. S1 实现按 behavior 时序：几何派生断言写成关系谓词——isCompactFor(area,cols)=(area.width===cols && area.x===0)、isFullFor(area,cols)=(area.x>0 && area.width>compactThresholdCols && area.width<=cols)（compactThresholdCols=40 即移动端 cols——用两端 cols 关系表达，禁 chrome 常量）；翻转链各步 pollUntil 收敛后落 check。
    4. wesh 层断言：stderr 行 JSON.parse 过滤 event==='session_start' 收集两条，client_id 各异 + pid 不等（pid 数值入 sensitivePids 只作断言材料）；两端 Welcome 帧 JSON session 字段==='per-client'；字节计数通道（每端 OUTPUT 帧 payload 累计）支撑流层关系断言。
    5. 清理 finally 序列按 behavior；敏感值自净 assertOutputClean 挂 S2 末统一执行（Task 2），本任务先保证 S1 单场景 node web/uat/phase14.mjs 可跑通（场景数组先挂 S1）。
    6. 前置构建：go build -o /tmp/wesh-uat/wesh ./cmd/wesh（time 前缀）；运行 node web/uat/phase14.mjs 两轮全绿（真实等待含 herder server 惰性启动，单轮估 1-2min——A6）；单提交收口（test(14-08) 前缀）。
  </action>
  <verify>
    <automated>go build -o /tmp/wesh-uat/wesh ./cmd/wesh && node web/uat/phase14.mjs</automated>
  </verify>
  <acceptance_criteria>
    - web/uat/phase14.mjs 存在且 node web/uat/phase14.mjs 退出码 0；输出含 S1 全部 check 行 PASS（翻转链四步 + wesh 层三项 + 流层关系断言）
    - 脚本内 grep 无字面 '94'/'x:26'/96625 等 chrome/spike 常量进断言行（几何断言全为关系谓词形态）
    - 清理后 herdr session list 输出不含 SESSION（手工核一次）；default 会话全程零触达（脚本无 default 字面定向）
    - 断言行无固定 sleep 精确时点（grep 轮询形态可证）；pid/token 数值零进 detail（assertOutputClean 挂接后自证）
    - 两轮运行结果一致（flake 面排除）
  </acceptance_criteria>
  <done>S1 driving 场景两轮全绿：翻转链/流层/wesh 层三通道互证「移动端 attach 桌面端不被压缩」；命名会话隔离与清理序列实证。</done>
</task>

<task type="auto">
  <name>Task 2: S2 ro 汇聚场景 + assertOutputClean 自净 + exit code 收口（D-08②，Pitfall 8 防假绿）</name>
  <reversibility rating="reversible">UAT 脚本新增段，git 可回退。</reversibility>
  <files>web/uat/phase14.mjs</files>
  <read_first>
    - web/uat/phase14.mjs（Task 1 落地骨架——S2 挂接点）
    - web/uat/phase05.mjs:43-49（tokenFromUrl 形态）与 :251-274（ro ticket 全链：POST /api/attach {token} → bodyRO.ticket → dialHello 携 ticket → Welcome.mode 自检）
    - web/uat/phase13.mjs:740-769（assertOutputClean 实现与串行场景收口/exit code 门禁尾段）
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §Architecture Patterns 3.4（ro 汇聚观测通道：ticket 全链 + herdr pane read w1:p1 --source visible + pane send-text）与 §Pitfalls 6/8
  </read_first>
  <behavior>
    - S2：独立 wesh 实例（同 S1 argv 形态，新 SESSION 后缀）→ 启动打印解析 shareRO/shareRW 链接（token 入 sensitiveTokens）
    - rw 桌面端：POST /api/attach {token: rw token} → ticket → dialHello(ticket) → Welcome.mode==='rw' 落 check
    - ro 移动端：POST /api/attach {token: ro token} → ticket → dialHello(ticket) → Welcome.mode==='ro' 自检落 check（mode 不符即假绿防线——Pitfall 8）
    - ro 端发 INPUT（echo RO_MARKER_STR 键序列）→ 护栏等待 → herdr pane read w1:p1 --source visible：内容与 ro 输入前逐字一致 + RO_MARKER_STR 缺席（wesh 服务端丢弃 ro INPUT 的实证）
    - rw 端发 INPUT（echo RW_MARKER_STR）→ pane read 含 RW_MARKER_STR（对照面——pane 观测通道本身工作）
    - assertOutputClean() 遍历 emittedDetails：/s/ 链接形态串/token 值/pid 数值命中即 FAIL；汇总行 + process.exit(failedN===0 && failed===0 ? 0 : 1)
  </behavior>
  <action>
    1. S2 挂进场景数组：复用 Task 1 helper；wesh 实例独立（SESSION 加 -s2 后缀或新 pid 后缀——命名唯一即可）；startWesh 解析 share 链接两行的 token（phase13 :106-134 母本已含 shareRO/shareRW 返回）。
    2. ticket 全链按 phase05.mjs :262-274 形态：fetch POST /api/attach JSON body {token} → resp.json() 取 ticket → dialHello(port, {ticket, cols, rows})；Welcome.mode 双端各自落 check（'rw'/'ro'）。
    3. pane 观测：execFileSync(HERDR, ['pane','read','w1:p1','--source','visible'],{env:{...process.env,HERDR_SOCKET_PATH:SOCK2},encoding:'utf8'})；ro 输入前后各读一次逐字比对 + 标记缺席断言；rw 对照标记可见断言。pane_id 若首窗非 w1:p1（spike 形态外漂移）按 herdr pane list 类通道发现后登记偏差（蓝本外形态不静默跟改）。
    4. 标记命令只用 echo RO_MARKER_STR / echo RW_MARKER_STR 最简形态（Pitfall 6 fish 兼容）；标记串入 sensitiveTokens 同类管理（不进 detail）。
    5. 尾段收口：assertOutputClean() + 场景串行循环（异常纳入 emittedDetails + failed 计数）+ 汇总行 + exit code 门禁（phase13 :748-769 形态）。
    6. node web/uat/phase14.mjs 两轮全绿（S1+S2 全链）；清理核验 session list 双 SESSION 均缺席；单提交收口（test(14-08) 前缀）。
  </action>
  <verify>
    <automated>node web/uat/phase14.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - node web/uat/phase14.mjs 退出码 0；S2 全部 check 行 PASS（Welcome.mode 双自检/ro 逐字不变 + 标记缺席/rw 对照可见）
    - assertOutputClean 挂接在场（grep 可证调用）且运行零命中（输出无自净 FAIL 行）
    - ticket 通道为 POST /api/attach + Hello ticket 键形态（grep 可证；无 WS query 参数携 ticket 形态）
    - 两轮运行结果一致；清理后 herdr session list 不含本脚本两个 SESSION
    - exit code 门禁在场（grep process.exit 形态可证）；任一 check FAIL 时退出码 1（可经临时注入负对照自证后还原，或逻辑核对——择一登记）
  </acceptance_criteria>
  <done>S2 ro 汇聚全绿：ro 链接独立进程入场券 + 输入门控经 pane read 实证（文档叙事防说谎防线落地）；脚本红线自净 + 门禁收口完成。</done>
</task>

</tasks>

<flagged_assumptions>
（plan-phase specless 探针 surfaced 的 unclassified 行——保持 flagged-unverified，不 auto-resolve（#1110））
- **PC-13 (unclassified, unresolved)**：探针 unclassified 行永不 auto-resolve——PC-13 的未分类边界形态（herdr 版本漂移对断言行韧性的影响区间、is_foreground 仲裁在多客户端 >2 或高频活动竞争下的行为面、ro 汇聚下 herdr 侧自身的输入广播语义）未在探针分类中显式覆盖；以需求文本 + D-05..D-09 决策面 + 14-RESEARCH §Pattern 3 spike 标定形态为完整裁决面。执行中若发现该面之外的未覆盖边界（如 herdr 0.8.100 后版本 API 形态漂移）须显式登记而非静默放行。保持 flagged-unverified，由 14-12 收口闸承载终验。
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT 脚本→用户日常 herdr 环境 | 测试驱动真实 herdr server，与用户 default 会话同机——命名会话隔离是唯一防线 |
| UAT 脚本→控制台/日志 | 分享链接 token/ticket/pid 经 check detail 或异常输出的泄漏面 |
| wesh 子进程群→本机资源 | herdr client/server、wesh 实例的进程残留面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-15 | Tampering | herdr 命名会话与用户 default 会话互污染（误停/误连 default） | high | mitigate | D-09 独立会话 + HERDR_SOCKET_PATH 定向 + 跑后 session list 核验；spike 全程实证 default 零触达（RESEARCH §Runtime State Inventory） |
| T-14-16 | Information Disclosure | token/pid 泄入 check detail/控制台 | medium | mitigate | 红线件三通道（sensitiveTokens/sensitivePids/emittedDetails）+ assertOutputClean() 运行时自净（phase13 三代先例同形态） |
| T-14-17 | DoS | herdr server（wesh 死后存活是特性）/wesh/子进程残留 | medium | mitigate | finally 清理序列（WS close → SIGTERM → session stop → list 核验）+ 场景间 300ms 间隔 + 异常纳入计数不跳清理 |
| T-14-SC | Tampering | 依赖面（go.mod/go.sum/lockfile/package.json） | high | mitigate | 零新依赖红线——脚本 Node 原生零依赖，无装包任务，legitimacy 门不触发；零 diff 由 14-12 收口闸断言 |
</threat_model>

<verification>
- go build -o /tmp/wesh-uat/wesh ./cmd/wesh && node web/uat/phase14.mjs 两轮全绿（S1+S2）
- herdr session list 核验零残留；assertOutputClean 零命中
- 断言行关系谓词形态自审（零 chrome 常量）；版本打日志在场
</verification>

<success_criteria>
1. S1 driving：翻转链 + 流层 + wesh 层三通道互证「移动端 attach 桌面端不被压缩」（PC-13 协议面）
2. S2 ro 汇聚：Welcome.mode 自检 + pane read 门控实证（D-08② 防文档说谎防线）
3. 零污染零残留零泄漏（D-09 + UAT 红线 + 清理序列）
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `web/uat/phase14.mjs`（新文件）：S1 driving / S2 ro 汇聚两场景函数；helper 符号 `startWesh`（phase14 形态）/ `dialHello`（复用形态）/ `herdrSnap` / `areaOf` / `pollUntil` / `isCompactFor` / `isFullFor` / `assertOutputClean`；常量 `WESH` / `HERDR` / `SESSION` / `SOCK`；会话名前缀 `wesh-uat-p14-`

<output>
Create `.planning/phases/14-herdr-uat/14-08-SUMMARY.md` when done
</output>
