---
phase: 07-deployment
plan: 08
type: execute
wave: 8
depends_on:
  - "07-07"
files_modified:
  - README.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/07-deployment/07-UAT.md
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-04
  - OPS-05
  - OPS-09
  - OPS-11
  - SEC-07

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "README 含部署与配置完整一节：--config TOML 全键参考与权限建议（chmod 600 + WESH_CREDENTIAL 优先）/ --socket 三 flag 与 systemd 配方 / --base-path nginx 反代配方（含 location = /wesh 精确重定向块与 Upgrade/Connection 头、proxy_read_timeout 与 ping 间隔关系）/ --auth-header+XFF 信任模型与 ttyd -H 模型差异明示 / 子进程管理四 flag / 降权与身份环境改写 / --open / 1001 优雅下线与退出码 255 运维注记"
    - "REQUIREMENTS.md 的 SEC-07 文本按 D-15 修订为服务端审计归因语义（原「作为子进程环境变量」表述作废），Traceability 表保持 Phase 7 映射"
    - "ROADMAP.md Phase 7 成功准则 2 同步修订（D-15 同语义）：「反代注入的可信用户头作为环境变量出现在子进程中」表述作废，改为服务端审计归因语义"
    - "07-UAT.md 人工清单覆盖 VALIDATION 的手动-only 项（真实浏览器 --open、真实 nginx 反代观感、root 降权 nobody 可选场景）与各 plan flagged assumptions 复核项"
    - "全量六段式验证通过：GOROOT gofmt 零漂移 / go vet / go test -race -count=1 ./... / time pnpm -C web build / 裸 clone embed 链 / 启动冒烟；十个 UAT 脚本（phase02..phase07 + 三 jsdom）全绿"
  artifacts:
    - path: "README.md"
      provides: "部署与配置一节（OPS-01/02/04/05/09/11 + SEC-07 模型差异）"
      contains: "--base-path"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SEC-07 文本 D-15 修订"
      contains: "审计归因"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 成功准则 2 的 D-15 同步修订"
      contains: "审计归因"
    - path: ".planning/phases/07-deployment/07-UAT.md"
      provides: "人工 UAT 清单（手动-only 项 + flagged assumptions 复核）"
      min_lines: 40
  key_links:
    - from: "README nginx 配方"
      to: "internal/server base-path 装配与 ping 保活"
      via: "proxy_read_timeout 必须大于 --ping-interval（默认 5s）——反代空闲超时看应用层流量（pinger 注释既有论证）；location = /wesh 精确块是裸路径 404 的唯一修复（RESEARCH A1 待复核项）"
      pattern: "proxy_read_timeout"
  prohibitions: []
---

<objective>
Phase 7 收口：README 部署与配置节（含 SEC-07 的 ttyd -H 模型差异公开承诺——D-15 one-way 交付物）、REQUIREMENTS.md SEC-07 文本修订、07-UAT.md 人工清单、全量六段式验证 + 十脚本回归。

Purpose: 部署特性的正确性一半在文档——nginx 配方错误（裸 /wesh 404、空闲超时断连）是用户第一批会遇到的问题；SEC-07 需求文本修订是 D-15 裁决的正式交付物。
Output: 文档与需求文本一致于实现；全量验证绿，phase 可密封。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-deployment/07-CONTEXT.md
@.planning/phases/07-deployment/07-RESEARCH.md
@.planning/phases/07-deployment/07-VALIDATION.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README 部署与配置节 + REQUIREMENTS.md SEC-07 修订 + ROADMAP.md SC2 同步修订 + 07-UAT.md 人工清单</name>
  <files>README.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/07-deployment/07-UAT.md</files>
  <read_first>
    - README.md（现有结构——认证/TLS 节、多客户端节、生命周期与重连节的文风与详度先例；反代脱敏示例/暴露面清单/标定方法论在 05-09 落地段）
    - .planning/REQUIREMENTS.md（SEC-07 现行文本 L49 与 Traceability 表 L140）
    - .planning/ROADMAP.md（Phase 7 成功准则 2 现行文本——「反代注入的可信用户头作为环境变量出现在子进程中」半句为 D-15 前表述）
    - .planning/phases/07-deployment/07-RESEARCH.md（Pattern 2 nginx 配方 + A1 待复核项；Pattern 5 ttyd -H 模型差异对照——protocol.c:188-189 核实引文；OQ2 accept-255 运维注记）
    - .planning/phases/07-deployment/07-VALIDATION.md（Manual-Only Verifications 表两行）
    - .planning/phases/07-deployment/07-CONTEXT.md（deferred 段——README 需明示的模型差异与信任模型边界）
  </read_first>
  <behavior>
    - README 新增「部署与配置」节（置于生命周期与重连节之后，含八小节：配置文件/UNIX socket/base-path 反代/auth-header 与 XFF/子进程管理/降权运行/--open/优雅下线）
    - 配置文件小节：TOML 形状示例（平铺 key=value）、26 键清单或代表性清单 + 「键名 = flag 名」规则、优先级链 flag > env > 配置文件 > 默认、列表替换语义、逃生门五键不可入配置、chmod 600 建议 + D-07 警告行为、WESH_CREDENTIAL 优先于配置明文
    - base-path 小节：--base-path 值规则（/ 开头无尾斜杠）、307 尾斜杠规范化、nginx 配方两块（`location = /wesh { return 308 /wesh/; }` + `location /wesh/ { proxy_pass http://127.0.0.1:7681; proxy_http_version 1.1; proxy_set_header Upgrade $http_upgrade; proxy_set_header Connection $connection_upgrade; proxy_read_timeout 3600s; }`）、proxy_read_timeout 与 --ping-interval 关系一句、nginx `location /wesh/` 不匹配裸 /wesh 故精确重定向块必需（RESEARCH A1——交付前按 pw/人工复核结论落文；若复核未做则文中标注「精确块为防御性建议」）
    - auth-header 小节：--auth-header 语义（审计归因进 stderr 事件行 remote_user 字段）、XFF 同闸（链首 IP 换日志 remote 与 per-IP 计数键）、裸信任 + 暴露面警告模型、「仅反代后部署」明示、ttyd -H 模型差异段落（ttyd per-connection spawn 可注 env；wesh GoTTY 共享进程模型下 shell 内身份感知结构性不成立——spawn 时无 HTTP 请求、多客户端共享一个 shell；D-15 收窄为服务端审计归因）、伪造头不绕过认证（D-17）
    - 优雅下线小节：SIGTERM/SIGINT → 1001 → stop-signal 序列；退出码 255 注记（子进程被信号终结时 wesh 退出状态 255——P6 OQ1 accept-255 同源；systemd SuccessExitStatus= 由部署侧自决）
    - REQUIREMENTS.md：SEC-07 文本修订为「反代 auth-header 透传：可信 HTTP 头注入的用户名记录进服务端审计日志（remote_user）」——附 D-15 注记（原子进程环境变量语义在 GoTTY 共享进程模型下结构性不成立）；状态勾选 [x] 待验证完成后由 verify 流程处理——本任务只改文本，勾选状态保持 [ ] 不变（验证流程职责）
    - ROADMAP.md：Phase 7 成功准则 2 的「反代注入的可信用户头作为环境变量出现在子进程中」半句同步修订为服务端审计归因语义（与 REQUIREMENTS.md SEC-07 新文本同语义——两处单一口径，防文档间漂移）；Success Criteria 其余半句（base-path 页面与 WS 升级正常）与准则编号保持不动
    - 07-UAT.md：手动-only 两项（VALIDATION 表逐字来源）+ 各 plan flagged assumptions 复核项（OPS-01 并发中断语义/SEC-07 多值头/OPS-04 symlink 与 TERM 任意值/OPS-05 nobody 无 shell 与附加组清空/OPS-09 TOML 语法变体与空 command/OPS-11 macOS open 与 TLS 组合）+ root 降权 nobody 可选场景步骤
  </behavior>
  <action>
    ① README.md 按 behavior 八小节落地（文风对齐既有节——先行为后配方，flag 全名逐字、默认值逐字；全部 flag 名与默认值以 cmd/wesh/main.go 落地实现为准核对一遍——文档与实现漂移是交付红线）。
    ② .planning/REQUIREMENTS.md 的 SEC-07 行文本修订（behavior 给定新文本 + D-15 注记一句）；[ ] 状态保持不动。
    ②b .planning/ROADMAP.md 的 Phase 7 成功准则 2 同步修订（behavior 给定形态——用 Edit 做 scoped 替换，不动任何其他 phase 条目）。
    ③ .planning/phases/07-deployment/07-UAT.md 新建（behavior 清单结构：每项 = 步骤 + 预期 + 勾选框；六项 flagged assumptions 复核逐项列出探针问题原文）。
    提交：`docs(07-08): deployment README, SEC-07 requirement revision, manual UAT checklist`
  </action>
  <verify>
    <automated>grep -c -- '--base-path' README.md && grep -c -- '--socket' README.md && grep -c -- '--auth-header' README.md && grep -c 'proxy_read_timeout' README.md && grep -c '审计归因' .planning/REQUIREMENTS.md && test -f .planning/phases/07-deployment/07-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '--base-path' README.md` ≥ 3、`grep -c -- '--socket' README.md` ≥ 3、`grep -c -- '--auth-header' README.md` ≥ 2、`grep -c 'proxy_read_timeout' README.md` ≥ 1
    - README 含 ttyd 模型差异表述（grep -i 'ttyd' README.md 在部署节上下文命中）
    - `grep -n 'SEC-07' .planning/REQUIREMENTS.md` 显示修订后文本含「审计归因」
    - `grep -c '审计归因' .planning/ROADMAP.md` ≥ 1（Phase 7 SC2 修订落位——文档间单一口径）
    - 07-UAT.md 存在且含 ≥ 8 个勾选项（2 手动-only + 6 探针复核 + 可选场景）
    - README 中出现的全部 flag 名/默认值与 `go run ./cmd/wesh --help` 输出一致（人工逐条比对记录于 SUMMARY）
  </acceptance_criteria>
  <done>
    部署文档与实现一致；SEC-07 需求文本完成 D-15 修订；人工 UAT 清单覆盖全部自动断言不可达项。
  </done>
</task>

<task type="auto">
  <name>Task 2: 全量六段式验证 + 十脚本 UAT 回归 + 启动冒烟</name>
  <files>（仅验证，无文件修改；若 gofmt 漂移则按先例独立 style 提交清零）</files>
  <read_first>
    - .planning/phases/07-deployment/07-VALIDATION.md（Full suite command 与 Sampling Rate）
    - cmd/wesh/main.go（help 输出——Task 1 文档一致性比对的另一侧）
  </read_first>
  <behavior>
    - 段 1：GOROOT gofmt -l 零漂移（/usr/bin/gofmt 陈旧须用 GOROOT 版本——01-03 登记；漂移则逐行核读纯排版后 -w 清零独立 style 提交，02-06/03-06/05-09 三先例）
    - 段 2：`go vet ./...` 退出 0
    - 段 3：`go test -race -count=1 ./...` 全绿
    - 段 4：`time pnpm -C web build` 退出 0 且 dist 与库内一致（git status 干净——产物已入库纪律）
    - 段 5：裸 clone embed 链验证（临时目录 git clone 本仓 → go build → 二进制可启动——embed 内嵌单 HTML 伺服成立；归档前置 rm -rf 防残留，04-06 先例）
    - 段 6：启动冒烟（--port 0 随机端口 + 启动行解析 + GET / 200 + WS 握手 Welcome——复用 phase07.mjs harness 形态或单行脚本）
    - 十脚本：phase02/03/04/05/05-dims/06/07 协议层 + 04-dom/05-dom/06-dom jsdom 各自退出 0
  </behavior>
  <action>
    按 behavior 六段式逐段执行并记录输出于 SUMMARY；任何一段失败即定位修复（属本 phase 预期适配的（如 gofmt 清零）按先例独立提交；非预期失败回上游 plan）。十脚本回归逐个执行。冒烟段验证 --socket 形态顺带复测（unix 监听 + unix:// 打印行）。
    提交（如需）：`style(07-08): gofmt zero-drift sweep`（仅在有漂移时）；验证结果全部记录于 07-08-SUMMARY.md。
  </action>
  <verify>
    <automated>go vet ./... && go test -race -count=1 ./... && time pnpm -C web build && node web/uat/phase07.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 六段式每段退出 0（gofmt -l 输出为空；git status 在 dist 重建后干净）
    - 裸 clone 构建的二进制启动冒烟通过（GET / 200 + Welcome）
    - 十个 UAT 脚本输出全 PASS（逐脚本结果记录于 SUMMARY）
  </acceptance_criteria>
  <done>
    全量验证绿；Phase 7 交付物（代码/测试/文档/UAT）齐备可密封。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档 → operator 部署行为 | README 配方错误会把部署引向不安全形态（裸暴露/断连误判） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-08a | Information Disclosure | README 配方示例泄露真实凭据 | medium | mitigate | 全部示例用占位值（alice:pw 型假凭据/192.0.2.1 文档地址）；示例 TOML 不含真实 secret |
| T-07-08b | Spoofing | nginx 配方漏 auth-header 部署约束 | high | mitigate | 「--auth-header 仅反代后部署」明示 + 暴露面警告机制交叉引用；ttyd -H 模型差异段落防误用预期 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V7 Error Handling/Logging（运维文档完整性） | ✓ 暴露面与退出码语义成文 |
</threat_model>

<verification>
- 六段式全绿 + 十脚本回归全绿
- README flag 名/默认值与 --help 输出逐条一致
- REQUIREMENTS.md SEC-07 修订落位
</verification>

<success_criteria>
- 新 operator 按 README 可完成：配置文件部署（含 chmod 600）、unix socket + systemd、nginx /wesh/ 子路径反代、auth-header 反代归因、降权运行、优雅下线——无需读源码
- Phase 7 可进入 verify 流程
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- README.md「部署与配置」节（八小节）
- .planning/REQUIREMENTS.md SEC-07 修订文本
- .planning/ROADMAP.md Phase 7 成功准则 2 修订文本
- .planning/phases/07-deployment/07-UAT.md（新文件）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-deployment/07-08-SUMMARY.md` when done
</output>
