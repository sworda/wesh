---
phase: 07-deployment
plan: 03
type: execute
wave: 3
depends_on:
  - "07-02"
files_modified:
  - internal/server/proxy.go
  - internal/server/proxy_test.go
  - internal/server/server.go
  - internal/server/auth.go
  - internal/server/clients.go
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
autonomous: true
requirements:
  - SEC-07

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "配置 --auth-header X-Remote-User 后，attach 链路的 logEvent 行携 remote_user=<头值>（经 sanitize：C0/C1/DEL 控制字符剥离 + 128 rune 截断）；未配置或头缺席时日志行与现状逐字节一致（不出 remote_user 键）"
    - "配置 --auth-header 时 X-Forwarded-For 被采信：链中首个 IP（TrimSpace 后非空）成为 logEvent remote 字段与 throttle/halfOpen per-IP 计数键；非法/缺席回退 TCP 对端现状取值"
    - "未配置 --auth-header 时 XFF 完全忽略（直连客户端自设 XFF 无任何效果）——信任闸单一开关 = --auth-header 给定（D-20 零双轨）"
    - "auth-header 值只做记录不做任何认证决定：Basic/ticket/share token 三通道语义零改动，伪造头不能绕过任何认证检查（D-17 正交）"
    - "--auth-header 非空 + bind 非 loopback + 无凭据时 validateStartup 返回 wesh: warning: 前缀的暴露面警告（D-16），凭据/TLS 矩阵其余行语义不变"
    - "token/ticket/凭据任何形态不作为 remote_user 或任何 logEvent 字段出现（D-03 红线延伸，运行时自净断言锁定）"
  artifacts:
    - path: "internal/server/proxy.go"
      provides: "proxyInfo 信任配置 + sanitizeRemoteUser + clientIP/remote/remoteUser 三提取 helper（headers.go/origin.go 同位文件组织先例）"
      contains: "sanitizeRemoteUser"
    - path: "internal/server/proxy_test.go"
      provides: "TestSanitizeRemoteUser/TestProxyClientIP/TestRemoteUserLogging/TestXFFThrottleKey"
      min_lines: 100
    - path: "internal/server/server.go"
      provides: "Options.AuthHeader + Server.proxy 装配 + Attach 消费点换键 + logEvent 可选第四字段"
      contains: "AuthHeader"
    - path: "cmd/wesh/main.go"
      provides: "--auth-header flag + validateStartup D-16 警告行 + Options 接线"
      contains: "auth-header"
  key_links:
    - from: "internal/server/auth.go basicAuth 与 server.go Attach/checkTicket"
      to: "internal/server/throttle.go per-IP 计数器"
      via: "throttle 键必须全部改走 proxyInfo.clientIP——部分换键会使日志归因与节流计数分叉（反代后计数聚合回代理 IP 的旧限制复发）"
      pattern: "clientIP"
    - from: "cmd/wesh/main.go --auth-header flag"
      to: "internal/server New() proxyInfo 装配"
      via: "Options.AuthHeader 生产直传（Credentials/Origins 先例形态）"
      pattern: "AuthHeader"
  prohibitions:
    - statement: "MUST NOT let the auth-header value influence any authentication or authorization decision——提取仅记录（D-17）；伪造头不得绕过 Basic/ticket/share-token 任一检查"
      status: resolved
      verification: judgment
    - statement: "MUST NOT record share token、ticket 或凭据 as remote_user or any logEvent field——D-03 红线随新字段延伸，值经 sanitize 后仍需语义上不可能为 token（提取源只能是配置头名对应的 HTTP 头）"
      status: resolved
      verification: judgment
---

<objective>
落地 SEC-07（D-15 收窄形态）+ X-Forwarded-For（D-20，P3 deferred 兑现）：`--auth-header` 可配头名，HTTP 层提取 remote_user 经 sanitize 后进 logEvent 第四字段（审计归因）；XFF 与 auth-header 共用信任闸，链首 IP 换入 logEvent remote 字段与 throttle/halfOpen per-IP 键。

SEC-07 语义按 D-15 收窄为「服务端审计归因」——GoTTY 共享进程模型下 per-client env 注入结构性不成立（spawn 时无 HTTP 请求、多客户端共享一个 shell）；需求文本修订与 README 模型差异说明在 07-08 落地。

Purpose: 反代挂 SSO（authelia/oauth2-proxy）是部署第一诉求；没有 XFF 换键，反代后 per-IP 节流全聚合为代理 IP，节流与日志归因双双失真。
Output: --auth-header 全链可用（提取/清洗/记录/XFF 换键/暴露面警告），零新认证语义。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deployment/07-CONTEXT.md
@.planning/phases/07-deployment/07-RESEARCH.md
@.planning/phases/07-deployment/07-PATTERNS.md
@internal/server/server.go
@internal/server/auth.go
@internal/server/throttle.go
@internal/server/headers.go
@internal/server/origin.go
@cmd/wesh/main.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: proxy.go 提取层 + logEvent remote_user 第四字段 + --auth-header flag + D-16 警告</name>
  <reversibility rating="one-way">D-15/D-18：--auth-header 是 CLI 公开契约；SEC-07 收窄是需求文本修订 + README 公开承诺的模型差异（env 注入需 attach 时 spawn 的架构变更才能回退）——用户已在 07-CONTEXT 锁定，记录评级不重复设卡。</reversibility>
  <files>internal/server/proxy.go, internal/server/proxy_test.go, internal/server/server.go, internal/server/auth.go, internal/server/clients.go, cmd/wesh/main.go, cmd/wesh/main_test.go</files>
  <read_first>
    - internal/server/server.go（logEvent L959-977 三要素与红线注释、Attach L561-602 守卫区、issueTicketJSON L452-466、checkTicket L876-899、Options/New L172-317）
    - internal/server/auth.go（basicAuth L94-116：clientIP/throttle/logEvent 三消费点）
    - internal/server/clients.go（client struct remote 字段 L72 区段、kickSlowConsumerLocked 的 logEvent 调用点）
    - internal/server/headers.go（中间件形态先例）、internal/server/origin.go（同位文件组织先例：包级纯函数 + 注释头登记决策依据）
    - internal/server/throttle.go（allow/recordFail 键语义 L57-85）
    - cmd/wesh/main.go（flag 注册区段、validateStartup 警告通道 L399-410）
    - web/src/lib/title.ts（C0/C1+128 sanitize 纪律的 TS 先例，Go 移植对照）
    - .planning/phases/07-deployment/07-RESEARCH.md（Pattern 5 逐字配方 + Pitfall 7）
  </read_first>
  <behavior>
    - sanitizeRemoteUser：逐 rune 剥离 ch<=0x1f、ch==0x7f、0x80<=ch<=0x9f；截断 128 rune；多字节 rune 不碎；空结果返回空串（缺席即不出键，与标题回退 'wesh' 的差异点）
    - proxyInfo{trust bool, userHeader string}：零值 = 不信任/无头名（行为与现状逐字节一致）
    - proxyInfo.clientIP(r)：trust 且 XFF 头非空 → 链首 IP（strings.Cut(",") 首段 + TrimSpace，空则落回退）；否则 net.SplitHostPort(r.RemoteAddr) host 部分，失败回退整串（现状语义）
    - proxyInfo.remote(r)：trust → clientIP(r)（XFF 换入 logEvent remote 字段，D-20）；否则 r.RemoteAddr 原样（现状 host:port 形态）
    - proxyInfo.remoteUser(r)：trust 且头存在 → sanitizeRemoteUser(头值)；否则 ""
    - logEvent 签名变可选第四参（variadic `remoteUser ...string`）：非空时行尾追加 ` remote_user=<u>`；全部既有调用点零改动编译通过
    - Server 装配：Options.AuthHeader 生产直传 → s.proxy = proxyInfo{trust: opts.AuthHeader != "", userHeader: opts.AuthHeader}
    - 消费点换键（D-20 范围锁定）：basicAuth 的 ip/remote/401/429 logEvent；Attach 的 ip（halfOpen/checkTicket 键）与 remote；issueTicketJSON 的 503 logEvent；client struct 加 remoteUser 字段（Attach 入口提取一次），clients.go 的会话事件 logEvent（kick/pong_timeout 等）携该字段
    - --auth-header flag 注册 + validateStartup D-16 警告行：authHeader != "" && 无凭据 && bind 非 loopback && socket 未配置 → warn「auth-header 可被直连伪造，确保 wesh 不直接暴露」（wesh: warning: 前缀先例；socket 形态 bind 矩阵已跳过，同行跳过本警告——unix socket 信任边界同 D-11 逻辑）
    - main_test.go TestStartupMatrix 新行：D-16 警告触发（非 loopback + 无凭据 + auth-header）与不触发（loopback + auth-header；非 loopback + 凭据 + auth-header）
  </behavior>
  <action>
    ① internal/server/proxy.go【新】（origin.go 同位文件组织纪律：注释头登记 D-15..D-20 决策依据与 token 红线延伸）：
    - `sanitizeRemoteUser(s string) string`（RESEARCH Pattern 5 逐字 Go 形态）
    - `type proxyInfo struct { trust bool; userHeader string }` + `clientIP(r *http.Request) string` / `remote(r *http.Request) string` / `remoteUser(r *http.Request) string` 三方法（RESEARCH Pattern 5 配方）
    ② internal/server/server.go：
    - 删除包级 `clientIP(r)` 自由函数（L519-529），消费点全部改走 proxyInfo 方法
    - Options 加 `AuthHeader string`（生产直传分档注释）；Server 加 `proxy proxyInfo` 字段，New 装配（behavior 给定形态）
    - logEvent 改 variadic 第四参（behavior 形态；红线注释 L970-974 扩展：remote_user 必须经 sanitize、值来源只能是配置头名的 HTTP 头——token/ticket/凭据永不入参红线逐字保持）
    - Attach（L577,587）：`ip := s.proxy.clientIP(r)`、`remote := s.proxy.remote(r)`；client 构造点（注册表登记处，clients.go）加 `remoteUser: s.proxy.remoteUser(r)`
    - issueTicketJSON（L454）：logEvent 调用改 `s.proxy.remote(r)` + remoteUser 第四参
    ③ internal/server/auth.go：basicAuth 签名加 `p proxyInfo` 参数；`ip := p.clientIP(r)`；401/429 logEvent 改 `p.remote(r)` + `p.remoteUser(r)` 第四参；Handler() 两处 basicAuth 调用点传 s.proxy；auth_test.go 既有直接构造 basicAuth 的调用点补零值 proxyInfo{}（行为不变）
    ④ internal/server/clients.go：client struct 加 `remoteUser string` 字段（remote 字段 L72 同位注释）；kickSlowConsumerLocked 与 pong_timeout 两处 logEvent 调用携 c.remoteUser 第四参
    ⑤ cmd/wesh/main.go：config struct 加 `authHeader string`（Phase 7 组注释 D-18）；flag 注册 `--auth-header`（help: trusted reverse-proxy user header name (e.g. X-Remote-User); logged as remote_user, no auth effect）；validateStartup 加 D-16 警告行（behavior 条件与文案——文案含 --auth-header flag 名，不含任何头值）；run() Options 接线 `AuthHeader: cfg.authHeader`
    ⑥ internal/server/proxy_test.go【新】：
    - TestSanitizeRemoteUser 表驱动（C0/DEL/C1 剥离、128 截断、多字节不碎、空串）
    - TestProxyClientIP（trust on/off × XFF 多值/单值/空/缺席 × 回退形态）
    - TestRemoteUserLogging：startTrackedServerWith 起 Options{AuthHeader: "X-Remote-User"} 实例，携头 attach 后断言 stderr 捕获行含 ` remote_user=alice` 子串；不携头断言无 `remote_user=` 子串（captureStderr 复用 limits_test.go 既有 helper）
    - TestXFFThrottleKey：Options{AuthHeader: "X-Remote-User", Credentials: 一组} 实例，两不同 XFF 首 IP 各发凭据失败请求，断言各自独立计数（同一 XFF 连续失败触发 429 而另一 XFF 不受影响——throttle 键已换 XFF）；对照组（无 AuthHeader）两 XFF 共享计数（回退代理 IP 键）
    - TestAuthHeaderNoAuthBypass：携伪造 X-Remote-User: root 头、无凭据请求 /api/attach → 401 照旧（D-17 正交性回归锁）
    提交：`feat(07-03): auth-header extraction, remote_user logging, and XFF trust gate`
  </action>
  <verify>
    <automated>go test ./internal/server -run 'TestSanitizeRemoteUser|TestProxyClientIP|TestRemoteUserLogging|TestXFFThrottleKey|TestAuthHeaderNoAuthBypass' -count=1 && go test ./internal/server ./cmd/wesh -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - 五个新测试全部 PASS；`go test ./internal/server -count=1` 全绿（既有套件对 variadic logEvent 与 proxyInfo 零值零回归）
    - `grep -c 'func sanitizeRemoteUser' internal/server/proxy.go` == 1
    - `grep -c 's.proxy.clientIP\|p.clientIP' internal/server/server.go internal/server/auth.go` ≥ 2（两处 per-IP 键换入）
    - `grep -c 'p.remote(r)' internal/server/auth.go` ≥ 2（basicAuth 的 401/429 两处 logEvent remote 全换 proxyInfo.remote——旧 RemoteAddr 直取在 auth.go 零残留由本正向断言与全绿测试共同锁定）
    - `go test ./cmd/wesh -count=1` 全绿（D-16 矩阵行）
  </acceptance_criteria>
  <done>
    --auth-header 提取/sanitize/记录/XFF 换键/暴露面警告全链落地；伪造头不绕过认证的回归锁成立；未配置时日志行与 per-IP 键语义与现状逐字节一致。
  </done>
</task>

<task type="auto">
  <name>Task 2: share token 通道同口径提取 + server 全量回归（-race）</name>
  <files>internal/server/server.go, internal/server/proxy_test.go</files>
  <read_first>
    - internal/server/server.go（shareAttach L413-437、attachHandler L474-485、issueTicketJSON L452-466——两签发通道唯一共享点）
    - internal/server/sharetoken.go（D-03 红线注释 L11-14）
  </read_first>
  <behavior>
    - share token 通道（/s/ 页面与 /api/attach 携 token 签发）的 attach 事件同样携 remote_user（CONTEXT discretion 倾向一致提取落地——sharePage/shareAttach 与 Basic 链同走 s.proxy 提取点，无需新代码路径则补测试锁定）
    - issueTicketJSON 的 max_clients 503 事件在两签发通道（Basic 链与 token 分支）均携 remote_user（Task 1 单点改造的自然覆盖，测试双通道断言）
    - `go test -race -count=1 ./internal/server` 全绿
  </behavior>
  <action>
    核验 Task 1 改造的通道完整性：shareAttach → issueTicketJSON 与 attachHandler → issueTicketJSON 共享同一签发点，Task 1 的 503 logEvent 改造天然覆盖双通道——本任务补双通道测试断言锁定（proxy_test.go 扩展：token 通道签发/503 事件携 remote_user、Basic 通道同款），并把 Attach 入口提取点（remote/remoteUser）对 token 渠道进入的 WS attach 同样生效的事实写入注释（share token 渠道进入的客户端其 remoteUser 来自同一 Attach 提取点）。
    随后跑 `go test -race -count=1 ./internal/server ./cmd/wesh` 全量竞态回归（client struct 新字段的并发读写面：remoteUser 在 Attach 入口写一次、此后只读——与 remote 字段既有并发形态相同，注释登记该论证）。
    如发现 Task 1 遗漏的消费点（grep 复查 clientIP/r.RemoteAddr 残留），同 Task 1 形态补齐。
    提交：`test(07-03): share-channel remote_user coverage and race regression`
  </action>
  <verify>
    <automated>go test -race -count=1 ./internal/server ./cmd/wesh && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race -count=1 ./internal/server ./cmd/wesh` 退出 0
    - proxy_test.go 含 share/token 通道的 remote_user 断言 ≥ 1 处
    - `grep -rn 'clientIP(' internal/server --include='*.go' | grep -v '_test.go' | grep -v 'proxy.go' | wc -l` == 0（旧自由函数零残留调用）
  </acceptance_criteria>
  <done>
    remote_user 提取在 Basic/share token 双通道同口径；-race 全量绿；clientIP 旧自由函数调用零残留。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reverse-proxy → wesh (HTTP headers) | X-Remote-User/X-Forwarded-For 头是不可信输入——只有 operator 配置 --auth-header 且部署保证 wesh 不直连暴露时才可采信 |
| wesh → stderr (logEvent) | remote_user 是用户可控字段，单行文本日志面临控制字符注入伪造日志行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03a | Spoofing | XFF/auth-header 采信 | high | mitigate | 裸信任 + D-16 暴露面启动警告（非 loopback 无凭据 stderr 醒目）+ D-17 零认证决定（伪造头只能污染日志不能越权）+ README「--auth-header 仅反代后部署」明示（07-08）；--trusted-proxy CIDR 列 deferred |
| T-07-03b | Tampering | remote_user 日志注入 | medium | mitigate | D-19：C0/C1/DEL 控制字符剥离 + 128 rune 截断（title.ts 同款纪律 Go 移植）；logEvent 单行形态下伪造日志行结构性消除 |
| T-07-03c | Information Disclosure | remote_user 误录 token | high | mitigate | 提取源限定为配置头名对应 HTTP 头（结构性不可能是 /s/ 路径 token 或 Hello ticket）；D-03 红线注释延伸 + 07-07 UAT assertOutputClean 运行时自净 |
| T-07-03d | Denial of Service | XFF 换键后节流被绕 | medium | accept | 攻击者轮换 XFF 首 IP 可获独立节流配额——D-20 已裁决接受（反代部署语义正确性优先）；throttle 凭据模式才构造，无凭据反代部署本无节流面 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V2 Authentication（无新认证语义，D-17 正交） | ✓ 三通道零改动回归锁 |
| V5 Input Validation（头值 sanitize） | ✓ D-19 测试锁定 |
| V7 Error Handling/Logging（日志注入防线） | ✓ sanitize + 红线延伸 |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/server ./cmd/wesh` 全绿
- 五个新测试 + TestStartupMatrix D-16 行全 PASS
- 既有 auth/handshake/limits 套件零回归（variadic logEvent 与 proxyInfo 零值行为守恒）
</verification>

<success_criteria>
- `wesh --bind 127.0.0.1 --auth-header X-Remote-User -- bash`：curl 携 `X-Remote-User: alice` 与 `X-Forwarded-For: 203.0.113.7, 10.0.0.1` 完成 attach 后，stderr 事件行含 `remote=203.0.113.7` 与 ` remote_user=alice`
- 同形态不配置 --auth-header：相同请求日志行为现状（remote=127.0.0.1:port，无 remote_user 键）
- 伪造头（X-Remote-User: root）在凭据模式下收 401 照旧
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **SEC-07 (unclassified, unresolved)**：边缘探针未能归类该需求（D-15 收窄后语义为「服务端审计归因」）——人工复核项：多值头（重复 X-Remote-User 头行）取 Header.Get 首值；空串头值 → sanitize 后空 → 不出键（与缺席同态）；这两点由 proxy_test.go 表驱动覆盖，07-08 人工 UAT 清单复核。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `sanitizeRemoteUser(s string) string`（internal/server/proxy.go，新文件）
- `type proxyInfo struct{ trust bool; userHeader string }` + 方法 `clientIP(r)` / `remote(r)` / `remoteUser(r)`（internal/server/proxy.go）
- `Options.AuthHeader string` + Server 字段 `proxy proxyInfo`（internal/server/server.go）
- logEvent variadic 第四参 `remoteUser ...string`（internal/server/server.go）
- client struct 字段 `remoteUser string`（internal/server/clients.go）
- basicAuth 新签名 `basicAuth(next, creds, th, p proxyInfo)`（internal/server/auth.go）
- flag `--auth-header` + config 字段 `authHeader`（cmd/wesh/main.go）
- `TestSanitizeRemoteUser` / `TestProxyClientIP` / `TestRemoteUserLogging` / `TestXFFThrottleKey` / `TestAuthHeaderNoAuthBypass`（internal/server/proxy_test.go，新文件）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-deployment/07-03-SUMMARY.md` when done
</output>
