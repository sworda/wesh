---
phase: 07-deployment
plan: 04
type: execute
wave: 4
depends_on:
  - "07-03"
files_modified:
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
  - internal/pty/spawn.go
  - internal/pty/spawn_test.go
  - internal/pty/signal_linux.go
  - internal/pty/signal_darwin.go
  - internal/server/server.go
  - internal/server/clients.go
autonomous: true
requirements:
  - OPS-04
  - OPS-05

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "wesh --cwd /tmp --term vt100 -- bash 的子进程以 cwd=/tmp、TERM=vt100 启动（落 cmd.Dir 与 env 白名单 TERM= 行）；--cwd 目录不存在时 validateStartup 预检 fail-fast exit 2（spawn 前零资源占用）"
    - "--stop-signal TERM --stop-timeout 2s 时，断开退出收口路径向子进程进程组（负 pid）发 SIGTERM，2s 后仍存活补 SIGKILL（ESRCH 幂等静默）；默认 --stop-signal HUP、--stop-timeout 0（不补 KILL 纯单信号）与现状语义一致"
    - "停止信号只发子进程进程组（setsid 使 pgid == 子进程 pid 既定不变量），Close master 的内核 SIGHUP 免费通道保留为兼容底层"
    - "wesh --uid U --gid G -- bash 的子进程以该 uid/gid 运行（SysProcAttr.Credential，fork 后 exec 前）；只给一个 validateStartup fail-fast exit 2"
    - "降权后子进程 HOME/USER/LOGNAME 按目标 uid 的 passwd 条目改写；查不到条目时三键从白名单剔除（shell 自默认）；SEC-06 替换式注入纪律不变（严禁 os.Environ() 追加）"
    - "未配置全部四 flag 时 pty.Start 行为与现状逐字节一致（Dir 空 = 继承、Term 空 = xterm-256color、Uid -1 = 不降权）"
  artifacts:
    - path: "internal/pty/spawn.go"
      provides: "StartOptions{Dir,Term,Uid,Gid} + Start(argv, opts) + whitelistEnv(term, uid) 身份改写"
      contains: "StartOptions"
    - path: "internal/pty/signal_linux.go"
      provides: "SignalGroup(sig) 泛化 + StopSignalByName 名→信号映射（darwin 对件同签名）"
      contains: "SignalGroup"
    - path: "cmd/wesh/main.go"
      provides: "--cwd/--term/--stop-signal/--stop-timeout/--uid/--gid 六 flag + validateStartup 三新行 + pty.Start 接线 + server.Options stop 字段接线"
      contains: "stop-signal"
    - path: "internal/server/clients.go"
      provides: "exit-when-empty 两触发点的 stop-signal 序列换入（SignalHangup → SignalGroup(stopSignal) + KILL 补发）"
  key_links:
    - from: "cmd/wesh/main.go --stop-signal/--stop-timeout"
      to: "internal/server Options.StopSignal/StopTimeout → clients.go maybeExitWhenEmptyLocked"
      via: "退出收口的信号配置必须经 Options 单一通道——07-05 的 Shutdown 复用同一字段，双写即漂移"
      pattern: "StopSignal|StopTimeout"
    - from: "cmd/wesh/main.go --uid/--gid"
      to: "internal/pty Start → SysProcAttr.Credential"
      via: "creack/pty StartWithSize 只补 Setsid/Setctty 不覆盖调用方 SysProcAttr（GOMODCACHE start.go:18-25 核实兼容）"
      pattern: "Credential"
  prohibitions:
    - statement: "MUST NOT send the stop-signal to any process outside the child's process group——负 pid 组信号 + setsid pgid==pid 不变量；KILL 补发 ESRCH 幂等（子进程死后 wesh 立即退出，PID 复用窗口极小已裁决接受）"
      status: resolved
      verification: judgment
    - statement: "MUST NOT silently run the child at the original privilege when the drop is half-configured——uid/gid 成对强制；配置矛盾零窗口暴露（exit 2 而非降级运行）"
      status: resolved
      verification: judgment
---

<objective>
落地 OPS-04（子进程 cwd/TERM/停止信号可配，D-21/D-22）+ OPS-05（uid/gid 降权，D-24/D-25）：pty.Start 选项化承载四新参，SignalHangup 泛化为 SignalGroup，exit-when-empty 收口换入可配 stop-signal 序列（含 stop-timeout 后 SIGKILL 补发），SysProcAttr.Credential 降权 + whitelistEnv 身份环境改写。

两需求同属 pty spawn/stop 子系统（spawn.go 注释预留挂点逐字兑现），合一 plan。

Purpose: 运维场景要求子进程工作目录/终端类型/停止行为可配（systemd 服务管理）、降权运行（root 启动 wesh 服务、子进程跑 nobody）；HOME 指向原用户家目录是降权开箱即坑（Pitfall 6）。
Output: 六 flag 全链可用，停止信号进程组语义锁定，降权身份环境直觉语义成立。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deployment/07-CONTEXT.md
@.planning/phases/07-deployment/07-RESEARCH.md
@.planning/phases/07-deployment/07-PATTERNS.md
@internal/pty/spawn.go
@internal/pty/signal_linux.go
@internal/pty/signal_darwin.go
@internal/server/clients.go
@cmd/wesh/main.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pty.Start 选项化（Dir/Term）+ --cwd/--term flag + --cwd stat 预检</name>
  <reversibility rating="one-way">D-21：--cwd/--term 是 CLI 公开契约（P2 D-15 纪律）——用户已在 07-CONTEXT 锁定，记录评级不重复设卡。</reversibility>
  <files>internal/pty/spawn.go, internal/pty/spawn_test.go, cmd/wesh/main.go, cmd/wesh/main_test.go</files>
  <read_first>
    - internal/pty/spawn.go（Start L43-58 与 cmd.Dir 注释预留位 L49-50、whitelistEnv L63-85 与 TERM= 行 L65）
    - internal/pty/spawn_test.go（env 白名单既有断言形态）
    - cmd/wesh/main.go（flag 注册区段、validateStartup L368-412 纯函数纪律注释 L362-367、run() pty.Start 调用 L452）
    - .planning/phases/07-deployment/07-RESEARCH.md（Pattern 6 StartOptions 配方）
  </read_first>
  <behavior>
    - `StartOptions{Dir, Term string; Uid, Gid int}`：Dir 空 = 继承服务端 cwd（exec.Cmd 零值语义）；Term 空 = "xterm-256color"；Uid -1 = 不降权（本任务只落 Dir/Term 消费，Uid/Gid 字段与 Credential 分支在 Task 3 落地——结构体一次定义）
    - `Start(argv []string, opts StartOptions)`：cmd.Dir = opts.Dir；whitelistEnv(opts.Term, opts.Uid) 的 TERM= 行参数化
    - 既有调用点全部机械适配：cmd/wesh/main.go:452、server 包测试 helper（e2e_test.go startTestServerWith 等）、pty 包测试——编译即契约
    - --cwd 非空且 os.Stat 失败（不存在/非目录）→ validateStartup 返回错误 exit 2（值非敏感可回显路径）
    - --term 空串值（--term=""）按未配置处理（空 = 默认 xterm-256color，防止显式空 TERM 使终端能力丢失）
    - spawn_test.go 新增：opts.Dir 落 cmd.Dir 断言、opts.Term 落 env TERM= 行断言、opts 零值与现状等价断言（env 白名单既有断言全绿）
  </behavior>
  <action>
    ① internal/pty/spawn.go：新增 StartOptions 结构体（注释登记 D-21/D-24 决策号与 creack/pty StartWithSize 兼容链 GOMODCACHE start.go:18-25）；Start 签名改 `Start(argv []string, opts StartOptions)`——cmd.Dir = opts.Dir（替换 L49-50 注释预留位为实装，注释改写为 OPS-04 已兑现）；whitelistEnv 签名改 `whitelistEnv(term string, uid int)`——TERM= 行用 term 参数（空则 "xterm-256color"），uid 参数本任务只接不消费（Task 3 落地身份改写，签名一次成型避免二次改调用点）。
    ② 调用点机械适配（grep -rn 'pty.Start(' 全仓定位）：main.go:452 传 `pty.StartOptions{Dir: cfg.cwd, Term: cfg.term, Uid: cfg.uid, Gid: cfg.gid}`（cfg 字段 Task 1/3 陆续接线，本任务先落 Dir/Term 两字段与 -1 哨兵 uid/gid 字段声明）；测试 helper 统一传零值等价形态 `pty.StartOptions{Uid: -1, Gid: -1}`（注释标注零值语义）。
    ③ cmd/wesh/main.go：config struct 加 `cwd string`、`term string`（Phase 7 子进程组注释 D-21）；flag 注册 `--cwd`（默认 ""，help: working directory for the child process (default: inherit)）、`--term`（默认 ""，help: TERM for the child process (default: xterm-256color)）；validateStartup 加 --cwd 预检行（os.Stat 只读检查不违反纯函数纪律——更新 L362-367 注释为「无副作用：禁止 listen/spawn/写文件；os.Stat 只读探测允许」）；uid/gid 字段以 -1 默认声明（Task 3 完成 flag 注册与校验）。
    ④ spawn_test.go 扩展 + main_test.go TestParseArgs 新行（--cwd/--term 解析）与 TestStartupMatrix 新行（--cwd 不存在拒绝、--cwd 合法放行——t.TempDir）。
    提交：`feat(07-04): StartOptions Dir/Term with --cwd/--term flags`
  </action>
  <verify>
    <automated>go test ./internal/pty ./cmd/wesh -count=1 && go build ./... && go test ./internal/server -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/pty -count=1` 全绿（新断言 + 既有 env 白名单断言）
    - `go test ./internal/server -count=1` 全绿（Start 签名变化的调用点适配零回归——server 套件是 pty.Start 的最大消费方）
    - `grep -c 'type StartOptions struct' internal/pty/spawn.go` == 1
    - `go test ./cmd/wesh -count=1` 全绿（新 flag 行 + stat 预检行）
  </acceptance_criteria>
  <done>
    --cwd/--term 全链可用且 stat 预检 fail-fast；pty.Start 选项化完成且全部既有调用点适配；零值语义与现状逐字节一致（server/pty 全套件绿）。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SignalGroup 泛化 + --stop-signal/--stop-timeout + exit-when-empty 收口换入 stop 序列</name>
  <reversibility rating="one-way">D-22：--stop-signal/--stop-timeout 是 CLI 公开契约（P2 D-15 纪律）——用户已在 07-CONTEXT 锁定，记录评级不重复设卡。</reversibility>
  <files>internal/pty/signal_linux.go, internal/pty/signal_darwin.go, internal/pty/spawn_test.go, cmd/wesh/main.go, cmd/wesh/main_test.go, internal/server/server.go, internal/server/clients.go</files>
  <read_first>
    - internal/pty/signal_linux.go（SignalHangup L15-17 逐字 + ESRCH 幂等注释纪律 L7-14）、internal/pty/signal_darwin.go（同签名对件）
    - internal/server/clients.go（maybeExitWhenEmptyLocked L706-760：SignalHangup 两调用点 L739/L756、宽限计时器形态）
    - internal/server/server.go（Options L172-196、New 零值兜底先例 L222-254、signalName L997-1027——注意方向：signal→name，本任务需 name→signal 反向表，不复用错方向）
    - cmd/wesh/main.go（exitEmptyValue 自定义 Value 先例 L68-102——--stop-timeout 取 DurationVar 直收形态，见 action）
    - .planning/phases/07-deployment/07-RESEARCH.md（Pattern 6 stop-signal 序列 + Pitfall 8 可中断计时）
  </read_first>
  <behavior>
    - `SignalGroup(sig syscall.Signal)`：负 pid 进程组发 sig，错误全静默（ESRCH 幂等注释纪律逐字沿用）；linux/darwin 同签名
    - `StopSignalByName(name string) (syscall.Signal, bool)`：HUP/TERM/INT/KILL 四枚举命中，其他 false；两平台文件同签名同表（4 项表的平台对件纪律——SignalHangup 注释同款逐字沿用声明）
    - --stop-signal 非法值（小写/未知名）parse 期 exit 2，文案列合法枚举；--stop-timeout 负值 parse 期 exit 2（DurationVar 直收 + 负值检查，exitEmptyValue.Set 负值闸同纪律）
    - server.Options 加 StopSignal（零值兜底 SIGHUP——零信号值无语义，New 显式兜底）/StopTimeout（零值 = 不补 KILL 合法，无兜底）
    - clients.go maybeExitWhenEmptyLocked 两触发点（立即 L739 与宽限到期 L756）：`s.sess.SignalHangup()` → `s.sess.SignalGroup(s.stopSignal)`；s.stopTimeout > 0 时随后 `time.AfterFunc(s.stopTimeout, func() { s.sess.SignalGroup(syscall.SIGKILL) })`（异步补发不占 hubMu，ESRCH 幂等使子进程早死无害——Pitfall 8 纪律：不用 sleep 阻塞；KILL 到达空 pgid 静默）
    - SignalHangup 方法删除（grep 全仓确认两调用点已换、测试无直接调用；若 emptyexit_test.go 等有直接调用则机械换 SignalGroup(syscall.SIGHUP)）
    - 测试：exit-when-empty 场景断言子进程被 TERM 终结（--stop-signal TERM 配置经 Options 直传，emptyexit_test.go 既有宽限测试形态沿用）；stopTimeout 补 KILL 场景（子进程 trap 忽略 TERM——sh -c 'trap "" TERM; sleep 100'——AfterFunc KILL 后 sess.Wait 返回）
  </behavior>
  <action>
    ① internal/pty/signal_linux.go + signal_darwin.go：SignalHangup 改写为 `SignalGroup(sig syscall.Signal)`（注释首行泛化「向子进程进程组发指定信号」，ESRCH/负 pid/setsid/不触 fdMu/平台对件五段注释纪律逐字沿用）；同文件加 `StopSignalByName`（HUP→syscall.SIGHUP、TERM→SIGTERM、INT→SIGINT、KILL→SIGKILL switch；注释登记与 server.go signalName 的方向差异——名→信号反向表不复用）。
    ② cmd/wesh/main.go：config struct 加 `stopSignal string`、`stopTimeout time.Duration`（D-22 注释）；flag 注册 `--stop-signal`（StringVar 默认 "HUP"，help 列 HUP|TERM|INT|KILL 枚举与默认 HUP 现状语义）、`--stop-timeout`（DurationVar 默认 0，help: grace before SIGKILL after stop-signal (0 = no escalation)）；Parse 返回处校验段：`pty.StopSignalByName` 解析（失败 → `invalid --stop-signal %q: must be HUP, TERM, INT or KILL`）、stopTimeout < 0 拒绝；解析产物 `stopSignalSig syscall.Signal` 入 cfg（新增字段）。run() Options 接线加 `StopSignal: cfg.stopSignalSig, StopTimeout: cfg.stopTimeout`。
    ③ internal/server/server.go：Options 加 `StopSignal syscall.Signal`、`StopTimeout time.Duration`（06-02 注释分档先例——生产直传 + 零值兜底说明）；New 加 `if opts.StopSignal == 0 { opts.StopSignal = syscall.SIGHUP }`（零信号无语义，兜底保持 D-22 默认 HUP 现状语义）；Server 加 `stopSignal`/`stopTimeout` 字段装配（07-05 Shutdown 复用同字段，注释标注）。
    ④ internal/server/clients.go：behavior 两触发点换入；KILL 补发注释登记 Pitfall 8（不与 lifecycle 协调、ESRCH 幂等静默、不占 hubMu）。
    ⑤ SignalHangup 删除与残留清理（grep 确认）；main_test.go TestParseArgs 新行（--stop-signal 四枚举 + 非法值 + --stop-timeout 负值/正值）；emptyexit_test.go（或新 stopseq_test.go）行为测试组按 behavior 落地——TERM 终结与 KILL 补发两场景。
    提交：`feat(07-04): configurable stop-signal sequence for process group shutdown`
  </action>
  <verify>
    <automated>go test ./internal/pty ./internal/server ./cmd/wesh -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/server -run 'TestExitWhenEmpty|TestStop' -count=1` 相关场景全绿（TERM 终结 + KILL 补发）
    - `grep -rn 'SignalHangup' --include='*.go' . | wc -l` == 0（旧方法零残留）
    - `grep -c 'func (s \*Session) SignalGroup' internal/pty/signal_linux.go internal/pty/signal_darwin.go` 各自 == 1
    - `go test ./internal/pty ./cmd/wesh -count=1` 全绿
  </acceptance_criteria>
  <done>
    stop-signal 序列可配且默认语义与现状一致（HUP/无 KILL）；两触发点换入经行为测试锁定；SignalHangup 零残留；07-05 复用的 Server.stopSignal/stopTimeout 字段就位。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: --uid/--gid 降权（Credential）+ whitelistEnv 身份环境改写 + 成对校验</name>
  <reversibility rating="one-way">D-24/D-25：--uid/--gid 是 CLI 公开契约；数字直通不做名字解析是部署行为契约（极简容器 NSS 差异规避）——用户已在 07-CONTEXT 锁定，记录评级不重复设卡。</reversibility>
  <files>internal/pty/spawn.go, internal/pty/spawn_test.go, cmd/wesh/main.go, cmd/wesh/main_test.go</files>
  <read_first>
    - internal/pty/spawn.go（Start 现状、whitelistEnv 白名单循环 L71-75——HOME/USER/LOGNAME 按名继承是 D-25 改写挂点）
    - internal/pty/spawn_test.go（既有 env 断言形态——新增降权用例的宿主）
    - cmd/wesh/main.go（validateStartup 组合矛盾区段）
    - .planning/phases/07-deployment/07-RESEARCH.md（Pattern 6 Credential 逐字结构 + forkExec 顺序 + Pitfall 6；Pattern 4 附录：os/user 双实现静态二进制自动纯 Go）
  </read_first>
  <behavior>
    - Start(argv, StartOptions{Uid: -1})：无 SysProcAttr 设置（现状）；Uid >= 0：`cmd.SysProcAttr = &syscall.SysProcAttr{Credential: &syscall.Credential{Uid: uint32(uid), Gid: uint32(gid)}}`——creack/pty StartWithSize 只补 Setsid/Setctty 两字段不覆盖
    - whitelistEnv(term, uid)：uid >= 0 时 `user.LookupId(strconv.Itoa(uid))` 成功 → HOME=u.HomeDir、USER=LOGNAME=u.Username 替换白名单三键（不再从服务端 env 继承）；失败（极简容器无 /etc/passwd 条目）→ 三键剔除（不 append，shell 自默认）；uid < 0 现状按名继承
    - --uid/--gid 只给一个 → validateStartup 拒绝 exit 2（文案含双 flag 名）；值域：-1 哨兵之外 < -1 或 > 4294967295 parse 期拒绝（uint32 转换安全）
    - spawn_test.go 降权到 self（os.Getuid/Getgid 免 root）：子进程 /usr/bin/id -u 输出 == self uid；env 断言 HOME/USER/LOGNAME == LookupId(self) 条目；LookupId 失败路径（uid 4999999999 查不到→ 三键剔除）——用极大 uid 断言 env 无 HOME/USER/LOGNAME 三键且不降权路径不受影响（注意：极大 uid 的 Credential 设置本身会 spawn 失败——该用例只测 whitelistEnv 纯函数，不起进程）
  </behavior>
  <action>
    ① cmd/wesh/main.go：Task 1 已声明的 uid/gid 字段接 flag：`--uid`/`--gid`（IntVar 默认 -1，help: numeric uid/gid to drop privileges to (must give both; resolve names with id -u first)）；Parse 返回处值域校验（behavior）；validateStartup 加成对校验行（(cfg.uid == -1) != (cfg.gid == -1) → 拒绝，文案 `--uid and --gid must be given together`——loopback 早退之前纯配置矛盾同位）；main_test.go TestParseArgs/TestStartupMatrix 新行。
    ② internal/pty/spawn.go：Start 内 Uid >= 0 分支（behavior 形态，注释登记 GOROOT exec_linux.go forkExec 顺序 setsid→setgid/setuid→TIOCSCTTY——降权后设 ctty 走继承 fd 无权限问题，OpenSSH 同款形态）；whitelistEnv 的 uid 消费分支（behavior 形态，注释登记 D-25 直觉语义与 SEC-06 替换式注入纪律不变——严禁 os.Environ() 追加红线逐字保持）。
    ③ spawn_test.go 扩展（behavior 三组用例；降权到 self 免 root 纪律——降权到 nobody 需 root，列 07-08 人工/可选场景）。
    提交：`feat(07-04): uid/gid privilege drop with identity env rewrite`
  </action>
  <verify>
    <automated>go test ./internal/pty -run 'TestStart|TestWhitelist|TestDropPriv' -count=1 -v && go test ./internal/pty ./cmd/wesh -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - 降权 self 用例 PASS（id -u 输出相等）；身份改写用例 PASS（HOME/USER/LOGNAME == LookupId 条目）；剔除用例 PASS（三键缺席）
    - `grep -c 'syscall.Credential' internal/pty/spawn.go` ≥ 1
    - `grep -c 'LookupId' internal/pty/spawn.go` ≥ 1
    - `go test ./cmd/wesh -count=1` 全绿（成对校验行）
    - `go test -race -count=1 ./internal/pty` 全绿
  </acceptance_criteria>
  <done>
    --uid/--gid 降权与身份环境改写经测试锁定；成对校验与值域校验 fail-fast；whitelistEnv 白名单既有断言全绿（SEC-06 纪律零漂移）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wesh → OS (process spawn) | cwd/TERM/uid/gid 是 operator 配置；子进程以配置身份运行是权限边界变更 |
| wesh → process group (signals) | stop-signal 发给进程组——组边界错误会误杀无关进程 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04a | Denial of Service | SignalGroup 误发无关进程组 | medium | mitigate | 负 pid 组信号 + setsid 组长 pgid==pid 不变量（signal_linux.go:9-10 既定）；KILL 补发 ESRCH 幂等静默；PID 复用窗口极小（子死即退）已裁决接受 |
| T-07-04b | Elevation of Privilege | 降权半配置静默放行 | high | mitigate | uid/gid 成对强制（exit 2 零窗口暴露）；数字直通避开极简容器 NSS 解析差异（D-24）；Credential 在 fork 后 exec 前生效（GOROOT 顺序核实） |
| T-07-04c | Information Disclosure（可用性/权限错乱面） | 降权后 HOME 指向原用户 | medium | mitigate | D-25 LookupId 改写 HOME/USER/LOGNAME；查不到剔除三键（Pitfall 6）；测试锁定 |
| T-07-04d | Tampering | --cwd 指向非预期目录 | low | mitigate | stat 预检 fail-fast（spawn 前零资源占用）；cwd 本身是 operator 显式配置，非攻击面 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V4 Access Control（最小权限运行） | ✓ Credential 降权 + 成对强制 |
| V5 Input Validation（flag 枚举/值域/路径预检） | ✓ parse/validate 双层 |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/pty ./internal/server ./cmd/wesh` 全绿
- TERM 终结与 KILL 补发行为测试锁定；降权 self 全链断言
- 既有 server 套件（pty.Start 最大消费方）零回归
</verification>

<success_criteria>
- `wesh --cwd /tmp --term vt100 -- sh -c 'pwd; echo $TERM'` 的终端输出为 /tmp 与 vt100
- `wesh --stop-signal TERM --stop-timeout 1s --exit-when-empty -- sh -c 'trap "" TERM; sleep 100'`：客户端断开后子进程先收 TERM（忽略），约 1s 后被 SIGKILL 终结，wesh 退出
- `wesh --uid $(id -u) --gid $(id -g) -- bash`：attach 后 `id` 显示当前用户；`wesh --uid 65534 -- bash` → exit 2 文案含 --uid 与 --gid
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **OPS-04 (unclassified, unresolved)**：人工复核项——--cwd 为符号链接时按内核语义解析（不额外规范化）；--term 任意字符串不校验（TERM 值合法性由终端数据库承担，wesh 不立场）；--stop-timeout 极大值只推迟 KILL 不阻塞 exitf（AfterFunc 异步）。07-08 人工 UAT 清单复核。
- **OPS-05 (unclassified, unresolved)**：人工复核项——降权到存在但无登录 shell 的 uid（如 nobody）时 shell 自默认行为由子进程命令承担；supplementary groups 不设置（Credential.Groups 空 + NoSetGroups false 默认 = 清空附加组，GOROOT 语义）——与「最小权限」一致，有意为之。07-08 人工 UAT 清单复核。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `type StartOptions struct{ Dir, Term string; Uid, Gid int }`（internal/pty/spawn.go）
- `Start(argv []string, opts StartOptions)` 新签名 + `whitelistEnv(term string, uid int)` 新签名（internal/pty/spawn.go）
- `SignalGroup(sig syscall.Signal)` + `StopSignalByName(name string) (syscall.Signal, bool)`（internal/pty/signal_linux.go + signal_darwin.go，SignalHangup 删除）
- flag `--cwd` / `--term` / `--stop-signal` / `--stop-timeout` / `--uid` / `--gid` + config 字段 cwd/term/stopSignal/stopTimeout/stopSignalSig/uid/gid（cmd/wesh/main.go）
- `Options.StopSignal syscall.Signal` / `Options.StopTimeout time.Duration` + Server 字段 stopSignal/stopTimeout（internal/server/server.go）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-deployment/07-04-SUMMARY.md` when done
</output>
