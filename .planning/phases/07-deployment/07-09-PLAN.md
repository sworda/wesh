---
phase: 07-deployment
plan: 09
type: execute
wave: 2
depends_on: [07-10]
files_modified:
  - README.md
  - web/uat/pw/phase07-a2-ctl.sh
  - web/uat/pw/phase07-a2-pw.mjs
autonomous: true
gap_closure: true
gap_ids:
  - G-07-2
requirements:
  - OPS-02

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "按 README「部署与配置 → 反代子路径」配方原样配置真实 nginx 后，跨机浏览器经反代访问 /wesh：308 重定向、页面加载、WS 升级成功、终端可用（G-07-2 truth 原文——跨机浏览器按文档部署不再即坏）"
    - "README 配方 location /wesh/ 块含 proxy_set_header Host $http_host;——nginx 默认转发 Host=$proxy_host（127.0.0.1:后端口）与浏览器 Origin 不同源被 wesh originAllowed 同源校验 403（origin.go:73 EqualFold）；$host 剥端口在 origin 含非默认端口时仍不匹配（已实证），必须 $http_host"
    - "精确块理据文案与 proxy_pass 实际行为一致：无精确块时裸 /wesh 在 proxy_pass 形态下为 nginx 自动 301 补斜杠（非 404——C1 的 404 结论基于 return 200 探针 handler，不向真实配方推广）；精确块价值 = 308 保方法 + 显式规范化、行为与 handler 无关"
    - "phase07-a2-pw.mjs 回归载具镜像修正配方（ctl Host $http_host）且 T5 预期校准为 301，双机全链回归 5/5"
  artifacts:
    - path: "README.md"
      provides: "反代子路径 nginx 配方 Host 转发行 + 精确块理据文案修正"
      contains: "proxy_set_header Host $http_host;"
    - path: "web/uat/pw/phase07-a2-ctl.sh"
      provides: "一次性 nginx conf 生成器镜像修正配方（回归测的就是文档配方）"
      contains: "proxy_set_header Host $http_host;"
    - path: "web/uat/pw/phase07-a2-pw.mjs"
      provides: "T5 无精确块变体预期校准（301 + Location 尾斜杠；/wesh/ 仍 200）"
      contains: "status() === 301"
  key_links:
    - from: "README nginx 配方 location /wesh/ 块"
      to: "wesh originAllowed 同源校验（internal/server/origin.go:73）"
      via: "proxy_set_header Host $http_host;——浏览器 Origin 含非默认端口时 host:port 全等才放行 WS 升级"
      pattern: "proxy_set_header Host"
    - from: "web/uat/pw/phase07-a2-ctl.sh conf()"
      to: "README 配方"
      via: "回归载具逐字镜像文档配方——文档即被测物，漂移即回归失效"
      pattern: "Host \\$http_host"
  prohibitions:
    - statement: "MUST NOT 让凭据/token 值进入 pw 脚本 detail/控制台输出——红线保持（只打状态码/布尔/文案常量，phase07-a2-pw.mjs:8 既定）"
      status: resolved
      verification: judgment
---

<objective>
闭合 G-07-2（major，OPS-02）：README 反代子路径 nginx 配方补 `proxy_set_header Host $http_host;`（缺省按文档部署跨机浏览器 WS 升级必 403——全链实证），精确块「否则 404」理据文案按 proxy_pass 实际行为（自动 301）改写；pw 回归载具（ctl 生成器 + T5 预期）同步修正后跑双机全链回归锁死。

Purpose: 部署文档是 operator 信任链起点——配方缺行使按文档部署即坏；理据文案失真会误导后续 handler 形态决策。文档与回归载具必须同一份实证事实。
Output: 修正配方入 README，回归载具镜像之且 5/5 全绿，G-07-2 truth 达成。
编排注记：depends_on 07-10（wave 2，串行、07-10 先）——两 plan 共享可变资源 /tmp/wesh-uat/wesh（07-10 Task 3 段 5 构建写入、本 plan Task 2 回归消费），且 07-10 Task 3 的 git-status 净闸会被本 plan 改动文件击破。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deployment/07-UAT.md
@README.md
@web/uat/pw/phase07-a2-ctl.sh
@web/uat/pw/phase07-a2-pw.mjs
@web/uat/pw/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README nginx 配方补 Host 转发行 + 精确块理据文案按 301 实证改写</name>
  <files>README.md</files>
  <read_first>
    - README.md（「部署与配置 → 反代子路径」节 L297-326——配方 nginx 块 L303-324、节首「两块均必需」句 L301、精确块注释 L311-313）
    - .planning/phases/07-deployment/07-UAT.md（G-07-2 条目 root_cause/artifacts/missing + A2 实测段——$http_host 修正实证与 T5 证伪细节以此为准）
    - internal/server/origin.go:73（EqualFold(r.Host, u.Host) 同源校验——Host 转发行必要性的机制依据）
  </read_first>
  <behavior>
    - location /wesh/ 块在 `proxy_http_version 1.1;` 行之后新增 `proxy_set_header Host $http_host;`，并配一行注释说明必要性（nginx 默认转发 $proxy_host=127.0.0.1:后端口，与浏览器 Origin 不同源被 wesh WS 同源校验 403；$host 剥端口在 origin 含非默认端口时仍不匹配——必须 $http_host，已全链实证）
    - 精确块注释重写：删去现行「不经精确块则 404」理据句（该结论来自 return 200 探针 handler 的 C1 复核，不向 proxy_pass 形态推广——A2 T5 实证证伪）；新文案如实陈述：精确块价值 = 308 保方法 + 显式规范化（规范化行为与 handler 无关）；缺省时本配方（proxy_pass handler）下 nginx 对裸 /wesh 自动 301 补斜杠（GET 入口可工作），但该自动跳转是 proxy_pass 系 handler 特例
    - 节首「两块均必需」表述校准：前缀块必需 + 精确块推荐（理据见块内注释）
    - 配方其余行（map $http_upgrade、Upgrade/Connection 头、proxy_read_timeout 3600s 及其后整段 idle 说明）逐字不动
  </behavior>
  <action>
    ① location /wesh/ 块插入 Host 行：落位 `proxy_http_version 1.1;` 之后、`proxy_set_header Upgrade` 之前，逐字为 `        proxy_set_header Host $http_host;`（缩进与邻行一致）；其上方插一行 `#` 注释承载 behavior ① 的理据（默认 $proxy_host 不同源 403 / $host 剥端口仍不匹配 / 必须 $http_host——三个事实点齐全，一句一行内）。
    ② 精确块注释（三行）整段重写为 behavior ② 的新理据：308 保方法 + 显式规范化与 handler 无关为「推荐」依据；缺省 301 自动补斜杠为 proxy_pass 系 handler 特例（换 return/fastcgi 等形态即不存在自动跳转）。注释中不得再现「404 理据」旧句的任何字面（验收负向 grep 是机械检查）。
    ③ 节首句 L301「nginx 配方（两块均必需）：」改为「nginx 配方（前缀块必需；精确块推荐——理据见块内注释）：」。
    ④ 其余行零改动；改完通读全节确认 markdown 围栏与缩进完整。
    提交：`docs(07-09): fix nginx subpath recipe — Host $http_host forwarding + exact-block rationale per proxy_pass 301 reality (G-07-2)`
  </action>
  <verify>
    <automated>awk '/location \/wesh\/ \{/,/\}/' README.md | grep -cF 'proxy_set_header Host $http_host;' && ! grep -qF '落不到反代块' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/location \/wesh\/ \{/,/\}/' README.md | grep -cF 'proxy_set_header Host $http_host;'` 输出 1（区域内正向：Host 行在 location /wesh/ 块内恰一处）
    - `grep -cF '落不到反代块' README.md` 输出 0（负向：旧理据句字面零残留——注释重写后任何位置不得再现）
    - `grep -c 'proxy_read_timeout 3600s' README.md` 输出 ≥ 1（配方既有行未动）
    - `grep -cF '```nginx' README.md` 输出 ≥ 1（围栏完整）
  </acceptance_criteria>
  <done>
    README 配方与 2026-08-26 全链实证通过的修正配方逐字一致；理据文案每个事实句都有实证依据（Host 必要性 = A2 实测 403→101；301 = T5 证伪改写；308 保方法 = 精确块保留价值）。
  </done>
</task>

<task type="auto">
  <name>Task 2: pw T5 预期 301 校准 + 双机全链回归 5/5（ctl $http_host 同步已由 e4d46bb 提前落地，实余工作量 = T5 三处改写 + 回归）</name>
  <precondition>双机拓扑可用：本机（Windows 工作站）Playwright Chromium 与 web/uat/pw 依赖就绪；`ssh -o BatchMode=yes 9.134.229.124` 免密可达；Linux 侧 /tmp/wesh-uat/wesh 二进制存在（缺失则按 web/uat/pw/README.md 前置 `go build -o /tmp/wesh-uat/wesh ./cmd/wesh` 重建——本 plan 无 Go 改动，HEAD 构建即可）。2026-08-26 UAT 同款环境。</precondition>
  <files>web/uat/pw/phase07-a2-ctl.sh, web/uat/pw/phase07-a2-pw.mjs</files>
  <read_first>
    - web/uat/pw/phase07-a2-ctl.sh（conf() L9-44——Host 行 $http_host 同步已由提交 e4d46bb 提前落地（前次 Edit/cp 同块竞态致仓库遗留 $host，该提交修正），仓库现状即修正形态；本任务仅核对、不改写本文件）
    - web/uat/pw/phase07-a2-pw.mjs（头注释 L6-7、T5 Check 声明 L41、T5 断言体 L80-87）
    - .planning/phases/07-deployment/07-UAT.md（A2 实测段——T5 的 404 预期证伪过程与 nginx 官方文档行为引述）
  </read_first>
  <behavior>
    - ctl conf() 的 location /wesh/ 块 Host 行逐字为 `      proxy_set_header Host $http_host;`——已由 e4d46bb 落地，本任务核对现状即可（两 variant exact/noexact 共用同一 conf()）；回归载具与 Task 1 修正后的 README 配方逐字镜像
    - pw T5 断言重写：无精确块变体下裸 /wesh → 301 且 Location 以 /wesh/ 结尾（nginx proxy_pass 自动补斜杠）；/wesh/ 仍 200（既有断言保持）；Check 名称与脚本头注释同步为 301 语义
    - T1-T4 断言与 setup/teardown 流程零改动；红线不变（凭据只作构造材料，detail 只打状态码/布尔/文案常量）
    - 双机全链回归执行：5/5 通过，a2-home/a2-idle 截图重生留档
  </behavior>
  <action>
    ① 【免执行——已由 e4d46bb 落地】ctl Host 行 `$http_host` 同步已提前完成；本步骤仅核对：`grep -cF 'proxy_set_header Host $http_host;' web/uat/pw/phase07-a2-ctl.sh` 输出 1（conf() 单引号 'EOF' 段内 $ 字面安全），零改写。
    ② pw 脚本三处同步：头注释 L7「无精确块变体裸路径 404」改为 301 语义（proxy_pass 自动补斜杠）；L41 t5 Check 名称改为「无精确块变体：裸 /wesh → 301（nginx proxy_pass 自动补斜杠）；/wesh/ 仍 200」；L80-87 断言体——resp404 更名 resp301，`resp404.status() === 404` 等值断言改为 `resp301.status() === 301`，并补 Location 尾斜杠断言（headers()['location'] ?? '' → endsWith('/wesh/')，T1 同款形态）；/wesh/ 200 断言与 variant 还原行不动。
    ③ 执行回归：`node web/uat/pw/phase07-a2-pw.mjs`（脚本自动 scp ctl 到 Linux → setup 一次性 nginx+wesh → T1-T5 → teardown；T4 含 65s 空闲窗，全程约 2 分钟）。失败先查 ctl diag/probe 通道，不得改断言迁就。
    ④ 截图 screenshots/a2-home.png、a2-idle.png 重生（gitignore 留档纪律不变）。
    提交：`test(07-09): sync a2-pw regression vehicle to fixed recipe — T5 expects 301 (ctl $http_host landed in e4d46bb) (G-07-2)`
  </action>
  <verify>
    <automated>node web/uat/pw/phase07-a2-pw.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF 'proxy_set_header Host $http_host;' web/uat/pw/phase07-a2-ctl.sh` 输出 1（e4d46bb 已落地的现状核对）
    - `grep -c 'status() === 301' web/uat/pw/phase07-a2-pw.mjs` 输出 ≥ 1
    - `grep -c 'status() === 404' web/uat/pw/phase07-a2-pw.mjs` 输出 0（负向：404 等值断言零残留）
    - 回归运行退出 0 且输出 `结果: 5/5 项通过`
  </acceptance_criteria>
  <done>
    回归载具镜像修正配方；T1 308 / T2 页面+提示符 / T3 echo 全链 / T4 空闲 65s 不断 / T5 无精确块 301+/wesh/ 200——5/5 全绿，G-07-2 truth（按文档配方部署跨机浏览器全链通）经自动化锁定。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → README → nginx 配置 | 文档配方是部署信任链起点；错误配方使按文档部署即坏（本 gap 本体） |
| browser → nginx → wesh WS 升级 | Host 头转发形态决定同源校验结果；配方行是唯一控制点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09a | Tampering | README nginx 配方 | high | mitigate | 配方每行须有全链实证依据；phase07-a2-pw.mjs 5/5 为回归锁（文档即被测物，载具逐字镜像） |
| T-07-09b | Denial of Service | WS 升级 403 误配 | high | mitigate | 本 gap 本体——Host $http_host 转发行入配方 + T1-T4 全链断言锁 |
| T-07-09c | Information Disclosure | pw 脚本凭据/token | medium | mitigate | 红线保持：值只作构造材料，detail/控制台只打状态码/布尔/文案常量（头注释既定，本 plan 零改动） |
</threat_model>

<verification>
- 静态：README 区域 grep 1/0 双闸 + ctl/pw 载具 grep 三闸（见两任务 acceptance）
- 动态：`node web/uat/pw/phase07-a2-pw.mjs` 退出 0，5/5（修正配方 T1-T4 + 校准 T5）
- 不涉 Go 代码（go test 不适用）；phase07.mjs 不触 nginx 面（无回归需求——07-10 协议回归顺带兜底）
</verification>

<success_criteria>
- operator 照抄 README 配方配置 nginx，跨机浏览器访问 http://host/wesh 全链可用（WS 101、终端可输入输出、空闲不断）
- 文档理据与 nginx 实际行为句句有实证：无精确块 = 301（proxy_pass 形态）；精确块 = 308 保方法显式规范化
- 后续 handler 形态变化（return/fastcgi）的读者不会被旧 404 理据误导
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **Host 变量选型无分档**：$http_host 在非默认端口必需（实证）；默认 80/443 场景 $host 亦可工作（origin 无端口可匹配）。配方取 $http_host 单一形态全端口通吃，不为默认端口另给 $host 分档——实证已覆盖非默认端口（10013），默认端口形态按同一变量语义安全子集推断。该假设已显式记录，07-UAT 复核可选项。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- README 反代子路径配方 `proxy_set_header Host $http_host;` 行 + 必要性注释 + 精确块 301/308 理据文案（README.md）
- ctl 生成器 `$http_host` 镜像行（web/uat/pw/phase07-a2-ctl.sh）
- T5 301 断言组（更名 resp301 + Location 尾斜杠断言；web/uat/pw/phase07-a2-pw.mjs）
- 回归证据：5/5 输出 + a2-home/a2-idle 截图（入 07-09-SUMMARY.md）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-deployment/07-09-SUMMARY.md` when done
</output>
