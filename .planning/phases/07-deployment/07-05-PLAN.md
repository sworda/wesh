---
phase: 07-deployment
plan: 05
type: execute
wave: 5
depends_on:
  - "07-04"
files_modified:
  - internal/server/server.go
  - internal/server/shutdown_test.go
  - internal/proto/proto.go
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
  - web/src/main.ts
  - web/uat/phase06-dom.mjs
  - web/dist/index.html
autonomous: true
requirements:
  - OPS-11

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "wesh 收到 SIGTERM/SIGINT 后：全部已注册客户端收到 1001 关闭（close reason server_shutting_down）→ 子进程进程组收 stop-signal 序列（默认 HUP，可配 + stop-timeout 补 KILL）→ 子进程死亡经既有 lifecycle 收口 exitf（单一收口零新分支）"
    - "前端收到 1001 显示 'Server shutting down' 面板且不进入重连循环（1001 不在 CORE-05 触发集——仅 1006）；1000/1006/1013 等既有分派逐字节不变"
    - "wesh --open -- bash 桌面环境下以 xdg-open（Linux）/ open（macOS）打开分享链接（--writable 开 rw 链接，否则 ro 链接，含 token 免交互）；无 DISPLAY 且无 WAYLAND_DISPLAY 时 stderr 提示后跳过不阻断启动"
    - "--socket 与 --open 同给 validateStartup fail-fast exit 2（unix socket 无 http URL 可开）"
    - "proto.go 关闭码纪律块 1001 翻正为启用态（发送路径 = server.go Shutdown、库常量 websocket.StatusGoingAway、close reason 机器串 server_shutting_down），main.ts 注释互相指路"
  artifacts:
    - path: "internal/server/server.go"
      provides: "Server.Shutdown()（exiting 门 + 注册表快照 + 每客户端 goroutine 同步 Close(1001) + stop-signal 序列）"
      contains: "func (s *Server) Shutdown()"
    - path: "internal/server/shutdown_test.go"
      provides: "TestShutdown1001（客户端收 1001 + 子进程终结 + 注册表空）"
      min_lines: 60
    - path: "cmd/wesh/main.go"
      provides: "signal.NotifyContext SIGTERM/INT 捕获 + --open flag + openBrowser（headless 跳过）+ --socket×--open 冲突校验"
      contains: "openBrowser"
    - path: "web/src/main.ts"
      provides: "onclose switch case 1001 → 'Server shutting down' 面板"
      contains: "Server shutting down"
  key_links:
    - from: "cmd/wesh/main.go signal.NotifyContext goroutine"
      to: "internal/server Server.Shutdown() → pty.SignalGroup"
      via: "SIGTERM/INT 是触发源不是 exitf 分支——进程终结仍由 lifecycle 子进程死亡路径收口（P1 硬约束）"
      pattern: "NotifyContext|Shutdown\\(\\)"
    - from: "internal/server Shutdown 1001 广播"
      to: "web/src/main.ts onclose case 1001"
      via: "关闭码 1001 + reason server_shutting_down 前后端手工对齐（proto.go ↔ main.ts 注释互指纪律）"
      pattern: "1001"
  prohibitions:
    - statement: "MUST NOT auto-reconnect on 1001——重连循环打正在重启的服务是 systemd restart 场景的反 UX（D-23）；1001 必须落终态面板（仅 1006 触发重连，P6 D-01）"
      status: resolved
      verification: judgment
---

<objective>
落地 D-23 1001 优雅下线（P6 deferred 兑现）+ OPS-11 --open 自动开浏览器（D-26/D-27）：wesh 捕获 SIGTERM/SIGINT → 向全部客户端发 1001 Going Away（P6 EXIT 帧广播先例形态）→ 子进程 stop-signal 序列（07-04 字段复用）→ 既有 lifecycle 收口；前端 onclose 1001 分派「Server shutting down」面板（不进重连循环）；--open 以 xdg-open/open 打开 operator 视角分享链接（headless 跳过不阻断）。

1001 启用 P2 D-08 占位，扩展线上关闭码集合；--open 消费 07-01 落地的 shareURL 单一事实源。

Purpose: systemd restart 场景客户端看到「服务关停」而非重连循环打一个正在重启的服务——UX 闭环；--open 是桌面 operator 的即打即用便利。
Output: SIGTERM/INT 优雅下线全链 + --open 三形态（desktop 打开/headless 跳过/socket 冲突拒绝）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deployment/07-CONTEXT.md
@.planning/phases/07-deployment/07-RESEARCH.md
@.planning/phases/07-deployment/07-PATTERNS.md
@internal/server/server.go
@internal/proto/proto.go
@cmd/wesh/main.go
@web/src/main.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Server.Shutdown（1001 广播 + stop-signal 序列）+ proto.go 1001 翻正 + 行为测试</name>
  <reversibility rating="one-way">D-23：1001 是前后端公开协议契约（P2 D-05 同评级）——用户已在 07-CONTEXT 锁定，记录评级不重复设卡。</reversibility>
  <files>internal/server/server.go, internal/server/shutdown_test.go, internal/proto/proto.go</files>
  <read_first>
    - internal/server/server.go（lifecycle EXIT 帧广播先例 L1090-1124：exiting 门/hubMu 快照/每客户端 goroutine 同步写/Close 内建 5s+5s 上界注释；terminate L1126-1133 单一收口；07-04 落地的 stopSignal/stopTimeout 字段）
    - internal/proto/proto.go（关闭码纪律块 L8-14——1001 占位行翻正为启用态的 1013 先例同形态）
    - .planning/phases/07-deployment/07-RESEARCH.md（Pattern 7 Shutdown 逐字配方 + 1001×EXIT 竞态论证 + OQ3：Close 内建上界不再盒）
  </read_first>
  <behavior>
    - Shutdown()：hubMu 内置 s.exiting = true（空触发抑制门复用 lifecycle:1096 先例）→ 注册表快照 → 解锁；每客户端 goroutine `_ = c.conn.Close(websocket.StatusGoingAway, "server_shutting_down")`（无 EXIT 帧前置——进程未退出，终结语义由关闭码承载；Close 内建 5s+5s 上界不再盒，OQ3）→ wg.Wait() → s.sess.SignalGroup(s.stopSignal) → s.stopTimeout > 0 时 time.Sleep 后 s.sess.SignalGroup(syscall.SIGKILL)（Shutdown 不在 hubMu 内等待，与 lifecycle 并发安全——ESRCH 幂等纪律）
    - Shutdown 不调 exitf（P1 硬约束：触发源非分支）：返回后子进程死亡 → sess.Wait 返回 → lifecycle 的 EXIT+1000 广播在已空注册表上零循环 → terminate(code) 收口
    - TestShutdown1001：startTrackedServerWith 起实例 + 握手客户端 → 调 s.Shutdown() → 客户端读循环收到 CloseError 且 code == 1001、reason 含 server_shutting_down → 子进程终结（exitCh 收码——默认 HUP 信号死亡 = -1 桩码与 P6 OQ1 同源）；断言 exitf 恰好一次
    - proto.go 纪律块：1001 行翻正为「1001 优雅下线已于 Phase 7 启用（D-08 占位兑现——发送路径 = server/server.go Shutdown，库常量 websocket.StatusGoingAway，close reason 机器串 server_shutting_down）」（1013 行先例逐字同构）；main.ts 注释指路在 Task 3 落地
  </behavior>
  <action>
    ① internal/server/server.go：新增 `func (s *Server) Shutdown()`（behavior 形态；注释登记：1001×EXIT 竞态论证——Shutdown 置 exiting 后子进程才死亡，lifecycle 快照在 Shutdown 之后注册表已空零重复关闭；子进程在 1001 广播完成前自然死亡时两端可能先后收到 1000/1001——coder/websocket Close 幂等，前端以先到码分派两语义都正确，风险接受 RESEARCH Pattern 7）。
    ② internal/proto/proto.go：关闭码纪律块 1001 占位行翻正（behavior 逐字形态；全集 {1000,1001,1002,1008,1009,1011,1013} 表述保持——1001 从「占位」翻「启用」）。
    ③ internal/server/shutdown_test.go【新】：TestShutdown1001（behavior 场景）；TestShutdownStopTimeout（Options{StopSignal: TERM, StopTimeout: 300ms} + 子进程 trap 忽略 TERM → Shutdown 后 KILL 补发终结——复用 07-04 trap 夹具形态）。
    提交：`feat(07-05): Server.Shutdown with 1001 Going Away broadcast`
  </action>
  <verify>
    <automated>go test ./internal/server -run 'TestShutdown' -count=1 -v && go test ./internal/server -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - TestShutdown1001 PASS（客户端收 1001 + reason server_shutting_down + exitf 恰好一次）
    - TestShutdownStopTimeout PASS（KILL 补发终结）
    - `grep -c 'StatusGoingAway' internal/server/server.go` ≥ 1
    - `grep -c '1001 优雅下线已于 Phase 7 启用' internal/proto/proto.go` == 1
    - `go test ./internal/server -count=1` 全绿（既有套件零回归）
  </acceptance_criteria>
  <done>
    Shutdown 触发源落地：1001 广播 → stop-signal 序列 → lifecycle 单一收口；1001 关闭码协议层启用；两行为测试锁定。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: main.go SIGTERM/INT 捕获接线 + --open flag + openBrowser + --socket×--open 冲突</name>
  <reversibility rating="one-way">D-26/D-27：--open 是 CLI 公开契约（P2 D-15 纪律）——用户已在 07-CONTEXT 锁定，记录评级不重复设卡。</reversibility>
  <files>cmd/wesh/main.go, cmd/wesh/main_test.go</files>
  <read_first>
    - cmd/wesh/main.go（run() 启动打印 L477-505 与 07-01 落地的 shareURLRO/shareURLRW 单一事实源、Serve/ServeTLS 分岔 L513-522、validateStartup 组合矛盾区段、config struct）
    - .planning/phases/07-deployment/07-RESEARCH.md（Pattern 7 NotifyContext 挂点 + Pattern 8 openBrowser 配方 + OQ1：--socket×--open fail-fast 建议行）
  </read_first>
  <behavior>
    - run() 在 hs 装配后、Serve 前：`sigCtx, stopSignals := signal.NotifyContext(context.Background(), syscall.SIGTERM, syscall.SIGINT)` + `defer stopSignals()` + goroutine `<-sigCtx.Done(); srv.Shutdown()`——Serve 返回路径现状零改动
    - --open flag：desktop（Linux 有 DISPLAY/WAYLAND_DISPLAY，或 darwin）→ 启动打印完成后 goroutine `exec.Command(tool, url).Start()`，url = --writable 时 shareURLRW 否则 shareURLRO（含 token 免交互，D-26）；tool = linux "xdg-open" / darwin "open"；失败仅 stderr 警告 `wesh: --open: failed to launch browser: %v`
    - headless（linux 且 DISPLAY=="" 且 WAYLAND_DISPLAY==""）→ stderr `wesh: --open: no display detected (headless), skipping browser launch` 后跳过（不阻断启动）
    - validateStartup 新行：cfg.open && cfg.socket != "" → 拒绝 exit 2（文案含 --open 与 --socket——unix socket 无 http URL 可开，RESEARCH OQ1 建议行落地）；loopback 早退之前纯配置矛盾同位
    - 测试：headless 跳过（t.Setenv 清两变量 → openBrowser 返回且 stderr 捕获提示行）；fake xdg-open（t.TempDir 写可执行脚本记录 argv 到文件 + PATH 前置 + DISPLAY=:99 → 断言 argv[1] == 预期分享 URL）；darwin 分支不做运行时断言（构建标签差异——CI macOS 跑同款测试形态，注释登记）
  </behavior>
  <action>
    ① cmd/wesh/main.go：config struct 加 `open bool`（D-26 注释）；flag 注册 `--open`（BoolVar 默认 false，help: open the share link in a browser after startup (rw link when --writable, otherwise ro)）。
    ② run() 接线：NotifyContext 捕获（behavior 形态——注释登记「不调 exitf：Shutdown 是触发源，进程终结由 lifecycle 子进程路径收口，P1 硬约束」；goroutine 内先等 Done 再 Shutdown，stopSignals 经 defer 注销恢复默认）。
    ③ 新增 `openBrowser(url string)`（behavior 形态——RESEARCH Pattern 8 配方；headless 检测只在 linux 分支判定，darwin 直接 open；goroutine 调用点在启动打印完成之后、Serve 之前——`if cfg.open { go openBrowser(pickShareURL()) }`，pickShareURL = cfg.writable ? shareURLRW : shareURLRO）。
    ④ validateStartup 加 --socket×--open 冲突行（behavior 文案）；main_test.go TestStartupMatrix 新行 + TestParseArgs --open 行。
    ⑤ main_test.go 新增 TestOpenBrowser（behavior 两场景；openBrowser 抽为可测纯函数形态——URL 入参、exec.Command 经包级变量或可注入 runner？取最小形态：openBrowser(url string) 内直接 exec.Command，测试用 PATH 前置 fake xdg-open 拦截——exec 的真实调用链不打桩，与 07-VALIDATION 「fake xdg-open PATH 前置断言 argv」策略一致）。
    提交：`feat(07-05): SIGTERM/SIGINT graceful shutdown wiring and --open browser launch`
  </action>
  <verify>
    <automated>go test ./cmd/wesh -run 'TestOpenBrowser|TestStartupMatrix|TestParseArgs' -count=1 -v && go test ./cmd/wesh -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - TestOpenBrowser 两场景 PASS（headless 跳过提示行 + fake xdg-open argv 断言）
    - TestStartupMatrix 含 --socket×--open 冲突行 PASS
    - `grep -c 'signal.NotifyContext' cmd/wesh/main.go` == 1
    - `grep -c 'func openBrowser' cmd/wesh/main.go` == 1
    - `go vet ./cmd/wesh` 退出 0
  </acceptance_criteria>
  <done>
    SIGTERM/INT → Shutdown 接线完成（exitf 单一收口纪律保持）；--open 三形态（desktop/headless/socket 冲突）经测试锁定。
  </done>
</task>

<task type="auto">
  <name>Task 3: 前端 onclose 1001 分派 + phase06-dom.mjs 1001 场景 + dist 重建</name>
  <files>web/src/main.ts, web/uat/phase06-dom.mjs, web/dist/index.html</files>
  <read_first>
    - web/src/main.ts（onclose switch L842-930——1000/1008/1013 case 文案形态与 showStatus 三参调用、1006 重连触发 L914-918、reconnecting 分派 L862-868；帧常量注释区 L12-30 与 proto.go 互指纪律）
    - web/uat/phase06-dom.mjs（synthClose 夹具与 _savedClose 副本——06-05 落地的 CloseEvent 驱动形态、assertOutputClean 自净）
    - internal/proto/proto.go（Task 1 翻正后的 1001 注释行——前后端互指的另一侧）
  </read_first>
  <behavior>
    - onclose switch 新增 case 1001：showStatus('Server shutting down', 'The wesh server is shutting down. The session has ended.', 'Start wesh again from your shell, then')——逐字文案（UI-SPEC 文案锁定纪律同 05-08/06-05）；不调用 startReconnect（prohibition 回归锁）
    - 重连上下文中收到 1001：`reconnecting && ev.code === 1006` 分支不命中 → 落 stopReconnect + 面板分派（既有 L862-868 形态自然覆盖，测试锁定）
    - main.ts 帧常量注释区补 1001 启用行（与 proto.go 互指：发送路径 server Shutdown、reason server_shutting_down）
    - phase06-dom.mjs 新增场景：synthClose(1001) → 断言面板 title == 'Server shutting down' 且未进入重连（无 Reconnecting 面板、无 attempt 定时器）；对照 synthClose(1006) 仍进重连（既有场景不回归）
    - dist 重建入库；三个 jsdom 套件全绿
  </behavior>
  <action>
    ① web/src/main.ts：onclose switch 在 case 1000 之后插入 case 1001（behavior 逐字文案——1000 'Session ended' 与 1001 语义边界：进程退出 vs 服务关停）；注释登记 D-23（1001 不在 CORE-05 触发集——仅 1006；systemd restart 场景 UX 闭环）。帧常量注释区（L12-30）补 1001 行：已于 Phase 7 启用（D-23）——server Shutdown 广播 + reason server_shutting_down，proto.go 互指。
    ② web/uat/phase06-dom.mjs：新增 1001 场景（behavior 断言形态；synthClose/_savedClose 复用 06-05 既有夹具）；红线 assertOutputClean 自动覆盖新 detail。
    ③ `time pnpm -C web build` 重建 dist 入库；跑全部 jsdom 套件 + 协议层套件零回归。
    提交：`feat(07-05): dispatch 1001 to shutdown panel, rebuild dist`
  </action>
  <verify>
    <automated>time pnpm -C web build && node web/uat/phase06-dom.mjs && node web/uat/phase04-dom.mjs && node web/uat/phase05-dom.mjs && grep -c 'Server shutting down' web/dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `node web/uat/phase06-dom.mjs` 退出 0 且输出含 1001 场景 PASS（面板 + 不重连）
    - `grep -c 'Server shutting down' web/dist/index.html` ≥ 1（字符串字面量进产物，esbuild 不重命名）
    - `time pnpm -C web build` 退出 0
    - 三个 jsdom 套件 + `node web/uat/phase02.mjs`、`node web/uat/phase06.mjs` 各自退出 0
  </acceptance_criteria>
  <done>
    1001 前端分派落地且 jsdom 场景锁定「面板 + 不重连」；dist 入库；既有分派矩阵（1000/1006/1013）零回归。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS → wesh (signals) | SIGTERM/SIGINT 来自 init/operator——优雅下线触发源 |
| wesh → OS desktop (xdg-open) | --open 把含 token 的 URL 传给系统启动器——URL 是 wesh 自构（非用户输入） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05a | Denial of Service | 1001 广播期间 stall 客户端拖延关停 | low | mitigate | Close 内建 5s+5s 上界（close.go:87-89）不再盒（OQ3）；exitf 由 lifecycle 子进程路径收口与 Shutdown goroutine 并发——stall 端最坏 10s 不阻塞进程退出 |
| T-07-05b | Tampering | --open URL 注入 | low | mitigate | URL 由 wesh 自构（scheme+host+port+base-path+自生成 token），exec.Command argv 分离不经 shell；--socket×--open 冲突拒绝消除无 URL 歧义形态 |
| T-07-05c | Information Disclosure | --open 把 token URL 暴露给桌面环境 | low | accept | D-26 已裁决：operator 本机操作无泄露面（token 通道免交互即打即用是 P5 D-01 既定语义）；启动打印两行本已含同 token |
| T-07-05d | Denial of Service | 1001×EXIT 竞态致客户端语义错乱 | low | accept | RESEARCH Pattern 7 论证：Close 幂等，先到码分派两语义都正确（进程死 vs 服务关停） |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V7 Error Handling/Logging（关停可观测） | ✓ 1001 关闭码 + 前端终态面板 |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/server ./cmd/wesh` 全绿（Shutdown 与 lifecycle 并发面）
- phase06-dom.mjs 1001 场景 PASS；三 jsdom 套件 + 协议层套件零回归
- dist 产物含 'Server shutting down' 字面
</verification>

<success_criteria>
- `wesh -- bash` + attach 后 `kill -TERM <wesh_pid>`：客户端收到 close 1001（reason server_shutting_down），前端显示 'Server shutting down' 面板不重连，wesh 进程退出
- `DISPLAY=:99 wesh --open --writable -- bash`（fake xdg-open 拦截）：xdg-open 收到 rw 分享链接；headless 环境同样命令：stderr 提示跳过后正常服务
- `wesh --socket /tmp/x.sock --open -- bash` → exit 2 文案含 --open 与 --socket
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **OPS-11 (unclassified, unresolved)**：人工复核项——macOS open 行为未在本机（Linux headless）实测（CI macOS 跑同款单测形态，真实弹窗列 07-08 人工清单）；xdg-open 存在但返回非零（桌面异常）只警告不阻断（D-27）；--open 与 TLS 组合打开 https:// 链接（自签证书浏览器警告属用户预期面）。07-08 人工 UAT 清单复核。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `func (s *Server) Shutdown()`（internal/server/server.go）
- `TestShutdown1001` / `TestShutdownStopTimeout`（internal/server/shutdown_test.go，新文件）
- flag `--open` + config 字段 `open bool` + `openBrowser(url string)`（cmd/wesh/main.go）
- main.ts onclose `case 1001`（'Server shutting down' 面板）
- phase06-dom.mjs 1001 场景（synthClose 1001 断言组）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-deployment/07-05-SUMMARY.md` when done
</output>
