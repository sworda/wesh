---
phase: 01-pty
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - .gitignore
  - cmd/wesh/main.go
  - internal/proto/proto.go
  - internal/pty/spawn.go
  - internal/pty/io.go
  - internal/pty/reap_linux.go
  - internal/server/server.go
  - internal/server/e2e_test.go
  - web/embed.go
  - web/dist/index.html
  - web/package.json
  - web/pnpm-lock.yaml
  - web/vite.config.ts
  - web/tsconfig.json
  - web/index.html
  - web/src/main.ts
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - FE-01
  - FE-03
  - SEC-06

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "用户运行 `wesh -- bash` 后浏览器打开页面即获得完整交互终端：键盘输入经 INPUT 帧写入 PTY master，程序输出经 OUTPUT 帧实时渲染到 xterm（CORE-01）"
    - "`wesh -- /bin/cat` 下 WS 客户端发送 INPUT 帧能收到同字节 OUTPUT 帧（TestEchoPTY 自动化证明，VALIDATION 1-01-06）"
    - "浏览器窗口变化时前端 fit 自适应并经 RESIZE 帧上报 cols/rows，服务端钳制 [1,1000] 后经 TIOCSWINSZ 同步 PTY 尺寸（CORE-02/FE-03，D-16）"
    - "子进程退出后当前 WS 客户端先收到 1000 正常关闭帧，随后服务端进程以子进程退出码退出（D-10）"
    - "WS 客户端主动断开或网络断开后，服务端 SIGHUP 子进程进程组（pgid = 子进程 pid，setsid 保证）并自身退出（D-11）"
    - "子进程环境仅含白名单变量：TERM=xterm-256color 与 COLORTERM=truecolor 固定，PATH/HOME/USER/LOGNAME/SHELL 按名继承，LANG=/LC_ 前缀继承；其余服务端变量一律丢弃（SEC-06，D-15）"
    - "WS 连接建立期间页面呈现已打开的空黑终端：body 背景 #000000 与终端同色、CSS 先于 JS 内联，无白闪、无 spinner（UI-SPEC loading 态）"
    - "WS onerror/onclose 时显示 #status 面板并按三种关闭路径显示对应文案（Unable to connect / Session ended / Connection lost），终端最终输出在遮罩下保持可读（UI-SPEC error 态）"
    - "OUTPUT 帧经 term.write(Uint8Array) 实时流入全视口终端（UI-SPEC populated 态）"
    - "超视口输出进入 10000 行 scrollback，xterm 原生滚动条可用；长行按 cols 折行（UI-SPEC overflow 态）"
    - "#status 面板文案为契约固定短文本，在 480px max-width 内自然折行，无截断（UI-SPEC long-text 态）"
  artifacts:
    - path: "go.mod"
      provides: "Go module 定义"
      contains: "module github.com/sworda/wesh"
    - path: "cmd/wesh/main.go"
      provides: "CLI 解析（wesh [flags] -- <cmd>）与组件装配"
      exports: ["main", "run", "parseArgs"]
    - path: "internal/proto/proto.go"
      provides: "协议帧单一事实源"
      exports: ["Input", "Resize", "Output", "DecodeResize", "ClampDim"]
    - path: "internal/pty/spawn.go"
      provides: "PTY spawn（exec 数组 + env 白名单 + 初始尺寸）"
      exports: ["Start", "Session"]
    - path: "internal/pty/io.go"
      provides: "master 读循环 / Resize / Close / 带时限 drain"
      exports: ["ReadLoop", "Resize", "Close"]
    - path: "internal/pty/reap_linux.go"
      provides: "Linux 收割（cmd.Wait() = stdlib pidfd waitid）"
      exports: ["awaitExit"]
    - path: "internal/server/server.go"
      provides: "HTTP + WS 网关、数据泵、生命周期（409/1000/退出码/SIGHUP）"
      exports: ["Server", "New", "Handler", "Attach"]
    - path: "internal/server/e2e_test.go"
      provides: "WS echo 端到端测试 + D-12 drain 行为证明"
      contains: "TestEchoPTY / TestDrainBeforeAttach"
    - path: "web/embed.go"
      provides: "go:embed 静态伺服（预 gzip 协商）"
      exports: ["Handler"]
    - path: "web/dist/index.html"
      provides: "embed 占位（构建后被真实单 HTML 覆盖）"
    - path: "web/src/main.ts"
      provides: "xterm 初始化 + WS 客户端 + fit/resize 回路 + #status 三态面板"
    - path: "web/index.html"
      provides: "页面壳（#terminal + #status 两个顶层元素 + 内联契约 CSS）"
    - path: "web/package.json"
      provides: "前端依赖钉死（@xterm/xterm 6.0.0 等）与 build 脚本"
  key_links:
    - from: "cmd/wesh/main.go"
      to: "internal/pty/spawn.go"
      via: "main 装配调用 pty.Start(argv)"
      pattern: "pty\\.Start\\("
    - from: "cmd/wesh/main.go"
      to: "internal/server/server.go"
      via: "注入 exitf=os.Exit 与 sess，挂 / 与 /ws 两路由"
      pattern: "server\\.(New|Serve|ListenAndServe)"
    - from: "internal/server/server.go"
      to: "internal/pty/io.go"
      via: "Session.ReadLoop 回调：未 attach 丢弃（D-12），已 attach 组 OUTPUT 帧写 WS"
      pattern: "ReadLoop\\("
    - from: "internal/server/server.go"
      to: "internal/proto/proto.go"
      via: "reader 按 data[0] 分派 proto.Input → master.Write / proto.Resize → DecodeResize → Session.Resize"
      pattern: "proto\\.(Input|Resize|DecodeResize)"
    - from: "web/src/main.ts"
      to: "internal/proto/proto.go"
      via: "前端帧常量 OUTPUT/INPUT/RESIZE = 0x30/0x30/0x31 与 proto 包手工对齐（D-16）"
      pattern: "0x3[01]"
    - from: "web/embed.go"
      to: "web/dist/index.html"
      via: "//go:embed all:dist 编译期内嵌（构建顺序：pnpm -C web build 先于 go build，D-18）"
      pattern: "go:embed all:dist"
  prohibitions:
    - statement: "wesh 不得向任何第三方端点发起运行时网络请求——无遥测、无使用统计、无崩溃上报（终端内容与按键属最高敏感数据，自托管工具必须完全本地闭环）"
      status: unresolved
    - statement: "终端 I/O（键盘输入与屏幕输出）不得写入日志或持久化到磁盘——shell 会话必然包含用户键入的口令与机密"
      status: unresolved
    - statement: "前端页面不得在运行时请求任何外部资源（webfont / CDN / 远程脚本）——单 HTML 必须离线可用（vite-plugin-singlefile + go:embed 硬约束）"
      status: unresolved
---

## 执行纪律（files_modified=18，超阈值的有意取舍，不拆 plan）

本 plan 触及 18 个文件，超出常规阈值——greenfield tracer 必须一次穿透 CLI→PTY→WS→embed→前端全链路，再拆即丢失端到端证明，故不拆 plan，以执行纪律替代：

1. **严格按 Task 1 → Task 2 → Task 3 顺序推进**，不并行、不跳跃。
2. **每个 Task 的 `<verify>` 全绿后立即提交一个原子 commit**（三段提交而非一次大提交），message 形如 `feat(01-01): <task 摘要>`。
3. **中断可恢复**：中断后以 `git log` 确认最后完成的 Task，从下一 Task 的 `<read_first>` 重新进入；每个 Task 的 acceptance_criteria 独立可断言，即为恢复检查点。

<objective>
交付 Phase 1 的行走骨架（tracer 切片）：`wesh [flags] -- <cmd>` 启动后，浏览器打开页面即获得可用的完整交互终端——PTY spawn（exec 数组 + env 白名单）→ master 读写 → WS 二进制帧数据泵 → go:embed 单 HTML → xterm.js 渲染，一条路径穿透全部层，生产质量。

Purpose: 本阶段是整个项目的第一刀；tracer 先证明 CLI→PTY→WS→浏览器全链路成立，后续 plan 只做测试加固、darwin 平台、CI 与文档收口。
Output: 可编译可运行的 wesh 后端 + 前端单 HTML + TestEchoPTY 端到端绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pty/01-CONTEXT.md
@.planning/phases/01-pty/01-RESEARCH.md
@.planning/phases/01-pty/01-PATTERNS.md
@.planning/phases/01-pty/01-UI-SPEC.md
@.planning/phases/01-pty/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 仓库重命名 + Go module + 协议契约 + embed 占位（项目骨架）</name>
  <reversibility rating="one-way">D-01 模块路径 github.com/sworda/wesh 与 D-02 CLI 形态是公开发布契约——用户在 CONTEXT.md 已锁定，仅记录、不再插入 checkpoint 重问（planner-reversibility：已决策的门不打断）。</reversibility>
  <files>go.mod, go.sum, .gitignore, internal/proto/proto.go, web/embed.go, web/dist/index.html</files>
  <read_first>
    - .planning/phases/01-pty/01-CONTEXT.md（D-01~D-08、D-13、D-16、D-18；specifics 99-105）
    - .planning/phases/01-pty/01-RESEARCH.md（协议帧规格 589-603；embed Pattern 4 行 394-426；Pitfall 6 行 547-551）
    - .planning/phases/01-pty/01-PATTERNS.md（§internal/proto/proto.go、§web/embed.go、§配置与 CI 的 go.mod 段）
  </read_first>
  <action>
    1. 仓库重命名（D-01，Wave 0 最前置）：`mv ~/open_src/stow ~/open_src/wesh`——.git 与 .planning 随目录整体移动；此后本 plan 全部命令的工作目录以 `~/open_src/wesh` 为准。若 `git remote get-url origin` 存在则 `git remote set-url origin git@github.com:sworda/wesh.git`，无 origin 则跳过（不创建、不推送）。
    2. 在新根执行 `go mod init github.com/sworda/wesh`（D-01 覆盖 RESEARCH 行 84 的 github 示例路径），随后 `go get github.com/creack/pty@v1.1.24 github.com/coder/websocket@v1.8.15 golang.org/x/sys@v0.47.0`（D-13）。
    3. 写 `.gitignore`：`node_modules/`、`web/dist/*.gz`（web/dist/index.html 占位保持被 git 跟踪，D-18）。
    4. 写 `internal/proto/proto.go`（package proto，协议单一事实源，D-16）：常量 `Input = '0'`、`Resize = '1'`、`Output = '0'`（数据面 binary frame = 1 字节 ASCII 类型 + 载荷）；`func DecodeResize(payload []byte) (cols, rows int, ok bool)`——JSON 解码 `{"cols":C,"rows":R}`（显式 json tag cols/rows），解码失败返回 ok=false（调用方静默丢弃，不关连接），成功时经 `ClampDim` 钳制到 [1,1000] 后返回；`func ClampDim(v int) int`。文件头注释登记 Phase 2 预留：类型字节 '2' PAUSE / '3' RESUME / 'E' ERROR+JSON / 'X' EXIT+退出码与子协议 wesh.v1（现在不实现；收到未知类型由 server 以 1002 关闭）；关闭码纪律：主动发送仅 1000 与 1002，1009 由 SetReadLimit 默认产生，1006 永不写入（RFC6455 §7.4，PITFALLS C9）。
    5. 写 `web/embed.go`（package web，与 dist/ 同级——go:embed 不能引用包目录外文件）：照 RESEARCH Pattern 4（行 398-424）实现 `func Handler() (http.Handler, error)`：`//go:embed all:dist` 嵌入，`fs.Sub` 剥前缀，空路径回落 index.html，Accept-Encoding 含 gzip 且存在 `.gz` 旁路时直发预压缩体并设 `Content-Encoding: gzip` 与 `Content-Type: text/html; charset=utf-8`，否则 `http.FileServerFS` 明文伺服。
    6. 写 `web/dist/index.html` 占位（Pitfall 6）：一行说明文本"前端未构建，请先运行 pnpm -C web build"——保证裸 clone 后 `go build ./...` / `go test ./...` 可编译；Task 3 的真实构建会覆盖它。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go build ./... && go vet ./... && grep -q 'module github.com/sworda/wesh' go.mod && git status --short >/dev/null && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `~/open_src/wesh/.git` 存在且 `git -C ~/open_src/wesh status` 正常；旧路径 `~/open_src/stow` 已不存在（D-01）
    - `go.mod` 首行恰为 `module github.com/sworda/wesh`；`go.sum` 含 creack/pty v1.1.24、coder/websocket v1.8.15、golang.org/x/sys v0.47.0 三个条目（D-13）
    - `go build ./...` 与 `go vet ./...` 退出码 0（此时仅 proto/web 两包，无 main.go——main.go 在 Task 2 落地）
    - `internal/proto/proto.go` 含 `Input = '0'`、`Resize = '1'`、`Output = '0'` 常量与 `DecodeResize`、`ClampDim` 两个函数，钳制区间为 [1,1000]
    - `web/embed.go` 含 `//go:embed all:dist`；`git ls-files web/dist/index.html` 非空（占位已被跟踪）
    - `.gitignore` 含 `node_modules/` 与 `web/dist/*.gz` 两条
  </acceptance_criteria>
  <done>模块路径与依赖版本钉死、协议常量与钳制 helper 落地、embed 占位链可编译，仓库以新名 wesh 存在。</done>
</task>

<task type="tracer">
  <name>Task 2: 端到端 PTY 管道——spawn/io/reap/server/生命周期 + TestEchoPTY（tracer 切片）</name>
  <files>cmd/wesh/main.go, internal/pty/spawn.go, internal/pty/io.go, internal/pty/reap_linux.go, internal/server/server.go, internal/server/e2e_test.go</files>
  <read_first>
    - .planning/phases/01-pty/01-RESEARCH.md（Pattern 1 spawn+env 行 179-233；Pattern 2 Linux 段 行 237-250；Pattern 3 数据泵 行 328-392；Pitfall 1/3/4/7 行 512-557；协议帧 行 589-603）
    - .planning/phases/01-pty/01-PATTERNS.md（§internal/pty/spawn.go、§internal/pty/io.go、§internal/pty/reap_linux.go、§internal/server/server.go、§No Analog Found 的 main.go 与生命周期两行）
    - .planning/phases/01-pty/01-CONTEXT.md（D-02~D-12、D-14~D-17）
    - 本 plan Task 1 产物：internal/proto/proto.go（常量与 DecodeResize 签名）
  </read_first>
  <action>
    1. `internal/pty/spawn.go`：照 RESEARCH Pattern 1。`type Session struct { Cmd *exec.Cmd; Master *os.File; ... }`；`func Start(argv []string) (*Session, error)`：`exec.Command(argv[0], argv[1:]...)` 数组形式（绝不经 shell，D-02/D-15）；`cmd.Env = whitelistEnv()`；不设 cmd.Stdin/Stdout/Stderr（StartWithAttrs 仅在三者全 nil 时接管 tty）与 cmd.Dir；`pty.StartWithSize(cmd, &pty.Winsize{Rows: 24, Cols: 80})`（D-15；首帧窗口可接受，首个 RESIZE 到达即纠正）；失败直接返回 err（creack/pty 失败路径只关自己打开的 fd，Pitfall 1——本包遵守"只关成功打开且登记在册的 fd"纪律）。`func whitelistEnv() []string` 照 RESEARCH 行 210-231：固定 `TERM=xterm-256color` 与 `COLORTERM=truecolor`；按名继承 PATH/HOME/USER/LOGNAME/SHELL；前缀继承 `LANG=` 与 `LC_`；PATH 缺失兜底 `/usr/local/bin:/usr/bin:/bin`——替换式注入，严禁 append(os.Environ(), ...)（ttyd pty.c:441-444 同款泄露，SEC-06）。
    2. `internal/pty/io.go`：`func (s *Session) ReadLoop(onChunk func([]byte))`——32KiB 缓冲循环读 master，n>0 回调，任何 read 错误（含 io.EOF 与 Linux EIO）统一归一为"输出终结"即 return（Pitfall 3，禁止 err == io.EOF 单判）；`func (s *Session) Resize(cols, rows int) error`——调 pty.Setsize（ws_xpixel/ws_ypixel 置 0 是 ttyd 与 creack/pty 共同实践）；`func (s *Session) Close() error`——关 master（有效指针判空，非零值推断）；`func (s *Session) Wait() error`——调平台 awaitExit。带时限 drain（Pitfall 4）：Wait 返回后给读循环最多 200ms 等 EOF/EIO，到点无条件 Close。
    3. `internal/pty/reap_linux.go`（`//go:build linux`）：`func awaitExit(cmd *exec.Cmd) error { return cmd.Wait() }` + 文档注释：Go ≥1.23 的 os/exec 在 Linux 5.3+ 自动 CLONE_PIDFD + waitid(P_PIDFD)（GOROOT syscall/exec_linux.go:310-312、os/pidfd_linux.go），即零额外线程、无僵尸的 pidfd 收割；禁止手写 pidfd_open 与 SIGCHLD+waitpid——会引入第二收割者与 Wait 竞争丢退出码（D-14，RESEARCH Anti-Patterns 行 487-488）。
    4. `internal/server/server.go`：照 RESEARCH Pattern 3 + PATTERNS §internal/server/server.go。`type Server` 持有 sess、attach 原子标志、exitf func(code int)（main 注入 os.Exit；测试注入捕获桩——生命周期必须可测，这是硬约束）；`func New(sess *pty.Session, exitf func(int)) *Server`——New 返回前启动两个 goroutine 并钉死启动点：`go sess.ReadLoop(onChunk)`（D-12 drain：读循环自装配起持续运行，早于任何客户端 attach，attach 前回调直接丢弃；serveWS attach 路径内不得再新建读循环）与生命周期 goroutine（sess.Wait() → 带时限 drain → 1000/exitf 收口，D-10 路径的触发源）；`func (s *Server) Handler() http.Handler`：`/` 走 web.Handler()，`/ws` 走 attach。/ws handler：attach 标志 CompareAndSwap 失败 → 在 Accept 之前 `http.Error(w, ..., http.StatusConflict)`（D-09 第二连接 409）；`websocket.Accept(w, r, &websocket.AcceptOptions{})`——不设置任何选项字段：Subprotocols 留空（wesh.v1 是 Phase 2）、压缩默认禁用、不关闭库默认 Origin 同源校验（D-17；Phase 1 页面与 WS 同 server 同 origin，默认即安全；Origin 白名单属 Phase 3）；SetReadLimit 用库默认 32768 即预认证基线（read.go:107，超限库自动 1009）；ctx 由 `context.Background()` 派生（禁止 r.Context()——hijack 后行为意外，官方 README 明示）。S→C：数据泵 goroutine 独占 WS 写端——sess.ReadLoop 回调内组帧 `frame[0]=proto.Output` 后 c.Write(MessageBinary)；无客户端期间（启动起至首 attach）回调直接丢弃（D-12 drain 语义，防 64KiB PTY 内核缓冲写阻塞）。C→S：单 reader 循环（c.Read 不可并发，Pitfall 7）：空帧 continue；`proto.Input` → sess.Master.Write(data[1:])；`proto.Resize` → proto.DecodeResize 失败静默丢弃、成功则 sess.Resize；未知类型 → `c.Close(websocket.StatusProtocolError, "unknown frame type")`（1002，协议演化无歧义）。
    5. 生命周期（D-10/D-11，PATTERNS §No Analog Found 直译）：子进程退出路径——sess.Wait() 返回后先带时限 drain（io.go 已实现），再向当前已 attach 客户端发 `c.Close(websocket.StatusNormalClosure, "")`（1000），随后 `exitf(code)`：code 取 `*exec.ExitError.ExitCode()`，正常退出为 0；WS 断开路径（reader 返回错误）——`syscall.Kill(-sess.Cmd.Process.Pid, syscall.SIGHUP)`（负 pid = 进程组；setsid 使子进程为组长，D-11）后 `exitf(0)`。两条路径用 sync.Once 收口，exitf 只触发一次。
    6. `cmd/wesh/main.go`（PATTERNS §No Analog Found：D-02~D-11 决策直译，stdlib flag 即可、勿引第三方 CLI 库）：`func parseArgs(args []string) (cfg, argv []string, err error)`——flags 仅 `--port`（int，默认 7681；0 = 随机并打印实际端口，D-06）、`--bind`（string，默认 "0.0.0.0"，D-05）、`--version`（D-04 只此 4 个含 flag 包自带 --help；不加 --once/--read-only/--config）；`--` 后参数原样收集为 argv（D-02）；argv 为空 → 返回错误（D-03，无命令不起登录 shell、不打 help 后跑命令）。`func run(args []string) int`：parseArgs 失败 → stderr 打 `wesh: missing command; usage: wesh [flags] -- <cmd> [args...]` 并返回 2；成功 → pty.Start → net.Listen("tcp", bind:port) → 打印单行 `listening on http://<ln.Addr()>`（D-07：无 banner、无 emoji、无第二行；port 0 时 Addr 已是实际端口，D-06；不打任何 ro/rw 分享链接，D-08）→ http.Serve(ln, srv.Handler()) 并阻塞至 exitf 路径。`func main() { os.Exit(run(os.Args[1:])) }`。
    7. `internal/server/e2e_test.go`：`TestEchoPTY`（VALIDATION 1-01-06）——构造 sess（argv = ["/bin/cat"]）+ New(sess, 捕获桩 exitf) + 127.0.0.1:0 监听 + coder/websocket `Dial` 客户端；发 binary 帧 `'0' + "hello wesh"`；断言 5s 内收到 binary 帧且 `[0]='0'`、载荷恰为同字节；随后 Close 客户端并断言捕获桩被以退出码 0 调用（客户端断开路径，顺带固化 D-11 触发面）。另加 `TestDrainBeforeAttach`（D-12 行为证明）：argv 取 `["seq", "1", "200000"]`（输出约 1.3MB，远超 64KiB PTY 内核缓冲），New(sess, 捕获桩 exitf) 后不连接任何 WS 客户端，断言 5s 内捕获桩以退出码 0 被调用——若 ReadLoop 未自 New 启动 drain，子进程写满 PTY 内核缓冲后阻塞、永不退出，测试超时即暴露接线缺失。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go vet ./... && go test ./internal/server -run 'TestEchoPTY|TestDrainBeforeAttach' -count=1 && go test ./... -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/server -run TestEchoPTY -count=1` 退出 0：INPUT 帧字节与 OUTPUT 帧字节一致（CORE-01 端到端证明）
    - `go test ./... -count=1` 与 `go vet ./...` 退出 0
    - `go test ./internal/server -run TestDrainBeforeAttach -count=1` 退出 0：无客户端 attach 时输出超 64KiB 的命令照常退出（D-12 drain 行为证明）
    - D-12 drain 启动点接线断言：`internal/server/server.go` 的 `func New(` 函数体内含 `ReadLoop(` 启动调用（读循环在客户端 attach 前即开始 drain）；serveWS（/ws attach handler）函数体内不含 `ReadLoop(`（不在 attach 时新建读循环）
    - `internal/pty/spawn.go` 中 `exec.Command` 第一实参为 `argv[0]`、第二实参为 `argv[1:]...`；文件内不存在对 shell 解释器的拼接调用（D-02/D-15）
    - `internal/pty/spawn.go` 中 `cmd.Env = whitelistEnv()` 为替换式赋值；whitelistEnv 含 TERM=xterm-256color 与 COLORTERM=truecolor 两条固定值且不含 `os.Environ()` 追加（SEC-06）
    - `internal/server/server.go` 的 `websocket.AcceptOptions` 复合字面量为空（Phase 2 才填 Subprotocols）；/ws 在 Accept 之前完成 409 判定（D-09）
    <!-- planner-discipline-allow: InsecureSkipVerify -->
    - `grep -c 'InsecureSkipVerify' internal/server/server.go` 输出为 0（D-17 默认 Origin 同源校验保留）
    - `cmd/wesh/main.go` 仅注册 --port/--bind/--version 三个显式 flag（D-04）；无 argv 时 run 返回 2 且 stderr 含 `usage: wesh [flags] -- <cmd> [args...]`（D-03）
    - 生命周期代码含 `syscall.Kill(-sess.Cmd.Process.Pid, syscall.SIGHUP)` 负 pid 进程组调用（D-11）与 `c.Close(websocket.StatusNormalClosure, ...)` 1000 关闭（D-10）；exitf 经 sync.Once 收口
  </acceptance_criteria>
  <done>单路径全链路绿：`wesh -- /bin/cat` 的 echo e2e 通过，CLI/装配/生命周期决策全部落地，整仓 `go test ./...` 绿。</done>
</task>

<task type="auto">
  <name>Task 3: 前端完整接入——xterm 6 + fit + WebGL 回落 + UI-SPEC 契约 + 构建覆盖 dist</name>
  <files>web/package.json, web/pnpm-lock.yaml, web/vite.config.ts, web/tsconfig.json, web/index.html, web/src/main.ts, web/dist/index.html</files>
  <read_first>
    - .planning/phases/01-pty/01-RESEARCH.md（Pattern 5 行 428-483；脚手架文件 行 605-629）
    - .planning/phases/01-pty/01-UI-SPEC.md（Terminal Options Contract 94-128 含全 16 色 theme；Page Shell 131-149；Renderer Contract 153-157；Interaction 161-167；Copywriting 171-183）
    - .planning/phases/01-pty/01-PATTERNS.md（§web/src/main.ts——UI-SPEC 三处扩展严于 Pattern 5，必须合并；§web/index.html）
    - .planning/phases/01-pty/01-CONTEXT.md（D-13、D-16、D-18）
    - 本 plan Task 1 产物：internal/proto/proto.go（帧常量取值）
  </read_first>
  <action>
    1. 手工落脚手架四个文件（不跑交互式 `pnpm create vite`——web/ 已有 embed.go 与 dist/ 会触发覆盖提示）：`web/package.json`（name=wesh-web、private=true、type=module；scripts.dev=vite；scripts.build=`vite build && gzip -k -9 -f dist/index.html`——产出 .gz 旁路供 embed 直发，RESEARCH 行 605-618）；`web/vite.config.ts`（defineConfig + viteSingleFile() 插件，插件对 viteMajor>=8 自动走 codeSplitting:false，RESEARCH 行 620-629）；`web/tsconfig.json`（取 create-vite vanilla-ts 现行模板内容：target/module ESNext 系、moduleResolution bundler、strict、isolatedModules、noEmit——以 create-vite 当前生成物为准微调，CONTEXT A2/Claude's Discretion）；`web/index.html` 按下条契约。
    2. `web/index.html`（UI-SPEC §Page Shell 逐字实现）：head 内 `<meta name="viewport" content="width=device-width, initial-scale=1">`、`<title>wesh</title>`、内联 style——`html, body { margin: 0; padding: 0; height: 100%; background: #000000; overflow: hidden; }` 与 `#terminal { width: 100%; height: 100%; }`（singlefile 保证 CSS 先于 JS 内联——无白闪契约）；body 仅两个顶层元素：`<div id="terminal">` 与默认 hidden 的 `<div id="status">`；#status 结构为全屏遮罩（position:fixed; inset:0; background:rgba(0,0,0,0.6); flex 居中）内含面板（max-width:480px; padding:24px; border-radius:8px; background:#161616; border:1px solid #2a2a2a），面板内三行：标题（16px/600/1.2、#ffffff）、正文（14px/400/1.5、#a3a3a3、与标题间距 8px）、提示行（12px/400/1.5、#a3a3a3、与正文间距 16px，内含 accent 色 #729fcf 的 `<a href="">Reload this page</a>` 原地刷新链接）；字体栈 `system-ui, -apple-system, "Segoe UI", Roboto, sans-serif`；禁止任何 webfont/外部资源请求（UI-SPEC 行 30 singlefile 离线硬约束）；body 尾部 `<script type="module" src="/src/main.ts"></script>`。
    3. `web/src/main.ts`（Pattern 5 骨架 + UI-SPEC 三处扩展合并）：`import '@xterm/xterm/css/xterm.css'`（xterm 必需样式，singlefile 内联）；`new Terminal({...})` 按 UI-SPEC §Terminal Options Contract 逐项显式钉死、不得依赖库默认值：fontSize 14；fontFamily 为 `Menlo, Monaco, "Cascadia Mono", Consolas, "DejaVu Sans Mono", "Liberation Mono", "Courier New", monospace`；lineHeight 1.0；letterSpacing 0；fontWeight 400；fontWeightBold 700；cursorStyle 'block'；cursorBlink true（有意覆盖 xterm 默认 false）；scrollback 10000；allowTransparency false；theme 按 UI-SPEC §Terminal Theme 全量照抄（foreground #ffffff、background #000000、cursor #ffffff、cursorAccent #000000、selectionBackground rgba(255,255,255,0.3)、black #2e3436、brightBlack #555753、red #cc0000、brightRed #ef2929、green #4e9a06、brightGreen #8ae234、yellow #c4a000、brightYellow #fce94f、blue #3465a4、brightBlue #729fcf、magenta #75507b、brightMagenta #ad7fa8、cyan #06989a、brightCyan #34e2e2、white #d3d7cf、brightWhite #eeeeec）。loadAddon(FitAddon)；`term.open(#terminal)`；WebglAddon 以 try/catch 包裹加载、失败留 DOM 渲染器，且必须注册 `webglAddon.onContextLoss(() => webglAddon.dispose())`（GPU 上下文丢失自动回落 DOM 不黑屏，UI-SPEC 行 153-157）。WS：`new WebSocket('ws://' + location.host + '/ws')`、binaryType='arraybuffer'；帧常量 OUTPUT=0x30、INPUT=0x30、RESIZE=0x31 与 proto 包手工对齐（D-16）。onmessage：`buf[0] === OUTPUT` 时 `term.write(buf.subarray(1))`（Uint8Array 二进制安全）。onData：仅 `ws.readyState === WebSocket.OPEN` 时发 INPUT 帧（TextEncoder 编码）；面板显示期间输入静默丢弃。resize 回路：`term.onResize` 直接 sendResize；window resize 经 100ms debounce → `fit.fit()`，发送前 `Number.isInteger(term.cols) && term.cols > 0` 防护（Pitfall 2/RESEARCH C10——display:none 时 proposeDimensions 返回无效值）；sendResize 发 `RESIZE + JSON.stringify({cols, rows})`。`ws.onopen`：`fit.fit(); sendResize(term.cols, term.rows); term.focus();`（启动即键盘可用）。`ws.onerror`/`ws.onclose`：移除 #status 的 hidden 并按三态填文案（UI-SPEC §Copywriting 逐字）：从未 onopen 过 → 标题 "Unable to connect"、正文 "The wesh server is unreachable. It may have exited, or another client is already attached (wesh currently allows a single client)."、提示行 "Check the shell where wesh is running, then [Reload this page]."；onopen 过且 ev.code === 1000 → "Session ended" / "The process exited and the wesh server has stopped." / "Start wesh again from your shell, then [Reload this page]."；onopen 过且非 1000 → "Connection lost" / "The connection closed unexpectedly. In this phase the server exits when the connection drops." / "Start wesh again from your shell, then [Reload this page]."（方括号段为 accent 链接，href="" 原地刷新）。
    4. `pnpm -C web install`（生成 pnpm-lock.yaml；依赖钉死：@xterm/xterm@6.0.0、@xterm/addon-fit@0.11.0、@xterm/addon-webgl@0.19.0；dev：vite@8.2.1、vite-plugin-singlefile@2.3.3、typescript——D-13；vite 的 [SUS] 标记已由 RESEARCH §Package Legitimacy Audit 裁定为启发式误报，pin 版本即可，无需 checkpoint）；`pnpm -C web build` 产出 dist/index.html + index.html.gz，覆盖 Task 1 的占位。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && pnpm -C web build && test -f web/dist/index.html && test -f web/dist/index.html.gz && go build ./... && echo OK</automated>
    <human-check>浏览器三项手动验证汇总在 plan 01-05 收口（FE-01 WebGL 回落 / FE-03 resize 跟随 / 交互可用性）；本任务完成后即可先行自测：`go run ./cmd/wesh -- bash` 开 http://localhost:7681 应可用。</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm -C web build` 退出 0（tsc 类型检查随 vite build 一体）；`web/dist/index.html` 与 `web/dist/index.html.gz` 均存在
    - `web/dist/index.html` 单文件内联全部 JS/CSS：grep 不到 `<script src=` 外链标签（vite-plugin-singlefile 全量 inline）
    - `web/src/main.ts` 含 `onContextLoss` 注册（UI-SPEC Renderer Contract）与三条三态文案字符串 "Unable to connect" / "Session ended" / "Connection lost"（UI-SPEC §Copywriting）
    - `web/src/main.ts` 的 Terminal 构造含 cursorBlink: true、scrollback: 10000、lineHeight: 1.0，且 theme 对象含全部 16 个 ANSI 色键与 UI-SPEC 相同取值
    - `web/index.html` 含 `#terminal` 与 `#status`（默认 hidden）两个顶层元素、`background: #000000` 内联样式、`<meta name="viewport"`、`<title>wesh</title>`；无任何 http(s) 外部资源引用
    - `web/package.json` 依赖恰为 @xterm/xterm@6.0.0、@xterm/addon-fit@0.11.0、@xterm/addon-webgl@0.19.0，devDependencies 含 vite@8.2.1 与 vite-plugin-singlefile@2.3.3（D-13）
    - 帧常量 OUTPUT/INPUT/RESIZE = 0x30/0x30/0x31 与 internal/proto/proto.go 一致（D-16）
    - `go build ./...` 在真实 dist 下退出 0（embed 链以真实产物重建成功，D-18）
  </acceptance_criteria>
  <done>浏览器打开 http://localhost:7681 获得完整交互终端：渲染、输入、resize、状态面板三态均按 UI-SPEC 契约落地；dist 为真实构建产物。</done>
</task>

</tasks>

## Flagged Assumptions（spec-less probe fallback §A/§C — 显式呈现，绝不静默丢弃）

本阶段无 SPEC ## Edge Coverage；edge probe（deterministic）对本阶段 5 条需求全部返回 `unclassified`。
按规则保持 `unresolved`——**绝不 auto-resolve、绝不加 backstop**——在此作为 flagged assumptions 显式登记：

| Requirement | probe 结果 | 状态 | 处置 |
|-------------|-----------|------|------|
| CORE-01 | unclassified — review manually | unresolved | 以 ROADMAP 成功准则 1 + VALIDATION 1-01-01/06（TestExecArrayNoShell / TestEchoPTY）为验收锚点 |
| CORE-02 | unclassified — review manually | unresolved | 以 ROADMAP 成功准则 2 + VALIDATION 1-01-04（TestResize）为验收锚点 |
| FE-01 | unclassified — review manually | unresolved | 以 UI-SPEC §Renderer Contract + VALIDATION 1-01-08 手动 checklist 为验收锚点 |
| FE-03 | unclassified — review manually | unresolved | 以 VALIDATION 1-01-09 手动 checklist + plan 01-05 human-check 为验收锚点 |
| SEC-06 | unclassified — review manually | unresolved | 以 ROADMAP 成功准则 3 + VALIDATION 1-01-02（TestEnvWhitelist）为验收锚点 |

§C 无静默丢弃等式：edge probe surfaced 5 条 = authored 进 must_haves 0 条 + flagged assumptions 5 条 ✓。
§B prohibition recall（两阶段：对抗召回 → 精确化）：5 条需求共产出 3 条存活 prohibition（values/privacy 类），
已 authoring 进本 plan `must_haves.prohibitions`（descriptor-less，无 check_* 字段——下游 dispose 为
flagged-unverified，never auto-dismiss）。canon 安全项（shell 注入 / 凭据入日志 / OWASP 类）按规则不 mint，
留 breadcrumb：由本 plan threat_model T-01-01/T-01-02 与 Phase 3 安全阶段覆盖。
prohibition recall surfaced 3 条 = authored 3 条 ✓。

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 浏览器 ↔ WS/HTTP | 不可信客户端输入（INPUT/RESIZE 帧）从此进入服务端；Phase 1 无认证 |
| 服务端 → 子进程 | argv 与 env 从此边界注入新进程（命令注入与密钥泄露面） |
| 服务端 → 文件系统/embed | 静态资产构建期嵌入，运行时零磁盘依赖 |
| npm/go 依赖安装 | 供应链边界（registry → 构建产物） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | internal/pty/spawn.go exec 路径 | critical | mitigate | `exec.Command(argv[0], argv[1:]...)` 数组形式绝不经 shell（D-02/D-15）；plan 01-02 TestExecArrayNoShell 断言 `$(id)` 不被展开；代码评审红线：出现 shell 拼接即打回 |
| T-01-02 | Information Disclosure | internal/pty/spawn.go env 注入 | high | mitigate | `cmd.Env = whitelistEnv()` 替换式注入（SEC-06/D-15）；plan 01-02 TestEnvWhitelist 宿主注入 AWS_SECRET_ACCESS_KEY 后断言子进程 `env` 不含 |
| T-01-03 | Elevation of Privilege | internal/server/server.go WS Accept（CSWSH） | high | mitigate | 不关闭 coder/websocket 默认 Origin 同源校验（同 Host 放行、跨源拒绝，D-17）；AcceptOptions 为空字面量；Origin 白名单属 Phase 3 |
| T-01-04 | DoS | server.go WS 读路径（预认证消息放大） | medium | mitigate | SetReadLimit 库默认 32768 字节（read.go:107 核实），超限库自动 1009；未知帧类型 1002 关闭；三层上限完整版 Phase 2 |
| T-01-05 | DoS | spawn 失败路径（ttyd close(0) 缺陷） | medium | mitigate | creack/pty 失败只关自己打开的 fd（Pitfall 1 实测）；本包纪律"只关成功打开的 fd"；plan 01-02 TestSpawnFailKeepsStdio 回归 fd 0/1/2 |
| T-01-06 | DoS | 收割路径（僵尸耗尽进程表） | medium | mitigate | `cmd.Wait()` 唯一收割者（D-14；Linux = stdlib pidfd waitid）；plan 01-02 TestReap 断言 /proc/\<pid\> 消失 |
| T-01-07 | DoS | internal/pty/io.go drain（孙进程持有 slave 挂死） | medium | mitigate | Wait 返回后 200ms 时限 drain，到点无条件 close(master)（Pitfall 4）；残留孙进程下次读写得 EIO 自然消亡 |
| T-01-08 | Tampering | 关闭码合法性 | low | mitigate | 主动发送仅 1000/1002；1009 由库默认产生；1006 永不写入（RFC6455 §7.4，PITFALLS C9） |
| T-01-09 | Information Disclosure | 监听面（无认证 + bind 0.0.0.0） | high | accept | D-05 用户明确锁定：Phase 1 接受 LAN 可达的取舍；补偿控制 = README 首屏醒目警示"Phase 1 无认证，仅在可信网络使用"（CONTEXT specifics 强制，plan 01-05 落地）+ Phase 3 认证/TLS 收口 |
| T-01-10 | Tampering | RESIZE 恶意尺寸 | low | mitigate | proto.DecodeResize 钳制 cols/rows 到 [1,1000]（D-16）；JSON 解码失败静默丢弃不关连接 |
| T-01-SC | Tampering | npm/pnpm/go 依赖安装 | high | mitigate | RESEARCH §Package Legitimacy Audit 全部 Approved（vite 的 [SUS] 已裁定为 too-new 启发式误报：164M 周下载+官方仓库，pin 8.2.1 即可，无需 blocking checkpoint）；版本全部钉死（D-13）；CI 走 pnpm --frozen-lockfile 与 go.sum 校验 |
</threat_model>

<verification>
- `go vet ./... && go test ./... -count=1` 全绿
- `go test ./internal/server -run TestEchoPTY -count=1` 绿（tracer 端到端证明）
- `pnpm -C web build` 绿且 dist 为真实单 HTML + .gz
- 启动 `go run ./cmd/wesh -- bash` 后 curl / 返回 HTML、curl -H 'Accept-Encoding: gzip' / 返回 gzip 体
</verification>

<success_criteria>
- ROADMAP 成功准则 1 成立：浏览器获得完整交互终端，键盘/输出双向实时
- ROADMAP 成功准则 2 的实现面成立：RESIZE 帧 → 钳制 → TIOCSWINSZ 链路代码就绪（自动化证明在 plan 01-02 TestResize）
- ROADMAP 成功准则 3 的实现面成立：env 白名单一次到位（证明在 plan 01-02 TestEnvWhitelist）、Linux 收割无僵尸（证明在 plan 01-02 TestReap）
- 本 plan 的 must_haves.truths 全部可由 acceptance_criteria 对应断言证明
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

新文件（Go module `github.com/sworda/wesh` 之下）：
- `go.mod` / `go.sum` / `.gitignore`
- `cmd/wesh/main.go`：`main`、`run(args []string) int`、`parseArgs(args []string)`；CLI flags `--port`（默认 7681，0=随机）/ `--bind`（默认 0.0.0.0）/ `--version` / `--help`
- `internal/proto/proto.go`：`Input = '0'`、`Resize = '1'`、`Output = '0'`；`DecodeResize(payload []byte) (cols, rows int, ok bool)`；`ClampDim(v int) int`
- `internal/pty/spawn.go`：`type Session`、`Start(argv []string) (*Session, error)`、`whitelistEnv() []string`
- `internal/pty/io.go`：`(*Session).ReadLoop(onChunk func([]byte))`、`(*Session).Resize(cols, rows int) error`、`(*Session).Close() error`、`(*Session).Wait() error`
- `internal/pty/reap_linux.go`：`awaitExit(cmd *exec.Cmd) error`（`//go:build linux`）
- `internal/server/server.go`：`type Server`、`New(sess *pty.Session, exitf func(int)) *Server`、`(*Server).Handler() http.Handler`
- `internal/server/e2e_test.go`：`TestEchoPTY`、`TestDrainBeforeAttach`（D-12）
- `web/embed.go`：`Handler() (http.Handler, error)`（package web，`//go:embed all:dist`）
- `web/dist/index.html`（占位 → 真实构建产物）与构建产物 `web/dist/index.html.gz`
- `web/package.json` / `web/pnpm-lock.yaml` / `web/vite.config.ts` / `web/tsconfig.json` / `web/index.html`
- `web/src/main.ts`：帧常量 `OUTPUT/INPUT/RESIZE`（0x30/0x30/0x31）、`sendResize(cols, rows)`、#status 三态面板逻辑

<output>
Create `.planning/phases/01-pty/01-01-SUMMARY.md` when done（相对路径——Task 1 完成后仓库根为 ~/open_src/wesh，相对路径自然解析到新根）。
</output>
