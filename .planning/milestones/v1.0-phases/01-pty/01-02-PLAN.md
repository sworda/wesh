---
phase: 01-pty
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internal/pty/spawn_test.go
  - internal/pty/io_test.go
  - internal/pty/reap_test.go
autonomous: true
requirements:
  - SEC-06
  - CORE-02
  - CORE-01

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "argv 以 exec 数组原样传递：含 `$(id)` 的命令行不被 shell 展开（CORE-01，D-02/D-15，VALIDATION 1-01-01）"
    - "宿主注入 AWS_SECRET_ACCESS_KEY 后，whitelistEnv() 输出与 spawn 的子进程 `env` 输出均不含该变量（SEC-06，VALIDATION 1-01-02）"
    - "spawn 不存在的二进制返回错误，且服务端自身 fd 0/1/2 保持有效（Fsync 非 EBADF）——ttyd pty.c:87,112 close(0) 缺陷回归（VALIDATION 1-01-03，Pitfall 1）"
    - "PTY 尺寸由 24 行 80 列调整到 50 行 132 列（rows 24→50、cols 80→132；按 01-01 签名 Resize(cols, rows) 传 Resize(132, 50)）后，子进程 `stty size` 按 'rows cols' 序报告 `50 132`（CORE-02，TIOCSWINSZ 同步，VALIDATION 1-01-04）"
    - "短命令退出后 /proc/<pid> 消失：无僵尸残留（成功准则 3，VALIDATION 1-01-05）"
  artifacts:
    - path: "internal/pty/spawn_test.go"
      provides: "spawn 三专项测试"
      contains: "TestExecArrayNoShell / TestEnvWhitelist / TestSpawnFailKeepsStdio"
    - path: "internal/pty/io_test.go"
      provides: "resize 集成测试"
      contains: "TestResize"
    - path: "internal/pty/reap_test.go"
      provides: "收割无僵尸测试（linux）"
      contains: "TestReap"
  key_links:
    - from: "internal/pty/spawn_test.go"
      to: "internal/pty/spawn.go"
      via: "直接调用 Start / whitelistEnv 断言白名单与 exec 数组语义"
      pattern: "whitelistEnv|pty\\.Start|Start\\("
    - from: "internal/pty/io_test.go"
      to: "internal/pty/io.go"
      via: "Session.Resize → pty.Setsize → TIOCSWINSZ → 子进程 stty 可见"
      pattern: "\\.Resize\\("
    - from: "internal/pty/reap_test.go"
      to: "internal/pty/reap_linux.go"
      via: "Session.Wait → awaitExit → cmd.Wait() 收割"
      pattern: "\\.Wait\\("
---

<objective>
为 plan 01-01 交付的 PTY 引擎补齐专项测试：SEC-06 env 白名单、exec 数组不经 shell、spawn 失败不伤 fd、resize TIOCSWINSZ、收割无僵尸——把 ROADMAP 成功准则 2/3 与 SEC-06 从"实现面成立"升级为"自动化证明成立"。

Purpose: 这些断言全部来自 RESEARCH 本机原型实测输出（行 563-575）与 VALIDATION.md 测试映射，是本 phase 防 ttyd 已核实缺陷重蹈的回归网。
Output: internal/pty 下三个测试文件，`go test ./internal/pty -count=1`（含 -race）全绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pty/01-CONTEXT.md
@.planning/phases/01-pty/01-RESEARCH.md
@.planning/phases/01-pty/01-PATTERNS.md
@.planning/phases/01-pty/01-VALIDATION.md
@.planning/phases/01-pty/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: spawn 专项三测——exec 数组 / env 白名单 / 失败不伤 fd</name>
  <files>internal/pty/spawn_test.go</files>
  <read_first>
    - internal/pty/spawn.go（plan 01-01 产物——被测对象当前实现）
    - .planning/phases/01-pty/01-VALIDATION.md（Per-Task 映射 1-01-01/02/03，行 43-45）
    - .planning/phases/01-pty/01-PATTERNS.md（§测试文件表格的 spawn_test.go 行：断言要点）
    - .planning/phases/01-pty/01-RESEARCH.md（Pattern 1 行 179-233；原型实测输出行 563-575；Pitfall 1 行 512-517）
    - .planning/phases/01-pty/01-CONTEXT.md（D-02、D-15）
  </read_first>
  <action>
    在 `internal/pty/spawn_test.go`（package pty 内测）写三个测试，断言要点照 PATTERNS §测试文件与 RESEARCH 原型实测输出：
    1. `TestExecArrayNoShell`（VALIDATION 1-01-01，CORE-01/D-02）：`Start([]string{"/bin/echo", "-n", "$(id)"})`，ReadLoop 收集输出至进程退出，断言输出恰为字面量 `$(id)`——若经 shell 展开则会变成 uid/gid 串，测试即红。
    2. `TestEnvWhitelist`（VALIDATION 1-01-02，SEC-06）：双层断言——(a) 单元层：`t.Setenv("AWS_SECRET_ACCESS_KEY", "test-leak-value")` 后调 `whitelistEnv()`，断言结果切片无任何元素含 `AWS_SECRET_ACCESS_KEY`，且含 `TERM=xterm-256color` 与 `COLORTERM=truecolor`；(b) e2e 层：同注入下 `Start([]string{"/usr/bin/env"})`，收集全部输出，断言不含 `AWS_SECRET_ACCESS_KEY`（ttyd pty.c:441-444 全继承缺陷的对照回归）。
    3. `TestSpawnFailKeepsStdio`（VALIDATION 1-01-03，Pitfall 1）：`Start([]string{"/nonexistent/wesh-definitely-missing-binary"})` 断言返回 err 非 nil；随后对 fd 0/1/2 逐个 `os.NewFile(uintptr(fd), ...).Fsync()` 或等价 syscall 探测，断言错误非 EBADF（fd 仍有效）；用例结束打印一次标记日志证明测试进程 stdio 完好。
    三个测试统一超时护栏（如 10s）：ReadLoop 收集用带 deadline 的读法或 select+time.After，防挂死；子进程短命令退出后务必 Wait 收割（避免测试自身制造僵尸）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test ./internal/pty -run 'TestExecArrayNoShell|TestEnvWhitelist|TestSpawnFailKeepsStdio' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/pty -run 'TestExecArrayNoShell|TestEnvWhitelist|TestSpawnFailKeepsStdio' -count=1` 退出 0
    - TestEnvWhitelist 含宿主注入 `AWS_SECRET_ACCESS_KEY` 的 t.Setenv 调用与"输出不含该键"的断言（单元 + e2e 两层）
    - TestSpawnFailKeepsStdio 断言 spawn 错误非 nil 且 fd 0/1/2 探测非 EBADF
    - TestExecArrayNoShell 断言输出恰为字面量 `$(id)`
  </acceptance_criteria>
  <done>spawn 路径三条红线（不经 shell / env 零泄露 / 失败不伤 stdio）全部自动化锁定。</done>
</task>

<task type="auto">
  <name>Task 2: 集成两测——resize TIOCSWINSZ + 收割无僵尸</name>
  <files>internal/pty/io_test.go, internal/pty/reap_test.go</files>
  <read_first>
    - internal/pty/io.go 与 internal/pty/reap_linux.go（plan 01-01 产物——被测对象当前实现）
    - .planning/phases/01-pty/01-VALIDATION.md（Per-Task 映射 1-01-04/05，行 46-47）
    - .planning/phases/01-pty/01-PATTERNS.md（§测试文件表格的 io_test.go / reap_test.go 行）
    - .planning/phases/01-pty/01-RESEARCH.md（原型实测：stty before/after resize [24 80 50 132]，行 571；reaped OK 行 570）
    - .planning/phases/01-pty/01-CONTEXT.md（D-14、D-16）
  </read_first>
  <action>
    1. `internal/pty/io_test.go`：`TestResize`（VALIDATION 1-01-04，CORE-02）——`Start([]string{"/bin/sh", "-c", "stty size; sleep 1; stty size"})` 仅作测试夹具（产品 spawn 路径不引入 shell，此为用例编排，属测试代码）；ReadLoop 持续收集输出；起 100-200ms 后调 `sess.Resize(132, 50)`——01-01 钉死的签名为 `Resize(cols, rows int)`（cols 在前），此处传 cols=132、rows=50，即语义目标 24 行 80 列 → 50 行 132 列；切勿按 (rows, cols) 序误传 Resize(50, 132)，否则子进程将报 "132 50" 必红（内部经 pty.Setsize 触发 TIOCSWINSZ）；Wait 后断言收集到的两行 stty 输出依次为 `24 80` 与 `50 132`（stty size 输出序为 "rows cols"，RESEARCH 行 571 本机实测该逻辑成立）；超时护栏 10s。
    2. `internal/pty/reap_test.go`：`TestReap`（VALIDATION 1-01-05，linux 专属——文件头加 `//go:build linux`，darwin 等效语义由 plan 01-04 的 TestKqueue 系列覆盖）——`Start([]string{"/bin/true"})`（或等价瞬时退出命令），记录 `sess.Cmd.Process.Pid`；`sess.Wait()` 返回后断言 `/proc/<pid>` 目录不存在（os.Stat 返回 IsNotExist）；断言 Wait 错误为 nil 或 *exec.ExitError 且 ExitCode() 为 0（/bin/true 正常退出）；重复 20 次循环跑（高频建销不累积僵尸，佐证 T-01-06）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test ./internal/pty -run 'TestResize|TestReap' -count=1 -v && go test -race -count=1 ./internal/pty</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/pty -run 'TestResize|TestReap' -count=1` 退出 0
    - TestResize 以 `sess.Resize(132, 50)` 调用（Resize(cols, rows) 签名：cols=132、rows=50），断言序列含 `24 80` → `50 132`（stty 按 'rows cols' 序输出：初始 24 行 80 列与 resize 后 50 行 132 列双双断言）
    - TestReap 断言 /proc/\<pid\> 消失且带 20 次循环；文件含 `//go:build linux` tag
    - `go test -race -count=1 ./internal/pty` 退出 0（竞态检测无报告）
  </acceptance_criteria>
  <done>resize TIOCSWINSZ 与零线程收割两条成功准则自动化证明成立，internal/pty 包 -race 全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试进程 → 子进程 | 本 plan 新增的唯一边界活动是以测试夹具 spawn 子进程（含 /bin/sh -c 测试编排——仅限测试代码，产品 spawn 路径纪律不变） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | internal/pty/spawn_test.go | critical | mitigate | 以 TestExecArrayNoShell 固化 T-01-01：`$(id)` 字面量不被展开——产品代码退化为 shell 拼接时测试即红 |
| T-02-02 | Information Disclosure | internal/pty/spawn_test.go | high | mitigate | 以 TestEnvWhitelist 固化 T-01-02：宿主注入 AWS_SECRET_ACCESS_KEY，白名单构造函数与子进程 `env` 输出双层断言不含 |
| T-02-03 | DoS | internal/pty/spawn_test.go / io_test.go / reap_test.go | medium | mitigate | 以 TestSpawnFailKeepsStdio / TestResize / TestReap 固化 T-01-05/06：fd 0/1/2 存活、TIOCSWINSZ 生效、/proc/\<pid\> 消失 |
| T-02-04 | Tampering | 测试夹具自身（sh -c 编排） | low | accept | /bin/sh -c 仅出现在 TestResize 的测试编排串中，不进入产品 spawn 路径；plan 01-01 的 acceptance_criteria 已对 spawn.go 本体独立断言 |
</threat_model>

<verification>
- `go test ./internal/pty -count=1 -v` 全绿（五个测试函数）
- `go test -race -count=1 ./internal/pty` 绿
- 整仓回归：`go test ./... -count=1` 绿
</verification>

<success_criteria>
- VALIDATION.md 1-01-01/02/03/04/05 五项全部从 pending 变 green
- ROADMAP 成功准则 2（resize 同步）与准则 3（env 零泄露 + 收割无僵尸）获得自动化证明
- SEC-06 在本 phase 的验收锚点成立
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `internal/pty/spawn_test.go`：`TestExecArrayNoShell`、`TestEnvWhitelist`、`TestSpawnFailKeepsStdio`
- `internal/pty/io_test.go`：`TestResize`
- `internal/pty/reap_test.go`：`TestReap`（`//go:build linux`）

<output>
Create `.planning/phases/01-pty/01-02-SUMMARY.md` when done
</output>
