---
phase: 06-session-lifecycle
plan: 07
type: execute
wave: 5
depends_on: ["06-05", "06-06"]
files_modified:
  - README.md
  - .planning/phases/06-session-lifecycle/06-UAT.md
  - .planning/phases/06-session-lifecycle/06-VALIDATION.md
  - web/uat/phase02.mjs
  - web/uat/phase03.mjs
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - CORE-05

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "README 生命周期节改写：--once（标明等价关系 ≡ --max-clients=1 --exit-when-empty=0，D-12 逐字）+ --exit-when-empty[=duration] 三形态（不写=不开启 / 裸写=最后一个客户端断开立即退出 / =duration=重连宽限，计时内任一端 attach 取消退出）+ 断开退出收口行为明示（SIGHUP 子进程进程组 → wesh 进程退出状态按 OQ1 门裁决值——accept-255 形态逐字写「子进程被 SIGHUP 终结，wesh 退出状态 255」）+ EXIT 终结帧行为（子进程退出后所有在线客户端收到含退出码/信号人话的终结提示再以 1000 关闭，非静默断开）"
    - "README 重连语义明示段（D-07 + ROADMAP 成功准则 3 文档侧）：自动重连触发范围（仅异常断开——被踢 1013 与服务端明确关闭不自动重连）+ 退避 1s×2 封顶 30s 无限重试 + 「Reconnect now」手动入口 + 「重连目标 = 同一 URL 的当前进程；服务端重启后是全新会话（share token 重启即废——重启后需用新链接）」+ 无滚动回放明示（重连成功清屏后靠程序重绘；行内 shell 历史用 tmux/herdr 覆盖——ROADMAP 既定分工）+ owner 重连不恢复写权限（D-06——重连按新 attach 走递补语义）"
    - "README flag 表两行（既有 flag 表逐行『flag 名 + 默认 + 语义』文风）：--once 与 --exit-when-empty；协议节帧表补 EXIT 'X' 行（类型字节 + JSON 载荷形状——proto.go 单一事实源同步）"
    - "06-UAT.md 人工清单（01..05-UAT.md 同位置同形态，外部浏览器可执行）：断网 30s 恢复自动重连观感（断网点→Reconnecting 面板倒计时→恢复后自动接回）/ 重连成功清屏与程序重绘观感 / Reconnect now 手动跳过 / 子进程退出 Session ended 面板退出码与信号人话 / --once 第二客户端 503 页 / owner 断线重连后 [ro] 前缀出现（不恢复写权限）"
    - "06-VALIDATION.md 同步：Per-Task Verification Map 占位契约行替换为实测行（06-01..06-06 各 plan 的自动化断言入口与命令）+ frontmatter nyquist_compliant 与 wave_0_complete 按实况置位 + Manual-Only Verifications 表两行与 06-UAT.md 互指"
    - "全量验证六段式全绿（06-VALIDATION full suite command）：GOROOT gofmt -l 零差异（/usr/bin/gofmt 陈旧须用 GOROOT 版本——Phase 01-03 决策，$(go env GOROOT)/bin/gofmt）+ go vet ./... + go test -race -count=1 ./... + time pnpm -C web build + node --test web/src/lib/*.test.ts + go build -o /tmp/wesh-uat/wesh ./cmd/wesh 冒烟（--port 0 启动行解析）+ UAT 全脚本对同一构建产物全过（phase02/03/04/05/05-dom/05-dims/04-t1-width/06/06-dom）"
    - { statement: "浏览器原生断网/恢复事件序列与 tmux/herdr 屏幕重绘观感为人工验证项（headless 硬约束——06-VALIDATION Manual-Only Verifications 既定）——06-UAT.md 清单承载；自动化层（phase06.mjs S6 协议等价物 + phase06-dom.mjs 合成事件状态机）已覆盖等价协议与逻辑面", verification: backstop }
  prohibitions:
    - statement: "README/06-UAT.md 示例不得含真实 token——示例链接用占位符（05-09 同款红线：<ro-token> 形态）"
      status: resolved
      verification: judgment
    - statement: "旧 UAT 脚本（phase02/03/04/05 系列）适配只允许『预期帧序列补 EXIT 帧』与注释/文案同步两类变更——借机删除或放宽既有安全断言（握手拒绝/认证矩阵/上限/踢出）即回归（05-09 适配纪律同款）"
      status: resolved
      verification: judgment
  artifacts:
    - path: "README.md"
      provides: "生命周期节（--once/--exit-when-empty/EXIT 帧/退出状态）+ 重连语义明示段 + flag 表两行 + 协议节 EXIT 行"
      contains: "exit-when-empty"
    - path: ".planning/phases/06-session-lifecycle/06-UAT.md"
      provides: "人工验证清单（外部浏览器可执行）"
      contains: "Reconnect"
    - path: ".planning/phases/06-session-lifecycle/06-VALIDATION.md"
      provides: "Per-Task Verification Map 实测行 + nyquist 置位"
      contains: "06-06"
  key_links:
    - from: "README.md"
      to: "cmd/wesh/main.go (parseArgs)"
      via: "flag 表与生命周期节逐行对应 CLI 实际形态（--once 等价关系与 --exit-when-empty 三形态——文档与 parse 行为同真）"
      pattern: "exit-when-empty"
    - from: "web/uat 全脚本"
      to: "go build 产物"
      via: "六段式对同一构建产物全量回归（含 phase06/phase06-dom 两新脚本）"
      pattern: "wesh-uat"
---

<objective>
Phase 6 收口：README 生命周期与重连文档（等价关系/三形态/退出状态/重连语义边界全套明示）+ 06-UAT.md 人工清单 + 06-VALIDATION.md 同步 + 全量六段式与全 UAT 脚本回归。

Purpose: 文档兑现 D-07/D-06 的明示义务与 ROADMAP 成功准则 3 的「文档明示」尾部；六段式回归证明本 phase 对既有行为零回归——phase 验收（/gsd:verify-work）前置。
Output: README 节改写 + 06-UAT.md（新）+ VALIDATION 同步 + 全量绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-session-lifecycle/06-CONTEXT.md
@.planning/phases/06-session-lifecycle/06-VALIDATION.md
@.planning/phases/06-session-lifecycle/06-01-SUMMARY.md
@.planning/phases/06-session-lifecycle/06-02-SUMMARY.md
@.planning/phases/06-session-lifecycle/06-03-SUMMARY.md
@.planning/phases/06-session-lifecycle/06-04-SUMMARY.md
@.planning/phases/06-session-lifecycle/06-06-SUMMARY.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README 生命周期节与重连语义 + flag 表 + 协议节 EXIT 行 + 06-UAT.md + VALIDATION 同步</name>
  <files>README.md, .planning/phases/06-session-lifecycle/06-UAT.md, .planning/phases/06-session-lifecycle/06-VALIDATION.md</files>
  <read_first>
    - README.md（全文——生命周期节与 flag 表、协议节的既有文风与落点；05-09 多客户端节先例）
    - cmd/wesh/main.go（06-04 产出——两 flag 的 help 文案与展开/校验行为，文档与 parse 行为同真的核对源）
    - internal/proto/proto.go（06-01 产出——EXIT 常量与载荷注释，协议节同步源）
    - .planning/phases/06-session-lifecycle/06-CONTEXT.md（D-06/D-07 明示义务逐字 + Deferred Ideas——不得写入本期文档的范畴）
    - .planning/phases/06-session-lifecycle/06-VALIDATION.md（占位契约表现状 + Manual-Only Verifications 表）
    - .planning/phases/05-multi-client/05-UAT.md（人工清单同位置同形态先例）
  </read_first>
  <action>
    1. `README.md`（照既有文风；示例 token 用占位符——05-09 红线）：
       - 生命周期节改写：--once 行（等价关系 ≡ --max-clients=1 --exit-when-empty=0 逐字标明，D-12）；--exit-when-empty 三形态行（含迁移语义明示——review 命名误读关切吸收：「所有客户端断开」= 最后一个客户端断开的**迁移事件**，启动期零客户端不触发退出，非「启动时为空即退」）；断开退出收口说明（SIGHUP 子进程进程组 + wesh 退出状态按 06-02 Task 1 门裁决值逐字——accept-255 形态：「子进程被 SIGHUP 终结，wesh 退出状态 255」）；EXIT 终结帧行为说明（含退出码/信号人话提示后 1000 关闭，非静默断开——SESS-03）。
       - 重连语义明示段（D-07）：触发范围（仅异常断开；1013 被踢与服务端明确关闭不自动重连）、退避 1s×2 封顶 30s 无限重试、「Reconnect now」手动入口、「重连目标 = 同一 URL 的当前进程；服务端重启后是全新会话（share token 重启即废，重启后需用新链接）」、无滚动回放（重连成功清屏后靠程序重绘；行内 shell 历史用 tmux/herdr——ROADMAP 既定分工）、owner 重连不恢复写权限（D-06）。
       - flag 表加 --once 与 --exit-when-empty 两行（flag 名 + 默认 + 语义逐行格式）；协议节帧表补 EXIT 行（'X' S→C JSON {"exit_code":N,"message":M}——信号死亡 exit_code=-1）。
    2. 新建 `.planning/phases/06-session-lifecycle/06-UAT.md`（05-UAT.md 同形态）：must_haves 第四条六项人工清单逐条落为可执行步骤（每项：**编号逐步**操作步骤 + 预期观察 + 对应需求 ID——review LOW 吸收，形态如断网场景：「1. 浏览器打开 wesh 会话 → 2. 断网（飞行模式/拔线）→ 3. 等待 30s 观察 Reconnecting 面板倒计时 → 4. 恢复网络 → 5. 预期：5s 内自动接回原会话」）；头部登记 headless 豁免前提与自动化覆盖对照（phase06.mjs/phase06-dom.mjs 已覆盖的等价面）。
    3. `.planning/phases/06-session-lifecycle/06-VALIDATION.md`：Per-Task Verification Map 占位行替换为实测行（06-01..06-06：每行 Task ID/Plan/Wave/Requirement/测试类型/Automated Command/文件存在状态）；frontmatter nyquist_compliant: true 与 wave_0_complete: true 按实况置位（全绿后）；Manual-Only Verifications 表两行与 06-UAT.md 互指（指向新文件路径）；同步修正 Test Infrastructure 表与 Sampling Rate 中的失效引用——项目内不存在该测试框架（web/package.json 与 web/uat/package.json 均零命中；web/src/lib/ 下实为 node --test 直跑 .ts，与 RESEARCH Validation Architecture 一致）：Quick run command 前端段改为 `node --test web/src/lib/*.test.ts`、Full suite command 与 Sampling Rate 各行改为实际 UAT 脚本清单（phase02/03/04/04-t1-width/05/05-dom/05-dims/06/06-dom 九脚本对同一构建产物）、Config file 行的框架在位表述改为 node --test 直跑实况（Node 24 内建 type stripping）。
  </action>
  <verify>
    <automated>test "$(grep -c 'exit-when-empty' README.md)" -ge 3 && test "$(grep -c 'Reconnect' README.md)" -ge 1 && test "$(grep -c 'Reconnect' .planning/phases/06-session-lifecycle/06-UAT.md)" -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - README 含：--once 等价关系逐字（--max-clients=1 --exit-when-empty=0）、--exit-when-empty 三形态、wesh 退出状态明示（OQ1 裁决值）、EXIT 帧行为、重连语义段（触发范围/退避/手动入口/重连目标/无滚动回放/不恢复写权限六要点）
    - grep -c 'exit-when-empty' README.md >= 3（生命周期节 + flag 表 + 等价关系）
    - grep -c "'X'\|0x58" README.md >= 1（协议节 EXIT 行）
    - 06-UAT.md 六项人工清单齐备且含 headless 豁免前提登记
    - 06-VALIDATION.md 占位契约行已替换、nyquist_compliant 与 wave_0_complete 按实况置位；Test Infrastructure 表与 Sampling Rate 的失效框架引用清零（grep -c vitest 06-VALIDATION.md == 0），Quick command = `node --test web/src/lib/*.test.ts`、Full suite/Sampling Rate = 实际九脚本 UAT 清单
    - README/06-UAT.md 全文无真实 token 值（人工核读——占位符形态）
  </acceptance_criteria>
  <done>文档面完整：README 生命周期/重连/flag/协议四节与实现同真；人工清单与 VALIDATION 同步落位——Task 2 全量回归前置。</done>
</task>

<task type="auto">
  <name>Task 2: 全量六段式验证 + 全 UAT 脚本回归（含旧脚本 EXIT 帧适配）</name>
  <files>web/uat/phase02.mjs, web/uat/phase03.mjs</files>
  <read_first>
    - .planning/phases/06-session-lifecycle/06-VALIDATION.md（Task 1 同步后形态——full suite command 行）
    - web/uat/phase02.mjs（握手/关闭码断言区——子进程退出场景若有帧序严格断言，EXIT 帧适配点）
    - web/uat/phase03.mjs（生命周期适配区——05-09 已迁移过多客户端语义，同区复查 EXIT 帧影响）
    - .planning/phases/05-multi-client/05-09-SUMMARY.md（六段式逐条命令与先例——GOROOT gofmt 等纪律）
  </read_first>
  <action>
    1. 六段式逐条执行并留证（输出摘要进 SUMMARY）：
       - $(go env GOROOT)/bin/gofmt -l .（零差异——/usr/bin/gofmt 陈旧纪律，01-03/02-06/03-06/05-09 先例；若有本 phase 新增文件的排版差异，-w 修正后独立 style 提交，纯排版零语义）
       - go vet ./...
       - go test -race -count=1 ./...
       - time pnpm -C web build
       - node --test web/src/lib/*.test.ts
       - go build -o /tmp/wesh-uat/wesh ./cmd/wesh + 冒烟（显式断言形态——review 吸收：spawn `--port 0 -- bash --norc --noprofile` → 8s 内 stdout 须出现 listening on 行且正则解析出端口数字（\d+），随后 SIGKILL 收口；行缺失或端口解析失败即红——02-06「随机端口 + 启动行解析」先例的具体断言面）
    2. UAT 全脚本对同一构建产物回归：node web/uat/phase02.mjs、phase03.mjs、phase04.mjs、phase04-t1-width.mjs、phase05.mjs、phase05-dom.mjs、phase05-dims.mjs、phase06.mjs、phase06-dom.mjs 全过。
       - 旧脚本适配纪律（prohibition 逐字）：若 phase02/03 系列中子进程退出场景因新增 EXIT 帧出现帧序严格断言变红，适配 = 预期帧序列补 EXIT 帧或读至 close 丢弃途中帧（05-09『生命周期预期改写』同款纪律）——只允许该两类变更；任何安全断言（握手拒绝/认证矩阵/上限/踢出）不得删除或放宽；适配点逐处注释登记。
       - 适配点预判明示（review MEDIUM 吸收——planner 本轮逐行核查结论）：phase02.mjs 唯一涉子进程退出的场景为 T4a scenarioExit（148-160 行——dialHello → waitClose 仅断言 close.code===1000，无帧序列/帧集合严格断言，EXIT 帧先于 1000 到达不触碰其断言面）；phase03.mjs 无子进程退出场景（全部场景以 inst.kill() 收口或 spawn-exit 校验矩阵）——**预期零适配**；执行期首跑两脚本复核该预判，若有个别变红按上方白名单纪律适配并逐处注释登记；phase04/05 系列同首跑复核。
    3. 全绿后收尾：git 状态清洁（dist 与全部产物已随各 plan 提交）；STATE.md 非本任务范围（orchestrator 收口）。
  </action>
  <verify>
    <automated>$(go env GOROOT)/bin/gofmt -l . && go vet ./... && go test -race -count=1 ./... && time pnpm -C web build && node --test web/src/lib/*.test.ts && go build -o /tmp/wesh-uat/wesh ./cmd/wesh && node web/uat/phase02.mjs && node web/uat/phase03.mjs && node web/uat/phase04.mjs && node web/uat/phase05.mjs && node web/uat/phase05-dom.mjs && node web/uat/phase05-dims.mjs && node web/uat/phase04-t1-width.mjs && node web/uat/phase06.mjs && node web/uat/phase06-dom.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 六段式逐条退出 0（gofmt -l 输出为空即零差异——命令首段输出非空即红）
    - 九个 UAT 脚本全部退出 0（skipped 豁免记录不计失败）
    - 旧脚本适配点（若有）逐处注释登记且断言面零丢失（git diff 核读：无安全断言删除/放宽）
    - go test -race -count=1 ./... 全绿（含 06-01/06-02 全部新测试）
  </acceptance_criteria>
  <done>Phase 6 收口完成：六段式与九脚本全绿——SESS-01/02/03/CORE-05 全部落地且对既有行为零回归，phase 验收就绪（/gsd:verify-work 前置）。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

四条需求（SESS-01/02/03、CORE-05）的 probe 状态（unclassified — review manually）已在 06-01/06-02/06-03 逐一显式登记（flagged-unverified）；本 plan 为四条提供文档与回归证据收口——无新增假设。

## Review Dispositions (06-REVIEWS, OpenCode)

| 发现 | 处置 |
|------|------|
| MEDIUM：phase02/03 适配点未明示（哪些测试受 EXIT 帧影响） | 已吸收——Task 2 补逐行核查结论：phase02.mjs T4a（148-160）仅断言 close code 无帧序列断言、phase03.mjs 无子进程退出场景，预期零适配 + 首跑复核纪律 |
| MEDIUM：smoketest 断言未具体化 | 已吸收——Task 2 冒烟补显式断言形态（listening on 行 + 端口数字解析 + SIGKILL 收口） |
| LOW：06-UAT.md 清单项需步骤化可执行 | 已吸收——Task 1 每项改编号逐步形态（含断网场景五步走示例） |
| Suggestion：加 README token 脱敏核查清单 | 已覆盖——acceptance_criteria 既有「README/06-UAT.md 全文无真实 token 值（人工核读——占位符形态）」条款，prohibitions 第一条同款锁定 |
| LOW（来自 06-04 评审）：--exit-when-empty 命名易被误读为「启动时为空即退」 | 已吸收——Task 1 README 三形态行加迁移语义明示 |

## Artifacts this plan produces

- `README.md`：生命周期节改写 + 重连语义明示段 + flag 表 --once/--exit-when-empty 两行 + 协议节 EXIT 'X' 行
- 新文件 `.planning/phases/06-session-lifecycle/06-UAT.md`：六项人工验证清单
- `.planning/phases/06-session-lifecycle/06-VALIDATION.md`：Per-Task Verification Map 实测行 + nyquist_compliant/wave_0_complete 置位
- `web/uat/phase02.mjs` / `web/uat/phase03.mjs`：条件适配（仅当 EXIT 帧致帧序断言变红——预期帧序列补 EXIT 帧，适配点注释登记）

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档→部署者 | README 示例与语义说明驱动真实部署——误导性文档是运维面 |
| 敏感值→文档/测试输出 | 示例链接与 UAT 输出的 token 红线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07a | Information Disclosure | 文档示例含真实 token | medium | mitigate | 示例链接占位符（<ro-token> 形态，05-09 红线同款）；人工核读入 acceptance_criteria |
| T-06-07b | Tampering | 旧 UAT 适配削弱安全断言面 | high | mitigate | 适配白名单两类变更（帧序列补 EXIT 帧 / 注释文案同步）；acceptance_criteria 要求 git diff 核读无安全断言删除/放宽；六段式全绿为证 |
| T-06-SC | Tampering | 依赖安装供应链 | low | accept | 本 plan 零新依赖零安装 |
</threat_model>

<verification>
- 六段式：GOROOT gofmt -l 零差异 / go vet / go test -race -count=1 ./... / time pnpm -C web build / node --test web/src/lib/*.test.ts / 冒烟
- UAT：phase02/03/04/04-t1-width/05/05-dom/05-dims/06/06-dom 九脚本全过
- README/06-UAT.md/06-VALIDATION.md 三节内容断言（acceptance_criteria 逐条）
</verification>

<success_criteria>
- ROADMAP Phase 6 成功准则三条全部成立且有分层证据（Go 测试 + 协议 UAT + jsdom UAT + 文档明示 + 人工清单）
- 全量回归零红——本 phase 对 Phase 1-5 行为零回归
- phase 验收就绪（/gsd:verify-work 前置完成）
</success_criteria>

<output>
创建 `.planning/phases/06-session-lifecycle/06-07-SUMMARY.md`：记录 README 节改写明细（四条需求的文档落点）、06-UAT.md 清单、VALIDATION 同步、六段式逐条证据（命令 + 退出码摘要）、旧脚本适配明细（如有）、Phase 6 遗留事项（Deferred Ideas 对照——顶部状态条/generation id/宽限标定/1001/配置文件收口/metrics 六项均按既定 phase 路由，未提前实现）。
</output>
