---
phase: 06-session-lifecycle
plan: 04
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
autonomous: false
requirements:
  - SESS-01
  - SESS-02

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "--once flag（D-12 语法糖，one-way 公开契约）：BoolVar 全名无短选项（P2 D-15），help 文案单行标明等价关系；parseArgs 内 fs.Visit 显式设置判定（maxClientsSet/exitEmptySet，writePolicySet 159-163 先例形态）后展开——未显式给 --max-clients 则置 1、未显式给 --exit-when-empty 则置 set+grace 0；显式给定时不覆盖（用户值保持，矛盾检测归 validateStartup）"
    - "--exit-when-empty[=duration] 单 flag 可选值（D-14，one-way 公开契约）：exitEmptyValue{set bool, grace time.Duration} 实现 flag.Value + IsBoolFlag() bool（GOROOT flag.go:350-356 官方惯例——裸写 ≡ =true 不消费下一命令行参数，`--exit-when-empty 30s` 空格分隔形态结构性排除）；Set(\"true\") → grace=0（裸写=最后一个客户端断开立即退出）；Set(duration) 经 time.ParseDuration，err!=nil 或 d<0 报错；不写 = 不开启（默认现状保持：无客户端时子进程继续运行，P5 推论）"
    - "解析错误上报纪律：duration 值非敏感——Set 直接 return error（--write-policy 180-182 同款），不走 credErr/clientOptErr 记录式（该形态仅用于值含敏感内容的 flag，96-104/126-144 注释既定）；flag 包包装串 `invalid value %q for flag -exit-when-empty` 回显 duration 值可接受（注释登记该论证）"
    - "启动校验矩阵扩展（validateStartup 纯函数零副作用、先于 pty.Start/net.Listen、exit 2——290-322 既有形态；纯配置矛盾与 bind 安全形态无关，在 loopback 早退之前判定，write-policy 组合校验同位先例）：冲突判定锚定**显式设置位**（review #3 吸收——`cfg.once && cfg.maxClientsSet && cfg.maxClients != 1` → 拒绝，文案含 '--once' 与 '--max-clients' 双 flag 名；`cfg.once && cfg.exitEmptySet && cfg.exitEmpty.grace != 0` → 拒绝，文案含 '--once' 与 '--exit-when-empty' 双 flag 名；不依赖『展开只填未显式位』不变量，判定自证性更强且与展开形态逻辑等价）；--once + 显式 --max-clients=1、--once + 显式裸 --exit-when-empty 为一致冗余放行（设置位为真但值一致）"
    - "cfg → server.Options 接线：run() 内 server.New 的 Options 字面量加 ExitWhenEmpty: cfg.exitEmpty.set, ExitWhenEmptyGrace: cfg.exitEmpty.grace（06-02 字段消费点落地——--once 展开后同通道）；TestStartupMatrix 四新行（两拒绝 + 两一致冗余放行）+ 既有行零回归；TestParseArgs 扩展行（--once 展开断言 + --exit-when-empty 三形态 + 默认零值）+ 非法 duration 错误表行"
  prohibitions:
    - statement: "--once 语法糖展开不得复活 409 单客户端门——第二客户端拒绝只走既有 503 计数路径（D-12；409 已随 P5 拆除，任何形似 409 的新闸都是语义倒退）"
      status: resolved
      verification: judgment
    - statement: "CLI 默认不开启任何断开退出形态——无 flag 时 maxClients=32 且 exit-when-empty 关闭（默认值漂移 = 部署语义静默变更，D-14 默认不开启）"
      status: resolved
      verification: judgment
  artifacts:
    - path: "cmd/wesh/main.go"
      provides: "--once BoolVar + exitEmptyValue 类型（flag.Value+IsBoolFlag）+ --exit-when-empty 注册 + 语法糖展开 + validateStartup 两组合冲突行 + Options 接线"
      contains: "exit-when-empty"
    - path: "cmd/wesh/main_test.go"
      provides: "TestParseArgs 扩展行 + 非法 duration 错误行 + TestStartupMatrix 四新行"
      contains: "exit-when-empty"
  key_links:
    - from: "cmd/wesh/main.go (parseArgs)"
      to: "internal/server (Options.ExitWhenEmpty)"
      via: "fs.Visit 显式判定 → --once 展开（maxClients=1 + exitEmpty set/grace 0）→ run() Options 字面量接线 → 06-02 空触发机制消费"
      pattern: "ExitWhenEmpty"
    - from: "cmd/wesh/main.go (validateStartup)"
      to: "cmd/wesh/main.go (config)"
      via: "once × maxClients≠1 / once × grace≠0 组合矛盾 fail-fast（exit 2，双 flag 名进文案）"
      pattern: "--once"
---

<objective>
SESS-01/02 CLI 半侧：--once 语法糖（≡ --max-clients=1 --exit-when-empty=0）+ --exit-when-empty[=duration] 可选值 flag（IsBoolFlag 惯例）+ 组合冲突校验矩阵 + Options 接线——把 06-02 的服务端能力投影为 ttyd 肌肉记忆的部署语义。

Purpose: 两个 one-way CLI 公开契约的实现（flag 名/语义发布后不可改）；全部机制（解析三形态、展开、冲突检测）均为纯函数可测，无运行时风险——难点在显式设置判定与展开/校验的分层纪律（parse = 形状与展开、validate = 组合矛盾）。
Output: main.go 两 flag + 展开 + 矩阵 + 接线 + main_test.go 扩展行，go test ./cmd/wesh 全绿。含 D-12/D-14 one-way 确认门。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-session-lifecycle/06-CONTEXT.md
@.planning/phases/06-session-lifecycle/06-RESEARCH.md
@.planning/phases/06-session-lifecycle/06-PATTERNS.md
@.planning/phases/06-session-lifecycle/06-VALIDATION.md
@.planning/phases/06-session-lifecycle/06-02-SUMMARY.md
@cmd/wesh/main.go
@cmd/wesh/main_test.go
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 确认门——--once / --exit-when-empty CLI 公开契约（D-12/D-14，one-way）</name>
  <read_first>
    - .planning/phases/06-session-lifecycle/06-CONTEXT.md（decisions 节 D-12/D-14 逐字 + Reversibility 注记）
    - .planning/phases/06-session-lifecycle/06-RESEARCH.md（Pattern 5 IsBoolFlag 惯例逐字形态 + State of the Art ttyd 对照行）
  </read_first>
  <decision>--once（BoolVar，≡ --max-clients=1 --exit-when-empty=0 语法糖，README 标明等价关系）+ --exit-when-empty[=duration]（自定义 flag.Value + IsBoolFlag，裸写=立即退出、=duration=重连宽限、不写=不开启）——CLI flag 公开契约（one-way door：发布后改名/改语义破坏部署脚本与文档，P2 D-15 纪律）</decision>
  <context>
    D-12/D-14 在 discuss-phase 已锁定（语法糖等价关系与三形态语义逐字）；IsBoolFlag 惯例经 GOROOT flag.go:350-356 源码核实（空格分隔传值被结构性排除，恰好符合裸写/=duration 两形态设计）；ttyd 肌肉记忆对齐（-o/--once、-q/--exit-no-conn——flag 名全新设计，不做 ttyd CLI 兼容，REQUIREMENTS Out of Scope）。本门是实现前最后一次确认，不是重新决策。
  </context>
  <options>
    <option id="as-locked">
      <name>按锁定值落地（推荐）</name>
      <pros>--once BoolVar + --exit-when-empty[=duration] IsBoolFlag + 展开与冲突矩阵，与 CONTEXT.md D-12/D-14 逐字一致</pros>
      <cons>空格分隔形态（--exit-when-empty 30s）不传值（IsBoolFlag 惯例副产品——README 用法行明示 = 号形态）</cons>
    </option>
    <option id="revise">
      <name>调整 flag 名/形态</name>
      <pros>若有部署侧新信息可低成本修正</pros>
      <cons>推翻 CONTEXT.md 锁定项，需重新 discuss 并回填 06-RESEARCH</cons>
    </option>
  </options>
  <resume-signal>选择 as-locked（继续实现）或 revise（说明调整点）</resume-signal>
  <acceptance_criteria>
    - 本门必须停止等待用户：用户明确选择 as-locked 后才进入 Task 2；revise 时停止执行并回填 CONTEXT.md（先例不得作为自动通过依据）
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 两 flag + 语法糖展开 + 冲突矩阵 + Options 接线 + TestParseArgs/TestStartupMatrix 扩展</name>
  <reversibility rating="one-way">--once/--exit-when-empty 是公开 CLI 契约（D-12/D-14）——发布后改名/改默认/改语义破坏部署脚本与文档；确认门见 Task 1。</reversibility>
  <files>cmd/wesh/main.go, cmd/wesh/main_test.go</files>
  <read_first>
    - cmd/wesh/main.go（全文 444 行——改造面逐处：config 结构 28-49（Phase 分组注释形态）、parseArgs flag 定义区 71-147、fs.Visit 159-163 显式判定先例、write-policy parse 期枚举校验 177-182、validateStartup 290-322（组合校验 296-298 双 flag 名先例 + loopback 早退 306）、run() 的 server.New Options 字面量 383）
    - cmd/wesh/main_test.go（TestParseArgs 表结构 30-125——零值语义断言先例（wantWritePolicy 零值=期望默认 owner）；TestStartupMatrix 356-431 结构与 wantErrSub2 双 flag 名断言位；既有行 maxClients:32 基值注入先例）
    - internal/server/server.go（Options.ExitWhenEmpty/ExitWhenEmptyGrace 两字段——06-02 产出，接线目标）
    - .planning/phases/06-session-lifecycle/06-RESEARCH.md（Pattern 5 exitEmptyValue 逐字骨架 + 错误上报红线段落）
    - .planning/phases/06-session-lifecycle/06-PATTERNS.md（§6/§7 先例映射）
  </read_first>
  <behavior>
    - TestParseArgs 新行：args [--once -- bash] → once==true 且 maxClients==1 且 exitEmpty.set==true 且 grace==0；[--exit-when-empty -- bash] → set==true 且 grace==0 且 maxClients==32；[--exit-when-empty=30s -- bash] → grace==30s；defaults 行断言 exitEmpty.set==false 且 once==false（既有行零值断言扩展）
    - 非法 duration 错误行：[--exit-when-empty=abc -- bash] 与 [--exit-when-empty=-5s -- bash] → parseArgs 返回 err 且文案含 "exit-when-empty"
    - TestStartupMatrix 新行：{once:true, maxClients:2, maxClientsSet:true} → 拒绝且文案含 "--once" 与 "--max-clients"；{once:true, exitEmpty:{set:true,grace:5s}} → 拒绝且文案含 "--once" 与 "--exit-when-empty"；{once:true, maxClients:1, maxClientsSet:true} → 放行；{once:true, exitEmpty:{set:true,grace:0}} → 放行（一致冗余）
  </behavior>
  <action>
    1. `cmd/wesh/main.go`：
       - config 结构加字段（Phase 6 分组成段注释，one-way 公开契约 P2 D-15 同纪律标注）：`once bool`（--once）、`exitEmpty exitEmptyValue`（--exit-when-empty）、`maxClientsSet bool` 与 `exitEmptySet bool`（fs.Visit 显式设置判定——展开与冲突校验消费）。
       - exitEmptyValue 类型（main.go 内未导出）：`set bool; grace time.Duration` 两字段；String() 返回当前形态串（flag.Value 契约）；`IsBoolFlag() bool { return true }`（注释逐字引 GOROOT flag.go:350-356 惯例——裸写等价 =true 不消费下一参数）；Set(s string)：s=="true" → set=true, grace=0；否则 time.ParseDuration(s)，err!=nil 或 d<0 → return fmt.Errorf（文案含 "exit-when-empty" 与类别，值非敏感可回显——注释登记该论证：flag 包会包装为 invalid value %q 回显 duration 值，可接受，非 SEC-01 面）；成功 → set=true, grace=d。
       - parseArgs flag 定义区：`fs.BoolVar(&cfg.once, "once", false, "accept only one client and exit when it disconnects (equivalent to --max-clients=1 --exit-when-empty=0)")`（help 文案单行标明等价关系，D-12）；`fs.Var(&cfg.exitEmpty, "exit-when-empty", "exit after all clients disconnect (optional grace: --exit-when-empty=30s; bare = exit immediately)")`。
       - fs.Visit 扩展（159-163 同款闭包内）：f.Name=="max-clients" → cfg.maxClientsSet=true；f.Name=="exit-when-empty" → cfg.exitEmptySet=true（write-policy 判定行保持）。
       - 语法糖展开（插入点：fs.Visit 之后、showVersion 早退之后的校验区之前——展开先于组合校验）：`if cfg.once { if !cfg.maxClientsSet { cfg.maxClients = 1 }; if !cfg.exitEmptySet { cfg.exitEmpty.set = true; cfg.exitEmpty.grace = 0 } }`——注释：D-12 展开不覆盖显式给定值（矛盾检测归 validateStartup，分层纪律：parse=形状与展开、validate=组合矛盾）。
       - validateStartup 扩展（loopback 早退之前的组合校验区，write-policy 行同位）：`if cfg.once && cfg.maxClientsSet && cfg.maxClients != 1 { return "", errors.New("--once conflicts with --max-clients: --once implies --max-clients=1") }`（注释：判定锚定显式设置位而非展开后终值——review #3 吸收；展开只填未显式位时两形态逻辑等价，但显式设置位判定不依赖该不变量，自证性更强）；`if cfg.once && cfg.exitEmptySet && cfg.exitEmpty.grace != 0 { return "", errors.New("--once conflicts with --exit-when-empty grace: --once implies immediate exit (--exit-when-empty=0)") }`（同款显式设置位形态）。
       - run() 内 server.New Options 字面量加两键：ExitWhenEmpty: cfg.exitEmpty.set, ExitWhenEmptyGrace: cfg.exitEmpty.grace（06-02 消费点接线；注释：--once 展开后同通道——服务端无 --once 概念，SESS-01 = maxClients=1 + ExitWhenEmpty grace 0 的组合语义）。
    2. `cmd/wesh/main_test.go`：
       - TestParseArgs 表结构加命名字段（03-04 命名字段转换先例——既有行值/断言零改动）：wantOnce bool、wantExitEmptySet bool、wantExitEmptyGrace time.Duration、wantMaxClients int（零值 = 期望默认 32——wantWritePolicy 零值语义同款注释）；逐字段断言段同步扩展；按 <behavior> 加四行（--once 展开行 / 裸写行 / =30s 行；defaults 行补零值断言）。
       - 非法 duration 错误表：照 TestTLSKeyPairError/TestClientOptionError 错误表形态（错误或并入既有 parse 错误表测——按文件既有归位选形，断言 err 文案含 "exit-when-empty"）加 =abc 与 =-5s 两行。
       - TestStartupMatrix 按 <behavior> 加四行（config 结构新字段直接构造；bind 127.0.0.1 隔离其他校验维度——max-clients 拒绝行同款）；既有行零改动零回归。
    3. 验证即提交：go build/vet + go test -race ./cmd/wesh/ 全绿。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -race -count=1 ./cmd/wesh/</automated>
  </verify>
  <acceptance_criteria>
    - go build/vet 退出 0；go test -race -count=1 ./cmd/wesh/ 全绿（新行 + 既有行零回归）
    - grep -c '"once"' cmd/wesh/main.go == 1（BoolVar 注册）且 grep -c 'exit-when-empty' cmd/wesh/main.go >= 3（fs.Var 注册 + help 文案 + 校验文案）
    - grep -c 'IsBoolFlag' cmd/wesh/main.go == 1（可选值 flag 惯例）
    - grep -c 'maxClientsSet\|exitEmptySet' cmd/wesh/main.go >= 4（两字段声明 + fs.Visit 两判定 + 展开两消费）
    - grep -c 'cfg.maxClientsSet && cfg.maxClients != 1' cmd/wesh/main.go == 1 且 grep -c 'cfg.exitEmptySet && cfg.exitEmpty.grace != 0' cmd/wesh/main.go == 1（review #3——冲突判定显式设置位形态钉死）
    - grep -c 'ExitWhenEmpty: cfg.exitEmpty.set' cmd/wesh/main.go == 1（Options 接线）
    - grep -c 'wantExitEmptyGrace\|wantOnce' cmd/wesh/main_test.go >= 2（表结构扩展）
    - TestStartupMatrix 含双 flag 名断言行（wantErrSub2 消费）：--once × --max-clients 与 --once × --exit-when-empty 两拒绝行 + 两一致冗余放行行
    - go test ./cmd/wesh/ -run 'TestParseArgs' 输出含 --once 展开行与 --exit-when-empty 三形态行全过
  </acceptance_criteria>
  <done>--once/--exit-when-empty CLI 契约落地：三形态解析、语法糖展开、组合冲突 fail-fast、Options 接线——SESS-01/02 的部署语义入口完整；端到端协议证据由 06-06 phase06.mjs S3-S5 锁定，README 等价关系明示由 06-07 落地。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

SESS-01/SESS-02 的 probe 状态（unclassified — review manually）已在 06-02 显式登记（flagged-unverified，no-silent-drop）；本 plan 承接其 CLI 投影面：边角（空格分隔形态排除/负值 duration/显式-语法糖组合矩阵）已由 06-RESEARCH Pattern 5（IsBoolFlag 惯例 GOROOT 逐字核实）与 TestParseArgs/TestStartupMatrix 扩展行实质覆盖并实证。

## Review Dispositions (06-REVIEWS, OpenCode)

| 发现 | 处置 |
|------|------|
| HIGH #3：冲突检测应判「显式设置」（maxClientsSet && maxClients != 1）而非展开后终值 | 已吸收——must_haves truth、validateStartup 两行、acceptance 双 grep 全部改为显式设置位形态（注释登记与展开形态的逻辑等价性论证） |
| MEDIUM：冲突错误文案应匹配 main.go 既有 errors.New 风格 | 已覆盖——plan 既定形态即 errors.New 双 flag 名文案（write-policy 180-182 同款先例），本轮零改动 |
| MEDIUM：Set() 的 d<0 检查冗余（ParseDuration 已处理负值） | **驳回**——`time.ParseDuration("-5s")` 解析**成功**返回 -5s（负 duration 合法语法），d<0 检查是负值拒绝的唯一闸；TestParseArgs 错误表 `=-5s` 行行为锁定该语义 |
| Suggestion：加「--exit-when-empty 启动期不触发」集成测试 | 已吸收——路由至 06-06 phase06.mjs S4（attach 前 400ms 守候断言进程存活，Pitfall 2 的协议层显式证据） |
| LOW：flag 名易被误读为「启动时为空即退」，README 应澄清迁移语义 | 已吸收——路由至 06-07 Task 1（README --exit-when-empty 行加迁移语义明示） |
| Suggestion：README 标明 --once 与显式 --max-clients=1 等价（非冲突） | 已覆盖——06-07 README --once 行等价关系逐字标明（D-12），本 plan TestStartupMatrix 一致冗余放行两行为其行为锁 |

## Artifacts this plan produces

- `cmd/wesh/main.go`：config 新字段 `once bool`/`exitEmpty exitEmptyValue`/`maxClientsSet bool`/`exitEmptySet bool`；`exitEmptyValue` 类型（`String()`/`Set(string) error`/`IsBoolFlag() bool`）；`--once` BoolVar 与 `--exit-when-empty` Var 注册；fs.Visit 两新判定；--once 语法糖展开段；validateStartup 两组合冲突校验行；server.New Options 接线 `ExitWhenEmpty`/`ExitWhenEmptyGrace` 两键
- `cmd/wesh/main_test.go`：TestParseArgs 新字段 `wantOnce`/`wantExitEmptySet`/`wantExitEmptyGrace`/`wantMaxClients` 与四新行；非法 duration 错误行 ×2；TestStartupMatrix 四新行

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 命令行→进程配置 | flag 值驱动进程生命周期语义——畸形/矛盾输入必须 fail-fast（exit 2）而非带病启动 |
| flag 值→stderr 错误串 | 解析错误经 flag 包包装打印——值内容回显面需显式论证 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04a | Information Disclosure | flag 值进错误串（invalid value %q 包装） | low | accept | duration 值非敏感（06-RESEARCH Pattern 5 红线论证：记录式 credErr/clientOptErr 仅用于值含敏感内容的 flag）；校验文案含 flag 名与类别——凭据/ticket 面与本 flag 无关 |
| T-06-04b | Denial of Service | 配置矛盾带病启动（--once × 显式矛盾值） | medium | mitigate | validateStartup 组合校验 fail-fast（exit 2，先于 pty.Start/net.Listen 零资源占用）；双 flag 名进文案；TestStartupMatrix 四行锁定 |
| T-06-04c | Tampering | 语法糖展开覆盖用户显式值 | medium | mitigate | fs.Visit 显式设置判定先行——展开只填未显式位，显式矛盾值留给 validateStartup 拒绝（不静默改写用户输入）；TestParseArgs 展开行锁定 |
| T-06-SC | Tampering | 依赖安装供应链 | low | accept | 本 plan 零新依赖零安装（06-RESEARCH Package Legitimacy Audit：无输入） |
</threat_model>

<verification>
- go build ./... && go vet ./... 退出 0
- go test -race -count=1 ./cmd/wesh/ 全绿（TestParseArgs 四新行 + 错误两行 + TestStartupMatrix 四新行）
- 源码断言：BoolVar/Var 注册、IsBoolFlag、显式判定与展开、Options 接线
</verification>

<success_criteria>
- `wesh --once -- bash` 只接受一个客户端（第二客户端 503）且其断开后服务端退出——SESS-01 CLI 面成立（协议证据 06-06 S3）
- `wesh --exit-when-empty[=duration]` 三形态解析正确、默认不开启——SESS-02 CLI 面成立（协议证据 06-06 S4/S5）
- 组合矛盾 fail-fast exit 2 且文案含双 flag 名
</success_criteria>

<output>
创建 `.planning/phases/06-session-lifecycle/06-04-SUMMARY.md`：记录确认门结果、exitEmptyValue 形态与 IsBoolFlag 惯例落码、展开/校验分层纪律、矩阵新行、与 06-02 服务端机制的接线点。
</output>
