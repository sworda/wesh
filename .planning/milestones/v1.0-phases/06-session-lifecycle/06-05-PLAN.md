---
phase: 06-session-lifecycle
plan: 05
type: execute
wave: 3
depends_on: ["06-01", "06-03"]
files_modified:
  - web/uat/phase06-dom.mjs
autonomous: true
requirements:
  - CORE-05

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D1 重连全链：jsdom 加载真实 dist/index.html 连真实 wesh 实例（--writable bash）→ 首连 WELCOME 建立会话（终端经 echo 写入可观测文本）→ SpyWebSocket 合成 CloseEvent{code:1006} → Reconnecting 面板出现（title=='Reconnecting'、body 含 attempt 1、hint 含 'Reconnect now' 与 'restart it from your shell'——C-9 逐字要点）→ 退避后自动重连（SpyWebSocket 构造计数 +1）→ 新 WELCOME 到达后面板隐藏 + term.clear() 可观测（断开前写入的终端文本在重连后从终端 DOM 消失）+ beforeunload 重注册（addEventListener/removeEventListener 包装记账断言移除后重注册）"
    - "D2/D3 不触发边界：合成 CloseEvent{code:1002} → C-5 'Connection lost' 面板 + 2.5s 窗口内 SpyWebSocket 构造计数不变（零新连接——default 桶残留语义，1002 协议错误不卷入重试循环）；合成 CloseEvent{code:1013} → 'Disconnected' 面板 + 构造计数不变（1013 维持手动刷新，P5 D-10）；合成 1008 → 'Connection refused' 面板 + 计数不变（抽样一档代表 1000/1008/1009/1011 专版集合）"
    - "D4 双触发幂等（RESEARCH Pitfall 5）：同一会话内 window.dispatchEvent(offline) 与合成 onclose(1006) 相继到达 → 单循环（attempt 计数不跳变、 SpyWebSocket 构造计数按退避节奏单调 +1 不并发翻倍——断网瞬间双触发不得启双循环）"
    - "D5 手动入口：Reconnecting 面板等待期点击 hint 内 'Reconnect now' 链接（#status-hint a）→ 倒计时未完即发起新连接（waitFor 构造计数 +1，800ms 容差窗 ≪ 标称退避 1s——轮询+容差形态，review #6 吸收）且循环不终止（失败仍按退避继续——ROADMAP『手动入口』落此）"
    - "D6 代际守卫（RESEARCH Pitfall 6）：重连成功新会话建立后，对旧 SpyWebSocket 实例迟到派发合成 onclose(1006) → 新会话状态零污染（面板保持隐藏、无第三连接构造、beforeunload 监听不被拆除）"
    - "D7 EXIT 帧全链（真实服务端行为）：实例 spawn `sh -c 'sleep 2; exit 7'` → 页面连接 → 子进程到期退出 → 真实 EXIT 帧 + 1000 到达 → 'Session ended' 面板正文逐字 == 'The process exited with code 7.'（06-01 服务端组文案前端直显的端到端证据）"
    - "D8 online 快路径：1006 进入重连等待后 dispatchEvent(online) → 当前等待被清、立即 attempt（waitFor 构造计数 +1，800ms 容差窗 ≪ 标称退避 1s——轮询+容差形态替代精确时点断言，review #6 吸收——D-04『online 立即试一次』）"
    - { statement: "真实 OS 断网栈与浏览器原生 online/offline 事件时序按 headless 硬约束豁免（CODEBUDDY.md 平台原生行为豁免条款）——本脚本以合成 CloseEvent/Event dispatch 驱动同一状态机覆盖等价逻辑面、06-06 phase06.mjs 以真实 TCP 断连覆盖协议面；真实栈场景以 skipped+reason 记录并指向 06-UAT.md 人工清单（06-UI-SPEC §UI Considerations backstop 行）", verification: backstop }
  prohibitions:
    - statement: "jsdom 断言不得削弱为『面板出现即过』——D1 必须断言到重连成功后的可观测状态（面板隐藏 + 清屏 + 新连接建立 + beforeunload 重注册），半绿断言是假绿"
      status: resolved
      verification: judgment
    - statement: "UAT 输出红线：token/凭据值永不进 check detail/控制台输出/汇总行（phase04.mjs:6-9 纪律沿用）——detail 只打状态码/布尔/形状/文案常量"
      status: resolved
      verification: judgment
  artifacts:
    - path: "web/uat/phase06-dom.mjs"
      provides: "CORE-05 重连状态机 jsdom 行为断言八场景（D1-D8）+ SpyWebSocket 合成 close 能力 + beforeunload 记账"
      contains: "Reconnecting"
  key_links:
    - from: "web/uat/phase06-dom.mjs (SpyWebSocket 合成 CloseEvent)"
      to: "web/src/main.ts (重连状态机)"
      via: "synthClose(1006) → onclose case 1006 → startReconnect → 面板/构造计数/清屏断言点"
      pattern: "synthClose|CloseEvent"
    - from: "web/uat/phase06-dom.mjs (window offline/online dispatch)"
      to: "web/src/main.ts (online/offline 监听)"
      via: "dispatchEvent(new window.Event('offline'/'online')) → 单例入口断言"
      pattern: "dispatchEvent"
    - from: "web/uat/phase06-dom.mjs (真实服务端 exit 7)"
      to: "web/src/main.ts (case EXIT → onclose 1000)"
      via: "EXIT 帧 → lastExit → Session ended 正文断言（06-01 链路端到端）"
      pattern: "Session ended"
---

<objective>
CORE-05 jsdom 行为证据：phase06-dom.mjs——SpyWebSocket 注入（合成 CloseEvent 能力）+ 真实 dist + 真实 wesh 实例，八场景锁定重连状态机（1006 触发/不触发边界/双触发幂等/手动入口/代际守卫/EXIT 全链/online 快路径）。

Purpose: headless 硬约束下重连前端逻辑面的最终自动化证据（CODEBUDDY.md 分层测试策略层 3）；06-03 的状态机改造（面板保护/代际守卫/单例循环）全部风险点在本 plan 转为可执行断言。
Output: web/uat/phase06-dom.mjs（新）八场景全绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-session-lifecycle/06-CONTEXT.md
@.planning/phases/06-session-lifecycle/06-RESEARCH.md
@.planning/phases/06-session-lifecycle/06-UI-SPEC.md
@.planning/phases/06-session-lifecycle/06-VALIDATION.md
@.planning/phases/06-session-lifecycle/06-03-SUMMARY.md
@web/uat/phase05-dom.mjs
@web/src/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase06-dom.mjs 骨架（loadTerminal + SpyWebSocket 合成 close + 事件记账）+ D1-D4 场景</name>
  <files>web/uat/phase06-dom.mjs</files>
  <read_first>
    - web/uat/phase05-dom.mjs（全文——loadTerminal 注入形态 124-151（JSDOM runScripts:'outside-only' + WebSocket/fetch/TextEncoder/TextDecoder 注入）、waitFor 轮询断言 48-55、check/skip 形态、红线注释）
    - web/uat/phase05.mjs（startWesh 68-93 spawn + stdout 三行解析 + stderr 捕获形态——本脚本同款复用）
    - web/src/main.ts（06-03 产出——重连状态机被测对象：面板 DOM id（status/status-title/status-body/status-hint）、C-9 文案、代际守卫入口）
    - .planning/phases/06-session-lifecycle/06-RESEARCH.md（Validation Architecture CORE-05 行 + Assumptions A2——SpyWebSocket 合成 CloseEvent 可行性论证与真实断连兜底）
    - .planning/phases/06-session-lifecycle/06-PATTERNS.md（§13 延伸点逐字）
  </read_first>
  <action>
    1. 新建 `web/uat/phase06-dom.mjs`（Node ≥22 原生 WebSocket/fetch + jsdom 25.0.1——web/uat 既有依赖零新增；phase05-dom.mjs loadTerminal 形态逐字复用）：
       - startWesh 复用 phase05.mjs 形态（--bind 127.0.0.1 --port 0 + stdout 解析 + stderr 捕获 + child 句柄 + SIGKILL kill）；本脚本场景不需要分享链接行（plain '/' 进入，无认证模式）。
       - loadTerminal 扩展两件：① SpyWebSocket 加合成关闭能力——实例方法 synthClose(code)：先取存 this.onclose 处理器并置 null（抑制随后真实 close 的 1000 事件混入断言面），再 try{ this.close() }catch{}，最后以该处理器调用合成事件（new window.CloseEvent('close', { code })——jsdom 25 提供 CloseEvent 构造器；若构造受阻回退 Object.assign(new window.Event('close'), { code })，A2 注释登记）；② 构造计数——类级计数器（模块级 `let constructed = 0`，构造器内递增），D2/D3/D4/D5/D8 的『零新连接/立即 attempt』断言材料。
       - beforeunload 记账：loadTerminal 内包装 window.addEventListener/removeEventListener——对 'beforeunload' 类型分别递增 onCount/offCount（D1/D6 断言材料；包装转发原实现，行为零漂移）。
       - window.fetch 按 phase05-dom 无认证形态（(u, o) => fetch(new URL(u, origin), o)）；'/api/attach' 404 探测直连链路既有（服务端无认证模式 404 → 前端跳过 ticket 直连 WS）。
    2. 场景骨架：场景数组顺序执行 + try/catch 计数 + 300ms 间隔 + 汇总行 + process.exit(failed===0?0:1)（phase05.mjs:640-655 逐字形态）；每场景独立 spawn 实例 + 独立 jsdom（重连状态是页面级——隔离防串扰，注释登记）。
    3. D1 重连全链（must_haves 逐字断言面）：spawn --writable -- bash --norc --noprofile → loadTerminal → waitFor 会话建立（WELCOME 后 welcomeDone——以终端 DOM 出现 echo 文本为可观测代理：页面向 ws 发 INPUT 经 SpyWebSocket.send 上行 'echo D1MARKER\r'……不可行——dist 内 main.ts 不暴露 ws；改用终端回显通道：jsdom 侧无法直接驱动键盘，以服务端输出驱动——spawn 命令改为 bash 并在建立后由协议侧辅助连接写 INPUT？保持简单：会话建立断言 = 面板隐藏且构造计数==1；清屏对照文本以 spawn `sh -c 'printf D1BANNER; exec bash --norc --noprofile'` 产出——D1BANNER 写入终端 DOM 后可断言存在，重连成功后断言该文本从终端 DOM 消失（term.clear() 可观测证据））→ synthClose(1006) → waitFor 面板 title=='Reconnecting' 且 body 含 'attempt 1' 且 hint 含 'Reconnect now' 与 'restart it from your shell' → waitFor 构造计数==2（标称退避 1s，waitFor 上限 3s 容差窗——轮询+容差形态替代精确时点断言，review #6 吸收）→ waitFor 面板隐藏（新 WELCOME 到达——重连成功）→ 断言 D1BANNER 文本已从终端 DOM 消失（清屏证据）→ beforeunload 记账断言（offCount 增加于断开、onCount 增加于重连 WELCOME）。
    4. D2 1002 不触发 / D3 1013 不触发 + 1008 抽样：各自独立场景——synthClose(1002) → waitFor C-5 面板（title=='Connection lost'）→ 2.5s 守候窗（> attempt 1 退避标称 1s——1.5s 容差吸收调度抖动，窗内零构造 = 不触发的行为证据，review #6 容差论证注释位）断言构造计数不变；synthClose(1013) → title=='Disconnected' + 计数不变；synthClose(1008) → title=='Connection refused' + 计数不变（注释：1000/1009/1011 同集合逐字面板由 main.ts 既有分支承载，抽样一档代表）。
    5. D4 双触发幂等：会话建立后 window.dispatchEvent(new window.Event('offline')) 紧随 synthClose(1006)（两触发源相继到达）→ waitFor 构造计数==2 → 3s 窗内断言计数不并发翻倍（attempt 不跳变的可观测代理 = 构造计数按退避节奏单调 +1；注释：Pitfall 5 防线——断网瞬间 offline+onclose 各启一循环是回归形态）。
  </action>
  <verify>
    <automated>go build -o /tmp/wesh-uat/wesh ./cmd/wesh && node web/uat/phase06-dom.mjs</automated>
  </verify>
  <acceptance_criteria>
    - go build 退出 0；node web/uat/phase06-dom.mjs 退出 0 且 D1-D4 全 pass（汇总行零 FAIL）
    - grep -c 'synthClose' web/uat/phase06-dom.mjs >= 2（方法定义 + 场景调用）
    - grep -c "new window.CloseEvent('close'" web/uat/phase06-dom.mjs >= 1（合成关闭事件构造；回退形态存在亦可——源码核读确认 A2 兜底注释）
    - grep -c "'beforeunload'" web/uat/phase06-dom.mjs >= 2（add/remove 记账包装）
    - D1 断言面含：Reconnecting 面板文案要点 + 构造计数 +1 + 面板隐藏 + 终端文本清屏消失 + beforeunload 移除/重注册记账
    - D2/D3 断言面含：对应专版面板 title + 2.5s 守候窗构造计数不变
  </acceptance_criteria>
  <done>重连状态机核心行为锁定：1006 全链（面板→退避→自动重连→清屏→重注册）、1002/1013/1008 不触发边界、双触发幂等。D5-D8 由 Task 2 补齐。</done>
</task>

<task type="auto">
  <name>Task 2: D5-D8 场景（Reconnect now 手动入口 / 代际守卫 / EXIT 全链 / online 快路径）+ 汇总收口</name>
  <files>web/uat/phase06-dom.mjs</files>
  <read_first>
    - web/uat/phase06-dom.mjs（Task 1 产出——骨架与 D1-D4 形态，同文件追加）
    - web/src/main.ts（06-03 产出——Reconnect now 链接 onClick、代际守卫入口、online 监听、case EXIT 暂存）
    - .planning/phases/06-session-lifecycle/06-UI-SPEC.md（§Reconnect Dispatch Contract 终态分派表 + C-9 逐字）
  </read_first>
  <action>
    1. D5 Reconnect now 手动入口：会话建立 → synthClose(1006) → waitFor Reconnecting 面板出现 → 立即点击 #status-hint 内 a 链接（dom window document querySelector + click()）→ waitFor(构造计数 +1, 800ms)——标称退避 1s，800ms 断言窗留 ≥200ms 调度容差，窗内到达即证明等待被跳过（轮询+容差形态，review #6 吸收；超时未到 = 未跳过 = FAIL）→ 新 WELCOME 后面板隐藏（循环以成功终止）。
    2. D6 代际守卫：会话建立（保留首连接的 SpyWebSocket 实例引用）→ synthClose(1006) → waitFor 重连成功（构造计数==2 且面板隐藏）→ 对旧实例再调用一次合成 onclose(1006)（旧实例的 onclose 处理器已在 synthClose 内置 null——为驱动本场景，synthClose 保留一份处理器副本供二次调用；或以独立 helper staleClose(inst, code) 直接调用捕获副本——review D6 简化建议与本形态同款）→ 断言：面板保持隐藏、构造计数==2（无第三连接）、beforeunload onCount 未因 stale 事件被拆除（offCount 不增）——注释：Pitfall 6 防线，stale 代际事件不得触碰新会话状态。
    3. D7 EXIT 全链：spawn `sh -c 'sleep 2; exit 7'` → loadTerminal 连接 → waitFor 会话建立（2s 窗口内）→ 子进程到期退出 → waitFor 'Session ended' 面板 → 断言 #status-body textContent 逐字 == 'The process exited with code 7.'（06-01 服务端组文案 → 前端 lastExit 暂存 → 1000 正文的全链证据）；wesh 进程 exit 事件码==7（进程级退出码传递顺带锁定）。
    4. D8 online 快路径：会话建立 → synthClose(1006) → waitFor Reconnecting 面板出现（等待期，未到 1s 退避点）→ window.dispatchEvent(new window.Event('online')) → waitFor(构造计数 +1, 800ms)（online 清等待立即 attempt——D-04；容差窗形态同 D5，review #6）；随后 WELCOME 面板隐藏。
    5. 真实断网栈豁免场景：skip(id, '浏览器原生断网/恢复事件序列', reason 指向 headless 硬约束与 `.planning/phases/06-session-lifecycle/06-UAT.md`（06-07 产出）人工清单)——backstop 行的 UAT 落点。
    6. 输出自净断言（review #7 吸收——红线由注释纪律升级为运行时自证）：check/skip 包装收集全部已发 detail 入数组；startWesh 解析 stdout 时把两条分享链接 token 留入闭包（只作本断言材料）；汇总行前执行 assertOutputClean()——遍历 detail 数组断言不含任一 token 值（含 '/s/' 链接形态串），命中即 FAIL（防未来回归静默破线，phase04.mjs:6-9 红线的可执行形态）。
    7. 汇总行与退出码（phase05.mjs 逐字形态）；红线注释（token 值永不进 detail——phase04.mjs:6-9 纪律沿用注释位，本轮由第 6 步运行时断言兜底）；全脚本八场景 + 1 skipped 全绿后提交。
  </action>
  <verify>
    <automated>node web/uat/phase06-dom.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node web/uat/phase06-dom.mjs 退出 0：D1-D8 全 pass + 豁免场景以 skipped+reason 记录（不计失败）
    - D5 断言面含：点击后 waitFor(构造计数 +1, 800ms) 容差窗内到达（退避到期前发起，review #6 形态）
    - D6 断言面含：stale onclose 后面板隐藏保持 + 构造计数不变 + beforeunload 记账不增
    - D7 断言面含：Session ended 正文逐字 'The process exited with code 7.' + 进程 exit 码 7
    - D8 断言面含：online dispatch 后 waitFor(构造计数 +1, 800ms) 容差窗内到达（review #6 形态）
    - grep -c 'skip(' web/uat/phase06-dom.mjs >= 1（豁免记录形态）
    - grep -c 'assertOutputClean' web/uat/phase06-dom.mjs >= 2（review #7——输出自净断言定义 + 汇总前调用）
  </acceptance_criteria>
  <done>CORE-05 jsdom 行为证据八场景全绿：重连全链/不触发边界/幂等/手动入口/代际守卫/EXIT 全链/online 快路径 + 真实断网栈豁免记录——06-03 的全部风险点转为可执行断言。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

CORE-05 的 probe 状态（unclassified — review manually）已在 06-03 显式登记（flagged-unverified）；本 plan 承接其行为实证面。Assumptions A2（SpyWebSocket 合成 CloseEvent 可行性）以 synthClose 实现 + D1 落地兑现；若 jsdom CloseEvent 构造受阻，以 A2 登记的回退形态（Event + code 赋值）或真实断连替代并记录。

## Review Dispositions (06-REVIEWS, OpenCode)

| 发现 | 处置 |
|------|------|
| HIGH #6：UAT 时序敏感（1s backoff 窗/400ms 宽限），建议轮询+容差 | 已吸收——D5/D8「退避到期前 +1」改 waitFor 800ms 容差窗形态；D1 waitFor 上限 3s 明示；D2/D3 守候窗补容差论证注释；精确时点断言零残留 |
| MEDIUM #7：token 红线检查缺失（需显式断言 token 不进输出） | 已吸收——Task 2 第 6 步 assertOutputClean() 运行时自净断言（detail 数组遍历不含 token/'/s/' 链接形态串），红线由注释纪律升级为可执行断言 |
| HIGH：D1 复杂度（bash 是否输出 printf 结果到终端 DOM / clear 后 DOM 是否真空） | 已覆盖——D1 既定机制与 reviewer 简化建议同款（spawn `printf D1BANNER; exec bash` 服务端标记串驱动终端 DOM）；waitFor 先断言 BANNER 存在再驱动断连，清屏消失断言因前置存在性而有意义 |
| MEDIUM：D6 stale handler 驱动简化（存处理器副本直接调用） | 已覆盖——plan 既定形态（synthClose 保留处理器副本 / staleClose helper）与 reviewer 建议同款 |
| Suggestion：考虑 sinon 等库做精确定时器控制 | **驳回**——零新依赖纪律（06-RESEARCH §Standard Stack 锁定全项目零新依赖，Package Legitimacy Audit 无输入）；轮询+容差窗是本项目 UAT 既定形态（phase05-dom waitFor 先例） |
| LOW：D8 时序敏感需容差规格 | 已吸收——与 HIGH #6 同批（800ms 容差窗 ≪ 标称退避 1s） |

## Artifacts this plan produces

- 新文件 `web/uat/phase06-dom.mjs`：`startWesh`（复用形态，stdout 解析留 token 闭包）、`loadTerminal`（扩展 SpyWebSocket `synthClose(code)` + 类级构造计数 + window addEventListener/removeEventListener 'beforeunload' 记账包装）、场景 D1-D8 + 豁免 skip 场景、`assertOutputClean()` 输出自净断言（review #7）、汇总行

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试驱动→真实服务端 | jsdom 页面连真实 spawn 实例——测试流量不应触发服务端异常面 |
| 敏感值→测试输出 | UAT 凭据/token 只作断言材料——测试输出可能进 CI 日志 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05a | Information Disclosure | token/凭据值进测试输出 | high | mitigate | 红线注释沿用（phase04.mjs:6-9 纪律）：detail 只打状态码/布尔/形状/文案常量；本脚本默认无认证模式场景为主，Basic 凭据不经本脚本（phase06.mjs 侧同纪律） |
| T-06-05b | Denial of Service | 测试挂死拖死 CI（重连等待类场景时序敏感） | medium | mitigate | waitFor 轮询带上限（phase05-dom 先例）+ 2.5s/3s 守候窗显式值 + 进程级场景 watchdog；spawn 实例全部 SIGKILL 收口 |
| T-06-SC | Tampering | 依赖安装供应链 | low | accept | 零新依赖（jsdom 25.0.1 为 web/uat 既有锁定依赖） |
</threat_model>

<verification>
- go build -o /tmp/wesh-uat/wesh ./cmd/wesh 退出 0
- node web/uat/phase06-dom.mjs 退出 0（D1-D8 pass + 豁免 skipped 记录）
</verification>

<success_criteria>
- 1006 → 自动重连接回（面板/退避/清屏/重注册全链断言）——ROADMAP 成功准则 3 的前端行为证据
- 1002/1013/1008 不自动重连、双触发幂等、stale socket 零污染——D-01 边界与 Pitfall 5/6 防线行为化
- EXIT 帧 → Session ended 正文显示服务端组文案——SESS-03 前端承接的端到端证据
</success_criteria>

<output>
创建 `.planning/phases/06-session-lifecycle/06-05-SUMMARY.md`：记录 SpyWebSocket 合成 close 形态（A2 兑现/回退）、八场景断言面、beforeunload 记账机制、豁免场景登记、与 06-03 状态机改造点的对应关系。
</output>
