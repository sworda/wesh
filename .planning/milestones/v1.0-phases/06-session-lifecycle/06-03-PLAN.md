---
phase: 06-session-lifecycle
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - web/src/lib/reconnect.ts
  - web/src/lib/reconnect.test.ts
  - web/src/main.ts
  - web/dist/index.html
autonomous: true
requirements:
  - CORE-05

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "触发谓词 = `ev.code === 1006` 显式判定（D-01/UI-SPEC §Reconnect Dispatch 逐字契约）——onclose switch 新增 `case 1006`（仅 opened 已置位的已建立会话断开才进重连），不是 default 桶；default 桶残留（1002 协议错误等带码关闭）维持 C-5『Connection lost』手动面板逐字不变；1000/1008/1009/1011 专版面板不变；1013 维持 P5 D-10 手动刷新（被踢端自动重连只会再被踢且后台标签页循环放大流量——D-01 确认保持）；main.ts:685-687『1006 永不作为分派依据』注释随实现改写为『1006 = 重连唯一触发码（本地合成码语义不变——永不出现于线上，RFC6455 §7.4）』"
    - "退避序列（D-02）：web/src/lib/reconnect.ts 纯函数 backoffMs(attempt) = Math.min(1000 * 2 ** attempt, 30000)（1s×2 封顶 30s 无限重试，throttle.go:12-13 同族参数族）+ shouldReconnect(code) = code === 1006；零 DOM 依赖（prefs.ts 先例），reconnect.test.ts 以 node --test 锁定序列 1000/2000/4000/8000/16000/30000/30000 与 attempt 0 起点；重连成功（WELCOME 到达）退避清零"
    - "重连循环单例（RESEARCH Pitfall 5）：onclose(1006)/window offline/window online 三触发源全经『已在循环则幂等返回』入口；online = 清当前等待定时器立即 attempt（不是新循环，D-04）；offline 仅在 welcomeDone 已建立且连接非 OPEN 时启动循环（健康连接等 onclose 权威信号——事件为快路径提示层，黑洞场景退化为 TCP 超时后重连，D-04 风险接受）"
    - "代际守卫（RESEARCH Pitfall 6，本 phase 新必要件）：connect() 内全部 socket handler（onmessage/onopen/onerror/onclose）入口 `if (sock !== ws) return;`（main.ts:476 `const sock = ws` 闭包先例天然承载——重连使页面首次存在『旧 socket 未死透 + 新 socket 已建立』双连接窗口，旧 socket 迟到的 onclose/onmessage 不得触碰新会话状态）；onclose 的 beforeunload 移除同受守卫（stale socket 不得拆新连接刚注册的监听）"
    - "重连中面板保护（RESEARCH Pitfall 7，UI-SPEC 逐字契约）：循环驱动的 attempt 失败只更新 Reconnecting 面板计数——connect() 既有 catch（fetch throw）在重连上下文改走 scheduleAttempt() 留在循环、onerror-!opened 在重连上下文不显示任何面板（onclose 随后分派）；『Unable to connect』与专版面板仅终态分支允许显示：fetch 401/404/429/503 与非 1006 关闭码 → 循环终止（stopReconnect）+ 落 05-UI-SPEC 对应专版面板逐字不变"
    - "Reconnecting 面板（D-03/D-11 + UI-SPEC C-9 逐字文案）：复用 #status 全屏三态面板零 DOM/CSS 改动；showStatus 动作链接参数化（第四可选参 {label, onClick}，缺省保持 'Reload this page' + location.reload() 现状零漂移——R3，全部既有调用点渲染逐字节不变）；title 恒 'Reconnecting'；等待期 body 1Hz 更新 `The connection was lost. Retrying in {S}s (attempt {N}).`；attempt 在途 body `The connection was lost. Retrying now (attempt {N})...`；hintPrefix `If the server has exited, restart it from your shell. To skip the wait,` + [Reconnect now] 链接（点击 = 清当前等待立即 attempt，循环不终止——ROADMAP『手动入口』落此）；attempt 计数语义：首次自动重试 = attempt 1（等待 1s），初始连接不算 attempt，WELCOME 到达清零；面板文案为契约约束短文本（480px max-width 内自然折行，UI-SPEC overflow covered）；面板期间键盘输入经 ws.readyState 门控静默丢弃（既有零新逻辑）"
    - "重连成功首屏（D-05，UI-SPEC §Reconnect Success Contract 逐字）：WELCOME 到达且 reconnecting 置位时 → stopReconnect（退避清零 + 面板隐藏）+ term.clear() 清屏（不保留旧 buffer——重连窗口期错过的输出形成断层，增量重绘有 G-05-1 同源花屏风险）→ 既有 WELCOME 处理链照常（sessionDims/isRO/prefs/refit/welcomeDone 逐字既有）→ beforeunload 按开关重注册（main.ts:635-637 先例，P4 D-18）；服务端 attach 路径既有 SIGWINCH 强制重绘（server.go:752 每 attach 恒触发——服务端零改动）；行内 shell 历史交 tmux/herdr（ROADMAP 既定分工）"
    - "重连后身份与标题零新 UI（D-06 + P5 D-12，UI-SPEC covered）：ro/rw 由新 WELCOME 重新驱动——[ro] 标题前缀/disableStdin/ro 一次性 console 提示全部经既有路径自然重应用（owner 重连不恢复写权限的 UI 表达 = 既有 [ro] 前缀出现，无新文案无通知组件）；标题保持最后 remoteTitle 直到下次 OSC 2/重绘，128 code point 截断既有裁决不变（sanitizeTitle 单一写口不动）"
    - "认证不绕行：每次 attempt 走完整 connect() 重入链（fetch ticket → Hello 核销，无静默豁免通道）；凭据模式浏览器 Basic 缓存凭据随同源 fetch 自动附带（P3 A2 已 UAT 验证）；share token URL 保留（05 D-03 红线）使分享链接进入的页面重连链路不变"
    - "首连失败（opened 从未置位）不进重连循环——既有 C-4『Unable to connect』面板语义不变（D-01 触发范围 = 已建立会话的异常断开）"
  prohibitions:
    - statement: "退避必须封顶 30s 且 1013 与终态码（1000/1008/1009/1011）绝不自动重连——后台僵尸标签页不得持续放大流量或形成踢出-重连循环（D-01/D-02 边界纪律）"
      status: resolved
      verification: judgment
    - statement: "重连成功判定恒为 WELCOME 到达——WS 建连成功但握手未完成不得清零退避或隐藏面板（不得伪装接回）"
      status: resolved
      verification: judgment
    - statement: "重连不得提供任何恢复写权限的通道——owner 重连按新 attach 走 P5 D-06/D-07 递补语义（原 owner 降级 ro 入队），UI 零特判零豁免入口（D-06）"
      status: resolved
      verification: judgment
  artifacts:
    - path: "web/src/lib/reconnect.ts"
      provides: "backoffMs + shouldReconnect 纯函数（node --test 可测，零 DOM）"
      contains: "backoffMs"
    - path: "web/src/lib/reconnect.test.ts"
      provides: "退避序列与触发谓词回归锁"
      contains: "backoffMs"
    - path: "web/src/main.ts"
      provides: "重连状态机（startReconnect/scheduleAttempt/runAttempt/stopReconnect）+ showStatus 参数化 + 代际守卫 + online/offline 监听 + C-9 文案常量"
      contains: "Reconnecting"
    - path: "web/dist/index.html"
      provides: "重建产物（重连状态机随构建入库）"
      contains: ""
  key_links:
    - from: "web/src/main.ts (onclose case 1006)"
      to: "web/src/main.ts (connect 重入)"
      via: "startReconnect → scheduleAttempt（backoffMs 退避 + 面板倒计时）→ runAttempt → connect() → WELCOME 到达 → stopReconnect + term.clear()"
      pattern: "startReconnect|scheduleAttempt"
    - from: "window (online/offline 事件)"
      to: "web/src/main.ts (重连循环)"
      via: "offline → 非 OPEN 即 startReconnect；online → runAttempt 清等待立即试一次（单例幂等入口）"
      pattern: "addEventListener('online'"
    - from: "web/src/main.ts (showStatus 第四参)"
      to: "Reconnecting 面板动作链接"
      via: "{label: 'Reconnect now', onClick: runAttempt}——缺省 Reload 零漂移（R3）"
      pattern: "Reconnect now"
    - from: "web/src/main.ts (WELCOME 分支)"
      to: "@xterm/xterm (term.clear)"
      via: "reconnecting 置位时 WELCOME 到达点 → 清零 + 隐藏面板 + 清屏 + beforeunload 重注册"
      pattern: "term\\.clear"
---

<objective>
CORE-05 前端重连状态机：1006 显式触发 + 1s×2 封顶 30s 无限退避 + online/offline 双触发 + Reconnecting 面板（showStatus 参数化）+ 代际守卫 + 重连成功清屏——共享进程模型下重连即接回原 PTY 进程（服务端零改动，SIGWINCH 重绘随 attach 恒触发）。

Purpose: CORE-05 的全部前端逻辑（D-01..D-05、D-11 收口）——断线重连是本 phase 用户可感知的主干能力；connect() 可重入入口与 per-connection 重置块（IN-01）已在 Phase 3/5 备好，本 plan 是唯一接线点。三个时序风险（双循环/代际污染/面板覆盖，RESEARCH Pitfall 5/6/7）全部在本 plan 以逐字契约形态落地。
Output: lib/reconnect.ts + 单测 + main.ts 状态机 + dist 重建——jsdom 行为证据由 06-05 phase06-dom.mjs 补齐。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-session-lifecycle/06-CONTEXT.md
@.planning/phases/06-session-lifecycle/06-RESEARCH.md
@.planning/phases/06-session-lifecycle/06-PATTERNS.md
@.planning/phases/06-session-lifecycle/06-UI-SPEC.md
@.planning/phases/06-session-lifecycle/06-VALIDATION.md
@.planning/phases/06-session-lifecycle/06-01-SUMMARY.md
@web/src/main.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: web/src/lib/reconnect.ts 纯函数（backoffMs + shouldReconnect）+ node --test 回归锁</name>
  <files>web/src/lib/reconnect.ts, web/src/lib/reconnect.test.ts</files>
  <read_first>
    - web/src/lib/prefs.ts（全文 73 行——零 DOM 纯函数模块形态与文件头注释先例）
    - web/src/lib/prefs.test.ts（全文 61 行——node --test 直跑 .ts 形态；相对导入必须带 .ts 扩展名；tsconfig exclude src/**/*.test.ts 纪律）
    - internal/server/throttle.go（12-13 行 defaultThrottleBase=1s/defaultThrottleCap=30s 同族参数族出处）
    - .planning/phases/06-session-lifecycle/06-RESEARCH.md（Pattern 6 退避纯函数骨架）
  </read_first>
  <behavior>
    - backoffMs(0)=1000、backoffMs(1)=2000、backoffMs(2)=4000、backoffMs(3)=8000、backoffMs(4)=16000、backoffMs(5)=30000、backoffMs(6)=30000（封顶截断）、backoffMs(10)=30000（深尝试仍封顶）
    - shouldReconnect(1006)=true；shouldReconnect(1000/1002/1008/1009/1011/1013)=false（D-01 触发范围逐字）
  </behavior>
  <action>
    1. 新建 `web/src/lib/reconnect.ts`（文件头注释同款 prefs.ts 形态：『CORE-05 重连退避纯函数（node --test 可测——零 DOM 依赖）』；D-02 参数族注释引 throttle.go:12-13 同族）：
       - `export function backoffMs(attempt: number): number`——返回 Math.min(1000 * 2 ** attempt, 30000)（注释：1s×2 封顶 30s 无限重试——无尝试次数上限，个人运维『标签页放着，回来已接回』主场景，30s 一次重试流量可忽略，D-02）。
       - `export function shouldReconnect(code: number): boolean`——返回 code === 1006（注释：触发谓词显式判定——1002 协议错误等带码关闭留 default 桶手动面板，1013 被踢维持手动刷新，D-01；1006 为浏览器本地合成码永不出现于线上，RFC6455 §7.4）。
    2. 新建 `web/src/lib/reconnect.test.ts`（prefs.test.ts 逐字形态：import { test } from 'node:test' + assert from 'node:assert/strict' + 相对导入带 .ts 扩展名；文件头注释『本文件只经 node --test 执行，不参与 tsc（tsconfig exclude）』同款）——按 <behavior> 逐条断言（backoffMs 序列用数组逐项等值断言；shouldReconnect 正反两行）。
    3. 验证即提交——先写测试跑红（文件不存在即红）再实现转绿，一次提交含两件。
  </action>
  <verify>
    <automated>node --test web/src/lib/reconnect.test.ts && node --test web/src/lib/*.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - node --test web/src/lib/reconnect.test.ts 退出 0（序列与谓词断言全过）
    - node --test web/src/lib/*.test.ts 全绿（prefs/title 既有套件零回归）
    - grep -c 'export function backoffMs\|export function shouldReconnect' web/src/lib/reconnect.ts == 2
    - grep -c "from './reconnect.ts'" web/src/lib/reconnect.test.ts == 1（相对导入带 .ts 扩展名纪律）
  </acceptance_criteria>
  <done>退避序列与触发谓词以纯函数形态钉死（1s×2 封顶 30s；仅 1006）——Task 2 状态机与 06-05 jsdom 断言共享同一事实源。</done>
</task>

<task type="auto">
  <name>Task 2: main.ts 重连状态机（单例循环 + 代际守卫 + 面板保护 + showStatus 参数化 + online/offline）+ dist 重建</name>
  <files>web/src/main.ts, web/dist/index.html</files>
  <read_first>
    - web/src/main.ts（全文 757 行——改造面逐处：帧常量区 11-26、文案常量 356-360 UNREACHABLE_BODY/HINT_RESTART 单写口先例、showStatus 365-381 幂等形态、connect() 390-757 全链（per-connection 重置块 391-404 / fetch 分派 427-472 / `const sock = ws` 476 / WELCOME welcomeDone+beforeunload 634-637 / ERROR 暂存 643-648 / onerror 677-683 / onclose 688-754 含 685-687 待改写注释））
    - web/src/lib/reconnect.ts（Task 1 产出——backoffMs/shouldReconnect 签名）
    - .planning/phases/06-session-lifecycle/06-UI-SPEC.md（§Reconnect Dispatch Contract / §Reconnect Panel Contract / §Reconnect Success Contract / §Copywriting C-9 / §Revisions R1/R3/R4 全部逐字契约）
    - .planning/phases/06-session-lifecycle/06-RESEARCH.md（Pattern 6 状态机形态与循环策略规则 / Pitfall 5/6/7 逐字 / State of the Art 1006 注释改写登记）
    - .planning/phases/06-session-lifecycle/06-PATTERNS.md（§9 Analog B/C/E/F/G/H 逐字先例）
  </read_first>
  <action>
    1. 文案常量化（UNREACHABLE_BODY/HINT_RESTART 356-360 同款单写口形态）：RECONNECTING_TITLE = 'Reconnecting'；RECONNECTING_HINT = 'If the server has exited, restart it from your shell. To skip the wait,'（C-9 hintPrefix 逐字）；等待期/在途 body 由函数组串：等待期 `The connection was lost. Retrying in ${S}s (attempt ${N}).`、在途 `The connection was lost. Retrying now (attempt ${N})...`（C-9 逐字模板；注释：三处同源单写口纪律，05-08 C-4/C-6 常量化先例）。
    2. showStatus 参数化（R3/OQ2 定稿）：
       - 前置影响面清点（review #2 吸收——「侵入面大于计划所述」关切的闭合动作）：grep -n 'showStatus(' web/src/main.ts 清点全部既有调用点并逐列清单（预期 = 各专版面板分支调用：1000/1008/1009/1011/1013/default/Unable to connect/认证两态等），逐一确认第四参可选形态下零改动——清点结果记入 SUMMARY。
       - 签名加第四可选参 `action?: { label: string; onClick: () => void }`——缺省时链接保持 'Reload this page' + location.reload() 逐字现状（全部既有调用点零改动、渲染逐字节不变）；传参时 a.textContent = action.label、click handler preventDefault 后调 action.onClick()（review LOW preventDefault 关切已含于此规格）；幂等纪律保持（textContent 先清后建）。
    3. 重连状态与循环函数（模块级——声明于页面级门闩区，osc52Loaded/retriedAuth 声明邻位，review placement 显式化建议吸收；注释：reconnecting/attempt 为页面级状态——osc52Loaded/retriedAuth 页面级门闩同族，connect() per-connection 重置块刻意不重置）：
       - `let reconnecting = false; let attempt = 0; let reconnectTimer: number | undefined; let retryAt = 0; let countdownTimer: number | undefined;`
       - `startReconnect()`：reconnecting 幂等门闩（已 true 直接 return——三触发源共用入口，Pitfall 5）；置位后 scheduleAttempt()。
       - `scheduleAttempt()`：`const delay = backoffMs(attempt); retryAt = Date.now() + delay;` showStatus(RECONNECTING_TITLE, 等待期 body(attempt+1, 剩余秒), RECONNECTING_HINT, { label: 'Reconnect now', onClick: runAttempt })；reconnectTimer = setTimeout(runAttempt, delay)；countdownTimer = setInterval(1000ms 内以 retryAt 重算剩余秒数更新 #status-body textContent——1Hz 更新只写既有节点，面板不闪隐，UI-SPEC §Reconnect Panel Contract)。
       - `runAttempt()`：清 reconnectTimer/countdownTimer；attempt++；showStatus 在途 body(attempt)；`void connect()`。
       - `stopReconnect()`：reconnecting=false；attempt=0；清两 timer；`document.getElementById('status')!.hidden = true;`。
    4. connect() 面板保护与终态分派（Pitfall 7 逐字契约）：
       - fetch 的 catch 分支：重连上下文（reconnecting 为 true）改走 `scheduleAttempt(); return;`（fetch throw = 网络不可达/服务端已退出不可区分，D-11——留在循环，面板持续）；非重连上下文保持既有 showStatus('Unable to connect', ...) 逐字。
       - fetch 非 ok 分派（401/404/429/503/通用认证失败）：各专版面板分支前统一 `if (reconnecting) stopReconnect();`（终态循环终止——面板文案逐字既有）。
       - sock.onerror 的 !opened 分支：`if (reconnecting) return;` 前置（onclose 随后来临分派——不覆盖 Reconnecting 面板）；非重连上下文既有形态不变。
    5. 代际守卫（Pitfall 6）：connect() 内 sock.onmessage/onopen/onerror/onclose 四个 handler 入口第一行统一 `if (sock !== ws) return;`（注释：重连引入双连接窗口，stale 代际事件丢弃；今日单连接生命周期下该判定恒真——06-03 起成为必需闸）。
    6. onclose 按码分派改造（在代际守卫之后、既有 auth_failed 重试块保持）：
       - beforeunload 移除行保持原位（现在在代际守卫保护下——stale socket 不拆新连接监听，R4）。
       - 新增：`if (reconnecting && ev.code === 1006) { scheduleAttempt(); return; }`（再 1006 留循环）；`if (reconnecting) { stopReconnect(); }`（非 1006 → 终态，落下方分派——注释：重连中收到带码关闭 = 服务端明确语义，终止循环落专版面板）。
       - 既有 `if (!opened)` Unable to connect 分支保持（首连失败不进重连循环——D-01 触发范围 = 已建立会话的异常断开）。
       - switch 在 default 前新增 `case 1006:` → `startReconnect(); break;`（注释：D-01 显式触发谓词——1002 协议错误等带码关闭留 default 桶 C-5 手动面板逐字不变；1013 维持手动刷新，P5 D-10）。
       - 685-687 注释改写：『1006 永不作为分派依据』→『1006 = 重连唯一触发码（浏览器本地合成码，永不出现于线上——RFC6455 §7.4，proto.go 关闭码纪律不变）；其余码分派语义不变』（State of the Art 登记项兑现）。
    7. WELCOME 分支重连成功点（welcomeDone = true 与 beforeunload 条件注册区，634-637）：`if (reconnecting) { stopReconnect(); term.clear(); }`——顺序注释：清零+隐藏面板 → 清屏（D-05 不保留旧 buffer——G-05-1 同源花屏风险裁决；服务端 SIGWINCH 随 attach 恒触发强制重绘，server.go:752 挂点零改动）→ beforeunload 重注册既有代码照常（P4 D-18 先例）。注释登记 term.clear() 与 osc52Loaded 门闩无耦合（buffer 清操作不触模块级页面门闩——review 核实建议吸收）。清屏后标题保持最后 remoteTitle 直到下次 OSC 2/重绘（P5 D-12——不主动重置）。
    8. online/offline 监听（模块级，connect 函数外）：
       - `window.addEventListener('offline', ...)`：`if (reconnecting || !welcomeDone) return;`（未建立会话的初连窗口不启动——防与在飞 connect 双连接）；`if (ws !== null && ws.readyState === WebSocket.OPEN) return;`（连接健康等 onclose 权威信号——loopback 开发场景 offline 不断 loopback TCP）；否则 startReconnect()（注释：D-04 快路径——OS 级网络断开秒级感知；黑洞场景退化为 TCP 超时后重连，风险接受）。
       - `window.addEventListener('online', ...)`：`if (reconnecting) runAttempt();`（清当前等待立即试一次——不是新循环，D-04）。
    9. dist 重建：`time pnpm -C web build`；web/dist/index.html 变更随本任务提交（dist 真实产物入库既定策略）。
    10. 回归：node --test web/src/lib/*.test.ts 全绿；既有面板分派（1000/1008/1009/1011/1013/default）逐字不动——git diff 人工核读确认只增不改（R1：default 桶残留语义不变）。
  </action>
  <verify>
    <automated>time pnpm -C web build && node --test web/src/lib/*.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - time pnpm -C web build 退出 0（tsc 严格模式过 + vite + gzip）
    - node --test web/src/lib/*.test.ts 全绿
    - grep -c 'ev.code === 1006' web/src/main.ts >= 1（显式触发谓词；shouldReconnect 谓词在主分派路径等价使用亦可——两形态其一存在即可，源码核读确认 1002 不进重连分支）
    - grep -c 'sock !== ws' web/src/main.ts >= 4（onmessage/onopen/onerror/onclose 四 handler 代际守卫）
    - grep -c "addEventListener('online'" web/src/main.ts == 1 且 grep -c "addEventListener('offline'" web/src/main.ts == 1
    - grep -c 'Reconnect now' web/src/main.ts >= 1 且 grep -c 'Reconnecting' web/src/main.ts >= 2（常量与面板调用点）
    - grep -c 'term.clear()' web/src/main.ts == 1（重连成功清屏唯一调用点）
    - grep -c 'function startReconnect\|function scheduleAttempt\|function runAttempt\|function stopReconnect' web/src/main.ts == 4（状态机四函数）
    - grep -c 'action?:' web/src/main.ts == 1（showStatus 第四可选参签名——review #2 形态锁定）
    - showStatus 前置影响面清点已执行（全部既有调用点清单入 SUMMARY）且既有调用点零改动（git diff 核读：1008/1009/1011/1013/default/Unable to connect 各 showStatus 调用行逐字不变）
    - git status 显示 web/dist/index.html 已修改并随任务提交
  </acceptance_criteria>
  <done>CORE-05 前端主链落地：1006 类断开 → Reconnecting 面板（倒计时 + Reconnect now）→ 退避自动重连 → WELCOME 清零 + 清屏接回原进程；终态码/1013/首连失败三边界不自动重连；代际守卫与面板保护防陈旧事件污染。行为证据由 06-05 phase06-dom.mjs 八场景锁定。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

本 phase 无 plain *-SPEC.md；edge-probe 对本条需求的分类为 unclassified（中文需求文本未被英文启发式命中，分类器局限），按 #1110 规则保持 unresolved，作为 flagged assumption 显式记录（no-silent-drop）：

- requirement: CORE-05 ｜ probe: "unclassified — review manually" ｜ status: flagged-unverified ｜ 处置：边角面已由 CONTEXT.md D-01..D-07 七条决策、06-RESEARCH Pattern 6（状态机形态）与 Pitfall 5/6/7（双循环/代际污染/面板覆盖）、06-UI-SPEC §Reconnect Dispatch/Panel/Success 三节逐字契约实质覆盖；可验证断言已提升为 must_haves.truths，执行期由 reconnect.test.ts 单测与 06-05 phase06-dom.mjs 实证；协议层『接回同一 PTY』由 06-06 phase06.mjs S6 实证。
- UI-SPEC dismissed 行登记（no-silent-drop）：empty/partial/zero-one-many 维度 dismissed——本 phase 无数据集合与列表（01 裁决同款，06-UI-SPEC §UI Considerations 表逐字理由）。
- canon-referral（ADR-550 D6）：重连认证绕行（每次 attempt 走完整 ticket 链）属 OWASP 认证 canon——由 threat_model T-06-03c 与既有 SEC-02 核销链覆盖；EXIT/面板文案注入面属注入 canon——T-06-03b 覆盖；均不立 prohibition 条目。

## Review Dispositions (06-REVIEWS, OpenCode)

| 发现 | 处置 |
|------|------|
| HIGH #2：showStatus 参数化侵入面大于所述；建议独立 `showStatusWithAction()` 避免改全部调用方 | **裁决：维持参数化形态，驳回独立函数**——① UI-SPEC R3/OQ2 已定稿参数化（approved contract 2026-08-23，「三态单组件纪律下唯一不复制 DOM 形态」）；独立函数须复制 #status-title/body/hint 三节点 DOM 操作，违反 P5 D-07 三态单组件哲学，且制造双写口漂移面；② TS 可选第四参使既有调用点**零改动**——review 的「需修改全部调用方」影响面评估基于必选参数误解；③ 本论吸收其有效内核：Task 2 补前置影响面清点（grep 调用点清单入 SUMMARY）+ `action?:` 签名 grep 验收 + 既有 git diff 核读条款保持 |
| HIGH：term.clear() 位置需核实（WELCOME handler 未查 reconnecting） | 已覆盖——Task 2 action 7 显式规格化 `if (reconnecting) { stopReconnect(); term.clear(); }` 于 WELCOME 分支（634-637 区）；本轮再补 osc52Loaded 无耦合注释（review Suggestion 吸收） |
| MEDIUM：connect() 重连上下文传参 vs 模块级状态选形 | 维持计划选形（模块级状态——review 认可 option 2）；per-connection 重置块刻意不重置页面级门闩的纪律已注释化 |
| MEDIUM：lastExit/reconnecting 声明位置未钉死 | 已吸收——lastExit 邻 lastError（06-01 Task 3）、重连状态邻 osc52Loaded/retriedAuth 门闩区（本 plan Task 2 action 3） |
| LOW：「Reconnect now」click 需 preventDefault | 已覆盖——Task 2 action 2 规格含「click handler preventDefault 后调 action.onClick()」 |
| Suggestion：代际守卫显式测试（旧 socket close 不得隐藏新会话面板） | 已覆盖——06-05 phase06-dom.mjs D6 场景逐字承载该断言 |

## Artifacts this plan produces

- 新文件 `web/src/lib/reconnect.ts`：`backoffMs(attempt: number): number`、`shouldReconnect(code: number): boolean`
- 新文件 `web/src/lib/reconnect.test.ts`：backoffMs 序列与 shouldReconnect 谓词 node --test 用例
- `web/src/main.ts`：模块级状态 `reconnecting`/`attempt`/`reconnectTimer`/`retryAt`/`countdownTimer`；函数 `startReconnect`/`scheduleAttempt`/`runAttempt`/`stopReconnect`；文案常量 `RECONNECTING_TITLE`/`RECONNECTING_HINT` 与 body 模板；`showStatus` 第四可选参 `action?: { label, onClick }`；四个 socket handler 代际守卫；onclose `case 1006`；WELCOME 重连成功点（stopReconnect + term.clear()）；window online/offline 监听
- `web/dist/index.html`：重建产物

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 网络事件→重连状态机 | online/offline/onclose 事件驱动自动重连——触发面不得被放大为流量放大器 |
| 重连 attempt→认证链 | 每次重连是新的完整认证——不得出现绕过 ticket 核销的静默通道 |
| 服务端文案→面板 DOM | 面板正文/提示行渲染——钓鱼文案注入面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03a | Denial of Service | 重连循环流量放大（恶意/僵尸标签页） | high | mitigate | 仅 1006 类触发（D-01）+ 退避 1s×2 封顶 30s（单标签页最坏 2 次/分钟 fetch+WS 握手，量级可忽略——CONTEXT 明示裁断）；1013 被踢端不自动重连（防再踢循环）；终态码终止循环；shouldReconnect 单测 + 06-05 D2/D3 断言锁定 |
| T-06-03b | Tampering | Reconnecting 面板钓鱼文案 | medium | mitigate | title/body/hint 全部前端硬编码常量（UNREACHABLE_BODY/HINT_RESTART 常量化先例，05-08）；textContent 渲染无 innerHTML；EXIT message 虽来自服务端但经 textContent 直显（06-01 T-06-01b 同防线） |
| T-06-03c | Spoofing | 重连绕过认证（免 ticket 静默通道） | high | mitigate | 结构性不存在：每次 attempt 走完整 connect() 重入链（fetch /api/attach → Hello 核销）；share token URL 保留使重 attach 链路不变（P5 D-03）；服务端 checkTicket 对本路径零特判（无新增豁免代码即无豁免面） |
| T-06-03d | Tampering | 陈旧 socket 代际事件污染新会话 | medium | mitigate | 全部 handler 入口 if (sock !== ws) return（const sock = ws 闭包承载）；beforeunload 移除同受守卫；06-05 D6 场景断言锁定 |
| T-06-SC | Tampering | 依赖安装供应链 | low | accept | 本 plan 零新依赖零安装（06-RESEARCH Package Legitimacy Audit：无输入） |
</threat_model>

<verification>
- time pnpm -C web build 退出 0（dist 变更入库）
- node --test web/src/lib/*.test.ts 全绿（含 reconnect.test.ts 新件）
- 源码断言：1006 显式谓词、四 handler 代际守卫、online/offline 监听、状态机四函数、term.clear() 唯一调用点、既有面板分支逐字不动
</verification>

<success_criteria>
- 断网类异常断开（1006）后前端自动重连（指数退避 + 30s 上限 + Reconnect now 手动入口）并接回同一 PTY 进程——ROADMAP 成功准则 3 的前端半侧（协议层证据 06-06 S6）
- 1000/1008/1009/1011/1013/1002 与首连失败的既有面板语义零漂移
- 重连成功首屏干净（清屏 + SIGWINCH 重绘），不保留旧 buffer（D-05）
</success_criteria>

<output>
创建 `.planning/phases/06-session-lifecycle/06-03-SUMMARY.md`：记录状态机四函数与触发谓词落码形态、Pitfall 5/6/7 三防线的实现落点、showStatus 参数化形态（R3）、C-9 文案常量、dist 重建、与 06-05 场景断言的对应关系。
</output>
