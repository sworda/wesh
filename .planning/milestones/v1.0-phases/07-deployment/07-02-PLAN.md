---
phase: 07-deployment
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
autonomous: true
requirements:
  - OPS-01

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "wesh --socket /run/wesh.sock -- bash 在指定路径监听 unix socket，打印 listening on unix:///run/wesh.sock，socket 文件权限恰为 --socket-mode（默认 0660），--socket-owner 给出时属主为解析后的 uid/gid"
    - "socket 路径已存在残留文件时 listen 前被移除（不 Remove 则 bind EADDRINUSE——GOROOT 实证 Go 不自动 unlink）；listen 后任一步失败回滚零残留（UnixListener Close 自动 unlink）"
    - "--socket 与显式 --port/--bind 同给、--socket-mode/--socket-owner 单独给出（无 --socket）均 validateStartup fail-fast exit 2；--socket-owner 未知用户/组 parse 期 exit 2"
    - "unix socket 形态下 validateStartup 的 bind 安全矩阵跳过（D-11 文件系统权限即认证边界）；分享链接两行退化为单行 unix 提示，绝不拼误导性 TCP 链接"
    - "未配置 --socket 时 TCP listen 路径与启动打印逐字节不变（端口 0=随机打印实际端口现状保持）"
  artifacts:
    - path: "cmd/wesh/main.go"
      provides: "--socket/--socket-mode/--socket-owner 三 flag + parse 期 owner 解析 + validateStartup 四新行 + run() unix listen 分岔 + unix 启动打印与分享链接退化"
      contains: "listenSocket"
    - path: "cmd/wesh/main_test.go"
      provides: "TestParseArgs/TestStartupMatrix 新行 + TestListenSocket（listen 序列/权限位/残留清理/失败回滚）"
      contains: "TestListenSocket"
  key_links:
    - from: "cmd/wesh/main.go run() unix 分岔"
      to: "net.Listen(\"unix\") + os.Chmod + os.Chown 序列"
      via: "listenSocket helper——Go 不做 bind 前 unlink、socket mode 由内核 umask 决定，0660 确定性必须显式达成"
      pattern: "listenSocket"
    - from: "cmd/wesh/main.go validateStartup"
      to: "fs.Visit 显式设置位（portSet/bindSet/socketModeSet/socketOwnerSet）"
      via: "互斥与单给判定锚定显式位而非终值（write-policy/max-clients 三先例同形态）"
      pattern: "socketModeSet|socketOwnerSet|portSet|bindSet"
  prohibitions:
    - statement: "MUST NOT widen the unix-socket access boundary beyond the configured mode/owner——socket 文件是认证边界（D-11），权限不得由 umask 漂移决定，Chmod/Chown 失败必须回滚退出而非带病放行"
      status: resolved
      verification: judgment
---

<objective>
落地 OPS-01 的 UNIX socket 半条（D-08..D-12）：独立 `--socket` flag 与 --port/--bind 互斥、`--socket-mode`（八进制默认 0660）+ `--socket-owner user[:group]` 属主权限、listen 前 os.Remove 残留清理、listen 后显式 Chmod/Chown、validateStartup 矩阵 unix 跳过 + 组合矛盾 fail-fast、启动打印 unix:// 形态与分享链接退化。

端口 0=随机并打印实际端口、绑定地址可配置两半条为现状行为（main.go:119-120,459,483），本 plan 不涉及。

Purpose: systemd/反代本机部署形态下 unix socket 是标配；文件系统权限即认证边界要求权限位确定性（不能靠 umask）。
Output: --socket 三 flag 全链可用，组合矛盾零窗口暴露，TCP 路径零漂移。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deployment/07-CONTEXT.md
@.planning/phases/07-deployment/07-RESEARCH.md
@.planning/phases/07-deployment/07-PATTERNS.md
@cmd/wesh/main.go
@cmd/wesh/main_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: --socket 三 flag + parse 期 owner 解析 + validateStartup 组合矛盾矩阵扩展</name>
  <reversibility rating="one-way">D-08/D-09：--socket/--socket-mode/--socket-owner 是 CLI 公开契约（P2 D-15 纪律）——用户已在 07-CONTEXT 锁定，记录评级不重复设卡。</reversibility>
  <files>cmd/wesh/main.go, cmd/wesh/main_test.go</files>
  <read_first>
    - cmd/wesh/main.go（config struct L28-54、flag 注册 L117-208、fs.Visit 显式设置位 L216-228、validateStartup L368-412——loopback 早退 L396-398 是 D-11 跳过的直接参照）
    - cmd/wesh/main_test.go（TestParseArgs 表结构 L32-86、TestStartupMatrix 三通道断言形态 L402-484、行尾红线断言 L478-481、TestStartupRefusalNoResource L490+）
    - .planning/phases/07-deployment/07-PATTERNS.md（§main.go：config struct 扩展形态 + validateStartup 扩展落点）
  </read_first>
  <behavior>
    - `--socket /run/wesh.sock` 原样入 cfg.socket；`--socket-mode 0660` 八进制解析入 cfg.socketMode（os.FileMode，默认 0660）；`--socket-owner alice:staff` parse 期经 os/user.Lookup + LookupGroup 解析为 uid/gid 整数对入 cfg
    - --socket-mode 非法值（非八进制数字、>0777 含特殊位）parse 期 exit 2
    - --socket-owner 未知用户/未知组 parse 期 exit 2（值非敏感可回显用户名，密码类红线不涉及）
    - validateStartup 新行（全部在 loopback 早退之前，纯配置矛盾同位纪律）：
      a. cfg.socket != "" 且（portSet 或 bindSet）→ 拒绝，文案含 --socket 与 --port/--bind 双 flag 名
      b. socketModeSet 或 socketOwnerSet 为真且 cfg.socket == "" → 拒绝，文案含 --socket-mode/--socket-owner 与 --socket
      c. cfg.socket != "" → 在 bind 安全矩阵（isLoopbackBind 早退及其后全部）之前 return "", nil（D-11 跳过）
    - TestParseArgs 新行：socket 三 flag 解析/默认 0660/owner 解析为数字对；TestStartupMatrix 新行：上述 a/b 两拒绝 + c 跳过（socket + 非 loopback 默认 bind 无凭据 → 不拒不警告）
    - TestStartupRefusalNoResource 同款纪律覆盖新拒绝路径（拒绝时零 listen 零 spawn）
  </behavior>
  <action>
    ① config struct（main.go:28-54）加 Phase 7 监听组字段（D-08/D-09 决策号注释）：`socket string`、`socketMode os.FileMode`（默认 0660）、`socketOwner string`（原始 user[:group]）、`socketUid int`、`socketGid int`（owner 解析产物，未给 = -1 哨兵）；显式设置位字段：`portSet`、`bindSet`、`socketModeSet`、`socketOwnerSet bool`。
    ② flag 注册（L117-208 区段，全名无短选项纪律）：`--socket`（StringVar 默认 ""）、`--socket-mode`（StringVar 默认 "0660"——八进制串，注释明示八进制语义与默认值）、`--socket-owner`（StringVar 默认 ""，help 注明 user[:group]）。
    ③ fs.Visit 块（L216-228）补四个显式位赋值（三先例逐字形态）。
    ④ Parse 返回处校验段（write-policy 枚举校验同位插入点）：
    - socketMode：`strconv.ParseUint(cfg.socketModeStr, 8, 32)` 转 os.FileMode；失败或值 > 0777 → `invalid --socket-mode %q: must be octal permission bits (e.g. 0660)`（值非敏感可回显）
    - socketOwner 非空时：`strings.Cut(s, ":")` → user.Lookup(user) 得 uid/gid；有 group 分量再 LookupGroup 覆盖 gid；任一失败 → `invalid --socket-owner: unknown user or group`（错误类别+flag 名，不泄露系统细节之外信息）；结果入 cfg.socketUid/socketGid
    ⑤ validateStartup（L368-412）按 behavior 三行扩展；D-11 跳过行注释写明「文件系统权限即认证边界，socket-mode/owner 就是访问控制——loopback 早退同款信任档位」。
    ⑥ main_test.go 扩展（03-04 命名字段转换先例，既有行零改动）：TestParseArgs 新行断言 socket/socketMode 默认/owner 解析；TestStartupMatrix 新行（wantErrSub/wantErrSub2/wantWarnSub 三通道形态沿用）+ 行尾红线断言（文案不含凭据值）自然覆盖新行。
    提交：`feat(07-02): add --socket/--socket-mode/--socket-owner flags with startup validation`
  </action>
  <verify>
    <automated>go test ./cmd/wesh -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/wesh -run 'TestParseArgs|TestStartupMatrix|TestStartupRefusalNoResource' -count=1 -v` 退出 0 且输出含新行名（socket 组 ≥ 5 个新用例名含 socket 子串）
    - `grep -c 'socketModeSet\|socketOwnerSet\|portSet\|bindSet' cmd/wesh/main.go` ≥ 8（四字段声明 + fs.Visit 四赋值 + validateStartup 消费）
    - `go vet ./cmd/wesh` 退出 0
  </acceptance_criteria>
  <done>
    三 flag 解析与全部组合矛盾校验经表驱动测试锁定；--socket + 默认 port/bind 不误判冲突（显式位锚定）；D-11 跳过行使 unix 形态下 bind 安全矩阵不可达。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run() unix listen 分岔（Remove→Listen→Chmod→Chown+回滚）+ unix 启动打印与分享链接退化</name>
  <files>cmd/wesh/main.go, cmd/wesh/main_test.go</files>
  <read_first>
    - cmd/wesh/main.go（run() net.Listen 分岔点 L457-466 失败回滚先例、启动打印 L477-505、ln.Addr().(*net.TCPAddr) 断言防御 L499-501）
    - .planning/phases/07-deployment/07-RESEARCH.md（Pattern 1 序列逐字配方 + 关键事实：UnixListener unlink:true 默认、umask 窗口 A5）
  </read_first>
  <behavior>
    - listenSocket(path, mode, uid, gid) helper：os.Remove(path)（不存在忽略）→ net.Listen("unix", path) → os.Chmod(path, mode) → uid>=0 时 os.Chown(path, uid, gid)；任一步失败 → ln.Close()（自动 unlink 回滚零残留）+ 返回 error
    - 测试（t.TempDir）：预建垃圾文件于 path → listenSocket 成功且监听可用（net.Dial("unix", path) 通）；stat 权限位恰为 0660；owner 为 self（os.Getuid/Getgid）时 Chown 成功属主正确；Chmod 失败注入（path 父目录只读或无效路径）→ error 且 path 无残留文件
    - run() unix 分岔：cfg.socket != "" 走 listenSocket，失败 `wesh: %v` exit 1 且 sess.Close() 回滚（net.Listen 失败路径逐字对称先例）；TCP 分岔现状逐字不动
    - 启动打印：unix 形态 `listening on unix://<path>`；分享链接两行替换为单行 `share links: unavailable on unix socket (front with a reverse proxy to share)`；ln.Addr().(*net.TCPAddr) 断言防御（L499-501）保留——unix 形态 ok=false 天然不拼 TCP 端口
    - Serve/ServeTLS 汇合点之后零分岔（hs.Serve(ln) 同一调用）
  </behavior>
  <action>
    ① 新增 `listenSocket(path string, mode os.FileMode, uid, gid int) (net.Listener, error)`（main.go run() 上方，isLoopbackBind 同位纯 helper 纪律）：按 behavior 序列实现，注释登记 D-10（Go listenStream 直接 syscall.Bind 无 unlink，GOROOT sock_posix.go 实证——os.Remove 是必需而非保险）、D-09（socket mode = 0777&~umask 内核行为，0660 确定性必须显式 Chmod；Chmod/Chown 与 listening 打印之间的 umask 窗口风险接受 A5——窗口内无客户端被指引）、回滚（UnixListener 默认 unlink:true——Close 自动删文件，GOROOT unixsock_posix.go:210-216,230）。
    ② run() 改造：net.Listen 调用点分岔——cfg.socket != "" 时 `ln, err = listenSocket(cfg.socket, cfg.socketMode, cfg.socketUid, cfg.socketGid)`，否则现状 TCP 一行；失败回滚块（sess.Close + 打印 + return 1）两分岔共用现状形态。
    ③ 启动打印分岔（L477-505 区段）：cfg.socket != "" 时打印 `listening on unix://%s\n`（cfg.socket 原样——/run/wesh.sock 即得 unix:///run/wesh.sock 三斜杠形态，D-12）与 share 退化单行（behavior 给定文案逐字）；TCP 形态现状逐字不动。unix 分支必须在 hs 装配之前完成——listening 打印在 Chmod/Chown 之后（umask 窗口内不指引客户端，A5 纪律）。
    ④ main_test.go 新增 TestListenSocket（t.TempDir 四子测：残留清理+可拨通、权限位 0660、owner=self 属主、失败回滚零残留——失败注入用 mode 合法但 path 父目录不存在使 Listen 失败、或对已 Close 场景断言；owner 子测用 os.Getuid()/os.Getgid() self 免 root）。
    提交：`feat(07-02): unix socket listen fork with Remove/Chmod/Chown sequence`
  </action>
  <verify>
    <automated>go test ./cmd/wesh -run 'TestListenSocket' -count=1 -v && go test ./cmd/wesh -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/wesh -run 'TestListenSocket' -count=1 -v` 退出 0 且四子测全 PASS
    - `grep -c 'func listenSocket' cmd/wesh/main.go` == 1
    - `grep -c 'listening on unix://' cmd/wesh/main.go` == 1
    - `grep -c 'unavailable on unix socket' cmd/wesh/main.go` == 1
    - `go test ./cmd/wesh -count=1` 全绿（Task 1 行不回归）
  </acceptance_criteria>
  <done>
    --socket 形态 listen/权限/属主/残留清理/失败回滚经测试锁定；启动打印 unix:// 形态与分享链接退化单行落地；TCP 形态零漂移（既有 main_test.go 全绿）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → wesh | socket 路径是 operator 配置；路径上既有文件可能是攻击者预置（/run 或共享 tmp 目录） |
| local user → unix socket | socket 文件权限位是唯一访问控制（D-11 文件系统即认证边界） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02a | Tampering | listenSocket 序列 | medium | mitigate | D-10 listen 前 os.Remove + listen 后显式 Chmod/Chown；失败回滚（Close 自动 unlink）零残留；部署建议 /run 下 root 拥有目录（README 07-08 落地） |
| T-07-02b | Elevation of Privilege | socket 文件权限位 | high | mitigate | --socket-mode 默认 0660 最小授权，显式 Chmod 达成（拒绝 umask 漂移）；Chmod/Chown 失败即回滚 exit 1 而非带病放行（prohibition 同义锁定） |
| T-07-02c | Information Disclosure | Chmod 与 listen 间 umask 窗口 | low | accept | RESEARCH A5 已评估：Chmod/Chown 在 listening 打印前完成，窗口内无客户端被指引；本地同机攻击者模型内风险极低 |
| T-07-02d | Denial of Service | 残留 socket 致重启失败 | low | mitigate | D-10 os.Remove 使 systemd Restart= 场景零人工干预 |
</threat_model>

<verification>
- `go test ./cmd/wesh -count=1` 全绿（含 TestListenSocket 四子测与全部新矩阵行）
- `go build ./...` 退出 0
- 既有 UAT 不涉及 socket（TCP 路径零漂移由 main_test.go 全绿佐证；协议层套件在 07-07 统一回归）
</verification>

<success_criteria>
- `wesh --socket $TMP/wesh.sock -- bash`：$TMP/wesh.sock 存在且权限 0660；curl --unix-socket 或 nc -U 可建连；stdout 首行 `listening on unix://$TMP/wesh.sock`，无 http:// 分享链接行
- `wesh --socket $TMP/wesh.sock --port 7682 -- bash` → exit 2 且文案含 --socket 与 --port
- `wesh --socket-mode 0660 -- bash` → exit 2 且文案含 --socket-mode 与 --socket
- TCP 默认形态 `wesh -- bash` 行为与今日完全一致
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **OPS-01 (concurrency, unresolved)**：探针问题「If interrupted or run in parallel, what is guaranteed?」——本 plan 的设计答案：两实例竞争同一 socket 路径时后者 bind EADDRINUSE exit 1（无静默赢者之外的保证）；Remove→Listen→Chmod 之间中断时，UnixListener Close 自动 unlink 保证零残留（已建 listener 场景）；未 listen 成功前中断（进程被杀）可能留下 socket 文件——下次启动 D-10 Remove 清理。该假设已显式记录，07-08 人工 UAT 清单复核。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- config 字段 `socket` / `socketMode` / `socketOwner` / `socketUid` / `socketGid` / `portSet` / `bindSet` / `socketModeSet` / `socketOwnerSet`（cmd/wesh/main.go）
- flag `--socket` / `--socket-mode` / `--socket-owner`（cmd/wesh/main.go）
- `listenSocket(path string, mode os.FileMode, uid, gid int) (net.Listener, error)`（cmd/wesh/main.go，新函数）
- `TestListenSocket`（cmd/wesh/main_test.go）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-deployment/07-02-SUMMARY.md` when done
</output>
