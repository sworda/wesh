---
phase: 07-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
  - internal/server/server.go
  - internal/server/sharetoken.go
  - internal/server/basepath_test.go
  - web/src/main.ts
  - web/dist/index.html
autonomous: true
requirements:
  - OPS-02

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "wesh --base-path /wesh -- bash 下，GET /wesh 收到 307 且 Location 为 /wesh/，GET /wesh/ 返回 200 终端页面，WS 升级 /wesh/ws 完成 Hello/Welcome 握手"
    - "base-path 形态下分享链接全链可用：GET /wesh/s/{token}/ 给页、裸 /wesh/s/{token} 307 补斜杠、POST /wesh/api/attach 携 token 签发 ticket"
    - "未配置 base-path 时全部既有路由行为逐字节不变（/ 给页、/ws、/api/attach、/s/{token}/）"
    - "前端在 / 、/wesh/ 、/s/{token}/ 、/wesh/s/{token}/ 四类挂载点下 fetch 与 WS URL 均解析到正确的服务端路由（相对路径 + share 页 ../../ 升级前缀）"
    - "非法 --base-path 值（无前导斜杠 / 尾斜杠 / .. / 重复斜杠 / 非 path 安全字符）parse 期 exit 2，根 / 视为未配置"
    - "启动打印的分享链接两行含 base-path 前缀（scheme://host:port<bp>/s/<token>/）"
  artifacts:
    - path: "cmd/wesh/main.go"
      provides: "--base-path flag + normalizeBasePath parse 期严格校验 + server.Options.BasePath 接线 + 分享链接拼串注入前缀"
      contains: "normalizeBasePath"
    - path: "internal/server/server.go"
      provides: "Options.BasePath + Handler() bp 前缀装配（StripPrefix 仅包静态伺服）"
      contains: "BasePath"
    - path: "internal/server/sharetoken.go"
      provides: "registerShareRoutes 带 bp 前缀的两条注册（GET + 405 fallback）"
    - path: "internal/server/basepath_test.go"
      provides: "TestBasePathRoutes/TestBasePathWS：307/404/405/share 交叉/WS 全链断言"
      min_lines: 80
    - path: "web/src/main.ts"
      provides: "share 正则不锚 ^ + up 升级前缀 + fetch/WS 相对 URL 构造"
      contains: "'../../'"
  key_links:
    - from: "web/src/main.ts connect()"
      to: "internal/server Handler() bp 路由"
      via: "相对 fetch/WS URL → 浏览器按地址栏路径解析 → mux bp 前缀模式"
      pattern: "up \\+ '(api/attach|ws)'"
    - from: "cmd/wesh/main.go 分享链接打印"
      to: "internal/server registerShareRoutes bp 注册"
      via: "同一 basePath 值拼串与注册——打印的链接必须命中注册的路由"
      pattern: "basePath"
  prohibitions:
    - statement: "MUST NOT auto-correct or silently normalize a malformed --base-path value — 非法值 parse 期拒绝，输入与生效值不得分叉（D-13 显式哲学）"
      status: resolved
      verification: judgment
---

<objective>
落地 OPS-02 反代子路径挂载（D-13/D-14）：`--base-path /wesh` 严格校验 + 服务端 mux 前缀装配（StripPrefix 仅包静态伺服，307 尾斜杠规范化免费获得）+ 前端相对 URL 三改（share 正则不锚 ^ 兼作挂载点检测 + `../../` 升级前缀）。

本 plan 是 phase 的 tracer：唯一横跨「CLI parse → HTTP 路由 → WS 升级 → 前端 → dist」全层的特性，且承载全 phase 最高回归风险（RESEARCH Pitfall 3：share 页相对解析 401 链）。

Purpose: 反代子路径挂载是真实运维部署的第一诉求（nginx location /wesh/）；前端相对路径改造若只去前导斜杠会使全部分享链接失效。
Output: --base-path 全链可用（页面/WS/share 链接/307 规范化），未配置时行为零漂移。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deployment/07-CONTEXT.md
@.planning/phases/07-deployment/07-RESEARCH.md
@.planning/phases/07-deployment/07-PATTERNS.md
@cmd/wesh/main.go
@internal/server/server.go
@internal/server/sharetoken.go
@web/src/main.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): --base-path flag + 服务端 bp 前缀装配端到端——页面/WS/share 一条路径打穿</name>
  <reversibility rating="one-way">D-13/D-14：--base-path 是 CLI 公开契约（P2 D-15 纪律），前后端相对 URL 语义同步变更——用户已在 07-CONTEXT 锁定，记录评级不重复设卡。</reversibility>
  <files>cmd/wesh/main.go, cmd/wesh/main_test.go, internal/server/server.go, internal/server/sharetoken.go, internal/server/basepath_test.go</files>
  <read_first>
    - cmd/wesh/main.go（config struct L28-54、parseArgs flag 注册 L117-208、validateStartup L368-412、run() 分享链接打印 L492-505、Options 接线 L476）
    - internal/server/server.go（Handler() L332-392 四条注册现状、issueTicketJSON L452-466、Attach L561-602）
    - internal/server/sharetoken.go（registerShareRoutes L116-122、GOROOT 通配语义三坑注释 L106-115、sharePage 改写语义 L87-96）
    - internal/server/origin.go（NormalizeOrigin parse 期规范化+校验先例形态）
    - .planning/phases/07-deployment/07-RESEARCH.md（Pattern 2 装配配方 + Pattern 3 验证矩阵 + Pitfall 4 单侧定义防线）
    - .planning/phases/07-deployment/07-PATTERNS.md（§server.go ②、§sharetoken.go、§main.go）
  </read_first>
  <behavior>
    - 合法值 `/wesh`、`/a/b` 原样存入 cfg.basePath；`/` 归一为未配置（空串）
    - 非法值 parse 期 exit 2：`wesh`（无前导斜杠）、`/wesh/`（尾斜杠）、`//wesh` 与 `/w//esh`（重复斜杠）、`/wesh/../x`（..）、含空格/`?`/`#`/`%` 等非 path 安全字符
    - Options.BasePath="/wesh" 的测试服务端：GET /wesh → 307 且 Location 路径为 /wesh/（RawQuery 保留）；GET /wesh/ → 200 且为终端 HTML；GET / 与 GET /api/attach → 404；POST 之外方法打 /wesh/api/attach → 405 且 Allow: POST
    - 同服务端：GET /wesh/s/{token}/ 携有效 token → 200 给页；GET /wesh/s/{token}（裸）→ 307 补斜杠；POST /wesh/api/attach 携有效 token body → 200 且签发 ticket
    - 同服务端：WS 客户端对 /wesh/ws 完成子协议握手 + Hello → 收到 Welcome（dialHello 同款路径）
    - Options.BasePath=""（零值兜底）时 Handler() 注册形态与现状逐字节一致（/ 、/ws、/api/attach、/s/{token}/ 无前缀）
    - main_test.go TestParseArgs 新行：合法/非法/根斜杠三族断言（既存行零改动，命名字段扩展纪律 03-04 先例）
  </behavior>
  <action>
    实现 D-13/D-14 服务端半侧，全部改动如下：

    ① cmd/wesh/main.go：
    - config struct 加 `basePath string` 字段（Phase 7 分组注释，D-13 标注 one-way 公开契约纪律）
    - parseArgs 注册 `fs.StringVar(&cfg.basePath, "base-path", "", "serve under a sub-path (e.g. /wesh; must start with /, no trailing slash)")`
    - 新增 `normalizeBasePath(s string) (string, error)` 纯函数（isLoopbackBind 同位，NormalizeOrigin 先例形态）：空串 → 空串；"/" → 空串（D-13 根视为未配置）；不以 / 开头、以 / 结尾、含 ".."、含连续 "//"、含 [A-Za-z0-9\-._~/] 之外字符 → 返回 error，文案 `invalid --base-path %q: ...`（值非敏感可回显，exitEmptyValue.Set 同纪律）
    - Parse 返回处（write-policy 枚举校验同位插入点）调用 normalizeBasePath，结果写回 cfg.basePath，error 即返回
    - run() 的 server.New Options 接线加 `BasePath: cfg.basePath`
    - run() 分享链接拼串（main.go:502-504 两行）在 `net.JoinHostPort(...)` 与 `/s/` 之间注入 cfg.basePath：`%s://%s%s/s/%s/`（scheme, hostport, basePath, token）——cfg.basePath 为空时逐字节不变；本拼串点是打印与后续 --open 消费的单一事实源，抽出共享局部变量 `shareURLRO/shareURLRW` 供 07-05 复用（注释标注）

    ② internal/server/server.go：
    - Options 加 `BasePath string`（生产直传字段分档注释，Origins 先例；零值 = 无前缀不做任何兜底改写）
    - Server struct 加 `basePath string` 字段，New 装配直传
    - Handler() 改造（RESEARCH Pattern 2 配方逐字落地）：`bp := s.basePath`；静态子树 = bp=="" 时 `mux.Handle("/", root)` 现状不变，bp!="" 时 `mux.Handle(bp+"/", http.StripPrefix(bp, root))`——StripPrefix 仅包静态伺服链（wh 或 basicAuth(wh)），是唯一路径敏感 handler；`"POST "+bp+"/api/attach"` 与 path-only 405 fallback 两条、`bp+"/ws"` 一条均带前缀注册；registerShareRoutes 调用加 bp 参数
    - sharePage/attachHandler/issueTicketJSON 路径无关，零改动（sharePage 自改写 r.URL.Path="/"）

    ③ internal/server/sharetoken.go：
    - registerShareRoutes 签名加 `bp string` 首参（或末参，与调用点统一）：两条注册模式串改为 `"GET "+bp+"/s/{token}/"` 与 `bp+"/s/{token}/"`；GOROOT 通配语义三坑注释（L106-115）补一行：bp 形态下裸 /wesh 与裸 /wesh/s/{token} 均由 mux matchOrRedirect 307 补斜杠（GOROOT server.go:2687,2721-2745 既有登记同机制）

    ④ internal/server/basepath_test.go【新】：
    - 复用 e2e_test.go 的 startTrackedServerWith / startTestServerWith 既有 helper（同包），Options{BasePath: "/wesh", ShareTokenRO/RW: 生成值} 起实例
    - TestBasePathRoutes 表驱动：上述 behavior 全部 HTTP 断言（307 Location、200、404、405 Allow、share 页 200、ticket JSON 200）
    - TestBasePathWS：WS 拨 /wesh/ws 走既有握手 helper（dialHello 同款，子协议 + Hello）断言 Welcome 到达
    - TestBasePathEmptyUnchanged：BasePath 零值实例，GET / 200、POST /api/attach 非 404（无认证模式 404 探测语义既有断言既有测试已覆盖处不重复，此处只锁 / 与 /ws 两路由存活）
    - 提交：`feat(07-01): add --base-path flag with strict validation and mux prefix mounting`
  </action>
  <verify>
    <automated>go test ./internal/server -run 'TestBasePath' -count=1 && go test ./cmd/wesh -run 'TestParseArgs|TestStartupMatrix|TestStartupRefusalNoResource' -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/server -run 'TestBasePath' -count=1` 退出 0（307/404/405/share/WS 全绿）
    - `go test ./cmd/wesh -count=1` 退出 0（TestParseArgs 新行 + 既有行全绿）
    - `grep -c 'BasePath' internal/server/server.go` ≥ 3（Options 字段 + struct 字段 + Handler 消费）
    - `grep -c 'http.StripPrefix' internal/server/server.go` ≥ 1
    - `go vet ./...` 退出 0
  </acceptance_criteria>
  <done>
    --base-path 合法值下页面/WS/share 三链路经 Go 测试全绿；非法值 parse 期 exit 2；零值时 Handler() 注册形态与现状一致（既有 server 测试套件全绿不回归）。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 前端相对 URL 三改 + dist 重建提交</name>
  <files>web/src/main.ts, web/dist/index.html</files>
  <read_first>
    - web/src/main.ts（share 正则 L498-501、fetch L509-518、WS URL 构造 L600-603、onclose 无关本任务）
    - .planning/phases/07-deployment/07-RESEARCH.md（Pattern 3 完整配方 + 验证矩阵四行 + Anti-Patterns）
    - web/uat/phase06-dom.mjs（jsdom harness 的 fetch 包装形态——相对参数经 new URL(u, origin) 解析，断言面兼容性的判断依据）
  </read_first>
  <behavior>
    - main.ts 中 `up` 常量：页面路径匹配 /\/s\/([^/]+)\/$/（不锚 ^）时 '../../'，否则 ''
    - fetch 目标为 up + 'api/attach'（相对构造，share 页深两段经升级前缀回到站根）
    - WS URL = new URL(up + 'ws', location.href) 后显式换 protocol 为 wss:/ws:（URL 构造器继承 http(s) scheme，WebSocket 只收 ws/wss）
    - RESEARCH Pattern 3 验证矩阵四行成立：/ → /api/attach+/ws；/wesh/ → /wesh/api/attach+/wesh/ws；/s/{t}/ → /api/attach+/ws；/wesh/s/{t}/ → /wesh/api/attach+/wesh/ws
    - 三个 jsdom UAT 套件（phase04-dom/phase05-dom/phase06-dom）在新 dist 下全绿（harness 的 window.fetch 包装对相对参数做 new URL 解析，预期零适配；若有断言检查原始参数字面，按 05-09 先例适配且断言面守恒）
  </behavior>
  <action>
    实现 D-14 前端半侧（RESEARCH Pattern 3 配方逐字落地，共三改）：

    ① share 正则（main.ts:500 现状）：去掉 ^ 锚——`location.pathname.match(/\/s\/([^/]+)\/$/)`；上方注释（L498-499）改写为：不锚 ^ 使 base-path 挂载（/wesh/s/{token}/）同样命中，正则兼作挂载点检测；前端仍不解析 token 种类（Welcome.mode 唯一来源不变）。
    ② 新增升级前缀常量（shareToken 赋值行同位）：`const up = shareMatch ? '../../' : '';`——share 页深两段，站根需上两级；根挂载为空串。
    ③ fetch（main.ts:509-510 现状 '/api'+'/attach' 前导斜杠字面）：改为 `fetch(up + 'api/attach', ...)`——请求体/headers 分支逻辑零改动。
    ④ WS URL（main.ts:601 现状 protocol+host+ 前导斜杠 ws 路径拼串）：改为 `const wsUrl = new URL(up + 'ws', location.href); wsUrl.protocol = location.protocol === 'https:' ? 'wss:' : 'ws:';` 然后 `ws = new WebSocket(wsUrl, [SUBPROTOCOL]);`——显式换 protocol 是必做步（RESEARCH Anti-Pattern 3）。

    dist 重建提交：`time pnpm -C web build`，产物 web/dist/index.html 入库（.gz 不入库为 .gitignore 既定策略）；随后全量跑三个 jsdom 套件确认零回归。
    提交分两步：`feat(07-01): relative fetch/WS URLs with share-page up-level prefix`（src）+ `chore(07-01): rebuild dist`（产物）。
  </action>
  <verify>
    <automated>time pnpm -C web build && node web/uat/phase04-dom.mjs && node web/uat/phase05-dom.mjs && node web/uat/phase06-dom.mjs && grep -c "'\.\./\.\./'" web/dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `time pnpm -C web build` 退出 0（tsc && vite build 全绿）
    - `grep -c "'\.\./\.\./'" web/dist/index.html` ≥ 1（升级前缀字面进产物——字符串字面量不被 esbuild 重命名，05-08 结构指纹先例）
    - `grep -c 'new URL(' web/src/main.ts` ≥ 1（WS URL 构造改造落位）
    - `node web/uat/phase04-dom.mjs`、`node web/uat/phase05-dom.mjs`、`node web/uat/phase06-dom.mjs` 各自退出 0
    - `node web/uat/phase02.mjs`、`node web/uat/phase03.mjs`、`node web/uat/phase04.mjs`、`node web/uat/phase05.mjs`、`node web/uat/phase05-dims.mjs`、`node web/uat/phase06.mjs` 各自退出 0（协议层套件对 dist 无依赖但要求服务端行为零漂移——本 plan 服务端改动只加前缀分支，必须全绿）
  </acceptance_criteria>
  <done>
    前端三改落位且 dist 入库；四类挂载点的相对解析矩阵（RESEARCH Pattern 3）成立；六个协议层 + 三个 jsdom 套件全绿——base-path 特性端到端可用，未配置时行为零漂移。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reverse-proxy → wesh (HTTP path) | 反代把外部请求的 path 前缀透传进 wesh 路由层——base-path 值是 operator 配置，请求 path 是不可信输入 |
| filesystem (go:embed dist) | StripPrefix 后的路径只进 go:embed 静态 FS，不触 OS 文件系统 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01a | Tampering | --base-path 值校验 | medium | mitigate | D-13 严格字符集/尾斜杠/.. 拒绝，parse 期 exit 2；宽容自动修正被否决（配置漂移隐蔽源） |
| T-07-01b | Information Disclosure | StripPrefix 静态伺服 | medium | mitigate | 前缀不匹配的请求由 mux 保证不可达（StripPrefix 404 分支结构性不可达，RESEARCH Pattern 2）；目标仅为 go:embed FS，无 OS 路径逃逸面 |
| T-07-01c | Tampering | 裸路径 307 重定向 Location | low | accept | token 出现在 Location 头属 P5 D-03 已接受暴露面（sharetoken.go:113 既有登记）；307 保方法是 PITFALLS 表正式规避 |
| T-07-01d | Spoofing | Origin 校验 × base-path | low | accept | Origin 头无 path 分量（origin.go:64-82 只看 scheme://host），base-path 不影响 Origin 判定（P3 D-12 张力已闭合，D-14 明示零影响） |
</threat_model>

<verification>
- `go test ./internal/server ./cmd/wesh -count=1` 全绿
- `time pnpm -C web build` 退出 0
- 九个既有 UAT 脚本（phase02/03/04/05/05-dims/06 协议层 + 04-dom/05-dom/06-dom jsdom）全绿
- RESEARCH Pattern 3 验证矩阵四行在 Go 测试与 dist 产物两个层面均有断言
</verification>

<success_criteria>
- `wesh --base-path /wesh --writable -- bash` 下：curl -i http://127.0.0.1:PORT/wesh 返回 307 且 Location 以 /wesh/ 结尾；curl http://127.0.0.1:PORT/wesh/ 返回 200 HTML；WS 客户端对 ws://127.0.0.1:PORT/wesh/ws 完成握手收到 Welcome
- 启动打印 `share read-only:  http://<host>:<port>/wesh/s/<token>/` 形态
- 未配置 base-path 时全部既有测试与 UAT 零回归
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **OPS-02 (unclassified, unresolved)**：边缘探针未能归类该需求——人工复核项：base-path 与 share token 通道/Origin 校验/无认证模式探测直连（404 语义）三处交叉行为已由本 plan 测试矩阵显式覆盖；未覆盖项（如有）在 07-08 人工 UAT 清单复核。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `normalizeBasePath(s string) (string, error)`（cmd/wesh/main.go，新函数）
- config 字段 `basePath string` + flag `--base-path`（cmd/wesh/main.go）
- `Options.BasePath string` + Server 字段 `basePath`（internal/server/server.go）
- `registerShareRoutes(mux, bp, page, root)` 新签名（internal/server/sharetoken.go）
- `TestBasePathRoutes` / `TestBasePathWS` / `TestBasePathEmptyUnchanged`（internal/server/basepath_test.go，新文件）
- main.ts：`up` 常量 + 相对 fetch/WS 构造（web/src/main.ts）
- main.go 局部变量 `shareURLRO`/`shareURLRW`（分享链接拼串单一事实源，07-05 --open 消费）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-deployment/07-01-SUMMARY.md` when done
</output>
