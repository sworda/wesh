---
phase: 07-deployment
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
  - web/uat/phase07-b6.sh
autonomous: true
gap_closure: true
gap_ids:
  - G-07-3
  - G-07-8
requirements:
  - OPS-01
  - OPS-11

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "两个 wesh 实例以同一 --socket 路径启动时，后者 exit 1 且 stderr 含 address already in use（07-02 OPS-01 设计答案原文）；存活实例不被孤儿化——socket 文件仍在、原 listener 仍 accept"
    - "SIGKILL 后残留 socket 仍被自动清理、启动成功（D-10 语义不回归——残留与存活在文件类型上不可区分，以活性探测区分：net.Dial unix ECONNREFUSED=残留→清理；连通=存活→拒绝）"
    - "xdg-open/open 返回非零（桌面异常）时 stderr 出现 wesh: warning: --open: 警告行、服务不阻断（07-05 D-27「只警告不阻断」字面达成）；opener 正常退出（0）无新增输出"
    - "opener 子进程经 Wait 收割——fire-and-forget 不再驻留僵尸进程（G-07-8 附带闭合）"
    - "phase07.mjs 协议层全场景零漂移（S2 unix socket / S8 --open 直触改动面）；b1b5.sh 7/7、b6.sh 7/7 二进制直证"
  artifacts:
    - path: "cmd/wesh/main.go"
      provides: "listenSocket 活性探测（Dial 成功即拒 EADDRINUSE 同形态文案）+ openBrowser goroutine Wait 非零警告行"
      contains: "browser launcher exited with error"
    - path: "cmd/wesh/main_test.go"
      provides: "TestListenSocket 存活竞争子测 + TestOpenBrowser 非零警告子测"
      contains: "live instance refused"
    - path: "web/uat/phase07-b6.sh"
      provides: "B6f 警告行断言轮询化（消除 listening 行与异步警告行到达竞态）"
      contains: "warn"
  key_links:
    - from: "cmd/wesh/main.go listenSocket Lstat 类型闸"
      to: "net.Dial(\"unix\", path) 活性探测"
      via: "CR-01 按类型收窄之后 G-07-3 按活性再分——类型不可区分时以能否建连区分存活/残留；探测-清理 TOCTOU 窗口两向安全降级（探后实例死亡→下次启动清理；清后对手抢绑→net.Listen 真 EADDRINUSE 兜底）"
      pattern: "net.Dial(\"unix\""
    - from: "cmd/wesh/main.go openBrowser Start 成功分支"
      to: "goroutine cmd.Wait() → stderr 警告行"
      via: "D-27 只警告不阻断延伸覆盖运行期非零退出；警告行自含 wesh: warning: 前缀、不含 URL（share token 红线——Wait err 仅 exit status N）"
      pattern: "cmd.Wait()"
  prohibitions:
    - statement: "MUST NOT 让分享 URL（含 token）进入任何 stderr 行——token 永不入日志红线（P5 D-03）覆盖新警告行；测试运行时断言 URL 占位串零命中"
      status: resolved
      verification: judgment
    - statement: "MUST NOT 改变 headless 跳过行、Start 失败警告行、listening/分享链接打印的任何字面——07-05 锁定文案与 phase07.mjs S8a/S8b 断言面零漂移"
      status: resolved
      verification: judgment
---

<objective>
闭合 G-07-3（major，OPS-01）与 G-07-8（minor，OPS-11）——main.go 两处 UAT 实证运行时偏差：

1. listenSocket 在 Lstat 类型闸后无条件 os.Remove——存活实例的 socket 与残留 socket 同被删，第二实例 unlink 存活端点后 listen 成功（静默赢者，07-02 设计答案 EADDRINUSE exit 1 未达成）。修复：Remove 前活性探测（net.Dial unix：ECONNREFUSED=残留→清理；连通=存活→拒绝 exit 1）+ 存活竞争单测。
2. openBrowser fire-and-forget .Start()——opener 非零退出静默（D-27「返回非零只警告」字面未达成）且子进程从不 Wait 驻留僵尸。修复（选项 A 裁决，理据见 Task 2 action）：goroutine Wait() + 非零退出补 stderr 警告行 + 非零警告单测。

Purpose: 两 gap 同文件同关注面（main.go 运行时行为与 UAT 二进制直证链），合并执行零文件冲突；修复使两个 UAT 实证背离点回归设计答案。
Output: 活性探测与警告行落地，单测锁定，b1b5.sh 7/7 与 b6.sh 7/7 二进制直证闭合，协议层零漂移。
执行面前提：全部 go/UAT 命令在 Linux 侧执行（Windows 工作站无 Go 工具链——CODEBUDDY.md 双机拓扑既定）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deployment/07-UAT.md
@.planning/phases/07-deployment/07-CONTEXT.md
@cmd/wesh/main.go
@cmd/wesh/main_test.go
@web/uat/phase07-b1b5.sh
@web/uat/phase07-b6.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: G-07-3 listenSocket 活性探测——存活实例拒绝 EADDRINUSE exit 1，残留照旧清理</name>
  <reversibility rating="reversible">内部行为修复（非 CLI 契约）——EADDRINUSE exit 1 本就是 07-02 flagged_assumptions 登记的 OPS-01 设计答案，本任务使实现回归设计；恢复 = revert 提交。</reversibility>
  <files>cmd/wesh/main.go, cmd/wesh/main_test.go</files>
  <read_first>
    - cmd/wesh/main.go（listenSocket L1010-1041 函数体与顺序敏感注释；run() listen 失败通道 L1092 区段——sess.Close + wesh: %v + return 1 既有形态）
    - cmd/wesh/main_test.go（TestListenSocket L896-1027 五子测——stale 子测 SetUnlinkOnClose(false) 造残留形态 L897-920 是新子测的直接参照）
    - .planning/phases/07-deployment/07-UAT.md（G-07-3 条目 root_cause/missing + B1 实测段——静默赢者证据与修复方向以此为准）
    - .planning/phases/07-deployment/07-02-PLAN.md（flagged_assumptions OPS-01 设计答案原文：后者 bind EADDRINUSE exit 1，无静默赢者之外的保证）
  </read_first>
  <behavior>
    - 存活实例占用 socket 路径：listenSocket 返回 error，文案逐字形态 `listen unix <path>: bind: address already in use`（与 net.Listen EADDRINUSE 错误文案全等）；run() 既有 listen 失败通道落地 `wesh: ...` exit 1；存活 socket 文件不被删除、原 listener 仍 accept（拒绝后 net.Dial 仍连通）
    - 残留 socket（无进程监听，Dial 收 ECONNREFUSED）：照旧 Remove → Listen 成功（既有 stale 子测语义不变）
    - 路径不存在（Lstat 错误）：跳过探测直落 net.Listen（现状逐字不变）
    - 非 socket 类型文件：CR-01 拒绝逐字不变
    - Dial 其他失败形态（EACCES 等）：按「不可服务即残留」落 Remove——注释登记边缘理据（跨用户活体误删由目录写权限/sticky 位结构性抑制；D-10 systemd Restart= 零人工干预优先；flagged_assumptions 登记）
    - TOCTOU 注释：探测后实例死亡 → 拒绝退出、下次启动清理（安全方向）；清理后对手抢绑 → net.Listen 真 EADDRINUSE 兜底（安全方向）
  </behavior>
  <action>
    ① main.go listenSocket：在 Lstat 类型闸（ModeSocket 判定）与 `_ = os.Remove(path)` 之间插入活性探测——`conn, derr := net.Dial("unix", path)`；`derr == nil`（连通=存活）→ `_ = conn.Close()` 并 `return nil, fmt.Errorf("listen unix %s: bind: address already in use", path)`（文案钉死，与 net.Listen EADDRINUSE 形态全等；零新 import——fmt/net 既有）；derr != nil → 落到既有 Remove 行（Dial 失败全形态按残留处理，behavior 末两条注释落位）。函数头注释更新：D-10 收窄链补一环——CR-01 按类型（非 socket 拒绝）、G-07-3 按活性（存活 socket 拒绝）；「存活实例被孤儿化」正是本 gap 修复对象，注释明说。
    ② main_test.go TestListenSocket 追加子测 `live instance refused EADDRINUSE and unharmed`：t.TempDir 路径上 net.Listen("unix") 预建真实存活 listener（保持默认 unlink 行为，defer Close 清理）→ listenSocket(path, 0o660, -1, -1) → 断言 err != nil 且 err.Error() 含 "address already in use"；os.Stat 断言 socket 文件仍存在（未被删除）；net.Dial("unix", path) 断言仍连通（原 listener 未被夺走——存活方零损伤）。
    ③ 既有五子测零改动——stale 子测（SetUnlinkOnClose(false)+Close 造残留）现经探测 ECONNREFUSED 分支通过，语义不变。
    提交：`fix(07-10): listenSocket liveness probe — refuse live socket EADDRINUSE instead of silent takeover (G-07-3)`
  </action>
  <verify>
    <automated>go test ./cmd/wesh -run 'TestListenSocket' -count=1 -v && go test ./cmd/wesh -count=1 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/wesh -run 'TestListenSocket' -count=1 -v` 退出 0，六子测全 PASS（五旧 + live 新子测）
    - `grep -c 'net.Dial("unix"' cmd/wesh/main.go` 输出 1（探测点唯一）
    - `grep -cF 'bind: address already in use' cmd/wesh/main.go` 输出 1（拒绝文案钉死一处）
    - `go test ./cmd/wesh -count=1` 全绿（无邻域回归）
  </acceptance_criteria>
  <done>
    OPS-01 设计答案达成：存活实例同路径第二实例收 EADDRINUSE 形态错误 exit 1（经 run() 既有失败通道），静默赢者结构性消除；残留清理（D-10）与非 socket 拒绝（CR-01）语义零漂移。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: G-07-8 openBrowser goroutine Wait + 非零退出警告行（选项 A 裁决）</name>
  <files>cmd/wesh/main.go, cmd/wesh/main_test.go</files>
  <read_first>
    - cmd/wesh/main.go（openBrowser L1224-1247 函数体与头注释；调用点 L1187-1198——`go openBrowser(url)` 已是 goroutine）
    - cmd/wesh/main_test.go（TestOpenBrowser L1028 起两子测——fake xdg-open PATH 前置夹具与 2s 轮询同步是新子测直接参照）
    - .planning/phases/07-deployment/07-CONTEXT.md（D-26/D-27 锁定原文）
    - .planning/phases/07-deployment/07-UAT.md（G-07-8 条目——二选一修复方向与 root_cause）
  </read_first>
  <behavior>
    - 裁决（选项 A：goroutine Wait() + 非零退出补 stderr 警告行）。理据四点：
      a. D-27 意图从实现侧闭合——07-05 flagged_assumption 明示「xdg-open 存在但返回非零（桌面异常）只警告不阻断」；选项 B 把该锁定表述降级为「运行期非零退出静默」是让既定承诺向现状实现收窄（scope reduction），且 07-CONTEXT/UAT 探针/plan 假设整条文档链是锁定产物，改文档链远比 ~6 行代码变更重
      b. D-27 纪律内核一致性——headless 跳过尚且给 stderr 提示行（可见性），桌面异常非零退出反而静默自相矛盾；「不阻断」是不变量（两选项同），「须可见」才是分差
      c. Wait() 附带闭合僵尸驻留——fire-and-forget .Start() 从不收割，xdg-open 退出后成 wesh 僵尸子进程驻留至服务终结（每次 --open 一个）
      d. operator 可观测性——带 --open 启动但浏览器未弹出时静默失败零诊断线索；警告行使「无可用 handler」等桌面异常可见，正是 B6 探针问题要的保证
    - Start 失败（binary 缺失/exec 错误）：既有警告行 `wesh: --open: failed to launch browser: %v` 逐字不动
    - Start 成功后非零退出：stderr 补一行，文案逐字钉死 `wesh: warning: --open: browser launcher exited with error: %v`（Wait err = exit status N；自含 wesh: warning: 前缀遵 03-04 启动警告纪律；不含 URL——share token 红线）；goroutine 异步执行，不阻断、零退出码影响、启动打印序列不变
    - opener 正常退出（0）：无新增输出
    - headless 分支与调用点 `go openBrowser(url)` 逐字不动；子进程必被 Wait 收割（Start 成功路径）
  </behavior>
  <action>
    ① main.go openBrowser：`exec.Command(tool, url)` 提为 cmd 句柄；Start 错误分支现状保留；Start 成功后起 goroutine `go func() { if err := cmd.Wait(); err != nil { fmt.Fprintf(os.Stderr, "wesh: warning: --open: browser launcher exited with error: %v\n", err) } }()`。函数头注释更新：Pattern 8 配方偏差登记（G-07-8 选项 A——Start 后 goroutine Wait 收割防僵尸 + 非零退出补警告；D-27「只警告不阻断」覆盖运行期非零退出）；T-07-05b argv 不经 shell 不变量保持。
    ② main_test.go TestOpenBrowser 追加子测 `non-zero opener warns without blocking`：fake xdg-open 脚本内容 `#!/bin/sh\nexit 1`（PATH 前置 + DISPLAY=:99 夹具同既有子测）；异步警告捕获形态——os.Pipe 换管 os.Stderr + 读取 goroutine 经 sync.Mutex 保护写入 bytes.Buffer + 2s 轮询 Deadline 读 buffer（同锁）；观察到警告行后先 close(w) 再 restore os.Stderr 并经 channel 等读取 goroutine 退出（05-01 happens-before 纪律：restore 必在观测之后，读写经 mutex 同步，-race 干净）。断言：buffer 含 `wesh: warning: --open: browser launcher exited with error`；且不含传入的 URL 占位串（token 红线运行时断言——URL 形态同既有子测的占位 host:port 串）。
    ③ 既有两子测（headless 跳过 / fake xdg-open argv 全等）零改动——exit 0 路径无新输出由实现结构保证（Wait 返回 nil 不打印），不为「无警告」设轮询断言（等待不可得事件必 flake）。
    ④ `go test -race ./cmd/wesh -run TestOpenBrowser -count=1 -v` 专项竞态验证（异步 goroutine 面）。
    提交：`fix(07-10): openBrowser reap opener via goroutine Wait + warn on non-zero exit (G-07-8, option A)`
  </action>
  <verify>
    <automated>go test -race ./cmd/wesh -run 'TestOpenBrowser' -count=1 -v && go test ./cmd/wesh -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `go test -race ./cmd/wesh -run 'TestOpenBrowser' -count=1 -v` 退出 0，三子测全 PASS（两旧 + 非零警告新子测）
    - `grep -c 'cmd.Wait()' cmd/wesh/main.go` 输出 1（收割点唯一）
    - `grep -cF 'wesh: warning: --open: browser launcher exited with error' cmd/wesh/main.go` 输出 1（警告行钉死一处）
    - `grep -cF 'wesh: --open: failed to launch browser' cmd/wesh/main.go` 输出 1（既有 Start 失败警告行未动）
    - `go test ./cmd/wesh -count=1` 全绿（无邻域回归）
  </acceptance_criteria>
  <done>
    D-27「只警告不阻断」字面覆盖运行期非零退出：警告行自含 warning 前缀（b6.sh B6f grep -qi warn 有稳定触发源）、不含 URL、服务不阻断；opener 子进程经 Wait 收割零僵尸；headless/Start 失败两既有路径逐字未动。
  </done>
</task>

<task type="auto">
  <name>Task 3: 二进制级直证回归（b1b5 7/7 + b6 7/7 + 协议套件零漂移）与六段式核心段</name>
  <precondition>Linux 开发机 ssh 可达且工作区已同步至本 plan HEAD（全部 go/UAT 命令在 Linux 侧执行——CODEBUDDY.md 双机拓扑既定执行面；/tmp/wesh-uat 可写）。</precondition>
  <files>web/uat/phase07-b6.sh（仅 B6f 轮询化一处；其余文件仅验证不修改）</files>
  <read_first>
    - web/uat/phase07-b1b5.sh（B1a 断言形态 L22-35——exit1-eaddrinuse 为设计答案分支，本 plan 使其转 PASS）
    - web/uat/phase07-b6.sh（B6e/B6f/B6g L49-61——B6f 即时 grep 与异步警告行存在到达竞态）
    - .planning/phases/07-deployment/07-08-PLAN.md（六段式各段形态 L133-146——GOROOT gofmt/vet/-race/web build/裸 clone/冒烟先例）
  </read_first>
  <behavior>
    - b6.sh B6f 即时 `grep -qi "warn"` 改为脚本既定 50×0.1s 轮询形态（`for i in $(seq 1 50); do grep -qi "warn" "$D/b.log" && break; sleep 0.1; done` 后取值断言）——消除 listening 行出现与 goroutine 警告行落盘之间的到达竞态；断言面（B6f 语义）不变，B6e/B6g 不动
    - 段 1：GOROOT gofmt -l cmd/wesh/main.go cmd/wesh/main_test.go 零漂移（/usr/bin/gofmt 陈旧须用 GOROOT 版本——01-03 登记；漂移则逐行核读纯排版后 -w + 独立 style 提交，02-06/03-06/05-09 先例）
    - 段 2：`go vet ./...` 退出 0
    - 段 3：`go test -race -count=1 ./...` 全绿
    - 段 4：web build 跳过并声明（本 plan 零前端改动，dist 不动）
    - 段 5 二进制直证：`time go build -o /tmp/wesh-uat/wesh ./cmd/wesh`（time 前缀纪律 + pw/README 前置形态）→ `bash web/uat/phase07-b1b5.sh` 输出 7 PASS, 0 FAIL（每次运行恰 7 项断言：B1a 三分支互斥执行其一 + B1c/B1d + B5a-d 四项；B1a 由 silent-winner 转 exit1-eaddrinuse 直证 G-07-3 闭合；B1c/B1d 残留清理链不回归；B5 四项顺带）→ `bash web/uat/phase07-b6.sh` 输出 7 PASS, 0 FAIL（B6f warn 行直证 G-07-8 闭合；B6e/B6g 不阻断不回归）
    - 段 6 协议套件回归：`node web/uat/phase07.mjs` 全场景绿（S2 unix socket 全链 / S8 --open 两子场景直触改动面）；phase02/03/04/05/06 五协议脚本 + phase04-dom/05-dom/06-dom 三 jsdom 套件顺带执行防非预期漂移（脚本清单 = 07-08 十脚本回归同款）
  </behavior>
  <action>
    ① b6.sh 轮询化按 behavior 首条落地（仅此一处；断言计数不变）。提交：`test(07-10): poll for async opener warning in b6 B6f (deflake)`
    ② 按 behavior 六段逐段执行，各段输出记录于 SUMMARY；段 5 两脚本输出全文留档（B1a/B6f 转行是 gap 闭合直证）。
    ③ 任何一段失败即定位：属本 plan 预期适配（如 gofmt 清零）按先例独立提交；非预期失败回 Task 1/2 修正，不得改断言迁就。
  </action>
  <verify>
    <automated>go vet ./... && go test -race -count=1 ./... && time go build -o /tmp/wesh-uat/wesh ./cmd/wesh && bash web/uat/phase07-b1b5.sh && bash web/uat/phase07-b6.sh && node web/uat/phase07.mjs</automated>
  </verify>
  <acceptance_criteria>
    - GOROOT gofmt -l 两触碰 Go 文件输出为空；`go vet ./...` 退出 0；`go test -race -count=1 ./...` 全绿
    - `bash web/uat/phase07-b1b5.sh` 退出 0 且输出 `结果: 7 PASS, 0 FAIL`（B1a 行 = exit1-eaddrinuse 分支 PASS）
    - `bash web/uat/phase07-b6.sh` 退出 0 且输出 `结果: 7 PASS, 0 FAIL`（B6f 行 PASS）
    - `node web/uat/phase07.mjs` 退出 0（S2/S8 直触面零漂移）；五协议脚本 + 三 jsdom 套件全绿
    - `git status --short` 除本 plan 三改动文件外干净
  </acceptance_criteria>
  <done>
    两 gap 二进制直证闭合：B1a 存活竞争 exit 1 EADDRINUSE、B6f 非零警告行；残留清理/不阻断/headless 等邻域语义全绿；六段式核心段通过，协议层零非预期漂移。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 并发 wesh 实例 → 同一 socket 路径 | 同机第二实例（operator 手误/systemd 单元竞态/双重启动）可能夺走存活实例端点 |
| wesh → opener 子进程（xdg-open/open） | 桌面态外部进程，退出码是异常信号；URL 含 share token 流经 argv |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-10a | Tampering | listenSocket 活性探测 | high | mitigate | Dial 连通即拒（EADDRINUSE 同形态文案 exit 1），存活端点零触碰；TOCTOU 窗口两向安全降级（Listen 真 EADDRINUSE 兜底 / 重试清理）；存活竞争子测锁定 |
| T-07-10b | Denial of Service | 残留清理回归 | medium | mitigate | ECONNREFUSED 残留照旧 Remove（D-10 systemd Restart= 零人工干预）；既有 stale 子测 + b1b5.sh B1c/B1d 二进制锁 |
| T-07-10c | Information Disclosure | --open 警告行 | high | mitigate | 警告行不含 URL（Wait err 仅 exit status N 结构性无 argv）；测试运行时断言 URL 占位串零命中（P5 D-03 token 红线） |
| T-07-10d | Denial of Service | opener 僵尸驻留 | low | mitigate | 本 gap 附带闭合——goroutine Wait() 收割（Start 成功路径必达） |
</threat_model>

<verification>
- 单测：TestListenSocket 六子测（存活竞争新子测含「文件仍在 + 仍连通」零损伤断言）+ TestOpenBrowser 三子测（非零警告含 token 红线反断言），-race 专项
- 二进制直证：b1b5.sh 7/7（B1a 转 exit1-eaddrinuse）、b6.sh 7/7（B6f warn）
- 协议层：phase07.mjs 全场景（S2/S8 直触面）+ 五协议脚本 + 三 jsdom 零漂移
- 六段式核心：GOROOT gofmt 零漂移 / vet 0 / -race 全量绿（web build 无改动声明跳过）
</verification>

<success_criteria>
- 两个 wesh 实例同 --socket 路径：后者 exit 1 + `wesh: listen unix <path>: bind: address already in use`，前者全程无感（socket 文件在、仍 accept）
- kill -9 后残留 socket 重启零人工干预（D-10 保持）
- stub opener exit 1 时 stderr 见 `wesh: warning: --open: browser launcher exited with error: exit status 1`、GET / 仍 200；exit 0 时零新增输出
- 全量 Go 测试（-race）与 phase07 协议 UAT 零回归
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **Dial 非 ECONNREFUSED 失败形态（EACCES 等）按残留清理**：探针「Dial 失败的全部错误形态如何分类」——设计答案「不可服务即残留」：EACCES 意味本进程无法向该端点提供服务，且跨用户活体误删由目录写权限/sticky 位结构性抑制（/run/wesh root 拥有、/tmp sticky）；若一刀切拒绝会重新打破 D-10 systemd Restart= 零人工干预（残留 0000 权限 socket 场景）。该假设已显式记录，07-UAT 复核可选项。
- **TOCTOU 窗口风险接受**：探测与 Remove/Listen 非原子——探后实例死亡 → 本次拒绝、重试即清理（安全方向）；清后对手抢绑 → net.Listen 真 EADDRINUSE 兜底（安全方向）。两向均不产生静默赢者，风险接受。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- listenSocket 活性探测块（cmd/wesh/main.go——Dial 成功拒绝 / 失败落 Remove，含 D-10 收窄链注释更新）
- openBrowser goroutine Wait + 非零警告行（cmd/wesh/main.go，含 Pattern 8 偏差登记注释）
- TestListenSocket 子测 `live instance refused EADDRINUSE and unharmed`（cmd/wesh/main_test.go）
- TestOpenBrowser 子测 `non-zero opener warns without blocking`（cmd/wesh/main_test.go）
- b6.sh B6f 轮询化（web/uat/phase07-b6.sh）
- 回归证据：b1b5 7/7 + b6 7/7 + phase07.mjs 全绿输出（入 07-10-SUMMARY.md）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-deployment/07-10-SUMMARY.md` when done
</output>
