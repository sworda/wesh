---
phase: 07-deployment
plan: 07
type: execute
wave: 7
depends_on:
  - "07-06"
files_modified:
  - web/uat/phase07.mjs
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-04
  - OPS-05
  - OPS-09
  - OPS-11
  - SEC-07

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "phase07.mjs 覆盖全场景：配置文件合并与优先级 / unix socket 全链（经 TCP relay）/ base-path 页面+WS 升级+share 交叉 / auth-header 记录与 sanitize / XFF 换键 / stop-signal 宽限补 KILL / 降权 self / 1001 关停序列 / --open headless 跳过与 fake xdg-open argv"
    - "每个场景 spawn 真实 wesh 二进制断言（协议层先例形态），全部 PASS 退出 0"
    - "assertOutputClean 运行时自净：全部已发 detail 零凭据/token/分享链接串（含场景异常消息通道）"
    - "既有九个 UAT 脚本（phase02/03/04/05/05-dims/06 协议层 + 04-dom/05-dom/06-dom jsdom）在新代码下全绿零回归"
  artifacts:
    - path: "web/uat/phase07.mjs"
      provides: "phase 7 协议层 UAT 全场景 + TCP↔unix relay 夹具 + assertOutputClean 红线"
      min_lines: 400
  key_links:
    - from: "web/uat/phase07.mjs unix socket 场景"
      to: "wesh --socket 实例"
      via: "Node 原生 WebSocket 不能直连 unix socket（本机探针实证）——15 行 TCP relay 管道转发复用全部既有 WS 断言机制"
      pattern: "net\\.createServer"
  prohibitions:
    - statement: "MUST NOT emit credential/token/分享链接串 in any check detail or error channel——UAT 输出自净红线（assertOutputClean 运行时自证，phase06.mjs 先例逐字形态）"
      status: resolved
      verification: judgment
---

<objective>
落地 phase 7 协议层 UAT（07-VALIDATION Wave 0 首项）：web/uat/phase07.mjs 零依赖 Node 原生 WS/fetch 脚本，覆盖 OPS-01/02/04/05/09/11、SEC-07、D-23 的真实二进制全链断言。

Purpose: 部署特性（socket/base-path/config/信号）的回归只能靠真实进程行为锁定；unix socket 场景经 TCP relay 夹具复用全部既有 WS 断言机制（RESEARCH 探针实证）。
Output: phase07.mjs 全场景 PASS + 既有九脚本零回归。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deployment/07-CONTEXT.md
@.planning/phases/07-deployment/07-RESEARCH.md
@.planning/phases/07-deployment/07-PATTERNS.md
@.planning/phases/07-deployment/07-VALIDATION.md
@web/uat/phase06.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: phase07.mjs harness + S1 配置合并 / S2 unix socket（relay）/ S3 base-path 交叉 / S4 auth-header+XFF</name>
  <files>web/uat/phase07.mjs</files>
  <read_first>
    - web/uat/phase06.mjs（全文件——harness 五件逐结构复制：check/skip/startWesh(redactArgs+sensitiveTokens)/dialHello/waitExit/collectUntilClose/rawUpgrade + assertOutputClean + 收尾纪律 L483-509）
    - .planning/phases/07-deployment/07-PATTERNS.md（§phase07.mjs：文件头纪律 + 新增 relay 夹具 + 场景矩阵）
    - .planning/phases/07-deployment/07-RESEARCH.md（Code Examples：UAT TCP↔unix relay 探针实证形态）
  </read_first>
  <behavior>
    - 文件头：覆盖范围段 + 红线段（token/凭据只作断言材料永不进 check detail）+ 运行方式行（phase06.mjs L1-19 同款）
    - S1 配置：t 临时目录落 TOML（port=0/bind=127.0.0.1/credential 两组/max-clients=1/command=["bash","--norc","--noprofile"]）→ spawn --config → 无凭据 GET / 收 401（配置凭据生效）；凭据 attach 后第二客户端 /api/attach 收 503（配置 max-clients=1 生效）；CLI 覆盖场景：同配置 + --max-clients 2 → 两客户端并存；未知键 TOML → exit 2 且 stderr 不含凭据探针值；不存在文件 → exit 2；credential + chmod 644 → stderr 权限警告行且不含值
    - S2 unix socket：spawn --socket $TMP/wesh.sock → 启动行含 unix://；socket 文件存在且权限 0660（fs.stat mode & 0o777）；relay（net.createServer pipe 到 unix 连接，listen 0 取随机端口）→ dialHello 经 relay → Welcome → typeText echo 全链；预建垃圾文件于 socket 路径 → spawn 成功（D-10 残留清理）；stdout 无 http:// 分享链接行（退化单行存在）
    - S3 base-path：spawn --base-path /wesh --writable → GET /wesh（裸）→ 307 Location 含 /wesh/；GET /wesh/ → 200 HTML；GET / → 404；WS /wesh/ws → Welcome；启动打印 share 行含 /wesh/s/；GET /wesh/s/{token}/ → 200；POST /wesh/api/attach 携 token → ticket；携 ticket Hello 经 /wesh/ws → Welcome（share×base-path 交叉全链）
    - S4 auth-header/XFF：spawn --auth-header X-Remote-User（loopback）→ 携 X-Remote-User: alice + X-Forwarded-For: 198.51.100.7 完成 attach+close → stderr 事件行含 remote=198.51.100.7 与 remote_user=alice；头值嵌控制字符（alice\r\nFAKE）→ 日志行无 FAKE 串且 alice 保留（sanitize）；对照组无 --auth-header → 同样请求日志行为现状（remote=127.0.0.1 系，无 remote_user 键）
  </behavior>
  <action>
    新建 web/uat/phase07.mjs：
    ① harness 逐结构复制 phase06.mjs（check/skip/startWesh/dialHello/waitExit/collectUntilClose/rawUpgrade/emittedDetails/sensitiveTokens/redactArgs——redactArgs 的脱敏清单补 --credential 之外的配置文件路径？不需要：--config 路径非敏感；凭据只经 TOML 文件内容出现，文件路径不含值）；新增 import net from 'node:net' 与 relay 夹具（RESEARCH Code Examples 探针形态：`net.createServer(c => { const u = net.createConnection(sockPath); c.pipe(u).pipe(c); })` + listen(0,'127.0.0.1') 取端口；场景结束 relay.close() 纪律）。
    ② S1..S4 场景按 behavior 落地（每场景独立 spawn 实例——单次语义独立 spawn 先例；S1 的 503 断言注意 max-clients 计数口径 = attach 成功后计数，dialHello 完成才算占用）；stderr 捕获断言用 startWesh 既有 stderr 收集闭包扩展（S4 需要读 stderr 事件行——phase03.mjs 的 stderr 断言先例参照）。
    ③ 场景数组循环 + 异常消息纳入 emittedDetails（WR-02 先例）+ assertOutputClean 段（S1 凭据值探针串入 sensitiveTokens 同口径数组——TOML 凭据值与 share token 均不得出现在任何 detail）。
    调试纪律：单场景失败先单独复跑（node web/uat/phase07.mjs 支持环境变量场景过滤则加，否则注释临时收窄——最终提交必须全场景开启）。
    提交：`test(07-07): phase07.mjs harness and S1-S4 scenarios`
  </action>
  <verify>
    <automated>node web/uat/phase07.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node web/uat/phase07.mjs` 退出 0，S1..S4 全 PASS（输出逐场景 PASS 行）
    - assertOutputClean PASS（details 计数 > 0 且零命中）
    - `grep -c 'node:net' web/uat/phase07.mjs` ≥ 1（relay 夹具落位）
    - `grep -c "remote_user=" web/uat/phase07.mjs` ≥ 1（S4 断言材料——断言字面可入源码，值探针不入）
  </acceptance_criteria>
  <done>
    S1..S4 真实二进制全链断言绿；relay 夹具可用；红线自净通过。
  </done>
</task>

<task type="auto">
  <name>Task 2: S5 stop-signal 宽限 / S6 降权 self / S7 1001 序列 / S8 --open + 全量九脚本回归</name>
  <files>web/uat/phase07.mjs</files>
  <read_first>
    - web/uat/phase07.mjs（Task 1 产物——场景数组插入点）
    - web/uat/phase06.mjs（waitExit/进程组信号场景先例：S5① 宽限计时锚定服务端 detach 的 sleep 纪律 L212 区段注释）
    - .planning/phases/07-deployment/07-CONTEXT.md（§Claude's Discretion：UAT 场景矩阵）
  </read_first>
  <behavior>
    - S5 stop-signal：spawn --stop-signal TERM --stop-timeout 1s --exit-when-empty -- sh -c 'trap "" TERM; sleep 100' → attach+detach → waitExit：wesh 在宽限+补 KILL 后退出（退出状态 255——P6 OQ1 accept-255 同源，信号死亡 exitf(-1)）；对照子测 --stop-signal TERM（无 timeout，子进程不 trap）→ TERM 即终结退出 255；计时断言宽松上界（<10s 护栏）
    - S6 降权：spawn --uid <self> --gid <self>（process.getuid()/getgid() 取值免 root）→ attach → typeText 'id -u' echo 断言 == self uid；env 断言 HOME/USER 与服务端进程一致（self 降权身份不变——LookupId(self) 条目即当前用户）
    - S7 1001：spawn → dialHello 完成 → process.kill(weshPid, 'SIGTERM') → collectUntilClose 收 close code == 1001 且 reason 含 server_shutting_down → waitExit（子进程 HUP 终结退出 255）；wesh 在 15s 护栏内退出
    - S8 --open：① headless 形态（env 清 DISPLAY/WAYLAND_DISPLAY）spawn --open → stderr 含 no display 提示行且服务正常（GET / 200 或 listening 行存在）；② fake xdg-open：t 临时目录写可执行脚本（#!/bin/sh 记录 "$@" 到文件）+ PATH 前置 + DISPLAY=:99 → spawn --open --writable → 断言记录文件内容 == 启动打印的 rw 分享 URL（token 比对在闭包内——detail 只报等/不等布尔）；真实弹浏览器 skip+reason（双机拓扑纪律：真实 GUI 属 Windows 工作站人工层）
    - 全量回归：phase02/03/04/05/05-dims/06 协议层 + 04-dom/05-dom/06-dom jsdom 九脚本各自退出 0
  </behavior>
  <action>
    ① phase07.mjs 场景数组追加 S5..S8（behavior 逐字；S5 的 waitExit 超时护栏 ≥ stop-timeout + KILL 传播余量；S7 的 SIGTERM 发送点在 dialHello 完成后——握手完成 ⇒ attach 已登记，1001 广播必达）。
    ② 全场景复跑至全 PASS（时序敏感场景按 phase06.mjs 既有 pacing 纪律处理——S5 的 1s 宽限 + 进程终结传播给 ≥3s 护栏）。
    ③ 九脚本回归逐个执行并记录输出（六段式纪律：新脚本不得破坏旧脚本——任何失败先定位是否本 phase 行为变更的预期适配（如 07-01 的 dist 重建已过的套件），非预期失败即真实回归，回上游 plan 修复）。
    提交：`test(07-07): phase07.mjs S5-S8 scenarios and full UAT regression`
  </action>
  <verify>
    <automated>node web/uat/phase07.mjs && node web/uat/phase02.mjs && node web/uat/phase03.mjs && node web/uat/phase04.mjs && node web/uat/phase05.mjs && node web/uat/phase05-dims.mjs && node web/uat/phase06.mjs && node web/uat/phase04-dom.mjs && node web/uat/phase05-dom.mjs && node web/uat/phase06-dom.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node web/uat/phase07.mjs` 退出 0：S1..S8 全 PASS + assertOutputClean PASS + 汇总行零失败
    - 九个既有脚本各自退出 0（输出逐脚本记录于 SUMMARY）
    - S8 含 skip 行（真实弹浏览器 + reason——平台豁免先例形态）
  </acceptance_criteria>
  <done>
    phase07.mjs 八场景 + 自净全绿；phase 7 全部特性经真实二进制端到端锁定；既有九脚本零回归。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT 脚本 → CI 日志/控制台 | UAT detail 输出是泄露面——凭据/token 只作断言材料，永不进输出 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07a | Information Disclosure | UAT 输出泄露凭据/token | high | mitigate | assertOutputClean 运行时自净（emittedDetails 遍历断言零命中，含场景异常消息通道——WR-02 先例）；redactArgs 脱敏；TOML 凭据探针值入 sensitiveTokens 同口径 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V7 Error Handling/Logging（测试面自净） | ✓ assertOutputClean |
</threat_model>

<verification>
- `node web/uat/phase07.mjs` 退出 0（S1..S8 + 自净）
- 九既有脚本零回归
</verification>

<success_criteria>
- phase07.mjs 单脚本覆盖 phase 7 全部需求行为面，CI/本地可重复运行退出 0
- 既有 UAT 资产零损失（九脚本全绿）
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `web/uat/phase07.mjs`（新文件：S1 配置合并 / S2 unix socket relay / S3 base-path 交叉 / S4 auth-header+XFF / S5 stop-signal 宽限 / S6 降权 self / S7 1001 序列 / S8 --open 八场景 + TCP↔unix relay 夹具）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-deployment/07-07-SUMMARY.md` when done
</output>
