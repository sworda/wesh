---
phase: 07-deployment
plan: 06
type: execute
wave: 6
depends_on:
  - "07-05"
files_modified:
  - cmd/wesh/config.go
  - cmd/wesh/config_test.go
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
  - go.mod
  - go.sum
autonomous: true
requirements:
  - OPS-09

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "wesh --config wesh.toml -- bash 加载 TOML 配置生效；裸 wesh -- bash（无 --config）行为与今日完全一致——仅显式指定，零隐式路径搜索（D-01）"
    - "优先级链 flag > env > 配置文件 > 内置默认（D-05）：CLI 显式给的标量覆盖配置值；WESH_CREDENTIAL 优先于配置 credential 键；可重复项（credential/origin/client-option）CLI 给出则替换整个列表（D-02）"
    - "配置文件不存在 / TOML 解析失败 / 未知键均 exit 2 fail-fast（D-06 严格模式）；--no-auth/--insecure-http/--version/--help/--config 五键写入配置文件即「未知键」拒绝（D-04 逃生门边界）"
    - "command = [\"bash\", \"-l\"] exec 数组可入配置；CLI `--` 后 argv 非空则覆盖（D-04）"
    - "配置文件含 credential 键且权限非 600/400 时 stderr 警告放行（D-07 不阻断），警告串不含凭据值；TOML 解析/校验错误文案只含类别+键名+行号，绝不含值（SEC-01 启动面红线延伸）"
    - "配置文件驱动与 CLI 驱动的 validateStartup 校验矩阵行为一致（合并后终值走同一校验；socket 族互斥/单给判定对配置来源值同档生效——fc.Port/fc.Bind/fc.SocketMode/fc.SocketOwner 非 nil 即置对应显式位）；全部 27 个可配键（26 flag 同名 + command）均可入配置"
  artifacts:
    - path: "cmd/wesh/config.go"
      provides: "fileConfig 指针标量结构 + loadFileConfig（DisallowUnknownFields 严格模式）+ D-07 权限检查 + 值剥离错误包装 + prescanConfigPath"
      contains: "DisallowUnknownFields"
    - path: "cmd/wesh/config_test.go"
      provides: "TestLoadFileConfig/TestConfigMerge/TestConfigPrecedence/TestConfigRedLines"
      min_lines: 150
    - path: "cmd/wesh/main.go"
      provides: "--config flag + parseArgs 两阶段合并（预扫路径 → TOML 铺底 → flag 注册解析 → fs.Visit 合并）"
      contains: "--config"
    - path: "go.mod"
      provides: "github.com/pelletier/go-toml/v2 v2.4.3 依赖"
      contains: "pelletier/go-toml"
  key_links:
    - from: "cmd/wesh/config.go loadFileConfig"
      to: "cmd/wesh/main.go parseArgs 合并段"
      via: "fileConfig 指针标量区分「键缺席」与「零值」——合并正确性依赖 nil 判定，值拷贝会吞掉显式零值"
      pattern: "fileConfig"
    - from: "cmd/wesh/main.go fs.Visit 显式设置位"
      to: "D-02/D-05 合并语义"
      via: "CLI 显式设置位是优先级链的判定锚——writePolicySet/maxClientsSet/exitEmptySet 三先例同形态扩展"
      pattern: "fs.Visit"
  prohibitions:
    - statement: "MUST NOT echo config-file values（especially credential）in any error/warning output——错误文案只含类别 + 键名 + 行号（SEC-01 启动面红线延伸，Pitfall 5）"
      status: resolved
      verification: judgment
    - statement: "MUST NOT load any config file not explicitly named by --config——无隐式默认路径搜索（D-01）；裸启动行为零漂移"
      status: resolved
      verification: judgment
---

<objective>
落地 OPS-09 配置文件（D-01..D-07，P5 deferred「新 flag 配置文件收口」兑现）：`--config` 显式指定 TOML 文件，pelletier/go-toml/v2 严格模式（DisallowUnknownFields 未知键拒绝），平铺 key=value 键名=flag 名，全部 26 个长期运行 flag + command exec 数组可入，优先级 flag > env > 配置 > 默认，CLI 给出则替换整个列表，含 credential 键时权限 600/400 检查警告。

本 phase 唯一新依赖 go-toml/v2 v2.4.3（RESEARCH Package Legitimacy Audit：OK/Approved——proxy.golang.org + pkg.go.dev + STACK.md 三通道核实，非 [ASSUMED]）。

Purpose: systemd/容器部署的常态是配置文件 + EnvironmentFile；CLI 全长 flag 串不可维护；严格模式未知键拒绝防拼写错误静默失效。
Output: --config 全链可用，27 键覆盖，合并语义经表驱动锁定，敏感值红线运行时自证。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-deployment/07-CONTEXT.md
@.planning/phases/07-deployment/07-RESEARCH.md
@.planning/phases/07-deployment/07-PATTERNS.md
@cmd/wesh/main.go
@cmd/wesh/main_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: config.go 加载层——fileConfig + 严格模式 + D-07 权限警告 + 值剥离错误 + go-toml 依赖引入</name>
  <reversibility rating="one-way">D-01/D-03/D-06：--config 是 CLI 公开契约；TOML 平铺形状与严格模式是配置文件的公开格式契约（分组 sections 被否决）——用户已在 07-CONTEXT 锁定，记录评级不重复设卡。</reversibility>
  <files>cmd/wesh/config.go, cmd/wesh/config_test.go, go.mod, go.sum</files>
  <read_first>
    - cmd/wesh/main.go（config struct L28-54 全字段清单——fileConfig 键的一一对应面；credErr/clientOptErr 记录式上报 L143-152,174-192）
    - .planning/phases/07-deployment/07-RESEARCH.md（Pattern 4 fileConfig 逐字结构 + 严格模式 API 引文 + D-07 警告串形态 + Pitfall 5 值剥离 + 依赖引入顺序纪律）
    - .planning/phases/07-deployment/07-PATTERNS.md（§config.go：纪律类比三件——记录式/parse 期校验/警告形态）
  </read_first>
  <behavior>
    - fileConfig 27 键（26 flag 同名 + command；指针标量 + []string 列表 + command []string）：port/bind/writable/ping-interval/write-policy/max-clients/once/exit-when-empty/credential/origin/client-option/tls-cert/tls-key/osc52/socket/socket-mode/socket-owner/base-path/auth-header/cwd/term/stop-signal/stop-timeout/uid/gid/open/command；无 no-auth/insecure-http/version/help/config 字段（DisallowUnknownFields 自然拒绝为未知键，D-04 边界）
    - loadFileConfig(path)：文件不存在 → error（exit 2 由调用方）；TOML 解析失败 → error；未知键 → StrictMissingError → error 只含未知键名清单（禁含值）；成功返回 *fileConfig
    - TOML 类型不符（如 port = "abc"、credential = "notalist"）→ error 只含键名与行号（go-toml DecodeError 的行列上下文提取，禁含值）
    - D-07：fc.Credential != nil 且 os.Stat(path).Mode().Perm() 非 0600/0400 → 返回 warn 串 `wesh: warning: config file <path> contains credentials and is readable by others (mode <perm>); recommend chmod 600`（不含凭据值）
    - prescanConfigPath(args)：手工扫描 `--config=<v>` 与 `--config <v>` 两形态，last-wins；未给返回 ""（扫描器不解析其他 flag，不报错——--config 本身亦注册为正式 flag 保持 help/一致性，正式 Parse 的值与预扫一致）
    - 错误包装统一 configErr(path, category, detail)：`invalid config file <path>: <category> (<detail>)`——detail 只含键名/行号
  </behavior>
  <action>
    ① 依赖引入（05-05 纪律：先落码再 go get）：先写 cmd/wesh/config.go 含 `import "github.com/pelletier/go-toml/v2"` 与完整实现，再 `go get github.com/pelletier/go-toml/v2@v2.4.3 && go mod tidy`（否则 tidy 回收无引用依赖）。
    ② cmd/wesh/config.go【新】：fileConfig 结构体（behavior 27 键（26 flag 同名 + command），toml tag = flag 名逐字；注释头登记 D-01..D-07 决策依据与「指针标量区分键缺席与零值」的合并前提）；loadFileConfig（behavior 形态——`toml.NewDecoder(f).DisallowUnknownFields().Decode(&fc)` 严格模式唯一 API，该库无 SetStrict 方法 RESEARCH 明示；StrictMissingError 经 errors.As 提取键名清单组 detail；其他 DecodeError 提取行号组 detail；全部经 configErr 包装——禁直接透传 err.Error()，go-toml 错误上下文可能含源行值 Pitfall 5）；D-07 权限检查（os.Stat → Mode().Perm()，warn 串 behavior 逐字形态）；prescanConfigPath（behavior 两形态扫描）。
    ③ cmd/wesh/config_test.go【新】：TestLoadFileConfig 表驱动（t.TempDir 落 TOML 文件）——合法全键加载、未知键拒绝（断言错误含键名不含值——凭据值探针串如 "s3cr3t" 写入 TOML 后断言错误输出零出现，红线运行时自证）、类型不符拒绝、文件不存在、D-07 警告触发（credential + chmod 644 → warn 且不含值）与不触发（chmod 600 → 无 warn；无 credential 键 + 644 → 无 warn）、prescan 两形态与 last-wins。
    提交：`feat(07-06): TOML config file loader with strict mode and permission warning`
  </action>
  <verify>
    <automated>go test ./cmd/wesh -run 'TestLoadFileConfig' -count=1 -v && go build ./... && go mod verify</automated>
  </verify>
  <acceptance_criteria>
    - TestLoadFileConfig 全子测 PASS（含值剥离红线断言：错误输出零出现探针串）
    - `grep -c 'DisallowUnknownFields' cmd/wesh/config.go` == 1
    - `grep -c 'pelletier/go-toml' go.mod` ≥ 1
    - `go mod verify` 输出 all modules verified
    - `go vet ./cmd/wesh` 退出 0
  </acceptance_criteria>
  <done>
    go-toml v2.4.3 入库；加载层 27 键严格模式 + 权限警告 + 值剥离错误经表驱动锁定（含红线探针断言）。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: parseArgs 两阶段合并——配置铺底 + CLI 覆盖 + env 夹层 + argv/command + 敏感值记录式</name>
  <files>cmd/wesh/main.go, cmd/wesh/main_test.go, cmd/wesh/config_test.go</files>
  <read_first>
    - cmd/wesh/main.go（parseArgs 全文 L117-282：flag 注册/showVersion 早退 L242-244/credErr+clientOptErr 上报点 L247-254/write-policy 枚举 L258/tls 成对 L263/WESH_CREDENTIAL env 兜底 L268-276/argv 检查 L277-280；fs.Visit L216-228；07-01..07-05 落地的全部新 flag）
    - cmd/wesh/config.go（Task 1 产物：fileConfig/loadFileConfig/prescanConfigPath）
    - .planning/phases/07-deployment/07-RESEARCH.md（Pattern 4 合并算法逐字 + OQ4：exit-when-empty 字符串单形态复用 exitEmptyValue.Set）
  </read_first>
  <behavior>
    - 装配顺序：prescanConfigPath(args) → 非空则 loadFileConfig（失败 exit 2；warn 非空即 stderr 打印）→ fileConfig 各标量键作为 flag 注册默认值（fc.X != nil → default = *fc.X）→ fs.Parse → fs.Visit 显式位 → 合并收尾
    - 标量：flag 注册默认值取配置值 ⇒ CLI 未给自然落配置值，CLI 给则覆盖（flag > 配置）；内置默认仅在配置键缺席时出现（配置 > 默认）——两档优先级由默认值替换机制天然成立
    - 列表（credential/origin/client-option）：CLI 回调填充非空 = 显式给出 → 整个列表替换（D-02）；CLI 未给且配置键非 nil → 配置列表经各自 parse 期校验函数逐项解析（ParseCredential/NormalizeOrigin/client-option 白名单+JSON 校验——同一校验函数复用零双写）；credential 配置项校验错误走 credErr 同款记录式（错误类别 + 键名，禁含值）
    - env 夹层（D-05）：WESH_CREDENTIAL 现状块（len(cfg.credentials)==0 时解析）保持在配置列表应用之前——执行序：flag 列表 → env → 配置列表（env 非空则配置 credential 不应用），优先级 flag > env > 配置成立
    - exit-when-empty：配置键为字符串单形态（OQ4），CLI 未显式给且配置非 nil → cfg.exitEmpty.Set(*fc.ExitWhenEmpty)（"true"/"0"/"30s" 全通——单一解析路径零双写；bool 形态由 go-toml 类型不符自然拒绝）
    - argv：fs.Args() 非空 → CLI argv；空且 fc.Command != nil → 配置 command；皆空 → missing command（D-04）
    - write-policy×writable 组合校验扩展：cfg.writePolicySet（CLI 显式）或配置 write-policy 键非 nil，且 !cfg.writable → 拒绝（配置驱动的组合矛盾与 CLI 同档——校验矩阵语义不漂移）
    - socket 族互斥的配置来源扩展（D-08/D-09 在配置路径下同语义）：合并收尾时 `fc.Port != nil → cfg.portSet`、`fc.Bind != nil → cfg.bindSet`、`fc.SocketMode != nil → cfg.socketModeSet`、`fc.SocketOwner != nil → cfg.socketOwnerSet` 置位——配置键存在即「已给定」，07-02 落地的互斥/单给校验矩阵对配置驱动与 CLI 驱动同档生效（write-policy 扩展同款模式；不置位则配置同时写 socket+port 或单写 socket-mode 会静默绕过 D-08/D-09 fail-fast）
    - --once 展开保持 fs.Visit 显式位锚定：配置 max-clients/exit-when-empty 不算显式 → --once 展开覆盖配置值（flag > 配置 优先级直接推论）
    - socket-mode 配置键（八进制串）与 stop-signal/stop-timeout/ping-interval/socket/uid/gid 等：经 Task 1 默认值机制进 cfg 后，沿用既有 parse 期校验（07-02/07-04 落地的校验段消费终值，零新代码路径）
  </behavior>
  <action>
    ① parseArgs 改造（behavior 装配顺序逐字落地）：
    - 函数首部：configPath := prescanConfigPath(args)；非空 → fc, warn, err := loadFileConfig(configPath)；err != nil → return（exit 2 现状通道）；warn != "" → fmt.Fprintln(os.Stderr, warn)（D-07 加载期警告，parseArgs 打印为最小签名扰动——fs.Usage 打印先例同文件）
    - 注册默认值替换：flag 注册前把 fc 标量键换算为局部 default 变量（fc.Port != nil → portDefault = *fc.Port，否则 7681；bind/writable/pingInterval/writePolicy/maxClients/once/osc52/tlsCert/tlsKey/socket/socketOwner/basePath/authHeader/cwd/term/open/uid/gid 同形态；socketMode 为八进制串默认值替换；stopTimeout 为 duration 解析——解析失败经 configErr 类别报错 exit 2 键名不含值... duration 非敏感可含值，与 exitEmptyValue.Set 既定纪律一致）
    - `--config` 正式注册（StringVar，help: load TOML config file (CLI flags override config values)）——预扫与正式 Parse 双通道同值，help 可见
    - 列表合并段（Parse + Visit 之后、env 块之后新插入）：credential/origin/client-option 三列表按 behavior D-02 语义应用（配置 credential 逐项 ParseCredential——错误记入 credErr 同款记录式变量 cfgCredErr，统一上报点与 credErr 并列插入 showVersion 早退之后；origin 逐项 NormalizeOrigin 错误直接返回（值非敏感先例）；client-option 逐项白名单+JSON 校验错误记入 clientOptErr 同款）
    - exit-when-empty/argv/write-policy×writable 扩展按 behavior 落地
    - parseArgs 头注释更新：17 flag → 30 flag 计数与 Phase 7 分组清单（配置文件合并顺序一段）
    ② main_test.go TestParseArgs 扩展（既有行零改动）：新增 config 专属用例函数 TestParseArgsWithConfig（t.TempDir 落 TOML + args 组合）——标量覆盖（config port=9999 vs CLI --port 1234 → 1234；仅 config → 9999）、列表替换（config credential 两组 + CLI 一组 → 1；仅 config → 2）、env 夹层（t.Setenv WESH_CREDENTIAL + config credential → env 胜计数 1）、argv 覆盖（config command=["bash","-l"] + CLI -- ls → ["ls"]；仅 config → ["bash","-l"]）、exit-when-empty 配置三形态、--once 覆盖配置 max-clients、write-policy×writable 配置驱动拒绝、socket 族配置驱动拒绝两行（配置同时给 socket+port → 拒绝且文案同 D-08；配置单给 socket-mode 无 socket → 拒绝且文案同 D-09）、逃生门键拒绝（no-auth 写入 TOML → exit 2 未知键）。
    ③ 全量 `go test ./cmd/wesh -count=1` + `go test -race -count=1 ./...` 回归。
    提交：`feat(07-06): two-phase config merge with flag > env > config > default precedence`
  </action>
  <verify>
    <automated>go test ./cmd/wesh -count=1 && go test -race -count=1 ./... && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - TestParseArgsWithConfig 全子测 PASS（behavior 列举的合并矩阵全覆盖）
    - `go test ./cmd/wesh -count=1` 全绿（Task 1 + 既有行零回归）
    - `go test -race -count=1 ./...` 全仓全绿
    - `grep -c 'prescanConfigPath' cmd/wesh/main.go` ≥ 1
    - TestStartupMatrix/TestStartupRefusalNoResource 全绿（配置驱动与 CLI 驱动同矩阵语义）
  </acceptance_criteria>
  <done>
    --config 两阶段合并落地：27 键可配、优先级链与列表替换语义经表驱动锁定、逃生门键拒绝、socket 族互斥配置同档、敏感值记录式；裸启动（无 --config）行为与今日完全一致（全部既有测试零改动通过为证）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem (TOML file) → wesh | 配置文件是 operator 控制输入，但可能含凭据——权限与错误输出是泄露面 |
| wesh → stderr (config errors) | go-toml 错误上下文可能回显源行值（Pitfall 5） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06a | Information Disclosure | 配置文件凭据 world-readable | high | mitigate | D-07 权限检查警告（600/400 之外 stderr 提醒不阻断）+ WESH_CREDENTIAL env 优先于配置明文（D-05 链内）+ README chmod 600 建议（07-08） |
| T-07-06b | Information Disclosure | 解析/校验错误回显敏感值 | high | mitigate | configErr 值剥离包装（类别+键名+行号三要素）；credErr 同款记录式扩展；测试探针串运行时自证（错误输出零出现） |
| T-07-06c | Tampering | 未知键静默失效（拼写错误） | medium | mitigate | D-06 DisallowUnknownFields 严格模式 exit 2——「显式优于静默」哲学与 --client-option 白名单同档 |
| T-07-06d | Tampering | 隐式配置加载意外行为 | medium | mitigate | D-01 仅 --config 显式指定；裸启动零路径搜索零行为漂移（prohibition 同义锁定） |
| T-07-SC | Tampering | go get 新依赖供应链 | high | mitigate | RESEARCH Package Legitimacy Audit：go-toml/v2 v2.4.3 经 proxy.golang.org @latest + pkg.go.dev 官方文档 + STACK.md 定案三通道核实（OK/Approved）；go.sum 锁定 + `go mod verify`；Go module zip 纯源码无安装期执行面 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V5 Input Validation（TOML 严格模式/类型校验） | ✓ DisallowUnknownFields + 指针标量 |
| V7 Error Handling（错误不含敏感值） | ✓ configErr + 探针断言 |
</threat_model>

<verification>
- `go test -race -count=1 ./...` 全仓全绿
- TestLoadFileConfig + TestParseArgsWithConfig 合并矩阵全覆盖
- 值剥离红线探针断言（错误输出零出现凭据值）
- `go mod verify` all modules verified
</verification>

<success_criteria>
- `printf 'port = 0\nbind = "127.0.0.1"\ncredential = ["alice:pw"]\ncommand = ["bash"]\n' > /tmp/wesh.toml && wesh --config /tmp/wesh.toml`：启动需 Basic 认证、随机端口打印、bash 运行
- 同上加 CLI `--max-clients 5`：maxClients 生效 5（CLI 覆盖）
- TOML 写入 `no-auth = true` → exit 2 且文案为未知键类别不含 "true" 值
- `wesh -- bash`（无 --config）与今日行为逐字节一致
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **OPS-09 (unclassified, unresolved)**：人工复核项——TOML 多行数组/内联表等合法 TOML 语法按 go-toml 语义接受（平铺形状是约定不是语法强制，D-03 拒绝的是分组 sections 设计而非语法特性）；配置重复键由 TOML 解析器拒绝（规范行为）；command 空数组 `command = []` 等价缺席（fc.Command 非 nil 但 len 0 → 走 missing command 检查——按缺席语义处理，与 CLI `--` 空 argv 同档）。07-08 人工 UAT 清单复核。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `type fileConfig struct`（27 键 toml tag 结构——26 flag 同名 + command，cmd/wesh/config.go，新文件）
- `loadFileConfig(path string) (fc *fileConfig, warn string, err error)`（cmd/wesh/config.go）
- `prescanConfigPath(args []string) string`（cmd/wesh/config.go）
- `configErr(path, category, detail string) error`（cmd/wesh/config.go）
- flag `--config`（cmd/wesh/main.go）
- `TestLoadFileConfig` / `TestParseArgsWithConfig`（cmd/wesh/config_test.go，新文件）
- go.mod 依赖 `github.com/pelletier/go-toml/v2 v2.4.3`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/07-deployment/07-06-SUMMARY.md` when done
</output>
