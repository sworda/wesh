---
phase: 02-protocol
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - internal/server/server.go
  - internal/server/limits_test.go
autonomous: true
requirements:
  - RES-01
  - SEC-08

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "16384 字节消息正常处理，16385 字节消息触发库自动 1009 关闭——单帧/累积字节两层硬顶边界精确（RES-01，D-09 修订）"
    - "1 字节 × N 分片洪水（客户端 Writer 逐非 fin 帧写）在累积 16385 字节处被 1009 切断——内存 ≤16KiB、CPU ≈ 16K 次帧头解析（RES-01 等效防线主证据，read.go:521-541）"
    - "预认证窗口内 >4KiB 消息触发 1009（ReadLimitPreAuth 档生效，SEC-08/D-11）"
    - "0 字节空消息洪水下服务存活、连接不断、握手与 echo 功能正常——D-09 修订残余风险的显式断言形态（不断言 1009；空帧无应用层钩子，CPU 受攻击者带宽约束）"
    - "全部 1009 路径在服务端 stderr 留下单行事件（对端/码值/message_too_big 机器串）——D-12② 超限可见性三腿之二，errors.Is(err, websocket.ErrMessageTooBig) 钩子"
    - statement: "预认证及超限洪水全程服务端内存平坦：无消息级缓冲预分配，SetReadLimit limitReader 流式截断（SEC-08 结构目标；HeapAlloc 增量采样为参考性宽松防线而非精确门禁）"
      verification: backstop
  prohibitions:
    - "超限处置不得发明新关闭码或自定义 4000 段——单帧/累积字节/分片层违规一律复用 1009（D-08 纪律）；库自动 1009 之外不得包装库、不得包装 conn 数帧（D-09 修订反模式清单）"
  artifacts:
    - path: "internal/server/server.go"
      provides: "reader 错误路径的 ErrMessageTooBig → stderr 单行事件钩子（message_too_big 归一化）"
      contains: "ErrMessageTooBig"
    - path: "internal/server/limits_test.go"
      provides: "RES-01 攻击面五测：超限/边界/分片洪水/空帧洪水存活/预认证档"
      contains: "TestOversize1009 / TestReadLimitBoundary / TestFragmentedFlood1009 / TestEmptyFragmentFloodResilience / TestPreHelloReadLimit"
  key_links:
    - from: "internal/server/server.go"
      to: "github.com/coder/websocket"
      via: "Read 返回错误经 errors.Is(err, websocket.ErrMessageTooBig) 识别库自动 1009（read.go:526-529），应用补 stderr 事件——不包装库"
      pattern: "ErrMessageTooBig"
    - from: "internal/server/limits_test.go"
      to: "internal/proto/proto.go"
      via: "边界值断言引用 proto.ReadLimitPostAuth(16384)/ReadLimitPreAuth(4096) 常量而非硬编码数字"
      pattern: "proto\\.ReadLimit"
---

<objective>
把 RES-01 三层上限（D-09 修订后的等效防线形态）与 D-12② 超限可见性锁成自动化证据：server.go 补 ErrMessageTooBig stderr 钩子，新建 limits_test.go 五测——17KiB 超限 1009、16384/16385 边界、1 字节分片洪水 1009、0 字节空消息洪水存活、预认证 4KiB 档 1009。

Purpose: ROADMAP 准则 1 的量化断言面。分片洪水用库客户端 Writer 精确构造（write.go:223-264 每次 Write 一个非 fin 帧），无需裸帧 helper——D-09 修订后测试矩阵全部库客户端可覆盖（planner 裁决，PATTERNS 的 rawws 裸帧 helper 作废）。
Output: server.go 1009 事件钩子 + limits_test.go 五测全绿。

规格探针标记（spec-less probe fallback）：RES-01 探针报为 unclassified；边界按 D-09 修订裁决显式落入 must_haves.truths（含空帧洪水"存活而非 1009"的断言形态修正），"内存平坦"以 backstop 标记（采样脆弱、宽松阈值），无静默丢弃。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-protocol/02-CONTEXT.md
@.planning/phases/02-protocol/02-RESEARCH.md
@.planning/phases/02-protocol/02-PATTERNS.md
@internal/server/server.go
@internal/server/e2e_test.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: server.go——ErrMessageTooBig stderr 钩子（D-12② 超限事件）</name>
  <files>internal/server/server.go</files>
  <read_first>
    - internal/server/server.go（被改文件现状：02-04 后的 Attach reader 循环错误处理段与 logEvent 方法）
    - .planning/phases/02-protocol/02-RESEARCH.md（§Code Examples 超限可见性三腿服务端钩子 423-435 行；Anti-Patterns 304 行 reason 不可定制）
    - .planning/phases/02-protocol/02-PATTERNS.md（§stderr 单行事件段 257 行、Planner 注意 7（491 行）：机器串落 stderr 不落线上 reason）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-12，行 38-39）
  </read_first>
  <behavior>
    - 库自动 1009 后（任一超限路径：稳态 16KiB / 预认证 4KiB），服务端 stderr 出现恰一行含 remote、code=1009、reason=message_too_big 的事件
    - 非超限的 reader 错误（对端正常关闭/网络断开）不产生 message_too_big 事件
  </behavior>
  <action>
    在 internal/server/server.go 的 Attach reader 错误处理段（对端关闭与网络断开统一走 wsDisconnected 的位置）前置分类钩子：
    1. errors.Is(err, websocket.ErrMessageTooBig) 为真时调 s.logEvent——三要素：对端 remote、code=1009、reason="message_too_big"（D-12②；库已自动发 1009 且其 close reason 是库内字符串 "read limited at N bytes" 不可定制——read.go:526-529，机器串落点在 stderr 而非线上 reason，PATTERNS 注意 7；禁止包装库或包装 conn）。
    2. 该钩子同时覆盖稳态 16KiB 与预认证 4KiB 两档的超限（同一错误标识）；预认证窗口首读的 ErrMessageTooBig 同样命中此路径（02-02 握手段首读 err 处理处若独立，需同步加钩子——两处埋点同一 logEvent 调用形态）。
    3. 埋点后错误仍按既有路径收口（wsDisconnected → terminate），不新增 exitf 分支。
    4. 复核全文件 logEvent 埋点清单（02-02/02-03/02-04 已落地）：hello_timeout/empty_frame/frame_before_hello/malformed_hello/version_mismatch/subprotocol_required（assert 兜底分支，02-02 Task 1 显式建立）/pong_timeout + 本次 message_too_big——统一三要素格式（remote/code/reason），不一致处顺手归一。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go build ./... && go vet ./... && go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - server.go reader 错误路径含 errors.Is(err, websocket.ErrMessageTooBig) 分类钩子与 logEvent 调用
    - 预认证首读错误路径与稳态读循环错误路径均有该钩子
    - 全部 logEvent 调用点三要素（remote/code/reason）格式一致
    - 既有测试全量保持绿色
  </acceptance_criteria>
  <done>1009 路径的 stderr 可见性落地；全部主动/库自动关闭路径事件格式归一。</done>
</task>

<task type="auto">
  <name>Task 2: limits_test.go——RES-01 攻击面五测</name>
  <files>internal/server/limits_test.go</files>
  <read_first>
    - internal/server/e2e_test.go（同包 helper：startTestServerWith/dialHello/waitExit；CloseError 读取循环 267-278 行）
    - internal/server/server.go（本 plan Task 1 产物：ErrMessageTooBig 钩子——stderr 断言目标）
    - internal/proto/proto.go（ReadLimitPreAuth/ReadLimitPostAuth 常量——边界值引用源）
    - .planning/phases/02-protocol/02-RESEARCH.md（§Validation Architecture RES-01 五行映射 542-546 行、§测试可注射性注意的分片洪水客户端构造 559 行、Pitfall 1 等效性分析 324-337 行）
    - .planning/phases/02-protocol/02-CONTEXT.md（D-09 修订，行 34）
  </read_first>
  <action>
    新建 internal/server/limits_test.go（package server_test，复用 helper，超时护栏 15s——洪水测试放宽）：
    1. TestOversize1009（RES-01）：startTestServerWith Writable:true；dialHello 后 c.Write(ctx, MessageBinary, make([]byte, proto.ReadLimitPostAuth+1))（首字节随意——超限在协议解析前由库拦截）→ 读至 CloseError 断言 Code==1009 → waitExit(0)。
    2. TestReadLimitBoundary（RES-01）：Writable:true；dialHello 后构造合法 INPUT 帧：append([]byte{proto.Input}, make([]byte, proto.ReadLimitPostAuth-1)...)（总长恰 16384）→ cat 回显 16383 字节载荷，累积收齐 OUTPUT 断言（边界内正常）；再发总长 16385 的 INPUT 帧 → CloseError 1009（边界外切断）→ waitExit(0)。（/bin/cat 回显大块可能分块到达，沿用累积模式；读 ctx 放宽 10s。）
    3. TestFragmentedFlood1009（RES-01 等效防线主证据）：Writable:true；dialHello 后 w, err := c.Writer(ctx, MessageBinary)，循环 w.Write([]byte{0x41}) 共 proto.ReadLimitPostAuth+100 次（每次 Write 产生一个非 fin continuation 帧——write.go:223-264，精确构造 1 字节分片流）→ 库在累积 16385 字节处自动 1009：Write 或后续 Read 返回错误，读至 CloseError 断言 Code==1009 → w.Close() 忽略错误（连接已死）→ waitExit(0)。断言放宽形态：Write 循环中出错即 break，最终 CloseError 必须是 1009（RESEARCH：内存 ≤16KiB、毫秒级切断）。
    4. TestEmptyFragmentFloodResilience（RES-01 D-09 修订残余风险显式断言）：Writable:true；dialHello 后 runtime.ReadMemStats 采基线；循环 c.Write(ctx, MessageBinary, []byte{}) 共 5000 次（0 字节空消息洪水——OQ2 既定：Hello 后空消息静默跳过）→ 洪水后：发 INPUT echo payload 累积收齐（服务存活且功能正常）+ 连接未被关闭 + exitf 未触发；再 ReadMemStats 采样，HeapAlloc 增量断言 < 8MiB（宽松参考防线——防回归性分配，非精确门禁；脆弱则降为仅记录不门禁，SUMMARY 注明）→ c.Close(1000) → waitExit(0)。注释写明 D-09 修订：空帧无应用层钩子，本测试断言"存活+内存平坦"而非"1009"。
    5. TestPreHelloReadLimit（SEC-08/D-11）：默认 Options；websocket.Dial（带 wesh.v1 子协议）成功但不发 Hello，直接 c.Write(ctx, MessageBinary, make([]byte, proto.ReadLimitPreAuth+1)) → 读至 CloseError 断言 Code==1009（4KiB 预认证档生效，库自动）→ waitExit(0)。
    stderr 事件断言：五测中 1009 四测可通过 captureFd 风格（os.Pipe 替换 os.Stderr）断言 "message_too_big" 单行出现——进程全局替换不并行，测试内串行执行；若与 e2e 既有测试并发冲突则仅 TestOversize1009 一处做 stderr 断言（避免 fd 替换竞争，其余以 CloseError 为准）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test -race -count=1 -v -run 'TestOversize1009|TestReadLimitBoundary|TestFragmentedFlood1009|TestEmptyFragmentFloodResilience|TestPreHelloReadLimit' ./internal/server/ && go test -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - 五测全部绿色且 -race 无报告
    - TestReadLimitBoundary 同时断言 16384 正常与 16385 → 1009（边界精确）
    - TestFragmentedFlood1009 以库客户端 Writer 构造分片流（无裸帧 helper、无手写帧字节）
    - TestEmptyFragmentFloodResilience 断言存活 + echo 正常 + 内存增量宽松阈值，注释含 D-09 修订残余风险说明（不断言 1009）
    - TestPreHelloReadLimit 断言预认证 4KiB 档 1009
    - TestOversize1009 含 stderr "message_too_big" 单行事件断言（D-12② 可见性证据）
    - `go test -race -count=1 ./...` 全仓绿
  </acceptance_criteria>
  <done>ROADMAP 准则 1 的量化断言面完整：两层硬顶边界精确、分片洪水被字节顶切断、空帧洪水存活、预认证档生效、超限 stderr 可见。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server（已/未认证数据帧流） | 本 plan 覆盖的边界：稳态 16KiB 与预认证 4KiB 两档内的全部字节流均不可信 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-17 | DoS | 单帧/累积字节超限（预认证内存放大，ttyd protocol.c:288-298 对照面） | critical | mitigate | SetReadLimit 两档（4KiB/16KiB）库流式 limitReader 执行，超限自动 1009；TestOversize1009/TestReadLimitBoundary/TestPreHelloReadLimit 锁定 |
| T-02-18 | DoS | 1 字节 × N 分片洪水（Bandit CVE-2026-65623 形） | high | mitigate | 累积字节硬顶 16385 处切断；库流式重组 O(1)/帧无 O(n²)；TestFragmentedFlood1009 锁定（D-09 修订等效防线主证据） |
| T-02-19 | DoS | 0 字节空帧洪水 | medium | accept | 无应用层钩子（库内部吞空帧，read.go:457-479）——D-09 修订用户已裁决接受；预认证窗口由 5s 超时 + per-IP 8 + 409 门盒住，认证后纯 CPU 受带宽约束、内存平坦；TestEmptyFragmentFloodResilience 断言存活形态 |
| T-02-20 | Information Disclosure | 库自动 1009 的线上 close reason（库内字符串） | low | accept | reason 不含敏感信息；机器串可见性落 stderr（D-12②，PATTERNS 注意 7），前端按 code 分派不读 reason |
| T-02-SC | Tampering | npm/pip/cargo installs | high | accept | 零新增依赖（RESEARCH §Package Legitimacy Audit） |
</threat_model>

<verification>
- `go test -race -count=1 ./...` 全绿（五新测 + 既有全部）
- `go build ./... && go vet ./...` 退出 0
- 攻击面用例与 RESEARCH §Validation Architecture 测试映射五行一一对应
</verification>

<success_criteria>
- ROADMAP 准则 1 自动化成立：超限帧 1009 合规关闭、空帧洪水下服务存活内存平坦、两层硬顶（16KiB/16KiB）经 SetReadLimit 库执行
- D-12 三腿齐了：前端 1009 文案（02-02）+ stderr 事件（本 plan）+ 机器串（stderr reason 字段）
- SEC-08 预认证窗口的读上限档（4KiB）获得攻击面证据
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `internal/server/server.go`：reader 错误路径 `errors.Is(err, websocket.ErrMessageTooBig)` 分类钩子（稳态 + 预认证首读两处）
- `internal/server/limits_test.go`：`TestOversize1009`/`TestReadLimitBoundary`/`TestFragmentedFlood1009`/`TestEmptyFragmentFloodResilience`/`TestPreHelloReadLimit`

<output>
Create `.planning/phases/02-protocol/02-05-SUMMARY.md` when done
</output>
