---
phase: 03-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/server/auth.go
  - internal/server/auth_test.go
  - internal/server/origin.go
  - internal/server/origin_test.go
  - internal/server/headers.go
  - internal/server/tls.go
  - internal/server/tls_test.go
autonomous: true
requirements:
  - SEC-01
  - SEC-04
  - SEC-05

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "凭据启动时预哈希为 SHA-256 摘要对（32B 定长），消除 subtle.ConstantTimeCompare 长度侧信道（官方明示长度不等立即返回 0，Pitfall 1）；parseCredential 切分首个 ':'（密码可含 ':'，user 不可含，RFC 7617），拒绝无冒号/空 user"
    - "matchCredential 多组凭据逐组比较、位或累积（每组 userHash 与 passHash 两次 ConstantTimeCompare 相与后 | 入结果）——不短路、不提前退出，循环恒跑满全部组"
    - "NormalizeOrigin 把 --origin 值规范化为小写 scheme://host[:port]：剥离默认端口（http:80/https:443，RFC 6454 浏览器序列化省略默认端口——Pitfall 3 白名单永不命中防线）；拒绝 path/query/fragment/userinfo；拒绝 glob 字符 *?[\\（使库 path.Match 退化为精确比较，D-12）；仅允许 http/https scheme 且 host 非空"
    - "originAllowed 语义与 coder/websocket accept.go:228-264 库内建对齐：空 Origin 放行（非浏览器客户端零摩擦，D-13）→ EqualFold(r.Host, u.Host) 同源放行 → 规范化集合精确查找 → 否则拒绝；Origin: null（u.Host 为空不匹配任何项）拒绝——null 是 CSWSH 常见载体"
    - "TLSConfig() 返回 tls.Config{MinVersion: TLS 1.2, CipherSuites: 显式 6 AEAD 清单}——Go 1.26.3 默认列表含 4 个 ECDHE CBC-SHA1 套件，不设清单 testssl.sh 必报弱项（Pitfall 2，GOROOT cipher_suites.go:283-312 一手核实）；TLS 1.3 cipher 不可配不设置"
    - "TLS 自动化断言（D-07）：客户端 MaxVersion 1.1 握手必败、1.2 必成、零值客户端默认协商出 1.3（ConnectionState.Version == TLS13）、CBC-only 客户端必败；测试内 crypto/x509+ecdsa 自签 localhost 证书，无 fixture 文件"
    - "安全头中间件恒在六项：Content-Security-Policy（default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self'; frame-ancestors 'none'; base-uri 'none'; form-action 'none'）、X-Frame-Options: DENY、X-Content-Type-Options: nosniff、Referrer-Policy: strict-origin-when-cross-origin、Cross-Origin-Opener-Policy: same-origin、Cross-Origin-Resource-Policy: same-origin；Strict-Transport-Security: max-age=63072000; includeSubDomains 仅 TLS 分支（D-06）"
    - statement: "凭据比较耗时与凭据组数、匹配组位置正交（SHA-256 等长化 + subtle + 逐组不短路的构造保证；时序测量断言不可移植，以代码形态走查为证——subtle 调用操作数均为 32B 定长摘要、循环无 break/条件 return）"
      verification: backstop
  prohibitions:
    - "禁止明文比较凭据（==/strings.Equal 逐字节短路时序泄露，ttyd strcmp 反例 C6）；ConstantTimeCompare 的操作数禁止是原始字符串"
    - "禁止尝试配置 TLS 1.3 cipher 套件（Go 设计不可配，配了即死代码）；禁止使用已废弃的 PreferServerCipherSuites"
    - "HSTS 不得在明文 HTTP 分支发送（Pitfall 7：规范要求仅 TLS，反代终止场景与反代策略打架）；安全头中间件必须带 tlsOn 分支参数"
    - "TLS 测试禁止引入 *.pem fixture 文件或依赖 GODEBUG 环境变量（Pitfall 9：GODEBUG 进程启动期读取 t.Setenv 无效；自签证书测试内生成）"
    - "不发送 X-XSS-Protection/Expect-CT/Public-Key-Pins 等废弃头（OWASP 业界共识，以 CSP 替代）"
  artifacts:
    - path: "internal/server/auth.go"
      provides: "Credential 类型（预哈希摘要对）+ ParseCredential + matchCredential"
      contains: "matchCredential"
    - path: "internal/server/origin.go"
      provides: "NormalizeOrigin + originAllowed"
      contains: "originAllowed"
    - path: "internal/server/headers.go"
      provides: "securityHeaders 中间件（tlsOn 分支感知 HSTS）"
      contains: "securityHeaders"
    - path: "internal/server/tls.go"
      provides: "TLSConfig（MinVersion 1.2 + 6 AEAD 清单）"
      contains: "TLSConfig"
    - path: "internal/server/tls_test.go"
      provides: "TLS 版本/cipher 下限矩阵 + 安全头双分支断言"
      contains: "TestTLSVersionAndCipherFloor"
  key_links:
    - from: "internal/server/auth.go"
      to: "cmd/wesh/main.go"
      via: "ParseCredential 导出供 03-04 --credential flag 的 fs.Func 回调 parse 期校验；Credential 经 Options.Credentials 入 New"
      pattern: "ParseCredential"
    - from: "internal/server/origin.go"
      to: "internal/server/server.go"
      via: "NormalizeOrigin 结果（规范化串）喂 03-03 的 AcceptOptions.OriginPatterns 与 /api/attach originAllowed 检查"
      pattern: "NormalizeOrigin"
    - from: "internal/server/tls.go"
      to: "cmd/wesh/main.go"
      via: "03-04 run() TLS 分岔 hs.TLSConfig = server.TLSConfig() 后 ServeTLS"
      pattern: "TLSConfig"
---

<objective>
落地四个零依赖纯组件：凭据预哈希常数时间比较（auth.go）、Origin 规范化与检查（origin.go）、安全响应头中间件（headers.go）、TLS 声明式配置（tls.go），各自配单元/集成测试。本 plan 不碰 server.go 装配与 main.go——与 03-01 并行同属 Wave 1，产出供 03-03（server 集成）与 03-04（main 装配）消费。

Purpose: SEC-01 的比较正确性、SEC-04 的规范化正确性、SEC-05 的 TLS/安全头形态全部可在组件层独立证明（RESEARCH 三个一手源码核实事实的直接落地）；组件先钉死，集成 plan 零语义争议。
Output: auth.go/origin.go/headers.go/tls.go 四组件 + auth_test.go/origin_test.go/tls_test.go 三测试文件全绿。

规格探针标记（spec-less probe fallback）：SEC-01/SEC-04/SEC-05 探针报为 unclassified——按 §C 保持 unresolved，不自动核销；边界按 D-06/D-12/D-13 决策与 RESEARCH Pattern 2/4/5/6 定稿落入 truths，「常数时间耗时正交」不可移植断言以 backstop 标记（走查为证），五条 must-NOT 经 §B 回忆落入 prohibitions（descriptor-less，下游 dispose flagged-unverified），无静默丢弃。

**planner 修正（RESEARCH Pattern 2 erratum）**：RESEARCH 行 288-297 的 matchCredential 定稿代码用 `matched &= ...` 且 matched 初值 0——`0 & x` 恒 0，结果永为 false（正确凭据永远拒绝）。本 plan 修正为 `matched |= (user 比较) & (pass 比较)`（初值 0，位或累积不短路，保持"耗时与组数正交"设计意图）。executor 必须按修正后形态实现，测试覆盖"多组各自可匹配"直接锁死该回归。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth/03-CONTEXT.md
@.planning/phases/03-auth/03-RESEARCH.md
@.planning/phases/03-auth/03-PATTERNS.md
@internal/server/server.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: auth.go——凭据预哈希与常数时间比较（SEC-01）</name>
  <files>internal/server/auth.go, internal/server/auth_test.go</files>
  <read_first>
    - .planning/phases/03-auth/03-RESEARCH.md（Pattern 2 行 265-318——比较组件形态；Pitfall 1 行 591-595 长度泄露；注意本 plan objective 的 erratum 修正，RESEARCH 的 &= 代码不可照抄）
    - .planning/phases/03-auth/03-PATTERNS.md（§auth.go 98-131 行——401 同文纪律与日志红线预埋点说明；basicAuth 中间件本体属 03-03，本任务不建）
    - .planning/phases/03-auth/03-CONTEXT.md（D-01 多组凭据按人撤销、D-02 整站 Basic 形态）
    - internal/server/server.go（headerHasToken 行 176-188——精确比较纪律的同包参照）
  </read_first>
  <behavior>
    - ParseCredential("alice:s3cret") 成功；ParseCredential("alice:pass:with:colon") 成功（密码含冒号，首个 ':' 切分）；ParseCredential("nocolon") 与 ParseCredential(":pass") 返回错误
    - matchCredential(creds, "alice", "s3cret") == true；错 user/错 pass/空 creds 列表均 == false
    - 多组凭据（3 组）各自正确组合均 == true；跨组错配（user1+pass2）== false
    - 空字符串 user/pass 对非空凭据 == false
  </behavior>
  <action>
    新建 `internal/server/auth.go`（package server）：
    1. 导出类型 `Credential` struct（字段不导出：userHash [sha256.Size]byte; passHash [sha256.Size]byte）——跨包需求：03-04 main.go 经 Options.Credentials 传入，类型必须导出；字段不导出保不变量（只能经 ParseCredential 构造，保证永远是预哈希摘要而非明文）。类型注释写清不变量：SHA-256 等长化 32B 消除长度侧信道（subtle.ConstantTimeCompare 长度不等立即返回 0，Pitfall 1）。
    2. 导出 `ParseCredential(s string) (Credential, error)`：strings.Cut(s, ":") 切首个冒号（密码可含 ':'，user 不可含——RFC 7617 user-id 约束）；!ok 或 u == "" 返回 error（"credential must be user:pass" 形态文案）；成功返回双方 sha256.Sum256 预哈希的 Credential。
    3. 内部 `matchCredential(creds []Credential, user, pass string) bool`：对入参 user/pass 各做 sha256.Sum256；matched := 0；逐组循环 `matched |= subtle.ConstantTimeCompare(uh[:], c.userHash[:]) & subtle.ConstantTimeCompare(ph[:], c.passHash[:])`（位或累积——**不短路不 break**，循环恒跑满全部组，耗时与组数正交、无组序号时序泄露；注释记录 planner erratum：RESEARCH Pattern 2 的 &= 初值 0 恒 false，本实现为修正形态）；返回 matched == 1。注释注明：无凭据模式（creds 空）调用方不进此函数。
    4. 本任务不创建 basicAuth 中间件（其节流钩子依赖 03-01 的 throttleStore，Wave 2 的 03-03 负责装配进本文件）。
    新建 `internal/server/auth_test.go`（package server 白盒——访问不导出字段与 matchCredential）：
    5. TestParseCredential 表驱动：正常/密码含冒号/无冒号报错/空 user 报错/空 pass 合法（user:→ 合法，passHash 为空串摘要——不额外禁止，文档化决策）。
    6. TestCredentialMatch：单组正/错 user/错 pass/空列表 false；三组各自命中 true；跨组错配 false；空 user/pass 对非空凭据 false。注释写明：常数时间属性由构造保证（subtle+定长摘要+不短路循环），时序测量断言不可移植不做（backstop truth 对应物）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test -race -count=1 -v -run 'TestParseCredential|TestCredentialMatch' ./internal/server/ && go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - auth.go 含导出 Credential（字段不导出）+ ParseCredential + 不导出 matchCredential；matchCredential 循环内无 break/条件 return，累积运算符为 |=（erratum 修正形态）
    - ConstantTimeCompare 操作数全部来自 sha256.Sum256 的 [32]byte（无原始字符串操作数）
    - TestParseCredential ≥5 行（含密码含冒号、两类报错）；TestCredentialMatch 含多组各自命中与跨组错配断言
    - `go test -race -count=1 ./internal/server/` 退出 0
  </acceptance_criteria>
  <done>SEC-01 比较核心锁定：等长化 + subtle + 多组不短路轮询；Credential 导出面就绪（03-04 flag 解析与 03-03 中间件装配的消费点）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: origin.go——Origin 规范化与白名单检查（SEC-04）</name>
  <files>internal/server/origin.go, internal/server/origin_test.go</files>
  <read_first>
    - .planning/phases/03-auth/03-RESEARCH.md（Pattern 4 行 389-450——库语义一手核实结论与 normalizeOrigin 定稿代码逐字采用；Pitfall 3 行 603-607 默认端口不对称）
    - .planning/phases/03-auth/03-PATTERNS.md（§origin.go 134-143 行——拒绝形态与双端点执行挂点预告）
    - .planning/phases/03-auth/03-CONTEXT.md（D-12 规范化精确比较、D-13 无 Origin 放行/有 Origin 必查）
    - internal/server/server.go（headerHasToken 行 176-188——头解析精确比较纪律参照）
  </read_first>
  <behavior>
    - NormalizeOrigin("https://Wesh.Example.com:443") == "https://wesh.example.com"（小写 host + 剥离默认端口）；http:80 同剥离；非默认端口保留（http://localhost:8080 原样）
    - NormalizeOrigin 拒绝：无 scheme/非 http(s) scheme/空 host/含 path（除裸 "/"）/含 query/含 fragment/含 userinfo/含 glob 字符 *?[\\ 任一
    - originAllowed：无 Origin 头 → true；Origin 与 r.Host 同源（大小写不敏感）→ true；Origin 在规范化集合内 → true；不在集合的跨源 → false；Origin: null → false
    - 空集合时行为 = 库默认（仅无 Origin/同源放行）——零配置零行为漂移（D-12）
  </behavior>
  <action>
    新建 `internal/server/origin.go`（package server）：
    1. 导出 `NormalizeOrigin(s string) (string, error)`（跨包需求：03-04 main.go 的 --origin fs.Func 回调 parse 期校验）：url.Parse；scheme 仅 http/https 且 Host 非空；Path 非空且非 "/" 即拒、RawQuery/Fragment/User 任一非空即拒；strings.ContainsAny(s, "*?[\\\\") 即拒（path.Match 退化为精确比较，D-12）；host = ToLower(u.Hostname())；port 为 scheme 默认端口（http:80/https:443）时剥离（浏览器 Origin 序列化省略默认端口，RFC 6454——Pitfall 3）；port 非空时 net.JoinHostPort 重组；返回 scheme + "://" + host。错误文案含原输入（flag 解析期报错面向部署者）。
    2. 内部 `originAllowed(r *http.Request, allowed map[string]struct{}) bool`：Origin 头为空返回 true（D-13 非浏览器客户端放行；CSWSH 威胁模型只约束浏览器，浏览器必发 Origin）；url.Parse 失败返回 false；strings.EqualFold(r.Host, u.Host) 同源返回 true；入站 Origin 经同一规范化逻辑（小写 host + 剥默认端口）后查 allowed 集合，命中 true，否则 false。注释注明：与 coder/websocket accept.go:228-264 语义逐项对齐（一手核实）；Origin: null 经 url.Parse 后 Host 为空、不同源不命中集合 → 拒绝（CSWSH 载体，正确行为）；规范化失败按拒绝处理（保守方向）。
    3. 全部注释中文，锚点 D-12/D-13/Pitfall 3/accept.go 行号。
    新建 `internal/server/origin_test.go`（package server 白盒）：
    4. TestNormalizeOrigin 表驱动 ≥10 行：默认端口剥离两 scheme、非默认端口保留、大写 host 小写化、裸 "/" path 放行、含路径/query/fragment/userinfo/glob 字符/无 scheme/ftp scheme/空 host 各拒绝。
    5. TestOriginAllowed 表驱动（httptest.NewRequest 构造，直接设置 Header 与 Host）：无 Origin true/同源（含大小写差）true/集合内 true/跨源不在集合 false/null false/畸形 Origin false/空集合仅同源 true。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test -race -count=1 -v -run 'TestNormalizeOrigin|TestOriginAllowed' ./internal/server/ && go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - origin.go 含导出 NormalizeOrigin 与不导出 originAllowed；规范化含小写化与默认端口剥离；glob 字符拒绝
    - originAllowed 四段语义（空放行/同源放行/集合查找/否则拒）与库对齐；null Origin 拒绝有专断言行
    - TestNormalizeOrigin ≥10 行表驱动；TestOriginAllowed ≥7 行表驱动含 null 与空集合
    - `go test -race -count=1 ./internal/server/` 退出 0
  </acceptance_criteria>
  <done>SEC-04 的规范化与检查语义组件级锁定，供 03-03 挂入 /ws 守卫区 ⓪ + AcceptOptions.OriginPatterns + /api/attach 中间件三处执行点。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: headers.go + tls.go——安全响应头中间件与 TLS 声明式配置（SEC-05）</name>
  <files>internal/server/headers.go, internal/server/tls.go, internal/server/tls_test.go</files>
  <read_first>
    - .planning/phases/03-auth/03-RESEARCH.md（Pattern 5 行 452-498——TLSConfig 定稿与 GOROOT 核实事实；Pattern 6 行 500-537——安全头表与 CSP 说明；Pitfall 7 行 627-629 HSTS 分支；Pitfall 9 行 637-640 测试形态）
    - .planning/phases/03-auth/03-PATTERNS.md（§headers.go 146-172 行——中间件首例说明；§测试文件组 313-336 行——TLS 测试无仓内先例按 RESEARCH 定稿落地）
    - .planning/phases/03-auth/03-CONTEXT.md（D-06 TLS 强度与安全头集合、D-07 验证形态）
    - internal/server/server.go（Handler() 行 117-130——mux 装配参照；本任务产物由 03-03 在此装配）
  </read_first>
  <behavior>
    - securityHeaders 中间件：tlsOn=false 时响应含六项恒在头精确值且无 Strict-Transport-Security；tlsOn=true 时追加 HSTS "max-age=63072000; includeSubDomains"
    - TLSConfig()：MinVersion == tls.VersionTLS12；CipherSuites 恰为 6 个 AEAD 套件（ECDSA/RSA × AES-128-GCM/AES-256-GCM/ChaCha20-Poly1305）
    - tls.Dial 矩阵：客户端 MaxVersion 1.1 → 握手失败；MaxVersion 1.2 → 成功；零值客户端 → 成功且 ConnectionState().Version == tls.VersionTLS13；客户端仅 CBC 套件（MaxVersion 1.2）→ 握手失败
  </behavior>
  <action>
    新建 `internal/server/headers.go`（package server）：
    1. `securityHeaders(next http.Handler, tlsOn bool) http.Handler`——本仓首个 func(http.Handler) http.Handler 中间件先例，保持最小形态不引入框架。逐字采用 RESEARCH Pattern 6 定稿头表：CSP `default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self'; frame-ancestors 'none'; base-uri 'none'; form-action 'none'`（unsafe-inline 为单文件全内联现实的已裁决接受项，D-06；connect-src 'self' 在 CSP3 覆盖同源 ws/wss——注释记 A1 假设与老 Safari 回退方案）；X-Frame-Options: DENY（frame-ancestors 老浏览器兼容）；X-Content-Type-Options: nosniff；Referrer-Policy: strict-origin-when-cross-origin；Cross-Origin-Opener-Policy: same-origin；Cross-Origin-Resource-Policy: same-origin；tlsOn 时追加 Strict-Transport-Security: max-age=63072000; includeSubDomains（OWASP 基线两年值，Open Question 3 裁决落地；去 preload——自托管工具不进预载表）。注释注明：Go 默认不发 Server/X-Powered-By 无需移除；废弃头（X-XSS-Protection 等）不发送。
    新建 `internal/server/tls.go`（package server）：
    2. 导出 `TLSConfig() *tls.Config`（跨包需求：03-04 main.go 分岔装配）：MinVersion: tls.VersionTLS12；CipherSuites 显式六件——TLS_ECDHE_ECDSA_WITH_AES_128_GCM_SHA256、TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256、TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384、TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384、TLS_ECDHE_ECDSA_WITH_CHACHA20_POLY1305_SHA256、TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256。注释写明 GOROOT 1.26.3 一手核实事实：默认列表含 4 个 ECDHE CBC-SHA1（cipher_suites.go:283-312），不设显式清单 testssl.sh 必报弱项（Pitfall 2）；TLS 1.3 不可配（三套件全 AEAD）；MaxVersion 不设（默认 1.3，D-06 默认协商 1.3）；PreferServerCipherSuites 已废弃不使用。
    新建 `internal/server/tls_test.go`（package server 白盒）：
    3. 自签证书 helper（约 40 行 stdlib 模式）：ecdsa.P256 生成密钥 + x509.CreateCertificate 自签（DNSNames 含 localhost，NotBefore/NotAfter 窗口，ExtKeyUsage ServerAuth）→ tls.Certificate。禁止落盘 fixture（Pitfall 9）。
    4. TestTLSVersionAndCipherFloor：httptest.NewUnstartedServer 或 tls.NewListener 装配（Handler 任意 200 响应），TLSConfig = TLSConfig()；表驱动四例——tls11-rejected（Min 1.0/Max 1.1，wantErr）、tls12-ok（Min/Max 1.2）、tls13-default（零值 client，追加断言 ConnectionState().Version == tls.VersionTLS13）、cbc-only-rejected（Max 1.2 + 仅 TLS_ECDHE_RSA_WITH_AES_128_CBC_SHA，wantErr）。全部客户端 InsecureSkipVerify: true（自签证书；注释注明测试内正当用途）。握手失败断言 = tls.Dial/Handshake 返回错误，不区分哪侧拒绝（Pitfall 9 纪律）。
    5. TestSecurityHeaders：httptest.NewRecorder + 直调 securityHeaders（无需真监听）——tlsOn=false 分支断言六项恒在头精确值且 Response.Header 无 Strict-Transport-Security；tlsOn=true 分支断言 HSTS 精确值。CSP 值逐字符精确断言（安全契约，防手滑漂移）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test -race -count=1 -v -run 'TestTLSVersionAndCipherFloor|TestSecurityHeaders' ./internal/server/ && go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - tls.go TLSConfig() 返回 MinVersion 1.2 + 恰 6 个 AEAD 套件；无 MaxVersion/PreferServerCipherSuites 设置
    - headers.go securityHeaders 恒在六项精确值 + tlsOn 分支 HSTS；中间件为最小形态无框架引入
    - TestTLSVersionAndCipherFloor 四例全过（1.1 败/1.2 成/1.3 默认/CBC 败）；TestSecurityHeaders 双分支精确值断言
    - 测试无 *.pem fixture、无 GODEBUG 依赖；`go test -race -count=1 ./internal/server/` 退出 0
  </acceptance_criteria>
  <done>SEC-05 两个声明式组件锁定：TLS 下限/cipher 清单与 OWASP 安全头集合，D-07 自动化断言矩阵落地（testssl.sh 手动部分属 03-06 UAT 清单）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 攻击者可控输入 → 凭据比较/Origin 检查 | Authorization 头与 Origin 头完全不可信；比较与规范化必须无侧信道、无绕过 |
| 客户端 ← TLS/安全头 | 传输层强度与浏览器安全策略是服务端对客户端的保护契约 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Information Disclosure | 凭据时序侧信道（长度泄露/逐字节短路/组序号泄露） | high | mitigate | SHA-256 等长化 + subtle + 逐组 \|= 不短路（erratum 修正形态）；TestCredentialMatch 多组断言；backstop truth 走查点 |
| T-03-07 | Elevation of Privilege | CSWSH 跨站 WS 劫持（邪恶页面借受害者浏览器凭据建连） | high | mitigate | originAllowed 与库语义对齐四段检查（D-13）；null Origin 拒绝专断言；03-03 双端点执行 + 03-06 UAT 403 场景 |
| T-03-08 | Tampering | Origin 白名单绕过（默认端口不对称/glob 注入/大小写） | high | mitigate | NormalizeOrigin 剥默认端口 + 拒 glob 字符 + 小写化（Pitfall 3）；TestNormalizeOrigin ≥10 行表驱动 |
| T-03-09 | Tampering | TLS 降级攻击（1.0/1.1 或 CBC 弱套件协商） | high | mitigate | MinVersion 1.2 + 显式 6 AEAD 清单声明式拒绝（Pitfall 2）；TestTLSVersionAndCipherFloor 四例锁定；testssl.sh 手动复核（03-06） |
| T-03-10 | Tampering | Clickjacking（终端页被 iframe 嵌套钓鱼） | medium | mitigate | frame-ancestors 'none' + X-Frame-Options: DENY 双发（D-06）；TestSecurityHeaders 精确值断言 |
| T-03-11 | Information Disclosure | HSTS 误发明文分支致反代策略冲突/配置混乱暴露 | low | mitigate | tlsOn 分支参数（Pitfall 7）；TestSecurityHeaders 双分支断言 |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | 零新增依赖（RESEARCH §Package Legitimacy Audit——全 stdlib） |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/server/` 全绿（新增五测 + 既有全部）
- `go build ./... && go vet ./...` 退出 0
- 代码评审：matchCredential 为 erratum 修正形态（|= 不短路）；TLSConfig 无 1.3 cipher 死代码；CSP 值与 RESEARCH Pattern 6 逐字符一致
</verification>

<success_criteria>
- SEC-01 比较核心、SEC-04 规范化核心、SEC-05 TLS/安全头核心全部组件级可测落地
- D-07 的 Go 自动化断言（1.1 败/1.2 成/1.3 默认/CBC 败）成立
- 03-03 集成的全部组件输入面（Credential/ParseCredential/NormalizeOrigin/originAllowed/securityHeaders/TLSConfig）就绪
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `internal/server/auth.go`：`Credential` 类型（导出，字段不导出）、`ParseCredential`（导出）、`matchCredential`（不导出）
- `internal/server/origin.go`：`NormalizeOrigin`（导出）、`originAllowed`（不导出）
- `internal/server/headers.go`：`securityHeaders` 中间件（不导出）
- `internal/server/tls.go`：`TLSConfig`（导出）
- `internal/server/auth_test.go`：`TestParseCredential`/`TestCredentialMatch`；`internal/server/origin_test.go`：`TestNormalizeOrigin`/`TestOriginAllowed`；`internal/server/tls_test.go`：`TestTLSVersionAndCipherFloor`/`TestSecurityHeaders` + 自签证书 helper

<output>
Create `.planning/phases/03-auth/03-02-SUMMARY.md` when done
</output>
