---
phase: 03-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/proto/proto.go
  - internal/proto/proto_test.go
  - internal/server/tickets.go
  - internal/server/tickets_test.go
  - internal/server/throttle.go
  - internal/server/throttle_test.go
autonomous: true
requirements:
  - SEC-02
  - SEC-03

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "HelloPayload 新增可选字段 Ticket（json tag `ticket,omitempty`），DecodeHello 往返保留 ticket 值；未知字段忽略纪律不受影响（P2 D-02 演化纪律，proto.go 注释预告兑现）"
    - "proto Error code 表新增 ErrAuthFailed = \"auth_failed\"（发 Error 帧 + 1008 关闭、close reason 同名机器串语义；P2 deferred 挂账兑现，D-10）；注释块中 auth_failed 的 deferred 表述改为已兑现，permission_denied 仍属 Phase 5"
    - "ticket 签发 = crypto/rand 16 字节 → base64.RawURLEncoding 22 字符，登记 map[ticket]{mode, exp}；签发顺手机会性清扫过期项（惰性清理，无常驻 goroutine）"
    - "ticket 核销 = map 原子查删：查即删保证单次使用；过期按不存在处理；重放/过期/非法同归 false（D-10 统一口径无 oracle）"
    - "ticket TTL 默认 60s（ROADMAP 锁定），ticketStore.ttl 可经 Options 覆写供测试（HelloTimeout 先例）；ticketEntry.mode 绑定签发时权限级别（D-11，Phase 5 ro/rw 分签发的占位字段）"
    - "ticket 与静态凭据是独立 secret——crypto/rand 直接生成，不从凭据派生（PITFALLS C6 锁定项）"
    - "throttleStore per-IP 指数退避：第 n 次失败后 notBefore = now + min(cap, base << (n-1))，生产默认 base=1s、cap=30s（级数 1/2/4/8/16/30s 封顶，D-09 研究标定）；窗口内命中不延长 notBefore"
    - "认证成功 recordSuccess delete(ip) 清零（D-08）；条目 15min 无活动在 recordFail 内惰性过期重置（Pitfall 4 内存界纪律，halfOpenCounter 同款）"
    - "退避参数下爆破 100 次累计等待 ≥47 分钟、稳态限速 2 次/分钟/IP（ROADMAP 准则 2「爆破 100 次触发可观测退避」锚点）"
    - "ticketStore 与 throttleStore 均为 mu+map 最小形态，零值可用字段经 New 兜底默认常量（defaultTicketTTL/defaultThrottleBase/defaultThrottleCap），不开常驻 janitor goroutine（零新 exitf 分支纪律）"
  prohibitions:
    - "ticket 不得经 URL query 或 WS 子协议头传输（query 进访问日志/浏览器历史——ARCHITECTURE §2.8 锁定；Hello JSON 载荷是唯一通道）"
    - "ticket 随机源禁止 math/rand、时间戳哈希或凭据派生（可预测 ticket = 认证绕过）"
    - "节流禁止以 sleep/延迟响应实现（挂住 goroutine+fd 放大慢速攻击）——失败即拒，HTTP 侧以 429 + Retry-After 告知"
  artifacts:
    - path: "internal/proto/proto.go"
      provides: "ErrAuthFailed 常量 + HelloPayload.Ticket 可选字段"
      contains: "ErrAuthFailed"
    - path: "internal/server/tickets.go"
      provides: "ticketStore（issue/redeem）+ ticketEntry{mode, exp}"
      contains: "ticketStore"
    - path: "internal/server/tickets_test.go"
      provides: "ticket 单次使用/TTL/惰性清扫单元测试"
      contains: "TestTicketStore"
    - path: "internal/server/throttle.go"
      provides: "throttleStore（allow/recordFail/recordSuccess）+ throttleEntry{fails, notBefore, lastSeen}"
      contains: "throttleStore"
    - path: "internal/server/throttle_test.go"
      provides: "退避级数/封顶/成功清零/惰性过期单元测试"
      contains: "TestThrottleStore"
  key_links:
    - from: "internal/server/tickets.go"
      to: "internal/server/server.go"
      via: "halfOpenCounter（server.go:136-162）mu+map+删 key 纪律的同构复制"
      pattern: "halfOpenCounter"
    - from: "internal/proto/proto.go"
      to: "web/src/main.ts"
      via: "帧常量/Error code 前后端手工对齐纪律（D-16）——proto 加 ErrAuthFailed 后 03-05 前端按 auth_failed 分派"
      pattern: "auth_failed"
---

<objective>
落地 Phase 3 的两个协议契约增量（ErrAuthFailed、HelloPayload.Ticket）与两个纯内存存储组件（ticketStore 一次性票表、throttleStore per-IP 指数退避计数器）。本 plan 全是零依赖纯组件：不碰 server.go 装配，产出物经单元测试锁定语义，供 03-03 集成 tracer 直接消费。

Purpose: ticket 生命周期与节流计数器是本 phase 的领域逻辑核心（RESEARCH：唯一需要手写的语义层）；先把它们以纯函数形态（now 参数注入）钉死，集成 plan 才能只做接线不测语义。
Output: proto.go 契约增量 + tickets.go/throttle.go 两组件 + 各自单元测试全绿。

规格探针标记（spec-less probe fallback）：SEC-02/SEC-03 探针报为 unclassified——按 §C 保持 unresolved，不自动核销；边界按 D-08/D-09/D-10/D-11 决策与 RESEARCH Pattern 1/3 定稿落入 must_haves.truths，三条 must-NOT（ticket 传输通道/随机源/sleep 节流）经 §B 回忆落入 prohibitions（descriptor-less，下游 dispose flagged-unverified），无静默丢弃。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth/03-CONTEXT.md
@.planning/phases/03-auth/03-RESEARCH.md
@.planning/phases/03-auth/03-PATTERNS.md
@internal/proto/proto.go
@internal/proto/proto_test.go
@internal/server/server.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: proto.go 契约增量——ErrAuthFailed + HelloPayload.Ticket</name>
  <reversibility rating="costly">D-10：Error code 入表即前后端公开契约（P2 D-07 同评级），更名/改义牵动前后端两侧；用户已在 CONTEXT 锁定，仅记录不插 gate。</reversibility>
  <files>internal/proto/proto.go, internal/proto/proto_test.go</files>
  <read_first>
    - internal/proto/proto.go（被改文件现状：Error code 表 43-46 行、HelloPayload 71-75 行、两处注释预告位 42 与 70 行）
    - internal/proto/proto_test.go（被改文件现状：TestDecodeHello 表驱动含 "unknown fields ignored" 行——该行的 ticket:"secret" 当前是未知字段，加字段后变为解码入 Ticket，测试断言仍成立）
    - .planning/phases/03-auth/03-CONTEXT.md（D-10 核销统一口径、D-11 ticket 绑 mode）
    - .planning/phases/03-auth/03-PATTERNS.md（§proto.go 改 211-240 行——两处增量的逐字形态与前端对齐纪律）
  </read_first>
  <behavior>
    - DecodeHello(`{"version":"wesh.v1","cols":80,"rows":24,"ticket":"abc123"}`) → ok=true 且 hp.Ticket=="abc123"
    - DecodeHello(无 ticket 字段载荷) → hp.Ticket==""（omitempty 只影响编码方向，解码零值）
    - 既有 unknown-fields 行（含 ticket/attach 混合载荷）→ 仍 ok=true，Version/Cols/Rows 断言不变
    - ErrAuthFailed 常量值 == "auth_failed"（snake_case 机器串，RFC6455 close reason ≤123 字节约束满足）
  </behavior>
  <action>
    1. `internal/proto/proto.go`：
       - Error code 表（43-46 行）追加一行：`ErrAuthFailed = "auth_failed"`，行尾注释写明语义——ticket 核销失败统一口径（过期/非法/重放/节流中同口径，无 oracle，D-10），发 Error 帧 + 1008，属正常客户端可见码（D-06 受众分治）。
       - 表上方注释块（39-42 行）中「auth_failed/permission_denied 属 Phase 3/5（deferred）」改为「auth_failed 已于 Phase 3 兑现；permission_denied 属 Phase 5（deferred）」。
       - HelloPayload（71-75 行）追加字段 `Ticket string \`json:"ticket,omitempty"\``；结构体上方注释中「Phase 3 加 ticket」表述改为已落地表述（Phase 5 加 attach/mode 仍只是加字段）。
    2. `internal/proto/proto_test.go`：TestDecodeHello 表加两行——"ticket round-trip"（载荷含 ticket 字段，断言 hp.Ticket 精确相等）与"ticket omitted"（无 ticket 载荷，断言 hp.Ticket==""）；表结构加 wantTicket 字段，既有行补零值。禁止改动既有 unknown-fields 行（它是 D-02 演化纪律的回归锁）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test -count=1 -v -run TestDecodeHello ./internal/proto/ && go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - proto.go 含 `ErrAuthFailed = "auth_failed"` 且注释写明 D-10 统一口径语义
    - proto.go HelloPayload 含 `Ticket string` 且 json tag 为 `ticket,omitempty`
    - TestDecodeHello 新增两行断言 Ticket 字段往返与缺省零值；既有五行行为不变
    - `go test ./internal/proto/` 退出 0；`go build ./... && go vet ./...` 退出 0
  </acceptance_criteria>
  <done>auth_failed 机器串与 ticket 字段入契约，前后端对齐面就绪（03-03 服务端核销分支与 03-05 前端重试均消费此契约）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tickets.go——一次性 ticket 签发/核销存储</name>
  <files>internal/server/tickets.go, internal/server/tickets_test.go</files>
  <read_first>
    - internal/server/server.go（halfOpenCounter 行 132-162——mu+map+防单调增长纪律的同构模拟；Options 零值兜底 90-99 行）
    - .planning/phases/03-auth/03-RESEARCH.md（Pattern 1 行 209-263——ticketStore/ticketEntry/issue/redeem 定稿代码逐字采用，含关键决策：不走 query/子协议头、独立 secret、惰性清理无 janitor）
    - .planning/phases/03-auth/03-PATTERNS.md（§tickets.go 29-68 行——复制要点与类型注释不变量风格）
    - .planning/phases/03-auth/03-CONTEXT.md（D-10/D-11）
  </read_first>
  <behavior>
    - issue(mode, now) 返回 22 字符 base64url 串，两次签发互不相同；签发的 ticket 可在 map 中查到且 exp == now+ttl、mode 精确绑定
    - redeem 首次成功返回 (mode, true)；同一 ticket 第二次 redeem 返回 ("", false)（单次使用，查即删）
    - 过期 ticket redeem 返回 ("", false) 且同样被删除（过期/重放/非法同归 false，D-10 无 oracle）
    - 从未签发的 ticket（含畸形串、空串）redeem 返回 ("", false)
    - 惰性清扫：签发时遍历删除已过期项（map 不随历史签发单调增长，Pitfall 4）
  </behavior>
  <action>
    新建 `internal/server/tickets.go`（package server，内部包不导出）：
    1. `ticketEntry` struct{ mode string; exp time.Time }——mode 取 proto.ModeRO/ModeRW，签发时 = 全局 --writable 模式（D-11）。
    2. `ticketStore` struct{ mu sync.Mutex; m map[string]ticketEntry; ttl time.Duration } + `newTicketStore(ttl time.Duration) *ticketStore`（ttl <= 0 取 defaultTicketTTL = 60 * time.Second 包级常量）。类型注释写清不变量：核销即删（单次使用）、签发顺手清扫过期项（无常驻 goroutine——零新 exitf 分支纪律）。
    3. `issue(mode string, now time.Time) string`：crypto/rand.Read 16 字节 → base64.RawURLEncoding.EncodeToString（22 字符）；mu 锁内先清扫 now.After(e.exp) 的过期项再登记 `m[t] = ticketEntry{mode, now.Add(ttl)}`。注释注明：ticket 与凭据是独立 secret（crypto/rand 直生不派生，PITFALLS C6）；rand.Read 失败沿用 Go 惯例处理。
    4. `redeem(t string, now time.Time) (mode string, ok bool)`：mu 锁内查表——未命中返回 ("", false)；命中先 delete 再判 now.After(e.exp)，过期返回 ("", false)，否则 (e.mode, true)。注释注明：查即删使重放/过期/非法同归 false（D-10 统一口径，不给攻击者区分 oracle）。
    5. 全部注释中文，引用 D-10/D-11/C6/Pitfall 4 锚点（仓内注释纪律）。
    新建 `internal/server/tickets_test.go`（package server——内部类型同包白盒测试，不走 server_test 黑盒；与既有测试文件包名不同属有意选择，go test 兼容同目录双包）：
    6. TestTicketStore 表驱动+时序注入（now 手工推进，零 sleep）：签发形态（22 字符/两次互异/mode 与 exp 精确）→ 首核销成功且 mode 正确 → 重放 false → 过期 false（now 推过 ttl）且已删除 → 未签发/空串 false → 惰性清扫断言（先签发短命 ticket，now 推进后再签发，断言 map 长度回落）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test -race -count=1 -v -run TestTicketStore ./internal/server/ && go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - tickets.go 含 ticketStore/ticketEntry/newTicketStore/issue/redeem，issue 用 crypto/rand + base64.RawURLEncoding，redeem 命中即 delete 再判过期
    - newTicketStore 零值 ttl 兜底 60s；代码无 time.Sleep、无 time.Ticker、无新建 goroutine
    - TestTicketStore 覆盖：单次使用/重放 false/过期 false/非法 false/惰性清扫，全部经 now 注入无真实 sleep
    - `go test -race -count=1 ./internal/server/` 退出 0（既有测试不受影响）
  </acceptance_criteria>
  <done>一次性 ticket 存储语义单元级锁定：单次使用/60s TTL/mode 绑定/独立 secret/无 oracle，供 03-03 /api/attach 签发与 Hello 核销直接装配。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: throttle.go——per-IP 指数退避节流计数器</name>
  <files>internal/server/throttle.go, internal/server/throttle_test.go</files>
  <read_first>
    - internal/server/server.go（halfOpenCounter 行 132-162 同构模拟；clientIP 行 164-174——节流键提取直接复用此函数不重写）
    - .planning/phases/03-auth/03-RESEARCH.md（Pattern 3 行 320-376——throttleStore/throttleEntry/allow/recordFail/recordSuccess 定稿代码与参数标定表；Pitfall 5 行 615-619 map 单调增长防线）
    - .planning/phases/03-auth/03-CONTEXT.md（D-08 两处失败统一计数器、D-09 参数交 research 标定）
  </read_first>
  <behavior>
    - 未知 IP allow 返回 true；第 1 次失败后 notBefore = now+1s（base），窗口内 allow 返回 false 且不延长 notBefore
    - 连续失败退避级数：1s→2s→4s→8s→16s→30s→30s（cap 封顶，位移即 ×2 幂，fails 计数经 min(fails-1, 5) 限幅）
    - recordSuccess 后该 IP 条目删除（D-08 成功清零），下次失败从 base 重新起级
    - lastSeen 超 15min 的条目在 recordFail 时惰性重置（视为新 IP 从 base 起级；map 内存界防线）
    - base/cap 经 newThrottleStore 参数注入（生产 1s/30s，测试 ms 级提速——HelloTimeout 先例）
  </behavior>
  <action>
    新建 `internal/server/throttle.go`（package server）：
    1. `throttleEntry` struct{ fails int; notBefore time.Time; lastSeen time.Time }。
    2. `throttleStore` struct{ mu sync.Mutex; m map[string]throttleEntry; base, cap time.Duration } + `newThrottleStore(base, cap time.Duration) *throttleStore`（零值分别兜底包级常量 defaultThrottleBase = 1 * time.Second、defaultThrottleCap = 30 * time.Second——D-09 研究标定：OWASP 原文 1s 翻倍锚点 + 100 次爆破累计 ≥47min 验收锚点；注释写明不做临时锁定的理由：个人工具场景，封顶退避已达稳态 2 次/分钟/IP，锁定状态机增加可区分 oracle）。类型注释写清不变量：成功清零、15min 惰性过期、窗口内命中不延长 notBefore（恢复期可预期）。
    3. `allow(ip string, now time.Time) bool`：查无条目或 now 不早于 notBefore 返回 true；窗口内返回 false（只读不改条目——不延长窗口）。
    4. `recordFail(ip string, now time.Time)`：mu 锁内取条目；now.Sub(lastSeen) > 15min 重置为零值条目；fails++；d = base << min(fails-1, 5)，超 cap 截断；notBefore = now+d；lastSeen = now；写回 map。
    5. `recordSuccess(ip string)`：mu 锁内 delete（D-08）。
    6. 注释注明内存界：条目约 56B，4096 恶意 IP ≈ 230KB 可接受；惰性过期已回收非活跃条目，不设硬上限驱逐（RESEARCH Pattern 3 裁决——随机驱逐复杂度换不到安全收益）。
    新建 `internal/server/throttle_test.go`（package server 白盒，同 tickets_test.go 纪律）：
    7. TestThrottleStore（now 注入零 sleep）：未知 IP allow true → fail#1 后窗口内 allow false、notBefore 恰好 now+base → 窗口内重复 allow 不延长 notBefore → 连续 7 次失败级数逐项断言（base<<0..base<<4、第 6 次起钉 cap）→ recordSuccess 后条目删除、再失败从 base 重启 → lastSeen 超 15min 惰性重置断言（先失败数次，now 推进 16min 后再失败，fails 回到 1 级）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && go test -race -count=1 -v -run TestThrottleStore ./internal/server/ && go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - throttle.go 含 throttleStore/throttleEntry/newThrottleStore/allow/recordFail/recordSuccess；退避位移级数与 cap 截断正确
    - recordSuccess 为 delete；recordFail 内含 15min 惰性过期重置；无 goroutine、无 sleep
    - TestThrottleStore 覆盖：级数 1/2/4/8/16/30/30、窗口内不延长、成功清零、惰性过期，全部 now 注入
    - `go test -race -count=1 ./internal/server/` 退出 0
  </acceptance_criteria>
  <done>per-IP 指数退避计数器语义单元级锁定（D-09 标定参数落地），供 03-03 接入 /api/attach 429 闸与 Hello 核销前置闸（D-08 统一计数器）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 攻击者可控输入 → ticket/节流状态机 | ticket 串（Hello 载荷）与失败事件（凭据错误/ticket 错误）驱动本 plan 两个 store 的状态迁移；输入不可信 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Spoofing | ticket 枚举/预测（在线猜测有效 ticket） | high | mitigate | crypto/rand 128bit + map 查表核销（2^128 空间在线枚举无意义）；prohibition 锁定禁止 math/rand/凭据派生 |
| T-03-02 | Spoofing | ticket 重放（截获后复用） | high | mitigate | redeem 原子查删（查即删单次使用）+ 60s TTL；TestTicketStore 重放 false 断言 |
| T-03-03 | Elevation of Privilege | 凭据在线爆破 | high | mitigate | per-IP 指数退避 1s×2 封顶 30s（100 次 ≥47min，稳态 2 次/分钟/IP）；TestThrottleStore 级数断言；03-03 接入双失败点统一计数（D-08） |
| T-03-04 | DoS | ticket/节流 map 单调增长（扫描器打满内存） | medium | mitigate | 核销即删 + 签发惰性清扫 + 15min 惰性过期 + 成功 delete；条目 56B×4096 IP ≈ 230KB 上界论证注释钉死（Pitfall 4/5） |
| T-03-05 | Information Disclosure | 核销结果 oracle（区分过期/非法/重放/节流） | medium | mitigate | 同归 false（D-10）+ 窗口内不延长 notBefore（恢复期不可区分）；单元测试同口径断言 |
| T-03-SC | Tampering | npm/pip/cargo installs | high | accept | 零新增依赖（RESEARCH §Package Legitimacy Audit——全 stdlib） |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/proto/ ./internal/server/` 全绿（新增两测 + 既有全部）
- `go build ./... && go vet ./...` 退出 0
- 代码评审：两个 store 均 mu+map 最小形态、无 goroutine、注释锚点齐全（D-08/D-09/D-10/D-11/C6/Pitfall 4）
</verification>

<success_criteria>
- SEC-02 的存储语义（单次使用/60s TTL/mode 绑定/独立 secret）与 SEC-03 的计数器语义（指数退避/封顶/成功清零/惰性过期）在纯组件层锁定
- auth_failed 与 ticket 字段入协议契约，03-03 集成 tracer 的全部前置件就绪
- ROADMAP 准则 2 的退避锚点（100 次 ≥47min）由参数级数直接推出并可测
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `internal/proto/proto.go`：`ErrAuthFailed` 常量、`HelloPayload.Ticket` 字段
- `internal/server/tickets.go`：`ticketStore` 类型（`newTicketStore`/`issue`/`redeem`）、`ticketEntry` 类型、`defaultTicketTTL` 常量（60s）
- `internal/server/throttle.go`：`throttleStore` 类型（`newThrottleStore`/`allow`/`recordFail`/`recordSuccess`）、`throttleEntry` 类型、`defaultThrottleBase`（1s）/`defaultThrottleCap`（30s）常量
- `internal/server/tickets_test.go`：`TestTicketStore`；`internal/server/throttle_test.go`：`TestThrottleStore`

<output>
Create `.planning/phases/03-auth/03-01-SUMMARY.md` when done
</output>
