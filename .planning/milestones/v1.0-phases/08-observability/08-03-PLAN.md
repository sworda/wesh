---
phase: 08-observability
plan: 03
type: execute
wave: 3
depends_on:
  - "08-02"
files_modified:
  - internal/server/health.go
  - internal/server/server.go
  - internal/server/health_test.go
autonomous: true
requirements:
  - OPS-06

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "GET /healthz 免认证返回 200 + 状态 JSON（D-07/D-10）：{\"status\":\"ok\",\"clients\":N,\"max_clients\":M,\"session_active\":bool}——凭据模式下无 Authorization 头同样 200（整站 Basic 闸唯一窄例外），探活器结构性可用"
    - "优雅关停进行中（SIGTERM/INT → 1001 广播后、进程退出前）/healthz 返回 503 + {\"status\":\"draining\",...}（D-11）——反代/编排不再向将死实例导新流"
    - "/healthz 根路径固定（D-09）：--base-path=/wesh 实例下 /healthz 仍 200、/wesh/healthz 不可达（无认证模式 404 / 凭据模式 401）；拒绝双挂"
    - "POST /healthz 返回 405 + Allow: GET（方法模式 + path-only fallback 成对注册——内建 405 会被 / 子树吞掉，sharetoken.go:122-128 先例）"
    - "session_active 数据源 = 新 sessionAlive atomic.Bool（New 置 true，lifecycle sess.Wait 返回后置 false）；clients = registry.n.Load()、max_clients = s.maxClients——hubMu 外读取全部 atomic（R-07 纪律）"
    - "运维端点与主服务同端口（D-12），securityHeaders 包裹自动继承（server.go:473 既定）；零新 CLI flag"
    - "全量 go test -race 绿 + 既有 UAT 脚本零回归"
  artifacts:
    - path: "internal/server/health.go"
      provides: "healthzHandler（draining 分支 + 状态 JSON 编码）"
      contains: "healthzHandler"
    - path: "internal/server/server.go"
      provides: "draining/sessionAlive 两 atomic.Bool 字段 + Handler() /healthz 双注册 + lifecycle sessionAlive 置 false + Shutdown draining 置位"
      contains: "sessionAlive"
    - path: "internal/server/health_test.go"
      provides: "TestHealthz（四字段/免认证/bp 固定/405/session_active 翻转）+ TestHealthzDraining"
      contains: "TestHealthzDraining"
  key_links:
    - from: "main.go SIGTERM/INT NotifyContext goroutine → Server.Shutdown()"
      to: "healthzHandler draining 分支"
      via: "Shutdown 入口 s.draining.Store(true)（与 s.exiting 同位同源，D-11）——healthz 503 与 1001 广播同一触发点"
      pattern: "draining"
    - from: "lifecycle sess.Wait 返回"
      to: "healthzHandler session_active 字段"
      via: "sessionAlive.Store(false)（与 session_end 事件同区段）——PTY 会话死亡即探活语义翻转"
      pattern: "sessionAlive"
  prohibitions:
    - statement: "/healthz body MUST NOT 含版本号/客户端身份/内部错误细节（枚举 oracle 面——version 只在需认证的 /metrics build_info；body 恒为粗粒度容量四字段，D-10）"
      status: resolved
      verification: test
---

<objective>
落地 OPS-06：新 health.go 装 healthzHandler，Handler() 根路径注册（GET 方法模式 + 405 fallback 成对、免认证双模式、不受 base-path 影响）；两枚新 atomic.Bool（sessionAlive/draining）挂进 New/lifecycle/Shutdown——/healthz 成为反代与编排可直接消费的探活端点。

Purpose: ttyd 缺失的可运维性第一面——k8s liveness/nginx 健康检查结构性带不了凭据，D-07 免认证窄例外 + D-11 draining 503 让探活器在进程存活与优雅关停两态都拿到正确信号。
Output: /healthz 全行为（200 四字段 / 503 draining / bp 固定 / 405）+ health_test.go 行为锁。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-observability/08-CONTEXT.md
@.planning/phases/08-observability/08-RESEARCH.md
@.planning/phases/08-observability/08-PATTERNS.md
@.planning/phases/08-observability/08-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): /healthz 端到端——healthzHandler + 双注册（免认证/bp 固定/405）+ sessionAlive 接线 + 200 JSON 四字段</name>
  <reversibility rating="one-way">D-07：免认证端点是公开暴露面契约，收紧会让已部署探活配置失效——用户已在 08-CONTEXT 锁定，记录评级不重复设卡。</reversibility>
  <files>internal/server/health.go, internal/server/server.go, internal/server/health_test.go</files>
  <read_first>
    - internal/server/server.go（Handler() L391-474 认证/无认证两分支与 L472 /ws 注册点——/healthz 注册插入点在两分支之外；Server struct 字段区 L60-157——两 atomic.Bool 落位；New L298-331——sessionAlive 初始化点；lifecycle L1167-1174——sessionAlive 置 false 挂点）
    - internal/server/sharetoken.go（L122-128 方法模式 + path-only 405 fallback 成对注册先例）
    - internal/server/e2e_test.go（startTrackedServerWith 夹具——wsURL 形态，http base URL 推导）
    - internal/server/exit_test.go（exitf 桩 + 子进程退出夹具——session_active 翻车子测宿主形态）
    - .planning/phases/08-observability/08-RESEARCH.md（Code Examples「/healthz handler 骨架」+ Pitfall 7 405 吞没实证）
  </read_first>
  <behavior>
    - 无认证实例：GET /healthz → 200，Content-Type application/json，body 四键：status=="ok"、clients==0（dialHello 后 ==1）、max_clients==32（默认）、session_active==true
    - 凭据实例：无 Authorization 头 GET /healthz → 200 同形态（D-07 唯一窄例外——/、/api/attach 同请求仍 401，例外不蔓延）
    - bp=/wesh 实例：GET /healthz → 200；GET /wesh/healthz → 无认证模式 404、凭据模式 401（根路径固定，D-09）
    - POST /healthz → 405 + Allow: GET（不落进静态伺服）
    - 子进程退出后（exitf 桩实例）：GET /healthz → 200 且 session_active==false（sessionAlive 翻转）
  </behavior>
  <action>
    ① internal/server/server.go：Server struct 加 draining、sessionAlive 两 atomic.Bool 字段（注释：hubMu 外读取故 atomic——与 registry.n 的「hubMu 外 atomic load」选型先例同构，clients.go:250-256 论证；D-10/D-11 数据源）。New 尾部 goroutine 启动前 sessionAlive.Store(true)；lifecycle 内 sess.Wait 返回与 exit code 提取完成后（08-02 session_end emit 同区段）sessionAlive.Store(false)。Handler() 在认证/无认证两分支之外（bp+"/ws" 注册行附近）注册：mux.HandleFunc("GET /healthz", s.healthzHandler) + mux.HandleFunc("/healthz", 405 fallback（Allow: GET，http.StatusMethodNotAllowed））——不带 bp 前缀（D-09）、两模式同达且不做任何认证包装（D-07——整站 Basic 闸唯一窄例外，注释登记「防例外蔓延」）。
    ② internal/server/health.go（新文件）：包声明 + 注释头登记 D-07/D-09/D-10/D-11 与红线（body 四字段粗粒度容量面，无版本无身份无错误细节——version 只在需认证的 /metrics）。healthzHandler：s.draining.Load() 为真 → status="draining" + 503，否则 "ok" + 200；body 经 encoding/json Marshal 匿名 struct（键逐字 status/clients/max_clients/session_active——禁止手写 JSON 拼接，Don't Hand-Roll 纪律；clients 取 s.registry.n.Load()、max_clients 取 s.maxClients、session_active 取 s.sessionAlive.Load()）；Content-Type application/json → WriteHeader → Write。
    ③ internal/server/health_test.go（新文件）：TestHealthz 表驱动五子测——ok 四字段（无认证实例，dialHello 前后 clients 0→1）、免认证窄例外（凭据实例无 Authorization 200 + 对照 GET / 401）、bp 固定（bp=/wesh 两模式：/healthz 200；/wesh/healthz 无认证 404/凭据 401）、405（POST → 405 + Allow: GET）、session_active 翻转（exitf 桩实例子进程退出后 false）。http 客户端用 net/http 直发（wsURL 推导 http base：ws://→http:// 换 scheme 去 /ws 尾）。
    提交：feat(08-03): /healthz endpoint — unauthenticated 200 JSON, root-pinned, 405 pair
  </action>
  <verify>
    <automated>go test ./internal/server/ -race -count=1 -run 'TestHealthz' -v && go test ./internal/server/ -count=1 && go vet ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - TestHealthz 五子测全 PASS（含凭据实例无头 200 与 /wesh/healthz 两模式精确码）
    - `grep -c '"GET /healthz"' internal/server/server.go` == 1（方法模式注册唯一）
    - `grep -c 'func (s \*Server) healthzHandler' internal/server/health.go` == 1
    - `grep -c 'sessionAlive' internal/server/server.go` ≥ 3（字段 + New 置 true + lifecycle 置 false）
    - `grep -c 'basicAuth' internal/server/health.go` == 0（handler 不涉认证包装——免认证结构性证据）
    - `go test ./internal/server/ -count=1` 全绿
  </acceptance_criteria>
  <done>
    /healthz 200 四字段 + 免认证 + bp 固定 + 405 + session_active 翻转全行为锁定；注册结构保证例外不蔓延。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: draining 503——Shutdown 入口置位 + TestHealthzDraining</name>
  <reversibility rating="reversible">draining 置位是纯增量状态位，撤回仅删一行 Store 调用与一处分支。</reversibility>
  <files>internal/server/server.go, internal/server/health_test.go</files>
  <read_first>
    - internal/server/server.go（Shutdown L1264-1286——s.exiting=true 置位点 L1266，draining 同源同位；08-02 落地的 shutdown 事件 emit 区段）
    - internal/server/shutdown_test.go（startShutdownServerWith 夹具——srv 句柄直调 Shutdown 的形态与 waitExit 断言）
    - internal/server/health_test.go（Task 1 落地的 TestHealthz 夹具——http base URL 推导复用）
  </read_first>
  <behavior>
    - srv.Shutdown() 调用前：GET /healthz → 200 status=="ok"；调用后：GET /healthz → 503 status=="draining"（clients/max_clients/session_active 三键仍在场）
    - 置位点 = Shutdown 入口（与 s.exiting=true 同源，D-11）——1001 广播开始前即翻转为 draining，关停全程探活器不再导新流
  </behavior>
  <action>
    ① internal/server/server.go：Shutdown() 首行（hubMu 锁定之前）s.draining.Store(true)——注释登记 D-11（与 exiting 同源的触发点、hubMu 外 atomic 读故 atomic.Bool；与 1001 广播同源挂点）。
    ② internal/server/health_test.go：TestHealthzDraining——startShutdownServerWith 变体夹具（shutdown_test.go 形态）；先 GET 断言 200/ok，再调 srv.Shutdown()，后 GET 断言 503/draining 且四键在场；进程级 waitExit 断言沿用夹具既有形态（Shutdown 不阻断测试进程退出路径）。
    提交：feat(08-03): /healthz 503 draining on graceful shutdown
  </action>
  <verify>
    <automated>go test ./internal/server/ -race -count=1 -run 'TestHealthz|TestShutdown' -v && go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - TestHealthzDraining PASS（200/ok → Shutdown → 503/draining 翻转序列锁定）
    - `grep -c 'draining.Store(true)' internal/server/server.go` == 1（置位点唯一——Shutdown 入口）
    - TestShutdown1001/TestShutdownStopTimeout 全 PASS（Shutdown 行为零回归）
    - `go test -race -count=1 ./internal/server/` 全绿
  </acceptance_criteria>
  <done>
    D-11 落地：关停进行中 /healthz 恒 503 draining；Shutdown 置位点唯一；-race 全绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 探活器/任意客户端 → /healthz | 免认证公开端点（D-07 唯一窄例外）——响应面必须零敏感信息 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03a | Information Disclosure | /healthz 成为枚举 oracle（版本/身份/内部状态泄漏） | low | accept | D-10 已裁决：body 恒为粗粒度容量四字段（status/clients/max_clients/session_active），无版本无身份无错误细节；prohibition 行为锁（body 键集白名单断言） |
| T-08-03b | Tampering | 免认证例外蔓延（其他端点被误挂免认证） | medium | mitigate | 注册结构：/healthz 双注册在认证分支之外唯一一处 + 注释「防例外蔓延」+ TestHealthz 对照子测（GET / 仍 401）行为锁定；README 明示义务随 08-05 |
| T-08-03c | Denial of Service | slow loris 打 /healthz | low | mitigate | 既有 http.Server ReadHeaderTimeout=5s（main.go:1188）盒住头读取；响应体 <200B 无写放大面 |
| T-08-03d | Denial of Service | 关停窗口探活翻转被滥用（伪造 draining） | low | accept | draining 只能经 SIGTERM/INT → Shutdown 触发（OS 信号面），无网络可达置位路径 |
| T-08-SC | Tampering | npm/pip/cargo 包安装供应链 | high | mitigate | N/A——本 plan 零新依赖（stdlib only，go.mod 不动） |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V2 Authentication（免认证窄例外边界） | ✓ /healthz 唯一例外 + 对照 401 行为锁 |
| V4 Access Control（根路径固定 + 405） | ✓ D-09 拒绝双挂 + 方法模式 405 fallback |
</threat_model>

<verification>
- `go test -race -count=1 ./internal/server/` 全绿（新增 health_test.go 全组 + Shutdown 回归）
- 冒烟：真实二进制 `--port 0` 启动，`curl -s localhost:PORT/healthz` 返回 200 四字段 JSON；`curl -X POST` 返回 405
</verification>

<success_criteria>
- 无认证与凭据两模式下 `curl localhost:PORT/healthz` 均 200（凭据模式无需 Authorization），body 四键与 D-10 逐字一致
- bp=/wesh 部署下探活路径恒为根 /healthz（反代/k8s probe 静态配置可写死）
- SIGTERM 后健康检查立即 503（systemd restart 场景零误路由）
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **OPS-06 (unclassified, unresolved)**：probe 无法从需求文本分类边界形态——本 plan 以 08-CONTEXT D-07..D-12 与 RESEARCH Validation Architecture 为验收来源（四字段/免认证/bp 固定/405/draining 全显式验证）。人工复核项：draining 窗口在默认配置下 <1s（stop-signal 默认 HUP 无 timeout，子进程速死）——探活器轮询周期内是否必观测到 503 属部署面时序，UAT 以 --stop-timeout 3 拉宽窗口确定性断言（08-05 S4），真实编排观测率随 08-05 人工清单复核。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `func (s *Server) healthzHandler(w http.ResponseWriter, _ *http.Request)`（internal/server/health.go，新文件）
- `Server.draining atomic.Bool` / `Server.sessionAlive atomic.Bool` 字段（internal/server/server.go）
- Handler() 注册对：`"GET /healthz"` 方法模式 + `"/healthz"` path-only 405 fallback（internal/server/server.go）
- `TestHealthz`（五子测）/ `TestHealthzDraining`（internal/server/health_test.go，新文件）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/08-observability/08-03-SUMMARY.md` when done
</output>
