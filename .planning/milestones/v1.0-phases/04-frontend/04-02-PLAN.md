---
phase: 04-frontend
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
  - web/pnpm-lock.yaml
  - web/tsconfig.json
  - web/index.html
  - web/src/main.ts
  - web/src/lib/title.ts
  - web/src/lib/title.test.ts
  - web/dist/index.html
autonomous: true
requirements:
  - CORE-03
  - FE-02
  - FE-04

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "unicode11 加载-激活硬顺序：term.loadAddon(new Unicode11Addon()) 紧随其后 term.unicode.activeVersion = '11'（仅加载不激活等于没装——宽度测量仍走 Unicode 6，FE-02/RESEARCH §Pitfall 2）"
    - "web-links 保持默认正则（0.12.0 产物实为仅 http/https——以产物源码为准，D-05 不自定义收紧或放宽）+ 不传自定义 handler（库默认 window.open → opener=null → location.href，等价 target=_blank rel=noopener，单击激活无修饰键，D-07）"
    - "OSC 8 显式设置 term.options.linkHandler（不设则核心回退 confirm() 原生警告框——d.ts 明示，D-06）；activate 走与库默认同形态（window.open + opener=null）；allowNonHttpProtocols 保持默认 false（javascript:/file: 结构性忽略）"
    - "link tooltip 双通道统一：div 带 xterm-hover class 创建于 Terminal.element 内，内容完整 URL 原文无前缀不截断（max-width 480px + word-break break-all 折行），position fixed 指针右下 +8px 偏移（视口边缘翻转），pointer-events none，z-index 901，hover 即显 leave 即隐无定时器（D-06 hover 展示真实 URL 钓鱼辨别，UI-SPEC §Link Tooltip Spec）"
    - "标题同步单一写口：term.onTitleChange → remoteTitle = sanitizeTitle(raw) → document.title = (isRO ? '[ro] ' : '') + remoteTitle；sanitize 剥离 C0(U+0000-001F)/DEL(U+007F)/C1(U+0080-009F) 后按 code point 截断 128（Array.from 不拆 surrogate pair），空串回退 'wesh'（D-01..D-04，UI-SPEC §Title Sync Contract）"
    - "Phase 3 在 WELCOME 分支的一次性 [ro] 前缀拼接写法由 setTitle() 单一写口取代（标题变化时前缀不丢；auth_failed 重试再收 WELCOME 不产生双重前缀）；isRO 提为模块级布尔供标题写口/04-03 粘贴门/04-05 osc52 门共用"
    - "node --test 锁定 sanitizeTitle：ASCII 原样、C0/DEL/C1 剥离、emoji surrogate pair 不拆、128 code point 截断、全控制字符回退 'wesh'"
    - "01-UI-SPEC 契约面不变：空黑终端即加载态（无 spinner 无白闪）、#status 五态面板与 z-index 1000 终态最高层不动（UI 考量 loading/error 行——本 phase 无新状态面板路径）"
    - "web/dist/index.html 重建入库且产物含 xterm-hover 与 sanitize 相关检索串（仓库提交真实构建产物纪律；.gz 为构建副产物按 .gitignore 既定策略不入库）"
    - statement: "中文拼音/日文 IME 组合输入不丢字、CJK/emoji 宽度正确占两格（Unicode 11 宽度测量经 activeVersion='11' 生效 + xterm 内建 textarea composition 既定路线，main.ts:90 输入链路不改）"
      verification: backstop
  prohibitions:
    - "禁止自定义 web-links urlRegex 或自定义打开 handler（D-05/D-07 锁定库默认——自维护正则与自写 window.open 包装都是重复造轮子且易丢 opener=null 防 reverse tabnapping）"
    - "标题写口禁止绕过 sanitizeTitle 直写 document.title（D-03：远程 OSC 内容不得携带控制字符或超长文本进标签页；[ro] 前缀恒在最前不得被远程内容顶掉，D-02）"
    - "禁止引入 webfont 或任何运行时网络字体/资源请求（UI-SPEC 字体策略：CJK/emoji 字形走 OS 字体回退，单 HTML 零运行时网络依赖）"
    - "禁止为 OSC 1 写标题同步兼容代码（xterm 核心不对 OSC 1 fire onTitleChange——RESEARCH §Pitfall 6，真实世界标题程序均用 OSC 0/2，接受现状）"
  artifacts:
    - path: "web/package.json"
      provides: "三 addon 钉版依赖 + pnpm.overrides js-base64 钉版"
      contains: "@xterm/addon-unicode11"
    - path: "web/src/lib/title.ts"
      provides: "sanitizeTitle 纯函数"
      contains: "sanitizeTitle"
    - path: "web/src/lib/title.test.ts"
      provides: "node --test 标题 sanitize 回归锁"
      contains: "node:test"
    - path: "web/src/main.ts"
      provides: "addon 加载段/linkHandler/tooltip/setTitle 单一写口/isRO 模块级"
      contains: "activeVersion"
    - path: "web/index.html"
      provides: "tooltip 样式（.xterm-hover）"
      contains: "xterm-hover"
    - path: "web/dist/index.html"
      provides: "重建后的单文件产物"
      contains: ""
  key_links:
    - from: "web/src/main.ts"
      to: "web/package.json"
      via: "Unicode11Addon/WebLinksAddon 命名导入（addon 加载段紧随 webgl 段后）"
      pattern: "addon-unicode11"
    - from: "web/src/main.ts"
      to: "web/src/lib/title.ts"
      via: "onTitleChange 钩子经 sanitizeTitle 纯函数（node --test 可测）"
      pattern: "sanitizeTitle"
    - from: "web/src/main.ts"
      to: "web/index.html"
      via: "tooltip div 的 .xterm-hover 样式（JS 创建元素 + CSS 提供样式同源）"
      pattern: "xterm-hover"
---

<objective>
前端三能力落地：FE-02（unicode11 宽字符/CJK/IME）、FE-04（URL 自动识别 + OSC8 双通道 + hover 真实 URL tooltip）、CORE-03（标题同步单一写口 + sanitize + [ro] 前缀融合）。

Purpose: ttyd 功能基线对等的核心三件；D-01..D-07 全部锁定决策在此接线。标题写口改造同时兑现 P2 D-14 的 [ro] 前缀挂账（D-02）。
Output: 三 addon 钉版安装 + main.ts 接线 + lib/title.ts 纯函数测试 + dist 重建 + `pnpm -C web build` 绿。

规格探针标记（spec-less probe fallback）：CORE-03/FE-04 探针报为 unclassified——CONTEXT D-01..D-07 与 UI-SPEC §Title Sync/§Link Contract 已防御性钉死全部语义，按 §C 核销为显式 truths；FE-02 encoding 探针（宽度定义归属）核销为「Unicode 11 EAW 表经 addon-unicode11 activeVersion 决定」显式 truth，empty 探针（空/单元素输入）核销为「term.write 对空载荷天然 no-op、IME 最终串经 onData 既有链路」；IME 真实栈不可自动化按 UI-SPEC 🧪 行落 backstop truth（verify 时无显式证据则 human_needed，非静默通过）。UI 考量 dismissed 两行（瞬态元素无加载/零态、无 form/list 元素探针类别不适用）记录于此不落 truths。四条 must-NOT 经 §B 回忆落入 prohibitions（descriptor-less）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend/04-CONTEXT.md
@.planning/phases/04-frontend/04-RESEARCH.md
@.planning/phases/04-frontend/04-UI-SPEC.md
@.planning/phases/04-frontend/04-PATTERNS.md
@web/src/main.ts
@web/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: 依赖钉版安装 + addon 加载段 + OSC8 linkHandler + link tooltip</name>
  <files>web/package.json, web/pnpm-lock.yaml, web/index.html, web/src/main.ts</files>
  <read_first>
    - web/src/main.ts（被改文件现状全文：import 段 1-4 行、Terminal 构造 21-56 行、addon 加载段 58-70 行、常驻接线 92-115 行）
    - web/index.html（style 段与两个顶层元素现状——tooltip 样式加入点）
    - web/package.json（现有 dependencies 精确钉版无 ^ 前缀纪律）
    - .planning/phases/04-frontend/04-RESEARCH.md（§Pattern 2 硬顺序、§Pattern 3 双通道逐字核实注、§Package Legitimacy Audit js-base64 override 建议、§Pitfall 1 默认正则仅 http(s)）
    - .planning/phases/04-frontend/04-UI-SPEC.md（§Addon Contract 加载点、§Link Contract 与 §Link Tooltip Spec 逐字样式契约）
  </read_first>
  <action>
    1. 依赖安装（pnpm 不用 npm，CODEBUDDY）：
       - 先在 `web/package.json` 加 `"pnpm": { "overrides": { "js-base64": "3.9.2" } }`（RESEARCH §Package Legitimacy Audit：addon-clipboard 传递依赖 js-base64 最新 3.9.3 发布仅 1 天 [SUS too-new]，钉到 2026-07-27 的 3.9.2 避开——采纳 override 故 install 前无需 human checkpoint）。
       - `pnpm -C web add @xterm/addon-unicode11@0.9.0 @xterm/addon-web-links@0.12.0 @xterm/addon-clipboard@0.2.0`（精确钉版无 ^ 前缀，与现有三依赖同形态）。
       - 校验：`grep 'js-base64' web/pnpm-lock.yaml` 解析版本为 3.9.2（非 3.9.3）；三包入 dependencies。
    2. `web/src/main.ts`：
       - import 段（第 4 行 webgl 后）加 Unicode11Addon/WebLinksAddon/ClipboardAddon 三个命名导入（ClipboardAddon 本 plan 仅导入——条件加载在 04-05；noUnusedLocals 下未用导入会报错，故 ClipboardAddon 的 import 推迟到 04-05 随用随加，本 plan 只加前两个）。
       - addon 加载段（webgl 段 62-70 行后续写）：`term.loadAddon(new Unicode11Addon());` 紧随其后 `term.unicode.activeVersion = '11';`（硬顺序注释引 RESEARCH §Pitfall 2——颠倒抛 unknown Unicode version、漏激活静默无效）；再 `term.loadAddon(new WebLinksAddon(undefined, { hover: showLinkTooltip, leave: hideLinkTooltip }));`（第一参 undefined = 库默认 handler，注释引 D-05/D-07）。
       - OSC8：`term.options.linkHandler = { activate: (_event, uri) => { const w = window.open(); if (w) { w.opener = null; w.location.href = uri; } }, hover: (event, text) => showLinkTooltip(event, text), leave: () => hideLinkTooltip() };`——注释引 D-06 与「不设则 confirm() 原生框（d.ts 明示回退）」；allowNonHttpProtocols 不写（默认 false，注释一句钓鱼面纵深防御）。noUnusedParameters 下未用参加下划线前缀。
       - tooltip：term.open 之后创建 div，classList.add('xterm-hover')（核心 hover 路径对该 class 提前 return 防抖动——注释引 RESEARCH §Pattern 3 核实注），append 到 `term.element!`；`showLinkTooltip(event: MouseEvent, text: string)`：textContent = text（完整 URL 原文无前缀）、按 event.clientX/clientY +8px 偏移定位（视口右/下边缘不足时向左/上翻转防溢出）、display 置显；`hideLinkTooltip()`：display 置隐。无延迟定时器（hover 即显 leave 即隐）。
    3. `web/index.html` style 段加 `.xterm-hover` 样式（UI-SPEC §Link Tooltip Spec 逐字）：position: fixed；padding: 4px 8px；background: #161616；border: 1px solid #2a2a2a；border-radius: 4px；color: #ffffff；12px/400/1.5 终端等宽栈（fontFamily 值与 main.ts 构造选项同栈）；max-width: 480px；word-break: break-all（长 URL 折行展示全文不截断——钓鱼辨别需完整 URL）；pointer-events: none；z-index: 901；初始 display: none。z-index 序注释：resize 浮层 900（04-03）< tooltip 901 < 状态面板 1000。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && pnpm -C web install && pnpm -C web exec tsc --noEmit && grep -q 'js-base64@3.9.2\|js-base64: 3.9.2\|js-base64.*3\.9\.2' web/pnpm-lock.yaml</automated>
    <human-check>链接 hover/单击与 OSC8 无 confirm 框的浏览器确认并入 04-06 的 04-UAT.md 清单统一执行</human-check>
  </verify>
  <acceptance_criteria>
    - pnpm install 与 tsc --noEmit 均退出 0
    - package.json dependencies 含三个新 addon 精确版本（0.9.0/0.12.0/0.2.0）且 pnpm.overrides 钉 js-base64 3.9.2；lockfile 解析 js-base64 为 3.9.2
    - main.ts addon 段顺序为 unicode11 → activeVersion='11' → web-links（硬顺序）；linkHandler 含 activate/hover/leave 三键且 activate 置 opener=null
    - index.html 含 .xterm-hover 规则（z-index 901、max-width 480px、break-all、pointer-events none）
  </acceptance_criteria>
  <done>三 addon 钉版入包，unicode11 激活生效，文本 URL 与 OSC8 双通道链接化 + hover 真实 URL tooltip 就绪。</done>
</task>

<task type="auto">
  <name>Task 2: 标题同步单一写口——lib/title.ts 纯函数 + node --test + setTitle/isRO 接线</name>
  <files>web/src/lib/title.ts, web/src/lib/title.test.ts, web/src/main.ts, web/tsconfig.json</files>
  <read_first>
    - web/src/main.ts（WELCOME 分支 197-210 行现状——ro 分支 disableStdin 与旧前缀写法 204 行；模块级状态区 72-78 行）
    - web/tsconfig.json（include 现状——test 文件排除点；noEmit/verbatimModuleSyntax 既有设置）
    - .planning/phases/04-frontend/04-RESEARCH.md（§Pattern 1 逐字契约 + 核实注 onTitleChange 实际触发范围为 OSC 0/2、§Pattern 6 核实注 isRO 模块级化要求）
    - .planning/phases/04-frontend/04-UI-SPEC.md（§Title Sync Contract sanitize 逐字契约与初始态/ro 前缀写口迁移）
  </read_first>
  <action>
    1. 新建 `web/src/lib/title.ts`：导出 `sanitizeTitle(raw: string): string`——`Array.from(raw)` 按 code point 迭代，filter 剔除 cp <= 0x1f、cp === 0x7f、0x80 <= cp <= 0x9f 的字符，slice(0, 128) 后 join；结果为空串返回 'wesh'（不清空标签页标题）。注释逐条引 UI-SPEC §Title Sync Contract 与 D-03（剥离 C0/DEL/C1 + 128 code point 截断 + 空回退）。
    2. 新建 `web/src/lib/title.test.ts`：`import { test } from 'node:test'; import assert from 'node:assert/strict'; import { sanitizeTitle } from './title.ts';`（Node 24 内建 type stripping 直跑 .ts 零新依赖，RESEARCH §A3；相对导入必须带 .ts 扩展名）。用例：纯 ASCII 原样返回；含 \r\n\t 等 C0 被剥离；DEL(U+007F) 与 C1(U+0085 U+009F) 被剥离；超出 128 code point 截断且长度恰 128；含 emoji（surrogate pair）截断不拆对（截断结果不含孤立代理）；全控制字符输入回退 'wesh'；空串回退 'wesh'。
    3. `web/tsconfig.json` 加 `"exclude": ["src/**/*.test.ts"]`（测试文件只经 node --test 执行——'node:test'/'node:assert' 无 @types/node 时 tsc 会报错；不引入新 devDependency）。
    4. `web/src/main.ts`：
       - import 段加 `import { sanitizeTitle } from './lib/title';`（bundler moduleResolution 免扩展名；此文件经 vite 打包不经 node）。
       - 模块级状态区加 `let isRO = false;`（注释：ro 判定模块级化——标题写口/04-03 粘贴门/04-05 osc52 门三处共用，RESEARCH §Pattern 6 核实注）与 `let remoteTitle = 'wesh';`（最近一次远程标题 sanitize 后形态，ro 前缀组合与重试重前缀防御的单一事实源）。
       - 新 `function setTitle(): void { document.title = (isRO ? '[ro] ' : '') + remoteTitle; }`（D-02 前缀恒最前 + D-04 无品牌后缀的单一写口注释）。
       - `term.onTitleChange((t) => { remoteTitle = sanitizeTitle(t); setTitle(); });`（常驻接线段；注释：onTitleChange 实际由 OSC 0/2 触发——OSC 1 只设 icon name 不触发，RESEARCH §Pitfall 6 修正理解；D-01 纯前端解析，服务端 OUTPUT 零拷贝不跑 OSC 状态机）。
       - WELCOME 分支 ro 处理改为：`isRO = true; term.options.disableStdin = true; setTitle();`——删除旧的一次性前缀拼接行（现状 204 行形态：直接对 document.title 拼 '[ro] ' 前缀）。新形态下 auth_failed 重试再收 WELCOME 时 remoteTitle 不含前缀，不产生 '[ro] [ro] …' 双重前缀（注释钉死该回归意图）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && node --test web/src/lib/title.test.ts && pnpm -C web exec tsc --noEmit && ! grep -q "document.title = '\[ro\] ' + document.title" web/src/main.ts</automated>
    <human-check>OSC 标题变化同步与 [ro] 前缀保持的浏览器确认并入 04-06 的 04-UAT.md 清单</human-check>
  </verify>
  <acceptance_criteria>
    - node --test 退出 0 且全部用例 PASS（含 emoji 不拆对与 'wesh' 回退）
    - tsc --noEmit 退出 0（test 文件经 exclude 不参与 tsc）
    - main.ts 含 setTitle 单一写口与模块级 isRO/remoteTitle；WELCOME ro 分支走 isRO 置位 + setTitle()；旧一次性前缀拼接行不复存在（负向 grep 绿）
    - onTitleChange 接线中 remoteTitle 先经 sanitizeTitle 再写 document.title（sanitize 不可旁路）
  </acceptance_criteria>
  <done>CORE-03 落地：OSC 0/2 标题变化经 sanitize 单一写口同步浏览器标签页，[ro] 前缀恒最前且不随标题变化丢失。</done>
</task>

<task type="auto">
  <name>Task 3: dist 重建与产物提交</name>
  <precondition>Task 1/2 已合入（dist 必须由含 addon 接线与标题写口的源码构建——否则产物内嵌旧前端）。</precondition>
  <files>web/dist/index.html</files>
  <read_first>
    - README.md（构建节：改 web/ 前端源码后必须先 pnpm -C web build 再 go build 纪律；产物提交约定）
    - web/.gitignore 与仓库根 .gitignore（dist/*.gz 不入库既定策略——P3 决策：gzip 头嵌 mtime 字节漂移，gz 缺失明文伺服降级）
  </read_first>
  <action>
    1. `time pnpm -C web build`（tsc && vite build && gzip -k -9 -f 全链，P1 钉死形态；time 前缀跟踪耗时，CODEBUDDY；全量构建非增量）。
    2. 校验产物：web/dist/index.html 时间戳新于 main.ts 改动（构建后验证产物时间戳，CODEBUDDY）；grep 产物含 xterm-hover 与 activeVersion 检索串（singlefile 内联后新接线入包的证据）。
    3. `go build ./...` 确认 embed 链编译通过；git add web/dist/index.html（.gz 为构建副产物按 .gitignore 既定策略不入库——gz 缺失时 embed 明文伺服降级，P3 既定决策）。
  </action>
  <verify>
    <automated>cd ~/open_src/wesh && time pnpm -C web build && grep -q 'xterm-hover' web/dist/index.html && grep -q 'activeVersion' web/dist/index.html && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - 构建退出 0；dist/index.html 含 xterm-hover 与 activeVersion 检索串（新接线入包证据）
    - web/dist/index.html 更新且随本 plan 提交入库；go build ./... 退出 0；.gz 不入库
  </acceptance_criteria>
  <done>dist 产物与 addon/标题新前端同步入库，裸 clone 构建内嵌的是 Phase 4 前端。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 远程终端输出 → 浏览器 UI 面 | PTY 对面的程序可发任意 OSC 0/2/8 序列驱动标题与链接——远程内容是不可信输入，直达标签页标题与可点击链接 |
| npm registry → 构建链 | 三个新 addon 与传递依赖 js-base64 进入单 HTML 产物，供应链完整性即运行时完整性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04 | Spoofing | OSC 0/2 标题注入伪装主机名/路径（钓鱼终端上下文） | high | mitigate | sanitizeTitle 剥离 C0/DEL/C1 + 128 code point 截断 + [ro] 前缀恒最前（D-02/D-03）；node --test 回归锁 |
| T-04-05 | Spoofing | OSC 8 钓鱼链接（显示 github.com 点开 evil.com） | high | mitigate | 双通道 hover tooltip 展示完整真实 URL 不截断（D-06）+ allowNonHttpProtocols 默认 false（javascript:/file: 结构性忽略）+ 服务端不过滤由前端兜底（D-08） |
| T-04-06 | Tampering | reverse tabnapping（window.opener 劫持新标签页） | medium | mitigate | 库默认 handleLink 与 linkHandler.activate 均 opener=null（等价 rel=noopener，产物源码逐字核实） |
| T-04-07 | DoS | 超长/控制字符标题使标签页 UI 异常 | low | mitigate | 128 code point 截断 + 空串回退 'wesh'（UI-SPEC 长文本考量行） |
| T-04-SC | Tampering | npm installs（三新 addon + js-base64 传递依赖） | high | mitigate | RESEARCH §Package Legitimacy Audit 三包全 OK（同 xterm 发布者同批次）+ 精确钉版 + pnpm.overrides js-base64@3.9.2 避开 1 天新包 + postinstall 空已核实 |
</threat_model>

<verification>
- `node --test web/src/lib/title.test.ts` 退出 0
- `pnpm -C web exec tsc --noEmit && time pnpm -C web build` 退出 0，dist 含新接线检索串
- `go build ./...` 退出 0
- 浏览器人工确认（CJK/IME、链接 hover/单击、标题同步）在 04-06 的 04-UAT.md 清单汇总执行
</verification>

<success_criteria>
- FE-02：CJK/emoji 宽度按 Unicode 11 测量（activeVersion='11' 硬顺序锁定）
- FE-04：终端输出 URL 可点击（单击新标签页 noopener 形态），hover 展示完整真实 URL
- CORE-03：标题变化同步标签页，[ro] 前缀恒最前，sanitize 防注入
- 供应链：三包钉版 + js-base64 override，零 postinstall
</success_criteria>

## Artifacts this phase produces（本 plan 部分）

- `web/package.json`：`@xterm/addon-unicode11@0.9.0` / `@xterm/addon-web-links@0.12.0` / `@xterm/addon-clipboard@0.2.0` 依赖、`pnpm.overrides.js-base64` 钉版
- `web/src/lib/title.ts`：`sanitizeTitle(raw: string): string`（新导出符号）
- `web/src/lib/title.test.ts`：node --test 用例集
- `web/src/main.ts`：`isRO` / `remoteTitle` 模块级状态、`setTitle()` 单一写口、`showLinkTooltip()` / `hideLinkTooltip()`、`term.options.linkHandler`、unicode11/web-links addon 加载段
- `web/index.html`：`.xterm-hover` 样式规则
- `web/tsconfig.json`：`exclude: ["src/**/*.test.ts"]`
- `web/dist/index.html`（重建产物）

<output>
Create `.planning/phases/04-frontend/04-02-SUMMARY.md` when done
</output>
