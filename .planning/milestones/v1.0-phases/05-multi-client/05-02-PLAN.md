---
phase: 05-multi-client
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - internal/server/clients.go
  - internal/server/server.go
  - internal/pty/io.go
  - go.mod
  - internal/server/slowclient_test.go
  - internal/server/multi_test.go
  - internal/server/e2e_test.go
autonomous: true
requirements:
  - MULTI-03
  - RES-04

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "stall 客户端（建连后不 Read）outbox 写满后被 1013 踢出，close reason 机器串逐字为 slow_consumer，stderr 落 logEvent(remote, 1013, slow_consumer)（R-10）；同实例其他客户端 fan-out 无卡顿（MULTI-03 成功准则 2 前半）"
    - "踢出与信用分工表（R-08 逐字）：ro 客户端 outbox 满 → 立即 1013 踢出；rw 客户端满但非全体可写端满 → 立即 1013 踢出；全体可写端均满（≥1）→ 不踢，hub 信用门持块等待；纯 ro 会话信用集为空 → 门永不闭合 → 满即踢"
    - "全局信用门：全体可写端 creditBlocked → hub 在 sync.Cond（挂 hubMu）上 Wait（onChunk 持块 = ReadLoop 停读 = PTY 内核 64KiB 缓冲填满 = 子进程 write 自然阻塞——RES-04 唯一合法反压路径）；任一可写端 writer drain 至 outbox 字节 < cap/2（半水位 256KiB，R-01 迟滞防门震颤）→ 清 creditBlocked + Broadcast 恢复"
    - "frame 别名安全不变量（review #1）：onChunk 内代码顺序恒为『信用门 Wait 循环 → 组帧（make+copy）→ trySend 遍历』——门持块期间 chunk 停留于 ReadLoop 缓冲（阻塞即无下次读，无别名窗口）；帧拷贝只发生在门开之后、trySend 之前，outbox 绝无持有跨门 chunk 的窗口（源码行号顺序断言 + TestGlobalCredit 门转换前后字节精确断言双锁定）"
    - "半水位迟滞带论证（review #2）：门关闭阈值 = outbox 字节写满（100% 字节上界才置 creditBlocked）、恢复阈值 = drain 至 <50%——2:1 迟滞带内建于分工表，非『50% 单点抖动』；残余振荡（滴漏读者每次 drain 开门、下一 chunk 可能再闭门）经 RESEARCH Open Question 3 裁决接受（各端持续前进；dwell 计时器 Phase 9 负载标定时评估）"
    - "pinger 与信用门结构性独立（review #2）：pinger 仅触达 conn 级 writeFrameMu（mu.lock ctx 5s，conn.go:286-292），从不取 hubMu；hubCond.Wait 原子释放 hubMu → 门闭合期间 detach/kick/新 attach 均可获锁 → Broadcast 链路无死锁环节"
    - "死锁免除：detach/kick/attach/子进程退出全部路径统一 Broadcast 重估门（P5-7）；门闭合期间全死连接由 pinger mu.lock(ctx) 超时 → 既有 02-04 精确分类（仅 errors.Is(err, context.DeadlineExceeded) → pong_timeout + CloseNow）≤(ping 间隔+5s) 收口 → detach → 门开"
    - "新客户端 attach 完成时服务端向 PTY 前台进程组显式发一次 SIGWINCH（D-11：TIOCGPGRP 取 pgid → unix.Kill(-pgid, SIGWINCH)），与仲裁 resize 是否发生无关（P5-3 实证：Linux 同尺寸 TIOCSWINSZ 不发信号）；TIOCGPGRP 失败/无前台进程组/会话已 closed 时静默降级"
    - "锁序纪律：hubMu > outboxMu——writer drain 完才取 hubMu 做恢复判定（afterDrain），绝不反序同持（R-07；信用门 cond 挂 hubMu，outbox 自有 mu）"
    - "新客首屏由服务端 SIGWINCH 强制重绘，前端零改动（UI-SPEC ## UI Considerations covered：populated terminal-viewport——重绘输出走既有 OUTPUT 扇出流入；行内 shell 下次输出自然追上）"
  prohibitions:
    - "慢客户端踢出机制不得误伤正常消费的客户端——踢出判定只能由『该客户端自己的 outbox 写满』触发（ro 满即踢 / rw 满且非全体满即踢），不得因其他客户端慢、信用门闭合或全局状态而踢出任何一个自己 outbox 未满的客户端"
    - "输出帧不得被静默丢弃——慢客户端只有两种归宿：1013 踢出 或 计入信用门停读 PTY；任何『丢帧保连接』『strikes 宽限内丢帧』的形态都是有序流画面静默损坏（RESEARCH Anti-Pattern 2），禁止出现"
  artifacts:
    - path: "internal/server/clients.go"
      provides: "hubCond 信用门 + allWritableBlockedLocked + kickOrCreditLocked 分工 + afterDrain 半水位恢复 + creditBlocked 字段"
      contains: "hubCond"
    - path: "internal/pty/io.go"
      provides: "Session.SignalForegroundGroup（TIOCGPGRP→SIGWINCH，fdMu 纪律，静默降级）"
      contains: "TIOCGPGRP"
    - path: "internal/server/slowclient_test.go"
      provides: "TestSlowConsumerKick + TestGlobalCredit（stall 夹具 + 输出洪水夹具）"
      contains: "TestSlowConsumerKick"
    - path: "go.mod"
      provides: "golang.org/x/sys 由 indirect 转 direct"
      contains: "golang.org/x/sys"
  key_links:
    - from: "internal/server/clients.go (onChunk)"
      to: "internal/server/clients.go (hubCond)"
      via: "扇出前 for s.allWritableBlockedLocked() { s.hubCond.Wait() }——Wait 释放 hubMu，chunk 持于 ReadLoop 缓冲（阻塞期间无下次读，别名安全）"
      pattern: "allWritableBlockedLocked"
    - from: "internal/server/clients.go (writer)"
      to: "internal/server/clients.go (信用门)"
      via: "drain 成功后 afterDrain：outbox 字节 < cap/2 → 清 creditBlocked + hubCond.Broadcast"
      pattern: "afterDrain"
    - from: "internal/server/server.go (Attach 升档完成)"
      to: "internal/pty/io.go (SignalForegroundGroup)"
      via: "注册表登记后无条件调用一次（D-11）；fdMu 持锁 → IoctlGetInt(master.Fd, TIOCGPGRP) → Kill(-pgid, SIGWINCH)"
      pattern: "SignalForegroundGroup"
---

<objective>
MULTI-03/RES-04 完整背压：在 05-01 的 hub 骨架上落地全局信用门（全体可写端阻塞 → 停读 PTY，任一恢复半水位即开门）、踢出/信用分工表、pinger 死锁免除的 Broadcast 统一挂点；并以真 stall 夹具测试锁定。附带 D-11 新客首屏 SIGWINCH（pty.Session.SignalForegroundGroup + attach 完成调用点）。

Purpose: 慢客户端不拖累他人是本 phase 核心差异化能力（ROADMAP 成功准则 2）；信用门保护 owner 演示者不被离群慢客户端误踢，同时把反压合法传导到子进程。D-11 解决演示场景第一痛点（观众打开链接面对黑屏）。
Output: clients.go 信用门 + io.go SignalForegroundGroup + slowclient_test.go 两测试 + SIGWINCH 集成断言，全量 -race 绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-client/05-CONTEXT.md
@.planning/phases/05-multi-client/05-RESEARCH.md
@.planning/phases/05-multi-client/05-PATTERNS.md
@.planning/phases/05-multi-client/05-VALIDATION.md
@internal/server/clients.go
@internal/server/server.go
@internal/pty/io.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: 全局信用门 + 踢出/信用分工表 + Broadcast 统一挂点</name>
  <files>internal/server/clients.go, internal/server/server.go</files>
  <read_first>
    - internal/server/clients.go（05-01 产出全文——hub/outbox/writer/registry/kick 现状，信用门挂载点）
    - internal/server/server.go（detach/lifecycle 现状——Broadcast 统一挂点的全部路径）
    - .planning/phases/05-multi-client/05-RESEARCH.md（Pattern 3 信用门完整形态与分工表、Pitfall P5-7 死锁免除证明、裁断 R-01/R-08）
    - .planning/phases/05-multi-client/05-PATTERNS.md（clients.go 节锁序纪律 hubMu > outboxMu）
  </read_first>
  <action>
    1. `internal/server/clients.go`：
       - client 结构加 `creditBlocked bool` 字段（注释：仅可写端参与信用集，ro 客户端此位恒 false——ro 满即踢永不持信用，R-08 分工表）。
       - Server 侧加 `hubCond *sync.Cond`（New 内以 &s.hubMu 构造；注释：cond 挂 hubMu——信用门状态与注册表同锁，R-07）。
       - registry 加 `gateTransitions int` 计数器（hubMu 保护，零 atomic 不违 R-07 单锁纪律）：kickOrCreditLocked 置位 creditBlocked 与 afterDrain 清位两点递增（注释：Phase 8 OPS-07 门开闭周期计数挂点——观测性 stub，review #10）。
       - `allWritableBlockedLocked() bool`（RESEARCH Pattern 3）：遍历 set 统计生效 mode==rw 的客户端；≥1 个且全部 creditBlocked → true；无可写端（纯 ro 会话/无客户端）→ false（门永不闭合）。注释：O(n) 遍历每 chunk 一次，规模 ≤ max-clients 32，可接受（review LOW 项登记）。
       - onChunk 扇出前加门（Pattern 1 目标形态）：`for s.allWritableBlockedLocked() { s.hubCond.Wait() }`——**代码顺序不变量（review #1）：门 Wait 循环必须位于组帧语句（frame := make + copy）之前**——门持块期间 chunk 停留于 ReadLoop 缓冲（阻塞即无下次读，无别名窗口）；帧拷贝只发生在门开之后、trySend 之前。注释逐字登记：Wait 释放 hubMu；持块即停读 PTY，反压经 64KiB 内核缓冲传导至子进程 write（RES-04）。
       - kick 路径升级为 `kickOrCreditLocked(c)` 分工（R-08 逐字）：c.mode == proto.ModeRO → 踢（原 kickSlowConsumerLocked 逻辑）；c 为 rw 且剔除 c 后仍存在未 blocked 的可写端 → 踢；c 为 rw 且全体可写端均满 → 不踢，置 c.creditBlocked = true（注释：保护 owner 演示者，离群慢端才踢）。
       - writer drain 成功后调 `afterDrain(bytes int)`：outbox 当前字节 < cap/2（半水位，defaultOutboxBytes 的 50% = 256KiB，注释 R-01 迟滞防门震颤）且 c.creditBlocked → 清位 + s.hubCond.Broadcast()。注释逐字登记迟滞带论证（review #2）：**门关闭阈值 = outbox 写满（100% 字节上界）、恢复阈值 = <50%——2:1 迟滞带内建于分工表**；残余振荡经 RESEARCH Open Question 3 裁决接受（dwell 计时器 Phase 9 评估）。
       - 统一 Broadcast 挂点（P5-7 注释清单逐字落码）：detach（removeLocked 后）、kickSlowConsumerLocked（移除后）、registerLocked（attach 后）、lifecycle 广播关闭路径——四处均 hubCond.Broadcast()（注释：死锁免除——pinger 结构性独立于 hubMu：仅触达 conn 级 writeFrameMu（mu.lock ctx 5s 超时，conn.go:286-292），从不取 hubMu；hubCond.Wait 原子释放 hubMu，假死连接经 02-04 精确分类 → pong_timeout + CloseNow → detach 获锁 → 门重估——review #2 链路逐环节注释）。
       - 锁序注释（clients.go 头部）：hubMu > outboxMu——writer 持 outboxMu drain 期间绝不取 hubMu；drain 完释放后才取 hubMu 做 afterDrain 判定（R-07）。
    2. `internal/server/server.go`：detach 内 removeLocked 之后补 Broadcast（经 clients.go 统一挂点）；lifecycle 广播关闭全部客户端后补 Broadcast（注册表空 → 门开 → ReadLoop 续 drain，D-12 语义）。无其他改动。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - go build/vet 退出 0；既有全套件（含 05-01 三测试）-race 全绿
    - grep -c 'hubCond' internal/server/clients.go >= 4（构造 + Wait + 至少两处 Broadcast）
    - grep -c 'func (s \*Server) allWritableBlockedLocked\|func.*afterDrain\|func.*kickOrCreditLocked' internal/server/clients.go == 3
    - grep -c 'creditBlocked' internal/server/clients.go >= 3（字段 + 置位 + 清位）
    - grep -c 'gateTransitions' internal/server/clients.go >= 3（registry 计数器字段 + 置位递增 + 清位递增——Phase 8 门开闭计数挂点，review #10）
    - 代码顺序断言（review #1）：`test "$(grep -n 'for s.allWritableBlockedLocked()' internal/server/clients.go | head -1 | cut -d: -f1)" -lt "$(grep -n 'frame := make' internal/server/clients.go | head -1 | cut -d: -f1)"` 退出 0——onChunk 内门判定行号恒小于组帧行号（锚定调用点排除函数定义行干扰）
    - grep -c '迟滞带' internal/server/clients.go >= 1（review #2 迟滞带论证注释落码）
    - clients.go 头部或锁序注释含 'hubMu > outboxMu' 逐字
  </acceptance_criteria>
  <done>信用门机制完整落码：全体可写端满 → hub 持块停读；半水位恢复开门；ro 满即踢/rw 满看全体分工明确；四条路径统一 Broadcast；既有套件零回归。行为断言由 Task 3 锁定。</done>
</task>

<task type="auto">
  <name>Task 2: pty.Session.SignalForegroundGroup（D-11）+ attach 完成调用点 + x/sys 转 direct</name>
  <files>internal/pty/io.go, internal/server/server.go, go.mod</files>
  <read_first>
    - internal/pty/io.go（全文 70 行——fdMu/closed 纪律 spawn.go:22 字段注释 + io.go:29-35/42-46、Resize 33-40 形态同款）
    - internal/server/server.go（Attach 升档完成点——注册表登记之后的位置）
    - .planning/phases/05-multi-client/05-RESEARCH.md（Code Examples SignalForegroundGroup 逐字形态、Pitfall P5-3 同尺寸不发信号实证、Standard Stack x/sys 行）
    - .planning/phases/05-multi-client/05-PATTERNS.md（io.go 节——fdMu 持锁范围与 Read 绝不可入此锁纪律）
  </read_first>
  <action>
    1. `internal/pty/io.go` 新增方法（RESEARCH Code Examples 逐字形态，fdMu 纪律与 Resize 同款——Read/Write 经 os.File 内部 fdmu 自同步、绝不可入此锁 spawn.go:22）：
       - `func (s *Session) SignalForegroundGroup()`：fdMu.Lock → closed 则 return → `pgid, err := unix.IoctlGetInt(int(s.Master.Fd()), unix.TIOCGPGRP)` → err != nil || pgid <= 0 静默 return → `_ = unix.Kill(-pgid, unix.SIGWINCH)`（负 pid = 进程组；失败静默）。
       - 注释逐字要点：D-11 新客强制重绘；TIOCGPGRP 失败/无前台进程组静默降级（D-11 授权）；Linux 同尺寸 TIOCSWINSZ 不发 SIGWINCH（P5-3 本机实证）故必须显式发且与仲裁 resize 是否发生无关；重复 SIGWINCH 无害（终端应用必须容忍伪信号）。
    2. `go.mod`：golang.org/x/sys 由 indirect 转 direct——go mod tidy 自动重标（io.go 新 import golang.org/x/sys/unix 后执行）；确认 go.sum 无新增条目（v0.47.0 已在树）。
    3. `internal/server/server.go`：Attach 升档完成（注册表登记 + writer/pinger 启动之后）无条件调用 `s.sess.SignalForegroundGroup()` 一次（注释引 D-11 + P5-3：不依赖仲裁 resize——新客秒见画面，行内 shell 下次输出自然追上）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -race -count=1 ./internal/pty/ ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'func (s \*Session) SignalForegroundGroup' internal/pty/io.go == 1
    - grep -c 'TIOCGPGRP\|SIGWINCH' internal/pty/io.go >= 2（IoctlGetInt 与 Kill 双点位）
    - grep -c 'SignalForegroundGroup' internal/server/server.go == 1（attach 完成唯一调用点）
    - grep 'golang.org/x/sys' go.mod 行不含 '// indirect'（direct 转化完成）
    - go build/vet 退出 0；pty 与 server 包 -race 全绿
  </acceptance_criteria>
  <done>SignalForegroundGroup 方法落位且 attach 完成无条件触发；x/sys direct；全量绿。SIGWINCH 实际送达的行为证据由 Task 3 集成断言锁定。</done>
</task>

<task type="auto">
  <name>Task 3: slowclient_test.go（踢出+信用门）+ SIGWINCH 送达集成断言</name>
  <files>internal/server/slowclient_test.go, internal/server/multi_test.go, internal/server/e2e_test.go</files>
  <read_first>
    - internal/server/e2e_test.go（83-84 seq 输出洪水夹具、105-202 startTestServerWith/dialHello 形态、TestHelperProcess helper 分支结构——新增 winch 分支位置）
    - internal/server/clients.go（Task 1/05-01 产出——信用门与踢出的被测对象）
    - .planning/phases/05-multi-client/05-PATTERNS.md（slowclient_test.go 节——stall 夹具与信用门验证序列逐字裁决）
    - .planning/phases/05-multi-client/05-VALIDATION.md（05-01-03/05-01-06 行：TestSlowConsumerKick/TestGlobalCredit 命名与断言面）
  </read_first>
  <action>
    1. 新建 `internal/server/slowclient_test.go`（package server_test 黑盒）：
       - stall 夹具：dialHello 成功后**不再调用 Read**（TCP 接收缓冲填满 → 服务端 writer 阻塞 → outbox 涨满）。
       - `TestSlowConsumerKick`（VALIDATION 05-01-03）：Options.OutboxBytes 覆写小值（如 8KiB——HelloTimeout 测试覆写先例）加速触发；子进程 seq 1 200000（约 1.3MB 输出洪水，e2e_test.go:83-84 夹具）；stall 客户端断言 CloseError.Code == websocket.StatusTryAgainLater（1013）；同实例第二客户端正常 Read——断言其 fan-out 持续前进（累积字节单调增长、在 5s 轮询窗口内收齐预期前缀）且服务端 ReadLoop 未被拖死（第二客户端无长时空窗）；客户端 Read 永不带 deadline ctx（Pitfall 2——goroutine + 缓冲 channel + select time.After 竞速形态）。
       - `TestGlobalCredit`（VALIDATION 05-01-06）：--writable 会话两 rw 客户端全部 stall（Options.OutboxBytes 小值）→ 断言门闭合可观测（子进程输出推进停滞——正常客户端视角累积字节冻结，或两 stall 端均未被 1013 踢出持续一个轮询窗口）→ 一端恢复 Read（drain 至半水位）→ 断言门开（输出恢复推进）。注意：05-03 落地后默认 write-policy=owner 会使第二客户端降级 ro——本测试届时须显式 Options 传 write-policy=all 保持两 rw 语义前提（05-03 Task 2 适配项登记）。
       - 门转换字节精确断言（review #1 行为证据）：seq 输出洪水场景下，恢复端在门开后累积收齐的字段序列与门闭合前已收部分衔接连续——单调递增、无重复、无乱序窗口（strings.Fields 切分免疫 ONLCR 既定纪律）；别名安全的端到端实证——门持块期间无 chunk 被覆写。
       - CloseNow 有界开门子场景（review #2「dead owner during gate closure」）：门闭合期间一端 CloseNow → detach → 门在 5s 轮询窗口内重开且输出恢复推进（P5-7 验证序列逐字 + 有界时限断言）。
    2. SIGWINCH 送达集成断言：e2e_test.go TestHelperProcess 新增 helper 分支（参照既有分支形态，命名为 wesh-helper-winch）：安装 SIGWINCH 处理器 → READY 报出 → 收到信号后落盘标记文件（GOT_WINCH）——形态与既有 helper 分支同款（落盘标记是信号送达证据的既定纪律：stdout 标记在 WS 断开后不可观测，Phase 01-03 决策）。multi_test.go 新增 `TestSigwinchOnAttach`：helperArgv(t, "wesh-helper-winch", markerFile) 起服 → dialHello attach → 轮询 5s 断言标记文件出现（D-11 送达证据）。注释登记：本测试在 CI macos runner 同样执行（.github/workflows/ci.yml 双平台矩阵既定）——darwin 同尺寸行为假设 A1 的验证通道（review MEDIUM 项处置：以 CI 双平台运行实证替代本机平台断言）。
  </action>
  <verify>
    <automated>go test -race -count=1 -run 'TestSlowConsumerKick|TestGlobalCredit|TestSigwinchOnAttach' ./internal/server/ && go test -race -count=1 ./internal/server/ ./internal/pty/</automated>
  </verify>
  <acceptance_criteria>
    - TestSlowConsumerKick 绿：stall 端 CloseError.Code == websocket.StatusTryAgainLater；第二客户端在轮询窗口内持续收流无卡顿
    - TestGlobalCredit 绿：全 rw stall → 输出停滞且两端未被踢；恢复/断开 → 门开输出恢复；含门转换字节精确断言（seq 字段序列连续无重复无乱序——review #1 行为证据）与 CloseNow 5s 有界开门断言（review #2 dead-owner 场景）
    - TestSigwinchOnAttach 绿：attach 后 5s 内 GOT_WINCH 标记文件出现（D-11 送达证据）
    - grep -c 'func TestSlowConsumerKick\|func TestGlobalCredit' internal/server/slowclient_test.go == 2（VALIDATION 命名逐字对齐）
    - grep -c 'wesh-helper-winch' internal/server/e2e_test.go >= 1
    - server+pty 两包 -race 全量绿（零回归）
  </acceptance_criteria>
  <done>MULTI-03（stall 端 1013 踢出 reason=slow_consumer、他人无卡顿）与 RES-04（全可写端 stall → 停读 PTY、恢复开门）行为锁定；D-11 SIGWINCH 送达有落盘证据；全套件 -race 绿。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

edge-probe 对本 plan 两条需求的分类为 unclassified（中文文本启发式局限，非无边缘），按 #1110 保持 unresolved 显式记录：

- requirement: MULTI-03 ｜ probe: "unclassified — review manually" ｜ status: flagged-unverified ｜ 处置：边角面由 CONTEXT D-10/D-11 与 RESEARCH Pattern 1-3、P5-2、R-01/R-08/R-10 实质覆盖；断言已提升为 must_haves.truths（1013 reason=slow_consumer、分工表、半水位、pinger 死锁免除），由 TestSlowConsumerKick 实证。
- requirement: RES-04 ｜ probe: "unclassified — review manually" ｜ status: flagged-unverified ｜ 处置：信用门全形态由 RESEARCH Pattern 3 + P5-7 源码级死锁免除证明覆盖；门闭合/恢复序列由 TestGlobalCredit 实证。

## Artifacts this plan produces

- `internal/server/clients.go`：`creditBlocked` 字段、`hubCond *sync.Cond`、`allWritableBlockedLocked()`、`kickOrCreditLocked()`（ro 满即踢/rw 满看全体分工）、`afterDrain()`（半水位 50% 恢复 + Broadcast）、四条统一 Broadcast 挂点（detach/kick/register/lifecycle）、`registry.gateTransitions` 计数器（Phase 8 OPS-07 门开闭周期挂点——review #10）、onChunk 代码顺序不变量注释（门 Wait 先于组帧——review #1）与迟滞带论证注释（review #2）
- `internal/pty/io.go`：`func (s *Session) SignalForegroundGroup()`（TIOCGPGRP → kill(-pgid, SIGWINCH)，fdMu 纪律）
- `internal/server/slowclient_test.go`（新）：`TestSlowConsumerKick`、`TestGlobalCredit`（含门转换字节精确断言与 CloseNow 有界开门子场景——review #1/#2）、stall 客户端夹具
- `internal/server/multi_test.go`：`TestSigwinchOnAttach`（CI macos runner 覆盖注释——A1 验证通道）
- `internal/server/e2e_test.go`：`wesh-helper-winch` helper 分支（SIGWINCH 落盘标记）
- `go.mod`：golang.org/x/sys v0.47.0 indirect→direct

## Review 发现处置（05-REVIEWS.md 交叉评审）

| # | 发现（严重度） | 处置 | 说明 |
|---|---------------|------|------|
| #1 | frame 别名与信用门 Wait 的交互未被证伪（HIGH） | **采纳** | 代码顺序不变量（门 Wait 恒在组帧之前）提升进 must_haves.truths + Task 1 行号顺序断言 + Task 3 TestGlobalCredit 门转换字节精确断言——源码证据与行为证据双锁定；别名安全性论证：门持块期间 ReadLoop 阻塞无下次读，chunk 停留读缓冲无覆写窗口 |
| #2 | 半水位 50% 振荡风险 / dead owner during gate closure / pinger 与 hubMu 交互（HIGH） | **采纳** | 迟滞带论证落码：关闭阈值=outbox 写满（100%）、恢复阈值=<50%——2:1 迟滞带内建于分工表，reviewer 设想的『257KiB 开门→513KiB 再关门』单点抖动不成立（关门条件是写满非水位线）；残余振荡经 RESEARCH OQ3 裁决接受（dwell 计时器 Phase 9）。dead-owner 场景由 TestGlobalCredit CloseNow 5s 有界开门子场景锁定；pinger 不取 hubMu 的结构性独立论证进 truths + 代码注释 |
| #10 | 观测性缺口（MEDIUM，跨切面） | **采纳（stub 形态）** | registry.gateTransitions 计数器（hubMu 保护）；完整结构化日志路由 Phase 8 OPS-07/OPS-08 |
| — | darwin SIGWINCH 行为未实证（MEDIUM） | **采纳** | TestSigwinchOnAttach 注释登记 CI macos runner 覆盖——双平台矩阵（.github/workflows/ci.yml）即 A1 假设验证通道；显式 SIGWINCH 即便在 darwin 同尺寸发信号的误判下也只是冗余无害（P5-3） |
| — | allWritableBlockedLocked O(n) 遍历（LOW） | **接受** | 规模 ≤ max-clients 32、每 chunk 一次遍历；代码注释登记 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 慢客户端→全局背压 | 不可信客户端的消费速度决定信用门开合——门机制不得被诱导死锁或误伤正常端 |
| PTY 前台进程组信号面 | SIGWINCH 发往 TIOCGPGRP 取到的进程组——失败必须静默降级不得影响 attach 主路径 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02 | Denial of Service | 信用门死锁（全体等待无人唤醒） | high | mitigate | pinger 结构性独立于 hubMu（仅触达 conn 级 writeFrameMu，mu.lock ctx 5s 超时，conn.go:286-292）→ 02-04 精确分类 → pong_timeout + CloseNow ≤(interval+5s) 收口假死连接；hubCond.Wait 原子释放 hubMu，detach/kick/attach/退出四路径统一 Broadcast；TestGlobalCredit 含 CloseNow 5s 有界开门子场景（P5-7 验证序列 + review #2 dead-owner 场景） |
| T-05-01c | Denial of Service | 踢出误伤（慢而合法的滴漏读者） | medium | mitigate | 分工表：仅『全体可写端同步满』才持块不踢；关闭阈值=outbox 写满（100%）/恢复 <50% 构成 2:1 迟滞带（review #2——非 50% 单点抖动）；滴漏读者每次 drain 即开门（前进保证）；连续信用阻塞 dwell 计时器评估挂 Phase 9（RESEARCH Open Question 3 裁决接受振荡） |
| T-05-11a | Denial of Service | SIGWINCH 调用阻塞 attach | low | mitigate | 单次 ioctl+kill 系统调用无阻塞面；TIOCGPGRP 失败静默降级（D-11 授权）；fdMu 与 Read 路径互斥纪律沿用（spawn.go:22） |
</threat_model>

<verification>
- go test -race -count=1 ./internal/server/ ./internal/pty/ 全绿（含 TestSlowConsumerKick/TestGlobalCredit/TestSigwinchOnAttach）
- 源码断言：hubCond/creditBlocked/分工函数齐备；锁序注释；x/sys direct
</verification>

<success_criteria>
- 一个客户端停止读取 TCP 流时其他客户端无卡顿：stall 端 1013 被踢，ReadLoop 永不阻塞（ROADMAP 成功准则 2）
- 全体可写端 stall 时子进程输出自然停顿（反压合法传导），任一端恢复或死亡即恢复推进
- 新客户端 attach 秒见全屏程序画面（SIGWINCH 落盘证据）
</success_criteria>

<output>
创建 `.planning/phases/05-multi-client/05-02-SUMMARY.md`：记录信用门形态（cond/半水位/分工表）、Broadcast 挂点清单、SignalForegroundGroup 降级语义、三测试锁定的行为与构造的 stall 场景、darwin 行为假设（A1：同尺寸不发信号外推，CI runner 覆盖）。
</output>
