---
phase: 05-multi-client
plan: 05
type: execute
wave: 5
depends_on: ["05-03"]
files_modified:
  - internal/server/clients.go
  - internal/server/server.go
  - go.mod
  - go.sum
  - internal/server/multi_test.go
autonomous: true
requirements:
  - RES-02

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "每客户端输入限速：Attach 升档时构造 rate.NewLimiter(rate.Limit(s.inputRate), s.inputBurst)（默认 32KiB/s + burst 64KiB，R-01 参数表——人类击键 ~10B/s、快粘 ~50KB 瞬时由 burst 容纳；Options.InputRate/InputBurst 测试可覆写）"
    - "INPUT 门链（读循环内逐帧）：c.mode == ro → 静默丢（既有，per-client 化 05-01 已落）→ !c.limiter.AllowN(time.Now(), len(data)-1) → 静默丢 + 计数（R-02：超限丢弃不断开不打逐次日志——Allow 官方语义即 drop；计数器注释挂 Phase 8 metrics）→ s.inputQ.tryEnqueue(data[1:])（满则丢 + 计数）；用户侧可见性通道 = README 明示（05-09）+ Phase 8 计数器 metrics，无协议反馈帧（P2 D-01 类型空间纪律——review #5 延期处置）"
    - "CR-01 完整背压修复（PROJECT.md Key Decisions 兑现）：会话级单输入队列（字节有界 256KiB，≥16 个 16KiB 满帧）+ 单 input-writer goroutine 独占 sess.Master.Write——Attach 读循环内的同步 Master.Write 彻底消失，master fd 保持默认阻塞模式（阻塞被关进专属 goroutine；Phase 2 的 O_NONBLOCK 最小缓解从未落地且不再需要，RESEARCH State of the Art 核实）"
    - "input-writer goroutine 生命周期挂服务端（New 启动、lifecycle 终结收口）；Drain→Close 解除在途写阻塞（与 Read 同机制 runtime poller，既有 D-12 语义）"
    - "多写者输入交错不做排序承诺（ARCHITECTURE §2.9 screen 同款语义——all 模式文档明示，README 在 05-09 落地）"
    - "INPUT 帧 data 入队安全：coder/websocket Read 每次返回新分配 payload——tryEnqueue 持引用无别名风险（注释登记；与 OUTPUT 方向 chunk 别名红线对照区分）"
  prohibitions:
    - "输入限速不得断开连接或降低连接可用性——超限唯一动作是丢弃该帧（R-02：all 模式下激进粘贴的合法用户被踢是 UX 灾难；INPUT 丢帧用户可感知可恢复，不同于 OUTPUT 丢帧的静默损坏）；禁止以限速为由 Close/踢出/降权任何客户端"
    - "禁止为输入方向恢复任何形式的读循环内同步 Master.Write（CR-01 老路回潮）或给读路径加 deadline（Pitfall 2 老路）"
  artifacts:
    - path: "internal/server/clients.go"
      provides: "client.limiter（x/time/rate）+ inputQ 字节有界队列（tryEnqueue/dequeue）+ input-writer goroutine"
      contains: "rate.NewLimiter"
    - path: "internal/server/server.go"
      provides: "INPUT 门链（mode→limiter→inputQ）+ input-writer 装配点"
      contains: "AllowN"
    - path: "go.mod"
      provides: "golang.org/x/time 新 direct 依赖（rate.Limiter）"
      contains: "golang.org/x/time"
    - path: "internal/server/multi_test.go"
      provides: "TestInputRateLimit（VALIDATION 05-01-05）"
      contains: "TestInputRateLimit"
  key_links:
    - from: "internal/server/server.go (INPUT case)"
      to: "internal/server/clients.go (limiter + inputQ)"
      via: "mode 门 → limiter.AllowN(now, len-1) → inputQ.tryEnqueue(payload)——超限/队列满均静默丢 + 计数"
      pattern: "AllowN"
    - from: "internal/server/clients.go (input-writer)"
      to: "internal/pty (Session.Master.Write)"
      via: "单 goroutine 顺序出队写 master——读循环零同步写（CR-01 修复核心）"
      pattern: "inputQ"
---

<objective>
RES-02 每客户端输入限速 + CR-01 完整背压修复：x/time/rate 令牌桶（超限丢弃 R-02）+ 会话级 256KiB 有界输入队列 + 单 input-writer goroutine 独占 Master.Write——读循环同步写 PTY 的已知缺陷（CR-01，ttyd 同款）在本 plan 终结。

Purpose: all 模式下恶意协作者/脚本的输入洪水不得拖死会话（RES-02）；合法快速粘贴由 burst 64KiB 容纳。CR-01 是 PROJECT.md 锁定落本 phase 的修复项。
Output: clients.go limiter/inputQ/input-writer + go.mod x/time + TestInputRateLimit，全量 -race 绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-client/05-CONTEXT.md
@.planning/phases/05-multi-client/05-RESEARCH.md
@.planning/phases/05-multi-client/05-PATTERNS.md
@.planning/phases/05-multi-client/05-VALIDATION.md
@internal/server/clients.go
@internal/server/server.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: x/time 引入 + per-client limiter + 有界输入队列 + input-writer goroutine</name>
  <files>internal/server/clients.go, internal/server/server.go, go.mod, go.sum</files>
  <read_first>
    - internal/server/server.go（INPUT case 现状——05-01 per-client mode 门之后的 Master.Write 直写点；New 装配区）
    - internal/server/clients.go（05-01..05-03 产出——client 结构与 writer 装配先例）
    - .planning/phases/05-multi-client/05-RESEARCH.md（Pattern 8 CR-01 完整修复、Code Examples 输入限速门逐字、裁断 R-02、Don't Hand-Roll x/time/rate 行、Standard Stack Supporting 表）
    - .planning/phases/05-multi-client/05-PATTERNS.md（Shared Patterns 常量与 Options 纪律节）
  </read_first>
  <action>
    1. 依赖引入：go get golang.org/x/time@v0.15.0（网络可达 proxy——RESEARCH Environment 核实；断网 fallback：GOFLAGS=-mod=mod go get golang.org/x/time@v0.10.0，模块缓存已有）→ go mod tidy。注释：rate API 十年稳定（rate.go:100-117 签名核实）；合法性见 RESEARCH §Package Legitimacy Audit（golang.org/x/* 官方命名空间 + sum.golang.org 校验）。**钉版 rationale 注释落 go.mod 临近注释或 clients.go 引入点（review MEDIUM 处置）：钉 v0.15.0 防版本漂移；rate 包自 2015 年签名稳定，升级经 go.sum 审计链显式进行。**
    2. `internal/server/clients.go`：
       - client 结构加 `limiter *rate.Limiter`（Attach 升档构造：rate.NewLimiter(rate.Limit(s.inputRate), s.inputBurst)——defaultInputRate 32KiB/s、defaultInputBurst 64KiB，05-01 已立常量；注释 R-01 论证：击键 ~10B/s、快粘 ~50KB 瞬时由 burst 容纳、持续 32KiB/s 远超合法远低于洪水）。
       - 会话级输入队列 `inputQ`：字节有界（256KiB 默认常量 defaultInputQueueBytes——注释：≥16 个 16KiB 满帧 ReadLimitPostAuth；Phase 9 标定回填）+ tryEnqueue(p []byte) bool（满 → 丢 + droppedInputs 计数器递增，注释挂『Phase 8 OPS-07 进 metrics』）+ 出队方法；队列存 INPUT payload（data[1:]）——注释：coder Read 每次返回新分配 payload，持引用无别名风险（与 OUTPUT 方向 chunk 别名红线对照）。
       - `func (s *Server) inputWriter()`：单 goroutine（New 内启动，生命周期挂服务端——lifecycle 终结时经 done/cancel 收口）：阻塞等队列信号 → 顺序出队 → sess.Master.Write(p)；写失败注释（子进程退出路径由 lifecycle 收口，Drain→Close 解除在途阻塞——D-12 机制同款）；独占 Master.Write 注释逐字（CR-01：读循环零同步写）。
    3. `internal/server/server.go`：
       - INPUT case 改造（05-01 mode 门之后续链）：`if !c.limiter.AllowN(time.Now(), len(data)-1) { s.inputDrops.Add(1); continue }`（R-02 丢弃——注释逐字：超限丢弃不断开不打逐次日志；Allow godoc 官方 drop 语义 rate.go:113-115）→ `s.inputQ.tryEnqueue(data[1:])`（false → 计数递增 continue——注释：限速器在前，队列满本应罕见）。
       - 读循环内 Master.Write 调用删除（CR-01 收口——注释登记：Phase 2 O_NONBLOCK 最小缓解从未执行，本队列化后 master 保持默认阻塞模式，RESEARCH Pattern 8 核实）。
       - New 内启动 inputWriter goroutine；lifecycle 终结路径补 inputWriter 收口（与 writer/pinger 同款 done 纪律）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - go build/vet 退出 0；server 包既有测试（含 echo/读写路径）-race 全绿——输入路径行为零回归
    - grep 'golang.org/x/time' go.mod 存在且 go.sum 含对应条目
    - grep -c 'rate.NewLimiter\|AllowN' internal/server/clients.go internal/server/server.go >= 2
    - grep -c 'func (s \*Server) inputWriter\|tryEnqueue' internal/server/clients.go >= 2
    - grep -c 'inputQ.tryEnqueue' internal/server/server.go >= 1（INPUT case 走队列——读循环同步写已被队列化取代，CR-01 收口的正向证据）
    - grep -c 'go s.inputWriter()' internal/server/server.go == 1（input-writer 唯一装配点——master 写路径独占在专属 goroutine）
    - go test -race 全绿证明 echo 路径（INPUT → inputQ → inputWriter → master → cat → OUTPUT）端到端无回归
  </acceptance_criteria>
  <done>限速门链与 CR-01 队列化落码：读循环 INPUT 经 limiter→inputQ→input-writer 写 master；go.mod 新依赖就位；echo 全链路 -race 绿。限速行为断言由 Task 2 锁定。</done>
</task>

<task type="auto">
  <name>Task 2: TestInputRateLimit——超限丢弃 + 连接存活 + 未超限送达</name>
  <files>internal/server/multi_test.go</files>
  <read_first>
    - internal/server/multi_test.go（05-01..05-03 产出——测试装配与 cat echo 夹具）
    - .planning/phases/05-multi-client/05-VALIDATION.md（05-01-05 行：TestInputRateLimit 断言面——Options 覆写 rate/burst 加速）
    - .planning/phases/05-multi-client/05-RESEARCH.md（裁断 R-02——丢弃语义与断言预期）
  </read_first>
  <action>
    1. `internal/server/multi_test.go` 追加 `TestInputRateLimit`（VALIDATION 05-01-05）：
       - 装配：opts Writable=true + InputRate=1024（1KiB/s）+ InputBurst=1024（1KiB）覆写加速；子进程 cat（echo 回读通道）。
       - 行为：Hello 完成后连续发送总计 64KiB INPUT（多帧）→ 读 OUTPUT 累积回显 → 断言三件套：送达回显字节数 > 0（未超限部分送达——burst 内容纳的首批）；送达字节数显著小于发送量（< 发送量的 25%——超限部分被丢弃；窗口滑动的时序不确定性用宽界断言免疫）；连接存活（洪水后 ping/pong 正常——或继续发小量 INPUT 有回显，未被踢未 1011）。
       - 对照子测：InputRate/InputBurst 调大（或不覆写默认）下同量 INPUT 全量送达（证明丢弃确由限速器而非队列/其他路径）。
       - -race 必跑；Read 永不带 deadline ctx（Pitfall 2 竞速形态）。
  </action>
  <verify>
    <automated>go test -race -count=1 -run TestInputRateLimit ./internal/server/ && go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - TestInputRateLimit 绿：超限部分丢弃（送达 < 25% 发送量）+ 未超限送达（> 0）+ 连接存活三断言齐备
    - 对照子测绿：大限额下同量 INPUT 全量送达
    - grep -c 'func TestInputRateLimit' internal/server/multi_test.go == 1（VALIDATION 05-01-05 命名逐字对齐）
    - server 包 -race 全量绿（零回归）
  </acceptance_criteria>
  <done>RES-02 行为锁定：INPUT 洪水超限被丢弃且连接存活、未超限部分送达、burst 内容纳合法快粘。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

edge-probe 对本条需求的分类为 unclassified（中文文本启发式局限，非无边缘），按 #1110 保持 unresolved 显式记录：

- requirement: RES-02 ｜ probe: "unclassified — review manually" ｜ status: flagged-unverified ｜ 处置：边角面由 RESEARCH Pattern 8、裁断 R-02（丢弃语义）、A2（x/time 版本稳定性）实质覆盖；断言已提升为 must_haves.truths（32KiB/s+64KiB 初值、丢弃不断开、队列 256KiB），由 TestInputRateLimit 实证。

## Artifacts this plan produces

- `internal/server/clients.go`：`client.limiter *rate.Limiter`、`inputQ` 字节有界队列（`tryEnqueue`/出队、`defaultInputQueueBytes` 256KiB、`droppedInputs` 计数器——Phase 8 metrics 挂点注释）、`func (s *Server) inputWriter()`
- `internal/server/server.go`：INPUT 门链（mode → AllowN → tryEnqueue）；读循环 `Master.Write` 删除（CR-01 收口）
- `go.mod`/`go.sum`：`golang.org/x/time` v0.15.0 新 direct 依赖（rate.Limiter）
- `internal/server/multi_test.go`：`TestInputRateLimit`

## Review 发现处置（05-REVIEWS.md 交叉评审）

| # | 发现（严重度） | 处置 | 说明 |
|---|---------------|------|------|
| #5 | 限速超限静默丢弃无用户反馈——『粘贴 100KB 只到了 64KB』造成协作困惑（HIGH） | **延期（锁定决策冲突记录，缓解链明确）** | R-02 锁定裁决：超限丢弃不断开、不打逐次日志（x/time Allow godoc 官方 drop 语义，rate.go:113-115）；用户可见的运行期反馈需新增 S→C 帧类型——P2 D-01 类型空间纪律（本 phase 不动协议，违反即动协议面）。**缓解链**：① INPUT 丢帧用户可感知可恢复（键不回显自然放慢）——与 OUTPUT 丢帧的静默损坏本质不同（RESEARCH R-02 论证）；② 计数器 stub（inputDrops/droppedInputs）Phase 8 OPS-07 进 metrics（review #10 同链）；③ README 明示丢弃语义与分段粘贴建议（05-09 落地）。协议级反馈通道若未来需要，须重新 discuss 动协议 |
| — | x/time 非主分发依赖的版本漂移/钉版（MEDIUM） | **采纳（文档化）** | 钉版 v0.15.0 + go.sum 入库（T-05-SC 既有）；钉版 rationale 注释落码（Task 1）；断网 fallback v0.10.0 模块缓存（RESEARCH Environment）；rate API 2015 年起签名稳定 |
| — | rate/burst 运行期不可配置（MEDIUM） | **延期** | CONTEXT deferred 锁定：新 flag 配置文件收口归 Phase 7 OPS-09；初值论证（击键 ~10B/s、快粘 ~50KB 由 burst 64KiB 容纳、持续 32KiB/s 远超合法远低于洪水——RESEARCH R-01 参数表）；Options.InputRate/InputBurst 测试可覆写已在 |
| #10 | 观测性缺口（MEDIUM，跨切面） | **采纳（stub 形态，原 plan 既有）** | inputDrops（限速丢弃）与 droppedInputs（队列满丢弃）两计数器已在本 plan Task 1 规格内，注释挂 Phase 8 OPS-07 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 客户端 INPUT→PTY master | 不可信输入字节流直抵子进程 stdin——洪水不得拖死会话或阻塞读循环 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Denial of Service | 输入洪水（all 模式恶意协作者/脚本） | high | mitigate | per-client x/time/rate 丢弃（32KiB/s+64KiB burst，R-02）+ 会话级 256KiB 有界队列满则丢 + ReadLimit 16KiB 帧顶（P2 既有）——三层叠加；TestInputRateLimit 锁定丢弃+存活 |
| T-05-03b | Denial of Service | 输入写阻塞复活 CR-01（慢子进程 stdin 反堵读循环） | high | mitigate | 单 input-writer goroutine 独占 Master.Write，读循环零同步写；队列有界满则丢；Drain→Close 解除在途阻塞（D-12 机制）；acceptance 断言读循环 Master.Write 零残留 |
| T-05-SC | Tampering | Go module 依赖引入（x/time） | high | mitigate | npm/pip/cargo 合法性门禁 seam 不覆盖 Go modules——等效权威链：golang.org/x/* 官方命名空间 + Go module proxy + sum.golang.org 校验（RESEARCH §Package Legitimacy Audit OK/Approved）；钉版 v0.15.0 + go.sum 入库；无 [ASSUMED]/[SUS] 项，无 [SLOP] |
</threat_model>

<verification>
- go test -race -count=1 ./internal/server/ 全绿（含 TestInputRateLimit 与对照子测）
- 源码断言：x/time 入 go.mod、读循环 Master.Write 零残留、门链三件套齐备
</verification>

<success_criteria>
- INPUT 洪水超限部分被丢弃、连接存活、未超限部分送达（RES-02）
- 任何客户端输入行为不得阻塞读循环或其他客户端（CR-01 完整修复兑现 PROJECT.md 锁定项）
</success_criteria>

<output>
创建 `.planning/phases/05-multi-client/05-05-SUMMARY.md`：记录 x/time 版本与引入形态（proxy/fallback）、门链落码、inputQ 参数与计数器挂点、CR-01 收口断言、TestInputRateLimit 宽界断言的时序论证。
</output>
