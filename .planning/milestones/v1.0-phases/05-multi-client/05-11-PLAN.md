---
phase: 05-multi-client
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/main.ts
  - web/dist/index.html
autonomous: true
requirements: [MULTI-01, MULTI-04]
gap_closure: true
gap_ids: [G-05-1]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "宽端（窗口 > 会话尺寸）把 xterm 视口约束到会话矩形渲染——超出面积为页面背景留白（纯布局，D-07 零新 UI 组件）；同 cols 渲染同字节流 = 两端逐屏严格一致"
    - "rw 端上行 RESIZE 恒为窗口 fit 尺寸（fit.proposeDimensions），永不是被约束的渲染尺寸——升格后尺寸接管纠正链（05-08 既定）不断裂"
    - "升格 Welcome 到达后约束解除：sessionDims 先更新为新会话尺寸再 refit，min(fit, session) 自然回到窗口尺寸；ro 期窗口变化过的端由 refit 上报当前 fit 尺寸触发服务端重算收口"
    - "重复 Welcome 全链幂等：ro console 一次性提示经门闩不随尺寸推送重放；osc52Loaded 门闩与 prefs queryKeys 跳过既有幂等保持；welcomeDone/beforeunload 重复注册去重不变"
    - "旧服务端（Welcome 无 cols/rows 键）下前端行为与改造前逐字节一致（sessionDims 恒 null → 渲染=fit）"
  artifacts:
    - "web/src/main.ts：sessionDims/lastReported/prevFit/roNotified 四个模块级状态 + refit() 统一入口 + sendResize 去重 + WELCOME 分支尺寸应用"
    - "web/dist/index.html：重建产物（embed 伺服链）"
  key_links:
    - "WELCOME 分支：尺寸键校验 → sessionDims 赋值 → mode 分支 → refit()（约束应用唯一入口）"
    - "refit 双概念拆分：上报尺寸 = fit.proposeDimensions()（窗口物理尺寸）；渲染尺寸 = 逐轴 min(fit, sessionDims)"
    - "onopen Hello 携 fit 尺寸 = 首次上报 → lastReported 同步置位（防握手 Welcome 后 refit 重发等值 RESIZE 的线序漂移）"
---

<objective>
G-05-1 前端半侧：消费 05-10 下发的会话尺寸，把「上报尺寸」与「渲染尺寸」拆成两个概念——rw 端上报恒为窗口 fit 尺寸（驱动 PTY 仲裁），渲染尺寸 = 逐轴 min(fit, 会话尺寸)。宽端约束到会话矩形真正留白，readline 行编辑等相对寻址流在异尺寸双端逐屏一致（40 列 PTY 字节流不再被 120 列端按自身宽度错位换行）。

Purpose: 用户实测「A 小 B 大时 A 内输入在 B 端叠写」的修复落点；同时保持升格解除约束与全部既有幂等纪律。
Output: main.ts 重构的 resize 链路 + 重建的 dist 产物。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-multi-client/05-UAT.md
@.planning/phases/05-multi-client/05-10-PLAN.md

@web/src/main.ts
@web/index.html
@web/uat/phase05-dom.mjs
</context>

<interfaces>
<!-- 消费契约（05-10 产出，禁止漂移）：Welcome JSON {"mode","cols","rows","prefs"?}——cols/rows 恒在、
[1,1000] 正整数；attach/升格/运行期推送三通道同形；推送帧按接收端当前 mode 携对应 prefs 档（ro 档永无 osc52）。
旧服务端无尺寸键 → sessionDims 保持 null → 渲染=fit（零漂移）。
本 plan 不动协议：C→S 帧形状零变化（RESIZE 仍 {"cols","rows"}，仅语义明确为窗口 fit 尺寸）。 -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: resize 链路重构——refit() 统一入口（上报=fit / 渲染=min 逐轴拆分）+ sendResize 去重 + overlay 语义钉死</name>
  <files>web/src/main.ts</files>
  <action>
    xterm 6.0.0 API 核实选型（web/package.json 钉版）：约束渲染用 `term.resize(cols, rows)`（ITerminal 稳定 API，FitAddon 自身底层同调用）+ `fit.proposeDimensions()` 取窗口 fit 尺寸（返回 {cols,rows} | undefined，display:none 时 undefined——PITFALLS C10 既有纪律）；**不采用**「CSS 约束容器再 fit」形态——那会使 proposeDimensions 返回被约束尺寸，上报/渲染两概念无法拆分（rw 端必须上报窗口物理尺寸驱动仲裁，否则升格后尺寸接管链断裂，G-05-1 设计约束 3）。

  具体改造（全部在 main.ts，行号为改造前参照）：

    1. 新增模块级状态（connect() 的 per-connection 重置块 L353-359 同批重置）：
       - `sessionDims: { cols: number; rows: number } | null = null`——最近一帧 Welcome 携会话尺寸；null = 旧服务端/未收到，渲染不约束。
       - `lastReported: { cols: number; rows: number } | null = null`——最近一次实际上行的 fit 尺寸（去重依据；只在真实发送后更新）。
       - `prevFit: { cols: number; rows: number } | null = null`——上一次 refit 的 fit 尺寸（overlay 触发判定）。
       - `roNotified = false`——ro console 一次性提示门闩（原「ro Welcome 每 attach 仅一次天然无重复」不变量被运行期推送打破后的等价物，Task 2 接线）。
    2. sendResize（L239-250）末尾 ws.send 前加去重：全部既有门（helloSent/isRO/ws OPEN/正整数校验）逐字保持位置不变，门后加 `if (lastReported !== null && lastReported.cols === cols && lastReported.rows === rows) return;` → 发送成功后 `lastReported = { cols, rows };`。注释写明：去重键 = 上行 fit 尺寸；ro 期被 isRO 门拦截的调用不触 lastReported（升格后首次 refit 必真实上报——05-08 纠正链的保持机制）。
    3. 新增 refit() 统一入口（收编 L270 窗口监听、L468 升格分支、L510 prefs 段、L582 onopen 四个 fit.fit() 调用点）：
       - `const d = fit.proposeDimensions(); if (!d) return;`（C10 守卫）
       - 渲染尺寸：`const rCols = sessionDims ? Math.min(d.cols, sessionDims.cols) : d.cols;`（rows 同理）——逐轴 min：窗口小于会话尺寸的轴按窗口渲染（裁剪语义不变，README 既有明示），大于的轴约束到会话（G-05-1 修复面）。
       - `if (term.cols !== rCols || term.rows !== rRows) term.resize(rCols, rRows);`（变化才调——幂等，推送重放零抖动）
       - 上报：`sendResize(d.cols, d.rows)`（恒报 fit 尺寸；去重在 sendResize 内）
       - overlay（D-17 语义钉死 = 窗口物理尺寸，本地 UX 辅助）：`const fitChanged = prevFit === null || prevFit.cols !== d.cols || prevFit.rows !== d.rows; prevFit = { cols: d.cols, rows: d.rows };` → `if (!welcomeDone || !resizeOverlayOn || !fitChanged) return;` 后接原 L258-265 overlay 驱动段逐字逻辑（文案 `${d.cols}x${d.rows}`）。注释登记：fit 尺寸未变的推送重放/约束变化不闪浮层（会话尺寸推送不改变本地窗口，浮层无由触发）；ro 端窗口 resize 浮层同显（05-UI-SPEC R4 既定语义保持）。
    4. 拆除 term.onResize 订阅（L252-266 整段移除）：其 sendResize/overlay 两职责已由 refit 收编；xterm 6 下 term.resize 的唯一调用方是本代码（fit 插件仅经 proposeDimensions 使用），无旁路触发面。保留 sendResize 函数本体（refit 调用）。
    5. onopen（L580-593）：`fit.fit()` → `refit()`；Hello sock.send 之后、helloSent 置位同批加 `lastReported = { cols: term.cols, rows: term.rows };`——Hello 载荷即首次尺寸上报（消除 80x24 首帧窗口的既有语义），同步 lastReported 防握手 Welcome 到达后的 refit 把等值尺寸作为「变化」重发一帧冗余 RESIZE（线序零漂移纪律：改造前握手后无此帧）。
    6. 窗口 resize 监听（L267-271）：debounce 100ms 逐字不变，`fit.fit()` → `refit()`。
    7. 帧常量注释块（L11-17）Welcome 载荷描述更新为 `{mode, cols, rows, prefs?}` 并指路 proto.go WelcomePayload（D-16 互相指路纪律）。

    禁止：不得给 #terminal 加任何 CSS/元素（留白 = body 背景纯布局，D-07 零新 UI）；不得改动 Hello/INPUT/RESIZE 帧形状；不得引入新依赖；overlay 文案格式不变。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh/web && time pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>tsc 严格模式 + vite build 干净通过；refit 四调用点收编完成；sendResize 去重在全部既有门之后；onResize 订阅移除且无残余引用（tsc noUnusedLocals 兜底）；行为级端到端断言由 05-12 D6 承载（本 wave 服务端尚未下发尺寸键）。</done>
</task>

<task type="auto">
  <name>Task 2: WELCOME 分支尺寸应用与升格交互 + ro 提示门闩 + dist 重建</name>
  <files>web/src/main.ts, web/dist/index.html</files>
  <action>
    1. WELCOME 分支（main.ts L441-463 区段）JSON.parse 成功后、mode 分支之前插入尺寸键处理：
       - 成对校验：`('cols' in w || 'rows' in w)` 视为服务端新形态 → 两键均须满足 typeof number && Number.isInteger && [1,1000] 区间才接受，`sessionDims = { cols: w.cols, rows: w.rows }`；任一键缺失/非法 → console.warn('ignoring invalid session dims in WELCOME frame') 且保持旧 sessionDims（D-16 容错纪律：非法输入不得使终端不可用，与 invalid query pref 同款静默降级方向）。
       - 两键均缺席（旧服务端）→ 不动 sessionDims（恒 null → 渲染不约束，行为零漂移）。
    2. mode 分支后、 prefs 段之前加一次 `refit()`（约束应用点：ro/rw 两分支统一覆盖——ro 端 attach 即按会话尺寸约束渲染，这正是 G-05-1 用户场景「宽端旁观」的修复动作；rw 端 attach/升格同理）。升格 rw 分支（L458-469）内部 `fit.fit()` → 删除（由分支后的统一 refit 承载）：**顺序硬约束 = sessionDims 先赋值（步骤 1）、isRO=false/disableStdin=false/setTitle 居中、refit 最后**——refit 时 sessionDims 已是新会话尺寸（= 本端 Hello 登记尺寸），min(fit, session) 自然解除约束回到窗口渲染（G-05-1 设计约束 4「先解除约束再 fit」的落地形态）；ro 期拖过窗口的端，refit 内 sendResize(当前 fit) 经 lastReported 去重判定（ro 期上报被门拦截未记账）→ 真实上行纠正服务端 cand.dims 瞬态偏差（05-08 纠正链保持，服务端 recalcNow 随后推送收口）。
    3. prefs 段末尾（L510）`fit.fit()` → `refit()`：fontSize/lineHeight/letterSpacing 改变单元格尺寸后 fit 与约束渲染同时重算（D-13 + RESEARCH §Pitfall 7 语义在约束形态下保持）。
    4. ro 一次性 console 提示（L453-457）包门闩：`if (!roNotified) { roNotified = true; console.info(...); }`——文案逐字不动（D1b 三要素断言 /input is not sent/、/clipped/、/reattach/ 锁定）；注释更新为「尺寸推送使 ro Welcome 运行期可多次到达，一次性语义改由门闩承载」。提示文案语义复核：「窗口小于会话尺寸可能裁剪」在约束形态下依然为真（min 逐轴——小窗口轴仍裁剪），文案零改动成立。
    5. connect() per-connection 重置块补 `sessionDims = null; lastReported = null; prevFit = null; roNotified = false;`（IN-01 防漂移登记延伸——auth_failed 重试/未来 Phase 6 重连不携带上连接残留尺寸约束）。
    6. 重建 dist：`time pnpm -C web build`（tsc && vite build && gzip -k9 既定链）；验证产物：`ls -l web/dist/index.html` 时间戳为本次构建 + `grep -c 'Math\.min' web/dist/index.html` ≥ 1（min 约束逻辑的结构指纹——Math.min 不被 esbuild 重命名；正向后验，非门控）。web/dist/*.gz 不入库（.gitignore 既定）不动。
    7. 幂等矩阵复核（写进代码注释）：尺寸推送重放同值 → term.resize 变化守卫跳过 + sendResize 去重拦截 + overlay fitChanged 拦截 → 零副作用；升格 Welcome（rw）→ 步骤 1-2 链；prefs 重放 → queryKeys 跳过 + osc52Loaded 门闩（既有）；welcomeDone/beforeunload → DOM 同 listener 去重（既有）。
  </action>
  <verify>
    <automated>cd /data1/home/zexueli/open_src/wesh/web && time pnpm build 2>&1 | tail -5 && ls -l dist/index.html && grep -c 'Math\.min' dist/index.html</automated>
  </verify>
  <done>构建干净且 dist/index.html 为本次产物（时间戳 + Math.min 指纹）；WELCOME 分支尺寸校验/赋值/refit 顺序锁定；ro 提示门闩在场且文案逐字不变；旧服务端形态（无尺寸键）代码路径保持渲染=fit。行为级端到端断言由 05-12 D6/S10 承载。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client Welcome | 会话尺寸键为远端输入——恶意/畸形值不得使终端崩溃或产生非法 resize |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05G-04 | Tampering | WELCOME 尺寸键解析 | medium | mitigate | 成对存在性 + typeof/Number.isInteger/[1,1000] 区间校验，非法即 console.warn 降级并保持旧值（D-16 容错纪律）；xterm term.resize 永收合法正整数 |
| T-05G-05 | Denial of Service | 推送风暴前端放大 | low | mitigate | refit 全链幂等：term.resize 变化守卫 + sendResize lastReported 去重 + overlay fitChanged 门——同值重放零副作用零回流（推送→refit→不上报→服务端无重算，反馈环结构性不存在） |
| T-05G-06 | Information Disclosure | sessionDims | low | accept | 纯布局整数，不进 console/localStorage/URL（T-03-24 红线延伸不涉及） |
| T-05G-SC | Tampering | 依赖供应链 | low | mitigate | 零新依赖（xterm 6.0.0 既有钉版 API 复用），Package Legitimacy Gate 不触发 |
</threat_model>

<verification>
- `time pnpm -C web build` 干净（tsc 严格 + vite + gzip 链），dist/index.html 时间戳更新、含 Math.min 指纹
- 代码走查核对清单：refit 四调用点收编；onResize 订阅移除；sendResize 去重在既有门后；connect() 重置块四状态清零；WELCOME 分支尺寸校验先于 mode 分支先于统一 refit
- 端到端行为门（05-12 承载，本 plan 不自证）：D6 约束渲染 rows/cols 断言 + 升格解除 + S10 协议面
</verification>

<success_criteria>
- 宽端 ro 旁观者 attach 后 xterm 按会话矩形渲染（留白为页面背景，零新 UI）
- rw 端任意时刻上行 RESIZE 恒为窗口 fit 尺寸；升格后纠正链一次往返内收口
- 重复/推送 Welcome 全链幂等；ro console 提示每连接恰一次
- 旧服务端（无尺寸键）下前端行为与改造前等价
</success_criteria>

<output>
Create `.planning/phases/05-multi-client/05-11-SUMMARY.md` when done
</output>
