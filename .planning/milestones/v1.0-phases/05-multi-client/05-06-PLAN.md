---
phase: 05-multi-client
plan: 06
type: execute
wave: 6
depends_on: ["05-03"]
files_modified:
  - internal/server/sharetoken.go
  - internal/server/server.go
  - internal/server/tickets.go
  - cmd/wesh/main.go
  - internal/server/sharetoken_test.go
autonomous: false
requirements:
  - MULTI-05

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "分享 token = 独立第三认证通道（D-01）：启动时生成 ro/rw 两个 128bit 随机 token（crypto/rand 16B → base64.RawURLEncoding 22 字符，tickets.go:45-49 形态）；持有效 token 可 GET 页面 + POST /api/attach 换 ticket（绕过 Basic）；无/错 token 时 P3 Basic 矩阵不变；与凭据共存（operator 走凭据、旁观者走链接）；/api/attach 不收 mode 参数——mode 由 token 绑定（ro token → ro ticket、rw token → rw ticket，P3 D-11 预期细化作废）"
    - "token 可复用至进程重启（D-02）：『一次性』落在 SEC-02 ticket 上（token → /api/attach → 一次性 ticket → Hello 链路不变）；ro/rw token 每轮启动重新随机生成——重启即废全部旧链接，吊销语义 = 重启"
    - "URL 形态 /s/{token}/ 路径段（D-03）：GET 时服务端即可校验 token 完成门禁；token 永不作 logEvent 参数（SEC-01 红线延伸——logEvent 三要素只有 remote/code/reason）；启动打印是产品行为不是日志（MULTI-05 明确授权）"
    - "mux 装配（GOROOT 1.22+ 通配语义，RESEARCH Pattern 6 三坑核实）：GET /s/{token}/ + path-only 405 fallback（Allow: GET——方法模式内建 405 会被 / 子树吞掉，P3 /api/attach 同款纪律）；/s/abc 无尾斜杠由 mux 内建 301 补斜杠（token 出现 Location 头属 D-03 已接受暴露面）"
    - "sharePage 双层响应（R-05 零新响应形态）：有效 token → r.URL.Path 改写为 / 后委托既有 embed handler（保留 gzip 旁路与 Vary 头语义；dist 单文件全内联无相对资源问题）；无效/缺席 token → 原样委托 / 处理链（凭据模式 basicAuth → 401 challenge 且 recordFail 自动计入 D-08 统一 per-IP 计数器；无认证模式直接给页）"
    - "token 通道与认证模式正交（OQ1 用户 2026-08-19 裁决）：无认证模式同样生成并打印链接；/api/attach 在无认证模式仅当 body 携 token 时非 404（ro/rw mode 绑定在无密码演示场景兑现）"
    - "有效 token 优先于 throttle 直接放行（capability 语义，避免 NAT 出口 IP 误伤持票旁观者）；token 校验失败经既有 401 路径自然计入统一 per-IP 退避计数器（R-03 零新代码）；128bit 空间使无节流枚举无意义（2^128）"
    - "shareTokens 仅两条目 struct{ro, rw [sha256.Size]byte} 启动生成即预哈希——不用 map 无 janitor（R-04：生命周期=进程，map/TTL 全是过度设计）；校验走 matchCredential 同款 SHA-256 等长化 + subtle 位或累积不短路（auth.go:56-65 修正形态），返回命中 mode"
    - "启动打印（MULTI-05 即打即用）：listening on 行不动，追加两行——share read-only:  {scheme}://{host}:{port}/s/{ro-token}/ 与 share read-write: ...（rw 行仅 --writable 时打印，D-05）；host 回填（D-04/R-04）：bind 0.0.0.0/:: → UDP-dial 路由感知首个非 loopback IPv4（net.Dial udp 192.0.2.1:80 零流量，RFC 5737——结构性避开 docker0/bridge，本机实证）→ 失败 fallback net.Interfaces() 索引序首个 up 非 loopback 首个 IPv4 → 全失败打印 bind 原样不阻断启动；具体 bind 原样使用；loopback bind 打印 loopback；端口取 ln.Addr()；scheme 随 TLS 分岔 https://"
    - "无效 token 的页面 GET 层零自绘 error page（UI-SPEC ## UI Considerations covered：share-link-entry——凭据模式浏览器原生 Basic 框、无认证模式直接给页，P3 矩阵不变）"
  prohibitions:
    - "token 任何形态（明文/base64/哈希前缀）不得出现在 logEvent、stderr 事件流、错误响应体、metrics 中（D-03 红线延伸 SEC-01）——唯一合法输出面是启动打印两行（MULTI-05 产品行为）与 URL 路径本身"
    - "不得按 REQUIREMENTS MULTI-05 字面实现『一次性链接』（token 单次使用）——D-02 裁决：token 可复用至重启、一次性在 ticket 上；单次失效的链接会使『复制链接发给同事』场景在首次使用后即废，违背即打即用意图"
  artifacts:
    - path: "internal/server/sharetoken.go"
      provides: "shareTokens 两条目 store（生成/预哈希/subtle lookup）+ sharePage handler + mux 装配"
      contains: "shareTokens"
    - path: "internal/server/server.go"
      provides: "attachHandler body token 分支（ro/rw mode 绑定签发）+ /s/ 路由注册（凭据与无认证双模式）"
      contains: "/s/{token}/"
    - path: "cmd/wesh/main.go"
      provides: "token 明文生成 + Options 传入 + 启动打印两行 + outboundIPv4 回填"
      contains: "share read-only:"
    - path: "internal/server/sharetoken_test.go"
      provides: "TestShareToken（lookup 矩阵 + /s/ 门禁 + attach token 分支，VALIDATION 05-02-01 Go 侧）"
      contains: "TestShareToken"
  key_links:
    - from: "cmd/wesh/main.go (启动)"
      to: "internal/server/sharetoken.go (shareTokens)"
      via: "main 生成两明文 token → Options.ShareTokenRO/ShareTokenRW 传入 → server 预哈希存储；main 持明文打印链接"
      pattern: "ShareToken"
    - from: "internal/server/server.go (POST /api/attach)"
      to: "internal/server/sharetoken.go (lookup)"
      via: "body JSON{token}（MaxBytesReader 4KiB 防御）→ lookup 命中 → tickets.issue(mode 绑定) → {ticket}；无/错 token → 委托既有 Basic 链"
      pattern: "lookup"
    - from: "internal/server/server.go (GET /s/{token}/)"
      to: "web/embed.go (embed handler)"
      via: "有效 → r.URL.Path = / 改写后委托；无效 → 原样委托 / 链（basicAuth 或直接给页）"
      pattern: "PathValue"
---

<objective>
MULTI-05 分享链接：ro/rw 两条 /s/{token}/ 链接启动即打即用——shareTokens 两条目 store（subtle 比较）+ /s/{token}/ 页面门禁（有效委托 embed、无效委托 / 链）+ /api/attach body token 分支（mode 绑定 ticket、绕过 Basic）+ 启动打印与出站 IP 回填（D-04 UDP-dial 路由感知）。

Purpose: 演示/协作的第一入口（ROADMAP 成功准则 3 后半）：用户预期『复制链接发给同事就能看』。token 通道与 Basic 凭据共存、与认证模式正交（OQ1）。
Output: sharetoken.go（新）+ server mux/attach 接线 + main.go 打印/回填 + TestShareToken，含 D-01+D-03 one-way 确认门。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-multi-client/05-CONTEXT.md
@.planning/phases/05-multi-client/05-RESEARCH.md
@.planning/phases/05-multi-client/05-PATTERNS.md
@.planning/phases/05-multi-client/05-VALIDATION.md
@.planning/phases/05-multi-client/05-UI-SPEC.md
@internal/server/server.go
@internal/server/tickets.go
@internal/server/auth.go
@cmd/wesh/main.go
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: 确认门——分享链接认证语义与 URL 形态（D-01 + D-03，one-way）</name>
  <read_first>
    - .planning/phases/05-multi-client/05-CONTEXT.md（decisions 节 D-01/D-02/D-03/D-04 逐字 + specifics 节 token 路径进反代日志的取舍）
  </read_first>
  <decision>分享链接 = 独立第三认证通道（持 token 绕过 Basic 换 mode 绑定 ticket）+ URL 形态 /s/{token}/ 路径段——两道 one-way door：认证语义是已分享链接的公开行为契约；URL 形态是用户书签/分享契约</decision>
  <context>
    D-01/D-03 在 discuss-phase 已锁定（含 D-02 token 复用至重启、D-04 host 回填）。一经发布，改认证语义会破坏所有已分享链接的预期行为，改 URL 形态会使既有书签失效。token 路径会进反代访问日志（D-03 已接受的取舍，README 脱敏建议在 05-09 落地）。本门是实现前最后一次确认，不是重新决策。
  </context>
  <options>
    <option id="as-locked">
      <name>按锁定值落地（推荐）</name>
      <pros>token 第三通道 + /s/{token}/ 路径段 + 复用至重启；与 CONTEXT.md D-01/D-02/D-03/D-04 逐字一致；服务端 GET 即可门禁（fragment 做不到）</pros>
      <cons>token 路径进反代访问日志（已裁决接受，README 明示脱敏）</cons>
    </option>
    <option id="revise">
      <name>调整认证语义或 URL 形态</name>
      <pros>若 threat model 变化可在此刻修正</pros>
      <cons>推翻 CONTEXT.md 锁定项，需重新 discuss 并回填 RESEARCH/UI-SPEC 全部派生结论</cons>
    </option>
  </options>
  <resume-signal>选择 as-locked（继续实现）或 revise（说明调整点）</resume-signal>
  <acceptance_criteria>
    - 用户明确选择 as-locked 后才进入 Task 2；revise 时停止执行并回填 CONTEXT.md
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: sharetoken.go（store + sharePage + mux）+ attachHandler token 分支 + tickets 注释兑现</name>
  <reversibility rating="one-way">分享链接认证语义（D-01）与 /s/{token}/ URL 形态（D-03）是公开行为契约——发布后变更破坏已分享链接与用户书签；确认门见 Task 1。</reversibility>
  <files>internal/server/sharetoken.go, internal/server/server.go, internal/server/tickets.go</files>
  <read_first>
    - internal/server/tickets.go（45-49 生成形态逐字复用点、16 行 mode 占位注释——本 plan 兑现）
    - internal/server/auth.go（56-65 matchCredential 位或累积修正形态——subtle 比较同款）
    - internal/server/server.go（179-207 Handler 三路装配与无认证 404 分支、190-198 405 fallback 先例、209-232 attachHandler 现状、497-515 checkTicket 顺序敏感）
    - web/embed.go（24-52 embed handler gzip 旁路与 Vary 语义——委托对象）
    - .planning/phases/05-multi-client/05-RESEARCH.md（Pattern 6 mux 三坑与页面伺服、Code Examples 分享 token 校验、裁断 R-03/R-05、Open Question 1 用户裁决）
    - .planning/phases/05-multi-client/05-PATTERNS.md（sharetoken.go 节——token 生成/subtle/mux 装配/委托形态全部先例）
  </read_first>
  <action>
    1. 新建 `internal/server/sharetoken.go`（同包 server）：
       - `type shareTokens struct{ ro, rw [sha256.Size]byte }`（R-04：仅两条目、生命周期=进程——注释：不用 map 无 janitor，map/TTL 清理全是过度设计）；`newShareTokens(ro, rw string)`——两明文 SHA-256 预哈希入 struct（SEC-01 长度侧信道纪律：等长化后比较）。
       - `func (st *shareTokens) lookup(token string) (string, bool)`（RESEARCH Code Examples 形态）：h := sha256.Sum256([]byte(token))；ro/rw 两组 subtle.ConstantTimeCompare 位或累积不短路（耗时与命中哪个 token 正交——ro/rw 是公开语义无需组序号防泄露，注释同源 auth.go）；命中返回 proto.ModeRO/ModeRW + true，否则 "" + false。注释红线：token 值永不入 logEvent 参数（D-03）。
       - `func (s *Server) sharePage(w, r)`：token := r.PathValue("token")（GOROOT request.go:1469）；s.shares.lookup 命中 → r.URL.Path = "/" 改写后调既有 embed handler（保留 gzip 旁路与 Vary 语义——dist 单文件全内联无相对资源问题，注释核实注）；未命中 → 原样委托 / 处理链（凭据模式 basicAuth(wh) → 401 challenge + recordFail 自动计入，R-03 零新代码；无认证模式 wh 直接给页——全站本无门，D-01『无/错 token 时 P3 Basic 矩阵不变』字面落地）。零新响应形态、不加 Cache-Control 新头。
       - mux 装配函数（凭据模式与无认证模式均注册——OQ1 正交裁决）：mux.Handle("GET /s/{token}/", ...) + mux.HandleFunc("/s/{token}/", path-only 405 fallback：Allow: GET + http.Error 405——注释：方法模式内建 405 会被 / 子树吞掉，P3 server.go:192-198 同款纪律）；注释登记 GOROOT 三坑（尾斜杠匿名多段通配 /s/abc/任意深度 也命中——token 段取值不受影响；无尾斜杠 301 补斜杠 token 入 Location 头属 D-03 已接受面）。
    2. `internal/server/server.go`：
       - Server 加 shares *shareTokens（New 内 newShareTokens(opts.ShareTokenRO, opts.ShareTokenRW)；Options 加 ShareTokenRO/ShareTokenRW string 两字段——main 持明文传入，server 只存哈希；两空串 = 通道关闭注释——本 phase main 恒生成）。
       - Handler 装配（179-207 区）：凭据模式与无认证模式均调用 sharetoken.go 的 mux 装配函数注册 /s/ 两条。
       - attachHandler 改造（209-232 区）：函数首位读 body（http.MaxBytesReader(w, r.Body, 4096) 防御放大）→ json.Unmarshal 提取 token 字段（解析失败=未携 token）→ 携 token 且 lookup 命中 → s.tickets.issue(mode=命中 mode) 返回 {"ticket": ...}（绕过 Basic——凭据模式下此分支在 basicAuth 中间件之前生效，装配形态：POST /api/attach 注册 handler 改为先做 token peek 的包装 handler，未携/错 token 再委托 originMiddleware(basicAuth(attachHandler)) 原链；有效 token 优先于 throttle 直接放行——capability 语义，R-03）→ 未携/错 token → 原 Basic 链零改动（401 同文同码无 oracle，失败经 recordFail 计入统一计数器）。
       - 无认证模式 /api/attach（201-203 现状 404 分支）改造：读 body 携 token 且命中 → issue（OQ1：仅当 body 携 token 时非 404）；否则维持 404（前端探测信号不变）。
       - tickets.go:16 行 mode 字段注释兑现：签发方由『全局 --writable 派生』扩展为『token 绑定 / Basic 全局模式』两通道（结构零改动——issue/redeem 签名与不变量不动）。
    3. `cmd/wesh/main.go`（本任务仅 Options 传参）：启动时 crypto/rand 16B×2 生成 ro/rw 两明文 token（base64.RawURLEncoding 22 字符，tickets.go:45-49 形态）→ Options.ShareTokenRO/ShareTokenRW 传入 server.New（打印在 Task 3）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - go build/vet 退出 0；server 包既有测试（含 auth_e2e/handshake 套件）-race 全绿——Basic 矩阵零回归
    - grep -c 'func (st \*shareTokens) lookup\|func (s \*Server) sharePage' internal/server/sharetoken.go == 2
    - grep -c '/s/{token}/' internal/server/server.go internal/server/sharetoken.go >= 2（GET 模式 + 405 fallback）
    - grep -c 'MaxBytesReader' internal/server/server.go >= 1（body 4KiB 防御）
    - grep -c 'ConstantTimeCompare' internal/server/sharetoken.go >= 2（ro/rw 双组位或不短路）
    - 无认证模式 /api/attach 携 token 非 404 分支存在（grep -B2 -A8 'func.*attachHandler\|StatusNotFound' internal/server/server.go 可见 token peek 逻辑）
  </acceptance_criteria>
  <done>分享 token 第三认证通道落码：/s/{token}/ 门禁 + 405 fallback + attach token 分支（mode 绑定、绕过 Basic、正交认证模式）；Basic 矩阵零回归。行为断言由 Task 3 测试锁定。</done>
</task>

<task type="auto">
  <name>Task 3: 启动打印两行 + outboundIPv4 回填 + sharetoken_test.go（TestShareToken）</name>
  <files>cmd/wesh/main.go, internal/server/sharetoken_test.go</files>
  <read_first>
    - cmd/wesh/main.go（272-278 启动打印区、TLS scheme 分岔先例、Task 2 的 token 生成点）
    - internal/server/tickets_test.go（1-20, 104-114 同包白盒 t.Run 形态——exact 先例）
    - .planning/phases/05-multi-client/05-RESEARCH.md（Pattern 7 打印形态与 outboundIPv4 逐字、裁断 R-04、本机实证结论）
    - .planning/phases/05-multi-client/05-VALIDATION.md（05-02-01 行：TestShareToken 断言面）
  </read_first>
  <action>
    1. `cmd/wesh/main.go`：
       - `func outboundIPv4() string`（R-04/Pattern 7 形态）：net.Dial("udp", "192.0.2.1:80")（RFC 5737 TEST-NET-1 零流量——注释：UDP dial 无握手仅让内核按路由表选出站接口本地地址，结构性避开 docker0/bridge 接口，2026-08-19 本机实证）→ LocalAddr().(*net.UDPAddr) IP.To4() 命中即返；失败 fallback：net.Interfaces() 索引序首个 up && !loopback 接口的首个 IPv4；全失败返回 ""（调用方兜底打印 bind 原样，不阻断启动）。
       - 启动打印（272-278 区）：listening on 行不动，追加两行——host 判定：bind 为 0.0.0.0/:: → outboundIPv4()（空则回退 bind 原样）；其余 bind 原样使用（D-04）；端口取 ln.Addr()；scheme 随 cfg.tlsCert 分岔。第一行 "share read-only:  {scheme}://{host}:{port}/s/{ro-token}/"（read-only 后两空格对齐 rw 行）；第二行 "share read-write: {scheme}://{host}:{port}/s/{rw-token}/" 仅 cfg.writable 时打印（D-05 总闸——不给 --writable 只打印 ro 行）。注释：启动打印是产品行为（MULTI-05 授权），token 永不入 logEvent/事件流（D-03 红线）。
    2. 新建 `internal/server/sharetoken_test.go`（`TestShareToken`，VALIDATION 05-02-01 Go 侧；同包白盒 store 测 + handler 集成，tickets_test.go 先例）：
       - lookup 矩阵（白盒 t.Run）：ro token 命中 → (proto.ModeRO, true)；rw token 命中 → (proto.ModeRW, true)；错 token/空串/22 字符同形异值 → ("", false) 无 oracle；生成形态断言（22 字符 base64.RawURLEncoding 字符集）。
       - /s/ 门禁（handler 集成，httptest 或 startTestServerWith 形态）：有效 ro/rw token GET /s/{token}/ → 200 且无 Basic challenge（凭据模式 spawn 实例——断言 WWW-Authenticate 头缺席）；错 token GET → 凭据模式 401 challenge（委托 / 链）/无认证模式 200 给页；POST /s/abc/ → 405 且 Allow: GET；/s/abc（无尾斜杠）→ 301 至 /s/abc/（GOROOT 内建行为断言——Location 头形态）。
       - /api/attach token 分支：ro token body → ticket 且经 Hello 核销得 mode=ro（dialHelloTicket 先例）；rw token body → mode=rw；错 token body → 与 Basic 失败同文同码（无 oracle 断言——响应体与无 token 401 逐字节一致）；无认证模式：body 携 token → 非 404 出 ticket；无 body → 404（探测信号不变）。
       - token 值红线：测试中断言材料仅存局部变量，永不进 t.Log/错误信息（phase04.mjs 同款红线纪律的 Go 映射——detail 只打状态码/布尔/形状）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test -race -count=1 -run TestShareToken ./internal/server/ && go test -race -count=1 ./internal/server/ ./cmd/wesh/</automated>
  </verify>
  <acceptance_criteria>
    - TestShareToken 全绿：lookup 矩阵 + /s/ 门禁（200/401/405/301）+ attach token 分支（mode 绑定/无 oracle/无认证正交）三面子测齐备
    - grep -c 'func TestShareToken' internal/server/sharetoken_test.go == 1（VALIDATION 05-02-01 命名逐字对齐）
    - grep -c 'share read-only:\|share read-write:' cmd/wesh/main.go == 2（两行打印逐字形态，read-only 后两空格对齐）
    - grep -c 'func outboundIPv4' cmd/wesh/main.go == 1 且含 192.0.2.1（UDP-dial 路由感知）
    - cmd/wesh 包测试绿（parseArgs/validateStartup 零回归）；server 包 -race 全量绿
  </acceptance_criteria>
  <done>MULTI-05 服务端全链锁定：启动打印两行链接（rw 行仅 --writable）、token GET 200 无 Basic、错 token → Basic 矩阵、token→ticket→attach 全链 mode 正确、无认证模式正交。</done>
</task>

</tasks>

## Flagged Assumptions (Spec-less Probe)

edge-probe 对本条需求的分类为 unclassified（中文文本启发式局限，非无边缘），按 #1110 保持 unresolved 显式记录：

- requirement: MULTI-05 ｜ probe: "unclassified — review manually" ｜ status: flagged-unverified ｜ 处置：边角面由 CONTEXT D-01..D-04、OQ1 裁决 与 RESEARCH Pattern 6/7、R-03/R-04/R-05、A4 实质覆盖；断言已提升为 must_haves.truths（mode 绑定、正交认证、mux 三坑、UDP-dial 回填），由 TestShareToken（Go 侧）与 05-09 phase05.mjs（UAT 侧）双侧实证；REQUIREMENTS『一次性 token』字面与 D-02 的偏差已在 prohibitions 显式拦截。

## Artifacts this plan produces

- 新文件 `internal/server/sharetoken.go`：`shareTokens` 类型（ro/rw [sha256.Size]byte 两条目）、`newShareTokens(ro, rw string)`、`func (st *shareTokens) lookup(token string) (string, bool)`、`func (s *Server) sharePage`、/s/ mux 装配函数（GET 模式 + 405 fallback）
- `internal/server/server.go`：`Options.ShareTokenRO`/`ShareTokenRW` 字段、attachHandler body token 分支（MaxBytesReader 4KiB + JSON{token} + mode 绑定签发）、无认证模式 /api/attach token 正交分支、/s/ 路由双模式注册
- `internal/server/tickets.go`：mode 字段注释兑现（两签发通道，结构零改动）
- `cmd/wesh/main.go`：ro/rw token 启动生成、`func outboundIPv4()`（UDP-dial + Interfaces 兜底）、启动打印 share read-only/read-write 两行
- 新文件 `internal/server/sharetoken_test.go`：`TestShareToken`

## Review 发现处置（05-REVIEWS.md 交叉评审）

| # | 发现（严重度） | 处置 | 说明 |
|---|---------------|------|------|
| #6 | token 暴露面大于 plan 所述——浏览器扩展/桌面索引/AV 扫描/截屏/PCAP 未覆盖（HIGH，与 05-08/05-09 共同处置） | **部分采纳** | T-05-06e 登记扩展暴露面（accept + 逐向量理由）；README 暴露面清单、nginx 脱敏示例与 TLS 部署建议由 05-09 落地；reviewer 的 replaceState 剥离建议与 D-03 锁定决策冲突——维持锁定，冲突记录见 05-08（D-03 路径段是分享/书签契约，且 D-10 手动刷新入口凭原 URL token 重新 attach） |
| — | 128bit 熵过大、建议 64bit 缩短 URL（MEDIUM） | **拒绝（锁定决策）** | D-01 锁定 128bit（22 字符 base64url）；64bit（11 字符）的 URL 长度收益微不足道；熵余量对 capability-URL 模型是零成本保险；reviewer 论证『暴露面使额外位数无意义』——对称地，缩短也同样不减少暴露面，不构成变更理由 |
| — | UDP-dial 出站 IP 探测在 VPN/自定义路由下脆弱（MEDIUM） | **接受** | 双 fallback 链既有（net.Interfaces 索引序 → 全失败打印 bind 原样不阻断启动——R-04/Pattern 7）；失败退化为次优展示地址而非功能故障（operator 可从 listening on 行获得真实地址） |
| — | 增加完全禁用分享链接的部署选项（Suggestion） | **拒绝（超出锁定范围）** | 新 flag 属新增功能面，超出 D-01/D-05 锁定范围且 MULTI-05 是硬需求；高安全部署的既有表达通道：不转发链接即不可见（128bit 不可枚举）+ --no-auth/凭据矩阵/TLS/Origin 白名单 |
| #11 | 9 波串行依赖链集成风险——建议 06 在 03 后启动（MEDIUM，跨切面） | **拒绝（维持波次，理由具体）** | 05-06 的 depends_on 已是最小逻辑集（仅 05-03——reviewer 判断正确）；wave 6 由 GSD 同波 files_modified 零重叠规则强制：本 plan 与 05-04（wave 4）/05-05（wave 5）均改 server.go，同波并发编辑同一文件必然冲突——这是物理文件所有权约束而非逻辑依赖；当前波次在该规则下已最短 |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 未认证 HTTP→/s/{token}/ 与 /api/attach | token 是唯一凭证——爆破/枚举/泄露即越权；token 通道不得削弱既有 Basic 矩阵 |
| token→日志/错误响应面 | token 出现在 URL 路径（D-03 已接受）——其他一切输出面必须零泄露 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Information Disclosure | 分享 token 在线爆破/枚举 | high | mitigate | 128bit 随机空间（2^128 枚举无意义）+ 失败经既有 401 路径计入 D-08 统一 per-IP 退避（R-03）+ subtle 位或不短路比较（时序正交）；TestShareToken 无 oracle 断言（错 token 响应与 Basic 失败逐字节一致） |
| T-05-06 | Information Disclosure | token 经日志/错误响应泄露 | high | mitigate | token 永不作 logEvent 参数（D-03 红线延伸 SEC-01——三要素只有 remote/code/reason）；MaxBytesReader + JSON 解析失败=未携 token（不回显）；残余面：URL 路径进反代访问日志——D-03 已裁决接受，README 脱敏建议在 05-09 落地（accept 的残余风险登记） |
| T-05-05b | Elevation of Privilege | token 通道削弱 Basic 矩阵或 throttle 语义 | medium | mitigate | 无/错 token 时 P3 Basic 矩阵零改动（auth_e2e 套件回归锁）；有效 token 优先于 throttle 放行是 capability 语义有意设计（R-03——避免 NAT 出口误伤持票旁观者，注释登记） |
| T-05-06e | Information Disclosure | token 经端点环境面泄露（浏览器历史/扩展/桌面索引/AV URL 扫描/截屏与屏幕共享/明文网络 PCAP） | medium | accept | D-03 capability-URL 模型取舍的扩展登记（review #6）：浏览器历史/地址栏/扩展/桌面索引/AV 扫描/截屏共享属端点环境面——自托管工具威胁模型外（端点失陷则凭据/会话同样不保）；明文 http:// 部署时 PCAP 可见完整 URL → README 增加 TLS 部署建议与暴露面清单（05-09 落地）；吊销语义 = 重启即废全部旧链接（D-02）；reviewer 的 replaceState 剥离建议与 D-03 锁定冲突，处置见 05-08 冲突记录 |
</threat_model>

<verification>
- go test -race -count=1 ./internal/server/ ./cmd/wesh/ 全绿（含 TestShareToken 三面断言）
- 源码断言：两行打印形态、outboundIPv4、mux 双注册、subtle 双组比较、MaxBytesReader 防御
</verification>

<success_criteria>
- 启动打印 ro/rw 两条 /s/{token}/ 链接，复制给同事即可看（ro）/可写（rw，仅 --writable 时打印）——ROADMAP 成功准则 3 后半
- 持 token 绕 Basic 换 mode 绑定 ticket；错 token 与 Basic 失败无区分（无 oracle）；重启即废全部旧链接
</success_criteria>

<output>
创建 `.planning/phases/05-multi-client/05-06-SUMMARY.md`：记录确认门结果、shareTokens 形态与 subtle 比较、/s/ 装配与 mux 三坑处置、attach token 分支与无 oracle 断言、outboundIPv4 实证结论、遗留（README 反代脱敏建议 → 05-09）。
</output>
