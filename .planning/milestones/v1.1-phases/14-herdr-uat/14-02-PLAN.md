---
phase: 14-herdr-uat
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - internal/server/e2e_test.go
  - internal/server/multi_test.go
autonomous: true
requirements: [PC-13]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "e2e_test.go 生命周期八测完成双模式断言分叉表改造：断开语义（shared=进程存活可重连 / per-client=断开即 SIGHUP 杀进程组）与重连语义（shared=接回同 pid（CORE-05）/ per-client=全新进程新 pid）两列显式成表，shared 列期望值逐字未动（D-01/D-02，蓝本 PITFALLS :378-399 e2e 行）"
    - "multi_test.go 十一测完成同类改造：fanout 列（shared=输出逐字节一致扇出 / per-client=各端各自进程输出互不串台）、MaxClients 列（shared=满员 503 / per-client=同 503 闸 + spawn-intent 口径不超编）、owner 四测列（TestOwnerPolicy:301 / TestAllPolicy:373 / TestSuccession:428 / TestSuccessionKickRace:497——蓝本「owner 递补（clients_test 大部）」行的实测归属修正：四测实在本文件，per-client 列=D-03 显式断言 owner 概念不装配）——蓝本 multi 行 + 归属偏差登记"
    - "全部经 newTestServer 小族装配 + t.Run(\"mode=shared\"/\"mode=per-client\") 双跑（D-01）；14-01 tracer 已实证装配点，本 plan 零新装配面——特殊调用点仅 multi_test.go:756 一处（TestMaxClients503 子测试 stderr 同步边），收编至 newTrackedTestServer"
    - "shared 列全部期望值字面与改造前逐字一致（D-02 红线；git diff 自审白名单 = 两文件结构包裹与分叉表新增）"
    - "startTestServerWith / startTrackedServerWith / startTrackedServerHandle 母本函数体零改动（e2e_test.go 内定义——改造只动调用点与测试体，装配函数本身保持 14-01 收编基准）"
  artifacts:
    - path: "internal/server/e2e_test.go"
      provides: "生命周期八测双模式断言分叉表"
      contains: "mode=per-client"
    - path: "internal/server/multi_test.go"
      provides: "fanout/MaxClients/计数不变量双模式断言分叉表"
      contains: "mode=shared"
  key_links:
    - from: "internal/server/e2e_test.go + internal/server/multi_test.go"
      to: "internal/server/harness_test.go"
      via: "全部装配调用收编至 newTestServer（14-01 落地）——startTrackedServerWith 仅经 harness shared 分支间接触达"
      pattern: "newTestServer"
  prohibitions:
    - requirement_id: PC-13
      category: values
      statement: "禁止断言放宽成「两模式都接受」——fanout/重连等两模式真值不同的断言严禁和稀泥（PITFALLS P11 警告征象：if mode == ... 跳过核心断言 即腐化）"
---

<objective>
mode-mapped 大型批：e2e_test.go（8 测，生命周期主干）与 multi_test.go（11 测，fanout/容量/计数不变量）双模式断言分叉表改造——两文件是三维归类蓝本中「同一需求两模式断言分叉」语义最重的批（断开/重连/fanout 真值两模式相反）。

Purpose: 这两文件承载 CORE-05（重连同 pid）与 MULTI-01（fanout 逐字节一致）等 v1.0 核心语义的零回归证据，per-client 列恰是这些语义的反转面——分叉表必须显式，和稀泥即证据毁灭。
Output: 两文件双模式改造 + -race 双模式逐名绿 + shared 列逐字未动自审。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-herdr-uat/14-CONTEXT.md
@.planning/phases/14-herdr-uat/14-RESEARCH.md
@.planning/phases/14-herdr-uat/14-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: e2e_test.go 生命周期八测双模式断言分叉表改造（D-01/D-02）</name>
  <reversibility rating="reversible">测试文件改造，git 可回退；无公开契约面。</reversibility>
  <files>internal/server/e2e_test.go</files>
  <read_first>
    - internal/server/e2e_test.go 全文（8 测 7 处普通装配调用：4 处 startTestServerWith + 3 处 startTestServer 兼容包装 + startTestServerWith:140 / startTrackedServerWith:171 / startTrackedServerHandle:199 母本定义 + killServer:120-134——改造对象；注意母本函数定义在同文件内，只动测试体与调用点；tracked 两母本在本文件零调用点）
    - internal/server/harness_test.go（14-01 落地的 newTestServer 小族——装配入口）
    - internal/server/perclient_test.go（Phase 11 断开 SIGHUP/重连新 pid/EXIT 私有化既有测——per-client 列断言的对照与边界，防双写漂移）
    - .planning/research/PITFALLS.md:378-399（蓝本 e2e 行：「per-client：断开即 SIGHUP、重连=新 pid（与 CORE-05 shared 断言相反）」）
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §Architecture Patterns 1/2（改造形态与红线）
  </read_first>
  <behavior>
    - 断开语义分叉：shared 列=客户端断开后 PTY 进程存活、重连接回（原断言逐字）；per-client 列=断开后进程组收 SIGHUP（pgid ESRCH 到达）
    - 重连语义分叉：shared 列=重连后同 pid（CORE-05 原断言逐字）；per-client 列=重连后新 pid（与旧 pid 不等）
    - 生命周期其余测（退出码传递/未知帧 1002/单次语义等）按蓝本归属逐测判定：同断言者两列同值，分叉者成表；归属与实测不符按 D-02 登记偏差
    - go test -v 输出八测均含 mode=shared / mode=per-client 子测试且双绿
  </behavior>
  <action>
    1. e2e_test.go 八测逐一包裹为 for-mode + t.Run("mode="+mode) 形态，装配调用换 newTestServer(t, mode, argv, mutate)（startTestServerWith / startTrackedServerWith / startTrackedServerHandle 函数定义本身逐字保留——14-01 收编基准）。
    2. 分叉表落地：断开/重连两族按 behavior 列成表；per-client 列 pid/ESRCH 观测形态复用 perclient_test.go 既有先例（waitPgroupESRCH 类探针——同包 helper 直接复用，零新写）。
    3. 同断言测（如未知帧 1002、握手类）两列跑同一断言体——注意红线：同断言是「该行为与进程模型无关」的显式判定，不是省略分叉表的理由；测试体内注释一行载明归属依据（蓝本行号）。
    4. 静默窗口观测一律 select + time.After 竞速（perclient_test.go:3-14 头注释纪律），客户端 Read 永不带 per-read deadline。
    5. time go test -race -count=1 -v -run 'TestEcho|TestExit|TestReconnect|TestDetach|TestSecond|TestUnknown' ./internal/server/（按实际测名调整）双模式全绿 → time go test -race -count=1 ./internal/server/ 全量回归；gofmt/vet 零输出；单提交收口（test(14-02) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v ./internal/server/ 2>&1 | grep -E '--- (PASS|FAIL): .*mode=' | head -80; time go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - e2e_test.go 全部 8 测含 t.Run("mode="+mode 包裹且经 newTestServer 装配（grep 命中数与测数一致）
    - -v 输出八测 mode=shared / mode=per-client 子测试全 PASS，零 FAIL（子测试 PASS 行有 4 空格缩进——grep 模式不带 ^ 锚）
    - shared 列期望值字面与改造前逐字一致（git diff 自审：仅结构包裹/分叉表/注释新增，零期望值字面改动）
    - startTestServerWith / startTrackedServerWith / startTrackedServerHandle 与 killServer 函数体零 diff（同文件内母本定义保持 14-01 收编基准）
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
  </acceptance_criteria>
  <done>生命周期八测双模式分叉表落地，CORE-05 反转面显式成表；shared 零回归证据逐字保持。</done>
</task>

<task type="auto">
  <name>Task 2: multi_test.go 十一测双模式断言分叉表改造 + owner 四测 D-03 未装配列（D-01/D-02/D-03，蓝本归属偏差修正）</name>
  <reversibility rating="reversible">测试文件改造，git 可回退；无公开契约面。</reversibility>
  <files>internal/server/multi_test.go</files>
  <read_first>
    - internal/server/multi_test.go 全文（11 测 16 处装配调用：13 处 startTestServerWith + 2 处 startTestServer 兼容包装 + 1 处 startTrackedServerWith :756——fanout/MaxClients/owner 四测现状；TestClientCountInvariant 不在本文件——实在 clients_test.go:29 为纯白盒 registry 测，归 14-06 收口核对登记）
    - internal/server/multi_test.go:301/373/428/497（owner 四测 TestOwnerPolicy/TestAllPolicy/TestSuccession/TestSuccessionKickRace——蓝本「resize_arb_test / TestGlobalCredit / owner 递补（clients_test 大部）」shared-only 行的实测归属修正：四测实在本文件，per-client 列走 D-03 显式断言未装配形态，蓝本 clients_test 归属误记按 D-02 登记偏差）
    - internal/server/multi_test.go:747-770（TestMaxClients503 的 :756 startTrackedServerWith 调用——captureStderr + waitHandlers 同步边，收编至 newTrackedTestServer）
    - internal/server/harness_test.go（newTestServer 小族装配入口）
    - internal/server/perclient_test.go（Phase 11-13 per-client 容量/spawn 既有测——MaxClients per-client 列的对照面；spawn-intent 口径见 11-03 D-03 复检回收落地形态）
    - .planning/research/PITFALLS.md:378-399（蓝本 multi 行：「fanout→各自进程输出；容量→spawn-intent 口径（P4）」+ shared-only 行）
    - internal/server/clients.go（注册表记账/容量闸/owner 递补与 WritePolicy 机制现状——分叉表与「未装配」断言的事实源）
  </read_first>
  <behavior>
    - fanout 分叉：shared 列=两端输出逐字节一致（原断言逐字）；per-client 列=各端输出即自身进程输出、互不含对方进程标记（双标记串交叉断言）
    - MaxClients 分叉：shared 列=满员第 N+1 端 503（原断言逐字）；per-client 列=同 503 闸 + 并发 attach 后实际 spawn 数 ≤ max-clients（spawn-intent 口径，程序序精确对照）；:756 子测试 stderr 同步边经 newTrackedTestServer 装配（两列同构）
    - owner 四测（TestOwnerPolicy/TestAllPolicy/TestSuccession/TestSuccessionKickRace）：shared 列=原断言逐字（owner 指派/全 rw/递补/递补竞态）；per-client 列=D-03 显式断言未装配——各端权限由自身 ticket 决定（rw/ro 门控归 PC-07 语义），owner 断开不触发任何递补行为（他端权限/角色零变化、无递补事件帧），可证伪（误装配递补逻辑即翻红），否决 t.Skip
    - 其余测按蓝本逐测归属判定，分叉者成表；归属与实测不符按 D-02 登记偏差
  </behavior>
  <action>
    1. multi_test.go 十一测逐一包裹 for-mode + t.Run 形态，装配换 newTestServer；:756 调用点换 newTrackedTestServer（waitHandlers 同步边保留）。
    2. fanout 测分叉表：per-client 列用双标记串（A 端 echo MARKER_A / B 端 echo MARKER_B——最简 echo 形态）交叉断言各自只见自身标记。
    3. MaxClients 测分叉表：per-client 列并发 attach 竞态形态沿用现有测试夹具，spawn 计数观测经 metrics wesh_pty_spawn_total 程序序对照（churn 格 :813 先例形态）或 perclient_test.go 既有计数通道——按可读性择一，注释载明。
    4. owner 四测：shared 列原断言逐字搬入；per-client 列按 behavior 落 D-03 未装配断言（双端 rw attach 后一端断开，断言他端权限/行为零变化且无 owner 指派/递补事件面——write-policy=owner × per-client 组合的启动期 warn 已在 Phase 10 落地，本列锁运行期未装配面）；测试体注释载明「蓝本 shared-only 行 owner 递补归属修正（蓝本误记 clients_test，实在本文件）——D-02 偏差登记」。
    5. time go test -race -count=1 -v -run 'TestMulti|TestFanout|TestMaxClients|TestOwner|TestAllPolicy|TestSuccession|TestDetach|TestExit|TestSigwinch|TestInputRate|TestWelcome' ./internal/server/（按实际测名调整）双模式全绿 → time go test -race -count=1 ./internal/server/ 全量回归；gofmt/vet 零输出；单提交收口（test(14-02) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v ./internal/server/ 2>&1 | grep -E '--- (PASS|FAIL): .*mode=' | head -80; time go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - multi_test.go 全部 11 测含 t.Run("mode="+mode 包裹且经小族装配；:756 调用点经 newTrackedTestServer（grep 可证）
    - -v 输出十一测 mode=shared / mode=per-client 子测试全 PASS（子测试 PASS 行 4 空格缩进——grep 模式不带 ^ 锚）
    - shared 列期望值字面与改造前逐字一致（git diff 自审：零期望值字面改动）
    - fanout per-client 列含双标记串交叉断言；MaxClients per-client 列含 spawn 数 ≤ max-clients 上界断言
    - owner 四测 per-client 列含未装配可证伪断言（owner 断开他端零变化 + 无递补事件面——grep 可证），零 t.Skip
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
  </acceptance_criteria>
  <done>fanout/容量/owner 十一测双模式分叉表落地；蓝本 multi 行 + owner 四测归属修正兑现，clients_test 误记偏差登记进 SUMMARY。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试改造面→零回归证据链 | fanout/重连/容量断言是 v1.0 MULTI/CORE 语义的证据本体——字面漂移即证据篡改面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03 | Tampering | fanout/重连两模式真值相反面被「和稀泥」改写（PITFALLS P11 警告征象本体） | high | mitigate | 断言分叉表显式成表（mode → expected）+ shared 列逐字未动 git diff 自审 + 14-12 收口闸 diff 白名单逐行终审 |
| T-14-04 | Tampering | per-client 列与 perclient_test.go 既有测双写漂移（同义断言两处维护） | medium | mitigate | read_first 强制对照既有测 + 各列只锁蓝本指定分叉点（细节面归 perclient_test.go 单一承载） |
| T-14-SC | Tampering | 依赖面（go.mod/go.sum/lockfile） | high | mitigate | 零新依赖红线——本 plan 无装包任务，legitimacy 门不触发；零 diff 由 14-12 收口闸断言 |
</threat_model>

<verification>
- time go test -race -count=1 -v ./internal/server/ 两文件 19 测双模式子测试逐名全绿
- git diff 自审：shared 列期望值逐字未动，白名单 = 两文件结构包裹与分叉表新增
</verification>

<success_criteria>
1. e2e 八测断开/重连语义分叉表显式成表（CORE-05 反转面可证伪）
2. multi 十一测 fanout/MaxClients 分叉表 + owner 四测 D-03 未装配列落地（spawn-intent 口径锁定，蓝本归属偏差修正登记）
3. shared 列逐字未动，-race 双模式全绿（D-02 验收闸前置自审）
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `internal/server/e2e_test.go`：8 测改造为 `t.Run("mode=shared"/"mode=per-client")` 双跑 + 断开/重连语义断言分叉表（装配调用收编至 newTestServer）
- `internal/server/multi_test.go`：11 测改造为双跑 + fanout 各自进程输出/MaxClients spawn-intent 口径断言分叉表 + owner 四测（TestOwnerPolicy/TestAllPolicy/TestSuccession/TestSuccessionKickRace）per-client 列 D-03 显式断言未装配

<output>
Create `.planning/phases/14-herdr-uat/14-02-SUMMARY.md` when done
</output>
