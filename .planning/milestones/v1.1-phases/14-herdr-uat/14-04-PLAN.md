---
phase: 14-herdr-uat
plan: 04
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - internal/server/resize_arb_test.go
  - .planning/phases/14-herdr-uat/14-CONTEXT.md
autonomous: true
requirements: [PC-13]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "resize_arb_test.go 一测（四子测试）落 D-03 双模式形态：shared 列=仲裁器 min-rect 行为原断言逐字（:111/:144/:190/:248 四处 startResizeServer 本地 sess 变体调用点收编至 newSessTestServer，本地 helper 删除）；per-client 列=显式断言仲裁器未装配——异尺寸双端各自尺寸直通（经 sessions 访问器逐一 ptySize/pollSize 观测）、无 min-rect 收敛、线上零 'W' 帧，可证伪（有人误在 per-client 装配仲裁器时测试翻红），否决 t.Skip（蓝本 resize 行 wire 面的实测归属即本文件——resize_test.go 为 arbitrate() 纯函数测，wire 级 resize 行为断言唯本文件承载）"
    - "clients_test.go 三测（TestClientCountInvariant:29 / TestWriterMergeControlFramesOnly:96 / TestAfterDrainResendsDims:150）实测判定为纯白盒测（package server，registry/writer 直构造，零 server 装配）——保持单跑原样，判定依据登记 SUMMARY + CONTEXT 回写（D-02 纪律）；蓝本「owner 递补（clients_test 大部）」行归属误记登记偏差：owner 四测实在 multi_test.go:301/373/428/497，D-03 未装配列由 14-02 承载"
    - "resize_test.go 三测（TestArbitrate:26 / TestSessionDimsLocked:87 / TestPushSessionDimsKickRecalc:123）实测判定为 arbitrate() 纯函数白盒测（零 server 装配，无 mode 参数可参数化）——保持单跑原样并登记；仲裁器「不装配」的 wire 级防线由 resize_arb_test.go per-client 列单一承载（防双写漂移）"
    - "shared 列全部期望值字面与改造前逐字一致（D-02 红线；git diff 自审白名单 = resize_arb_test.go 结构包裹与未装配断言新增 + CONTEXT.md 偏差回写）"
  artifacts:
    - path: "internal/server/resize_arb_test.go"
      provides: "D-03 仲裁器未装配显式断言（per-client 分支）+ 本地 startResizeServer 收编删除"
      contains: "mode=per-client"
    - path: ".planning/phases/14-herdr-uat/14-CONTEXT.md"
      provides: "蓝本归属偏差回写（owner 四测实在 multi_test.go；resize wire 面实在 resize_arb_test.go；clients_test/resize_test 纯白盒判定）"
      contains: "14-04"
  key_links:
    - from: "internal/server/resize_arb_test.go"
      to: "internal/server/resize.go"
      via: "per-client 列断言「未装配」的可证伪面 = 仲裁器在 per-client 分支不进入装配路径（运行期零 'W' 帧/各自尺寸直通为 wire 级观测）——owner 递补的同类防线归 14-02（multi_test.go owner 四测）"
      pattern: "arbiter|newSessTestServer"
  prohibitions:
    - requirement_id: PC-13
      category: values
      statement: "mode-exclusive 测试的 per-client 分支禁止 t.Skip 或空分支——「不装配」必须做成可证伪断言（D-03 否决 t.Skip 的既定裁决：腐化无信号）；禁止断言放宽成「两模式都接受」"
---

<objective>
mode-exclusive 未装配断言批（修订后实测形态）：resize_arb_test（1 测 4 子测试，shared-only）的 D-03「显式断言未装配」双模式改造——三维归类第三维（mode-exclusive）的落地承载——+ clients_test/resize_test 纯白盒面判定登记（蓝本两处归属偏差的 D-02 登记与 CONTEXT 回写）。

Purpose: 「不装配」若无断言承载，未来有人误在 per-client 装配仲裁器时无任何测试翻红——D-03 把组件缺席做成可证伪防线，这是三维归类相对 naive 参数化的核心增量。修订期实证修正了蓝本两处归属误记（owner 递补四测实在 multi_test.go；resize wire 级行为实在 resize_arb_test.go），clients_test/resize_test 为纯白盒文件，无可参数化装配面。
Output: resize_arb_test.go 双模式改造（newSessTestServer 首证）+ 未装配断言可证伪形态 + 纯白盒判定与偏差登记（SUMMARY + CONTEXT 回写）+ -race 双模式逐名绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-herdr-uat/14-CONTEXT.md
@.planning/phases/14-herdr-uat/14-RESEARCH.md
@.planning/phases/14-herdr-uat/14-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: resize_arb_test.go D-03 双模式改造——仲裁器未装配显式断言 + startResizeServer 收编（D-01/D-03，newSessTestServer 运行期首证）</name>
  <reversibility rating="reversible">测试文件改造，git 可回退；无公开契约面。</reversibility>
  <files>internal/server/resize_arb_test.go</files>
  <read_first>
    - internal/server/resize_arb_test.go 全文（1 测 4 子测试，4 处 startResizeServer 本地 sess 变体调用 :111/:144/:190/:248——仲裁器 min-rect wire 级现状断言；蓝本 resize 行「单端 last-wins/双模式断言分叉」的 wire 面实测归属即本文件）
    - internal/server/harness_test.go（newTestServer 小族装配入口——newSessTestServer 为本任务装配形态）
    - internal/server/perclient_test.go（Phase 12 RESIZE 直通/防抖既有测——per-client 列断言对照面，防双写漂移；ro 放行 RESIZE 测为互补面）
    - internal/server/resize.go（arbiter 防抖与变化检测机制——shared 列事实源与「未装配」断言的对照本体）
    - .planning/research/PITFALLS.md:378-399（蓝本 resize 行：「per-client=直通 + 防抖（P7），无 min-rect」+ shared-only 行「resize_arb_test……断言「未装配」而非强跑」）
    - .planning/phases/14-herdr-uat/14-CONTEXT.md D-03（显式断言未装配的既定形态：resize 直通无 min-rect 约束、线上零 'W' 帧；否决 t.Skip）
  </read_first>
  <behavior>
    - shared 列：四子测试仲裁器原断言逐字（min-rect 收敛/逐维独立取 min/变化检测/防抖面——现状断言零改写）
    - per-client 列：异尺寸两端（如 100x30 / 60x20）attach 后各自 RESIZE 直通生效（各自 TIOCSWINSZ 等于自报值，钳制 [1,1000] 与 50ms 防抖保留），无 min-rect 收敛（两端尺寸不互相压缩），运行期帧流零 'W' 类型字节——任一反向出现即 FAIL（可证伪）
    - 装配形态：四处 startResizeServer 调用点收编至 newSessTestServer(t, mode, argv, nil)，sess 读回统一经 sessions() 访问器（shared=单例，per-client=spawnedSessions 逐会话）；startResizeServer 本地 helper 删除（本文件为其唯一调用面）
    - go test -v 输出子测试含 mode=shared / mode=per-client 双绿，零 t.Skip
  </behavior>
  <action>
    1. resize_arb_test.go 套用 for-mode + t.Run("mode="+mode) 包裹形态；四处装配调用点换 newSessTestServer；shared 列原断言逐字搬入（ptySize/pollSize 经 sessions() 访问器取唯一会话——访问器形态注释一行载明）。
    2. per-client 列实现：双端异尺寸 attach（Hello 携不同 cols/rows），各自发 RESIZE 后经 sessions() 逐一 ptySize/pollSize 观测各自会话 winsize == 各自自报值（钳制后），帧收集面断言零 'W' 类型字节；测试体注释载明 D-03「显式断言未装配——误装配仲裁器时本列翻红」与蓝本行归属（resize 行 wire 面）。
    3. startResizeServer 本地 helper 删除（收编后全仓零引用——D-01 散点消除）；ptySize/pollSize 保留（per-client 列消费）。
    4. 与 perclient_test.go 既有 Phase 12 直通测的边界：本列锁「分叉点 + 未装配」（直通 vs 仲裁 + 零 'W' 帧），resize 风暴/防抖细节归 perclient_test.go 单一承载，不重复造同义断言。
    5. time go test -race -count=1 -v -run 'TestArbitrate|TestResizeArbitration' ./internal/server/（按实际测名调整）双模式全绿 → time go test -race -count=1 ./internal/server/ 全量回归；gofmt/vet 零输出；单提交收口（test(14-04) 前缀）。
  </action>
  <verify>
    <automated>time go test -race -count=1 -v -run 'TestArbitrate|TestResizeArbitration' ./internal/server/ && time go test -race -count=1 ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - resize_arb_test.go 含 mode=shared / mode=per-client 子测试且 -v 输出全 PASS（零 t.Skip——grep 无 t.Skip 命中）
    - 四处装配调用点经 newSessTestServer（grep 可证）；startResizeServer 已删除且全仓零引用（grep -rn 零命中）
    - per-client 列含零 'W' 帧断言与各自 winsize 直通断言（grep 可证观测面存在）
    - shared 列期望值字面与改造前逐字一致（git diff 自审：零期望值字面改动）
    - time go test -race -count=1 ./internal/server/ 全绿；go vet 与 GOROOT gofmt -l 零输出
  </acceptance_criteria>
  <done>resize_arb D-03 双模式落地：仲裁器在 per-client 的缺席成为可证伪防线；newSessTestServer 形态获运行期首证；startResizeServer 散点消除。</done>
</task>

<task type="auto">
  <name>Task 2: 纯白盒面判定登记批——clients_test/resize_test 纯函数单跑判定 + 蓝本归属偏差登记与 CONTEXT 回写（D-02）</name>
  <reversibility rating="reversible">登记性任务（SUMMARY + CONTEXT 回写），git 可回退；两测试文件零改动。</reversibility>
  <files>.planning/phases/14-herdr-uat/14-CONTEXT.md</files>
  <read_first>
    - internal/server/clients_test.go 全文（3 测：TestClientCountInvariant:29 / TestWriterMergeControlFramesOnly:96 / TestAfterDrainResendsDims:150——package server 白盒，registry/writer 直构造零 server 装配，修订期已实证）
    - internal/server/resize_test.go 全文（3 测：TestArbitrate:26 / TestSessionDimsLocked:87 / TestPushSessionDimsKickRecalc:123——package server 白盒，arbitrate() 纯函数直测零装配，修订期已实证）
    - internal/server/multi_test.go:301/373/428/497（owner 四测实测归属——14-02 承载 D-03 未装配列的对照确认）
    - .planning/research/PITFALLS.md:378-399（蓝本表：「resize_arb_test / TestGlobalCredit / owner 递补（clients_test 大部）」shared-only 行与「resize_test（单端 last-wins）」双模式行——两处归属误记的登记对象）
    - .planning/phases/14-herdr-uat/14-CONTEXT.md D-02（蓝本为准 + 偏差登记纪律——「plan 偏差登记 + 本 CONTEXT 回写」通道）
  </read_first>
  <behavior>
    - clients_test.go / resize_test.go 两文件零改动——纯白盒测无装配可参数化，保持单跑原样（形态判定，非覆盖缺口）
    - 偏差登记三则进 SUMMARY + CONTEXT 回写：① 蓝本「owner 递补（clients_test 大部）」误记——owner 四测实在 multi_test.go:301/373/428/497，D-03 未装配列由 14-02 承载，clients_test 三测为纯白盒单跑；② 蓝本「resize_test（单端 last-wins）双模式断言分叉」行的 wire 面实在 resize_arb_test.go——resize_test 三测为 arbitrate() 纯函数测保持单跑；③ 蓝本 multi 行「计数不变量」的白盒面 = clients_test.go TestClientCountInvariant（纯白盒单跑），wire 面 = multi_test.go TestMaxClients503（14-02 已双跑）
    - CONTEXT 回写位置：decisions 节 D-02 条目下追加「偏差登记」小节（执行期偏差亦续登此处）
  </behavior>
  <action>
    1. 复核两文件纯白盒形态（修订期 grep 实证：clients_test/resize_test 零装配族调用 + package server——harness 小族在 server_test 包，包墙结构上不可达，且两文件无 server 装配可参数化）。
    2. 14-CONTEXT.md 回写：D-02 条目下追加「偏差登记（14-04 规划修订期实证）」小节，逐则登记 behavior 三则偏差（每条含蓝本行原文、实测归属、处置面、证据命令）。
    3. 本任务无代码改动；gofmt/vet 不涉及；SUMMARY 收口时复核 CONTEXT 回写与 14-06 收口核对清单的一致性（同一偏差不双头登记矛盾——14-06 清单引用本处登记）。
    4. 单提交收口（test(14-04) 前缀——docs 面登记随测试批提交）。
  </action>
  <verify>
    <automated>grep -n 'owner 四测实在 multi_test.go' .planning/phases/14-herdr-uat/14-CONTEXT.md && test -z "$(git status --porcelain internal/server/clients_test.go internal/server/resize_test.go)"</automated>
  </verify>
  <acceptance_criteria>
    - 14-CONTEXT.md 含 D-02 偏差登记小节与三则偏差逐条登记（grep 可证）
    - clients_test.go / resize_test.go git status 零输出（两文件零改动）
    - SUMMARY 含纯白盒判定清单与偏差登记（与 CONTEXT 回写一致）
  </acceptance_criteria>
  <done>纯白盒面判定与三则蓝本归属偏差登记完毕（SUMMARY + CONTEXT 双通道）；三维归类第三维（mode-exclusive + 纯白盒保持面）收口。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试改造面→零回归证据链与装配漂移防线 | shared 列是 v1.0 仲裁语义证据本体；per-client 列是「组件不装配」的防腐防线——两面都怕字面漂移与空断言 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07 | Tampering | mode-exclusive 的 per-client 分支写成 t.Skip/空断言，未来误装配仲裁器时无信号（D-03 否决形态本体） | high | mitigate | 可证伪断言强制（零 'W' 帧/各自直通观测面）+ 验收闸 grep 零 t.Skip + 14-12 终审 |
| T-14-08 | Tampering | resize 分叉表「和稀泥」（直通与仲裁两语义都被接受） | high | mitigate | 断言分叉表显式成表 + shared 列逐字未动 git diff 自审 + 14-12 收口闸 diff 白名单终审 |
| T-14-08b | Tampering | 蓝本归属误记被静默跟改或重复登记（owner 递补/resize wire 面两处偏差） | low | mitigate | Task 2 单点登记（SUMMARY + CONTEXT 回写双通道一致）+ 14-06 收口核对引用而非重登记 |
| T-14-SC | Tampering | 依赖面（go.mod/go.sum/lockfile） | high | mitigate | 零新依赖红线——本 plan 无装包任务，legitimacy 门不触发；零 diff 由 14-12 收口闸断言 |
</threat_model>

<verification>
- time go test -race -count=1 -v ./internal/server/ resize_arb 四子测试双模式逐名全绿
- grep resize_arb_test.go 零 t.Skip；per-client 列未装配断言观测面在场
- git diff 自审：shared 列期望值逐字未动；clients_test/resize_test 零改动
- 14-CONTEXT.md D-02 偏差登记小节三则在册
</verification>

<success_criteria>
1. resize_arb 四子测试直通 vs 仲裁分叉 + 仲裁器未装配 per-client 列可证伪（D-03 否决 t.Skip 兑现，PC-05 语义对照面）
2. 纯白盒面（clients_test/resize_test 六测）单跑判定与蓝本两处归属偏差登记落地（D-02 纪律）
3. shared 列逐字未动，-race 双模式全绿
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `internal/server/resize_arb_test.go`：1 测 4 子测试改造为 `t.Run("mode=shared"/"mode=per-client")` 双跑 + per-client 分支仲裁器未装配显式断言（零 'W' 帧 + 各自直通）；`startResizeServer` 本地 helper 删除（收编至 `newSessTestServer`）
- `.planning/phases/14-herdr-uat/14-CONTEXT.md`：D-02 偏差登记小节新增（规划修订期实证三则——非代码符号）

<output>
Create `.planning/phases/14-herdr-uat/14-04-SUMMARY.md` when done
</output>
