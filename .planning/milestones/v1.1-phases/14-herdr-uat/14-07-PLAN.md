---
phase: 14-herdr-uat
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/server/load_test.go
autonomous: true
requirements: [PC-12]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "TestLoadPerClientFloodMatrix 落地（//go:build load 隔离）：N ∈ {1,4,16,32} 会话各自独立 seq 洪水，每客户端各发一次触发 INPUT（per-client 每会话独立 stdin——与 shared 格单端触发的关键差异点，RESEARCH §Pattern 4）；断言每端收流完整（字节数跨端相等 + 流尾末位字段==loadFloodLast()）+ wesh_pty_spawn_total==N（程序序精确对照）+ kicks==0 + 峰值采样入 LOADDATA cell=pc_flood 行——per D-10"
    - "TestLoadPerClientResident 驻留格落地：N ∈ {1,4,16,32} 会话 sh 零输入驻留；wesh 侧硬断言 Alloc 增量 ≤ N×(768KiB outbox+inputQ 账面 + ε 容差)（双采样差值形态，ε 首跑实测定值并注释论证——churn 格 +8 goroutine 容差推导形态先例）；子进程观测 /proc/<pid>/status VmRSS 采样入 LOADDATA cell=pc_resident + ≤15MB/进程宽松上界防线——per D-12"
    - "fd 账面 ~2×N+pidfd、goroutine 账面 1+6N（ARCHITECTURE §10 表）作差值断言参照；回收窗口轮询禁固定 sleep 精确时点（200ms 间隔 + 护栏上限，churn 格 :776-789 母本）"
    - "LOADDATA per-client 扩展字段产出：cell=pc_flood / pc_resident 前缀 + sessions=N spawn_total=N rss_per_proc_max=… 字段（09-09 D-13 先例表格形态的数据源——README 标定表回填由 14-11 承载）"
    - "D-11 两段式纪律：maxClients=32 默认值不动（零公开契约变更）；仅当实测数据证伪（32 会话资源超不可接受界线）时停下升级 one-way 确认门（13-01 D-01 先例：用户派发确认），executor 不得自行改默认值或调常量"
    - "load tag Linux 手动跑纪律不变（load_test.go:1-12 头注释逐字纪律继承）；darwin 面沿用 skip 先例（P9 kqueue N 规模由 CI macOS leg 常规测试承担）"
  artifacts:
    - path: "internal/server/load_test.go"
      provides: "TestLoadPerClientFloodMatrix + TestLoadPerClientResident 双剖面格 + pc_flood/pc_resident LOADDATA 行"
      contains: "TestLoadPerClientFloodMatrix"
  key_links:
    - from: "internal/server/load_test.go"
      to: "README.md"
      via: "LOADDATA cell=pc_flood/pc_resident 行 → 14-11 README 资源义务段标定表回填（09-06 D-11/D-12 → 09-09 D-13 先例链）"
      pattern: "LOADDATA"
  prohibitions:
    - requirement_id: PC-12
      category: values
      statement: "负载数据不得被「美化」回填——实测值超账面即如实登记并升级 D-11 one-way 门，禁止为让账面成立而调松断言常量或删格（标定诚信是「验证为主、证伪才改」纪律的本体）"
---

<objective>
per-client 负载矩阵双剖面（D-10/D-12）：load_test.go 新增洪水格（N∈{1,4,16,32} 每会话独立 seq 洪水）与驻留格（N 会话 idle 空转），实测内存/fd/goroutine/吞吐有界性，产出 LOADDATA 行作为 README 标定表与 maxClients 建议值的数据源——「32 并发 shell 是重负载」账面推算（ROADMAP Research flag 唯一 MEDIUM 置信面）的实证裁决。

Purpose: SC4 要求负载矩阵实测回填；D-11 把本格定位为「默认 32 是否成立」的实证裁决工具——默认动作零契约变更，数据证伪才开 one-way 门。
Output: 双剖面格 + LOADDATA 数据行 + （条件性）D-11 证伪升级记录。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-herdr-uat/14-CONTEXT.md
@.planning/phases/14-herdr-uat/14-RESEARCH.md
@.planning/phases/14-herdr-uat/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 洪水格 TestLoadPerClientFloodMatrix——N∈{1,4,16,32} 每会话独立洪水（D-10）</name>
  <reversibility rating="reversible">load 测试新增，git 可回退；//go:build load 隔离不进常规 CI，无公开契约面。</reversibility>
  <files>internal/server/load_test.go</files>
  <read_first>
    - internal/server/load_test.go:1-12（//go:build load 头注释纪律）与 :80-297（夹具层全套：drainClient:80 / drainRateLimited:103 / awaitDrain:126 / assertClosed1000:139 / dialLoadClient:149 / scrapePeakSampler:174 / allocPeakSampler:211 / readAlloc:232 / loadFloodLast:242 / gatedFloodArgv:256 / startLoadSamplers:268）
    - internal/server/load_test.go:298-380（TestLoadFanoutMatrix shared 母本——单进程洪水仅 conns[0] 触发 :331 的差异点本体）与 :555-575（countFds/readProcState）
    - internal/server/load_test.go:725-835（TestChurnPerClientSpawnThrottle——startPerClientServer(t, gatedFloodArgv(last), nil) 装配同款 :726 + 双采样差值断言母本 :809-831 + spawn_total 程序序对照 :813）
    - internal/server/perclient_test.go:60-122（startPerClientServerWithSpawn Cleanup 追踪纪律——32 会话洪水子进程群泄漏防线）
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §Architecture Patterns 4（洪水格设计与断言面）与 §Pitfalls 7（禁无基线对照绝对上限）
  </read_first>
  <behavior>
    - N ∈ {1,4,16,32} 每格：N 端 dialLoadClient + drainClient 就绪后，每客户端各发一次 {proto.Input, 'x', '\n'} 触发 INPUT（每会话独立 stdin 各自起洪水）
    - 每端收流完整：字节数跨端相等 + 流尾末位字段 == loadFloodLast()
    - wesh_pty_spawn_total == N（程序序精确对照）；kicks == 0（活跃读面零误踢）
    - 峰值内存/fd/goroutine 采样入 LOADDATA cell=pc_flood 行（含 sessions=N spawn_total=N 字段）
    - awaitDrain 240s/端护栏沿用；N=32 总量 32×33.8MB 超时按先例上调并注释论证（A2）
  </behavior>
  <action>
    1. load_test.go 新增 TestLoadPerClientFloodMatrix：last := loadFloodLast()，for N ∈ {1,4,16,32} t.Run(fmt.Sprintf("sessions_%d", n))；装配 startPerClientServer(t, gatedFloodArgv(last), nil)（churn 格 :726 同款）。
    2. N 端 dialLoadClient + drainClient 就绪，runtime.GC() 基线 + startLoadSamplers 峰值采样启动（:268 先例）；随后循环每端各写一次触发 INPUT——注释载明「per-client 每会话独立 stdin，单端触发先例必须调整（RESEARCH §Pattern 4）」。
    3. awaitDrain 240s/端 + assertClosed1000；断言面：字节数跨端相等 + 末位字段==last + getMetrics wesh_pty_spawn_total==N + kicks==0；t.Logf 产 LOADDATA cell=pc_flood 行（字段沿用 :370-371 形态 + sessions/spawn_total 扩展）。
    4. 运行：time go test -tags=load -count=1 -timeout=30m -run 'TestLoadPerClientFloodMatrix' ./internal/server/ -v 全绿（四格逐名）；LOADDATA 行摘录进 SUMMARY。
    5. gofmt（GOROOT）与 go vet（-tags=load 面）零输出；单提交收口（test(14-07) 前缀）。
  </action>
  <verify>
    <automated>time go test -tags=load -count=1 -timeout=30m -run 'TestLoadPerClientFloodMatrix' ./internal/server/ -v</automated>
  </verify>
  <acceptance_criteria>
    - load_test.go 含 func TestLoadPerClientFloodMatrix( 且四格 sessions_1/4/16/32 子测试全 PASS
    - 每格断言含 spawn_total==N 与 kicks==0 与末位字段==last（grep 可证断言面存在）
    - 输出含 LOADDATA cell=pc_flood 行四行（sessions=1/4/16/32 各一）
    - 触发 INPUT 为每客户端各发一次的循环形态（grep 可证，非 conns[0] 单端）
    - go vet -tags=load 与 GOROOT gofmt -l 零输出；既有 load 格零改动（git diff 白名单 = 新增函数）
  </acceptance_criteria>
  <done>洪水格四档全绿，pc_flood LOADDATA 数据行产出；每会话独立触发差异点正确落地。</done>
</task>

<task type="auto">
  <name>Task 2: 驻留格 TestLoadPerClientResident——N 会话 idle 空转账面实证（D-12 + D-11 两段式）</name>
  <reversibility rating="reversible">load 测试新增，git 可回退；D-11 默认值变更仅数据证伪时经 one-way 确认门（本任务不触发即零契约变更）。</reversibility>
  <files>internal/server/load_test.go</files>
  <read_first>
    - internal/server/load_test.go:725-835（churn 格双采样差值断言母本：基线/终态双采样 + 回收窗口轮询 :776-789 + 容差注释论证 memDeltaCeil :822 + spawn_total 程序序对照 :813）
    - internal/server/load_test.go:555-575（countFds / readProcState——/proc/<pid>/status VmRSS 读取同文件先例）
    - internal/server/clients.go:34-50（defaultOutboxBytes = 512*1024 :36 与 inputQ 256KiB :50 账面锚点）
    - .planning/research/ARCHITECTURE.md §10 :475-486（资源账面表：fd ~2×N+pidfd、goroutine 1+6N、32 shell 驻留 ~160MB 推算——证真/证伪对象）
    - .planning/phases/14-herdr-uat/14-CONTEXT.md D-11（两段式：默认不动；证伪才开 one-way 门）与 D-12（wesh 侧硬断言 + 子进程观测口径）
    - .planning/phases/14-herdr-uat/14-RESEARCH.md §Pattern 4 驻留格段与 §Assumptions A3/A4
  </read_first>
  <behavior>
    - N ∈ {1,4,16,32} 每格：startPerClientServer(t, []string{"sh"}, nil) + N 端 attach 后零输入驻留
    - wesh 侧硬断言：Alloc 增量 ≤ N×(768KiB+ε)（ε 首跑实测定值并注释论证）；goroutine ≤ 基线+6N+ε；fd ≤ 基线+2N+pidfd+ε
    - 子进程观测：每会话 /proc/<pid>/status VmRSS 采样入 LOADDATA cell=pc_resident（含 rss_per_proc_max 字段）+ ≤15MB/进程宽松上界
    - 驻留稳定后双采样差值断言（基线在 attach 前 GC 后采，终态在驻留窗口后采）；回收/稳定等待用轮询 + 护栏，禁固定 sleep 精确时点
    - 若 32 格实测超不可接受界线（如 Alloc 增量数倍于账面或 VmRSS 持续超上界）：任务停下，SUMMARY 记录证伪数据，向用户升级 D-11 one-way 确认门——不得自行改 maxClients 默认值或调常量
  </behavior>
  <action>
    1. load_test.go 新增 TestLoadPerClientResident：for N ∈ {1,4,16,32} t.Run；装配 startPerClientServer(t, []string{"sh"}, nil)；N 端 dialLoadClient attach（无需 drain——零输入驻留）。
    2. 双采样：attach 前 runtime.GC() + readAlloc/countFds/goroutine 基线；attach 全部就绪 + 驻留窗口（轮询 spawn_total==N 确认全活）后终态采样；差值断言三面（Alloc ≤ N×(768KiB+ε) / goroutine ≤ 基线+6N+ε / fd ≤ 基线+2N+pidfd+ε），ε 首跑实测定值并在注释载明推导（churn 格 +8 容差形态先例——Pitfall 7 禁无论证常量）。
    3. 子进程观测：经 startPerClientServerWithSpawn 的 spawnedSessions 或 stderr session_start pid 归因取 pid 集，readProcState 读 /proc/<pid>/status VmRSS 逐进程采样，rss_per_proc_max 入 LOADDATA cell=pc_resident 行 + ≤15MB/进程宽松上界断言（防 wesh 侧大 env/驻留缓冲注入子进程泄漏面；bash 自身 RSS 环境因子不硬断——注释载明 D-12 口径）。
    4. 运行：time go test -tags=load -count=1 -timeout=30m -run 'TestLoadPerClientResident' ./internal/server/ -v 全绿（四格逐名）；LOADDATA 行摘录进 SUMMARY；32 格实测与 ~160MB 账面（A4）的对照结论（证真/证伪）显式登记。
    5. D-11 两段式判定：数据在可接受界线内 → 默认值不动，数据供 14-11 建议值表；证伪 → 停下升级 one-way 确认门（本任务到此中断，SUMMARY 记录证伪数据与待决项）。
    6. gofmt（GOROOT）与 go vet（-tags=load 面）零输出；单提交收口（test(14-07) 前缀）。
  </action>
  <verify>
    <automated>time go test -tags=load -count=1 -timeout=30m -run 'TestLoadPerClientResident' ./internal/server/ -v</automated>
  </verify>
  <acceptance_criteria>
    - load_test.go 含 func TestLoadPerClientResident( 且四格 sessions_1/4/16/32 子测试全 PASS
    - 差值断言三面（Alloc/goroutine/fd）在场且 ε 容差常量有注释论证来源（grep 可证）
    - 输出含 LOADDATA cell=pc_resident 行四行（含 rss_per_proc_max 字段）
    - VmRSS ≤15MB/进程宽松上界断言在场；等待面零固定 sleep 精确时点（grep 轮询形态可证）
    - SUMMARY 含 32 格与 ~160MB 账面证真/证伪结论 + D-11 判定结果登记
    - go vet -tags=load 与 GOROOT gofmt -l 零输出；既有 load 格零改动
  </acceptance_criteria>
  <done>驻留格四档全绿，pc_resident LOADDATA 数据行产出；160MB 账面实证结论与 D-11 判定（默认不动或证伪升级）登记完毕。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 负载格→本机资源 | 32 会话 × 33.8MB 洪水子进程群的 CPU/内存冲击面——泄漏即宿主机资源耗损 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-13 | DoS | 洪水/驻留格子进程群（seq/bash ×32）泄漏抢资源（测试返回后继续输出级联减速——e2e_test.go:120-134 教训先例） | medium | mitigate | startPerClientServerWithSpawn Cleanup 逐一 Kill+Close 追踪纪律（perclient_test.go:88-98）+ 30m timeout 护栏 + load tag Linux 手动跑隔离 |
| T-14-14 | Tampering | 标定诚信面：为让账面成立调松断言常量/删格（「验证为主、证伪才改」纪律破坏） | medium | mitigate | ε 容差注释论证强制（Pitfall 7）+ D-11 两段式（证伪升级 one-way 门而非自行调整）+ LOADDATA 原始行入 SUMMARY 留档 |
| T-14-SC | Tampering | 依赖面（go.mod/go.sum/lockfile） | high | mitigate | 零新依赖红线——本 plan 无装包任务，legitimacy 门不触发；零 diff 由 14-12 收口闸断言 |
</threat_model>

<verification>
- time go test -tags=load -count=1 -timeout=30m -run 'TestLoadPerClient' ./internal/server/ -v 双剖面八格全绿
- LOADDATA cell=pc_flood / pc_resident 行各四行产出并入 SUMMARY
- 32 格与 ~160MB 账面证真/证伪结论 + D-11 判定登记
</verification>

<success_criteria>
1. 洪水格四档：每端收流完整 + spawn_total==N + kicks==0（D-10）
2. 驻留格四档：Alloc ≤ N×(768KiB+ε) + VmRSS ≤15MB/进程观测入档（D-12）
3. maxClients=32 默认值维持不动，或证伪数据经 one-way 门升级（D-11 两段式）
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `internal/server/load_test.go`：`TestLoadPerClientFloodMatrix` 洪水格函数（sessions_1/4/16/32 子测试）+ `TestLoadPerClientResident` 驻留格函数（sessions_1/4/16/32 子测试）+ `LOADDATA cell=pc_flood` / `cell=pc_resident` 行格式（含 sessions/spawn_total/rss_per_proc_max 扩展字段）

<output>
Create `.planning/phases/14-herdr-uat/14-07-SUMMARY.md` when done
</output>
