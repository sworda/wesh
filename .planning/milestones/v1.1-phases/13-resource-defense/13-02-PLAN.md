---
phase: 13-resource-defense
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/server/spawnthrottle.go
  - internal/server/perclient.go
  - internal/server/server.go
  - internal/server/metrics.go
  - internal/server/perclient_test.go
autonomous: true
requirements: [PC-08]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "并发子进程计数达到 max-clients 时第 max-clients+1 个握手收 503；恰好等于 max-clients 时 spawn 允许放行（等值两侧各一步断言）——PC-08 boundary 探针边（explicit）；三道闸（503/pre-spawn 再闸/注册点复检回收）Phase 11 已成立，本 plan 保持零改动"
    - "并发计数为 hubMu 保护的整数精确比较，无浮点/取整/舍入面——PC-08 precision 探针边（explicit）：len(s.pcSessions) >= s.maxClients 整数比较（perclient.go:162-168 现状保持），令牌桶计数为 rate.Limiter 内部整数/浮点语义的库级既定实现，判定输出为 Allow() 布尔无舍入歧义"
    - "spawn 双令牌桶为内部常量：全局 8/s burst 16（防断网惊群）+ per-IP 1/s burst 4（防单点 churn），Options 覆写仅供测试注入，零 CLI flag/TOML 键（per D-03，12-03 defaultSlowDwell 先例同构）"
    - "取不到令牌的拒绝 wire 形态 = Error{server_error, 定值常量文案} + Close(1011)——与容量再闸/spawn 失败同码同串（wire 聚合、日志细分第三次应用，per D-04）；分辨率由 logEvent 事件名 spawn_throttled 承担 + mc.ptySpawnThrottled 计数器递增（per D-08 四件之一）"
    - "关闭码避开 1006 硬约束满足：1011 不在前端 shouldReconnect 触发集（web/src/main.ts:1023 仅 1006；reconnect.test.ts:16-20 对 1000/1001/1008/1011/1013 全 false），无重连放大循环（per D-04）"
    - "per-IP 桶键 = s.proxy.clientIP(r) XFF 换键（--auth-header 信任闸开启时以 XFF 链首作键，未开启用 TCP 对端——throttleStore XFF 换键同闸采信先例，per D-05）；桶键在 Attach 上游（server.go:883 ip := s.proxy.clientIP(r) 既有提取点）提取后经签名扩展传入 upgradePerClient"
    - "per-IP 桶存储 = map + 惰性过期防无界增长（throttle.go:36-53/:70-85 先例同形态；15min 量级窗口，无常驻 janitor goroutine——零新 exitf 分支纪律）"
    - "桶判定是纯内存操作放闸内（Allow() 无阻塞），spawnFunc 调用点（perclient.go:174）保持 hubMu 之外不动（Anti-Pattern 1：fork/exec 阻塞不得冻结全控制面）"
  artifacts:
    - path: "internal/server/spawnthrottle.go"
      provides: "spawn 双令牌桶存储（全局桶 + per-IP map + 惰性过期 + 四内部常量）"
      contains: "defaultSpawnGlobalRate"
    - path: "internal/server/perclient.go"
      provides: "upgradePerClient 桶判定挂点（pre-spawn 容量闸之前/同位）+ spawn_throttled 拒绝序列"
      contains: "spawn_throttled"
    - path: "internal/server/server.go"
      provides: "Options 四覆写字段 + New 零值兜底 + per-client 分支桶装配 + Attach 调用点传 ip"
      contains: "SpawnGlobalRate"
    - path: "internal/server/metrics.go"
      provides: "metricsCounters 四枚新 atomic.Int64（ptySpawn/ptySpawnFailures/ptyKills/ptySpawnThrottled——一次性全加，本 plan 仅接线 ptySpawnThrottled 递增点）"
      contains: "ptySpawnThrottled"
    - path: "internal/server/perclient_test.go"
      provides: "TestPerClientSpawnThrottle tracer e2e + 扩展测试组"
      contains: "TestPerClientSpawnThrottle"
  key_links:
    - from: "internal/server/server.go"
      to: "internal/server/perclient.go"
      via: "Attach :883 既有 ip := s.proxy.clientIP(r) 提取 → :1043 upgradePerClient 调用点传参 → pre-spawn 桶判定"
      pattern: "clientIP"
    - from: "internal/server/perclient.go"
      to: "internal/server/metrics.go"
      via: "节流拒绝点 mc.ptySpawnThrottled.Add(1) → snapshotMetrics 读出（13-05 接线 series 输出）"
      pattern: "ptySpawnThrottled"
  prohibitions:
    - requirement_id: PC-08
      category: safety
      statement: "节流拒绝不得触发前端自动重连放大循环——关闭码绝不使用 1006（每重连一次 spawn 尝试 = 节流面自我放大 fork 循环，PITFALLS P4）；1008 亦禁用（认证/版本策略受众，容量/节流策略混入污染受众分治）"
    - requirement_id: PC-08
      category: values
      statement: "防线不得误伤反代后的合法多用户——桶键不经 XFF 换键的实现在反代部署下把全部用户压进共享一桶，per-IP 1/s burst 4 形同虚设（D-05 裁决的存在理由）"
---

<objective>
PC-08 churn 防线本体（本 phase tracer）：spawn 双令牌桶端到端——桶存储（spawnthrottle.go 新件）→ Options/New 装配 → Attach 桶键传参 → upgradePerClient pre-spawn 闸前判定 → 拒绝序列（Error+1011+spawn_throttled 日志+计数器）→ Go e2e 断言。纵穿本 phase 的 server 改动层（新存储件/装配层/挂点/观测计数器），端到端证明「已认证高频断开重连被限速且前端不重连放大」；13-03/13-05/13-06/13-07 全部消费本 plan 建立的机制与计数器字段。

Purpose: PITFALLS P4 fork bomb 防线——合法票据 connect→spawn→disconnect 死循环打不垮服务端；断网惊群（N 浏览器同时自动重连 = N 个 fork+exec）由全局桶收口。
Output: spawnthrottle.go 新件 + 双桶装配 + 拒绝序列 + 四计数器字段 + tracer e2e 与扩展测试组。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-resource-defense/13-CONTEXT.md
@.planning/phases/13-resource-defense/13-RESEARCH.md
@.planning/phases/13-resource-defense/13-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: tracer——spawn 双令牌桶端到端（存储件 + 装配 + 挂点 + 拒绝序列 + 计数器字段 + 单路径 e2e 断言）（D-03/D-04/D-05）</name>
  <reversibility rating="reversible">D-03 内部常量+零公开契约面（不暴露 flag/TOML）；D-04 wire 形态复用既有 Error{server_error}+1011 第三次同构（非新契约）；spawn_throttled 事件名与计数器为 additive 新增。</reversibility>
  <files>internal/server/spawnthrottle.go, internal/server/server.go, internal/server/perclient.go, internal/server/metrics.go, internal/server/perclient_test.go</files>
  <read_first>
    - internal/server/throttle.go 全文 108 行（throttleStore mu+map+惰性过期母本——:11-14 常量区 / :36-53 结构+构造零值兜底 / :70-85 recordFail 内 15min 惰性过期）
    - internal/server/perclient.go:133-190（rejectCapacity :133-137 拒绝序列逐字母本 + upgradePerClient :143 签名现状与 pre-spawn 闸 :162-168 + spawnFunc 调用点 :174 + spawn 失败分支 :183-186）与 :236（rate.NewLimiter 既有用法）
    - internal/server/server.go:883/:898（ip/remote 提取点）与 :1040-1046（upgradePerClient 调用点）与 :183-192（Server 字段区 pcSessions 注释块）与 :249-317（Options 结构——inputRate/inputBurst 零值兜底先例所在）与 :382-395（New 零值兜底先例）与 :535-538（New per-client 分支桶装配点）
    - internal/server/metrics.go:52-58（metricsCounters 现状五枚 atomic——四枚追加点）
    - internal/server/proxy.go:89-103（clientIP XFF 换键——trust 且 XFF 非空 → 链首 IP（ParseIP 校验闸）→ 回退 TCP 对端）
    - internal/server/perclient_test.go:40-135（harness + dialHello + readOutputUntil）与 :441-452（SpawnFunc 注入 atomic 失败开关夹具）与 :588-610（barrier 竞态注入先例）
    - .planning/phases/13-resource-defense/13-RESEARCH.md §Pattern 1 + §Code Examples 1（桶参数/挂点/拒绝序列逐字段蓝本）
  </read_first>
  <behavior>
    - 全局桶与 per-IP 桶串联判定：任一 Allow() 失败即拒（全局 8/s burst 16 防惊群；per-IP 1/s burst 4 防单点 churn；四值均为内部常量 defaultSpawnGlobalRate/defaultSpawnGlobalBurst/defaultSpawnPerIPRate/defaultSpawnPerIPBurst）
    - 拒绝序列与 rejectCapacity 同码同串不同事件名：Error 帧（proto.ErrServerError + capacityMessage 同一定值文案——wire 不可区分三拒绝是有意为之）→ logEvent(remote, 1011, "spawn_throttled", remoteUser) → Close(websocket.StatusInternalError, proto.ErrServerError) → mc.ptySpawnThrottled.Add(1) → return nil
    - per-IP 桶条目惰性过期：lastSeen 距今 > 15min 重置条目（map 上界纪律——56B/条 × 4096 IP ≈ 230KB 内存界论证平移 throttle.go 先例）；取桶与判定在 store 方法内持 mu 完成（单锁最小临界区）
    - Options 四覆写字段零值兜底：New 内逐一 <= 0 → 对应 default 常量（inputRate/inputBurst 先例形态）；shared 模式零装配（桶字段 nil，upgradePerClient 为唯一消费点——「装配期一次分岔」纪律）
    - tracer e2e：覆写 per-IP 桶参数（rate=1 burst=2 级小值）→ 同 IP 连续 attach 第 burst+1 个起收 1011 + Error server_error + stderr 出现 spawn_throttled 事件行 + 计数器观测递增
  </behavior>
  <action>
    1. 新建 internal/server/spawnthrottle.go：四内部常量声明（注释载 D-03 不暴露公开配置面与取值依据——PITFALLS P4 推荐量级）；spawnThrottleStore 结构（mu + global *rate.Limiter 单例 + perIP map[string]条目{limiter *rate.Limiter, lastSeen time.Time}）；构造函数的零值兜底四参数形态（newThrottleStore :39 先例注释「测试经参数覆写提速」同构）；allow(ip string, now time.Time) bool 方法——now 参数化（throttleStore recordFail(now) 先例，测试时间注入面）内做惰性过期重置 + per-IP 桶创建/取用 + 全局桶 Allow() 串联；注释载 D-05 键来源纪律（调用方传 clientIP 产物，本件不碰 *http.Request）。
    2. server.go：Options 加 SpawnGlobalRate/SpawnGlobalBurst/SpawnPerIPRate/SpawnPerIPBurst 四 int 字段（注释分档先例：测试覆写通道，OutboxBytes/SlowDwell 同档）；New 零值兜底四行；Server 字段区加 spawnThrottle *spawnThrottleStore；New per-client 分支（:535-538）初始化桶（make 同位——零值兜底后构造）；shared 分支零装配。Attach 调用点（:1043）传 :883 既有 ip——upgradePerClient 签名扩展一位（见步 3）。
    3. perclient.go：upgradePerClient 签名加 throttleIP string 形参（remoteUser 之后）；桶判定段插在 pre-spawn 容量闸（:162-168）之前——判定失败走 behavior 拒绝序列（注释载 D-04：wire 聚合日志细分第三次应用；1011 避开 1006 重连触发集 main.ts:1023 实证；拒绝路径零注册零登记零残留——半开名额已在升档分岔前 release() 既有结构性保证）；容量闸与 spawnFunc 调用点逐字不动。
    4. metrics.go：metricsCounters 追加四枚 atomic.Int64——ptySpawn/ptySpawnFailures/ptyKills/ptySpawnThrottled（D-08 四件一次性全加，注释载各递增点归属 plan：本 plan 接线 ptySpawnThrottled，其余三件由 13-05 接线递增点、series 输出归 13-05）；本文件除结构体四行外零改动。
    5. tracer e2e（perclient_test.go）：TestPerClientSpawnThrottle——startPerClientServerWithSpawn mutate 覆写四桶参数小值 → 同 loopback IP 连续 dialHello 至 burst 耗尽 → 下一端断言收 Error 帧（ErrServerError 定值文案）+ close code == 1011 逐值（Pitfall 3 判别面）+ captureStderr/parseEvents 断言 spawn_throttled 事件恰一 + 事件四段 schema（remote/code/reason 键，零敏感值）+ 既有容量闸路径不受影响（桶参数放宽后 max_clients 拒绝仍 1011 同码——两事件名分治断言）。
    6. go build ./... && go vet ./... && time go test -race ./internal/server/ -count=1 全绿（shared 既有组与 per-client 既有组全绿 = 零回归）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && time go test -race -run 'TestPerClientSpawnThrottle' ./internal/server/ -count=1 -v && time go test -race ./internal/server/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - internal/server/spawnthrottle.go 存在且含四常量（8/16/1/4）与 allow(ip, now) 参数化形态；全仓 grep 无对应 CLI flag/TOML 键（`grep -rn 'spawn.global\\|spawn_per_ip\\|spawn-rate' cmd/wesh/` 零命中——D-03 零公开面）
    - 拒绝路径 Close 码为 websocket.StatusInternalError 常量（region grep upgradePerClient 函数体：StatusInternalError 命中、1006 魔数零命中）
    - tracer e2e -race 全绿：1011 逐值断言 + spawn_throttled 事件恰一 + 计数器递增观测三通道齐
    - upgradePerClient 内 spawnFunc 调用点（:174 区）保持 hubMu 之外（region grep：spawnFunc( 调用行不在 Lock/Unlock 区间内——形态承现状）
    - `git diff --quiet HEAD -- cmd/wesh/main.go web/`（本 plan 零 CLI/前端面改动）
    - 全量 time go test -race ./internal/server/ -count=1 绿（per-client 既有测试组原样绿 = 挂点插入零回归）
  </acceptance_criteria>
  <done>churn 防线端到端成立：同 IP 高频 attach 被双桶限速收 1011 + spawn_throttled 日志与计数器出数，容量三道闸零改动，前端不重连放大（1011 不在触发集）。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 扩展测试组——全局桶独立判定 / XFF 换键两态 / 惰性过期 / 计数器与定值文案（D-03/D-04/D-05 收口）</name>
  <files>internal/server/perclient_test.go</files>
  <read_first>
    - internal/server/perclient_test.go（Task 1 新增的 TestPerClientSpawnThrottle 与 harness mutate 覆写面）
    - internal/server/proxy_e2e_test.go:116-165（TestXFFThrottleKey 同形态先例——trust on/off 两态键行为断言结构）
    - internal/server/throttle.go:70-85（惰性过期被测形态——now 参数注入）
    - internal/server/events_test.go:427-471（TestSessionEnd schema 断言母本——captureStderr → parseEvents → eventsNamed 计数锁 + 键存在/缺席双向断言）
  </read_first>
  <behavior>
    - 全局桶独立判定：per-IP 参数放宽（大 burst）+ 全局小 burst → 耗尽后任意 IP 皆 1011（惊群防线独立成立）
    - XFF 换键两态：AuthHeader 配置开启时不同 X-Forwarded-For 头值的客户端各自独立桶（A 耗尽不影响 B）；未开启时全部 loopback 同键（任一耗尽皆拒）——proxy_e2e_test.go:121 先例同构
    - 惰性过期：构造出口或 allow(ip, now) 直调注入 lastSeen 超窗 → 条目重置后令牌满额（map 无界增长防线单元级断言）
    - 拒绝文案定值：Error 帧 payload 与容量拒绝逐字相同（wire 不可区分三拒绝）；spawn_throttled 事件四段 schema 零敏感值（token/ticket/凭据永不入参红线）
  </behavior>
  <action>
    1. 四组扩展测试落 perclient_test.go（单一家纪律）：全局桶/XFF 两态/惰性过期/文案定值。
    2. XFF 两态经 harness Options.AuthHeader 配置 + dialHello 请求头注入 X-Forwarded-For 不同值；harness 若不支持头注入则扩展 dialHello 变体（phase07.mjs UAT 层已有 XFF 先例可对照形态——Go 侧以 ws.DialOptions HTTPHeader 注入）。
    3. 惰性过期测试直调桶件（package server_test 经 export_test.go 出口或同包构造——若需新出口，export_test.go 加 spawnThrottleAllowForTest 薄桥，ForTest 四件套纪律）。
    4. TDD 提交收口：Task 1 feat 提交 + 本任务 test 提交（11-01/11-03 先例）。
    5. time go test -race -run 'TestPerClientSpawnThrottle|TestPerClientSpawnGlobal|TestPerClientSpawnXFF|TestSpawnThrottleExpiry' ./internal/server/ -count=1 -v 全绿 → 全量 time go test -race ./internal/server/ -count=1 回归。
  </action>
  <verify>
    <automated>time go test -race -run 'TestPerClientSpawn' ./internal/server/ -count=1 -v && time go test -race ./internal/server/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 四组扩展测试全部存在且 -race 全绿（XFF 两态含 trust on 异键互不影响与 trust off 同键共享两半边）
    - 惰性过期断言经 now 参数注入确定性成立（零真实等待 15min——时间注入面）
    - Error 帧文案与 capacityMessage 逐字一致断言在场（wire 聚合口径锁定）
    - perclient_test.go 既有测试断言行零改动（git diff 白名单 = 仅新增函数；export_test.go 若加薄桥归白名单登记）
  </acceptance_criteria>
  <done>PC-08 churn 防线四证齐：双桶独立成立、XFF 换键两态正确、桶存储有界、拒绝 wire/日志/计数器三通道锁定。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server（attach 频率） | 已认证客户端的 connect→spawn→disconnect 频率是 churn 防线的不可信输入面——fork 预算保护必须在 spawn 调用点之前 |
| 反代→server（XFF 头） | XFF 值只在 --auth-header 信任闸开启时被采信为桶键（07-03 既有信任边界），伪造头在 trust off 时零影响 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04 | DoS | 已认证 churn fork bomb（合法票据 connect→spawn→disconnect 死循环） | high | mitigate | spawn 双令牌桶（D-03 全局 8/s burst 16 + per-IP 1/s burst 4，Task 1）+ churn 压测 RSS/goroutine/fd 有界断言（13-07） |
| T-13-05 | DoS | 断网惊群（N 浏览器同时自动重连 = N 个 fork+exec） | high | mitigate | 全局桶 8/s 上限（Task 1）+ 前端退避既有（30s 封顶，06-03 既有零改动） |
| T-13-06 | DoS | 节流拒绝误入 1006 → 前端重连放大 fork 循环 | medium | mitigate | D-04 锁 1011（不在 shouldReconnect 触发集，main.ts:1023 实证）+ Task 1 e2e 逐值断言 close code |
| T-13-07 | DoS | per-IP 桶键不换 XFF → 反代部署全部用户共享一桶误伤合法重连 | medium | mitigate | D-05 桶键 = s.proxy.clientIP(r) 同源（Task 1 签名传参）；Task 2 XFF 两态测试锁定 |
| T-13-08 | DoS | per-IP map 无界增长（IP 基数放大内存） | low | mitigate | 15min 惰性过期 + 56B/条 × 4096 IP ≈ 230KB 内存界（throttle.go 先例平移）；Task 2 过期测锁定 |
| T-13-SC | Tampering | 依赖面 | high | mitigate | 零新依赖——x/time/rate v0.15.0 为 go.mod 既有直接依赖且在产线热路径在用（perclient.go:236）；go.mod/go.sum 零 diff 由 13-08 收口闸断言；无装包任务，legitimacy 门不触发 |
</threat_model>

<verification>
- time go test -race ./internal/server/ -count=1 全绿（tracer + 四扩展组 + 既有组零回归）
- D-03 红线：cmd/wesh/ 零 diff（零 flag/TOML 面）
- 关闭码纪律：1011 常量形态，1006 零命中
</verification>

<success_criteria>
1. 同 IP churn 超 burst 即收 1011 + spawn_throttled 事件 + 计数器递增；前端不触发自动重连
2. 全局桶耗尽任意 IP 皆拒；XFF 换键两态互不误伤；桶存储惰性过期有界
3. shared 全量测试原样绿、main.go 与 web/ 零 diff
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `internal/server/spawnthrottle.go`（新件）：常量 `defaultSpawnGlobalRate`/`defaultSpawnGlobalBurst`/`defaultSpawnPerIPRate`/`defaultSpawnPerIPBurst`（8/16/1/4）；`spawnThrottleStore` 结构（mu + global *rate.Limiter + perIP map）与 `allow(ip, now)` 判定方法、惰性过期重置
- `internal/server/server.go`：`Options.SpawnGlobalRate/SpawnGlobalBurst/SpawnPerIPRate/SpawnPerIPBurst` 四测试覆写字段 + New 零值兜底四行 + `Server.spawnThrottle` 字段与 per-client 分支装配 + Attach 调用点传 ip
- `internal/server/perclient.go`：`upgradePerClient` 签名加 `throttleIP string` 形参 + pre-spawn 桶判定段 + spawn_throttled 拒绝序列（Error+1011+日志+计数器）
- `internal/server/metrics.go`：`metricsCounters` 追加 `ptySpawn`/`ptySpawnFailures`/`ptyKills`/`ptySpawnThrottled` 四枚 atomic.Int64（本 plan 接线 ptySpawnThrottled 递增点）
- `internal/server/perclient_test.go`：`TestPerClientSpawnThrottle`（tracer e2e）+ 全局桶/XFF 两态/惰性过期/文案定值扩展组
- `internal/server/export_test.go`（或）：spawnThrottleAllowForTest 薄桥（如需——白名单登记）

<output>
Create `.planning/phases/13-resource-defense/13-02-SUMMARY.md` when done
</output>
