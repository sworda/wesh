---
phase: 13-resource-defense
plan: 08
type: execute
wave: 6
depends_on: [13-07]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - README.md
  - docs/CONFIGURATION.md
autonomous: true
requirements: [PC-08, PC-09, SEC-09, OPS-12]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "收口闸六段式全绿（承 11-06/12-05 先例）：静态面（GOROOT gofmt 零输出 + go vet）+ 全量 time go test -race ./... + darwin 双编译闸 + web 构建 dist byte-identical（本 phase 零前端改动——dist 差异即异常）+ UAT 矩阵（phase02-12 既有脚本默认模式零修改重跑全过 + phase13.mjs 两轮 + load 格 -tags=load 单跑）+ diff 白名单审查"
    - "期望值逐字未动：既有 Go 测试与 web/uat/phase02-12*.mjs 的 diff 审查白名单外零改动——白名单 = 本 phase 各 plan 声明的 files_modified 集合（含 perclient_test.go:420-422 断言翻转、metricsSeries17→21 镜像扩展、SpawnFunc 签名机械加参三项明示登记项）"
    - "禁止断言放宽成「两模式都接受」——全部既有期望值保持单一形态逐字锁定（CONTEXT 已锁定不重复决策）"
    - "零新依赖红线：go.mod/go.sum 零 diff（diff 审查段断言）"
    - "REQUIREMENTS.md 勾选 PC-08/PC-09/SEC-09/OPS-12 四条（证据链映射：Go 新测组 + phase13.mjs 六场景两轮 + churn 负载格 + diff 审查）"
    - "PROJECT.md Key Decisions 登记两行：D-01 per-client stop-timeout 默认值 0→5s（one-way 公开契约变更）+ 第二终结源 pcSupervisor/pcExitReq 与退出状态 255 对齐（ROADMAP 准则 5 登记要求）"
    - "D-10 文档明示段落地：README/CONFIGURATION 相应段写明 1006 先杀时序语义（默认 --ping-interval=5s 下 TCP 级全停读连接在停读+5s~10s 窗口被 pong_timeout 1006 先杀，dwell 1013 结构性后到；真实浏览器结构性不可达——网络栈自动回 pong；herdr 类自管 socket 客户端注意面；phase12.mjs S6 --ping-interval=0 隔离形态固化为既定测试纪律）"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "PC-08/PC-09/SEC-09/OPS-12 勾选 + Traceability 表状态 + Last updated 行"
      contains: "[x] **PC-08**"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions 两行登记（D-01 默认值变更 + 第二终结源 255 对齐）"
      contains: "stop-timeout"
    - path: "README.md"
      provides: "1006 先杀时序明示段（D-10）"
      contains: "1006"
  key_links:
    - from: ".planning/phases/13-resource-defense/13-08-SUMMARY.md"
      to: ".planning/STATE.md"
      via: "WR-02 闭合回指登记（STATE.md「[Phase 11 REVIEW WR-02 → Phase 13]」行——waitDone 非阻塞 select 形态已落地）"
      pattern: "WR-02"
  prohibitions:
    - requirement_id: PC-08
      category: values
      statement: "收口不得以放宽断言换绿——任何「两模式都接受」形态的断言改写即收口失败（diff 审查逐行核对，12-05 先例同构）"
    - requirement_id: PC-09
      category: values
      statement: "收口闸零跳过项——任何脚本/测试失败必须修复而非标记跳过（skipped 仅平台豁免类且带 reason，CODEBUDDY.md 分层策略 §5）"
---

<objective>
Phase 13 收口闸：零回归双证据（shared 全量 Go 测试原样绿 + phase02-12 既有协议 UAT 默认模式零修改重跑全过）+ 新证据（phase13.mjs 两轮 + churn 负载格）+ 期望值逐字未动 diff 白名单审查 + 需求勾选与 Key Decisions 登记 + D-10 文档明示段。

Purpose: 「双桶/第二终结源/Shutdown N 组/观测面/env 注入全部只在 per-client 分支生效，shared 路径逐字不动」的结构承诺必须由双证据兑现，而非口头声明。
Output: 六段式收口闸执行记录 + REQUIREMENTS/ROADMAP/PROJECT 勾选登记 + README/CONFIGURATION D-10 段 + SUMMARY（含 WR-02 闭合回指段与 flagged_assumptions 终验登记）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-resource-defense/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 收口闸六段式执行 + 期望值逐字未动 diff 白名单审查</name>
  <files>无（纯收口闸验证任务——正常态零文件改动；web/dist 复建须 byte-identical，出现差异归 diff 审查段处置）</files>
  <read_first>
    - .planning/STATE.md（Phase 11-06/12-05 收口闸先例登记：六段式构成与两轮基线口径）
    - .github/workflows/ci.yml（darwin 编译闸与负载 leg 形态——若存在则以同命令执行）
    - web/uat/ 目录清单（phase02-13 全部脚本枚举——零遗漏重跑清单）
    - .planning/phases/13-resource-defense/13-01-PLAN.md 至 13-07-PLAN.md 的 files_modified 声明（diff 白名单全集来源）
  </read_first>
  <action>
    按 11-06/12-05 先例六段式执行，全部构建/测试命令带 time 前缀（项目偏好）：
    1. 静态面：GOROOT gofmt -l . 零输出（go1.26.3 现代 doc-comment 规则工具，10-05 先例）+ go vet ./... 零输出。
    2. 全量：time go test -race ./... -count=1 全绿（五包——新测组逐名登记入 SUMMARY）。
    3. darwin 编译闸：GOOS=darwin GOARCH=amd64 go build ./... + GOOS=darwin GOARCH=arm64 go build ./... 双绿。
    4. web 构建：pnpm -C web build 后 web/dist 与 HEAD byte-identical（本 phase 零前端改动——dist 出现差异即收口失败，归 diff 审查段处置）。
    5. UAT 矩阵：phase02-12 既有协议脚本以默认 shared 模式零修改重跑全过（逐脚本与基线比对——12-05 登记的基线计数口径）；phase13.mjs 两轮全绿；time go test -tags=load -run TestChurn -count=1 ./internal/server/ 单跑绿。
    6. diff 白名单审查：git diff 全量逐行核对——白名单 = 七 plan 声明文件集合（main.go/perclient.go/server.go/clients.go/metrics.go/health.go/spawn.go/spawnthrottle.go + 各测试文件 + load_test.go + phase13.mjs + 文档行）；shared 列期望值逐字未动（既有测试断言行零改动）；go.mod/go.sum 零 diff（零新依赖红线）；断言放宽形态（「两模式都接受」）零命中；main.ts/web src 零 diff。
  </action>
  <verify>
    <automated>go vet ./... && time go test -race ./... -count=1 && GOOS=darwin GOARCH=amd64 go build ./... && GOOS=darwin GOARCH=arm64 go build ./... && node web/uat/phase13.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 六段式每段执行记录落 SUMMARY（命令 + 结果 + 耗时）
    - UAT 矩阵逐脚本计数与既有基线一致；phase13.mjs 两轮计数一致
    - diff 审查结论：白名单外零改动 + 三项明示登记项逐字核对 + go.mod/go.sum 零 diff
    - 收口闸零跳过项（skipped 仅平台豁免类且带 reason）
  </acceptance_criteria>
  <done>零回归双证据与新证据全绿；diff 白名单审查通过。</done>
</task>

<task type="auto">
  <name>Task 2: 需求勾选 + Key Decisions 登记 + D-10 文档段 + SUMMARY 收口登记</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md, README.md, docs/CONFIGURATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md:87-100（PC-08/PC-09/SEC-09/OPS-12 四条原文）与 :198-205（Traceability 表四行）与末尾 Last updated 行
    - .planning/ROADMAP.md Phase 13 段（checkbox 与 Plans 清单勾选举证）
    - .planning/PROJECT.md Key Decisions 表（登记格式先例）
    - README.md 重连/保活节与 docs/CONFIGURATION.md ping-interval 段（D-10 文档落点——grep 定位）
    - .planning/STATE.md（WR-02 登记行现位——SUMMARY 闭合回指对象）
  </read_first>
  <action>
    1. REQUIREMENTS.md：PC-08/PC-09/SEC-09/OPS-12 四条勾选 [x]；Traceability 表四行 Pending → Complete；Last updated 行更新（勾选证据链：Go 新测组清单 + phase13.mjs 六场景两轮 + churn 负载格 + diff 审查结论）。
    2. ROADMAP.md：Phase 13 checkbox 勾选 + Plans 清单逐条 [x] + Progress 表行更新（13. 资源防线与终结语义 → Complete + 完成日期）。
    3. PROJECT.md Key Decisions 表登记两行：D-01（per-client stop-timeout 默认值 0→5s，one-way 公开契约变更，13-01 确认门派发记录）与第二终结源（pcSupervisor/pcExitReq——「注册表空且无子进程可等」形态退出，退出状态 255 与 shared 语义对齐，ROADMAP 准则 5 登记要求）。
    4. D-10 文档段：README.md 重连/保活节与 docs/CONFIGURATION.md ping-interval 段各加一段 1006 先杀时序明示（默认 --ping-interval=5s 下全停读连接在停读+5s~10s 窗口被 1006 pong_timeout 收口、dwell 1013 结构性后到；真实浏览器结构性不可达——网络栈自动回 pong；herdr 类自管 socket 客户端注意面；1006 触发前端自动重连 → per-client 下重连=全新进程的合理恢复路径）；精确措辞属 Claude's Discretion（文档即被测物纪律）。
    5. 13-08-SUMMARY.md：六段式执行记录 + 证据链映射表 + WR-02 闭合回指登记（waitDone 非阻塞 select 形态，STATE.md 登记行回指）+ flagged_assumptions 终验登记（PC-09/SEC-09 两条 unclassified 保持 flagged-unverified 或未覆盖边界检索结论——11-05 先例同构）+ 13-01 确认门派发结果记录。
  </action>
  <verify>
    <automated>grep -c '\[x\] \*\*PC-08\*\*\|\[x\] \*\*PC-09\*\*\|\[x\] \*\*SEC-09\*\*\|\[x\] \*\*OPS-12\*\*' .planning/REQUIREMENTS.md && grep -n 'stop-timeout' .planning/PROJECT.md && grep -n '1006' README.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md 四条勾选 + Traceability 四行 Complete + Last updated 行更新（grep 四命中）
    - ROADMAP.md Phase 13 勾选与 Progress 表行更新
    - PROJECT.md Key Decisions 两行登记在场
    - README.md 与 docs/CONFIGURATION.md 各含 1006 先杀时序段（grep 命中）
    - SUMMARY 含 WR-02 闭合回指段与 flagged_assumptions 终验登记段与确认门派发记录
  </acceptance_criteria>
  <done>Phase 13 四需求收口：证据链登记完毕，Key Decisions 两行落地，D-10 时序语义文档明示，v1.1 进入 Phase 14。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 收口闸→交付物 | 收口是本 phase 全部防线与语义承诺的终验面——放宽断言换绿即整个 phase 证据链失效 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-25 | Tampering | shared 回归未检出（per-client 改动溢出污染 shared 路径） | high | mitigate | 零回归双证据（shared 全量 Go 测试原样绿 + phase02-12 默认模式零修改重跑）+ diff 白名单审查逐行核对（Task 1） |
| T-13-26 | Tampering | 断言放宽换绿（「两模式都接受」形态） | high | mitigate | diff 审查放宽形态零命中断言 + prohibitions 红线（Task 1 步 6） |
| T-13-SC | Tampering | 依赖面（供应链） | high | mitigate | 零新依赖红线终验：go.mod/go.sum 零 diff 断言（Task 1 步 6）；全 phase 无装包任务，legitimacy 门不触发 |
</threat_model>

<verification>
- 六段式全绿 + UAT 矩阵两轮基线一致 + diff 白名单审查通过
- 四需求勾选 + Key Decisions 登记 + D-10 文档段落地
</verification>

<success_criteria>
1. 收口闸六段式全绿，零跳过项，零白名单外改动
2. PC-08/PC-09/SEC-09/OPS-12 四需求证据链完整勾选
3. SUMMARY 含 WR-02 闭合回指与 flagged_assumptions 终验登记
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `.planning/REQUIREMENTS.md`：PC-08/PC-09/SEC-09/OPS-12 勾选 + Traceability 表四行 Complete
- `.planning/ROADMAP.md`：Phase 13 勾选 + Progress 表行
- `.planning/PROJECT.md`：Key Decisions 两行（D-01 stop-timeout 双默认值 / pcSupervisor 第二终结源 255 对齐）
- `README.md` / `docs/CONFIGURATION.md`：1006 先杀时序明示段（D-10）
- `.planning/phases/13-resource-defense/13-08-SUMMARY.md`：六段式记录 + 证据链映射 + WR-02 闭合回指 + flagged_assumptions 终验

<output>
Create `.planning/phases/13-resource-defense/13-08-SUMMARY.md` when done
</output>
