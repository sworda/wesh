---
phase: 13-resource-defense
plan: 07
type: execute
wave: 5
depends_on: [13-04, 13-05, 13-06]
files_modified:
  - web/uat/phase13.mjs
  - internal/server/load_test.go
autonomous: true
requirements: [PC-08, PC-09, SEC-09, OPS-12]
estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "phase13.mjs 六场景全绿两轮：S1 churn 节流 1011+XFF 换键 / S2 KILL 兜底默认 5s 生效（trap '' HUP 零配置）与显式 0 对照 / S3 --once·exit-when-empty 三形态 per-client 进程级退 255 / S4 Shutdown N 组 pgid ESRCH + session_end 数==N / S5 metrics 四计数器+session_active 计数语义+零身份 label / S6 WESH_REMOTE_USER env 可见 + shared 对照无键"
    - "churn 负载（合法连接 10rps×30s）下 RSS/goroutine/fd 有界：起点/终点双采样差值上界 + 多次轮询（Pitfall 7 断言形态——无单个硬编码绝对上限；观测钩子 = /metrics 黑盒 scrape wesh_goroutines/wesh_mem_alloc_bytes 既有 series + /proc/self/fd 计数）"
    - "churn 负载下 wesh_pty_spawn_throttled_total > 0（防线「生效过」的 metrics 面证据——注入速率 10rps 超 per-IP 1/s burst 4 结构性触发节流）"
    - "phase13.mjs 红线纪律逐字沿用 phase12.mjs:27-30：token/凭据/pid 数值只作断言材料，永不进入 check detail 或任何控制台输出——detail 只打印状态码/布尔/形状/退出码/文案常量；时序纪律真实等待，超时上限只做护栏，禁精确时点断言"
    - "S2 对照形态（显式 --stop-timeout=0）断言泄漏 + stderr warn 行后，测试尾必须 kill -9 清理免疫进程（防 UAT 自身泄漏残留）"
  artifacts:
    - path: "web/uat/phase13.mjs"
      provides: "协议层 UAT 六场景（零依赖 Node 脚本，phase12.mjs 同构第三代）"
      min_lines: 400
    - path: "internal/server/load_test.go"
      provides: "churn 负载格（10rps×30s + 双采样差值断言）"
      contains: "TestChurn"
  key_links:
    - from: "web/uat/phase13.mjs"
      to: "internal/server"
      via: "spawn 真实二进制全链断言（startWesh 夹具 phase12.mjs:94-121 逐字复用）——六场景分别钉死 13-01..13-06 的机制产出"
      pattern: "session-mode=per-client"
    - from: "internal/server/load_test.go"
      to: "internal/server/metrics.go"
      via: "churn 期间 /metrics 黑盒 scrape（getMetrics/metricSample 先例）——wesh_pty_spawn_throttled_total 计数与 runtime gauge 双采样"
      pattern: "wesh_pty_spawn_throttled_total"
  prohibitions:
    - requirement_id: PC-08
      category: values
      statement: "负载断言不得写成无基线对照的单个硬编码绝对上限——CI 慢 runner 基线漂移使该类断言随机过/不过（假绿/假红双害，Phase 9/11/12 瞬态 flake 教训）；断言形态 = 起点/终点双采样 + 差值上界 + 多次轮询"
    - requirement_id: PC-09
      category: values
      statement: "UAT 断言不得放宽成「两模式都接受」——per-client 场景断言 per-client 形态逐值（1011/255/事件数==N），shared 对照场景断言 shared 形态逐字未动"
---

<objective>
Phase 13 协议层证据主体：phase13.mjs 六场景（spawn 真实二进制全链断言）+ load_test.go churn 负载格（10rps×30s 资源有界）——13-01..13-06 全部机制产出的进程级验收。

Purpose: Go 单测证明机制语义，进程级 UAT 证明「真实二进制 + 真实 wire + 真实信号」下防线与终结语义成立；churn 压测先行于 Phase 14 负载矩阵（防护缺失会使压测失真）。
Output: web/uat/phase13.mjs 新件 + load_test.go churn 格。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-resource-defense/13-CONTEXT.md
@.planning/phases/13-resource-defense/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: web/uat/phase13.mjs——协议层 UAT 六场景（churn 节流 / KILL 兜底 5s / 退出三形态 255 / Shutdown N 组 / metrics 四计数器 / WESH_REMOTE_USER）</name>
  <files>web/uat/phase13.mjs</files>
  <read_first>
    - web/uat/phase12.mjs 全文 762 行（:1-28 文件头纪律三段式 / :55-68 check/skip 记录器与 emittedDetails 自净 / :94-121 startWesh 夹具 / :221 RawStallClient 停读夹具 / :676-726 场景函数模式 / :733-762 收口模式——同构母本逐段复刻）
    - web/uat/phase11.mjs（S5d trap 免疫 KILL 兜底场景与 S7 双 pid 先例——11-04 勘误：信号夹具用 kill -HUP/trap 形态）
    - web/uat/phase07.mjs（auth-header/XFF 场景先例——S1/S6 的头注入形态对照）
    - .planning/phases/13-resource-defense/13-CONTEXT.md（Claude's Discretion 场景建议面 + D-10 的 phase12.mjs S6 --ping-interval=0 纪律固化口径）
    - .planning/phases/13-resource-defense/13-PATTERNS.md §10（phase13.mjs 同构锚点）
  </read_first>
  <action>
    1. 新建 web/uat/phase13.mjs：文件头纪律三段式（红线声明 + 时序纪律 + 运行方式）逐形态复刻 phase12.mjs；check/skip 记录器、startWesh 夹具、assertOutputClean 自净、场景串行收口逐字复用；红线：token/凭据/pid 数值永不进 check detail。
    2. S1 churn 节流 + XFF 换键：实例 A（--session-mode=per-client --writable，无认证——loopback 单 IP 键）高频 attach 循环（真实 WebSocket connect→Hello→close）→ 超过 per-IP burst 后断言某端收 Error{server_error}+close 1011 逐值 + stderr 出现 spawn_throttled 事件行（detail 只打印布尔/码值）；实例 B（加 --auth-header=X-Remote-User）携不同 X-Forwarded-For 头值交替 attach → 各自独立桶不被节流（多 IP 形态）与同值被节流（单 IP 形态）双断言。
    3. S2 KILL 兜底默认 5s：per-client 零配置（不传 --stop-timeout）attach trap '' HUP 子进程（sh -c "trap '' HUP; ..."）→ 客户端断开 → 轮询 pgid ESRCH（kill -0 探针）在 5s+护栏窗口内成立（免疫进程被 KILL 收割）；对照实例显式 --stop-timeout=0 → 5s+ 窗内 ESRCH 不成立（泄漏形态）+ stderr warn 行断言 → finally 内 kill -9 清理免疫进程（防 UAT 自身泄漏）。时序纪律：轮询替代固定 sleep，护栏上限只防挂死。
    4. S3 退出三形态进程级 255：--once per-client 唯一客户端断开 → 服务端进程退出码 255；--exit-when-empty（grace 0）→ 255；--exit-when-empty=2s 宽限到期 → 255；宽限内重连 → 服务端存活不退出（取消形态，accept-255 裁决的进程级映射）。
    5. S4 Shutdown N 组：两客户端 attach 两个独立 sh → SIGTERM 服务端 → 两 pgid 各 ESRCH + stderr session_end 事件数==2 + 服务端进程退出。
    6. S5 metrics：per-client 实例 attach 1 会话 → GET /metrics → wesh_pty_spawn_total≥1 且四计数器 series 全在 + wesh_session_active==1（计数语义）+ wesh_session_active HELP 文案 per-client 形态 + 全部 series 零身份 label（label 花括号形态零命中——既有红线断言形态沿用）；shared 对照实例四计数器恒 0 且 series 保留不摘。
    7. S6 WESH_REMOTE_USER：--auth-header=X-Remote-User per-client 实例 → attach 携头 → 子进程内 env 回读（WS 内发 env 命令读输出）含 WESH_REMOTE_USER=头值 sanitize 产物（控制字符注入头 → 回读值已剥离形态）；shared 对照实例同头 → env 无该键（D-15 不变）。
    8. node web/uat/phase13.mjs 全绿两轮（时序 flake 面复跑确认）；既有 phase02-12 脚本不在本 plan 重跑（归 13-08 收口闸矩阵）。
  </action>
  <verify>
    <automated>node web/uat/phase13.mjs && node web/uat/phase13.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 六场景全绿两轮（check 全 PASS；skip 仅平台豁免类且带 reason——CODEBUDDY.md 分层策略 §5）
    - assertOutputClean 自净通过（emittedDetails 零 token/pid 数值泄漏）
    - S2 对照形态 finally 清理在场（trap 免疫进程 kill -9——脚本结束后无残留进程，`pgrep -f "trap '' HUP"` 类残留零命中由场景内断言承载）
    - S3 三形态退出码 255 逐值断言 + 取消形态存活断言
    - 文件头红线/时序纪律注释段在场（phase12.mjs 同构复刻）
  </acceptance_criteria>
  <done>六场景进程级证据落地：churn 节流 1011、KILL 兜底默认 5s、三形态 255、Shutdown N 组、metrics 粒度、WESH_REMOTE_USER 全链各归各位。</done>
</task>

<task type="auto">
  <name>Task 2: load_test.go churn 负载格——合法连接 10rps×30s，RSS/goroutine/fd 双采样差值有界（PC-08 churn 语境操作化）</name>
  <files>internal/server/load_test.go</files>
  <read_first>
    - internal/server/load_test.go:1-80（首行 //go:build load 硬纪律 + 夹具层 :51-80——drainClient Read 永不带 deadline ctx、select time.After 竞速收口；getMetrics/metricSample 先例）
    - internal/server/perclient_test.go:40-135（per-client harness——churn 格服务端装配：SessionMode/SpawnFunc/Writable/桶参数保持生产默认）
    - .planning/phases/13-resource-defense/13-RESEARCH.md §Pitfall 7（假绿防线：双采样差值 + 轮询纪律）与 §Pattern 1（10rps×30s 既定形态）
    - .planning/research/PITFALLS.md P4（churn 压测形态与验证清单 :342-349）
  </read_first>
  <action>
    1. load_test.go 加 TestChurnPerClientSpawnThrottle（//go:build load 隔离——首行硬纪律保持）：per-client 服务端（生产默认桶参数）→ 30s 内 10rps 合法连接循环（WS dial → Hello → 立即 close——票据在无认证 harness 下为直接 attach 形态）→ 全程多次轮询观测。
    2. 断言形态（Pitfall 7 纪律）：起点/终点双采样 /metrics（wesh_goroutines/wesh_mem_alloc_bytes 经 getMetrics/metricSample 黑盒 scrape）+ /proc/self/fd 计数 → goroutine 回落基线 ±N（churn 停止后等回收窗口再终点采样）；mem/fd 差值上界断言（上界值执行期按基线标定取保守量级，注释载标定来源）；wesh_pty_spawn_throttled_total > 0（防线生效证据——10rps 超 per-IP 1/s burst 4 结构性触发）。
    3. 禁单个硬编码绝对上限无基线对照；禁固定 sleep 精确时点断言（多次轮询 + 竞速收口先例逐字沿用）。
    4. time go test -tags=load -run TestChurn -count=1 ./internal/server/ -v 全绿；本格不进默认 CI leg（build tag 隔离既有纪律——ci.yml 负载 leg 形态若有则同构登记，无则零改动）。
  </action>
  <verify>
    <automated>time go test -tags=load -run TestChurn -count=1 ./internal/server/ -v && go vet ./internal/server/</automated>
  </verify>
  <acceptance_criteria>
    - //go:build load 首行硬纪律保持（既有首行逐字未动）
    - churn 格全绿：双采样差值上界断言 + throttled_total > 0 断言 + 轮询形态在场
    - 断言面零单个硬编码绝对上限无基线对照形态（region grep 自检）
    - go test -race ./... 默认面不含本格（build tag 隔离——默认全量零影响）
  </acceptance_criteria>
  <done>PC-08 churn 防线负载证据落地：10rps×30s 合法连接 churn 下 RSS/goroutine/fd 有界且节流计数器出数。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→server（churn 注入面） | 压测自身即防线攻击面的受控重放——断言失败 = 防线破口 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-22 | DoS | churn 负载下资源无界（RSS/goroutine/fd 泄漏未被检出） | high | mitigate | TestChurn 双采样差值上界 + 轮询 + throttled_total>0 生效证据（Task 2）；phase13.mjs S1 协议层节流全链（Task 1） |
| T-13-23 | DoS | UAT 自身泄漏（trap 免疫对照进程残留） | medium | mitigate | S2 对照形态 finally kill -9 清理 + 场景内残留断言（Task 1 步 3） |
| T-13-24 | Information Disclosure | UAT 控制台输出泄漏 token/pid | medium | mitigate | emittedDetails 收集 + assertOutputClean 自净（phase12.mjs 先例逐字沿用） |
| T-13-SC | Tampering | 依赖面 | high | mitigate | 零新依赖——phase13.mjs 为 Node 原生 WebSocket/fetch 零依赖脚本（双机拓扑：Linux 侧禁装浏览器/playwright，本 plan 无浏览器面）；go.mod 零 diff |
</threat_model>

<verification>
- node web/uat/phase13.mjs 两轮全绿 + assertOutputClean 自净
- time go test -tags=load -run TestChurn -count=1 ./internal/server/ -v 全绿
- 全量 time go test -race ./... -count=1 绿（build tag 隔离默认面零影响）
</verification>

<success_criteria>
1. phase13.mjs 六场景两轮全绿（churn 1011/KILL 5s/三形态 255/Shutdown N 组/metrics 粒度/env 注入）
2. churn 10rps×30s 资源有界且防线生效证据在 metrics 面可读
3. 红线纪律：detail 零敏感值、UAT 零残留进程
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `web/uat/phase13.mjs`（新件）：S1 churn 节流+XFF 换键 / S2 KILL 兜底默认 5s（+显式 0 对照）/ S3 退出三形态 255（+宽限取消）/ S4 Shutdown N 组 / S5 metrics 四计数器+session_active+零 label / S6 WESH_REMOTE_USER env 可见（+shared 对照）
- `internal/server/load_test.go`：`TestChurnPerClientSpawnThrottle` 负载格（10rps×30s 双采样差值断言）

<output>
Create `.planning/phases/13-resource-defense/13-07-SUMMARY.md` when done
</output>
