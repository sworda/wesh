---
phase: 13-resource-defense
plan: 05
type: execute
wave: 3
depends_on: [13-02, 13-03]
files_modified:
  - internal/server/metrics.go
  - internal/server/health.go
  - internal/server/perclient.go
  - internal/server/metrics_test.go
  - internal/server/perclient_test.go
  - internal/server/events_test.go
autonomous: true
requirements: [OPS-12]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "零会话时活跃会话 gauge 输出 0 且不摘除 series；注册表空且无子进程可等时第二终结源生效（不永不退出）——OPS-12 empty 探针边（explicit）：per-client 下 wesh_session_active==0 时 series 恒在（scrape 可证），第二终结源机制由 13-03 落地、本 plan 镜像断言不重复覆盖"
    - "全部 metrics series 零身份 label，无编码/归一化歧义面；审计关联键 client_id 为 sanitize 后定长形态——OPS-12 encoding 探针边（explicit）：series 名/HELP/TYPE 全部为定值 ASCII 常量，label 集合恒空；client_id = attachSeq int64 定长数值（无用户输入编码面）"
    - "新计数器四件全入：wesh_pty_spawn_total / wesh_pty_spawn_failures_total / wesh_pty_kills_total / wesh_pty_spawn_throttled_total（per D-08）；per-client-only 出数，shared 恒 0 series 保留不摘（credit_gate 恒 0 先例）"
    - "wesh_session_active 同名 series 按模式分支取值（per D-07）：shared = 0/1 探活语义（metrics.go:125-130 现状逐字不动）；per-client = len(pcSessions) 活跃会话计数（metricsSnap 新字段单趟快照内读——Open Question 1 推荐形态，绝不第二趟取锁）；HELP 文案按模式生成"
    - "metricsSeries17 测试侧镜像一次性扩展 17→21（新四条追加尾部；行数闸 3*len 自动跟随）；HELP 双模式文案断言随分支新增（Phase 11 D-04 既定窗口兑现）"
    - "healthz session_active per-client 恒 true（per D-06——「会话服务可用」语义诚实；编排探活只看 200/503 status 不受影响）；四字段键集红线（status/clients/max_clients/session_active）不变；perclient_test.go:420-422 窗口期断言随之翻转"
    - "session_start 每次 spawn 成功 emit：pid + client_id + 紧随 attach 事件之后、startSessionGoroutines 之前（:267-269 区间——程序序保证事件先于会话流量落流，Open Question 2 推荐形态，per D-09）"
    - "三计数器递增点：ptySpawn=upgradePerClient 登记成功点（:253 区段）；ptySpawnFailures=spawn 失败分支既有 logEvent 同点（:183-186）；ptyKills=teardownPCLocked 与 reapOrphanSession 两 AfterFunc 补 KILL 实际发送点（:488/:533 复检通过后、SignalGroup 之前——两路径都计，Open Question 3 推荐采纳）"
  artifacts:
    - path: "internal/server/metrics.go"
      provides: "metricsSnap.pcSessions 字段 + 四 writeCounter 尾部追加 + session_active 模式分支 + HELP 双模式文案"
      contains: "wesh_pty_spawn_total"
    - path: "internal/server/health.go"
      provides: "session_active per-client 恒 true 分支（D-06 形态①：handler 内模式分支）"
      contains: "SessionModePerClient"
    - path: "internal/server/perclient.go"
      provides: "session_start emit + 三计数器递增点（ptySpawn/ptySpawnFailures/ptyKills 两路径）"
      contains: "session_start"
    - path: "internal/server/metrics_test.go"
      provides: "metricsSeries17→21 镜像扩展 + HELP 双模式断言 + session_active 两模式取值断言"
      contains: "wesh_pty_spawn_throttled_total"
  key_links:
    - from: "internal/server/perclient.go"
      to: "internal/server/metrics.go"
      via: "四递增点（mc atomic Add）→ snapshotMetrics 单趟快照读出 → metricsHandler 四 series 输出"
      pattern: "ptySpawn"
    - from: "internal/server/perclient.go"
      to: "internal/server/log.go"
      via: "session_start/session_end 经 emitEvent 既有单出口（零新日志出口纪律）"
      pattern: "emitEvent"
  prohibitions:
    - requirement_id: OPS-12
      category: privacy
      statement: "metrics 任何 series 不得出现身份 label（client_id/pid/remote/remote_user 永不进 label——metrics.go:15-19 注释红线扩到新四 series）——身份 label 即基数爆炸 DoS 面 + 信息泄露面；per-client 明细一律查审计日志"
    - requirement_id: OPS-12
      category: privacy
      statement: "审计事件不得携带 token/ticket/凭据/未经清洗的原始头值——spawn_failed/spawn_throttled 为定值常量文案，绝不拼 err.Error()/路径/errno（SEC-01 红线扩到新事件）"
---

<objective>
OPS-12 观测面 per-client 粒度（防线装配序第四片）：metrics 四计数器 series 输出 + session_active 同名 series 模式分支 + HELP 双模式文案 + healthz D-06 恒 true + session_start 审计 emit + 三计数器递增点接线 + metricsSeries17→21 镜像扩展 + perclient_test.go:421 断言翻转。

Purpose: churn 防线若无量化信号，「防线是否生效过」只能靠事后日志检索——wesh_pty_spawn_throttled_total 是唯一「防线动作」指标；运维者从 /metrics 读到 per-client 粒度活跃会话数。
Output: metrics/healthz/audit 三面 per-client 粒度落地 + 镜像锁同步 + 断言翻转。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-resource-defense/13-CONTEXT.md
@.planning/phases/13-resource-defense/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: metrics 面——四计数器 series 输出 + session_active 模式分支 + HELP 双模式 + 三递增点接线 + 镜像 17→21（D-07/D-08）</name>
  <reversibility rating="costly">D-08 四 series 命名与 HELP 文案一经发布即被采集方（Prometheus 规则/看板）依赖，改名/改语义需采集侧迁移——metrics_test.go:38 既有注释「costly 级暴露面一次性锁死」同档；additive 扩展不破坏既有 17 series。</reversibility>
  <files>internal/server/metrics.go, internal/server/perclient.go, internal/server/metrics_test.go</files>
  <read_first>
    - internal/server/metrics.go 全文 177 行（:15-19 label 红线注释 / :52-58 metricsCounters（13-02 已加四枚字段）/ :87-110 snapshotMetrics 单趟形态 / :118-147 metricsHandler 17 series 逐字序 / :152-158 writeGauge/writeCounter 三行组）
    - internal/server/perclient.go:174-190（spawn 失败分支 :183-186 logEvent 同点——ptySpawnFailures 递增点）与 :218-267（注册点复检与登记成功点 :253 区段——ptySpawn 递增点与 session_start emit 区间）与 :481-491/:525-535（teardownPCLocked 与 reapOrphanSession 两 AfterFunc 补 KILL 发送点 :488/:533——ptyKills 递增点，复检通过后 SignalGroup 之前）
    - internal/server/metrics_test.go:30-130（metricsSeries17 :40-58 契约清单 + :115-116 行数闸 + assertExpositionShape :109-129 + getMetrics/metricSample 夹具）
    - .planning/phases/13-resource-defense/13-PATTERNS.md §5（计数器/快照/series 输出/HELP 分支逐行锚点）
  </read_first>
  <behavior>
    - metricsSnap 加 pcSessions int64 字段：snapshotMetrics 单趟 hubMu 持有内 sn.pcSessions = int64(len(s.pcSessions))（shared 下 pcSessions 为 nil，len==0 自然成立）；绝不第二趟取锁
    - metricsHandler 尾部追加四条 writeCounter（wesh_pty_spawn_total/wesh_pty_spawn_failures_total/wesh_pty_kills_total/wesh_pty_spawn_throttled_total——17→21，契约序 = 既有 17 条逐字不动 + 新四条尾部追加；snapshot 内读四枚 mc atomic）
    - session_active 分支：per-client → sessionActive = snap.pcSessions；shared → 现状 sessionAlive.Load() 逐字不动；HELP 文案按模式生成（handler 内分支——shared 文案逐字不动，per-client 文案述活跃会话计数语义）
    - 三递增点按 must_haves 锚点落位（ptySpawn 成功登记 / ptySpawnFailures 失败分支同点 / ptyKills 两 AfterFunc 复检通过后）
    - 镜像：metricsSeries17 → 尾部追加四条（变量名同步改语义注释——17→21 一次性扩展，Phase 11 D-04 既定窗口）；行数闸 3*len 自动跟随零改动；HELP 双模式断言（两模式各 scrape 一次断言 wesh_session_active HELP 文案差异 + per-client 取值 == 实际会话数）
  </behavior>
  <action>
    1. metrics.go：snapshot 扩展 + handler 四 series 尾部追加 + session_active 分支与 HELP 双模式生成；label 红线注释（:15-19）扩一句「13-05 四新 series 同红线」。
    2. perclient.go：三递增点落位（注释载 D-08 语义——ptyKills 计「SIGKILL 兜底实际发送次数」，teardown 与孤儿回收两路径同计）。
    3. metrics_test.go：metricsSeries17 尾部追加四条 name/typ 对（counter×4）+ HELP 双模式断言新测 + session_active per-client 取值断言（attach N 会话后 scrape 比对）；shared 形态既有断言逐字不动。
    4. go build ./... && go vet ./... && time go test -race -run 'TestMetrics' ./internal/server/ -count=1 -v 全绿 → 全量 time go test -race ./internal/server/ -count=1 回归。
  </action>
  <verify>
    <automated>time go test -race -run 'TestMetrics' ./internal/server/ -count=1 -v && time go test -race ./internal/server/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - metricsHandler 输出 21 series（metricsSeries17 镜像扩展同步；行数闸 3*len 自动覆盖）
    - session_active 两模式断言：shared 探活语义逐字未动 / per-client == len(pcSessions) 实计数；HELP 文案双模式差异断言在场
    - 四新 series 零身份 label（assertExpositionShape 扩展面覆盖：label 集合恒空）
    - shared 实例四计数器恒 0 且 series 保留不摘（scrape 断言——credit_gate 恒 0 先例同形态）
    - 三递增点 region grep 锚定（:253 区/:183-186 同点/两 AfterFunc 复检后）；snapshotMetrics 单趟形态保持（函数体仅一处 hubMu.Lock）
    - perclient_test.go 既有断言行零改动（本任务三文件 git diff 白名单 = 新增段）
  </acceptance_criteria>
  <done>metrics per-client 粒度落地：四计数器出数、session_active 模式分支、HELP 双模式、镜像 17→21 同步。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: healthz D-06 + session_start emit + 审计 schema 扩展 + :421 断言翻转（D-06/D-09 收口）</name>
  <files>internal/server/health.go, internal/server/perclient.go, internal/server/events_test.go, internal/server/perclient_test.go</files>
  <read_first>
    - internal/server/health.go 全文 54 行（:35-45 四字段匿名 struct 键集红线 + :44 SessionActive 分支点——D-06 形态①：handler 内模式分支；选型理据：避免 server.go 与 13-04 文件冲突面，改动局部化）
    - internal/server/perclient_test.go:380-450（:420-422 断言翻转点逐字现状「Phase 13 OQ①② 裁决落地时本断言随之翻转」）
    - internal/server/perclient.go:253-271（pcSessions 登记点与 attach 事件 :258-267 与 startSessionGoroutines :269 区段——session_start emit 插点区间）
    - internal/server/server.go:546-547（shared session_start emit 先例——「审计事件先于任何连接/会话流量落流」注释）
    - internal/server/events_test.go:427-471（schema 断言母本 + eventsNamed 计数锁）
  </read_first>
  <behavior>
    - healthz：per-client → SessionActive 恒 true；shared → s.sessionAlive.Load() 逐字不动；四字段键集不变
    - session_start emit：upgradePerClient 成功路径 pcSessions 登记后、attach 事件之后、startSessionGoroutines 之前——emitEvent(slog.String("event","session_start"), slog.Int("pid", pc.sess.Cmd.Process.Pid), slog.Int64("client_id", cl.attachSeq))；startedAt 字段为 pc 既有写一次字段（duration 数据源已由 13-03 session_end 消费）
    - perclient_test.go:420-422：断言翻转为恒 true 形态（注释窗口期表述移除，回指 D-06 落地）
    - events_test.go：session_start per-client 断言组（pid 键为正整数 + client_id 与 attach 事件同值 + 恰一发射）+ spawn_failed/spawn_throttled 零敏感值红线断言（定值文案 + 无 err.Error()/路径/errno 形态——事件四段 schema 键集断言）
  </behavior>
  <action>
    1. health.go：:44 分支改造（per-client → true；注释载 D-06 语义诚实论证——per-client 无「会话死亡=服务终结」态，编排探活只看 status 不受影响）。
    2. perclient.go：session_start emit 按 behavior 形态落位（emitEvent 既有单出口；零敏感值红线注释回指 log.go:85-89）。
    3. perclient_test.go:420-422 断言翻转（恒 true + 注释改写）。
    4. events_test.go：session_start 与零敏感值两断言组落位。
    5. time go test -race -run 'TestPerClientSessionStart|TestSessionStart|TestHealthz|TestPerClient' ./internal/server/ -count=1 -v 全绿 → 全量 time go test -race ./internal/server/ -count=1 回归。
  </action>
  <verify>
    <automated>time go test -race ./internal/server/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - healthz per-client session_active==true（翻转断言绿）且 shared 既有 healthz 测试零改动；四字段键集断言在场（无第五键）
    - session_start 事件：pid 正整数 + client_id 与 attach 同值 + 恰一；spawn_failed/spawn_throttled 事件键集四段（remote/code/reason/remote_user）且无敏感值形态断言在场
    - emit 插点位于 startSessionGoroutines 之前（region grep 程序序断言）
    - 全量 -race 绿；git diff 白名单 = 本任务四文件新增/翻转段（:420-422 翻转归白名单——13-CONTEXT D-06 明示翻转点）
  </acceptance_criteria>
  <done>OPS-12 观测面全量落地：healthz 恒 true 语义诚实、session_start/end 全生命周期事件带 pid+client_id 关联、零敏感值红线扩到新事件。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→运维通道（/metrics /healthz /审计日志） | 观测面输出是跨信任边界的数据出流——身份粒度与敏感值是红线面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-16 | Information Disclosure | metrics 新 series 带身份 label（client_id/pid/remote）→ 基数爆炸 DoS + 身份泄露 | high | mitigate | 零身份 label 红线扩到新四 series（Task 1 label 红线注释扩展 + assertExpositionShape 断言）；per-client 明细一律审计日志 |
| T-13-17 | Information Disclosure | session_start/end 事件携带敏感值（token/ticket/凭据） | high | mitigate | emitEvent 既有红线通道（log.go:85-89）+ 事件键集白名单断言（pid/client_id/exit_code/duration/signal 数值与关联键，Task 2） |
| T-13-18 | Tampering | healthz session_active 语义漂移（恒 true 被误读为「会话健康」） | low | accept | D-06 语义诚实论证：per-client 无「会话死亡=服务终结」态；编排探活只看 200/503 status（health.go draining 分支既有）；文档面归 13-08/Phase 14 |
| T-13-SC | Tampering | 依赖面 | high | mitigate | 零新依赖红线；本 plan 无装包任务，legitimacy 门不触发 |
</threat_model>

<verification>
- time go test -race ./internal/server/ -count=1 全绿（metrics 双模式 + 事件 schema + healthz 翻转 + 既有组零回归）
- 13-07 phase13.mjs S5 补进程级 scrape 断言（四计数器出数 + session_active 恒 true + 零身份 label）
</verification>

<success_criteria>
1. /metrics 21 series：四计数器 per-client 出数、shared 恒 0 不摘、零身份 label
2. session_active 双模式语义各归各位；HELP 文案按模式生成
3. 审计日志可串联单个 per-client 会话全生命周期（session_start pid+client_id → session_end exit_code/duration/signal+client_id）
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `internal/server/metrics.go`：`metricsSnap.pcSessions` 字段；metricsHandler 四新 series（`wesh_pty_spawn_total`/`wesh_pty_spawn_failures_total`/`wesh_pty_kills_total`/`wesh_pty_spawn_throttled_total`）；session_active 模式分支与 HELP 双模式文案生成
- `internal/server/perclient.go`：`session_start` emit（pid+client_id）；`mc.ptySpawn`/`ptySpawnFailures`/`ptyKills` 三递增点（KILL 计 teardown 与孤儿回收两路径）
- `internal/server/health.go`：session_active per-client 恒 true 分支（D-06 形态①）
- `internal/server/metrics_test.go`：metricsSeries17→21 镜像扩展（变量注释同步）+ HELP 双模式断言 + session_active per-client 取值断言
- `internal/server/perclient_test.go`：:420-422 断言翻转（恒 true 形态）
- `internal/server/events_test.go`：session_start per-client schema 组 + spawn_failed/spawn_throttled 零敏感值断言组

<output>
Create `.planning/phases/13-resource-defense/13-05-SUMMARY.md` when done
</output>
