---
phase: 13-resource-defense
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
  - cmd/wesh/config_test.go
  - docs/CONFIGURATION.md
  - README.md
autonomous: false
requirements: [PC-08]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "per-client 模式下 --stop-timeout 未显式设置时默认 5s（D-01 落地——HUP 免疫泄漏防线默认开启；Phase 11 post-merge 已实证 bash 4.4 交互模式在「提示符 pselect + 竞态输入行待读」窗口内可无声吸收 SIGHUP）"
    - "shared 模式 stop-timeout 默认值 0 逐字不变——main.go:244 `stopTimeoutDefault := time.Duration(0)` 字面不动（v1.0 零回归红线，Pitfall 4）"
    - "显式设置位双源置位区分「未设置」vs「显式 0」：fs.Visit 块新增第八位（f.Name == \"stop-timeout\"）+ fc.StopTimeout 非 nil 置位（07-06 合并收尾先例同形态，per D-02）"
    - "用户显式 --stop-timeout=0（或 TOML 显式 stop-timeout = \"0s\"）+ per-client 时尊重用户意图，validateStartup 经 warn 通道一行明示泄漏风险（--auth-header 暴露面警告先例；不静默改写用户输入纪律，per D-02）"
    - "文档即被测物：docs/CONFIGURATION.md:77（TOML 键表）与 :160（flag 表）双行 + README.md 默认值行同步为双默认值语义"
  artifacts:
    - path: "cmd/wesh/main.go"
      provides: "cfg.stopTimeoutSet 显式位 + per-client 双默认值落定 + validateStartup warn 分支"
      contains: "stopTimeoutSet"
    - path: "cmd/wesh/main_test.go"
      provides: "stop-timeout 三态（shared 未设=0 / per-client 未设=5s / per-client 显式 0=0+warn）断言组"
      contains: "stopTimeoutSet"
    - path: "docs/CONFIGURATION.md"
      provides: "stop-timeout 双行双默认值语义"
      contains: "per-client"
  key_links:
    - from: "cmd/wesh/main.go"
      to: "internal/server/perclient.go"
      via: "cfg.stopTimeout → Options.StopTimeout → teardownPCLocked 既有 KILL 兜底分支（:483-491）消费——本 plan 只改默认值来源，机制零改动"
      pattern: "StopTimeout"
  prohibitions:
    - requirement_id: PC-08
      category: values
      statement: "shared 模式 stop-timeout 默认值 0 逐字不动——「断开不退出、子进程继续运行」是 v1.0 产品承诺的组成部分，动它即零回归红线破坏（D-01 核心边界：双默认值而非统一改默认）"
    - requirement_id: PC-08
      category: values
      statement: "不得静默改写用户显式输入——显式 --stop-timeout=0 被悄悄覆盖为 5s 即违反「不静默改写用户输入」纪律；尊重+warn 是唯一合法形态"
---

<objective>
D-01/D-02 落地：per-client 模式下 --stop-timeout 双默认值（未显式设置默认 5s，显式 0 尊重+warn），shared 字面 0 逐字不动。本 plan 是防线装配序第一片（纯 CLI 装配层，零服务端机制改动，零风险热身）；D-01 为 one-way 公开契约变更，确认门先行（项目先例 05-03/06-04/09-04/12-01）。

Purpose: per-client 的产品语义是 ttyd 语义（断开=进程该死），stop-timeout 默认 0 在新模式下 = HUP 免疫泄漏防线默认关闭（已实证泄漏窗真实存在）；默认值的产品前提翻转了，默认值来源必须按模式分岔。
Output: 显式位第八位 + 双默认值落定 + warn 分支 + 三态测试组 + 双文档行同步。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-resource-defense/13-CONTEXT.md
@.planning/phases/13-resource-defense/13-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: D-01 one-way 确认门——per-client --stop-timeout 默认值公开契约变更</name>
  <decision>per-client 模式下 --stop-timeout 未显式设置时默认值 0→5s（HUP 免疫泄漏防线默认开启）——v1.1.0 起发布即不可收回的公开行为契约</decision>
  <context>13-CONTEXT.md D-01 已锁定该方案并评级 one-way：发布后 per-client 用户将对默认 5s KILL 兜底形成依赖，改回 0 即泄漏防线默认关闭的行为破坏。Phase 11 post-merge 已实证泄漏窗真实存在（2026-09-04：bash 4.4 交互模式在「提示符 pselect + 竞态输入行待读」窗口内可无声吸收 SIGHUP，kill 成功发出、非阻塞非 pending、进程存活；cat 对照组 50/50 全收）。本门按项目 one-way 确认门先例（05-03 D-05 / 06-04 D-12/D-14 / 09-04 / 12-01 D-08）在实现前确认派发。Undo 成本：改回 0 = 已发布默认行为破坏 + 用户脚本/运维手册已按 5s 语义撰写。</context>
  <options>
    <option id="option-a">
      <name>per-client 双默认值 5s + 显式 0 尊重+warn（D-01/D-02 既定，推荐）</name>
      <pros>HUP 免疫泄漏防线默认开启（实证泄漏窗闭合）；shared 默认 0 逐字不动（零回归红线保持）；用户显式意图经显式位区分受尊重（不静默改写纪律）；5s = PITFALLS P8 推荐值（正常程序收 HUP 清理窗口足够，泄漏存活上界有界）</pros>
      <cons>公开契约变更（per-client 与 shared 默认行为分岔），发布即不可收回（one-way 本义）</cons>
    </option>
    <option id="option-b">
      <name>维持默认 0 两模式统一（D-01 否决面）</name>
      <pros>零契约变更</pros>
      <cons>per-client 断开即杀语义下 HUP 免疫进程（nohup/trap '' HUP）默认泄漏且叠加 churn 可绕开 maxClients 驻留无界——防线默认关闭，已实证的泄漏窗保持敞开</cons>
    </option>
    <option id="option-c">
      <name>两模式统一改默认 5s</name>
      <pros>单一默认值心智简单</pros>
      <cons>shared「断开不退出、子进程继续运行」是 v1.0 产品承诺组成部分——动它即零回归红线破坏（结构性否决）</cons>
    </option>
  </options>
  <resume-signal>Select: option-a（确认 D-01/D-02 既定）, option-b, or option-c</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 机制——cfg.stopTimeoutSet 显式位双源置位 + per-client 双默认值落定 + validateStartup warn + 双文档行（D-01/D-02）</name>
  <reversibility rating="one-way">D-01：per-client 默认 5s 一经发布用户即对 KILL 兜底默认开启形成依赖，改回 0 即行为破坏——Task 1 确认门先行；D-02 显式位机制本身 reversible。</reversibility>
  <files>cmd/wesh/main.go, docs/CONFIGURATION.md, README.md</files>
  <read_first>
    - cmd/wesh/main.go:244（stopTimeoutDefault 字面 0——逐字不动红线）与 :314-320（fc.StopTimeout TOML 合并先例）与 :477（fs.DurationVar 注册行）与 :536-567（fs.Visit 显式位块现状七位——新增第八位）与 :576-595（配置键存在即置位块）与 :272-274（sessionModeDefault 合并顺序——sessionMode 在 parse 期已知）与 :986-1031（validateStartup modeWarns 累积形态与 warn 红线注释 :985）与 :1280-1344（run() warn 消费点 :1285-1287 与 Options 装配 :1392-1396——终值落定插点在装配前）
    - cmd/wesh/config.go:71（StopTimeout *string 键既有——无需新增键）
    - docs/CONFIGURATION.md:77 与 :160（stop-timeout 双行现状）
    - README.md 的 stop-timeout/flag 表对应行（grep 定位）
    - .planning/phases/13-resource-defense/13-PATTERNS.md §1（四区段模式与落点逐字锚点）
  </read_first>
  <behavior>
    - parseArgs 后 cfg.stopTimeoutSet 三态正确：CLI 显式（任意值含 0）→ true；TOML 键存在（含 "0s"）→ true；双源皆缺席 → false
    - 终值：sessionMode==per-client && !stopTimeoutSet → cfg.stopTimeout = 5s；其余组合（shared 未设 / 任意模式显式设置）→ 保持解析产出值
    - validateStartup：per-client && stopTimeoutSet && stopTimeout==0 → modeWarns 追加一行泄漏风险 warn（经 mergeWarn 既有透出点合并，零透出点改动）；shared+显式 0 → 无该 warn；per-client+显式 5s → 无该 warn
    - warn/err 文案定值无插值，不含凭据值（:985 红线注释保持）
  </behavior>
  <action>
    1. main.go cfg struct 加 stopTimeoutSet bool 字段（writePolicySet/sessionModeSet 等既有显式位字段同位同注释形态——注释载「D-02：区分未设置 vs 显式 0，07-06 先例」）。
    2. fs.Visit 块（:536-567）新增第八位：f.Name == "stop-timeout" 时 cfg.stopTimeoutSet = true；配置显式位块（:576-595）追加 fc.StopTimeout != nil 时置位——双源同档（review #3 吸收先例：判定锚定显式位而非终值）。
    3. 终值落定插点：run() 内 Options 装配字面量（:1392-1396）之前、sessionMode 与 stopTimeoutSet 均在手处——per-client 且未置位时 cfg.stopTimeout 覆写为 5 * time.Second；注释载 D-01 双默认值论证（shared 字面 0 逐字不动 = v1.0 产品承诺；per-client 产品语义翻转 = ttyd 断开即杀语义，PITFALLS P8 论证摘要；5s 取值依据 = 正常程序收 HUP 清理窗口足够 + 泄漏存活上界有界）。main.go:244 字面 0 与 :314-320 合并块逐字不动。
    4. validateStartup（:986-1031）modeWarns 累积形态加分支：per-client && stopTimeoutSet && stopTimeout == 0 → append 一行 warn，文案形态对齐 :1006 既有 warn（"wesh: warning: ..." 前缀，明示 SIGHUP 免疫进程（nohup/trap）将在客户端断开后泄漏）；定值文案零插值。
    5. 文档同步：docs/CONFIGURATION.md:77 TOML 键表 stop-timeout 行与 :160 flag 表 stop-timeout 行的默认值列改为双默认值语义（shared 默认 0；per-client 未显式设置默认 5s，显式 0 尊重并 warn）；README.md 对应 flag 行同语义同步——文档即被测物纪律，精确措辞属 Claude's Discretion。
    6. go build ./... && go vet ./... && time go test ./cmd/wesh/ -count=1 全绿（既有测试零改动为 shared 零回归证据）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && time go test ./cmd/wesh/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'stopTimeoutDefault := time.Duration(0)' cmd/wesh/main.go` 恰一命中且行为 :244（shared 字面 0 逐字未动）
    - cfg struct 含 stopTimeoutSet 字段；fs.Visit 块内含 "stop-timeout" 位；配置显式位块含 fc.StopTimeout 置位行（三处 grep 可证）
    - 终值落定行存在且位于 Options 装配之前（region grep：run() 函数体内 5 * time.Second 恰一命中）
    - validateStartup 函数体内含 stop-timeout=0 warn 分支（region grep 'stopTimeoutSet && cfg.stopTimeout == 0' 一命中）
    - docs/CONFIGURATION.md 与 README.md 的 stop-timeout 行含 per-client 默认 5s 语义（grep '5s' 于两文件各行命中）
    - time go test ./cmd/wesh/ -count=1 全绿且既有测试断言行零改动（git diff 白名单 = 本任务三文件新增段）
  </acceptance_criteria>
  <done>双默认值机制与 warn 通道落地，shared 默认值逐字未动；文档双行同步；Task 3 测试组收口后 D-01/D-02 完成。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 测试——三态断言组（显式位/双默认值/warn 三态）+ TOML 置位（D-01/D-02 收口）</name>
  <files>cmd/wesh/main_test.go, cmd/wesh/config_test.go</files>
  <read_first>
    - cmd/wesh/main_test.go:882-903（TestValidateStartupWarnMerge 形态——直调 validateStartup 纯函数 + strings.Contains 子串断言 + socket 早退透出锁）与 parseArgs 矩阵测现状（config struct 直构形态）
    - cmd/wesh/config_test.go（TOML 解析断言形态——fc 键存在/缺席断言先例）
    - cmd/wesh/main.go（Task 2 落地后的 stopTimeoutSet 三置位点与终值落定行——测试断言对象）
  </read_first>
  <behavior>
    - 三态断言：shared 未设 → 终值 0；per-client 未设 → 终值 5s；per-client 显式 --stop-timeout=0 → 终值 0 且 stopTimeoutSet==true；per-client 显式 --stop-timeout=3s → 终值 3s
    - TOML 置位：stop-timeout = "0s" 键存在 → stopTimeoutSet==true 且终值 0（per-client 下）；键缺席 → false
    - warn 分支：TestValidateStartupWarnMerge 加 t.Run——per-client+显式 0 → warn 含 stop-timeout 相关子串；per-client+显式 5s → 无该子串；shared+显式 0 → 无该子串
  </behavior>
  <action>
    1. main_test.go 新增 stop-timeout 三态测试组（表驱动，parseArgs/终值落定直调——config struct 直构 + CLI 参数切片两通道；测名以 TestStopTimeout 前缀成组）。
    2. TestValidateStartupWarnMerge 加 t.Run 分支三枚（形态逐字对齐 :882-903 既有分支）。
    3. config_test.go 加 TOML 置位断言（"0s" 显式零形态 + 键缺席形态双断言）。
    4. TDD 提交收口：Task 2 feat 提交 + 本任务 test 提交（11-01/11-03/12-03 两提交先例）。
    5. time go test ./cmd/wesh/ -count=1 -v -run 'TestStopTimeout|TestValidateStartupWarnMerge' 全绿 → time go test ./cmd/wesh/ -count=1 全量回归。
  </action>
  <verify>
    <automated>time go test ./cmd/wesh/ -count=1 -v -run 'TestStopTimeout|TestValidateStartupWarnMerge' && time go test ./cmd/wesh/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 新测试函数组全部存在且全绿（TestStopTimeout 前缀组 + warn 三 t.Run 分支）
    - 三态断言逐一锁定（per-client 未设=5s 为 D-01 核心断言；显式 0=0 且 warn 在场为 D-02 核心断言）
    - 既有测试断言行零改动（git diff 白名单 = 仅新增函数/t.Run 分支）
  </acceptance_criteria>
  <done>D-01/D-02 三态行为锁定：per-client 未显式设置默认 5s、显式 0 尊重+warn、shared 字面 0 不变，CLI/TOML 双源显式位正确置位。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 操作者→CLI/配置面 | 启动期默认值决定 HUP 免疫泄漏防线的开关状态——未显式设置的默认路径是不可信省略面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | DoS | per-client stop-timeout 默认 0（HUP 免疫进程泄漏 ×N，叠加 churn 绕开 maxClients 驻留无界） | high | mitigate | D-01 默认 5s KILL 兜底默认开启（Task 2）；trap '' HUP 进程级实证由 13-07 S2 承载 |
| T-13-02 | Tampering | 显式位误判（「未设置」被误判为「显式 0」→ 防线静默关闭；或反向误判 → 用户显式 0 被覆盖） | medium | mitigate | fs.Visit + TOML 键存在双源置位（Task 2 步 2）；三态测试组锁定（Task 3） |
| T-13-03 | Information Disclosure | warn 文案含敏感值 | low | accept | warn 通道既有红线（main.go:985 注释）——文案定值无插值，零新风险面 |
| T-13-SC | Tampering | 依赖面 | high | mitigate | 零新依赖红线——go.mod/go.sum 零 diff 由 13-08 收口闸断言；本 plan 无装包任务，legitimacy 门不触发 |
</threat_model>

<verification>
- time go test ./cmd/wesh/ -count=1 全绿（新三态组 + 既有矩阵零回归）
- main.go:244 字面 0 逐字未动（grep 锚定）
- 文档双行与代码行为一致（文档即被测物）
</verification>

<success_criteria>
1. per-client 未显式设置 → 5s；显式 0 → 0+warn；shared 未设 → 0 逐字不变——三态测试锁定
2. fs.Visit 第八位与 TOML 键存在双源置位，「未设置」vs「显式 0」区分可证
3. D-01 one-way 确认门经用户派发确认后落地
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `cmd/wesh/main.go`：`cfg.stopTimeoutSet` 显式位字段；fs.Visit 第八位（"stop-timeout"）；fc.StopTimeout 配置显式位置位行；per-client 双默认值落定行（5s 覆写）；validateStartup stop-timeout=0 warn 分支（modeWarns 累积）
- `cmd/wesh/main_test.go`：`TestStopTimeout*` 三态测试组 + `TestValidateStartupWarnMerge` 三个新 t.Run 分支
- `cmd/wesh/config_test.go`：TOML stop-timeout 键存在即置位断言（含 "0s" 显式零形态）
- 文档行：docs/CONFIGURATION.md:77/:160 双默认值语义行 + README.md 对应 flag 行

<output>
Create `.planning/phases/13-resource-defense/13-01-SUMMARY.md` when done
</output>
