---
phase: 13-resource-defense
plan: 04
type: execute
wave: 3
depends_on: [13-03]
files_modified:
  - internal/server/server.go
  - internal/server/shutdown_test.go
autonomous: true
requirements: [PC-09]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "优雅关停（SIGTERM/SIGINT）时全部存活 per-client 进程组各执行一遍 stop-signal 序列：快照源 = pcSessions（含「客户端已断开、会话 HUP 免疫待收割」的残留会话——pcSessions 独立于 registry 存在的核心理由，Pitfall 6），不是 registry"
    - "快照在 hubMu 内取、信号在 hubMu 外发（Shutdown 现状即不在 hubMu 内发信号——server.go:1616 调用点在放锁后；锁序红线保持）"
    - "每组执行形态 = SignalGroup(s.stopSignal) + stopTimeout>0 时 AfterFunc 补 SIGKILL（hubMu 内复检 !reaped——teardownPCLocked :484-491 同形态母本每会话化）；teardownOnce 幂等承接「1001 detach 已触发 teardown」与「Shutdown 快照再发信号」双触发"
    - "有界 join 后无条件经 termOnce 退出——不等 D-state（PITFALLS P10）、不丢 session_end 事件（join 界限内 watcher 收割链正常 emit）；上界值 = stopTimeout + 余量（Claude's Discretion 定值，研究 A1 建议 stopTimeout+2s 保守形态）"
    - "Shutdown 尾部 exiting=true（既有 :1592 置位点原位）+ hubCond.Broadcast() 补行唤醒 pcSupervisor（13-03 钉死的第二终结源消费端）"
    - "shared 分支 else 体（server.go:1615-1620）逐字不动——v1.0 关停语义零回归"
  artifacts:
    - path: "internal/server/server.go"
      provides: "Shutdown stop-signal 段 per-client 分支填充（:1614-1621 空 if 体 → 快照/逐组信号/有界 join/Broadcast）"
      contains: "pcSessions"
    - path: "internal/server/shutdown_test.go"
      provides: "per-client Shutdown N 组测试（在线双端 + 断开未收割残留形态 + session_end 数==N）"
      contains: "PerClientShutdown"
  key_links:
    - from: "internal/server/server.go"
      to: "internal/server/perclient.go"
      via: "Shutdown 快照逐组 SignalGroup → 各会话 watcher 收割 → session_end emit + pcSessions delete + Broadcast → pcSupervisor 终局收口"
      pattern: "SignalGroup"
  prohibitions:
    - requirement_id: PC-09
      category: safety
      statement: "Shutdown 不得被 D-state 不可杀进程拖死——join 必须有界，到期无条件 termOnce 退出；无界等待全部 Wait 返回即把关停控制权让给最坏子进程（PITFALLS P10）"
    - requirement_id: PC-09
      category: safety
      statement: "关停不得丢下任何存活 per-client 进程组静默退出——只覆盖在线客户端对应会话而漏掉「断开未收割」残留者即进程泄漏（Pitfall 6）"
---

<objective>
PC-09 后半：Shutdown stop-signal 段模式守卫（server.go:1614-1621，per-client 现状为空 if 体）填充为 N 进程组快照逐组信号 + 有界 join + exiting/Broadcast 补行——优雅关停覆盖全部存活 per-client 进程组。

Purpose: 1001 广播驱动 detach→teardown 链路覆盖不了无客户端的残留会话；pcSessions 快照是唯一完备信号面。本 plan 与 13-05（观测面）同 wave 并行（文件零重叠）。
Output: Shutdown per-client 分支 + shutdown_test.go per-client 测试组（在线 N 组 + 残留会话形态 + session_end 数==N）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-resource-defense/13-CONTEXT.md
@.planning/phases/13-resource-defense/13-RESEARCH.md
@.planning/phases/13-resource-defense/13-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 机制——Shutdown N 进程组快照逐组信号 + 有界 join + Broadcast 补行（PC-09 关停覆盖）</name>
  <reversibility rating="reversible">关停覆盖补齐是内部行为实现（per-client 现状空 if 体的既定填充点），零公开契约面；有界 join 上界值为内部常量可后调。</reversibility>
  <files>internal/server/server.go</files>
  <read_first>
    - internal/server/server.go:1581-1622（Shutdown 全文——1001 广播段 / :1592 exiting 置位点 / :1614-1621 stop-signal 段模式守卫空 if 体填充点 / shared else 分支逐字不动面）
    - internal/server/clients.go:1009-1014（stopChildLocked 母本——SignalGroup + AfterFunc 补 KILL 形态）与 internal/server/perclient.go:481-491（teardownPCLocked KILL 兜底 hubMu 复检形态——Shutdown 快照逐组信号的同形态依据）
    - internal/server/perclient.go:473-504（teardownOnce 幂等承接面——双触发论证锚点）与 :494-503（慢半段 delete + 13-03 Broadcast 补行——join 观测面）
    - .planning/research/ARCHITECTURE.md §4.4（Shutdown 两处分支序列细节）
    - .planning/phases/13-resource-defense/13-PATTERNS.md §3（快照/信号/锁序红线逐行锚点）
  </read_first>
  <behavior>
    - per-client 分支序列：hubMu 内 pcSessions 快照（[]*pcSession 拷贝）→ 放锁 → 逐组 SignalGroup(s.stopSignal)（ESRCH 幂等——已死 pgid 静默）→ stopTimeout>0 时每组 AfterFunc(stopTimeout) 回调 hubMu 内复检 !pc.reaped 才补 SIGKILL（:484-491 同形态）
    - 有界 join：等待 len(pcSessions)==0（hubCond 等或轮询——实现形态属 Claude's Discretion），deadline = stopTimeout + 余量（建议 stopTimeout+2s，研究 A1）；到期无论收割完毕与否无条件继续（不等 D-state）
    - join 后：exiting=true 置位点保持原位（:1592——若现状序在 stop-signal 段前则不动，若在段后则同序保持）+ hubCond.Broadcast() 补行唤醒 pcSupervisor
    - 1001 广播段（detach→teardown 链）与快照信号双触发由 teardownOnce 幂等承接——注释载 Pitfall 6 论证（残留会话只被快照链覆盖）
  </behavior>
  <action>
    1. server.go Shutdown :1614-1621 空 if 体按 behavior 序列填充——快照/放锁/逐组信号/AfterFunc 补 KILL/有界 join/Broadcast；注释载：快照源 = pcSessions 非 registry（Pitfall 6：覆盖断开待收割残留者）；锁序红线（快照 hubMu 内、信号 hubMu 外）；teardownOnce 双触发幂等论证；不等 D-state 纪律（PITFALLS P10）。
    2. shared else 分支逐字不动；1001 广播段与 exiting 置位点现状原位保持。
    3. go build ./... && go vet ./... && time go test -race ./internal/server/ -count=1 全绿（既有 shutdown/stopseq shared 用例零改动 = 零回归证据；13-03 落地组原样绿）。
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && time go test -race ./internal/server/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Shutdown 函数体 per-client 分支含 pcSessions 快照 + 逐组 SignalGroup + 有界 join + hubCond.Broadcast()（region grep 限定 Shutdown 函数体四锚点）
    - shared else 分支（:1615-1620 原字面序列）逐字未动（git diff 该区域零改动）
    - 信号发送点不在 hubMu 持有内（region 断言：快照循环在 Unlock 之后；AfterFunc 回调内 Lock 仅为 reaped 复检——teardownPCLocked 同形态）
    - 全量 -race 绿且既有测试零改动
  </acceptance_criteria>
  <done>Shutdown N 进程组覆盖机制落地；Task 2 测试组（含残留会话形态）收口后 PC-09 关停面完成。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 测试——per-client Shutdown N 组（在线双端 + 断开未收割残留 + session_end 数==N + 有界 join 不拖死）（PC-09 收口）</name>
  <files>internal/server/shutdown_test.go</files>
  <read_first>
    - internal/server/shutdown_test.go:1-61（startShutdownServerWith 夹具 :45-61——pty.Start → New → Listen → Cleanup → Serve 序列 + TERM 忽略形态复用 stopseq 夹具：trap 安装与关停信号竞态经落盘标记文件同步）
    - internal/server/stopseq_test.go（trap '' HUP 夹具与标记文件同步先例——若存在）
    - internal/server/events_test.go:427-471（eventsNamed 计数锁——session_end 数==N 断言工具）
    - internal/server/perclient_test.go:40-135（per-client harness——Shutdown 测试需 per-client 服务端变体：spawnFn 直通 + MaxClients 覆写）
  </read_first>
  <behavior>
    - 在线双端形态：per-client 服务端 + 两客户端 attach（各自独立 sh）→ 触发 Shutdown → 两 pgid 各收 stop-signal（ESRCH 收割观测）→ 服务端进程退出（exitCh/exitf 桩）→ session_end 事件数 == 2
    - 断开未收割残留形态（Pitfall 6）：客户端 attach 后子进程 trap '' HUP → 客户端断开（teardown HUP 被免疫，stopTimeout 覆写长值防 KILL 兜底抢跑）→ 会话残留 pcSessions → Shutdown → 快照链覆盖该残留组（标记文件/ESRCH 观测）→ 退出不被拖死
    - 有界 join 形态：trap 免疫 + stopTimeout 覆写短值 → KILL 兜底到期收割 → join 在界限内完成；断言 Shutdown 调用总耗时 < 上界 + 护栏余量（时序取下界/上界双锚，禁精确时点断言）
  </behavior>
  <action>
    1. shutdown_test.go 加 per-client 夹具变体（startShutdownServerWith 参数化 SessionMode/SpawnFunc——:45-61 先例扩展；spawnFn 直通 pty.StartWithSize argv 捕获）。
    2. 三形态测试落位（在线双端 / 残留会话 / 有界 join）；trap 免疫与关停信号竞态经落盘标记文件同步（stopseq 先例逐字沿用）；pgid ESRCH 观测经 syscall kill 0 探针（既有 waitPgroupESRCH 先例同包复用——11-07 EPERM 容忍语义已参数化）。
    3. session_end 数==N 经 captureStderr/parseEvents/eventsNamed 计数锁断言。
    4. TDD 提交收口：Task 1 feat 提交 + 本任务 test 提交。
    5. time go test -race -run 'TestPerClientShutdown' ./internal/server/ -count=1 -v 全绿 → 全量 time go test -race ./internal/server/ -count=1 回归。
  </action>
  <verify>
    <automated>time go test -race -run 'TestPerClientShutdown' ./internal/server/ -count=1 -v && time go test -race ./internal/server/ -count=1</automated>
  </verify>
  <acceptance_criteria>
    - 三形态测试全部存在且 -race 全绿（CI ubuntu 形态——waitPgroupESRCH EPERM 容忍先例继承 darwin 面）
    - 残留会话形态在场（Pitfall 6 告警面闭合——「只有客户端在线形态」即不及格）
    - session_end 数==N 断言在场；Shutdown 耗时有上界断言（无精确时点断言）
    - shutdown_test.go 既有 shared 用例断言行零改动（git diff 白名单 = 仅新增函数与夹具参数化）
  </acceptance_criteria>
  <done>PC-09 关停面锁定：SIGTERM/SIGINT 覆盖全部存活 per-client 进程组（含残留会话），有界 join 不拖死，session_end 零丢失。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS 信号→server（关停时序） | SIGTERM/SIGINT 到达时各会话所处生命周期阶段（在线/断开待收割/收割中）是不可信时序面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-13 | DoS | Shutdown 无界 join 被 D-state 不可杀进程拖死（PITFALLS P10） | high | mitigate | 有界 join（stopTimeout+余量）后无条件 termOnce 退出（Task 1）+ 有界形态测试（Task 2） |
| T-13-14 | DoS | 「断开未收割」残留会话漏信号 → 服务端退出后残留进程（Pitfall 6） | high | mitigate | 快照源 = pcSessions 非 registry（Task 1）+ 残留形态测试（Task 2） |
| T-13-15 | Tampering | 双触发竞态（1001 detach 链与 Shutdown 快照链对同会话重复信号/重复收口） | medium | mitigate | teardownOnce 幂等承接（perclient.go:474 既有）+ ESRCH 幂等；kill-after-reap 由 13-03 WR-02 栅栏 + reaped 复检双防线承接 |
| T-13-SC | Tampering | 依赖面 | high | mitigate | 零新依赖红线；本 plan 无装包任务，legitimacy 门不触发 |
</threat_model>

<verification>
- time go test -race ./internal/server/ -count=1 全绿（三形态新测 + 既有 shutdown/stopseq shared 用例零回归）
- shared else 分支逐字未动；exiting 置位点原位
- 13-07 phase13.mjs S4 补进程级断言（两 pgid ESRCH + session_end 数==2）
</verification>

<success_criteria>
1. 关停时 N 个存活进程组各收一遍 stop-signal 序列（含断开未收割残留者）
2. join 有界不拖死，session_end 事件数==N
3. shared 关停语义逐字未动
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `internal/server/server.go`：Shutdown stop-signal 段 per-client 分支（:1614-1621 填充——pcSessions 快照 / 逐组 SignalGroup + AfterFunc 补 KILL / 有界 join / hubCond.Broadcast() 补行）
- `internal/server/shutdown_test.go`：per-client 夹具变体（startShutdownServerWith 参数化）+ `TestPerClientShutdown*` 三形态组（在线双端 / 断开未收割残留 / 有界 join）

<output>
Create `.planning/phases/13-resource-defense/13-04-SUMMARY.md` when done
</output>
