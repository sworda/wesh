---
phase: 10-mode-assembly
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/wesh/main.go
  - cmd/wesh/config.go
  - cmd/wesh/main_test.go
  - internal/server/clients.go
  - internal/server/server.go
  - internal/pty/spawn.go
autonomous: true
requirements:
  - PC-01

estimate:
  tokens: 47000
  raw_tokens: 47000
  tasks: 2
  confidence: low

assumption_delta_decision:
  noun: 会话模式（session mode）
  decision: promote
  rationale: "shared 单态 → shared|per-client 双态按 10-CONTEXT.md 既定架构落地：装配期一次分岔、运行期零分岔、不抽象 session 接口；shared 保持默认与逐字节零回归。detector 本轮 detected=false，此块按团队预授权登记既定裁决，不重启讨论。"
  contract_test: "server.ValidateOptions 互斥校验 + parse 期枚举闸将「模式 ∈ {shared, per-client}」的一般化意图编码为可执行契约"

must_haves:
  truths:
    - "用户以 --session-mode=per-client 或 --session-mode=shared 启动被接受（解析成功、服务正常启动打印 listening on）；缺省不给 flag 时终值 == shared（SC1，D-03 内置默认）"
    - "--session-mode=banana（CLI）与 session-mode = \"banana\"（TOML）均在 parse 期被拒绝：exit 2，错误串含 `invalid --session-mode \"banana\": must be shared or per-client` 全文（D-04 定案文案；SC2 启动面红线保持——凭据/token/文件内容不回显，枚举非敏感面豁免回显）"
    - "优先级链对 session-mode 成立：CLI flag > TOML 配置 > 内置默认 shared（D-03：env 层对该键真空成立，不引入 WESH_SESSION_MODE）"
    - "run() 装配期一次分岔落地：shared 分支 SpawnFunc=nil；per-client 分支装配捕获 argv+StartOptions 的 SpawnFunc 闭包（函数体为 pty.StartWithSize 直通）并透传 Options.SessionMode——两模式本阶段均经启动期 pty.Start 创建会话（行为与 shared 等价，全部接缝 inert，Phase 11 才切换 sess=nil + attach 期 spawn）"
    - "server.ValidateOptions 互斥校验 fail-fast：SessionMode=per-client × SpawnFunc=nil 拒绝；SpawnFunc≠nil × SessionMode=shared 拒绝（ROADMAP「含」锁定两规则）；Options.SessionMode 零值归一为 shared（New 零值兜底，WritePolicy 先例）"
    - "pty.Start 缩为单行委托 pty.StartWithSize(argv, opts, SpawnCols, SpawnRows)——80×24 字面量零第二副本（spawn.go:34-41 单一事实源纪律）"
    - "v1.0 全量 Go 测试原样全绿且本 plan 零既有测试文件改动（零回归收口闸——期望值逐字未动，禁止断言放宽成「两模式都接受」）"
  artifacts:
    - path: "cmd/wesh/main.go"
      provides: "--session-mode flag 注册 + config 结构 sessionMode/sessionModeSet/argv0 三字段 + Parse 返回处枚举闸 + run() 装配分岔与 ValidateOptions 调用"
      contains: "session-mode"
    - path: "cmd/wesh/config.go"
      provides: "fileConfig.SessionMode 第 30 键（toml:\"session-mode\"，全连字符命名 P7 D-03）"
      contains: "session-mode"
    - path: "internal/server/clients.go"
      provides: "SessionModeShared/SessionModePerClient 枚举常量（WritePolicy 常量块同位同形态）"
      contains: "SessionModePerClient"
    - path: "internal/server/server.go"
      provides: "Options.SessionMode/SpawnFunc 两字段 + ValidateOptions 导出 + New 零值兜底与字段直传"
      contains: "ValidateOptions"
    - path: "internal/pty/spawn.go"
      provides: "StartWithSize 导出（承载现 Start 全部逻辑 + 尺寸参数化）+ Start 单行委托"
      contains: "StartWithSize"
    - path: "cmd/wesh/main_test.go"
      provides: "TestParseArgs wantSessionMode 断言位与两新行 + TestTLSKeyPairError D-04 枚举拒绝行"
      contains: "wantSessionMode"
  key_links:
    - from: "cmd/wesh/main.go parseArgs 枚举闸"
      to: "internal/server/clients.go SessionMode 常量"
      via: "parse 校验与 Options 消费共用同一对常量——双写漂移结构性消除（WritePolicy 先例）"
      pattern: "SessionMode(Shared|PerClient)"
    - from: "cmd/wesh/main.go run() per-client 分支 SpawnFunc 闭包"
      to: "internal/pty/spawn.go StartWithSize"
      via: "闭包捕获 argv+StartOptions，函数体为 StartWithSize 直通——Phase 11 attach spawn 的装配挂点（本阶段零调用方）"
      pattern: "StartWithSize"
    - from: "cmd/wesh/config.go fileConfig.SessionMode"
      to: "cmd/wesh/main.go Parse 返回处枚举闸"
      via: "TOML 非法枚举值经默认值替换机制落 cfg.sessionMode 同一终值——一闸双覆盖零第二校验行（pingInterval 负值闸 :655-658 同款机制）"
      pattern: "SessionMode"
    - from: "cmd/wesh/main.go run()"
      to: "internal/server/server.go ValidateOptions → New"
      via: "装配契约 fail-fast 在 New 之前——拒绝经 run() 现状 exit 2 通道（main.go:1171-1175 同形态）"
      pattern: "ValidateOptions"
  prohibitions:
    - statement: "MUST NOT 宽容归一任何近形模式值（大小写变体 SHARED/Shared、前后空白、其他拼写）——精确匹配两枚举常量，其余一律 parse 期拒绝；输入与生效模式分叉是配置漂移隐蔽源"
    - statement: "MUST NOT 在本阶段激活任何 per-client 运行期行为——SpawnFunc 零调用方、attach/INPUT/RESIZE/终结路径零模式分支、sess 不得以 nil 传入 New（New 体 server.go:431 sess.Cmd.Process.Pid 取引用即 panic）"
    - statement: "MUST NOT 改动 shared 路径任何既有行为——run() 现状行（pty.Start/listen/分享链接段）逐字保持，Options 字面量只追加不重排；默认模式值永远 shared（REQUIREMENTS 反特性 A5）"
    - statement: "MUST NOT 引入 WESH_SESSION_MODE 或任何 env 兜底键（D-03 裁决——env 层是敏感值专用通道，不开非敏感键 env 先例，优先级链 env 层对本键真空成立）"
---

<objective>
模式阀门端到端装配（tracer）：`--session-mode=shared|per-client` flag + TOML `session-mode` 键 + parse 期枚举校验（D-04 文案）+ `Options.SessionMode/SpawnFunc` 接缝 + `ValidateOptions` 互斥校验 + run() 装配期一次分岔 + `pty.StartWithSize` 导出——「CLI/TOML 输入 → 校验 → Options → server.New」一条完整路径一次打通，全部接缝 inert（本阶段结束时不存在任何 per-client 运行期行为）。

Purpose: PC-01 的公开契约面（flag 名/枚举值/TOML 键名/错误文案）是 one-way 门——先锁定契约（P2 D-15 纪律），Phase 11-14 在其上装配生命周期与交互语义；PATTERNS「No Analog Found」的 New 校验形态缺口经 option (b)（包级 ValidateOptions，main 在 New 前调用）闭合。
Output: 双源（CLI/TOML）模式选择可用、非法值 exit 2、接缝常量/字段/函数全部就位、零回归证据（全量 -race 原样绿 + 冒烟三形态）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-mode-assembly/10-CONTEXT.md
@.planning/phases/10-mode-assembly/10-PATTERNS.md
@cmd/wesh/main.go
@cmd/wesh/config.go
@internal/server/server.go
@internal/server/clients.go
@internal/pty/spawn.go
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 模式阀门端到端装配——SessionMode 常量 + StartWithSize + Options/ValidateOptions + flag/TOML 键/枚举闸 + run() 分岔</name>
  <reversibility rating="one-way">--session-mode flag 名、shared|per-client 枚举值、session-mode TOML 键名与 D-04 错误文案构成公开 CLI/配置契约（P2 D-15 one-way 纪律），发布后改名/改值破坏已部署配置——用户已在 10-CONTEXT/ROADMAP 锁定，记录评级不重复设卡。</reversibility>
  <files>cmd/wesh/main.go, cmd/wesh/config.go, internal/server/clients.go, internal/server/server.go, internal/pty/spawn.go</files>
  <read_first>
    - cmd/wesh/main.go（config struct :35-90——writePolicy/writePolicySet :41-43 字段对先例；parseArgs 头注释 flag 清单 :140-164；默认值铺底 :214-239 与 fc 换算 :257-259；flag 注册 write-policy :330-334；fs.Visit :504-530；fc 显式位合并 :537-553；write-policy 枚举校验 :615-620 与 pingInterval 一闸双覆盖注释 :655-658；run() :1156-1242——pty.Start 现状行 :1210、Options 字面量 :1242、validateStartup 错误 exit 2 通道 :1171-1175）
    - cmd/wesh/config.go（fileConfig :42-82——WritePolicy 键 :51；文件头覆盖面注释 :9-14 与「29 键」两处计数 :9, :42；configErr 单写口 :84-88）
    - internal/server/clients.go（WritePolicyOwner/WritePolicyAll 常量块 :69-77）
    - internal/server/server.go（Options :234-287——WritePolicy/AuthHeader/StopSignal 注释分档先例；New 零值兜底 :313-355——WritePolicy :338-340 与 StopSignal :346-350 同位；Server 字面量装配 :356-394；session_start emit :431——sess 非 nil 依赖的实证行）
    - internal/pty/spawn.go（SpawnCols/SpawnRows 单一事实源注释 :34-41；StartOptions :43-59；Start 本体 :61-95——creack/pty StartWithSize 调用 :88）
    - .planning/phases/10-mode-assembly/10-CONTEXT.md（D-03/D-04 决策全文 +「已锁定不重复决策」节）
    - .planning/phases/10-mode-assembly/10-PATTERNS.md（§1 parse 段六先例 / §3 run() 分岔 / §4 config.go / §5 StartWithSize / §7 Options/New / §8 常量 + S1-S6 Shared Patterns）
  </read_first>
  <behavior>
    - `--session-mode=per-client` / `--session-mode=shared` 解析入 cfg.sessionMode；缺省（CLI/TOML 均未给）终值为 server.SessionModeShared
    - `--session-mode=banana` → parseArgs 返回 error，文案 `invalid --session-mode "banana": must be shared or per-client`（D-04）；TOML `session-mode = "banana"` 经默认值替换机制落同一终值、同一闸同文案拒绝（一闸双覆盖）
    - fs.Visit 给 `--session-mode` 置 sessionModeSet；fc.SessionMode != nil 同档置位（D-02 双源机制——本阶段采集备用，warn 锚定为 writePolicySet 见 10-02）
    - ValidateOptions：per-client × SpawnFunc==nil → error；shared（含零值归一）× SpawnFunc!=nil → error；合法两态 → nil
    - run()：两模式均经启动期 pty.Start 创建 sess（:1210 现状行逐字保持）；per-client 分支额外装配 spawnFunc 闭包 + Options 两键；ValidateOptions 失败经 exit 2
    - pty.Start ≡ pty.StartWithSize(argv, opts, SpawnCols, SpawnRows)（委托等价由 10-02 Task 2 锁定）
    - 既有 v1.0 全量测试不改动一行且全绿（零回归硬闸）
  </behavior>
  <action>
    ① internal/server/clients.go：WritePolicy 常量块（:69-77）同位同形态追加 `SessionModeShared = "shared"` 与 `SessionModePerClient = "per-client"` 两常量（裸 string 类型对齐 WritePolicy 先例——PATTERNS §8；注释登记：公开 CLI 契约（P2 D-15），main.go parse 期枚举校验与 server Options 消费共用同一对常量防双写漂移；shared 为默认——REQUIREMENTS 反特性 A5）。
    ② internal/pty/spawn.go：导出 `StartWithSize(argv []string, opts StartOptions, cols, rows int) (*Session, error)`，承载现 Start（:61-95）全部逻辑（empty argv 拒绝/env 白名单/Dir/Uid Credential 分支/readDone 初始化逐行保留），唯一差异 = creack/pty Winsize 构造由字面量 SpawnRows/SpawnCols 换成参数（uint16 转换在 creack/pty 边界——注释登记调用方契约：cols/rows 为已钳制尺寸，ClampDim [1,1000] 归 Phase 12 调用侧，本函数不做二次钳制）；Start 缩为单行委托 `return StartWithSize(argv, opts, SpawnCols, SpawnRows)`——80×24 字面量不得复制第二份（:34-41 注释已预言本纪律，PATTERNS §5）。
    ③ internal/server/server.go：Options 追加 `SessionMode string` 与 `SpawnFunc func(cols, rows int) (*pty.Session, error)` 两字段（研究 ARCHITECTURE §2 签名定案——cols/rows 为 Phase 11 attach 期 Hello 钳制尺寸；注释分档照 WritePolicy/AuthHeader 先例：SessionMode 生产直传 + 零值 = shared 兜底（零值等价纪律：v1.0 逐字节不变的结构性保证）；SpawnFunc 注释登记「10-01 inert 装配挂点，消费归 Phase 11 attach spawn，本阶段零调用方」）；New 零值兜底块（:338-340 WritePolicy 同位）追加 `opts.SessionMode == "" → opts.SessionMode = SessionModeShared`；Server 结构体追加 sessionMode/spawnFunc 两字段并在字面量装配段（:356-394 注释分档同款）直传。新增导出 `ValidateOptions(opts Options) error`（PATTERNS「No Analog Found」option (b) 定案——New 现无 error 返回、包内零 panic 先例，包级校验函数与 validateStartup「拒绝路径零资源占用」纪律同构）：mode := opts.SessionMode，空串按 SessionModeShared 归一；mode == SessionModePerClient && opts.SpawnFunc == nil → error；mode == SessionModeShared && opts.SpawnFunc != nil → error（ROADMAP「含」锁定的两条互斥规则——sess 维度规则归 Phase 11 生命周期落地，本阶段不加第三规则防双写）。
    ④ cmd/wesh/config.go：fileConfig 追加 `SessionMode *string`（toml tag `session-mode`——全连字符命名 P7 D-03；ROADMAP/REQUIREMENTS 的 session_mode 下划线写法按 10-CONTEXT 修正记录统一为 session-mode，DisallowUnknownFields 下划线形态会被当未知键拒绝）；文件头覆盖面注释（:9-14）与 fileConfig 头注释（:42-43）的「27 flag 同名/29 键」改「28 flag 同名/30 键」并登记 10-01 覆盖面扩增；非法枚举值不经 configErr——拒绝归 parseArgs 统一枚举闸（PATTERNS §4 定案）。
    ⑤ cmd/wesh/main.go parseArgs：config struct 追加 `sessionMode string`、`sessionModeSet bool`、`argv0 string` 三字段（writePolicy/writePolicySet :41-43 同形态注释——one-way 公开契约 P2 D-15；argv0 注释登记「validateStartup per-client LookPath 预检数据源，10-02 消费」）；parseArgs 头注释 flag 清单（:140-164）补 Phase 10 行且「共 31 个」改 32，显式位补置清单（:171-172）补 sessionModeSet；默认值铺底段（:214-239，writePolicyDefault :218 同位）追加 `sessionModeDefault := server.SessionModeShared`，fc 换算块（:257-259 同位）追加 `fc.SessionMode != nil → sessionModeDefault = *fc.SessionMode`；flag 注册段（write-policy :330-334 同位，注释纪律同款——one-way 公开契约 + parse 期枚举校验在 Parse 返回处 + 值非敏感直接 return error）注册 `fs.StringVar(&cfg.sessionMode, "session-mode", sessionModeDefault, "session mode: shared|per-client (default shared; per-client is being assembled and currently behaves as shared)")`（D-05 help 文案随注册同 PR——精确措辞 Discretion，但须含枚举名单与装配中等价注记）；fs.Visit 块（:504-530）追加 `if f.Name == "session-mode" { cfg.sessionModeSet = true }`；fc 显式位合并块（:537-553，含注释清单）追加 `fc.SessionMode != nil → cfg.sessionModeSet = true`；Parse 返回处枚举校验（write-policy :615-620 同位——showVersion 早退之后）追加：`cfg.sessionMode != server.SessionModeShared && cfg.sessionMode != server.SessionModePerClient` 即 `return cfg, nil, fmt.Errorf("invalid --session-mode %q: must be shared or per-client", cfg.sessionMode)`（D-04 定案文案——TOML 源非法值经默认值替换机制落同一终值一闸双覆盖，:655-658 pingInterval 注释同款机制）；argv 落定后（:801-803 missing command 检查通过之后、`return cfg, argv, nil` 之前）`cfg.argv0 = argv[0]`。
    ⑥ cmd/wesh/main.go run() 分岔（:1208-1242 区——装配期一次分岔）：`sess, err := pty.Start(...)` 现状行（:1210）与 listen/prefs/share 段逐字保持；per-client 分支在 pty.Start 之前装配 `spawnFunc`（声明为 `func(cols, rows int) (*pty.Session, error)`，零值 nil）：`cfg.sessionMode == server.SessionModePerClient` 时赋闭包——捕获 argv 与 `pty.StartOptions{Dir: cfg.cwd, Term: cfg.term, Uid: cfg.uid, Gid: cfg.gid}`（闭包内 StartOptions 字面量与 :1210 内联字面量为 Phase 11 重写前的临时重复形态，注释登记「Phase 11 切换 sess=nil + attach 期 spawn 时收编」，本阶段为保 :1210 逐字未动有意为之）；闭包体 = `return pty.StartWithSize(argv, startOpts, cols, rows)`。**planner 裁定（executor 不得回改）**：两模式本阶段均经启动期 pty.Start 创建 sess——PATTERNS §3 的「sess = nil」建议与 New 体 server.go:431 `sess.Cmd.Process.Pid` 取引用冲突（nil 即 panic），且违反 D-05 文档注记「当前版本与 shared 等价」与全部 inert 约束；sess=nil 形态归 Phase 11 生命周期主干。Options 字面量（:1242）尾部追加 `SessionMode: cfg.sessionMode, SpawnFunc: spawnFunc` 两键（只追加不重排既有键序）；字面量提取为命名 `opts` 变量，server.New 调用前插入 `server.ValidateOptions(opts)` 校验——失败经 `fmt.Fprintf(os.Stderr, "wesh: %v\n", verr)` + `return 2`（:1171-1175 validateStartup 错误同通道同形态），通过则 `srv := server.New(sess, os.Exit, opts)`。
    ⑦ 零回归自检：shared 分支全部既有行逐字未动（例外仅 :1242 字面量追加两键与 opts 提取、parseArgs 头注释计数与清单更新）；`time go build ./...` 通过（time 前缀项目纪律）。
    提交：`feat(10-01): session-mode valve end-to-end assembly — flag/TOML key/enum gate/Options seam/ValidateOptions/run() fork/StartWithSize (PC-01, D-03/D-04)`
  </action>
  <verify>
    <automated>time go build ./... && go vet ./... && go test -race -count=1 ./... && mkdir -p /tmp/wesh-p10 && time go build -o /tmp/wesh-p10/wesh ./cmd/wesh && /tmp/wesh-p10/wesh --session-mode=banana -- bash; echo "banana_exit=$status"</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` 成功；`go vet ./...` 无输出；`go test -race -count=1 ./...` 全绿（v1.0 既有测试零改动——本任务不触碰任何 *_test.go）
    - `grep -c 'SessionModeShared = "shared"' internal/server/clients.go` == 1 且 `grep -c 'SessionModePerClient = "per-client"' internal/server/clients.go` == 1
    - `grep -c 'func StartWithSize' internal/pty/spawn.go` == 1 且 `grep -c 'return StartWithSize(argv, opts, SpawnCols, SpawnRows)' internal/pty/spawn.go` == 1
    - `grep -c 'toml:"session-mode"' cmd/wesh/config.go` == 1
    - `grep -c '"session-mode"' cmd/wesh/main.go` ≥ 2（flag 注册 + fs.Visit 分支）
    - `grep -c 'invalid --session-mode %q: must be shared or per-client' cmd/wesh/main.go` == 1
    - `grep -c 'func ValidateOptions' internal/server/server.go` == 1 且 `grep -c 'SessionMode: cfg.sessionMode' cmd/wesh/main.go` == 1
    - 冒烟：banana_exit == 2 且 stderr 含 `invalid --session-mode "banana": must be shared or per-client`
    - 冒烟：`timeout 3 /tmp/wesh-p10/wesh --session-mode=per-client --bind 127.0.0.1 --port 0 -- bash` 与缺省（不带 --session-mode）两形态的日志均含 `listening on`（启动接受 SC1）
  </acceptance_criteria>
  <done>--session-mode flag 与 TOML session-mode 键双源可用，非法值 exit 2（D-04 文案），ValidateOptions 互斥契约落地，run() 分岔两模式等价装配，Start ≡ StartWithSize 委托，全量 -race 原样绿。</done>
</task>

<task type="auto">
  <name>Task 2: CLI 契约测试——TestParseArgs wantSessionMode 断言位与两新行 + TestTLSKeyPairError D-04 枚举拒绝行</name>
  <files>cmd/wesh/main_test.go</files>
  <read_first>
    - cmd/wesh/main_test.go（TestParseArgs 表结构 :56-117——wantWritePolicy 零值注释惯例 :63、wantStopSignal 零值语义先例 :98-104；表行 write policy 两行 :133-134；断言体零值惯例落点（:180-380 区，照 wantWritePolicy 形态）；TestTLSKeyPairError 错误表 :403-478——malformed write-policy 行 :415、wantSub/forbiddenSub 双字段断言循环 :464-477）
    - cmd/wesh/main.go（Task 1 落地后的 parseArgs 枚举闸与默认值铺底现状——断言目标形态）
    - .planning/phases/10-mode-assembly/10-PATTERNS.md（§11 TestParseArgs/TestTLSKeyPairError 先例分析 + S6 测试宿主归属先例）
  </read_first>
  <action>
    ① TestParseArgs 表结构追加 `wantSessionMode string` 字段（注释照 wantWritePolicy :63 先例——「D-04：零值 = 期望默认 shared」；本表行均为命名字段初始化，追加字段不影响既有行）；断言体照 wantWritePolicy 零值惯例加 sessionMode 断言（零值时期望 server.SessionModeShared——server 包已 import）。
    ② TestParseArgs 表尾追加两行（全默认值 + wantSessionMode + wantArgv，照 write policy 行 :133-134 形态）：`{name: "session-mode per-client", args: []string{"--session-mode", "per-client", "--", "bash"}, ..., wantSessionMode: "per-client"}` 与 `{name: "session-mode shared explicit", args: []string{"--session-mode", "shared", "--", "bash"}, ..., wantSessionMode: "shared"}`。
    ③ TestTLSKeyPairError 错误表追加一行：`{"malformed session-mode", []string{"--session-mode", "banana", "--", "bash"}, ` + 反引号全文 `invalid --session-mode "banana": must be shared or per-client` + `, ""}`——wantSub 取 D-04 定案全文（Contains 断言一次性锁定 flag 名、回显值、枚举名单三要素）；forbiddenSub 置空（枚举值非敏感豁免面，D-04——本行是回显口径的行为锁，不是值剥离面）。
    ④ 全部改动 append-only：既有行（表结构既有字段、既有表行、既有断言）逐字未动——零回归证据链（S4 纪律）。
    提交：`test(10-01): session-mode CLI contract tests — parse matrix rows + D-04 enum rejection (PC-01)`
  </action>
  <verify>
    <automated>go test ./cmd/wesh -run 'TestParseArgs$|TestTLSKeyPairError$' -count=1 -v && git diff -U0 -- cmd/wesh/main_test.go | grep -c '^-[^-]'; echo "removed_lines=$status"</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/wesh -run 'TestParseArgs$|TestTLSKeyPairError$' -count=1 -v` 全 PASS（含两新 parse 行与 D-04 拒绝行）
    - `grep -c 'wantSessionMode' cmd/wesh/main_test.go` ≥ 3（字段声明 + 断言 + 新行）
    - `grep -c 'invalid --session-mode' cmd/wesh/main_test.go` ≥ 1
    - `git diff -U0 -- cmd/wesh/main_test.go` 的删除行计数 == 0（append-only 证据——既有行逐字未动）
  </acceptance_criteria>
  <done>CLI 侧契约（合法值解析/默认值/枚举拒绝文案）经表驱动测试锁定，既有行零改动，两目标测试全 PASS。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flag 值 / TOML 文件 → parseArgs/decodeFileConfig | operator 输入经启动面进入进程；错误输出落 stderr（systemd EnvironmentFile/journald 持久化面） |
| main run() → server.New | 装配契约边界——SessionMode×SpawnFunc 失配属程序错误，须 fail-fast 不得带病运行 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01a | Information Disclosure | 非法枚举值拒绝文案（CLI/TOML 双源） | medium | mitigate | D-04 定案：枚举值非敏感豁免回显（值域两固定单词无秘密可泄）；configErr 值剥离三要素保持（go-toml 错误文本绝不透传）；TestTLSKeyPairError/config 红线子测锁定文案形态 |
| T-10-01b | Tampering | 近形模式值宽容归一（大小写/空白变体静默映射到合法枚举） | medium | mitigate | 精确匹配枚举闸（== 两常量，无常量外值）；拒绝矩阵测试锁定 banana 拒绝；fuzz 语料含近形种子（10-03 下划线/类型不符面） |
| T-10-01c | Elevation of Privilege | per-client 接缝误激活（SpawnFunc 本阶段被调用 → 未审计 spawn 路径提前暴露） | high | mitigate | inert 纪律：SpawnFunc 零调用方（注释登记装配挂点）；run() 单分岔静态结构、无运行期模式分支；ValidateOptions 互斥校验 fail-fast；两模式共用启动期 pty.Start（行为等价，sess 永非 nil） |
| T-10-SC | Tampering | 依赖供应链（go get/包安装） | high | accept | 零新依赖（STACK.md 锁定——go-toml v2 既有机制覆盖新键）；本 plan 无 go get/包安装任务，go.mod/go.sum 零漂移 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V5 Input Validation（输入校验——枚举精确匹配/严格模式 TOML） | ✓ parse 枚举闸 + DisallowUnknownFields 既有机制 |
| V7 Error Handling（错误不含敏感值） | ✓ D-04 豁免面分档 + configErr 值剥离单写口保持 |
</threat_model>

<verification>
- `go test -race -count=1 ./...` 全仓全绿（v1.0 期望值逐字未动——零回归收口闸）
- 冒烟三形态（per-client / 显式 shared / 缺省）均打印 listening on；banana CLI 源 exit 2 且文案全文匹配 D-04
- `git diff --stat` 不触及任何既有 *_test.go 文件的既有行（本 plan 仅 main_test.go append-only）
</verification>

<success_criteria>
- `/tmp/wesh-p10/wesh --session-mode=per-client --bind 127.0.0.1 --port 0 -- bash` 与缺省形态均启动接受（SC1 第一半）
- `printf 'session-mode = "banana"\n' > /tmp/wesh-p10/bad.toml && /tmp/wesh-p10/wesh --config /tmp/wesh-p10/bad.toml -- bash` → exit 2 且错误串与 CLI 源同文案（一闸双覆盖进程级证据，SC2）
- CLI flag 覆盖 TOML 值：`session-mode = "per-client"` 配置 + `--session-mode=shared` → 终值 shared（SC3，由 10-03 precedence 子测锁定）
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **PC-01 (unclassified, unresolved)**：自动边缘探针未能对 PC-01 分类——人工裁定：PC-01 全部可断言边缘已由 10-CONTEXT.md D-01..D-06 锁定决策覆盖（非法值双源 parse 拒绝 exit 2、CLI>TOML>默认三层链、缺省 shared 逐字节零回归、write-policy×per-client warn、per-client LookPath 预检、env 键不引入）。未发现剩余未决边缘；此行保持 flagged-unverified 不自动闭合，由 10-04 收口全量验证（Go 测试矩阵 + 既有协议 UAT 原样重跑）承载显式证据。
- **run() per-client 分支 sess 语义（planner 裁定）**：PATTERNS §3「sess = nil」建议与 New 体 server.go:431 取引用冲突且违反 D-05「与 shared 等价」注记——裁定两模式共用启动期 pty.Start（sess 非 nil），sess=nil + attach 期 spawn 归 Phase 11；10-CONTEXT 将「sess nil 语义」列为 Claude's Discretion，本裁定即该 Discretion 的落地。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- flag `--session-mode`（cmd/wesh/main.go parseArgs 注册）
- TOML 键 `session-mode`（cmd/wesh/config.go fileConfig.SessionMode——第 30 键，全连字符）
- 常量 `SessionModeShared = "shared"` / `SessionModePerClient = "per-client"`（internal/server/clients.go）
- 字段 `Options.SessionMode string` / `Options.SpawnFunc func(cols, rows int) (*pty.Session, error)`（internal/server/server.go）+ Server 结构 sessionMode/spawnFunc 直传字段
- 函数 `ValidateOptions(opts Options) error`（internal/server/server.go——包级装配契约校验，PATTERNS option (b)）
- 函数 `StartWithSize(argv []string, opts StartOptions, cols, rows int) (*Session, error)`（internal/pty/spawn.go——Start 委托目标）
- config 字段 `sessionMode`/`sessionModeSet`/`argv0`（cmd/wesh/main.go）
- 测试断言位 `wantSessionMode` 与新表行（cmd/wesh/main_test.go——append-only）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/10-mode-assembly/10-01-SUMMARY.md` when done
</output>
