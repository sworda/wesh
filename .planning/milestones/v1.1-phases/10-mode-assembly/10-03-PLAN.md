---
phase: 10-mode-assembly
plan: 03
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - cmd/wesh/config_test.go
  - cmd/wesh/fuzz_test.go
autonomous: true
requirements:
  - PC-01

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "TOML session-mode 键铺底生效（fc.SessionMode 非 nil → 默认值换算 + sessionModeSet 置位——D-02 显式位机制对配置来源同档）；CLI flag 覆盖 TOML 值"
    - "优先级链三层断言成立：flag > TOML > 内置默认 shared（D-03——env 层真空成立，无 WESH_SESSION_MODE）"
    - "TOML `session_mode`（下划线）按未知键拒绝：错误含 unknown keys 与 session_mode 键名（D-03 键名修正的行为锁——DisallowUnknownFields 结构性拒绝）；TOML `session-mode = \"banana\"` 与 CLI 同文案拒绝（`invalid --session-mode \"banana\": must be shared or per-client`——一闸双覆盖）；`session-mode = 1` 类型不符经 invalid toml 分支拒绝（键名入文案、不经枚举闸）"
    - "FuzzDecodeFileConfig 追加五种子（合法 shared/合法 per-client/非法枚举 banana/下划线键/类型不符）——随常规 go test 零时长回归门全过（09-02 D-10 机制）；值剥离红线不变量保持（err.Error() 非键名上下文零 FUZZ_PROBE_SECRET，stripKeyNameEcho 既有断言面不动）"
  artifacts:
    - path: "cmd/wesh/config_test.go"
      provides: "TestConfigMerge/TestConfigPrecedence/TestConfigRedLines 三具名测试的 session-mode 子测扩展（append-only）"
      contains: "session-mode"
    - path: "cmd/wesh/fuzz_test.go"
      provides: "FuzzDecodeFileConfig 五新种子（session-mode 键合法/非法/边界形态）"
      contains: "session-mode"
  key_links:
    - from: "cmd/wesh/config_test.go TOML banana 子测"
      to: "cmd/wesh/main.go Parse 返回处枚举闸（10-01 落地）"
      via: "TOML 非法值经默认值替换机制落 cfg.sessionMode 同一终值——一闸双覆盖的行为锁（零第二校验行）"
      pattern: "invalid --session-mode"
    - from: "cmd/wesh/fuzz_test.go 下划线种子"
      to: "cmd/wesh/config.go DisallowUnknownFields（10-01 fileConfig.SessionMode 注册后）"
      via: "session_mode 未知键拒绝 = D-03 键名修正（session-mode 连字符）的结构性防线"
      pattern: "session_mode"
    - from: "cmd/wesh/config_test.go 显式位子测"
      to: "cmd/wesh/main.go fc 显式位合并块（10-01 落地）"
      via: "fc.SessionMode != nil → sessionModeSet = true——D-02 warn 锚定机制的配置来源同档证据"
      pattern: "sessionModeSet"
  prohibitions:
    - statement: "MUST NOT 在任何新断言中把配置值内容塞进预期错误串（值剥离红线延伸到新键断言面——枚举值本身回显是 D-04 豁免面，其余值内容禁入）；canon 提示：go-toml 错误文本透传属既有值剥离红线（SEC-01 启动面延伸），由 stripKeyNameEcho/fuzz 探针机制覆盖，本 plan 不新增该面"
    - statement: "MUST NOT 为 TOML 源非法枚举值新增第二条校验行（一闸双覆盖是结构特性——parseArgs 枚举闸单写口，config.go 只负责类型解码）"
---

<objective>
session-mode 的 TOML 侧三面证据（merge 铺底与 CLI 覆盖 / precedence 三层链 / redlines 下划线拒绝+非法枚举同文案+类型不符）与 fuzz 语料扩展（Pitfall 11「fuzz 语料/红线测试同 PR」纪律——新键入白名单与非法值 parse 拒绝同阶段落地）。

Purpose: D-03 定案的三层链（flag > TOML > 默认，env 真空）与键名修正（session-mode 连字符）需要行为锁；一闸双覆盖（TOML 非法值与 CLI 同文案）是零双写漂移的直接证据。
Output: config_test.go 三面 append-only 扩展 + fuzz 五种子；全部经 parseConfigArgs 既有夹具，零新机制。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-mode-assembly/10-CONTEXT.md
@.planning/phases/10-mode-assembly/10-PATTERNS.md
@.planning/phases/10-mode-assembly/10-01-SUMMARY.md
@cmd/wesh/config_test.go
@cmd/wesh/fuzz_test.go
@cmd/wesh/config.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: config_test.go 三面扩展——merge 两子测 + precedence 一子测 + redlines 三子测</name>
  <files>cmd/wesh/config_test.go</files>
  <read_first>
    - cmd/wesh/config_test.go（parseConfigArgs 夹具 :402-411；TestConfigMerge 标量子测形态 :431-482 与 string/enum 子测 :483-515；TestConfigPrecedence 五子测 :809-858；TestConfigRedLines 逃生门拒绝 :867-879 与值剥离探针族 :880-916、index-max-size 显式零值行 :790-803）
    - cmd/wesh/main.go（10-01 落地后的 fc 换算块/枚举闸/显式位合并块现状——断言目标形态）
    - .planning/phases/10-mode-assembly/10-PATTERNS.md（§12 三面先例分析 + S3 错误文案红线分档）
    - .planning/phases/10-mode-assembly/10-CONTEXT.md（D-03 三层链/env 真空裁决；D-04 一闸双覆盖）
  </read_first>
  <action>
    ① TestConfigMerge 追加两子测（append-only；parseConfigArgs 夹具直用；server.SessionModePerClient/SessionModeShared 常量引用——server 包已 import）：
    - `session-mode scalar from config only`：`parseConfigArgs(t, "session-mode = \"per-client\"\n", nil, "--", "bash")` → err nil；cfg.sessionMode == server.SessionModePerClient；**且 cfg.sessionModeSet == true**（D-02 显式位合并面证据——fc.SessionMode 非 nil 置位，10-01 落地机制）。
    - `session-mode CLI overrides config`：`parseConfigArgs(t, "session-mode = \"per-client\"\n", []string{"--session-mode", "shared"}, "--", "bash")` → cfg.sessionMode == server.SessionModeShared（flag > 配置）。
    ② TestConfigPrecedence 追加一子测 `session-mode precedence chain`（D-03 三层链——env 层真空成立不断言）：三腿合一——(a) flag > TOML：配置 per-client + flag shared → shared；(b) TOML > 内置默认：仅配置 per-client → per-client；(c) 键缺席 → 内置默认：无 session-mode 键（如仅 `bind = "127.0.0.1"`）→ cfg.sessionMode == server.SessionModeShared 且 cfg.sessionModeSet == false。
    ③ TestConfigRedLines 追加三子测：
    - `session_mode underscore key rejected as unknown`：`parseConfigArgs(t, "session_mode = \"shared\"\n", nil, "--", "bash")` → err 非 nil，含 `unknown keys` 与 `session_mode` 键名（D-03 键名修正的行为锁——DisallowUnknownFields 结构性拒绝；键名入文案合法）。
    - `session-mode invalid enum from config, same gate as CLI`：`parseConfigArgs(t, "session-mode = \"banana\"\n", nil, "--", "bash")` → err 非 nil，含全文 `invalid --session-mode "banana": must be shared or per-client`（一闸双覆盖锁——TOML 源非法枚举经默认值替换机制落同一终值、parseArgs 枚举闸单写口拒绝；本行是 D-04 文案的 TOML 侧面证据）。
    - `session-mode non-string type rejected`：`parseConfigArgs(t, "session-mode = 1\n", nil, "--", "bash")` → err 非 nil，含 `invalid toml` 与 `key "session-mode"`（类型不符面——go-toml DecodeError 分支，不经枚举闸；值非敏感不禁言，断言语义限键名与类别）。
    ④ 全部改动 append-only——既有子测逐字未动（S4 零回归纪律）。
    提交：`test(10-03): session-mode TOML coverage — merge/precedence/redlines (PC-01, D-03/D-04)`
  </action>
  <verify>
    <automated>go test ./cmd/wesh -run 'TestConfigMerge$|TestConfigPrecedence$|TestConfigRedLines$' -count=1 -v && git diff -U0 -- cmd/wesh/config_test.go | grep -c '^-[^-]'; echo "removed_lines=$status"</automated>
  </verify>
  <acceptance_criteria>
    - 三具名测试全 PASS（含六新子测）
    - `grep -c 'session-mode' cmd/wesh/config_test.go` ≥ 6 且 `grep -c 'session_mode' cmd/wesh/config_test.go` ≥ 1（下划线拒绝子测）
    - `grep -c 'invalid --session-mode' cmd/wesh/config_test.go` ≥ 1（一闸双覆盖锁）
    - `git diff -U0 -- cmd/wesh/config_test.go` 删除行计数 == 0（append-only 证据）
  </acceptance_criteria>
  <done>TOML 铺底/CLI 覆盖/三层链/下划线拒绝/非法枚举同文案/类型不符六面全部经表驱动锁定，既有子测零改动。</done>
</task>

<task type="auto">
  <name>Task 2: FuzzDecodeFileConfig 语料扩展——session-mode 键五种子（Pitfall 11 同 PR 纪律）</name>
  <files>cmd/wesh/fuzz_test.go</files>
  <read_first>
    - cmd/wesh/fuzz_test.go（FuzzDecodeFileConfig :75-92——五既有种子形态与注释纪律；文件头两不变量 :1-16；stripKeyNameEcho 键名回显豁免面 :25-42）
    - .planning/phases/10-mode-assembly/10-PATTERNS.md（§10 五种子定案集与「非法枚举值在 decodeFileConfig 层不报错」注释要点）
    - .planning/milestones/v1.0-phases/09-release-polish/09-CONTEXT.md（D-09/D-10 fuzz 目标面与运行形态——种子随常规 go test 零时长回归门）
  </read_first>
  <action>
    ① FuzzDecodeFileConfig 的 f.Add 种子段（:76-80 之后）追加五种子（PATTERNS §10 定案集；注释逐种子登记断言面归属）：
    - `session-mode = "shared"`（合法键——新键入白名单的 fuzz 面）
    - `session-mode = "per-client"`（合法键）
    - `session-mode = "banana"`（非法枚举——注释登记：decodeFileConfig 层不报错（合法 string 类型），枚举拒绝归 parseArgs 闸（10-01），本种子断言面 = 两不变量不破坏）
    - `session_mode = "shared"`（下划线形态 → 未知键拒绝面——D-03 键名修正的行为锁）
    - `session-mode = 1`（类型不符面——DecodeError 分支）
    ② 零新崩溃语料文件预期（种子经 f.Add 内联）；若 CI fuzz 长跑产出新崩溃语料，入 testdata/fuzz/FuzzDecodeFileConfig/（09-02 入库机制）——本任务不预建。
    ③ 两不变量注释（文件头 :3-11）不动；stripKeyNameEcho 豁免面不动（session-mode/session_mode 键名回显属合法键名上下文，天然落入既有剥除形态）。
    提交：`test(10-03): extend FuzzDecodeFileConfig seeds for session-mode key (PC-01, Pitfall 11)`
  </action>
  <verify>
    <automated>go test ./cmd/wesh -run 'FuzzDecodeFileConfig$' -count=1 -v && go test ./cmd/wesh -fuzz 'FuzzDecodeFileConfig' -fuzztime 30s -count=1 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/wesh -run 'FuzzDecodeFileConfig$' -count=1` PASS（全部种子含五新种子零时长回归门）
    - 30s fuzz 短跑零崩溃零红线破口（输出尾见 fuzz 统计无 FAIL）
    - `grep -c 'session-mode' cmd/wesh/fuzz_test.go` ≥ 4 且 `grep -c 'session_mode' cmd/wesh/fuzz_test.go` ≥ 1
    - `git diff -U0 -- cmd/wesh/fuzz_test.go | grep -c '^-[^-]'` == 0（append-only）
  </acceptance_criteria>
  <done>session-mode 键合法/非法/边界五形态入 fuzz 语料，零时长回归门与 30s 短跑全过，值剥离红线不变量保持。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML 文件（operator 控制）→ decodeFileConfig/parseArgs | 严格模式 + 值剥离是启动面既有防线——新键扩展不得弱化（fuzz 探针运行时自证） |
| fuzz 输入（任意字节）→ decodeFileConfig | 崩溃面与红线破口面——语料扩展只增断言覆盖不改不变量 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03a | Information Disclosure | 新键断言面值泄露（测试把配置值塞进预期错误串 / fuzz 断言弱化） | low | mitigate | 键名-only 断言纪律（configErr 三要素先例）；枚举值回显仅限 D-04 豁免面；stripKeyNameEcho 机制零改动 |
| T-10-03b | Tampering | 下划线 session_mode 键被宽容接受（键名纪律漂移——用户写下划线键静默生效） | low | mitigate | DisallowUnknownFields 结构性拒绝 + redline 子测行为锁（D-03 修正记录）+ fuzz 下划线种子永久回归 |
| T-10-03c | Tampering | TOML 源非法枚举走第二条校验路径（双写漂移——两闸文案分叉） | medium | mitigate | 一闸双覆盖结构锁：config.go 零枚举逻辑，redline 子测断言与 CLI 同文案（PATTERNS §1f/§4 定案） |
| T-10-SC | Tampering | 依赖供应链 | high | accept | 零新依赖；fuzz 语料为仓库内文本，无外部获取面 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V5 Input Validation（TOML 严格模式/枚举闸单写口） | ✓ 三面测试 + fuzz 种子锁定 |
| V7 Error Handling（错误文案值剥离） | ✓ 键名-only 断言 + 既有探针机制保持 |
</threat_model>

<verification>
- `go test ./cmd/wesh -count=1` 全 PASS（merge/precedence/redlines 六新子测 + fuzz 五新种子）
- 30s fuzz 短跑零崩溃零红线破口
- 两文件 append-only（git diff 删除行 == 0）
</verification>

<success_criteria>
- TOML `session-mode = "per-client"` 生效、被 CLI `--session-mode=shared` 覆盖、缺席默认 shared（SC3 三层链证据齐）
- TOML `session_mode = "shared"` → exit 2 未知键；`session-mode = "banana"` → exit 2 与 CLI 同文案（SC2 双源证据齐）
- fuzz 五种子随常规 go test 全过（Pitfall 11 同 PR 纪律兑现）
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- TestConfigMerge 追加子测 ×2（`session-mode scalar from config only` / `session-mode CLI overrides config`）
- TestConfigPrecedence 追加子测 ×1（`session-mode precedence chain`）
- TestConfigRedLines 追加子测 ×3（`session_mode underscore key rejected as unknown` / `session-mode invalid enum from config, same gate as CLI` / `session-mode non-string type rejected`）
- FuzzDecodeFileConfig 五新种子（cmd/wesh/fuzz_test.go——append-only；零新文件）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/10-mode-assembly/10-03-SUMMARY.md` when done
</output>
