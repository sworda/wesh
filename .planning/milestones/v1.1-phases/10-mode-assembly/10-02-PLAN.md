---
phase: 10-mode-assembly
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - cmd/wesh/main.go
  - cmd/wesh/main_test.go
  - internal/server/options_test.go
  - internal/pty/spawn_test.go
autonomous: true
requirements:
  - PC-01

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "writePolicySet（CLI 显式或 TOML 配置来源同档）× sessionMode=per-client → validateStartup 放行并返回 warn，文案含 --write-policy 与 --session-mode 双 flag 名（D-01/D-02——owner|all 任一显式均触发；静默永不接受）"
    - "writePolicySet × shared、per-client 未显式设置 write-policy → 不触发新 warn（既有行为零漂移）"
    - "新 warn 与非 loopback 逃生门安全警告（--no-auth / --insecure-http / --auth-header 暴露面）同现时两类文案均达 stderr——合并拼接不遮蔽；socket/loopback 早退路径同样透出累积 warn；既有 warn 文案逐字未动"
    - "per-client × argv0 不可执行（exec.LookPath 失败）→ validateStartup fail-fast 拒绝（SC4——命令缺失暴露在启动期而非首个客户端 attach）；per-client × 可执行命令 → 放行；shared × 不可执行命令 → 不预检（spawn 失败仍走 pty.Start exit 1 现状通道，零漂移）"
    - "server.ValidateOptions 三态契约经表驱动锁定：per-client×nil SpawnFunc 拒绝、shared×非 nil SpawnFunc 拒绝、合法两态与零值归一（\"\"→shared）放行"
    - "pty.Start ≡ pty.StartWithSize(argv, opts, SpawnCols, SpawnRows)（cmd.Env/Dir/SysProcAttr 逐字段等价 + 80×24 Winsize 同源）；StartWithSize 自定义尺寸（如 132x43）真实到达 TIOCSWINSZ"
  artifacts:
    - path: "cmd/wesh/main.go"
      provides: "validateStartup write-policy×per-client warn 行（D-01/D-02）+ per-client LookPath 预检行（SC4）+ warn 合并透出形态"
      contains: "sessionMode"
    - path: "cmd/wesh/main_test.go"
      provides: "TestStartupMatrix 追加行（warn 双形态/不触发面/LookPath 拒绝与放行/shared 不预检）+ TestValidateStartupWarnMerge 组合测试"
      contains: "SessionModePerClient"
    - path: "internal/server/options_test.go"
      provides: "TestValidateOptions 表驱动互斥契约测试（新文件）"
      contains: "TestValidateOptions"
    - path: "internal/pty/spawn_test.go"
      provides: "TestStartWithSizeDelegation 委托等价与自定义尺寸测试（追加）"
      contains: "TestStartWithSizeDelegation"
  key_links:
    - from: "cmd/wesh/main.go validateStartup warn 判定"
      to: "config.writePolicySet × config.sessionMode"
      via: "D-02 锚定 writePolicySet 显式设置位（CLI/TOML 双源经 10-01 落地的显式位机制同档）× 模式终值"
      pattern: "writePolicySet && cfg.sessionMode"
    - from: "cmd/wesh/main.go validateStartup LookPath 预检"
      to: "config.argv0（10-01 parseArgs 落定）"
      via: "SC4 启动期暴露——per-client 推迟 spawn 到 attach 的结构性补偿；只读探测纪律内（--cwd stat 先例）"
      pattern: "LookPath"
    - from: "cmd/wesh/main.go validateStartup warn 返回值"
      to: "cmd/wesh/main.go run() stderr 打印（:1176-1178）"
      via: "warn 通道现状不动——合并形态在 validateStartup 内部完成，调用方零改动"
      pattern: "warn"
    - from: "internal/server/server.go ValidateOptions"
      to: "cmd/wesh/main.go run() New 前调用（10-01 落地）"
      via: "装配契约 fail-fast——本 plan 补全其三态行为证据"
      pattern: "ValidateOptions"
  prohibitions:
    - statement: "MUST NOT 静默放行 write-policy×per-client 组合——无 stderr 警告的接受形态被 ROADMAP 明示禁止（「静默永不接受」）；warn 是放行前提不是可选提示"
    - statement: "MUST NOT 让新 warn 遮蔽、替换或改写既有非 loopback 安全警告文案（D-03/D-05/D-16 通道回归）——合并后两类文案逐字均在"
    - statement: "MUST NOT 将 LookPath 预检施加到 shared 模式或 argv0 空值路径——shared 启动行为零漂移（spawn 失败暴露通道保持 pty.Start exit 1）"
---

<objective>
validateStartup 双行落地：write-policy × per-client 组合 warn（D-01/D-02 定案——warn 明示放行，锚定 writePolicySet 显式设置位，CLI/TOML 双源同档）与 per-client LookPath(argv0) 启动预检（SC4——命令缺失暴露在启动期）；配套启动矩阵测试、warn 合并不遮蔽组合证据，以及 10-01 两条接缝（ValidateOptions 互斥契约 / StartWithSize 委托等价）的行为锁定。

Purpose: D-01 是 ROADMAP 明示的规划期裁决项（静默永不接受），SC4 是 per-client「spawn 推迟到 attach」的启动面补偿——两行同属 validateStartup 纯函数纪律（无副作用、只读探测允许、loopback 早退前判定）。
Output: warn/预检双行 + 全部组合面测试证据 + 接缝契约测试；shared 路径零漂移。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-mode-assembly/10-CONTEXT.md
@.planning/phases/10-mode-assembly/10-PATTERNS.md
@.planning/phases/10-mode-assembly/10-01-SUMMARY.md
@cmd/wesh/main.go
@cmd/wesh/main_test.go
@internal/server/server.go
@internal/pty/spawn.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: validateStartup 双行——write-policy×per-client warn（D-01/D-02）+ per-client LookPath 预检（SC4）+ 启动矩阵/warn 合并测试</name>
  <reversibility rating="one-way">D-01：validateStartup 行为契约——warn 明示放行后若收紧为拒绝，会破坏依赖该组合启动的既有部署（10-CONTEXT Reversibility 注记原文）；用户已锁定，记录评级不重复设卡。</reversibility>
  <files>cmd/wesh/main.go, cmd/wesh/main_test.go</files>
  <read_first>
    - cmd/wesh/main.go（validateStartup :923-1058 全函数——函数头纯函数纪律注释 :923-929、write-policy×writable fail-fast :936-938、--cwd stat 只读预检 :958-968、D-16 warn 行形态 :1040-1049、socket 早退 :1027-1032、loopback 早退 :1033-1035、函数尾 :1051-1057；run() warn 打印点 :1176-1178；os/exec import :18）
    - cmd/wesh/main_test.go（TestStartupMatrix 表结构 :674-680（wantErrSub/wantErrSub2/wantWarnSub 三通道）与既有行形态 :682-771——D-16 warn 行 :729、--cwd 预检行 :739-740；断言循环 :773 起）
    - .planning/phases/10-mode-assembly/10-CONTEXT.md（D-01/D-02 决策全文与 Reversibility 注记；SC4 成功准则 4）
    - .planning/phases/10-mode-assembly/10-PATTERNS.md（§2 Analog A/B/C——warn 行落点、warn 合并选型提示、LookPath 同位纪律；S1 parse/validate 分层）
    - .planning/phases/10-mode-assembly/10-01-SUMMARY.md（cfg.sessionMode/sessionModeSet/argv0 字段与 SessionMode 常量的落地形态）
  </read_first>
  <behavior>
    - writePolicySet=true（writePolicy=owner 或 all 任一）× sessionMode=per-client → validateStartup 返回 warn 非空、err 为 nil；warn 文案含 `--write-policy` 与 `--session-mode` 两个 flag 名
    - writePolicySet=true × sessionMode=shared（含零值）、sessionMode=per-client × writePolicySet=false → warn 为空（不触发）
    - 组合 warn 与非 loopback 安全警告同现：bind=0.0.0.0 + noAuth + writePolicySet + per-client → warn 串同时含 `--no-auth` 与 `--write-policy`/`--session-mode`（合并不遮蔽）；socket 形态同组合 → warn 仍透出（socket 早退不吞）
    - sessionMode=per-client × argv0 不存在 → err 非 nil（fail-fast），文案含命令名回显与 not found 语义；argv0 可执行（sh）→ 放行；sessionMode=shared × argv0 不存在 → err 为 nil（不预检，零漂移）；argv0 为空串 → 不预检
    - writePolicySet × per-client × 未开 writable → 既有 --write-policy×--writable fail-fast 优先（配置矛盾先行，warn 不可达——既有行 :698 语义不变）
  </behavior>
  <action>
    ① cmd/wesh/main.go validateStartup：write-policy×writable fail-fast（:936-938）之后、后续校验之前，捕获组合 warn——锚定 `cfg.writePolicySet && cfg.sessionMode == server.SessionModePerClient`（D-02：owner|all 任一显式均触发，writePolicySet 已由 07-06 合并收尾对 CLI/TOML 双源同档置位；不锚定 sessionModeSet——模式终值判定即双源覆盖）。warn 文案建议 `wesh: warning: --write-policy has no effect with --session-mode=per-client; owner/all arbitration and succession are not assembled in per-client mode (ro/rw permission levels still apply per ticket)`（D-01 语义：仲裁/递补不装配、ro/rw 级别仍按 ticket 生效；双 flag 名进文案纪律；精确措辞 Discretion，两 flag 名为强制要素）。
    ② warn 合并形态（防遮蔽——既有 warn 均互斥单返回且挂在非 loopback 分支后，新 warn 在 loopback/socket 也可达，PATTERNS §2 提示的选型点）：引入函数内累积变量与拼接（如 `warn` 变量 + 早退点透出 + 既有三 warn 返回点拼接，或等价的合并 helper——形态 Discretion），保证：socket 早退（:1030-1032）与 loopback 早退（:1033-1035）与函数尾（:1057）透出累积 warn；既有三 warn 返回点（:1047/:1049/:1055）与累积 warn 换行拼接（安全警告在前显著性优先，拼接顺序 Discretion）；**既有 warn 文案逐字未动**（TestStartupMatrix 既有行 wantWarnSub 断言零改动即其证明）；run() warn 打印（:1176-1178）通道零改动（Fprintln 多行可接受）。
    ③ LookPath 预检行（SC4）：--cwd stat 预检（:964-968）同位（loopback 早退之前、只读探测纪律内——exec.LookPath 为 PATH 只读解析，不产生任何资源占用；os/exec 已在 import :18，openBrowser 同款使用）追加：`cfg.sessionMode == server.SessionModePerClient && cfg.argv0 != ""` 时 `exec.LookPath(cfg.argv0)`，失败即 `return "", fmt.Errorf(...)`——文案须含三要素：命令名 %q 回显（--cwd 路径回显先例，非敏感）、not found 语义、per-client 语义注记（推迟 spawn 到 attach 的启动期补偿）；建议形态 `invalid command %q: not found in PATH (per-client startup preflight)`（精确措辞 Discretion）。shared（含零值模式）与 argv0 空串不触发——零漂移。
    ④ cmd/wesh/main_test.go TestStartupMatrix 表尾追加行（append-only，既有行逐字未动；新行 config 字面量均带 bind/maxClients/indexMaxSize 基值照 :682 先例；server.SessionModePerClient 常量引用——server 包已 import）：
    - `{"write-policy owner x per-client warns (D-01/D-02)", config{bind: "127.0.0.1", maxClients: 32, indexMaxSize: 16 << 20, writable: true, writePolicy: "owner", writePolicySet: true, sessionMode: server.SessionModePerClient}, "", "", "--write-policy"}`
    - `{"write-policy all x per-client warns (D-02 same tier)", ...writePolicy: "all"..., "", "", "--session-mode"}`（与上行成对——两行合力锁定 warn 文案双 flag 名）
    - `{"per-client without explicit write-policy silent", ...writable: true, sessionMode: server.SessionModePerClient..., "", "", ""}`
    - `{"write-policy x shared no new warn", ...writable: true, writePolicy: "all", writePolicySet: true, sessionMode: server.SessionModeShared..., "", "", ""}`
    - `{"per-client missing command refused (SC4)", ...sessionMode: server.SessionModePerClient, argv0: "wesh-no-such-cmd-7f3a"..., "not found in PATH", "", ""}`（wantErrSub 按 ③ 落地文案要素调整）
    - `{"per-client missing command refusal echoes name", 同上官 config, "wesh-no-such-cmd-7f3a", "", ""}`（命令名回显锁——非敏感豁免面）
    - `{"per-client existing command allowed", ...sessionMode: server.SessionModePerClient, argv0: "sh"..., "", "", ""}`
    - `{"shared missing command no preflight (zero drift)", ...argv0: "wesh-no-such-cmd-7f3a"（sessionMode 零值）..., "", "", ""}`
    ⑤ cmd/wesh/main_test.go 新增 `TestValidateStartupWarnMerge`（TestClientOptionError 分函数先例——组合面与主矩阵断言形态不同，独立小函数）：bind=0.0.0.0 + noAuth=true + writable=true + writePolicySet=true（all）+ sessionMode=per-client + maxClients=32 + indexMaxSize=16MiB → validateStartup err 为 nil 且 warn 同时含 `--no-auth`、`--write-policy`、`--session-mode` 三子串（Contains 逐断言——既有安全警告未遮蔽 + 新 warn 未吞）；socket 形态行（socket="/run/wesh.sock" + 同组合）→ warn 含 `--write-policy`（socket 早退透出锁）。
    提交：`feat(10-02): validateStartup session-mode rows — write-policy x per-client warn (D-01/D-02) + per-client LookPath preflight (SC4) (PC-01)`
  </action>
  <verify>
    <automated>go test ./cmd/wesh -run 'TestStartupMatrix$|TestValidateStartupWarnMerge$' -count=1 -v && go build ./... && go vet ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./cmd/wesh -run 'TestStartupMatrix$|TestValidateStartupWarnMerge$' -count=1 -v` 全 PASS（含 8 追加行与合并组合两形态）
    - `grep -c 'sessionMode == server.SessionModePerClient' cmd/wesh/main.go` ≥ 2（warn 行 + LookPath 行）
    - `grep -c 'exec.LookPath' cmd/wesh/main.go` ≥ 1
    - `git diff -U0 -- cmd/wesh/main_test.go | grep -c '^-[^-]'` == 0（append-only + 新函数——既有行逐字未动）
    - 既有 TestStartupMatrix 行（D-16 warn / 逃生门 warn 族）全部 PASS——既有 warn 文案逐字未动的行为证据
  </acceptance_criteria>
  <done>write-policy×per-client 双源 warn 落地（静默永不接受禁令兑现）、per-client LookPath 预检 fail-fast 落地（SC4）、warn 合并不遮蔽经组合测试锁定、shared 路径零漂移。</done>
</task>

<task type="auto">
  <name>Task 2: 接缝契约测试——internal/server/options_test.go（ValidateOptions 三态）+ spawn_test.go（StartWithSize 委托等价与自定义尺寸）</name>
  <reversibility rating="costly">SpawnFunc 签名 func(cols, rows int) (*pty.Session, error) 与 ValidateOptions/StartWithSize 形态是 Phase 11-14 跨阶段消费面——改动需协调后续阶段的调用点（非迁移/公开契约破坏，故非 one-way）。</reversibility>
  <files>internal/server/options_test.go, internal/pty/spawn_test.go</files>
  <read_first>
    - internal/server/server.go（Options :234-287 与 ValidateOptions/New 零值兜底——10-01 落地后现状）
    - internal/server/export_test.go（文件头纪律 :3——测试期专属出口零生产 API 面；本任务直接调导出函数 ValidateOptions，无需新出口）
    - internal/pty/spawn.go（StartWithSize/Start 委托——10-01 落地后现状）
    - internal/pty/spawn_test.go（TestStartZeroValueParity :219-242——awaitSession/startCollect 夹具与逐字段等价断言形态；slices 包已 import）
    - .planning/phases/10-mode-assembly/10-PATTERNS.md（§6 委托等价断言面 / §9 测试暴露面先例 / S6 测试宿主归属）
  </read_first>
  <action>
    ① internal/server/options_test.go【新】：`TestValidateOptions` 表驱动（{name, opts, wantErrSub}）——`per-client × SpawnFunc=nil` → err 含 `SpawnFunc`；`shared × SpawnFunc=非nil` → err 含 `SessionMode`；`per-client × SpawnFunc=非nil` → nil；`shared × SpawnFunc=nil` → nil；`零值 mode "" × SpawnFunc=nil` → nil（零值 = shared 归一语义锁）。SpawnFunc 非 nil 形态用空闭包占位（`func(cols, rows int) (*pty.Session, error) { return nil, nil }`——只断言校验分派不调用闭包，inert 纪律保持）。文件头注释登记：10-01 装配契约的行为锁，ROADMAP「含」两互斥规则。
    ② internal/pty/spawn_test.go 追加 `TestStartWithSizeDelegation`（TestStartZeroValueParity :219-242 同文件同夹具）：
    - 委托等价面：`Start([]string{"/usr/bin/env"}, StartOptions{Uid: -1, Gid: -1})` 与 `StartWithSize(同 argv, 同 opts, SpawnCols, SpawnRows)` 两会话——cmd.Env 经 slices.Equal 逐项相等、cmd.Dir 相等、SysProcAttr 同 nil（Uid -1 不降权）；两会话各自 awaitSession 正常退出（startCollect 夹具直用）。
    - 尺寸功能面：`StartWithSize([]string{"/usr/bin/env"}, StartOptions{Uid: -1, Gid: -1}, 132, 43)` 的 sess.Master 经 creack/pty GetsizeFull 读回 Cols==132、Rows==43（尺寸参数真实到达 TIOCSWINSZ——80×24 之外的第二数据点防「参数被忽略回落默认」）。
    ③ 两文件全部追加形态——既有行逐字未动。
    提交：`test(10-02): ValidateOptions mutex contract + StartWithSize delegation parity (PC-01)`
  </action>
  <verify>
    <automated>go test ./internal/server -run 'TestValidateOptions$' -count=1 -v && go test ./internal/pty -run 'TestStartWithSizeDelegation$|TestStartZeroValueParity$' -count=1 -v && go test -race -count=1 ./...</automated>
  </verify>
  <acceptance_criteria>
    - TestValidateOptions 五态全 PASS（两拒绝 + 两放行 + 零值归一）
    - TestStartWithSizeDelegation 两面全 PASS（等价面 + 132x43 尺寸面）；既有 TestStartZeroValueParity 原样 PASS（Start 委托零回归）
    - `grep -c 'func TestValidateOptions' internal/server/options_test.go` == 1 且 `grep -c 'func TestStartWithSizeDelegation' internal/pty/spawn_test.go` == 1
    - `go test -race -count=1 ./...` 全绿
  </acceptance_criteria>
  <done>ValidateOptions 互斥契约与 Start/StartWithSize 委托等价经测试锁定，80×24 单一事实源纪律保持，全量 -race 绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config（CLI/TOML 合并终值）→ validateStartup | 启动校验纯函数——warn/err 文案落 stderr（持久化面）；只读探测（os.Stat/exec.LookPath）不产生资源占用 |
| 测试夹具（伪造命令名/配置组合）→ validateStartup | 断言面不得引入新泄露通道（命令名非敏感豁免面） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02a | Information Disclosure | warn/err 文案含敏感值 | low | mitigate | warn 文案只含双 flag 名不含值；LookPath 错误 %q 回显命令名（--cwd 路径回显先例，非敏感）；TestStartupMatrix wantWarnSub/wantErrSub 形态锁 |
| T-10-02b | Tampering | 组合处置静默化（write-policy×per-client 无警告放行——用户预期 owner 仲裁实际真空） | high | mitigate | D-01/D-02 warn 通道强制（ROADMAP「静默永不接受」）；双源两形态测试行 + CONFIGURATION.md warn 清单文档化（10-04） |
| T-10-02c | Denial of Service | LookPath 预检误伤 shared 路径（启动行为漂移——shared 本无此闸） | medium | mitigate | 预检仅 per-client × argv0 非空分支（模式判定先行）；「shared missing command no preflight」测试行锁零漂移 |
| T-10-02d | Information Disclosure | 新 warn 遮蔽既有非 loopback 安全警告（用户看不到「任何人可达即得终端」） | medium | mitigate | 合并拼接形态（两类文案均达 stderr）；TestValidateStartupWarnMerge 组合断言；既有 warn 文案逐字未动（既有矩阵行零改动证据） |
| T-10-SC | Tampering | 依赖供应链 | high | accept | 零新依赖；本 plan 无包安装任务 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V5 Input Validation（配置组合校验——显式位锚定/启动预检） | ✓ validateStartup 双行 + 矩阵测试 |
| V7 Error Handling（warn/err 文案不含敏感值、安全警告不丢失） | ✓ 合并形态 + 组合测试锁定 |
</threat_model>

<verification>
- `go test -race -count=1 ./...` 全仓全绿（既有期望值逐字未动）
- TestStartupMatrix 追加行八态 + TestValidateStartupWarnMerge 两形态 + TestValidateOptions 五态 + TestStartWithSizeDelegation 两面全 PASS
- 既有 D-16/逃生门 warn 行全部 PASS（合并形态零回归证据）
</verification>

<success_criteria>
- write-policy=all（CLI 或 TOML）× --session-mode=per-client 启动：stderr 出现含双 flag 名的警告行且服务正常启动（D-01 放行语义）
- --session-mode=per-client -- 不存在的命令：启动期 exit 2 拒绝（SC4——不推迟到 attach）
- 同组合加 --no-auth 非 loopback：stderr 同时出现两类警告（不遮蔽）
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- validateStartup write-policy×per-client warn 行与 warn 合并透出形态（cmd/wesh/main.go）
- validateStartup per-client LookPath(argv0) 预检行（cmd/wesh/main.go——SC4）
- TestStartupMatrix 八追加行 + `TestValidateStartupWarnMerge`（cmd/wesh/main_test.go——append-only）
- internal/server/options_test.go【新文件】：`TestValidateOptions`
- internal/pty/spawn_test.go 追加 `TestStartWithSizeDelegation`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/10-mode-assembly/10-02-SUMMARY.md` when done
</output>
