---
phase: 11-per-client
plan: 05
type: execute
wave: 3
depends_on: ["11-01", "11-03"]
files_modified:
  - web/uat/phase11.mjs
autonomous: true
requirements:
  - PC-02
  - PC-03
  - PC-04

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "web/uat/phase11.mjs 协议层 UAT 八场景全绿（D-06，phase02-09.mjs 先例形态：Node 原生 WebSocket/fetch 零依赖、spawn 真实二进制断言、场景数组 + 串行 runner + 汇总退出码、宽限/退避类场景真实等待超时上限只做护栏）"
    - "S1 双端独立 pid 互不串台 + 启动期零子进程实证（attach 前 pgrep -P <weshPid> 无输出——spawn 点后置 = PC-02 的直接证据；attach 后两端 echo $$ 回读 pid 不等 + A 端唯一标记 B 端静默窗零命中）"
    - "S2 首帧 winsize = Hello 钳制尺寸：dialHello(cols=111, rows=44) 首帧 Welcome JSON cols==111/rows==44；stty size 回读 \"44 111\"（rows cols 序）与 Hello 逐字一致——无 80x24 中间态（首帧即可断言正确尺寸承载）"
    - "S3 运行期删命令 spawn 失败注入（启动期 LookPath 预检通过后、attach 前 unlink argv[0] 的 tmp 副本——Phase 10 SC4 启动期暴露与运行期 per-request degrade 的哲学分界实证，Pitfall 5b）：B 收 Error{server_error, \"failed to start process\"} 逐字 + close 1011；A（删除前已 attach）echo 照常 + /healthz 200——他端与服务端零影响"
    - "S4 断开 → pgid ESRCH 无僵尸：正常关闭（ws.close(1000)）后 2s 护栏内 process.kill(-pid, 0) 抛 ESRCH（setsid pgid==pid；僵尸未收割则组仍存在——ESRCH = 收割完成强证据）；1006 真实异常形态登记 skipped + reason（CODEBUDDY.md 分层策略 §5 平台豁免 + Node 原生 WebSocket 无 TCP 层强杀面——协议层可覆盖形态 = 正常关闭 + 服务端侧 pgid 断言）"
    - "S5 EXIT 私有化：A exit 42 → 仅 A 末帧 EXIT exit_code==42 + close 1000；B 1.5s 窗零帧扰动且随后 echo 照常；A2 信号死亡（kill -TERM 自身）→ EXIT exit_code==-1 + 1000——两形态他端均逐字节无扰动"
    - "S6 --max-clients=1 容量再闸（D-02 wire 形态协议层实证）：trap '' HUP linger 注入（A attach 得 pid → 断开 → 会话 trap 免疫滞留 pcSessions 而注册表清空）→ B attach 过 ③位 503 闸（registry.n==0）后命中 WS 面再闸——Error{server_error, \"server is at capacity\"} 逐字 + close 1011；A 的 pgid 仍存活（linger 实证）；场景尾部显式 SIGKILL 清场滞留进程组（CI 夹具纪律）"
    - "S7 断开重连 = 全新进程：pid1 → close → ESRCH → 重连 pid2 ≠ pid1（服务端语义；前端 terminal.reset() 归 Phase 12 不在此断言）"
    - "S8 trap '' HUP + --stop-timeout=1s KILL 兜底（D-01 机制先行的端到端证据）：断开后 ~300ms 时点进程组仍存活（HUP 免疫静默窗）→ 1s 到期后 5s 护栏内 ESRCH（SIGKILL 补发收割）"
    - "红线沿用（phase06.mjs:11-13 逐字）：token/凭据值只作断言材料，永不进 check detail/控制台输出；detail 只打印状态码/布尔/形状/退出码/文案常量（pid 数值不进 detail——以布尔「不等/ESRCH 到达」表达）"
  artifacts:
    - path: "web/uat/phase11.mjs"
      provides: "per-client 协议层 UAT 八场景（S1-S8 + S4b skipped）+ startWesh/dialHello/collectUntilClose/readPid/pollESRCH 夹具"
      contains: "phase11"
  key_links:
    - from: "web/uat/phase11.mjs 八场景"
      to: "wesh 真实二进制（--session-mode=per-client）"
      via: "spawn 真实二进制 + 协议层帧断言——D-06 兑现 10-CONTEXT deferred 项「per-client 真实协议行为 UAT 随 Phase 11+ 建设」"
      pattern: "session-mode=per-client"
    - from: "S6 容量再闸断言"
      to: "11-03 D-02 wire 形态（Go 侧 TestPerClientCapacityGate 同形态对照）"
      via: "Error{server_error, 容量文案} + 1011——Go 与协议层双证据同断言"
      pattern: "server is at capacity"
  prohibitions:
    - statement: "MUST NOT 让 UAT 断言放宽为「503 或 1011 都接受」形态——S6 的 linger 注入使 D-02 闸确定性可达，两关闭面（HTTP 503 既有闸 / WS 1011 再闸）各自逐字锁定（Pitfall 11 纪律延伸到 UAT 层）"
      status: unresolved
      verification: null
    - statement: "MUST NOT 在控制台/detail/产物中打印 token、ticket、凭据或 pid 数值（审计面红线；pid 断言一律以布尔/状态码表达）"
      status: unresolved
      verification: null
    - statement: "MUST NOT 让 S6/S8 的 trap 免疫滞留进程组随脚本结束泄漏（场景尾部显式 SIGKILL 清场 + startWesh 子进程追踪收口——CPU 受限 CI 级联减速实证纪律）"
      status: unresolved
      verification: null
---

<objective>
per-client 协议层 UAT 全链八场景（D-06 一次建齐）：web/uat/phase11.mjs——spawn 真实二进制（--session-mode=per-client）、协议层断言 PC-02/PC-03/PC-04 的全部用户可观测面，并与 Go 测试（11-03/11-04）形成双证据对照。

Purpose: Go 测试证明内部不变量；UAT 证明真实二进制在真实协议面上的端到端行为（含启动期零子进程、运行期删命令注入、trap 免疫 KILL 兜底等只有进程级才能实证的形态）。本脚本是 10-CONTEXT D-06 deferred 项「per-client 真实协议行为 UAT 随 Phase 11+ 建设」的兑现，也是 Phase 14 双模式收口的前置资产。
Output: phase11.mjs 八场景全绿（S4b 按豁免登记 skipped + reason）；运行入口 `node web/uat/phase11.mjs [wesh 二进制路径]`（argv 默认值形态照 phase06.mjs:19/:24）。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-per-client/11-CONTEXT.md
@.planning/phases/11-per-client/11-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/11-per-client/11-01-PLAN.md
@.planning/phases/11-per-client/11-03-PLAN.md
@.planning/phases/11-per-client/11-04-PLAN.md
@web/uat/phase06.mjs
@web/uat/phase02.mjs
@CODEBUDDY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: phase11.mjs 骨架 + 夹具层 + S1-S4 四场景（双 pid/首帧 winsize/spawn 失败注入/断开 ESRCH）</name>
  <files>web/uat/phase11.mjs</files>
  <read_first>
    - web/uat/phase06.mjs（全文骨架母本：头注释红线纪律 :1-19、check/skip 形态 :51-64、startWesh :89-118（spawn 真实二进制 + stdout 启动行解析）、dialHello :123-142（Hello 首帧 + Welcome 到达即握手完成 + 10s watchdog）、collectUntilClose :163-170、readPid :423-433（S6PID 正则锚定——S1 的判定方向反转：per-client 两端 pid **不等** = 独立进程强证据，注释须锚定语义差异防误抄）、echo 标记回读 :371-381、时序容差论证 :354-356、场景数组 + 串行 runner + 汇总退出码 :490-509）
    - web/uat/phase02.mjs（握手面断言形态——Welcome/Error 帧 JSON 解码先例）
    - .planning/phases/11-per-client/11-CONTEXT.md（D-06 八场景原文逐字——断言目标）
    - .planning/phases/11-per-client/11-PATTERNS.md（§9 四母本 + No Analog ②——pgid ESRCH 断言通道与运行期删命令注入均无脚本先例，本文给形态）
    - CODEBUDDY.md（双机拓扑与分层测试策略 §5 平台豁免条款——S4b skipped + reason 的依据；Linux 开发机纪律：本脚本全部场景在 Linux 开发机运行，无浏览器依赖）
  </read_first>
  <action>
    ① 新建 web/uat/phase11.mjs：头注释照 phase06.mjs:1-19 形态登记红线（token/凭据/pid 永不进 detail 与控制台；detail 只打印状态码/布尔/形状/退出码/文案常量）+ D-06 八场景清单 + 运行入口（`node web/uat/phase11.mjs [wesh 二进制路径]`，argv 默认值形态照 phase06.mjs:19/:24）。夹具层复用 phase06 形态：startWesh（spawn 二进制 + stdout 启动行解析取端口 + 子进程追踪收口）；dialHello(port, {cols, rows})（无认证实例——Hello 不携 ticket，checkTicket 对 nil store 既有跳过形态；Welcome 到达即握手完成，10s watchdog 防挂死）；collectUntilClose（帧收集至 close）；sendInput（INPUT 类型字节 + 载荷）；readPid（`/S1PID=(\d+)/` 形态正则数字锚定——回显含命令原文无数字不命中，正则只命中结果行，phase06.mjs:423-433 纪律）；echoMark（唯一标记回读）；pollESRCH（每 50ms process.kill(-pid, 0) 探测，catch ESRCH 即到达，总护栏参数化——PATTERNS No Analog ② 新形态：setsid 不变量使 pgid==pid，echo 回读的 pid 即 pgid 锚点；**严禁**把无错返回当死亡证据）。
    ② S1 双端独立 pid 互不串台：startWesh(["--session-mode=per-client", "--writable", "--", "sh"])——S1a attach 前 execFileSync('pgrep', ['-P', String(weshPid)]) 空输出（exit 1 = 无子进程；启动期零子进程 = PC-02 spawn 点后置实证，注释锚定 main.go sess=nil 切换）；S1b A/B 各 dialHello → 各发 `echo S1PID=$$\r` 回读 → pidA !== pidB；S1c A 发唯一标记 echo，B 1.5s 静默窗零命中（互不串台）。
    ③ S2 首帧 winsize：dialHello(port, {cols: 111, rows: 44}) → 首帧类型字节 Welcome 且 JSON 载荷 cols===111 && rows===44（无 80x24 中间态——首帧即正确尺寸）；随后 `stty size\r` 回读含 "44 111"（rows cols 序——PTY 实际尺寸与 Hello 钳制值逐字一致，StartWithSize 直通实证）。
    ④ S3 运行期删命令 spawn 失败：mkdtemp + cp /bin/sh 副本为 argv0 → startWesh per-client 以该副本启动（启动期 LookPath 预检通过——10-02 SC4 既成事实）→ A attach 得 Welcome + echo 标记回读（删除前会话正常）→ unlink 副本 → B attach → collectUntilClose：恰一帧 Error（code==="server_error" && message==="failed to start process" 逐字）且 close code===1011；A 再 echo 回读照常 + fetch /healthz 200（他端与服务端零影响）；cleanup rm tmpdir。注释锚定 Pitfall 5b 哲学分界（启动期 fail-fast vs 运行期 per-request degrade）。
    ⑤ S4 断开 pgid ESRCH 无僵尸：A attach（sh）→ 回读 S4PID → ws.close(1000) → pollESRCH(pid, 2000ms 护栏) 到达；S4b 登记 skip + reason（1006 真实异常形态 = OS 网卡栈时序，平台豁免 §5；Node 原生 WebSocket 无 TCP 层强杀面——协议层可覆盖形态 = 正常关闭 + 服务端侧 pgid 断言，PATTERNS §9 红线沿用）。runner 汇总中 S4b 以 skipped 呈现不阻塞退出码。
    ⑥ 自检：`node web/uat/phase11.mjs /tmp/wesh-p11/wesh`（二进制不存在则先 time go build -o /tmp/wesh-p11/wesh ./cmd/wesh）——S1-S4 已注册场景全 PASS、S4b skipped 带 reason、退出码 0。
    提交：`test(11-05): phase11.mjs skeleton + S1-S4 — dual-pid/first-frame winsize/runtime-deleted-command spawn failure/disconnect ESRCH (PC-02/PC-03, D-06)`
  </action>
  <verify>
    <automated>mkdir -p /tmp/wesh-p11 && time go build -o /tmp/wesh-p11/wesh ./cmd/wesh && node web/uat/phase11.mjs /tmp/wesh-p11/wesh</automated>
  </verify>
  <acceptance_criteria>
    - `node web/uat/phase11.mjs /tmp/wesh-p11/wesh` 退出码 0；S1/S2/S3/S4 全 PASS、S4b skipped 且带 reason 串
    - `grep -c 'session-mode=per-client' web/uat/phase11.mjs` ≥ 1
    - `grep -c 'pgrep' web/uat/phase11.mjs` ≥ 1（S1a 启动期零子进程断言位）
    - `grep -c 'ESRCH' web/uat/phase11.mjs` ≥ 1（pollESRCH 夹具）
    - `grep -c 'failed to start process' web/uat/phase11.mjs` ≥ 1（S3 文案逐字断言位）
    - 输出复核（SUMMARY 记录）：S1 detail 无 pid 数值（布尔表达）；S2 stty size 回读行 "44 111"；S3 close 1011
    - 脚本零依赖（无 import 第三方包——Node 原生 WebSocket/fetch/child_process/fs 仅内置模块）
  </acceptance_criteria>
  <done>phase11.mjs 骨架与 S1-S4 落地全绿：双 pid 独立 + 首帧尺寸直通 + 运行期 spawn 失败 1011 他端零影响 + 断开 ESRCH 无僵尸，全部经真实二进制协议层实证。</done>
</task>

<task type="auto">
  <name>Task 2: S5-S8 四场景（EXIT 私有化/容量再闸 linger 注入/重连新 pid/KILL 兜底）+ 八场景全绿收口</name>
  <files>web/uat/phase11.mjs</files>
  <read_first>
    - web/uat/phase11.mjs（Task 1 落地后现状——场景数组追加位与夹具复用面）
    - web/uat/phase06.mjs（S5b 唯一标记形态 :371-381——他端零感知断言母本；collectUntilClose 的 Error 帧 + close code 收集形态）
    - .planning/phases/11-per-client/11-CONTEXT.md（D-06 ⑤-⑧ 场景原文；specifics :128 窗口期登记）
    - .planning/phases/11-per-client/11-03-PLAN.md（TestPerClientCapacityGate 的 linger 注入形态——S6 的 Go 侧对照，同一确定性机理）
    - .planning/phases/11-per-client/11-04-PLAN.md（TestPerClientStopTimeoutKillFallback 时序双断言——S8 的 Go 侧对照）
    - internal/server/server.go（③位 503 闸 :851-856——S6 注释锚定 B 过 HTTP 闸的注册表空出前提）
  </read_first>
  <action>
    ① S5 EXIT 私有化：A/B 同实例各 dialHello（sh）→ A 发 `exit 42\r` → A collectUntilClose：末帧 EXIT 且 exit_code===42、close code===1000；B 在 A 终结后 1.5s 窗内零帧到达（含 OUTPUT——「连终结余波都不可见」强形态）；窗后 B echo 标记回读照常（服务端续跑 + 他端无扰动）。S5b 信号死亡形态：A2 attach → `kill -TERM $$\r` → 末帧 EXIT exit_code===-1 + close 1000。
    ② S6 --max-clients=1 容量再闸（linger 注入，D-02 闸的确定性形态——planner 细化已锚定 11-03）：argv = sh -c 'trap "" HUP; echo S6PID=$$; while true; do sleep 1; done'（启动即印 pid，非交互死循环不读 stdin——pid 经初始 OUTPUT 回读）→ A attach 回读 S6PID → ws.close() → 真实等待 300ms（断开 SIGHUP 被 trap 免疫、会话滞留 pcSessions 而注册表清空——linger 窗）→ B attach：过 ③位 503 闸（registry.n==0——注释锚定 server.go:851-856 计数源与「两关闭面分工」）→ collectUntilClose 断言 Error message==="server is at capacity" 逐字 + close code===1011；断言 A 的 pgid 仍存活（process.kill(-pidA, 0) 无错——trap 免疫实证）；场景尾部 process.kill(-pidA, 'SIGKILL') 清场（CI 夹具纪律——滞留进程组绝不随脚本泄漏）。
    ③ S7 重连新 pid：A attach（sh）回读 pid1 → ws.close() → pollESRCH(pid1, 2s) → 同端口 A2 dialHello 回读 pid2 → pid2 !== pid1（注释锚定：前端 terminal.reset() 归 Phase 12 不在此断言——D-06 ⑦ 括注原文）。
    ④ S8 KILL 兜底（D-01 端到端证据）：startWesh(["--session-mode=per-client", "--stop-timeout=1s", "--", "sh", "-c", "trap '' HUP; echo S8PID=$$; while true; do sleep 1; done"] 形态——注意 argv 拆数组，shell 引号在 JS 字符串内的转义以实际可运行形态为准）→ A attach 回读 S8PID → ws.close() → 真实等待 300ms 时点 process.kill(-pid, 0) 无错（HUP 免疫存活——静默窗断言，stopseq_test.go 形态的 UAT 同构）→ pollESRCH(pid, 5s 护栏) 到达（1s stopTimeout 到期 SIGKILL 补发收割）；detail 打印布尔「静默窗存活 / 护栏内 ESRCH」两形状。
    ⑤ 收口：场景数组注册 S1-S8（S4b skipped）→ `node web/uat/phase11.mjs /tmp/wesh-p11/wesh` 八场景全绿退出码 0；`node --check web/uat/phase11.mjs` 语法闸；头注释场景清单与实际注册逐一对账。
    提交：`test(11-05): phase11.mjs S5-S8 — private EXIT/capacity re-gate linger injection/reconnect new pid/KILL fallback — eight scenarios green (PC-02/PC-03/PC-04, D-06)`
  </action>
  <verify>
    <automated>node --check web/uat/phase11.mjs && time go build -o /tmp/wesh-p11/wesh ./cmd/wesh && node web/uat/phase11.mjs /tmp/wesh-p11/wesh</automated>
  </verify>
  <acceptance_criteria>
    - `node web/uat/phase11.mjs /tmp/wesh-p11/wesh` 退出码 0：S1-S8 全 PASS（S4b skipped + reason 不阻塞）
    - `grep -c 'server is at capacity' web/uat/phase11.mjs` ≥ 1（S6 文案逐字断言位）
    - `grep -c 'exit_code' web/uat/phase11.mjs` ≥ 2（S5 的 42 与 -1 两断言位）
    - `grep -c 'SIGKILL' web/uat/phase11.mjs` ≥ 1（S6 清场位）
    - `grep -c 'stop-timeout=1s\|stop-timeout", "1s' web/uat/phase11.mjs` ≥ 1（S8 参数位）
    - 输出复核（SUMMARY 记录逐场景结果行）：S5 B 端 1.5s 窗零帧、S6 close 1011 逐字文案、S7 pid 不等布尔、S8 静默窗存活 + 护栏内 ESRCH 双形状
    - 全部断言无「两码/两形态都接受」的和稀泥形态（Pitfall 11 延伸到 UAT 层——prohibitions 第一条）
  </acceptance_criteria>
  <done>D-06 八场景一次建齐全绿：EXIT 私有化两形态他端零扰动、容量再闸 1011 逐字文案、重连新 pid、trap 免疫 KILL 兜底静默窗+到期收割——协议层与 Go 测试双证据成形。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UAT 脚本 → 真实 wesh 二进制（进程级断言面） | spawn 真实子进程与进程组信号探测（kill(-pgid, 0)/SIGKILL 清场）——误伤面限于测试自身 spawn 的进程组 |
| UAT 控制台输出 → 审计/日志面 | token/凭据/pid 泄漏进 CI 日志 = 红线事故 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-05a | Information Disclosure | UAT detail/控制台打印 token/凭据/pid | medium | mitigate | phase06.mjs:11-13 红线逐字沿用（detail 只打印状态码/布尔/形状/退出码/文案常量）；acceptance 复核 SUMMARY 输出行 |
| T-11-05b | Denial of Service | trap 免疫滞留进程组随脚本泄漏（CI 级联减速实证） | medium | mitigate | S6/S8 场景尾部显式 SIGKILL 清场 + startWesh 子进程追踪收口；prohibitions 第三条入 must_haves |
| T-11-05c | Tampering | UAT 断言放宽（「503 或 1011 都接受」形态使 D-02 wire 锁定失效） | high | mitigate | S6 linger 注入使 1011 面确定性可达；两关闭面各自逐字断言；acceptance 末条负向复核 |
| T-11-SC | Tampering | 依赖供应链 | high | accept | 零新依赖（Node 原生 WebSocket/fetch 零三方包）；pnpm 零新增 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V7 Error Handling（客户端可见错误形态逐字锁定） | ✓ S3/S6 文案与关闭码逐字断言（Task 1/2） |
| V8 Audit（测试输出零敏感值） | ✓ 红线沿用 + detail 形状白名单（Task 1） |
</threat_model>

<verification>
- `node --check web/uat/phase11.mjs` 语法通过
- `node web/uat/phase11.mjs /tmp/wesh-p11/wesh` 退出码 0（八场景：S1-S8 PASS、S4b skipped+reason）
- 脚本仅内置模块、零三方依赖；token/凭据/pid 零打印
</verification>

<success_criteria>
- D-06 八场景一次建齐——ROADMAP SC1/SC2/SC3/SC4 全部用户可观测面在真实二进制上获得协议层证据
- S3/S6/S8 三注入形态（运行期删命令 / linger 容量窗 / trap 免疫 KILL）与 Go 测试同构对照成立
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **PC-02/PC-03/PC-04 (unclassified, unresolved)**：本脚本为三需求的协议层证据主体——保持 flagged-unverified 不自动闭合，由 11-06 收口闸承载终验。
- **S4b（1006 真实异常形态）skipped 登记**：CODEBUDDY.md 分层测试策略 §5 平台豁免（真实 OS 断网时序不列阻塞项）+ Node 原生 WebSocket 无 TCP 层强杀面；服务端侧等价证据 = S4a（正常关闭 → ESRCH）+ 11-01 挂点覆盖论证（一切断开形态同走 detach/kick 移除点）+ 11-04 竞态注入测。
- **S6 的 linger 注入形态**（planner 细化，与 11-03 Go 侧同机理）：顺序 attach 下第二客户端在 ③位收 HTTP 503（既有行为零改动）；D-02 的 WS 面 1011 确定性触发需「注册表空出但 pcSessions 满」窗口，trap 免疫 linger 提供该窗口。
- **Linux 开发机运行面**：本脚本全部场景为协议层（无浏览器依赖），符合双机拓扑分工；Windows 工作站 Playwright 层属 Phase 14 herdr 场景（ROADMAP Phase 14 SC3）。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- `web/uat/phase11.mjs`（新文件——per-client 协议层 UAT 八场景 + S4b skipped 登记）
- 夹具函数：startWesh / dialHello / collectUntilClose / sendInput / readPid / echoMark / pollESRCH（脚本内——pollESRCH 为 PATTERNS No Analog ② 新形态首落地）
- 场景函数 S1-S8（含 S1a 启动期零子进程 pgrep 断言、S6 linger 注入、S8 时序双断言）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/11-per-client/11-05-SUMMARY.md` when done
</output>
