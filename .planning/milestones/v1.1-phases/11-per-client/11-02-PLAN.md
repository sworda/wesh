---
phase: 11-per-client
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/pty/reap_darwin.go
  - internal/pty/reap_darwin_test.go
autonomous: true
requirements:
  - PC-03

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "darwin 共享 kqueue exitWatcher 的 watch() 对重复 pid 注册 fail-closed：w.mu 内 `if _, dup := w.subs[pid]; dup { w.mu.Unlock(); return nil, errDupWatch }`——kqueue (ident,filter) 唯一键语义下重复注册是替换而非叠加，先注册者 channel 被影子化则 awaitExit 永等（会话收割挂死）；fail-closed 把「挂死」变「可观测错误」（Pitfall 9）"
    - "调用方退化兜底零新面：awaitExit（reap_darwin.go:109-120）对 watch 错误返回的既有分支 `return cmd.Wait()` 直接消费 errDupWatch——重复注册退化为阻塞直等，收割语义保持（唯一收割者纪律不受影响）"
    - "errDupWatch 为包级错误值（errors.New 单点定义），文案不含敏感值；watch() 注册失败摘除分支（:54-60，Rule 2 自动修复）逐字不动"
    - "Linux 侧零改动（pidfd 每进程独立，N 规模天然成立——reap_linux.go 注释已论证）；reap_darwin.go 除 dup 检查外逐字不动"
    - "零回归收口闸：shared 全量 Go 测试原样绿 + 期望值逐字未动；本 plan 不触碰任何既有测试断言（reap_darwin_test.go 仅追加新测试函数）"
  artifacts:
    - path: "internal/pty/reap_darwin.go"
      provides: "watch() dup-watch fail-closed 防御 + errDupWatch 包级错误值"
      contains: "errDupWatch"
    - path: "internal/pty/reap_darwin_test.go"
      provides: "TestWatchDupPidFailClosed——同 pid 二次注册返回 errDupWatch 且首个订阅不被影子化（Pitfall 9 防御的 CI 锁，macOS leg 运行）"
      contains: "TestWatchDupPidFailClosed"
  key_links:
    - from: "internal/pty/reap_darwin.go watch()"
      to: "internal/pty/reap_darwin.go awaitExit()"
      via: "重复注册错误经既有 watch-error 分支退化 cmd.Wait() 直等——fail-closed 闭环"
      pattern: "errDupWatch"
  prohibitions:
    - statement: "MUST NOT 让重复 pid 注册静默覆盖既有订阅（kqueue 替换语义下先注册者 channel 影子化 = 该会话收割挂死、EXIT 永不送达——fail-closed 是唯一可接受形态）"
      status: unresolved
      verification: null
    - statement: "MUST NOT 为 dup 防御引入新锁类型或改变 w.mu 的既有保护面（watcher 单例纪律与锁序全序保持）"
      status: unresolved
      verification: null
---

<objective>
darwin 共享 kqueue exit watcher 的 dup-watch fail-closed 防御（Pitfall 9 防御面，P11 阶段映射项）——per-client N 会话形态下，重复 pid 注册从「静默影子注册→收割挂死」变为「可观测错误 → 调用方退化 cmd.Wait() 兜底」。

Purpose: reap_darwin.go 的 exitWatcher 注释自挂账「待 Phase 5 多会话时重估」——Phase 11 每会话一 goroutine 消费共享 watcher 使该挂账到期；dup-watch 防御是 N 规模安全的必要件。与 11-01 主干文件面零重叠（internal/pty 平台文件 vs internal/server+cmd/wesh），Wave 1 并行。
Output: watch() fail-closed + errDupWatch + CI darwin leg 新测试锁；Linux 行为零漂移。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-per-client/11-CONTEXT.md
@.planning/phases/11-per-client/11-PATTERNS.md
@.planning/research/PITFALLS.md
@internal/pty/reap_darwin.go
@internal/pty/reap_darwin_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: reap_darwin.go watch() dup-watch fail-closed 防御 + errDupWatch 包级错误值</name>
  <files>internal/pty/reap_darwin.go</files>
  <read_first>
    - internal/pty/reap_darwin.go（全文 :1-120——文件头「N 会话共用」设计注释 :24-27、watch() 注册段 :42-62、注册失败摘除分支 :54-60、awaitExit 退化兜底 :109-120）
    - .planning/phases/11-per-client/11-PATTERNS.md（§7 dup-watch 三母本与防御形态定案）
    - .planning/research/PITFALLS.md（Pitfall 9 全文——影子注册机理与 fail-closed 处置）
  </read_first>
  <action>
    ① 包级新增错误值：var errDupWatch = errors.New("pty: duplicate exit watch pid")（errors 包导入如缺则补；注释登记 Pitfall 9——kqueue kevent 以 (ident,filter) 为唯一键，同 pid 重复 EV_ADD 是替换而非叠加；subs[pid] 覆盖使先注册者 channel 影子化，awaitExit 的 <-exited 永等 → 会话收割挂死。fail-closed 把挂死变可观测错误）。
    ② watch() 内（:42-62）w.mu.Lock 之后、subs 赋值之前插入 dup 检查：若 w.subs[pid] 已存在则 w.mu.Unlock() 并 return nil, errDupWatch——已登记订阅零改动（先注册者不受影子化）；正常路径（赋值 + Kevent 注册 + 失败摘除分支 :54-60）逐字不动。
    ③ 文件头 N 会话设计注释（:24-27 区）追加一行锚定：dup-watch fail-closed 于 11-02 落地（Pitfall 9 挂账兑现之一；N 规模并发退出复演归 Phase 14 darwin CI）。
    ④ awaitExit（:109-120）零改动——watch 错误返回的既有 `return cmd.Wait()` 分支即兜底消费点；在该分支注释补一句 errDupWatch 为当前唯一预期错误源。
    ⑤ Linux 零改动自检：reap_linux.go 与 signal_linux.go 不在本任务 diff 中出现。
    提交：`fix(11-02): darwin exit watcher dup-watch fail-closed — duplicate pid registration returns errDupWatch, caller degrades to cmd.Wait (PC-03, Pitfall 9)`
  </action>
  <verify>
    <automated>GOOS=darwin go build ./internal/pty/ && GOOS=darwin go vet ./internal/pty/ && time go build ./... && go vet ./... && go test -race -count=1 ./internal/pty/</automated>
  </verify>
  <acceptance_criteria>
    - `GOOS=darwin go build ./internal/pty/` 与 `GOOS=darwin go vet ./internal/pty/` 均成功（Linux 开发机上的 darwin 编译闸）
    - `grep -c 'errDupWatch' internal/pty/reap_darwin.go` ≥ 2（定义 + 返回点）
    - `grep -c 'dup := w\.subs\[pid\]' internal/pty/reap_darwin.go` == 1
    - `grep -c 'return cmd\.Wait()' internal/pty/reap_darwin.go` ≥ 1（退化兜底形态保持）
    - `go test -race -count=1 ./internal/pty/` Linux 侧全绿（darwin 文件不参与本机构建/测试，linux 套件零漂移）
    - `git diff --stat -- internal/pty/reap_linux.go internal/pty/signal_linux.go` 为空
  </acceptance_criteria>
  <done>watch() 对重复 pid 注册 fail-closed（errDupWatch），先注册订阅零影子化；awaitExit 经既有分支退化 cmd.Wait()；darwin 编译闸与 Linux 全量双绿。</done>
</task>

<task type="auto">
  <name>Task 2: reap_darwin_test.go 新增 TestWatchDupPidFailClosed（CI macOS leg 锁）</name>
  <files>internal/pty/reap_darwin_test.go</files>
  <read_first>
    - internal/pty/reap_darwin_test.go（全文——既有测试形态与 build tag、Q1 裁决测试 TestKqueueExitNormal/ZombieRace 的 CI-only 纪律注释）
    - internal/pty/reap_darwin.go（Task 1 落地后的 watch() 防御形态——断言目标）
    - .planning/phases/11-per-client/11-PATTERNS.md（§7 Analog C 退化兜底形态）
  </read_first>
  <action>
    ① reap_darwin_test.go 追加 TestWatchDupPidFailClosed：构造 newExitWatcher（或复用既有测试的 watcher 装配形态，按文件内先例）→ 同一 pid 两次 watch——首次成功（返回非 nil channel），第二次 errors.Is(err, errDupWatch) 为真且返回 channel 为 nil；随后断言首个订阅未被影子化：向该 pid 的实际进程退出事件路径按既有测试形态触发（或按文件内既有模拟形态），首 channel 能收到通知（防「dup 检查误伤正常注册」的反向锁）。
    ② 测试注释登记：本测仅在 darwin 运行（文件 build tag 既定）；Linux 开发机验证 = GOOS=darwin 编译/vet 闸（Task 1 acceptance 已锁）+ CI macOS leg 运行；挂账 Phase 14 的 N 路并发退出 + 混合僵尸竞态复演（Pitfall 9 剩余面）。
    ③ 既有测试函数（TestKqueueExitNormal/ZombieRace 等）逐字不动——append-only。
    提交：`test(11-02): TestWatchDupPidFailClosed — duplicate pid watch rejected, first subscriber unshadowed (PC-03, Pitfall 9)`
  </action>
  <verify>
    <automated>GOOS=darwin go vet ./internal/pty/ && go test -race -count=1 ./internal/pty/ && $(go env GOROOT)/bin/gofmt -l internal/pty/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'func TestWatchDupPidFailClosed' internal/pty/reap_darwin_test.go` == 1
    - `grep -c 'errors.Is(err, errDupWatch)' internal/pty/reap_darwin_test.go` ≥ 1
    - `GOOS=darwin go vet ./internal/pty/` 成功（测试文件 darwin 编译闸——Linux 本机不执行该测试体，CI macOS leg 承担运行）
    - `go test -race -count=1 ./internal/pty/` Linux 侧全绿（零漂移）
    - `$(go env GOROOT)/bin/gofmt -l internal/pty/` 零输出
    - `git diff -U0 -- internal/pty/reap_darwin_test.go` 的删除行计数 == 0（append-only 证据）
  </acceptance_criteria>
  <done>dup-watch fail-closed 防御经 darwin CI 测试锁定（同 pid 二次注册拒、首订阅不影子化）；Linux 侧零漂移；append-only 纪律保持。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 服务端内部 → darwin 内核 kqueue 订阅面 | (ident,filter) 唯一键语义下重复注册 = 替换；影子化使收割挂死（可用性事故） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02a | Denial of Service | 重复 pid 注册影子化先注册者 → 该会话收割挂死（goroutine 泄漏 + EXIT 永不送达） | medium | mitigate | watch() fail-closed dup 检查（Task 1）+ awaitExit 退化 cmd.Wait() 既有兜底 + TestWatchDupPidFailClosed CI 锁（Task 2） |
| T-11-SC | Tampering | 依赖供应链 | high | accept | 零新依赖；本 plan 仅 pty 包平台文件改动，go.mod/go.sum 零漂移 |

## ASVS L1 Mapping

| Control | Status |
|---------|--------|
| V7 Error Handling（内部错误可观测、不静默恶化） | ✓ fail-closed 错误返回 + 退化兜底（Task 1） |
</threat_model>

<verification>
- `GOOS=darwin go build ./internal/pty/` + `GOOS=darwin go vet ./internal/pty/` 成功（darwin 编译闸）
- `go test -race -count=1 ./internal/pty/` Linux 侧全绿
- `git diff --stat` 仅含 reap_darwin.go / reap_darwin_test.go 两文件
</verification>

<success_criteria>
- 重复 pid 注册返回 errDupWatch（fail-closed），先注册订阅不受影响
- awaitExit 对该错误退化 cmd.Wait()（既有分支零改动消费）
- CI macOS leg 新增 TestWatchDupPidFailClosed 运行锁
</success_criteria>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe, EDGE_ABSENT 回退)

- **PC-03 (unclassified, unresolved)**：探针未分类——本 plan 承担 PC-03 的平台收割防御面（Pitfall 9）；断开 SIGHUP 主语义与测试锁由 11-01/11-04 承担。保持 flagged-unverified，由 11-06 收口闸承载显式证据。
- **darwin 运行时验证依赖 CI macOS leg**：Linux 开发机硬约束（CODEBUDDY.md 双机拓扑）——本机仅做 GOOS=darwin 编译/vet 闸；TestWatchDupPidFailClosed 的实际运行在 CI macOS leg（Phase 1 双平台 CI 矩阵既有）。N 路并发退出 + 混合僵尸竞态复演（Pitfall 9 剩余面）归 Phase 14。
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this plan produces

- 错误值 `errDupWatch`（internal/pty/reap_darwin.go——包级 errors.New 单点定义）
- watch() dup 检查分支（internal/pty/reap_darwin.go——w.mu 内重复 pid 拒绝）
- 测试 `TestWatchDupPidFailClosed`（internal/pty/reap_darwin_test.go——append-only）
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/11-per-client/11-02-SUMMARY.md` when done
</output>
