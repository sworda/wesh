---
phase: 12-per-client
plan: 04
type: execute
wave: 4
depends_on: [12-01, 12-02, 12-03]
files_modified:
  - web/uat/phase12.mjs
autonomous: true
requirements: [PC-05, PC-06, PC-07, PC-10, PC-11]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "phase12.mjs S1：Welcome.session 字段双模式对照——per-client 实例收 \"per-client\"、shared 实例收 \"shared\"，且 mode/cols/rows 既有键同帧共存（D-08 协议面端到端证据）"
    - "phase12.mjs S2：resize 直通双端互不影响——A RESIZE 后 A 的 stty size 变、B 不变，且 attach Welcome 之后双端零 'W' 帧（PC-05 SC1 协议层证据）"
    - "phase12.mjs S3：ro RESIZE 直通——ro 端 RESIZE 后经 SIGWINCH trap 回读新尺寸到达 OUTPUT；shared 同形态对照零新输出（D-06/D-07 wire 面证据）"
    - "phase12.mjs S4：ro INPUT 丢弃（零回显）+ rw 限速保留（flood 后不踢不断、后续输入照常被处理）（PC-07 协议层证据）"
    - "phase12.mjs S5：停读期输出不丢、恢复后完整到达——stall 客户端 TCP 停读 → seq 洪水积压 → 恢复读 → 序号连续无缺口，且对端全程不受影响（PC-11 端到端证据）"
    - "phase12.mjs S6：真实 10s+ dwell 到期 → 1013 slow_consumer 端到端（D-12——真实等待，时钟不 mock、服务端等待不缩短；phase06 保活测 11s+ 先例）"
    - "phase12.mjs 红线：token/凭据/pid 数值只作断言材料永不进 detail/控制台输出；assertOutputClean() 运行时自净逐字保留（phase11.mjs:27-30 纪律沿用）"
  artifacts:
    - path: "web/uat/phase12.mjs"
      provides: "Phase 12 协议层 UAT 六场景"
      min_lines: 400
      contains: "S6"
  key_links:
    - from: "web/uat/phase12.mjs"
      to: "wesh 二进制（/tmp/wesh-uat/wesh）"
      via: "spawn 真实二进制 + 原生 WebSocket 握手 + 帧级断言（phase11.mjs 同构）"
      pattern: "startWesh"
  prohibitions:
    - requirement_id: PC-11
      category: values
      statement: "UAT 时序纪律不得破——dwell/停读类场景真实等待，禁精确时点断言、时钟不 mock、服务端等待不缩短（超时上限只做护栏）"
---

<objective>
Phase 12 协议层 UAT（web/uat/phase12.mjs，零依赖 Node 原生 WebSocket/fetch，phase11.mjs 同构母本骨架逐字可抄）：以真实二进制在真实协议面上端到端断言本 phase 五需求的 wire 行为——Welcome.session 双模式、resize 直通隔离、ro 放行、ro INPUT 门控+限速、停读续读不丢、真实 dwell 到期 1013。

Purpose: Go 测试证明内部不变量（12-01/12-02/12-03），本脚本证明真实二进制在真实协议面上的端到端行为（phase11.mjs 定位先例逐字）。
Output: web/uat/phase12.mjs 六场景全绿。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-per-client/12-CONTEXT.md
@.planning/phases/12-per-client/12-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: phase12.mjs 骨架 + S1-S4（Welcome.session 对照 / resize 隔离 / ro RESIZE 直通 / ro INPUT+rw 限速）</name>
  <files>web/uat/phase12.mjs</files>
  <read_first>
    - web/uat/phase11.mjs 全文（同构母本——头注释纪律 :1-35、check/skip 双通道 :61-74、startWesh :100-128、dialHello :135-152、帧工具 :44-59/:187-194、pid 工具 :199-255、stty 回读先例 S2b :331-340、assertOutputClean :563-573、场景收尾纪律 :576-595）
    - internal/proto/proto.go:21-40（帧常量 + session 键注释）与 :100-125（WelcomePayload）
    - .planning/phases/12-per-client/12-CONTEXT.md D-12/D-13（真实 dwell 等待与验证面切片裁决）
  </read_first>
  <behavior>
    - 骨架逐字继承 phase11.mjs：头注释（覆盖 PC-05/06/07/10/11 场景清单 + 红线 + 时序纪律 + 运行方式）、check/skip/emittedDetails/assertOutputClean/startWesh（--bind 127.0.0.1 --port 0 + stdout 两行解析 + 50ms 落定窗 + SIGKILL 收口）/dialHello（Welcome 到达=握手完成 + 10s watchdog）/帧常量与 proto.go 对齐注释
    - S1 Welcome.session：per-client 实例（--session-mode=per-client -- bash）dialHello → Welcome JSON session=="per-client"；shared 实例（默认模式 -- bash）→ session=="shared"；两实例均断言 mode/cols/rows 键同帧存在
    - S2 resize 直通隔离：per-client --writable 实例双端 attach（A/B 各自 bash）→ A 发 RESIZE(120,50) → A 端 sendInput "stty size\n" 回读 "50 120"；B 端 stty size 仍为其 Hello 尺寸；静默窗断言双端 attach Welcome 后零 'W' 帧到达
    - S3 ro RESIZE 直通：per-client 实例（无 --writable → ro）spawn bash 携 WINCH trap（trap 打印 stty size，S8 trap 先例同族）→ ro 端 RESIZE(133,55) → OUTPUT 到达新尺寸行；shared 无 --writable 实例同脚本对照 → 静默窗零新尺寸行（D-06 第二闸 shared 逐字保留证据）
    - S4 ro INPUT + rw 限速：ro 端 sendInput "echo MARKER_RO\n" → 静默窗零 MARKER_RO；per-client --writable 实例 rw 端超速率 flood INPUT（>32KiB/s 持续 ~2s）→ 连接存活（后续 echo MARKER_RW 正常回显 = 丢弃语义不踢不断）
  </behavior>
  <action>
    1. 新建 web/uat/phase12.mjs（骨架构成件清单见 behavior 首行，逐字复用母本对应段）；帧常量区注释补 session 键互指（proto.go D-16 同款）。
    2. S1-S4 场景函数按 behavior 逐条落地；ro/rw 形态经启动参数控制（--writable 有无），不依赖分享 token 通道（断言面更直接，token 通道非本 phase 改动面）。
    3. WINCH trap 脚本形态：bash -c 内 trap 'stty size' WINCH + 起始先打一次基线 + 主循环 sleep 驻留——argv 经 spawn 数组传递不经 shell 拼接（exec 数组纪律，P1 既有）。
    4. 时序纪律逐字：真实等待、护栏上限、禁精确时点断言；静默窗 1-1.5s 级（防抖 50ms 的 20 倍余量）。
    5. 运行：time node web/uat/phase12.mjs（构建最新二进制至 /tmp/wesh-uat/wesh 先行：time go build -o /tmp/wesh-uat/wesh ./cmd/wesh）。
  </action>
  <verify>
    <automated>time go build -o /tmp/wesh-uat/wesh ./cmd/wesh && time node web/uat/phase12.mjs</automated>
  </verify>
  <acceptance_criteria>
    - phase12.mjs 存在且头注释载五需求编号清单 + 红线 + 时序纪律 + 运行方式四件
    - S1-S4 check 全过（skipped 须带 reason）；assertOutputClean() 自净断言过
    - 脚本退出码 0；detail/输出零 token/凭据/pid 数值（grep 输出日志复核）
  </acceptance_criteria>
  <done>S1-S4 协议层端到端绿——Welcome.session 双模式、resize 直通隔离、ro 放行配对、ro 门控+限速四条 wire 语义锁定。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: phase12.mjs S5-S6（停读续读不丢 / 真实 dwell 到期 1013）+ 全脚本收口</name>
  <files>web/uat/phase12.mjs</files>
  <read_first>
    - web/uat/phase12.mjs（Task 1 落地的骨架与 S1-S4）
    - web/uat/phase11.mjs:199-255（pid 工具/pollESRCH——S6 踢出后进程组收割复核可复用）与 :32-33（时序纪律原文）
    - internal/server/slowclient_test.go:7-13（stall 形态纪律——TCP 停读 = dialHello 后不再 Read 的语义对应）
    - .planning/phases/12-per-client/12-CONTEXT.md D-12（真实 10s+ dwell 裁决原文）
  </read_first>
  <behavior>
    - S5 停读续读不丢：per-client --writable 实例双端——A stall（dialHello 后不 read，TCP 缓冲填满 → outbox 涨满 → 停读）+ spawn seq 洪水（seq 1 400000 级，超 outbox 512KiB 即足量）；stall 窗 ~3s（≪ dwell 10s 安全边）→ A 恢复读 → 序号连续无缺口 + 连接存活（无 1013）；B 端全程 echo 照常（不受影响）
    - S6 dwell 到期 1013：per-client 实例单端 stall 恒不读 → 真实等待 ≥10s（默认 defaultSlowDwell 生产值，零覆写零测试钩子）→ CloseError code==1013 且 reason=="slow_consumer"（护栏 25s）；踢出后 pollESRCH 复核进程组收割（PC-03 挂点联动证据）
    - 全脚本串行收口：scenarios 数组顺序执行 + 场景间 300ms + 异常纳入 emittedDetails + skipped 不阻塞退出码（phase11.mjs:576-595 逐字）
  </behavior>
  <action>
    1. S5/S6 两场景按 behavior 落地；stall 判定以「暂停读 + 等待窗」表达（协议层无法直读服务端计数，行为证据 = 恢复后字节连续+连接存活）。
    2. S6 真实 10s+ 等待（D-12 硬约束：时钟不 mock、服务端 dwell 不缩短——phase06 保活测 11s+ 先例）；1013 到达后 pollESRCH 复核该会话进程组消亡（断开即杀联动，phase11.mjs S4 形态复用）。
    3. 全脚本两轮连跑（时间漂移面排查，11-06 收口两轮基线先例）：time node web/uat/phase12.mjs 连续两次退出码 0。
    4. README/文档零改动（PC-12 模式语义文档归 Phase 14，CONTEXT out-of-scope 明示）。
  </action>
  <verify>
    <automated>time node web/uat/phase12.mjs && time node web/uat/phase12.mjs</automated>
  </verify>
  <acceptance_criteria>
    - S5/S6 check 全过；S6 实测等待 ≥10s（脚本日志可见 dwell 真实等待形态）
    - 两轮连跑均退出码 0（基线一致）
    - S6 踢出后进程组 ESRCH 复核通过；S5 序号连续性断言过
    - assertOutputClean() 全脚本自净过
  </acceptance_criteria>
  <done>phase12.mjs 六场景两轮全绿——Phase 12 五需求协议层端到端证据全齐（含真实 dwell 10s+ 1013）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试脚本→真实二进制 | UAT 以真实 spawn 二进制 + 真实 WS 握手为证据面；红线 = 敏感值（token/pid）永不落盘/日志 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-11 | Information Disclosure | phase12.mjs 测试输出 | medium | mitigate | assertOutputClean() 运行时自净逐字保留 + detail 只打状态码/布尔/形状/退出码/文案常量（phase11.mjs:27-30 红线沿用） |
| T-12-12 | DoS | UAT 资源残留 | low | mitigate | startWesh SIGKILL 收口 + 场景间 300ms + pollESRCH 复核——泄漏子进程零残留（phase11.mjs 既有纪律） |
| T-12-SC | Tampering | 依赖面 | high | mitigate | 零新依赖（Node 原生 API，package.json 零 diff）；无安装任务，legitimacy 门不触发 |
</threat_model>

<verification>
- time go build -o /tmp/wesh-uat/wesh ./cmd/wesh && time node web/uat/phase12.mjs 两轮退出码 0
- 六场景 check 全过、assertOutputClean 自净过、skipped 行均带 reason
</verification>

<success_criteria>
1. phase12.mjs S1-S6 两轮全绿（真实二进制、真实 dwell 10s+）
2. 五需求协议层证据齐备且红线自净通过
</success_criteria>

## Artifacts this phase produces

本 plan 新建符号（供 plan-review-convergence 源 grounding 排除清单）：

- `web/uat/phase12.mjs`（新文件）：场景 S1（Welcome.session 双模式对照）/ S2（resize 直通双端隔离 + 零 'W' 帧）/ S3（ro RESIZE 直通 WINCH trap 回读 + shared 对照）/ S4（ro INPUT 丢弃 + rw 限速保留）/ S5（停读续读字节连续）/ S6（真实 dwell 到期 1013 + ESRCH 复核）
- 无生产代码符号新增/变更

<output>
Create `.planning/phases/12-per-client/12-04-SUMMARY.md` when done
</output>
