---
phase: 12-per-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/proto/proto.go
  - internal/proto/proto_test.go
  - internal/server/server.go
  - internal/server/perclient.go
  - internal/server/clients.go
  - internal/server/resize.go
  - internal/server/clients_test.go
  - internal/server/perclient_test.go
  - web/src/main.ts
  - web/dist/index.html
  - web/uat/phase12-dom.mjs
autonomous: false
requirements: [PC-06]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "WelcomePayload 恒携 session 键（值域 shared|per-client，与 --session-mode 同词同值域，恒序列化无省略语义，per D-08）"
    - "Welcome 五调用点（attach 两模式/递补升格/afterDrain 补发/运行期尺寸推送）组帧全部携带服务端当前 sessionMode（per D-08；G-05-1 恒在键先例）"
    - "旧前端/旧服务端互操作零漂移：旧前端忽略未知 session 键；新前端对缺 session 键的 Welcome 按 shared 处理（不 reset，per D-08 协议演化纪律）"
    - "前端在 WELCOME 分支内统一判断：模式位=per-client → terminal.reset()（首连 reset no-op 等价，shared 永不 reset，per D-09）"
    - "重连 reset 静默无提示——零新面板/文案，画面恢复交给子程序重绘（per D-10）"
    - "模式位为 per-connection 状态，入 connect() 重置块同批清零（IN-01 防漂移登记口径，per D-09/CONTEXT Claude's Discretion 落定）"
    - "jsdom 断言三面：per-client Welcome → reset 效应可观测；1006 重连全链新 Welcome → 旧屏文本消失；缺 session 键 Welcome → 不 reset（per D-13）"
  artifacts:
    - path: "internal/proto/proto.go"
      provides: "WelcomePayload.Session 字段 + WelcomeFrame 第 5 参 session"
      contains: "Session string"
    - path: "web/src/main.ts"
      provides: "sessionMode per-connection 变量 + WELCOME 分支 reset 统一判断"
      contains: "sessionMode"
    - path: "web/uat/phase12-dom.mjs"
      provides: "jsdom 断言 D1（reset 全链）/D3（缺键不 reset）"
      min_lines: 200
    - path: "internal/server/perclient_test.go"
      provides: "TestPerClientWelcomeSession e2e 断言（Welcome.session == per-client）"
      contains: "TestPerClientWelcomeSession"
  key_links:
    - from: "internal/server/perclient.go"
      to: "internal/proto/proto.go"
      via: "upgradePerClient Welcome 组帧点（:217）传 s.sessionMode"
      pattern: "WelcomeFrame\\(effMode, prefs, h\\.Cols, h\\.Rows, s\\.sessionMode\\)"
    - from: "web/src/main.ts"
      to: "internal/proto/proto.go"
      via: "WELCOME 分支解析 w.session → sessionMode 存储 → reset 判断"
      pattern: "w\\.session"
  prohibitions:
    - requirement_id: PC-06
      category: values
      statement: "前端 reset 不得应用于 shared 模式与缺 session 键（旧服务端）的 Welcome——CORE-05 接回同进程语义下旧屏有效，误清屏即数据面破坏"
    - requirement_id: PC-06
      category: values
      statement: "重连 reset 不得引入任何「新会话」提示面板/文案/状态行——ttyd parity 静默语义（D-10），提示即噪音"
---

<objective>
PC-06 全链 tracer：Welcome 帧加 session 模式位（本 phase 唯一协议面变更，additive，D-08），前端按模式位在重连分支静默 terminal.reset() 清屏（D-09/D-10）——per-client 异常断线重连后用户看到干净的新会话而非旧屏残影。本 plan 是 phase tracer：一帧 Welcome 从 proto 定义 → 两模式服务端组帧 → wire → 前端解析/reset → jsdom 断言，纵穿本 phase 全部改动层（proto/server/frontend/jsdom），端到端证明协议接缝可用，后续 plan 的 D-07（ro RESIZE 第一闸按模式位放开）与 phase12.mjs S1（Welcome.session 两模式对照）都消费本 plan 建立的模式位。

Purpose: 重连=新进程的服务端语义 Phase 11 已锁（phase11.mjs S7 双 pid）；本 plan 交付其前端半侧——模式位下发 + reset，同时把 v1.1.0 公开协议面（session 键）一次性钉死。
Output: WelcomePayload.Session + WelcomeFrame 五参形态全调用点同步 + main.ts 模式位/reset + dist 重建 + phase12-dom.mjs jsdom 双脚本断言 + Go/proto 断言组。
</objective>

<execution_context>
@$HOME/.codebuddy/gsd-core/workflows/execute-plan.md
@$HOME/.codebuddy/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-per-client/12-CONTEXT.md
@.planning/phases/12-per-client/12-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: D-08 one-way 确认门——Welcome.session 公开协议契约</name>
  <decision>WelcomePayload 新增 session 键（"shared"|"per-client" 字符串枚举，恒序列化无省略语义）——v1.1.0 起发布即不可收回的公开 wire 契约</decision>
  <context>12-CONTEXT.md D-08 已锁定该方案并评级 one-way：发布后前端按该键分支（reset/ro RESIZE 放行），删键/改词即破坏已发布契约。本门按项目 one-way 确认门先例（05-03 D-05 / 06-04 D-12/D-14 / 09-04）在实现前确认。Undo 成本：任何改词都是协议破坏 + 前端两处分支重写。</context>
  <options>
    <option id="option-a">
      <name>session 字符串枚举恒序列化（D-08 既定，推荐）</name>
      <pros>与 --session-mode 同词同值域自描述零注释互指；恒在键使「缺席=旧服务端」识别契约成立（G-05-1 Cols/Rows 先例）；additive 对旧前端零影响</pros>
      <cons>协议面 +1 键，发布后不可删改词（one-way 本义）</cons>
    </option>
    <option id="option-b">
      <name>per_client 布尔键（D-08 已否决）</name>
      <pros>载荷小几个字节</pros>
      <cons>表达力弱（第三态无法扩展）；与 CLI flag 不同词需注释互指，双写漂移面</cons>
    </option>
    <option id="option-c">
      <name>不加字段（前端用其他启发式判模式）</name>
      <pros>协议零改动</pros>
      <cons>无可靠模式信号——前端无法区分重连后该不该 reset，PC-06 结构性不可达</cons>
    </option>
  </options>
  <resume-signal>Select: option-a（确认 D-08 既定）, option-b, or option-c</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: 协议面 tracer——WelcomePayload.session + WelcomeFrame 五参全调用点同步 + 两模式 e2e 断言（D-08）</name>
  <reversibility rating="one-way">D-08：session 键一经发布前端即按它分支（reset/ro 放行），删键改词 = 破坏 v1.1.0 公开 wire 契约，Task 1 确认门先行。</reversibility>
  <files>internal/proto/proto.go, internal/proto/proto_test.go, internal/server/server.go, internal/server/perclient.go, internal/server/clients.go, internal/server/resize.go, internal/server/clients_test.go, internal/server/perclient_test.go, web/src/main.ts</files>
  <read_first>
    - internal/proto/proto.go:100-116（WelcomePayload 现状 + G-05-1 恒序列化注释先例）与 :146-157（WelcomeFrame 组帧）与 :1-40（帧常量区 + D-16 互指纪律）
    - internal/server/perclient.go:207-220（per-client attach Welcome 组帧点）
    - internal/server/server.go:1067-1078（shared attach Welcome 组帧点）
    - internal/server/clients.go:543-570（afterDrain WR-02 补发 Welcome）与 :667-695（promoteNextLocked 升格 Welcome）
    - internal/server/resize.go:165-182（pushSessionDimsLocked 运行期推送）
    - internal/proto/proto_test.go:60-130（WelcomeFrame 既有断言四调用点 :65/:100/:115/:124）
    - internal/server/clients_test.go:95-105 与 :160-170（WelcomeFrame 三调用点）
    - web/src/main.ts:12-34（帧常量注释块，D-16 互指同步点）
  </read_first>
  <behavior>
    - proto 新断言：WelcomeFrame(ModeRO, nil, 80, 24, "shared") 组帧后 JSON 含 session 键且值 "shared"；第二形态值 "per-client"；session 键恒在（无省略语义——JSON map 必含该键，G-05-1 同形态断言）
    - proto 既有四调用点（proto_test.go:65/:100/:115/:124）机械加第 5 参 "shared" 字面量——断言行（期望值）逐字不动
    - server 包 clients_test.go 三调用点（:99/:100/:166）机械加第 5 参 SessionModeShared 常量——断言行逐字不动
    - Go e2e（perclient_test.go 新增 TestPerClientWelcomeSession）：per-client harness dialHello → Welcome JSON 解析 session=="per-client"，且 mode/cols/rows 键同帧共存不挤压
    - shared 模式 Welcome.session=="shared" 的协议层对照由 12-04 phase12.mjs S1 承担（本 plan 不为 shared 新起 Go e2e——perclient_test.go 保 per-client 单一归属，D-11）
  </behavior>
  <action>按 D-08 落地协议面，一次性同提交（签名变更编译原子性）：
    1. internal/proto/proto.go：WelcomePayload（:111-116）加字段 Session string 配显式 json tag "session"——插入位置 Mode 之后；注释按 Cols/Rows 同款形态载「恒序列化无省略语义；与 --session-mode 同词同值域（clients.go SessionModeShared/SessionModePerClient 常量同源）；缺席=旧服务端=shared 的识别契约（前端防御性缺省依据）」。WelcomeFrame（:154-157）加第 5 参 session string，组帧填入 Session；注释更新三通道+本键取值来源（调用方恒传 s.sessionMode）。帧常量区 Welcome 注释（:27-32）补 session 键说明并按 D-16 与 main.ts 互指。
    2. 五个生产调用点全部传 s.sessionMode（perclient.go:217 / server.go:1078 / clients.go:568 afterDrain / clients.go:680 promoteNextLocked / resize.go:172 pushSessionDimsLocked）——运行期再推送三通道在 per-client 结构性不可达（arbiter 零值天然 no-op），shared 路径运行期值恒 "shared"，统一传参无双写。
    3. 既有测试七调用点机械适配：proto_test.go 四处传 "shared" 字面量（proto 包不引 server 常量，注释指 clients.go:88-92）；clients_test.go 三处传 SessionModeShared。既有断言行一字不动（收口闸「期望值逐字未动」的白名单形态=仅加参）。
    4. proto_test.go 新增 TestWelcomeFrameSession：两值组帧 + JSON map 键存在性断言（恒序列化）。
    5. perclient_test.go 新增 TestPerClientWelcomeSession（harness startPerClientServer + dialHelloPayload 既有形态，:56-135）：Welcome JSON session 值断言 + 三既有键共存。
    6. web/src/main.ts:12-34 帧常量注释块 Welcome 载荷描述加 session 键互指（D-16 纪律，proto.go 注释同款互指）。
    7. 构建命令带 time 前缀（项目偏好）。
  </action>
  <verify>
    <automated>go build ./... && time go test ./internal/proto/ ./internal/server/ -run 'Welcome' -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - proto.go WelcomePayload 含 `Session string` 字段且 json tag 为 "session"；WelcomeFrame 恰 5 参且第 5 参名为 session
    - 五生产调用点逐点正向 grep 证第 5 实参在场（全部生产 WelcomeFrame 调用点均携 session 实参）：`grep -c 'WelcomeFrame(.*s\.sessionMode)' internal/server/perclient.go internal/server/server.go internal/server/clients.go internal/server/resize.go` 输出须恰为 perclient.go:1（:217 升格组帧）/ server.go:1（:1078 shared attach）/ clients.go:2（:568 afterDrain 补发 + :680 promoteNextLocked 升格）/ resize.go:1（:172 pushSessionDimsLocked）——任一计数短缺即该调用点漏传 session
    - go vet ./... 零输出；go build ./... 成功
    - TestWelcomeFrameSession 与 TestPerClientWelcomeSession 全绿；既有 Welcome 相关测试（proto_test.go/clients_test.go）断言行相对 phase 起点零改动（git diff 白名单=仅调用点加参）
    - main.ts:12-34 注释块 Welcome 载荷描述含 session 键与 proto.go 互指字样
  </acceptance_criteria>
  <done>Welcome 帧两模式恒携 session 键且全部调用点编译/测试通过；协议面 one-way 契约落定，前端消费（Task 3）与 D-07 放行（12-02）可直接依赖该键。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 前端 reset 全链——模式位解析/存储 + WELCOME 分支 terminal.reset() + dist 重建 + phase12-dom.mjs（D-09/D-10/D-13）</name>
  <files>web/src/main.ts, web/dist/index.html, web/uat/phase12-dom.mjs</files>
  <read_first>
    - web/src/main.ts:176-263（模块级 per-connection 状态区与页面级门闩分档注释）与 :496-512（connect() 重置块，IN-01 既有面）与 :639-807（WELCOME 分支全文——:648-664 sessionDims 容错段为模式位解析形态母本，:665-691 mode 分支，:782-793 重连成功点 term.clear() 先例）与 :311-341（sessionDims 四状态 + sendResize）
    - web/uat/phase06-dom.mjs:126-285（loadTerminal 夹具 + SpyWebSocket 全文——构造计数/sentFrames/synthClose 形态逐字可抄）与 :287-350（terminalText 清屏断言通道 D1h 先例）
    - internal/proto/proto.go:100-120（Task 2 落地后的 session 键注释契约）
  </read_first>
  <behavior>
    - 模式位解析（WELCOME 分支内、sessionDims 段之前）：w.session 为 "shared"/"per-client" 之一才赋值 sessionMode；键缺席或值非法 → console.warn 且保持默认 "shared"（D-08 缺键=shared 防御性缺省，sessionDims :655-664 容错同构）
    - reset 判断（sessionDims 段之后、mode 分支之前）：sessionMode==="per-client" → term.reset()；无 if/else 分支（首连屏幕本空，reset no-op 等价，D-09）；零提示文案（D-10）
    - sessionMode 入 connect() 重置块（:498-512）同批清零回 "shared"（IN-01 口径）
    - jsdom D1：per-client 实例 attach → 服务端真实 OUTPUT 写入文本 → synthClose(1006) → 重连全链 → 新 WELCOME 到达后 terminalText 为空（reset ⊇ clear 的 DOM 效应，phase06-dom.mjs:287-288 D1h 同通道）
    - jsdom D3：SpyWebSocket 送达前剥除 session 键（loadTerminal 注入面扩展，Claude's Discretion 落定形态=投递拦截改写）→ 连接/重连后旧文本仍在（不 reset）
  </behavior>
  <action>
    1. main.ts 模块级新增 per-connection 变量 sessionMode（string，默认 "shared"），注释载 D-08 取值域/缺键=shared/IN-01 登记口径；connect() 重置块（:498-512）同批清零。
    2. WELCOME 分支：JSON.parse 成功后、sessionDims 段（:655-664）之前插入模式位解析（合法两值才赋值，否则 warn 保持默认）；sessionDims 段之后、mode 分支（:665）之前插入统一 reset 判断（D-09 落位约束：只能在 Welcome 分支内，onopen 时模式位未到手；与 reconnecting 门闩无耦合——首连也执行）。注释互指 proto.go WelcomePayload.Session 与 D-08/D-09/D-10。
    3. 既有重连成功点（:790-793 if (reconnecting) { stopReconnect(); term.clear(); }）逐字不动——reset 与其独立（per-client 下 reset 先行清 buffer，reconnecting 分支的 clear 幂等无害）。
    4. 新建 web/uat/phase12-dom.mjs：头注释载 PC-06/D-08/D-09/D-10/D-13 与红线（token/凭据/pid 永不进 detail + assertOutputClean() 运行时自净，phase06-dom.mjs:38-41 逐字沿用）；loadTerminal/SpyWebSocket 夹具逐字复用 phase06-dom.mjs:126-285 形态；新增 SpyWebSocket 投递拦截面（剥 session 键注入通道）；D1/D3 两场景（behavior 段形态）；隔离纪律=每场景独立 spawn 实例 + 独立 jsdom。
    5. 构建：cd web && time pnpm build → dist/index.html 重建提交（embed 链机械步骤，CONTEXT Claude's Discretion 已载）。
  </action>
  <verify>
    <automated>cd web && time pnpm build && node --test uat/phase12-dom.mjs 2>/dev/null || node uat/phase12-dom.mjs</automated>
  </verify>
  <acceptance_criteria>
    - main.ts 含 sessionMode 变量声明、connect() 重置块清零行、WELCOME 分支解析与 reset 判断四处锚点（grep 可证）
    - 行为断言：phase12-dom.mjs 运行退出码 0，D1/D3 两场景 check 全过（skipped 行须带 reason）
    - dist/index.html 为本次构建产物且 git 已纳管（embed 链完整：go build ./... 成功即证）
    - shared 行为零漂移：phase06-dom.mjs 零修改重跑全绿（D1 重连后清屏=term.clear() 既有形态，无 reset 介入——shared sessionMode 恒 "shared" 不触发新分支）
  </acceptance_criteria>
  <done>per-client 重连后浏览器旧屏残留被静默清除（jsdom 可证），shared/旧服务端行为逐字节不变；PC-06 全链（协议位→前端 reset）端到端可用。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server→client (Welcome 帧) | session 键为服务端→客户端的可信通道新增键；前端解析为不可信输入处理（值域白名单校验，非法值降级 shared + warn） |
| client→server (无新面) | 本 plan 零新 C→S 输入面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | main.ts WELCOME 分支 session 解析 | low | mitigate | 值域白名单（仅两枚举值赋值，其余 warn 保持 shared）——非法值不得触发 reset（D-16 容错纪律同构 sessionDims 段） |
| T-12-02 | Information Disclosure | WelcomePayload.session | low | accept | 值为公开枚举（与 CLI flag 同词），零敏感面；additive 键不含任何会话/身份信息 |
| T-12-03 | DoS | terminal.reset() 调用点 | low | accept | reset 为本地 UI 操作，频率上界 = Welcome 帧率（每连接一次）；无放大面 |
| T-12-SC | Tampering | pnpm build（dist 重建） | high | mitigate | 零新依赖红线（CONTEXT 已锁定）——pnpm-lock.yaml 零 diff；供应链 legitimacy 门无安装任务故不触发 |
</threat_model>

<verification>
- go build ./... && go vet ./... && time go test -race ./internal/... -count=1（全量绿）
- cd web && time pnpm build && node uat/phase12-dom.mjs 退出码 0
- 零回归：phase06-dom.mjs 零修改重跑全绿（前端 shared 半侧）；既有 Go 测试断言行 diff 白名单审查（仅 WelcomeFrame 调用点加参）
</verification>

<success_criteria>
1. Welcome 帧在 shared/per-client 两模式恒携 session 键且值正确（proto 测试 + per-client e2e 断言）
2. per-client 重连全链 jsdom 可证旧屏清除、缺键不 reset；shared 零漂移（phase06-dom 原样重跑）
3. dist 重建纳管，embed 链完整；D-08 one-way 确认门已過
</success_criteria>

## Artifacts this phase produces

本 plan 新建/变更符号（供 plan-review-convergence 源 grounding 排除清单）：

- `proto.WelcomePayload.Session` 字段（`json:"session"`，恒序列化）
- `proto.WelcomeFrame` 签名第 5 参 `session string`
- `main.ts` 模块级变量 `sessionMode`（per-connection，connect() 重置块清零）
- `main.ts` WELCOME 分支：session 解析段 + `term.reset()` 统一判断调用点
- `web/uat/phase12-dom.mjs`（新文件）：场景 D1（per-client reset 全链）/ D3（缺 session 键不 reset）
- 新测试：`TestWelcomeFrameSession`（proto_test.go）、`TestPerClientWelcomeSession`（perclient_test.go）
- 机械适配（非新符号）：proto_test.go 四处 / clients_test.go 三处 WelcomeFrame 调用点加参

<output>
Create `.planning/phases/12-per-client/12-01-SUMMARY.md` when done
</output>
